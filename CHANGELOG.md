# 변경 이력

이 파일은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따르고,
버전은 [유의적 버전](https://semver.org/lang/ko/)을 따릅니다.

릴리즈 노트는 **여기서 가져갑니다.** `v1.2.0` 태그를 밀면 릴리즈 워크플로가
아래 `## [1.2.0]` 절을 통째로 릴리즈 본문에 넣습니다. 그러니 릴리즈에 적고 싶은 말은
태그를 달기 전에 여기 써두세요 — 태그 메시지나 커밋 목록이 아니라 이 파일이 기준입니다.

> 해당 버전의 절이 없으면 릴리즈가 실패하지는 않고, GitHub이 커밋 목록으로 만든 노트가
> 대신 들어가며 워크플로 로그에 경고가 남습니다.

## [Unreleased]

### 개선

- **C# 리더가 런을 행마다 풀지 않습니다.** 로드 시간을 분해해 보니 거의 전부가 최대
  테이블 하나였고, 그 안에서 디코드가 3.99 ms인데 필드 대입만 남긴 바닥이 1.55 ms였습니다
  — 차이는 **같은 값이 만 번 이어져도 커서를 만 번 부르고** 매번 인코딩을 다시 분기한
  비용이었습니다. 커서에 「다음 값과 그 값이 몇 행 이어지는지」를 한 번에 답하는
  `NextSameI32`·`NextSameString`을 두고, 생성되는 루프가 그 횟수만큼 평범한 대입을
  돌게 했습니다.

  **전체 로드 8 ms → 5 ms** (67개 테이블 · 109,218행), 그 테이블의 디코드는
  3.99 ms → 2.13 ms입니다. `.scb`는 한 바이트도 바뀌지 않았고 — 런 길이 인코딩이 파일에서
  지운 반복을 디코드에서도 지운 것뿐입니다 — 할당량과 상주 메모리도 그대로입니다.
  런이 없는 컬럼은 1을 답하므로 모든 인코딩에서 루프가 그대로 맞습니다.
  ([벤치마크](doc/benchmark.md#읽는-법))

### 추가

- **중첩 필드 — 컬럼 여러 개를 레코드 하나로.** `Pos.X`/`Pos.Y`는 레코드 하나가 되고,
  `Slot1.Id`/`Slot1.Label`/`Slot2.Id`/`Slot2.Label`은 레코드의 배열이 됩니다. 배열 길이는
  `Text1`/`Text2`가 접히는 것과 **같은 연번 규칙**이 정합니다. 멤버마다 타입을 따로 적으므로
  `int`와 `string`을 한 원소에 담을 수 있고, 그것이 배열 대신 레코드를 쓰는 이유입니다.
  `.`은 지금까지 필드 이름에서 **오류였던 문자**라서, 되던 시트의 뜻이 바뀔 여지가 없습니다.
  ([시트 작성](doc/sheets.md#중첩-필드--컬럼-여러-개를-레코드-하나로) · [설계](spec/nested-fields.md))

  **파일 형식은 바뀌지 않았습니다.** 컬럼 지향이라 레코드의 배열이 **멤버마다 고정 배열 컬럼
  하나**입니다 — API는 구조체의 배열이고 파일은 배열의 구조체입니다. 형식 버전도 그대로이고,
  `lib/`의 테이블 리더 13개도 손대지 않았습니다. 멤버마다 별도 컬럼이므로 런렝스·딕셔너리
  인코딩이 멤버 단위로 그대로 걸리고, 멤버 추가는 컬럼 태그가 하나 늘는 가산적 변경입니다.

  **지금 지원하는 타깃은 `csharp` · `json` · `binary` 셋입니다.** 나머지는 전부 — 코드 생성기
  12개와 `html`·데이터베이스·`summary`·`history` 까지 — 레코드를 만나면 **이름을 대며 거부**합니다.
  조용히 다른 모양으로 내보내면 열세 산출물 중 하나만 달라지고 어느 것이 맞는지 알 수 없기
  때문입니다. 하나씩 늘립니다.

- **히스토리에 배포 판정이 붙었습니다.** 커밋마다, 그리고 두 버전 사이 범위 전체에
  대해 **데이터 패치와 코드 배포 중 무엇이 나가야 하는지**를 판정합니다 — 웹 페이지에는
  `DATA`/`CODE` 칩으로, `--format text`에는 `=> ship:` 줄로, JSON에는 `deployment`
  필드로. 상수만 고치고 데이터를 올리는 헛배포, enum 레이블 값이 밀린 채 코드만 나가는
  조용한 오독처럼 **아무것도 실패하지 않는 사고에는 경고가 함께 붙습니다.** 판정은
  저장이 아니라 조회 시점에 계산되므로 이 기능 이전에 기록된 스냅샷에도 붙습니다.
  ([Summary와 히스토리](doc/history.md#배포-판정--이-구간을-내보내려면-무엇이-나가야-하나))

- **`.scb`가 컬럼마다 인코딩을 고릅니다.** 형식 버전이 **102**로 올라가고 디스크립터에
  인코딩 바이트가 하나 붙었습니다. 컬럼마다 RAW·varint·델타·런 길이·사전·사전+런 길이를
  **전부 인코딩해 보고 가장 작은 것**을 고릅니다 — 인코딩 시간은 이 형식이 신경 쓰지
  않는 유일한 자원이고, 실제로 잰 바이트 수는 틀리는 법이 없는 유일한 선택 기준이라서.

  아홉 가지입니다 — 원문, varint, 델타, 런 길이, 델타의 런 길이, 사전, 사전의 런 길이,
  그리고 **접두사로 접은 사전**과 그 런 길이 판. 사전은 문자열만이 아니라 실수·64비트
  정수·시각에도 걸립니다.

  상용 프로젝트 데이터(67개 테이블 · 109,218행)에서 **7.84 MB가 166 KB**가 됩니다.
  JSON의 126분의 1이고, **압축하지 않은 `.scb`가 gzip한 JSON보다 6.6배 작습니다** —
  압축 라이브러리를 13개 언어에 다는 대신 형식이 스스로 한 결과입니다. 가장 큰 테이블
  (`StatGrowth`, 103,395행 × 9컬럼)은 원본 CSV 8.35 MB에서 **4,467 B**가 됩니다.
  전체 로드는 24 ms에서 **8 ms**로, 로드 후 상주 메모리는 27.6 MB에서 **11.9 MB**로
  줄었습니다. 마지막 것은 사전 인코딩이 문자열 객체를 유니크 개수만큼만 만들기
  때문입니다.
  ([벤치마크](doc/benchmark.md) · [컬럼 인코딩](doc/binary-format.md#컬럼-인코딩) ·
  [왜 컬럼 지향인가](spec/scb-column-oriented-rationale.md))

  **읽는 쪽은 아무것도 달라지지 않습니다** — 생성된 코드를 다시 뽑아 데이터와 함께
  배포하기만 하면 됩니다. 모르는 컬럼 건너뛰기, 타입 승격, 태그 기반 식별 등 스키마
  진화의 보장은 전부 그대로입니다. 다만 **형식 버전이 하나뿐**이라는 정책대로 101을
  읽는 경로는 없으므로, 이 버전의 코드와 이전 데이터를 섞지 마세요.

- **익스포터가 자기가 더 이상 쓰지 않는 파일을 지웁니다.** 코드 생성기는 오래전부터
  하던 일인데 익스포터 둘에는 없었습니다. 테이블 이름을 바꾸면 옛 이름의 `.scb`·`.json`이
  출력 폴더에 그대로 남았고, 그건 안 읽히는 파일이 CDN에 올라가는 것에서 끝나지 않습니다
  — 옛 이름을 찾는 빌드가 그걸 **읽습니다**. 아무도 하지 않은 롤백의 옛 값으로요.

  생성된 소스는 머리의 배너로 자기가 우리 것임을 말하지만 데이터 파일은 그럴 수 없어서,
  **매니페스트가 그 자리를 대신합니다.** 이미 익스포터가 그 폴더에 무엇을 넣었는지 적어
  두는 장부이고, 장부에 있는 것만 지웁니다 — 배너는 남이 파일을 복사해 오면 딸려오지만
  장부에 없는 파일은 어떤 경로로도 건드릴 수 없습니다. 다른 타깃과 같이 `Sweep: false`로
  끌 수 있습니다.

### 변경

- **바이너리 데이터 파일의 기본 확장자가 `.table`에서 `.scb`로 바뀌었습니다.**
  SheetMan Compiled Binary의 약자입니다. `.table`은 어느 도구의 파일인지 말해주지 않는
  이름이었고, 실제로 다른 도구들과 겹칩니다.

  **파일 안의 바이트는 달라지지 않았습니다** — 이름만 바뀝니다. 이미 배포한 데이터를
  그대로 쓰려면 recipe에서 확장자를 되돌리면 됩니다. 익스포터와 코드 생성 쪽 **둘 다**
  바꿔야 합니다.

  ```jsonc
  "Export":  { "Binary": [{ "Path": "./out/data", "FileExtension": ".table" }] },
  "CSharp":  [{ "Path": "./out/cs", "BinaryTableFileExtension": ".table" }]
  ```

  되돌리지 않고 올린다면, 생성된 코드를 데이터와 **함께** 배포하세요. 새 코드가 찾는
  이름과 CDN에 올라가 있는 이름이 다르면 테이블 리더가 파일을 찾지 못합니다.

## [0.1.0] - 2026-08-07

첫 배포입니다. 그동안은 소스에서 빌드해야 했고, 이제 [릴리즈](https://github.com/maxidea1024/SheetMan/releases)에서
받아서 바로 쓸 수 있습니다.

### 무엇을 하는 도구인가

엑셀·구글 스프레드시트에 적은 테이블 데이터를 **13개 언어의 타입과 데이터 파일**로 바꿉니다.
틀린 값은 게임이 아니라 변환 단계에서 걸립니다.

- **입력** — 엑셀, 구글 스프레드시트. 여러 파일·시트로 나눠 적어도 하나로 합쳐집니다.
- **내보내기** — 바이너리(자체 형식), JSON, 그리고 MySQL · PostgreSQL · MongoDB · Redis 직접 적재.
- **코드 생성** — C#(.NET·유니티), TypeScript, C++, C, Go, Rust, Python, Java, Kotlin, Ruby,
  PHP, Dart, 언리얼 모듈. 사람이 읽는 HTML 문서도 함께.
- **정적 검증** — 타입, 중복 인덱스, 참조 무결성, 서버/클라이언트 경계. 문제를 한 번에 모아
  보고하고 거기서 멈춥니다.
- **원자적 교체** — 변환이 중간에 실패해도 쓰던 데이터는 그대로입니다. 읽는 쪽도 같습니다.
- **데이터만 패치** — 익스포트 결과를 CDN에 올려두면 생성된 업데이터가 바뀐 파일만 받아갑니다.
  13개 언어 전부.
- **변경 이력** — 누가 언제 무엇을 바꿨는지 셀 단위로 기록하고 브라우저에서 봅니다.

### 배포

|플랫폼|아키텍처|
|--|--|
|Linux|x64 · arm64|
|Windows|x64 · arm64|
|macOS|x64 · arm64 (애플 실리콘)|

**.NET을 설치하지 않아도 됩니다** — 런타임이 실행 파일 안에 들어 있습니다. 여섯 개 모두 각자의
운영체제와 아키텍처에서 빌드하고, 실제로 변환을 한 번 돌려본 뒤에 올립니다.

### 이번에 고친 것

- **대문자 약어가 이름 변환에서 쪼개지던 문제.** `SFXCategoryType`이 파이썬에서
  `enum_s_f_x_category_type.py`가 되고, 시트에 `ATK_Growth`·`Name_KR`로 적은 필드가
  `a_t_k_growth`·`name_k_r`로 나왔습니다. 파일명은 7개 언어, 멤버 이름은 파이썬·러스트가
  영향을 받았습니다.
- **HTML 문서에 생성 마커가 없어 sweep이 지우지 못하던 문제.** 테이블 이름을 바꾸면 옛
  페이지가 출력 폴더에 영원히 남았습니다.
- **템플릿 반복 한도가 사실상 행 수의 상한이었던 문제.** 103,398행짜리 테이블이 걸렸습니다.
- **엑셀 잠금 파일(`~$Book.xlsx`)을 워크북으로 읽으려다 실패하던 문제.** 엑셀에 워크북을
  열어둔 채로 변환할 수 있습니다.
- 언리얼 가이드가 생성 코드와 어긋나 있던 두 곳.

### 이번에 더한 것

- **`rescue` 시트 레이아웃.** 마커 없이 시트 탭 이름이 곧 테이블인, 다른 프로젝트의 기존
  엑셀을 그대로 읽습니다. 레이아웃은 recipe의 소스 항목마다 지정하므로 기존 레이아웃과
  **한 번에 섞어 읽을 수 있습니다.** ([다른 규칙으로 쓰인 시트 읽기](doc/adopting-a-project.md))
- 소스 공통 `IncludeSheets` / `ExcludeSheets`. 기본은 전부 포함이고, 적어놓고 없는 시트는
  조용히 빠지는 대신 오류입니다.

### 알아둘 것

- **생성물에서 OS 계정명을 빼냈습니다.** HTML 문서 꼬리말이 `created at ... by <계정명>`으로
  변환을 돌린 사람의 계정을 박아 넣고 있었습니다. 생성된 페이지는 저장소에 커밋되므로 그대로
  남고, 기여자마다 그 줄이 바뀌었습니다. 이제 생성 일시까지만 적습니다.
- 실행 파일 이름은 `sheetman`입니다(소문자). 세 플랫폼 모두 같습니다.

---

[Unreleased]: https://github.com/maxidea1024/SheetMan/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/maxidea1024/SheetMan/releases/tag/v0.1.0
