# 앞으로 할 것

> [문서 목록으로](../readme.md)

---

## TODO

아직 안 한 것, 그리고 **하지 않기로 한 것**입니다. 후자에는 이유를 적어둡니다 — 이유 없이 비어 있는 항목은 다음 사람이 같은 판단을 다시 해야 합니다.

### 다음에 할 것 — 이 순서로

앞의 것이 뒤의 것을 싸게 만들기 때문에 정해진 순서입니다. 조회 API가 확정되기 전에 폴더를 옮기면 같은 파일을 두 번 건드리게 됩니다.

|#|무엇|어디까지 됐나|
|--|--|--|
|1|**레코드 조회 API 통일** (바로 아래 항목)|C#·TypeScript 완료. **나머지 11개 언어 남음**|
|2|**업데이터를 나머지 언어로** (아래 「데이터 갱신기」)|C#·Unreal·TypeScript·Go·Python 완료. Ruby·Dart·Java·Kotlin·PHP 남음|
|3|**폴더 구조 통일** (아래 「생성 폴더 구조」)|착수 전|

### 아직 안 함

- 개별 셀 단위 참조 (`Table.Field#Index` 형태로 특정 셀을 가리키는 기능)
- 히스토리 **백필**. 기능이 생기기 전 커밋들은 채우지 않습니다 — 그 구간의 변경은 다음 스냅샷에 뭉치고, 뭉쳤다는 사실은 리포트에 표시됩니다.
- **레코드 조회 API 통일.** 지금 언어마다 다르고, **차이가 이름에 그치지 않습니다** — C#과 TypeScript만 인덱싱된 필드마다 조회 함수를 내고, 나머지 11개 언어는 기본 인덱스의 `find` 하나뿐입니다. 시트에서 `*`로 지정한 **보조 인덱스가 11개 언어에서 아예 닿지 않습니다.**

  인덱싱된 필드마다 세 개를, 언어의 표기 관례로:

  |하는 일|이름|없을 때|
  |--|--|--|
  |찾기|`FindByIndex(key)`|널·`None`·`nullptr`·`Option::None`|
  |찾고, 없으면 실패|`GetByIndexOrThrow(key)`|예외. 메시지에 테이블·필드·키|
  |있는지만|`ContainsIndex(key)`|`false`|

  예외가 없는 언어는 이름이 사실대로 바뀝니다: Go는 `GetByIndexOrError`가 `(*T, error)`를, Rust는 `Result`를 돌려주고, C는 던질 것이 없으므로 `Find`와 `Contains`만 냅니다. 지금 이름인 `GetByIndex`가 던진다는 것을 이름으로 알 수 없다는 것이 이 작업의 출발점입니다.

  **공개 API가 바뀝니다.** 소비자가 생기기 전에 하는 편이 싸고, 각 함수가 무엇을 하고 무엇을 돌려주는지는 [언어별 가이드](languages/readme.md)에 함수 단위로 적습니다 — 이름이 왜 긴지에 대한 설명도 거기 있습니다.

  **C#과 TypeScript는 끝났습니다.** `src/templates/csharp-table.sbn`과 `ts-table.sbn`의 인덱싱 블록이 참고할 형태이고, C#은 `csharp-accessor.sbn`의 참조 해석도 새 이름을 부르도록 함께 고쳤습니다. 남은 11개 언어는 리네임이 아니라 **기능 추가**입니다: 그 언어들의 뷰에는 인덱싱된 필드 목록 자체가 없고(지금은 기본 인덱스 맵 하나만 만듭니다), 언어당 (1) 뷰에 인덱스 필드 목록과 키 타입 노출, (2) 읽기에서 인덱스별 맵 생성, (3) 세 함수와 그 문서 주석이 필요합니다.
- **스큐 하네스를 나머지 12개 언어로.** [스키마 진화](binary-format.md)의 교차 읽기 게이트가 지금 C#만 돌립니다. 값 읽기는 적합성 코퍼스가 전 언어를 덮지만, 모르는 컬럼 건너뛰기와 타입 승격을 **실제로** 확인한 것은 C#입니다.
- **TypeScript 업데이터.** 같은 이유는 아닙니다. axios를 import하던 빈 스텁은 지웠고, `fetch` 기반 구현은 C# 쪽 설계(스테이징·해시 검증·백오프 재시도)를 그대로 옮기면 됩니다.
- **데이터 갱신기를 나머지 언어로.** 지금 C#(유니티 포함)·Unreal·TypeScript·Go·Python 다섯입니다. 표준 라이브러리만으로 가능한 나머지: Ruby(net/http), Dart(dart:io), Java·Kotlin(java.net.http), PHP(curl). 설계는 `lib/cs/sheetman/SheetManUpdater.cs`가 기준이고 — 매니페스트 비교, 파일별 MD5 검증, `.staging`을 거쳐 로컬 매니페스트를 마지막에 쓰기, 두 배씩 늘어나는 백오프 재시도(404는 제외) — 검증은 `SheetManUpdaterTests`가 쓰는 `UpdaterTestServer`를 그대로 재사용하면 됩니다.

  **C·C++·Rust는 표준 라이브러리에 HTTP가 없습니다.** Rust는 크레이트 의존을 허용하되 생성되는 `Cargo.toml`과 문서에 명시하기로 했습니다. C·C++는 libcurl을 강제하게 되므로 아직 결정하지 않았습니다.

- **생성 폴더 구조 통일.** 지금 언어마다 다릅니다. 목표는 `sheetman/`(런타임) · `tables/` · `enums/` · `constants/` · `updater/`이고, 언어가 강제하는 예외가 둘 있습니다: **Go는 디렉터리가 곧 패키지**라 테이블을 하위로 내리면 내부 필드를 전부 export해야 하고, **Unreal은 `Public/`·`Private/`**를 요구합니다. Java·Kotlin은 패키지 경로 아래, Rust는 `src/` 아래, Python은 패키지 디렉터리 아래에 같은 이름으로 둡니다.

  런타임 파일에는 `Generated by SheetMan` 마커가 없어서 **sweep이 지우지 못합니다** — 파일 이름을 바꾸면 소비자의 프로젝트에 옛 이름이 영원히 남습니다. TypeScript에서 `lite_binary_reader.ts` → `lite-binary-reader.ts`로 바꿀 때 실제로 겪었습니다. 폴더를 옮기기 전에 이것부터 고쳐야 합니다.

- **컬럼 단위 압축.** 형식의 `flags` 바이트와 wire의 예약 비트가 자리를 비워두고 있고, 컬럼이 연속 블록이라 붙이기 싸지만 아직 아무것도 읽거나 쓰지 않습니다.

### 하지 않기로 함

- **`foreign[]` (가변 개수 참조).** 로우마다 개수가 다른 참조를 해석하려면 생성되는 리더가 로드 후 연결 단계에서 길이가 다른 배열을 다뤄야 하는데, 지원 언어들에 그런 형태가 없습니다. 조용히 해석되지 않는 코드를 뱉는 대신 명시적으로 거부하고, 고정 개수가 필요하면 `SerialField`를 쓰도록 안내합니다.
- **TypeScript 외의 언어에 JSON 읽기.** 바이너리가 런타임 형식이고 JSON은 산출물입니다. 되읽으려면 .NET의 `datetime`·`timespan`·`uuid` 텍스트 형식을 언어마다 손으로 파싱해 바이너리 경로와 같은 값에 도달해야 하는데, 어긋나면 실패하지 않고 값이 달라집니다. 사람이 보는 용도는 Html 타깃이, 변경 추적은 히스토리가 대신합니다. 특정 언어에서 필요해지면 그 언어에만 코퍼스 게이트와 함께 넣습니다. 자세한 이유는 「[왜 바이너리만인가](languages/readme.md#왜-바이너리만인가-typescript만-예외인가)」에 있습니다.

- **타깃을 외부 어셈블리에서 로드하는 플러그인 구조.** 버전 간 안정적인 계약을 유지해야 하는데, 타깃이 전부 이 저장소 안에 있는 도구에는 값이 없습니다. 타깃 등록은 실행 어셈블리 스캔으로 합니다.

### 지난 작업

<details>
<summary>완료된 항목 펼쳐보기</summary>

- 참조 기능 강화 및 필드 참조 구현
- 디테일한 오류처리 / 정적 검증 복구 / 다중 오류 보고
- 엔티티 레이아웃 확장, 상수 테이블 코드 생성
- 구글 시트 정의 위치를 URL로 출력
- 건너뛴 시트와 처리된 시트 확인 수단
- `manifest` 파일 생성
- 키 컬럼 유니크 검사
- 주석 처리된 필드 처리
- `TargetSide` 적용 및 커맨드라인 `--target-side`
- 배열 타입 (기본 구분자 `;`, recipe의 `ArrayDelimiter`로 변경)
- MySQL / PostgreSQL / MongoDB / Redis 적재
- 코드 생성: C# · TypeScript · C++ · C · Go · Rust · Python · Java · Kotlin · Ruby · PHP · Dart · Unreal
- 템플릿 엔진(Scriban) 전면 도입, 직접 만든 printer 제거
- 타깃/소스 등록을 어트리뷰트 스캔으로 전환
- 적합성 코퍼스 — 전 언어 리더를 실제로 컴파일·실행해 익스포터 JSON과 대조
- 데이터 갱신기 (C#·유니티·언리얼) — 매니페스트 비교, 해시 검증, 스테이징 후 원자적 교체, 백오프 재시도
- **UBT 게이트** — 생성된 언리얼 코드를 실제 엔진의 UnrealBuildTool로 빌드·실행. 스텁으로는 답할 수 없는 질문(엔진 API가 실제로 그런가)에 답합니다
- 바이너리 형식 개편 — 컬럼 지향·태그로 스키마 진화 ([문서](binary-format.md)), 리더 13개 재작성, 스큐 코퍼스(C#), 형식 바이트 고정, 배포 전 베이스라인 검사
- Summary 문서 및 변경 히스토리 (MySQL, 조회 CLI, 웹서버)

</details>
