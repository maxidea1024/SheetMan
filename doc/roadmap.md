# 앞으로 할 것

> [문서 목록으로](../readme.md)

---

## TODO

아직 안 한 것, 그리고 **하지 않기로 한 것**입니다. 후자에는 이유를 적어둡니다 — 이유 없이 비어 있는 항목은 다음 사람이 같은 판단을 다시 해야 합니다.

### 다음에 할 것

**`uwo` 레이아웃 지원.** 순서에 이유가 있습니다 — 앞의 것이 없으면 뒤의 것을 확인할 방법이 없습니다.
규격과 근거는 [uwo 레이아웃 분석](uwo-레이아웃-분석-20260808.md)에 있고, 워크북 사본은 [samples/uwo/](../samples/uwo/)에 있습니다.

> **처음 잡은 순서를 바꿨습니다.** 「나머지 언어 → uwo 파서」로 뒀는데, 그건 *중첩 필드를 완성하는*
> 순서였습니다. 목표가 uwo 검증이라면 필요한 타깃은 `json`·`binary`·`csharp` 셋이고 그건 이미 됩니다 —
> Go·Rust·Java 등 11개는 uwo 검증에 하나도 필요하지 않습니다. 그래서 파서를 먼저 했습니다.

|순서|무엇|상태|
|--|--|--|
|1|**중첩 필드** — `SerialField`의 원소 타입을 스칼라에서 레코드로. [설계와 진행](../spec/nested-fields.md#7-단계--언어를-하나씩)|`csharp`·`typescript`·`json`·`binary` **됨**. 나머지 11개 언어 남음 (uwo 검증에는 불필요)|
|2|**`uwo` 레이아웃 파서** — 워크북 정의된 이름을 테이블 경계로|**됨.** `UWO_상회.xlsx` 7개 테이블 변환, 4개가 uwo 산출물과 전부 일치 ([§9](uwo-레이아웃-분석-20260808.md#9-첫-변환--uwo_상회xlsx-7개-테이블))|
|3|**행 수 차이 원인** — `Guild` 10 vs 11 등. 정의된 이름의 마지막 행까지 NPOI가 안 읽는 것으로 의심|**다음**|
|4|**가변 길이 레코드 배열** — uwo의 배열은 마지막 비-`-` 원소에서 잘립니다. 우리는 컬럼 수만큼 고정|다음. 대비에서 남은 차이의 대부분|
|5|타입 매핑 나머지 — `strkey` · `name[0]` 스칼라 배열 · 타일 테이블 · `text`의 textset|—|
|6|**컬럼 제약을 정적 검증으로** — 필수값 · 최소/최대 · 값 화이트리스트 · 에셋 존재|uwo는 이것을 **익스포트가 끝난 JSON에 Lua로** 검사합니다. 변환 단계로 당겨오는 것이 도구를 바꾸는 실질적인 이득입니다|
|7|다중 대상 참조(`:links`에 여러 테이블)와 `_BC` 지역 변종의 표현 결정|결정 사항이 있어 설계를 먼저 적어야 합니다|
|8|큰 워크북의 메모리 — NPOI 이벤트 모델로 읽기 경로 교체|61 MiB 워크북이 **2.2 GB**입니다. 라이브러리 교체가 아니라 같은 패키지의 스트리밍 API. 큰 워크북을 대상에 넣을 때가 기준입니다|

### 아직 안 함

- 개별 셀 단위 참조 (`Table.Field#Index` 형태로 특정 셀을 가리키는 기능)
- 히스토리 **백필**. 기능이 생기기 전 커밋들은 채우지 않습니다 — 그 구간의 변경은 다음 스냅샷에 뭉치고, 뭉쳤다는 사실은 리포트에 표시됩니다.
- **스큐 하네스를 나머지 12개 언어로.** [스키마 진화](binary-format.md)의 교차 읽기 게이트가 지금 C#만 돌립니다. 값 읽기는 적합성 코퍼스가 전 언어를 덮지만, 모르는 컬럼 건너뛰기와 타입 승격을 **실제로** 확인한 것은 C#입니다.
- **`.scb` 대칭키 암호화.** 봉투 배치(ChaCha20, 내용 해시에서 뽑는 nonce, 복호 확인용 magic)는 [스펙](../spec/scb-v102-column-encoding.md)에 확정되어 있고 `flags` bit0이 자리를 잡고 있습니다. 남은 것은 변환기의 키 공급과 13개 언어의 구현입니다.
- **범용 압축 레이어(zstd 등).** `flags` bit1이 비어 있습니다. 다만 [컬럼 인코딩](binary-format.md#컬럼-인코딩)만으로 gzip한 JSON보다 작아진 뒤라, 13개 언어에 압축 의존성을 다는 값이 예전만 못합니다.

### 하지 않기로 함

- **`foreign[]` (가변 개수 참조).** 로우마다 개수가 다른 참조를 해석하려면 생성되는 테이블 리더가 로드 후 연결 단계에서 길이가 다른 배열을 다뤄야 하는데, 지원 언어들에 그런 형태가 없습니다. 조용히 해석되지 않는 코드를 뱉는 대신 명시적으로 거부하고, 고정 개수가 필요하면 `SerialField`를 쓰도록 안내합니다.
- **TypeScript 외의 언어에 JSON 읽기.** 바이너리가 런타임 형식이고 JSON은 산출물입니다. 되읽으려면 .NET의 `datetime`·`timespan`·`uuid` 텍스트 형식을 언어마다 손으로 파싱해 바이너리 경로와 같은 값에 도달해야 하는데, 어긋나면 실패하지 않고 값이 달라집니다. 사람이 보는 용도는 Html 타깃이, 변경 추적은 히스토리가 대신합니다. 특정 언어에서 필요해지면 그 언어에만 코퍼스 게이트와 함께 넣습니다. 자세한 이유는 「[왜 바이너리만인가](languages/readme.md#왜-바이너리만인가-typescript만-예외인가)」에 있습니다.

- **타깃을 외부 어셈블리에서 로드하는 플러그인 구조.** 버전 간 안정적인 계약을 유지해야 하는데, 타깃이 전부 이 저장소 안에 있는 도구에는 값이 없습니다. 타깃 등록은 실행 어셈블리 스캔으로 합니다.

### 지난 작업

<details>
<summary>완료된 항목 펼쳐보기</summary>

- **시트 레이아웃 파서 레이어와 `rescue` 레이아웃.** 임포터는 셀 격자만 만들고 그 아래는 `Model`만 보므로, 레이아웃은 정확히 그 사이 한 단계입니다. 기존 마커 파싱을 `SheetManLayoutParser`로 옮기고(골든 무변경), 값·타입·이름 규칙은 `CookingContext`로 빼서 **두 파서가 "이 값이 무엇인가"에 대해 다른 답을 낼 수 없게** 했습니다. `rescue`는 마커 없이 시트 탭 = 테이블인 기존 프로젝트 엑셀을 그대로 읽습니다. 레이아웃은 소스 항목마다 지정하므로 **한 번에 섞어 읽을 수 있고**, enum 선언은 레이아웃을 가로질러 해석됩니다. 소스 공통으로 `IncludeSheets`/`ExcludeSheets`도 붙였습니다(기본은 전부 포함, 적었는데 없는 시트는 오류). 실제 프로젝트 워크북 67개 테이블·37개 enum·103,395행을 변환해 생성된 C#을 컴파일하고 바이너리를 되읽어 확인했습니다

- **템플릿 반복 한도가 데이터 크기의 상한이었던 문제.** `LoopLimit`이 100,000이었는데 행을 도는 템플릿은 행마다 1회이므로, 실제로는 "프로젝트가 가질 수 있는 행 수"의 상한이었습니다. 103,398행짜리 테이블이 여기 걸렸고, 나온 것은 **줄 번호가 찍힌 템플릿 오류** — 이 저장소의 버그처럼 읽히지만 아닙니다. 템플릿 작성자가 통제하는 루프는 없으므로, 무한 루프만 막는 값으로 올렸습니다

- 참조 기능 강화 및 필드 참조 구현
- 디테일한 오류처리 / 정적 검증 복구 / 다중 오류 보고
- 엔티티 레이아웃 확장, 상수 테이블 코드 생성
- 구글 시트 정의 위치를 URL로 출력
- 건너뛴 시트와 처리된 시트 확인 수단
- `manifest` 파일 생성
- 키 컬럼 유니크 검사
- 주석 처리된 필드 처리
- `TargetSide` 적용 및 커맨드라인 `--target-side`
- 배열 타입 (기본 구분자 `;`, recipe의 `ArrayDelimiter`로 변경)
- MySQL / PostgreSQL / MongoDB / Redis 적재
- 코드 생성: C# · TypeScript · C++ · C · Go · Rust · Python · Java · Kotlin · Ruby · PHP · Dart · Unreal
- 템플릿 엔진(Scriban) 전면 도입, 직접 만든 printer 제거
- 타깃/소스 등록을 어트리뷰트 스캔으로 전환
- 적합성 코퍼스 — 전 언어 테이블 리더를 실제로 컴파일·실행해 익스포터 JSON과 대조
- 데이터 갱신기 (C#·유니티·언리얼) — 매니페스트 비교, 해시 검증, 스테이징 후 원자적 교체, 백오프 재시도
- **UBT 게이트** — 생성된 언리얼 코드를 실제 엔진의 UnrealBuildTool로 빌드·실행. 스텁으로는 답할 수 없는 질문(엔진 API가 실제로 그런가)에 답합니다
- 바이너리 형식 개편 — 컬럼 지향·태그로 스키마 진화 ([문서](binary-format.md)), 테이블 리더 13개 재작성, 스큐 코퍼스(C#), 형식 바이트 고정, 배포 전 베이스라인 검사
- Summary 문서 및 변경 히스토리 (MySQL, 조회 CLI, 웹서버)
- **생성 폴더 구조 통일.** 런타임(테이블 리더 + 업데이터)은 **13개 언어 전부** `sheetman/` 아래에 있습니다 — C#·Python·Rust가 마지막이었고, Rust는 `src/sheetman/mod.rs`로 묶어 `sheetman::updater`가 되게 했습니다. 타입 파일은 `tables/`·`enums/`·`constants/`로 나눕니다 — C#·TypeScript·Kotlin·Ruby·PHP·Dart에 이어 **C와 C++**까지. C·C++는 `#include` 줄이 파일 경로와 **같은 헬퍼에서** 나오므로 둘이 어긋날 수 없고, 이름의 접근자 접두어는 남겨두었습니다 — 두 생성물을 한 include 경로에 두었을 때 부딪히지 않게 하는 것은 폴더가 아니라 그 접두어입니다.

  **평평한 채로 둔 네 언어는 이유가 하나입니다 — 그 언어에선 디렉터리가 곧 패키지/모듈입니다.** Go는 하위로 내리면 내부 필드를 전부 export해야 하고, Python은 `tables/` 패키지가 접근자 `tables.py`를 가려버리며, Rust는 접근자 모듈이 이미 `tables`라 부딪히고, Java는 모든 생성 타입의 임포트 경로가 바뀝니다. Kotlin만 JVM 중에서 예외인 것은 패키지와 경로를 따로 둔 언어이기 때문입니다. Unreal은 엔진이 `Public/`·`Private/`를 요구합니다.

  선행 조건이었던 **마커 문제도 함께 고쳤습니다.** 런타임 파일은 `lib/`에서 그대로 복사되느라 `Generated by SheetMan` 헤더가 없었고, 그래서 sweep이 지우지 못해 **이름이나 위치를 바꾸면 옆 이름이 소비자 프로젝트에 영원히 남았습니다.** 이제 배너를 쓸 때 붙입니다 — `lib/`의 원본은 생성물이 아니므로 거기에 적지 않았고, PHP의 `<?php`와 Ruby의 `frozen_string_literal`만 그 뒤에 넣습니다. 이번에 옮긴 파일들이 실제로 스윕되는 것으로 확인됐습니다
- **TypeScript 참조 연결.** `solveCrossReferences`가 **빈 메서드**로 생성되고 있었습니다 — 레코드마다 있는 `setReference_*_INTERNAL`을 부르는 곳이 어디에도 없어서, 바이너리에서 읽으면 참조가 `undefined`이고 JSON에서 읽으면 **레코드 타입으로 선언된 멤버에 숫자 키가** 들어갔습니다. 왕복 테스트가 테이블을 하나씩 읽고(그 경우 연결되지 않는 것이 정상) 원시 키만 비교했기 때문에 잡히지 않았습니다. 연결 단계를 구현했고, 접근자에 `readAllBinarySync`를 더해 바이너리도 전부 읽고 연결할 수 있게 했으며, 왕복 테스트가 이제 두 경로의 **해석된 참조**를 비교합니다
- **데이터 갱신기 — 13개 언어 전부.** C# 설계 그대로이고, 열 개 언어는 표준 라이브러리만 씁니다. 언어마다 없는 것을 그 파일 안에서 채웠습니다: MD5가 없는 Dart·Rust·C·C++에는 MD5를, JSON 파서가 없는 Java·Kotlin·Rust·C·C++에는 매니페스트용 파서를 함께 넣었습니다. PHP는 `ext-curl`로 쓰다가 게이트를 돌린 인터프리터가 `curl_init()`을 모른다고 답해 **코어 HTTP 스트림 래퍼로 바꿨습니다.** **외부 의존은 세 언어뿐이고 모두 명시되어 있습니다** — Rust는 `ureq` 하나를 생성되는 `Cargo.toml`에, C·C++는 libcurl 하나를 링크 플래그로. 켜지 않으면 세 언어의 생성물도 여전히 자립적입니다. 언어당 `<Lang>UpdaterTests`가 실제 툴체인으로 실제 HTTP 서버에 붙어 재시도·해시 거부·404 비재시도를 확인하고, 직접 쓴 MD5는 공개 벡터로 따로 확인합니다
- **레코드 조회 API 통일 — 13개 언어 전부.** 인덱싱된 필드마다 `FindBy<Field>` · `GetBy<Field>OrThrow` · `Contains<Field>`를 언어 표기 관례로 냅니다. 이전에는 C#과 TypeScript만 그랬고 나머지 11개는 기본 인덱스의 `find` 하나뿐이라, 시트에서 `*`로 지정한 **보조 인덱스가 11개 언어에서 아예 닿지 않았습니다.** 이름이 바뀐 것이 아니라 기능이 생긴 것입니다 — 언어마다 인덱스별 맵(C는 정렬 배열)을 만들고, 키 타입을 그 컬럼의 타입에서 가져옵니다. 던질 것이 없는 C와 Unreal은 두 함수만 냅니다. 참조 해석도 대상 테이블의 **기본 인덱스 함수 이름을 읽어서** 부르므로, 첫 컬럼 이름이 `index`가 아닌 시트에서도 맞습니다. 자세한 것은 [언어별 가이드](languages/readme.md#레코드-조회--같은-세-함수-13개-언어에서-같은-이름)에 있습니다

</details>
