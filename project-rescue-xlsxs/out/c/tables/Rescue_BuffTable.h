/* ------------------------------------------------------------------------------
 * Generated by SheetMan. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef RESCUE_BUFF_TABLE_H
#define RESCUE_BUFF_TABLE_H

#include "sheetman/sheetman_lite_binary_reader.h"

#include "Rescue_Forward.h"
#include "enums/Rescue_EnumSkillType.h"
#include "enums/Rescue_EnumStatType.h"
#include "enums/Rescue_EnumAttributeType.h"
#include "enums/Rescue_EnumTargetType.h"
#include "enums/Rescue_EnumBuffConditionType.h"

#ifdef __cplusplus
extern "C" {
#endif
/* Generated from project-rescue-xlsxs\MasterData.xlsx : BuffTable : A1
 */
struct Rescue_BuffTableRecord_t {
  /* ID */
  int32_t id;
  /* SkillTable에서 Name을 불러와서 사용 */
  const char* buff_name;
  /* 한글 명 */
  const char* name;
  /* 버프 타입_메모확인 */
  Rescue_SkillType_t skill_type;
  /* 어떤 종류의 버프 인지 */
  Rescue_StatType_t stat_type;
  /* 버프 우선 적용 순위 */
  int32_t priority;
  /* 버프 속성 */
  Rescue_AttributeType_t attribute_type;
  /* 타겟 설정 */
  Rescue_TargetType_t target_type;
  /* 상태에 따른 발동 조건 */
  Rescue_BuffConditionType_t buff_condition_type;
  /* 증가되는 값(상수) */
  int32_t buff_value;
  /* 증가되는 값(%) */
  float buff_value_rate;
  /* 버프 시간 */
  int32_t buff_time;
  /* 버프 틱 시간 */
  int32_t buff_tick_time;
  /* 버프 최대 스택 */
  int32_t buff_max_stack;
  /* 버프 쿨타임 1초 = 1000 */
  float buff_cool_down_time;
  /* 버프 표시 유무 FALSE/TRUE */
  bool is_visible;
  /* 버프 아이콘 경로 */
  const char* icon_path;
  /* 버프 튤팁 */
  const char* description;
};

/* Every row of BuffTable.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct Rescue_BuffTableTable_t {
  Rescue_BuffTableRecord_t* records;
  int32_t count;

  sm_index_entry* by_id;
  sm_arena arena;
} Rescue_BuffTableTable_t;

/* Loads the table from a .table file written by SheetMan.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool Rescue_BuffTableLoad(Rescue_BuffTableTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void Rescue_BuffTableFree(Rescue_BuffTableTable_t* table);

/* The row with the given Id, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const Rescue_BuffTableRecord_t* Rescue_BuffTableFindById(
  const Rescue_BuffTableTable_t* table, int32_t key);

/* Whether the table holds a row with this Id. */
bool Rescue_BuffTableContainsId(
  const Rescue_BuffTableTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* RESCUE_BUFF_TABLE_H */
