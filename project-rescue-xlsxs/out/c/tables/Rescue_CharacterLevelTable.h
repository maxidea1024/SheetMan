/* ------------------------------------------------------------------------------
 * Generated by SheetMan. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef RESCUE_CHARACTER_LEVEL_TABLE_H
#define RESCUE_CHARACTER_LEVEL_TABLE_H

#include "sheetman/sheetman_lite_binary_reader.h"

#include "Rescue_Forward.h"

#ifdef __cplusplus
extern "C" {
#endif
/* Generated from project-rescue-xlsxs/MasterData.xlsx : CharacterLevelTable : A1
 */
struct Rescue_CharacterLevelTableRecord_t {
  /* Index */
  int32_t id;
  /* 영문 */
  const char* name;
  /* 한글이름 */
  const char* name_kr;
  /* 레벨 */
  int32_t level;
  /* 필요 경험치 */
  int64_t character_exp;
  /* 누적 경험치 */
  int64_t accumulated_exp;
  /* 공격력 고정 비율 */
  float atk_growth;
  /* 방어력 고정 비율 */
  float def_growth;
  /* 체력 증가 비율 */
  float hp_growth;
};

/* Every row of CharacterLevelTable.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct Rescue_CharacterLevelTableTable_t {
  Rescue_CharacterLevelTableRecord_t* records;
  int32_t count;

  sm_index_entry* by_id;
  sm_arena arena;
} Rescue_CharacterLevelTableTable_t;

/* Loads the table from a .table file written by SheetMan.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool Rescue_CharacterLevelTableLoad(Rescue_CharacterLevelTableTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void Rescue_CharacterLevelTableFree(Rescue_CharacterLevelTableTable_t* table);

/* The row with the given Id, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const Rescue_CharacterLevelTableRecord_t* Rescue_CharacterLevelTableFindById(
  const Rescue_CharacterLevelTableTable_t* table, int32_t key);

/* Whether the table holds a row with this Id. */
bool Rescue_CharacterLevelTableContainsId(
  const Rescue_CharacterLevelTableTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* RESCUE_CHARACTER_LEVEL_TABLE_H */
