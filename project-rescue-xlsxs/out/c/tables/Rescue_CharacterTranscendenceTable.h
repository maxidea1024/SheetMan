/* ------------------------------------------------------------------------------
 * Generated by SheetMan. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef RESCUE_CHARACTER_TRANSCENDENCE_TABLE_H
#define RESCUE_CHARACTER_TRANSCENDENCE_TABLE_H

#include "sheetman/sheetman_lite_binary_reader.h"

#include "Rescue_Forward.h"
#include "enums/Rescue_EnumGradeType.h"
#include "enums/Rescue_EnumCurrencyType.h"

#ifdef __cplusplus
extern "C" {
#endif
/* Generated from project-rescue-xlsxs/MasterData.xlsx : CharacterTranscendenceTable : A1
 */
struct Rescue_CharacterTranscendenceTableRecord_t {
  /* Transcend_ID */
  int32_t id;
  /* 영문 */
  const char* name;
  /* 한글이름 */
  const char* name_kr;
  /* 등급 */
  Rescue_GradeType_t grade_type;
  /* 초월 단계 */
  int32_t transcend_step;
  /* 초월 최대 레벨 */
  int32_t max_level;
  /* 초월 특수 재료 타입 */
  Rescue_CurrencyType_t material_type;
  /* 초월 특수 재료 개수 */
  int32_t material_count;
  /* 초월시 스탯 증가율 */
  float power_multiplier;
  /* 다음 성급 Index */
  int32_t next_step_id;
};

/* Every row of CharacterTranscendenceTable.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct Rescue_CharacterTranscendenceTableTable_t {
  Rescue_CharacterTranscendenceTableRecord_t* records;
  int32_t count;

  sm_index_entry* by_id;
  sm_arena arena;
} Rescue_CharacterTranscendenceTableTable_t;

/* Loads the table from a .table file written by SheetMan.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool Rescue_CharacterTranscendenceTableLoad(Rescue_CharacterTranscendenceTableTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void Rescue_CharacterTranscendenceTableFree(Rescue_CharacterTranscendenceTableTable_t* table);

/* The row with the given Id, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const Rescue_CharacterTranscendenceTableRecord_t* Rescue_CharacterTranscendenceTableFindById(
  const Rescue_CharacterTranscendenceTableTable_t* table, int32_t key);

/* Whether the table holds a row with this Id. */
bool Rescue_CharacterTranscendenceTableContainsId(
  const Rescue_CharacterTranscendenceTableTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* RESCUE_CHARACTER_TRANSCENDENCE_TABLE_H */
