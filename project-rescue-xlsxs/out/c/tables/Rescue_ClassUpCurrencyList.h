/* ------------------------------------------------------------------------------
 * Generated by SheetMan. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef RESCUE_CLASS_UP_CURRENCY_LIST_H
#define RESCUE_CLASS_UP_CURRENCY_LIST_H

#include "sheetman/sheetman_lite_binary_reader.h"

#include "Rescue_Forward.h"
#include "enums/Rescue_EnumCurrencyType.h"

#ifdef __cplusplus
extern "C" {
#endif
/* Generated from project-rescue-xlsxs/ItemData.xlsx : ClassUpCurrencyListTable : A1
 */
struct Rescue_ClassUpCurrencyListRecord_t {
  /* Currency_Table */
  int32_t id;
  /* 한글 */
  const char* name;
  /* 재화 타입 */
  Rescue_CurrencyType_t type;
  /* 타겟 ID */
  int32_t target_id;
  /* 최대 획득 */
  int32_t max_count;
  /* 최대 소유 값 */
  int64_t max_stack;
  /* 아이콘 경로 */
  const char* icon_path;
  /* 바로가기 */
  int32_t* short_cut_index;
  int32_t short_cut_index_count;
};

/* Every row of ClassUpCurrencyList.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct Rescue_ClassUpCurrencyListTable_t {
  Rescue_ClassUpCurrencyListRecord_t* records;
  int32_t count;

  sm_index_entry* by_id;
  sm_arena arena;
} Rescue_ClassUpCurrencyListTable_t;

/* Loads the table from a .scb file written by SheetMan.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool Rescue_ClassUpCurrencyListLoad(Rescue_ClassUpCurrencyListTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void Rescue_ClassUpCurrencyListFree(Rescue_ClassUpCurrencyListTable_t* table);

/* The row with the given Id, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const Rescue_ClassUpCurrencyListRecord_t* Rescue_ClassUpCurrencyListFindById(
  const Rescue_ClassUpCurrencyListTable_t* table, int32_t key);

/* Whether the table holds a row with this Id. */
bool Rescue_ClassUpCurrencyListContainsId(
  const Rescue_ClassUpCurrencyListTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* RESCUE_CLASS_UP_CURRENCY_LIST_H */
