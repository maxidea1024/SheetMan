/* ------------------------------------------------------------------------------
 * Generated by SheetMan. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef RESCUE_COLLECTION_TABLE_H
#define RESCUE_COLLECTION_TABLE_H

#include "sheetman/sheetman_lite_binary_reader.h"

#include "Rescue_Forward.h"
#include "enums/Rescue_EnumCollectionTabType.h"
#include "enums/Rescue_EnumCurrencyType.h"
#include "enums/Rescue_EnumStatType.h"

#ifdef __cplusplus
extern "C" {
#endif
/* Generated from project-rescue-xlsxs/CollectionData.xlsx : CollectionTable : A1
 */
struct Rescue_CollectionTableRecord_t {
  /* CollectionID */
  int32_t id;
  /* 영문 */
  const char* name;
  /* 캐릭터ID */
  int32_t character_id;
  /* 보상 UI분기 타입 */
  Rescue_CollectionTabType_t tab_type;
  /* 조건 값 */
  int32_t condition_id;
  /* 재화 보상 타입 */
  Rescue_CurrencyType_t reward_type;
  /* 재화 보상 값 */
  int32_t reward_value;
  /* 스탯 보상 타입 */
  Rescue_StatType_t reward_stat_type1;
  /* 스탯 보상(%) */
  float reward_stat_rate;
  /* 스탯 보상 타입 */
  Rescue_StatType_t reward_stat_type2;
  /* 스탯 보상(+) */
  int32_t reward_stat_value;
  /* 바로가기 ID */
  int32_t short_cut_id;
};

/* Every row of CollectionTable.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct Rescue_CollectionTableTable_t {
  Rescue_CollectionTableRecord_t* records;
  int32_t count;

  sm_index_entry* by_id;
  sm_arena arena;
} Rescue_CollectionTableTable_t;

/* Loads the table from a .table file written by SheetMan.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool Rescue_CollectionTableLoad(Rescue_CollectionTableTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void Rescue_CollectionTableFree(Rescue_CollectionTableTable_t* table);

/* The row with the given Id, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const Rescue_CollectionTableRecord_t* Rescue_CollectionTableFindById(
  const Rescue_CollectionTableTable_t* table, int32_t key);

/* Whether the table holds a row with this Id. */
bool Rescue_CollectionTableContainsId(
  const Rescue_CollectionTableTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* RESCUE_COLLECTION_TABLE_H */
