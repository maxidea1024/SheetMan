/* ------------------------------------------------------------------------------
 * Generated by SheetMan. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef RESCUE_CONFIG_TABLE_H
#define RESCUE_CONFIG_TABLE_H

#include "sheetman/sheetman_lite_binary_reader.h"

#include "Rescue_Forward.h"

#ifdef __cplusplus
extern "C" {
#endif
/* Generated from project-rescue-xlsxs/MasterData.xlsx : ConfigTable : A1
 */
struct Rescue_ConfigTableRecord_t {
  /* Row number, added by SheetMan because `Id` cannot be a primary index. */
  int32_t index;
  /* ConfigTable */
  const char* id;
  /* 카테고리 */
  const char* category;
  /* 테이터타입 */
  const char* data_type;
  /* 기본값 */
  const char* default_value;
  /* 설명 */
  const char* description;
};

/* Every row of ConfigTable.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct Rescue_ConfigTableTable_t {
  Rescue_ConfigTableRecord_t* records;
  int32_t count;

  sm_index_entry* by_index;
  sm_string_index_entry* by_id;
  sm_arena arena;
} Rescue_ConfigTableTable_t;

/* Loads the table from a .table file written by SheetMan.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool Rescue_ConfigTableLoad(Rescue_ConfigTableTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void Rescue_ConfigTableFree(Rescue_ConfigTableTable_t* table);

/* The row with the given Index, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const Rescue_ConfigTableRecord_t* Rescue_ConfigTableFindByIndex(
  const Rescue_ConfigTableTable_t* table, int32_t key);

/* Whether the table holds a row with this Index. */
bool Rescue_ConfigTableContainsIndex(
  const Rescue_ConfigTableTable_t* table, int32_t key);
/* The row with the given Id, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const Rescue_ConfigTableRecord_t* Rescue_ConfigTableFindById(
  const Rescue_ConfigTableTable_t* table, const char* key);

/* Whether the table holds a row with this Id. */
bool Rescue_ConfigTableContainsId(
  const Rescue_ConfigTableTable_t* table, const char* key);

#ifdef __cplusplus
}
#endif

#endif /* RESCUE_CONFIG_TABLE_H */
