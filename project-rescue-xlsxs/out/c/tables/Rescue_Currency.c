/* ------------------------------------------------------------------------------
 * Generated by SheetMan. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#include "tables/Rescue_Currency.h"

#include <string.h>
static bool Rescue_CurrencyParse(Rescue_CurrencyTable_t* table, sm_reader* reader) {
  int32_t row;
  int32_t at;
  sm_column* columns = NULL;
  int32_t column_count = 0;
  sm_cursor cursor;

  if (!sm_read_table_header(reader, &table->count, &columns, &column_count))
    return false;

  if (table->count > 0) {
    table->records = (Rescue_CurrencyRecord_t*)sm_arena_alloc(
      &table->arena, (size_t)table->count * sizeof *table->records);

    if (table->records == NULL)
      return sm_fail_with(reader, "out of memory allocating the rows");

    table->by_id = (sm_index_entry*)sm_arena_alloc(
      &table->arena, (size_t)table->count * sizeof *table->by_id);

    if (table->by_id == NULL)
      return sm_fail_with(reader, "out of memory allocating the Id index");

  }


  /* The arena hands back zeroed memory, so every member starts at its zero - which for
   * a string is NULL, and NULL is what reaches printf when a column the file does not
   * carry is read. An empty string instead: a literal, so there is nothing to free and
   * nothing to allocate. */
  for (row = 0; row < table->count; ++row) {
    Rescue_CurrencyRecord_t* record = &table->records[row];

    record->name = "";

    record->item_name = "";

    record->icon_path = "";

    record->drop_prefab_path = "";

    record->description = "";
  }

  /* Column by column, matched by tag rather than by position: a column this build has
   * no member for is skipped by its declared length, and one whose type no longer fits
   * the member stops the read naming the field. Rows arrive with every member at an
   * empty value, so one the file has nothing for keeps it. */
  for (at = 0; at < column_count && !sm_failed(reader); ++at) {
    const sm_column* column = &columns[at];
    int32_t block_end = reader->position + column->byte_length;

    switch (column->tag) {

    case 1:
      (void)sm_check_column(reader, column, "Currency.Id", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "Currency.Id");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_CurrencyRecord_t* record = &table->records[row];

        (void)sm_cursor_next_i32(&cursor, &record->id);
      }

      break;

    case 2:
      (void)sm_check_column(reader, column, "Currency.Name", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_STRING));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "Currency.Name");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_CurrencyRecord_t* record = &table->records[row];

        (void)sm_cursor_next_string(&cursor, &record->name);
      }

      break;

    case 3:
      (void)sm_check_column(reader, column, "Currency.ItemName", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_STRING));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "Currency.ItemName");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_CurrencyRecord_t* record = &table->records[row];

        (void)sm_cursor_next_string(&cursor, &record->item_name);
      }

      break;

    case 4:
      (void)sm_check_column(reader, column, "Currency.ItemType", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_VARINT));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "Currency.ItemType");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_CurrencyRecord_t* record = &table->records[row];
        int32_t scratch = 0;

        (void)sm_cursor_next_i32(&cursor, &scratch);
        record->item_type = (Rescue_ItemType_t)scratch;
      }

      break;

    case 5:
      (void)sm_check_column(reader, column, "Currency.Type", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_VARINT));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "Currency.Type");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_CurrencyRecord_t* record = &table->records[row];
        int32_t scratch = 0;

        (void)sm_cursor_next_i32(&cursor, &scratch);
        record->type = (Rescue_CurrencyType_t)scratch;
      }

      break;

    case 6:
      (void)sm_check_column(reader, column, "Currency.CycleType", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_VARINT));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "Currency.CycleType");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_CurrencyRecord_t* record = &table->records[row];
        int32_t scratch = 0;

        (void)sm_cursor_next_i32(&cursor, &scratch);
        record->cycle_type = (Rescue_CycleType_t)scratch;
      }

      break;

    case 7:
      (void)sm_check_column(reader, column, "Currency.Stackable", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_BOOL));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "Currency.Stackable");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_CurrencyRecord_t* record = &table->records[row];

        (void)sm_cursor_next_bool(&cursor, &record->stackable);
      }

      break;

    case 8:
      (void)sm_check_column(reader, column, "Currency.MaxStack", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I64) | SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "Currency.MaxStack");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_CurrencyRecord_t* record = &table->records[row];

        (void)sm_cursor_next_i64(&cursor, &record->max_stack);
      }

      break;

    case 9:
      (void)sm_check_column(reader, column, "Currency.Cooltime", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "Currency.Cooltime");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_CurrencyRecord_t* record = &table->records[row];

        (void)sm_cursor_next_i32(&cursor, &record->cooltime);
      }

      break;

    case 10:
      (void)sm_check_column(reader, column, "Currency.Duration", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "Currency.Duration");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_CurrencyRecord_t* record = &table->records[row];

        (void)sm_cursor_next_i32(&cursor, &record->duration);
      }

      break;

    case 11:
      (void)sm_check_column(reader, column, "Currency.IconPath", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_STRING));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "Currency.IconPath");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_CurrencyRecord_t* record = &table->records[row];

        (void)sm_cursor_next_string(&cursor, &record->icon_path);
      }

      break;

    case 12:
      (void)sm_check_column(reader, column, "Currency.DropPrefabPath", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_STRING));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "Currency.DropPrefabPath");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_CurrencyRecord_t* record = &table->records[row];

        (void)sm_cursor_next_string(&cursor, &record->drop_prefab_path);
      }

      break;

    case 13:
      (void)sm_check_column(reader, column, "Currency.Description", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_STRING));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "Currency.Description");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_CurrencyRecord_t* record = &table->records[row];

        (void)sm_cursor_next_string(&cursor, &record->description);
      }

      break;

    case 14:
      (void)sm_check_column(reader, column, "Currency.ShortCutIndex", SM_KIND_VAR_ARRAY, 0, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));


      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_CurrencyRecord_t* record = &table->records[row];
        int32_t element_count = 0;
        int32_t element;

        (void)sm_read_counter32(reader, &element_count);

        /* The count came off the wire, and one element is at least one byte. */
        if (!sm_row_count_is_plausible(reader, element_count, 1))
          return sm_fail_with(reader, "an array is longer than the file can hold");

        record->short_cut_index_count = element_count;
        record->short_cut_index = (int32_t*)sm_arena_alloc(
          &table->arena, (size_t)element_count * sizeof *record->short_cut_index);

        if (element_count > 0 && record->short_cut_index == NULL)
          return sm_fail_with(reader, "out of memory allocating an array");

        for (element = 0; element < element_count && !sm_failed(reader); ++element)
          (void)sm_read_i32_as(reader, column->element, &record->short_cut_index[element]);
      }

      break;

    default:
      /* A column this build has no member for: added to the schema after the code
       * was generated, or removed from the code while the data still carries it.
       * The block's declared length is the whole of what skipping needs to know. */
      (void)sm_skip(reader, column->byte_length);
      break;
    }

    if (!sm_check_block_end(reader, column, block_end))
      return false;
  }

  if (sm_failed(reader))
    return false;

  /* The keys can only be gathered once every column has been read: the column that
   * carries one arrives wherever the file happens to put it. */
  for (row = 0; row < table->count; ++row) {
    table->by_id[row].key = table->records[row].id;
    table->by_id[row].position = row;
  }

  sm_index_sort(table->by_id, table->count);

  return true;
}

bool Rescue_CurrencyLoad(Rescue_CurrencyTable_t* table, const char* filename,
                   char* error, size_t error_size) {
  uint8_t* bytes = NULL;
  int32_t length = 0;
  sm_reader reader;
  bool ok;

  /* Loaded beside whatever the table is already holding, arena and all, and swapped in
   * only if it worked. That is what makes a reload safe: reading a patched file that
   * turns out to be truncated - or to carry a column this build cannot read - leaves the
   * rows the caller has pointers to exactly where they were.
   *
   * It also has to be this way round for the memory. The table owns its arena, and
   * zeroing the table before reading - which is what this did - dropped that arena
   * without freeing it, so every reload leaked the whole of the previous load. */
  Rescue_CurrencyTable_t loaded;

  memset(&loaded, 0, sizeof loaded);

  if (!sm_read_all_bytes(filename, &bytes, &length)) {
    sm_copy_error(error, error_size, filename, "cannot be read");
    return false;
  }

  sm_reader_init(&reader, bytes, length, &loaded.arena);

  ok = Rescue_CurrencyParse(&loaded, &reader);

  if (ok) {
    /* The previous load goes now, and not before: its arena holds the strings and
     * arrays the caller was reading up to this line. */
    Rescue_CurrencyFree(table);
    *table = loaded;
  } else {
    sm_copy_error(error, error_size, filename, sm_error(&reader));

    /* Nothing half-read is kept, and the table is untouched. */
    Rescue_CurrencyFree(&loaded);
  }

  /* The strings were copied into the arena, so the file buffer goes either way. */
  sm_free_bytes(bytes);

  return ok;
}

void Rescue_CurrencyFree(Rescue_CurrencyTable_t* table) {
  sm_arena_free(&table->arena);

  memset(table, 0, sizeof *table);
}

const Rescue_CurrencyRecord_t* Rescue_CurrencyFindById(
  const Rescue_CurrencyTable_t* table, int32_t key) {
  int32_t position = sm_index_find(table->by_id, table->count, key);

  return position < 0 ? NULL : &table->records[position];
}

bool Rescue_CurrencyContainsId(
  const Rescue_CurrencyTable_t* table, int32_t key) {
  return sm_index_find(table->by_id, table->count, key) >= 0;
}
