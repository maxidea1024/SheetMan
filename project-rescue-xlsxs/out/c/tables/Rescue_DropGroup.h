/* ------------------------------------------------------------------------------
 * Generated by SheetMan. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef RESCUE_DROP_GROUP_H
#define RESCUE_DROP_GROUP_H

#include "sheetman/sheetman_scb_reader.h"

#include "Rescue_Forward.h"

#ifdef __cplusplus
extern "C" {
#endif
/* Generated from project-rescue-xlsxs/MasterData.xlsx : DropGroupTable : A1
 */
struct Rescue_DropGroupRecord_t {
  /* 기본값 */
  int32_t id;
  /* 영문 */
  const char* name;
  /* 한글 */
  const char* drop_name;
  /* 드랍 구역 */
  const char* drop_area;
  /* 아이템 종류1 */
  int32_t* drop_item_ids;
  int32_t drop_item_ids_count;
  /* 아이템 최소 수량 값 */
  int32_t* drop_item_min;
  int32_t drop_item_min_count;
  /* 아이템 최대 수량 값 */
  int32_t* drop_item_max;
  int32_t drop_item_max_count;
  /* 확률 */
  float* item_drop_rate;
  int32_t item_drop_rate_count;
};

/* Every row of DropGroup.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct Rescue_DropGroupTable_t {
  Rescue_DropGroupRecord_t* records;
  int32_t count;

  sm_index_entry* by_id;
  sm_arena arena;
} Rescue_DropGroupTable_t;

/* Loads the table from a .scb file written by SheetMan.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool Rescue_DropGroupLoad(Rescue_DropGroupTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void Rescue_DropGroupFree(Rescue_DropGroupTable_t* table);

/* The row with the given Id, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const Rescue_DropGroupRecord_t* Rescue_DropGroupFindById(
  const Rescue_DropGroupTable_t* table, int32_t key);

/* Whether the table holds a row with this Id. */
bool Rescue_DropGroupContainsId(
  const Rescue_DropGroupTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* RESCUE_DROP_GROUP_H */
