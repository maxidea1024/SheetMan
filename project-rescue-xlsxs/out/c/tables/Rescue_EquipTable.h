/* ------------------------------------------------------------------------------
 * Generated by SheetMan. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef RESCUE_EQUIP_TABLE_H
#define RESCUE_EQUIP_TABLE_H

#include "sheetman/sheetman_lite_binary_reader.h"

#include "Rescue_Forward.h"
#include "enums/Rescue_EnumJobType.h"
#include "enums/Rescue_EnumEquipmentType.h"
#include "enums/Rescue_EnumGradeType.h"
#include "enums/Rescue_EnumStatType.h"

#ifdef __cplusplus
extern "C" {
#endif
/* Generated from project-rescue-xlsxs/ItemData.xlsx : EquipTable : A1
 */
struct Rescue_EquipTableRecord_t {
  /* Equipment_Table */
  int32_t id;
  /* 한글명 */
  const char* name;
  /* 영문명 */
  const char* equip_name;
  /* 타입 */
  Rescue_JobType_t type;
  /* 장비 종류 */
  Rescue_EquipmentType_t equip_type;
  /* 등급 */
  Rescue_GradeType_t grade;
  /* 최대 성급 */
  int32_t max_class;
  /* 최대 레벨 */
  int32_t max_level;
  /* 스탯1 */
  Rescue_StatType_t stat_type1;
  /* 스탯2 */
  Rescue_StatType_t stat_type2;
  /* 아이콘 경로 */
  const char* icon_path;
  /* 프리펩 위치 */
  const char* prefab_path;
  /* 설명 */
  const char* description;
};

/* Every row of EquipTable.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct Rescue_EquipTableTable_t {
  Rescue_EquipTableRecord_t* records;
  int32_t count;

  sm_index_entry* by_id;
  sm_arena arena;
} Rescue_EquipTableTable_t;

/* Loads the table from a .scb file written by SheetMan.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool Rescue_EquipTableLoad(Rescue_EquipTableTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void Rescue_EquipTableFree(Rescue_EquipTableTable_t* table);

/* The row with the given Id, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const Rescue_EquipTableRecord_t* Rescue_EquipTableFindById(
  const Rescue_EquipTableTable_t* table, int32_t key);

/* Whether the table holds a row with this Id. */
bool Rescue_EquipTableContainsId(
  const Rescue_EquipTableTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* RESCUE_EQUIP_TABLE_H */
