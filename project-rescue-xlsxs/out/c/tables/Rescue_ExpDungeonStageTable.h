/* ------------------------------------------------------------------------------
 * Generated by SheetMan. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef RESCUE_EXP_DUNGEON_STAGE_TABLE_H
#define RESCUE_EXP_DUNGEON_STAGE_TABLE_H

#include "sheetman/sheetman_lite_binary_reader.h"

#include "Rescue_Forward.h"
#include "enums/Rescue_EnumDungeonType.h"

#ifdef __cplusplus
extern "C" {
#endif
/* Generated from project-rescue-xlsxs/DailyDungeon.xlsx : ExpDungeonStageTable : A1
 */
struct Rescue_ExpDungeonStageTableRecord_t {
  /* ExpDungeonStage_Table */
  int32_t id;
  /* 영문 */
  const char* name;
  /* 한글 */
  const char* stage_name;
  /* 던전 아이디 */
  Rescue_DungeonType_t dungeon_type;
  /* 던전 단계 */
  int32_t dungeon_floor;
  /* 제한 시간 */
  int32_t time_limit;
  /* 생성 몬스터 ID */
  int32_t* spawn_ids;
  int32_t spawn_ids_count;
  /* 보스 ID */
  int32_t* boss_id;
  int32_t boss_id_count;
  /* 위치 생성 가능 횟수 */
  int32_t spawn_point_count;
  /* 최대 생성 수 */
  int32_t spawn_max_count;
  /* 다음 포인터 생성 시간 */
  float spawn_next_time;
  /* 스테이지 클리어 조건 */
  int32_t stage_clear_count;
  /* 권장 전투력 */
  const char* recommend_power;
  /* 보상 설정 */
  int32_t reward_id;
  /* 던전 공격력 증가 값 */
  float atk_up_percent;
  /* 던전 방어력 증가 값 */
  float def_up_percent;
  /* 던전 체력 증가 값 */
  float mhp_up_percent;
  /* 던전 이미지 */
  const char* dungeon_image_path;
  /* 몬스터 이미지 */
  const char* monster_image_path;
  /* AssetData 경로(Stage처럼 에셋만 추가하면 바뀌게) */
  const char* asset_data_path;
};

/* Every row of ExpDungeonStageTable.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct Rescue_ExpDungeonStageTableTable_t {
  Rescue_ExpDungeonStageTableRecord_t* records;
  int32_t count;

  sm_index_entry* by_id;
  sm_arena arena;
} Rescue_ExpDungeonStageTableTable_t;

/* Loads the table from a .table file written by SheetMan.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool Rescue_ExpDungeonStageTableLoad(Rescue_ExpDungeonStageTableTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void Rescue_ExpDungeonStageTableFree(Rescue_ExpDungeonStageTableTable_t* table);

/* The row with the given Id, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const Rescue_ExpDungeonStageTableRecord_t* Rescue_ExpDungeonStageTableFindById(
  const Rescue_ExpDungeonStageTableTable_t* table, int32_t key);

/* Whether the table holds a row with this Id. */
bool Rescue_ExpDungeonStageTableContainsId(
  const Rescue_ExpDungeonStageTableTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* RESCUE_EXP_DUNGEON_STAGE_TABLE_H */
