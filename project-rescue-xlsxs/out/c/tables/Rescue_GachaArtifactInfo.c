/* ------------------------------------------------------------------------------
 * Generated by SheetMan. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#include "tables/Rescue_GachaArtifactInfo.h"

#include <string.h>
static bool Rescue_GachaArtifactInfoParse(Rescue_GachaArtifactInfoTable_t* table, sm_reader* reader) {
  int32_t row;
  int32_t at;
  sm_column* columns = NULL;
  int32_t column_count = 0;
  sm_cursor cursor;

  if (!sm_read_table_header(reader, &table->count, &columns, &column_count))
    return false;

  if (table->count > 0) {
    table->records = (Rescue_GachaArtifactInfoRecord_t*)sm_arena_alloc(
      &table->arena, (size_t)table->count * sizeof *table->records);

    if (table->records == NULL)
      return sm_fail_with(reader, "out of memory allocating the rows");

    table->by_id = (sm_index_entry*)sm_arena_alloc(
      &table->arena, (size_t)table->count * sizeof *table->by_id);

    if (table->by_id == NULL)
      return sm_fail_with(reader, "out of memory allocating the Id index");

  }


  /* The arena hands back zeroed memory, so every member starts at its zero - which for
   * a string is NULL, and NULL is what reaches printf when a column the file does not
   * carry is read. An empty string instead: a literal, so there is nothing to free and
   * nothing to allocate. */
  for (row = 0; row < table->count; ++row) {
    Rescue_GachaArtifactInfoRecord_t* record = &table->records[row];

    record->name = "";

    record->icon_path = "";
  }

  /* Column by column, matched by tag rather than by position: a column this build has
   * no member for is skipped by its declared length, and one whose type no longer fits
   * the member stops the read naming the field. Rows arrive with every member at an
   * empty value, so one the file has nothing for keeps it. */
  for (at = 0; at < column_count && !sm_failed(reader); ++at) {
    const sm_column* column = &columns[at];
    int32_t block_end = reader->position + column->byte_length;

    switch (column->tag) {

    case 1:
      (void)sm_check_column(reader, column, "GachaArtifactInfo.Id", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "GachaArtifactInfo.Id");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_GachaArtifactInfoRecord_t* record = &table->records[row];

        (void)sm_cursor_next_i32(&cursor, &record->id);
      }

      break;

    case 2:
      (void)sm_check_column(reader, column, "GachaArtifactInfo.Name", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_STRING));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "GachaArtifactInfo.Name");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_GachaArtifactInfoRecord_t* record = &table->records[row];

        (void)sm_cursor_next_string(&cursor, &record->name);
      }

      break;

    case 3:
      (void)sm_check_column(reader, column, "GachaArtifactInfo.GachaType", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_VARINT));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "GachaArtifactInfo.GachaType");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_GachaArtifactInfoRecord_t* record = &table->records[row];
        int32_t scratch = 0;

        (void)sm_cursor_next_i32(&cursor, &scratch);
        record->gacha_type = (Rescue_GachaType_t)scratch;
      }

      break;

    case 4:
      (void)sm_check_column(reader, column, "GachaArtifactInfo.Priority", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "GachaArtifactInfo.Priority");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_GachaArtifactInfoRecord_t* record = &table->records[row];

        (void)sm_cursor_next_i32(&cursor, &record->priority);
      }

      break;

    case 5:
      (void)sm_check_column(reader, column, "GachaArtifactInfo.ConditionID", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "GachaArtifactInfo.ConditionID");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_GachaArtifactInfoRecord_t* record = &table->records[row];

        (void)sm_cursor_next_i32(&cursor, &record->condition_id);
      }

      break;

    case 6:
      (void)sm_check_column(reader, column, "GachaArtifactInfo.RateId", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "GachaArtifactInfo.RateId");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_GachaArtifactInfoRecord_t* record = &table->records[row];

        (void)sm_cursor_next_i32(&cursor, &record->rate_id);
      }

      break;

    case 7:
      (void)sm_check_column(reader, column, "GachaArtifactInfo.TriggerCount", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "GachaArtifactInfo.TriggerCount");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_GachaArtifactInfoRecord_t* record = &table->records[row];

        (void)sm_cursor_next_i32(&cursor, &record->trigger_count);
      }

      break;

    case 8:
      (void)sm_check_column(reader, column, "GachaArtifactInfo.TriggerRateId", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "GachaArtifactInfo.TriggerRateId");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_GachaArtifactInfoRecord_t* record = &table->records[row];

        (void)sm_cursor_next_i32(&cursor, &record->trigger_rate_id);
      }

      break;

    case 9:
      (void)sm_check_column(reader, column, "GachaArtifactInfo.EndCount", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "GachaArtifactInfo.EndCount");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_GachaArtifactInfoRecord_t* record = &table->records[row];

        (void)sm_cursor_next_i32(&cursor, &record->end_count);
      }

      break;

    case 10:
      (void)sm_check_column(reader, column, "GachaArtifactInfo.EndRateId", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "GachaArtifactInfo.EndRateId");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_GachaArtifactInfoRecord_t* record = &table->records[row];

        (void)sm_cursor_next_i32(&cursor, &record->end_rate_id);
      }

      break;

    case 11:
      (void)sm_check_column(reader, column, "GachaArtifactInfo.EndCharacterId", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "GachaArtifactInfo.EndCharacterId");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_GachaArtifactInfoRecord_t* record = &table->records[row];

        (void)sm_cursor_next_i32(&cursor, &record->end_character_id);
      }

      break;

    case 12:
      (void)sm_check_column(reader, column, "GachaArtifactInfo.EnableReset", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_BOOL));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "GachaArtifactInfo.EnableReset");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_GachaArtifactInfoRecord_t* record = &table->records[row];

        (void)sm_cursor_next_bool(&cursor, &record->enable_reset);
      }

      break;

    case 13:
      (void)sm_check_column(reader, column, "GachaArtifactInfo.IsSharedCounter", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_BOOL));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "GachaArtifactInfo.IsSharedCounter");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_GachaArtifactInfoRecord_t* record = &table->records[row];

        (void)sm_cursor_next_bool(&cursor, &record->is_shared_counter);
      }

      break;

    case 14:
      (void)sm_check_column(reader, column, "GachaArtifactInfo.WishListConditionID", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "GachaArtifactInfo.WishListConditionID");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_GachaArtifactInfoRecord_t* record = &table->records[row];

        (void)sm_cursor_next_i32(&cursor, &record->wish_list_condition_id);
      }

      break;

    case 15:
      (void)sm_check_column(reader, column, "GachaArtifactInfo.WishListMaxValue", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "GachaArtifactInfo.WishListMaxValue");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_GachaArtifactInfoRecord_t* record = &table->records[row];

        (void)sm_cursor_next_i32(&cursor, &record->wish_list_max_value);
      }

      break;

    case 16:
      (void)sm_check_column(reader, column, "GachaArtifactInfo.IsSingle", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_BOOL));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "GachaArtifactInfo.IsSingle");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_GachaArtifactInfoRecord_t* record = &table->records[row];

        (void)sm_cursor_next_bool(&cursor, &record->is_single);
      }

      break;

    case 17:
      (void)sm_check_column(reader, column, "GachaArtifactInfo.IsTen", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_BOOL));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "GachaArtifactInfo.IsTen");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_GachaArtifactInfoRecord_t* record = &table->records[row];

        (void)sm_cursor_next_bool(&cursor, &record->is_ten);
      }

      break;

    case 18:
      (void)sm_check_column(reader, column, "GachaArtifactInfo.CurrencyType1", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_VARINT));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "GachaArtifactInfo.CurrencyType1");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_GachaArtifactInfoRecord_t* record = &table->records[row];
        int32_t scratch = 0;

        (void)sm_cursor_next_i32(&cursor, &scratch);
        record->currency_type1 = (Rescue_CurrencyType_t)scratch;
      }

      break;

    case 19:
      (void)sm_check_column(reader, column, "GachaArtifactInfo.SingleCost1", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "GachaArtifactInfo.SingleCost1");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_GachaArtifactInfoRecord_t* record = &table->records[row];

        (void)sm_cursor_next_i32(&cursor, &record->single_cost1);
      }

      break;

    case 20:
      (void)sm_check_column(reader, column, "GachaArtifactInfo.CurrencyType2", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_VARINT));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "GachaArtifactInfo.CurrencyType2");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_GachaArtifactInfoRecord_t* record = &table->records[row];
        int32_t scratch = 0;

        (void)sm_cursor_next_i32(&cursor, &scratch);
        record->currency_type2 = (Rescue_CurrencyType_t)scratch;
      }

      break;

    case 21:
      (void)sm_check_column(reader, column, "GachaArtifactInfo.SingleCost2", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "GachaArtifactInfo.SingleCost2");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_GachaArtifactInfoRecord_t* record = &table->records[row];

        (void)sm_cursor_next_i32(&cursor, &record->single_cost2);
      }

      break;

    case 22:
      (void)sm_check_column(reader, column, "GachaArtifactInfo.IconPath", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_STRING));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "GachaArtifactInfo.IconPath");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_GachaArtifactInfoRecord_t* record = &table->records[row];

        (void)sm_cursor_next_string(&cursor, &record->icon_path);
      }

      break;

    case 23:
      (void)sm_check_column(reader, column, "GachaArtifactInfo.EnableSkip", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_BOOL));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "GachaArtifactInfo.EnableSkip");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_GachaArtifactInfoRecord_t* record = &table->records[row];

        (void)sm_cursor_next_bool(&cursor, &record->enable_skip);
      }

      break;

    default:
      /* A column this build has no member for: added to the schema after the code
       * was generated, or removed from the code while the data still carries it.
       * The block's declared length is the whole of what skipping needs to know. */
      (void)sm_skip(reader, column->byte_length);
      break;
    }

    if (!sm_check_block_end(reader, column, block_end))
      return false;
  }

  if (sm_failed(reader))
    return false;

  /* The keys can only be gathered once every column has been read: the column that
   * carries one arrives wherever the file happens to put it. */
  for (row = 0; row < table->count; ++row) {
    table->by_id[row].key = table->records[row].id;
    table->by_id[row].position = row;
  }

  sm_index_sort(table->by_id, table->count);

  return true;
}

bool Rescue_GachaArtifactInfoLoad(Rescue_GachaArtifactInfoTable_t* table, const char* filename,
                   char* error, size_t error_size) {
  uint8_t* bytes = NULL;
  int32_t length = 0;
  sm_reader reader;
  bool ok;

  /* Loaded beside whatever the table is already holding, arena and all, and swapped in
   * only if it worked. That is what makes a reload safe: reading a patched file that
   * turns out to be truncated - or to carry a column this build cannot read - leaves the
   * rows the caller has pointers to exactly where they were.
   *
   * It also has to be this way round for the memory. The table owns its arena, and
   * zeroing the table before reading - which is what this did - dropped that arena
   * without freeing it, so every reload leaked the whole of the previous load. */
  Rescue_GachaArtifactInfoTable_t loaded;

  memset(&loaded, 0, sizeof loaded);

  if (!sm_read_all_bytes(filename, &bytes, &length)) {
    sm_copy_error(error, error_size, filename, "cannot be read");
    return false;
  }

  sm_reader_init(&reader, bytes, length, &loaded.arena);

  ok = Rescue_GachaArtifactInfoParse(&loaded, &reader);

  if (ok) {
    /* The previous load goes now, and not before: its arena holds the strings and
     * arrays the caller was reading up to this line. */
    Rescue_GachaArtifactInfoFree(table);
    *table = loaded;
  } else {
    sm_copy_error(error, error_size, filename, sm_error(&reader));

    /* Nothing half-read is kept, and the table is untouched. */
    Rescue_GachaArtifactInfoFree(&loaded);
  }

  /* The strings were copied into the arena, so the file buffer goes either way. */
  sm_free_bytes(bytes);

  return ok;
}

void Rescue_GachaArtifactInfoFree(Rescue_GachaArtifactInfoTable_t* table) {
  sm_arena_free(&table->arena);

  memset(table, 0, sizeof *table);
}

const Rescue_GachaArtifactInfoRecord_t* Rescue_GachaArtifactInfoFindById(
  const Rescue_GachaArtifactInfoTable_t* table, int32_t key) {
  int32_t position = sm_index_find(table->by_id, table->count, key);

  return position < 0 ? NULL : &table->records[position];
}

bool Rescue_GachaArtifactInfoContainsId(
  const Rescue_GachaArtifactInfoTable_t* table, int32_t key) {
  return sm_index_find(table->by_id, table->count, key) >= 0;
}
