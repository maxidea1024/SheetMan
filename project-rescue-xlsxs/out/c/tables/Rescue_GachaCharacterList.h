/* ------------------------------------------------------------------------------
 * Generated by SheetMan. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef RESCUE_GACHA_CHARACTER_LIST_H
#define RESCUE_GACHA_CHARACTER_LIST_H

#include "sheetman/sheetman_lite_binary_reader.h"

#include "Rescue_Forward.h"
#include "enums/Rescue_EnumGradeType.h"

#ifdef __cplusplus
extern "C" {
#endif
/* Generated from project-rescue-xlsxs/GachaData.xlsx : GachaCharacterListTable : A1
 */
struct Rescue_GachaCharacterListRecord_t {
  /* GachaCharacter_List */
  int32_t id;
  /* 캐릭터 ID */
  int32_t character_id;
  /* 태생 등급 */
  Rescue_GradeType_t grade_type;
  /* 기본 가중치 */
  int32_t base_weight;
  /* 위시리스트 여부 */
  bool is_wishlist;
  /* 위시리스트 가중치 */
  int32_t wishlist_weight;
  /* 성급 재화ID */
  int32_t class_up_currency_id;
  /* 성급 재화값 */
  int32_t class_up_currency_value;
  /* 변환 조건 ID */
  int32_t ex_condition_id;
  /* 변환 재화ID */
  int32_t ex_currency_id;
  /* 변환 재화값 */
  int32_t ex_currency_value;
};

/* Every row of GachaCharacterList.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct Rescue_GachaCharacterListTable_t {
  Rescue_GachaCharacterListRecord_t* records;
  int32_t count;

  sm_index_entry* by_id;
  sm_arena arena;
} Rescue_GachaCharacterListTable_t;

/* Loads the table from a .scb file written by SheetMan.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool Rescue_GachaCharacterListLoad(Rescue_GachaCharacterListTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void Rescue_GachaCharacterListFree(Rescue_GachaCharacterListTable_t* table);

/* The row with the given Id, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const Rescue_GachaCharacterListRecord_t* Rescue_GachaCharacterListFindById(
  const Rescue_GachaCharacterListTable_t* table, int32_t key);

/* Whether the table holds a row with this Id. */
bool Rescue_GachaCharacterListContainsId(
  const Rescue_GachaCharacterListTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* RESCUE_GACHA_CHARACTER_LIST_H */
