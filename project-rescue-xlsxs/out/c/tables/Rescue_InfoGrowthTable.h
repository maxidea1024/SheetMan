/* ------------------------------------------------------------------------------
 * Generated by SheetMan. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef RESCUE_INFO_GROWTH_TABLE_H
#define RESCUE_INFO_GROWTH_TABLE_H

#include "sheetman/sheetman_lite_binary_reader.h"

#include "Rescue_Forward.h"
#include "enums/Rescue_EnumGrowthType.h"
#include "enums/Rescue_EnumCurrencyType.h"

#ifdef __cplusplus
extern "C" {
#endif
/* Generated from project-rescue-xlsxs/StageGrowth.xlsx : InfoGrowthTable : A1
 */
struct Rescue_InfoGrowthTableRecord_t {
  /* 기본값 */
  int32_t id;
  /* 영문 */
  const char* name;
  /* 한글 */
  const char* info_name;
  /* 실제 데이터가 있는 시트 이름 (AccountGrowth/TraitGrowth/RelicGrowth) */
  const char* sheet_name;
  /* 시스템 분류 (Account/Trait/Relic) */
  Rescue_GrowthType_t growth_type;
  /* 초기화 가능 불가능 체크 */
  bool reset;
  /* 초기화 재화 타입 */
  Rescue_CurrencyType_t reset_cost_type;
  /* 초기화 비용 값 */
  int32_t reset_cost_value;
  /* 루프형 시스템만 사용 (예: 유물 20단계) */
  int32_t max_step;
  /* 단일 레벨 최대치 (루프형 시스템은 100 고정) */
  int32_t max_level;
  /* 해금 조건 */
  const char* unlock_condition_type;
  /* 해금 제한 */
  int32_t unlock_condition_value;
  /* 설명 */
  const char* description;
  /* 아이콘 */
  const char* icon_path;
};

/* Every row of InfoGrowthTable.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct Rescue_InfoGrowthTableTable_t {
  Rescue_InfoGrowthTableRecord_t* records;
  int32_t count;

  sm_index_entry* by_id;
  sm_arena arena;
} Rescue_InfoGrowthTableTable_t;

/* Loads the table from a .scb file written by SheetMan.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool Rescue_InfoGrowthTableLoad(Rescue_InfoGrowthTableTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void Rescue_InfoGrowthTableFree(Rescue_InfoGrowthTableTable_t* table);

/* The row with the given Id, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const Rescue_InfoGrowthTableRecord_t* Rescue_InfoGrowthTableFindById(
  const Rescue_InfoGrowthTableTable_t* table, int32_t key);

/* Whether the table holds a row with this Id. */
bool Rescue_InfoGrowthTableContainsId(
  const Rescue_InfoGrowthTableTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* RESCUE_INFO_GROWTH_TABLE_H */
