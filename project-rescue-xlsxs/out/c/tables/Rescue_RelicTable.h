/* ------------------------------------------------------------------------------
 * Generated by SheetMan. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef RESCUE_RELIC_TABLE_H
#define RESCUE_RELIC_TABLE_H

#include "sheetman/sheetman_lite_binary_reader.h"

#include "Rescue_Forward.h"
#include "enums/Rescue_EnumStatType.h"

#ifdef __cplusplus
extern "C" {
#endif
/* Generated from project-rescue-xlsxs/StageGrowth.xlsx : RelicTable : A1
 */
struct Rescue_RelicTableRecord_t {
  /* 기본값 */
  int32_t id;
  /* 영문 */
  const char* name;
  /* 한글 */
  const char* relic_name;
  /* 영향력 단계 */
  int32_t infuluence_step;
  /* 스탯 타입 */
  Rescue_StatType_t relic_type1;
  /* 유물 기본 값 1 */
  float relic_value1;
  /* 유물 레벨 당 스탯 증가 수치 1 */
  float relic_inc_value1;
  /* 유물 기본 값 2 */
  Rescue_StatType_t relic_type2;
  /* 유물 레벨 당 스탯 증가 수치 2 */
  float relic_value2;
  /* 유물 기본 값 3 */
  float relic_inc_value2;
  /* 유물 레벨 당 스탯 증가 수치 3 */
  Rescue_StatType_t relic_type3;
  /* 유물 기본 값 4 */
  float relic_value3;
  /* 유물 레벨 당 스탯 증가 수치 4 */
  float relic_inc_value3;
  /* 설명 */
  const char* description;
  /* 아이콘 경로 */
  const char* icon_path;
};

/* Every row of RelicTable.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct Rescue_RelicTableTable_t {
  Rescue_RelicTableRecord_t* records;
  int32_t count;

  sm_index_entry* by_id;
  sm_arena arena;
} Rescue_RelicTableTable_t;

/* Loads the table from a .table file written by SheetMan.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool Rescue_RelicTableLoad(Rescue_RelicTableTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void Rescue_RelicTableFree(Rescue_RelicTableTable_t* table);

/* The row with the given Id, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const Rescue_RelicTableRecord_t* Rescue_RelicTableFindById(
  const Rescue_RelicTableTable_t* table, int32_t key);

/* Whether the table holds a row with this Id. */
bool Rescue_RelicTableContainsId(
  const Rescue_RelicTableTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* RESCUE_RELIC_TABLE_H */
