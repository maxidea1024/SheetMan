/* ------------------------------------------------------------------------------
 * Generated by SheetMan. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef RESCUE_SD_ALCHEMY_INFO_TABLE_H
#define RESCUE_SD_ALCHEMY_INFO_TABLE_H

#include "sheetman/sheetman_lite_binary_reader.h"

#include "Rescue_Forward.h"
#include "enums/Rescue_EnumCurrencyType.h"
#include "enums/Rescue_EnumStatType.h"
#include "enums/Rescue_EnumTargetType.h"

#ifdef __cplusplus
extern "C" {
#endif
/* Generated from project-rescue-xlsxs\SD_Contens.xlsx : SDAlchemyInfoTable : A1
 */
struct Rescue_SDAlchemyInfoTableRecord_t {
  /* SDAlchemyInfo_Table */
  int32_t id;
  /* 영문명 */
  const char* name;
  /* 한글명 */
  const char* name_kr;
  /* 전용 재료 타입 */
  Rescue_CurrencyType_t material_type;
  /* 공용 재료1 타입 */
  Rescue_CurrencyType_t common_material_type1;
  /* 공용 재료2 타입 */
  Rescue_CurrencyType_t common_material_type2;
  /* 재료값 증가 비율 */
  float material_type2_rate;
  /* 스텟 타입 */
  Rescue_StatType_t stat_type;
  /* 타겟 대상 */
  Rescue_TargetType_t target_type;
  /* 최대 레벨값 */
  int32_t max_level_id;
  /* 가속 시간 */
  int32_t accelerate_time;
  /* 가속권 타입 */
  Rescue_CurrencyType_t accelerate_item_type1;
  /* 가속권 개수 */
  int32_t accelerate_item_cost1;
  /* 다이아 */
  Rescue_CurrencyType_t accelerate_item_type2;
  /* 필요 다이아 개수 */
  int32_t accelerate_item_cost2;
  /* 아이콘 경로 */
  const char* icon_path;
};

/* Every row of SDAlchemyInfoTable.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct Rescue_SDAlchemyInfoTableTable_t {
  Rescue_SDAlchemyInfoTableRecord_t* records;
  int32_t count;

  sm_index_entry* by_id;
  sm_arena arena;
} Rescue_SDAlchemyInfoTableTable_t;

/* Loads the table from a .table file written by SheetMan.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool Rescue_SDAlchemyInfoTableLoad(Rescue_SDAlchemyInfoTableTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void Rescue_SDAlchemyInfoTableFree(Rescue_SDAlchemyInfoTableTable_t* table);

/* The row with the given Id, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const Rescue_SDAlchemyInfoTableRecord_t* Rescue_SDAlchemyInfoTableFindById(
  const Rescue_SDAlchemyInfoTableTable_t* table, int32_t key);

/* Whether the table holds a row with this Id. */
bool Rescue_SDAlchemyInfoTableContainsId(
  const Rescue_SDAlchemyInfoTableTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* RESCUE_SD_ALCHEMY_INFO_TABLE_H */
