/* ------------------------------------------------------------------------------
 * Generated by SheetMan. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef RESCUE_SD_ALCHEMY_LEVEL_TABLE_H
#define RESCUE_SD_ALCHEMY_LEVEL_TABLE_H

#include "sheetman/sheetman_lite_binary_reader.h"

#include "Rescue_Forward.h"

#ifdef __cplusplus
extern "C" {
#endif
/* Generated from project-rescue-xlsxs/SD_Contens.xlsx : SDAlchemyLevelTable : A1
 */
struct Rescue_SDAlchemyLevelTableRecord_t {
  /* SDAlchemyLevel_Table */
  int32_t id;
  /* 영문명 */
  const char* name;
  /* 한글명 */
  const char* name_kr;
  /* 필요 재화값 */
  int32_t level_up_cost;
  /* 누적 재화값 */
  int32_t level_up_total;
  /* 소요 시간 */
  int64_t time;
  /* 필요 다이아 개수 */
  int32_t use_dia_cost;
  /* 필요 가속권 개수 */
  int32_t use_item_cost;
  /* 크리티컬 데미지 증가값 */
  float crit_dmg_value;
  /* 크리티컬 데미지 누적값 */
  float crit_dmg_total;
  /* 크리티컬 확률 증가값 */
  float crit_rate_value;
  /* 크리티컬 확률 누적값 */
  float crit_rate_total;
  /* 블록 확률 증가값 */
  float block_rate_value;
  /* 블록 확률 누적값 */
  float block_rate_total;
  /* 명중률 증가값 */
  float hit_value;
  /* 명중률 누적값 */
  float hit_total;
};

/* Every row of SDAlchemyLevelTable.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct Rescue_SDAlchemyLevelTableTable_t {
  Rescue_SDAlchemyLevelTableRecord_t* records;
  int32_t count;

  sm_index_entry* by_id;
  sm_arena arena;
} Rescue_SDAlchemyLevelTableTable_t;

/* Loads the table from a .scb file written by SheetMan.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool Rescue_SDAlchemyLevelTableLoad(Rescue_SDAlchemyLevelTableTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void Rescue_SDAlchemyLevelTableFree(Rescue_SDAlchemyLevelTableTable_t* table);

/* The row with the given Id, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const Rescue_SDAlchemyLevelTableRecord_t* Rescue_SDAlchemyLevelTableFindById(
  const Rescue_SDAlchemyLevelTableTable_t* table, int32_t key);

/* Whether the table holds a row with this Id. */
bool Rescue_SDAlchemyLevelTableContainsId(
  const Rescue_SDAlchemyLevelTableTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* RESCUE_SD_ALCHEMY_LEVEL_TABLE_H */
