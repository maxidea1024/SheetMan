/* ------------------------------------------------------------------------------
 * Generated by SheetMan. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef RESCUE_SD_CONTENS_INFO_TABLE_H
#define RESCUE_SD_CONTENS_INFO_TABLE_H

#include "sheetman/sheetman_lite_binary_reader.h"

#include "Rescue_Forward.h"
#include "enums/Rescue_EnumSdContensType.h"
#include "enums/Rescue_EnumCurrencyType.h"
#include "enums/Rescue_EnumCycleType.h"

#ifdef __cplusplus
extern "C" {
#endif
/* Generated from project-rescue-xlsxs\SD_Contens.xlsx : SDContensInfoTable : A1
 */
struct Rescue_SDContensInfoTableRecord_t {
  /* SDContensInfo_Table */
  int32_t id;
  /* 영문 */
  const char* name;
  /* 한글 */
  const char* info_name;
  /* 실제 데이터가 있는 시트 이름 */
  const char* sheet_name;
  /* SD 콘텐츠 타입 */
  Rescue_SdContensType_t sd_contens_type;
  /* 콘텐츠 해금 조건 ID */
  int32_t condition_id;
  /* 소모 재화 */
  Rescue_CurrencyType_t currency_type;
  /* 초기화 적용 유무 */
  bool enable_reset;
  /* 초기화 타입 */
  Rescue_CycleType_t cycle_type;
  /* SD  캐릭터 프리펩 경로 */
  const char* sd_character_path;
  /* UI 3d프리펩 경로 */
  const char* prefab_path;
};

/* Every row of SDContensInfoTable.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct Rescue_SDContensInfoTableTable_t {
  Rescue_SDContensInfoTableRecord_t* records;
  int32_t count;

  sm_index_entry* by_id;
  sm_arena arena;
} Rescue_SDContensInfoTableTable_t;

/* Loads the table from a .table file written by SheetMan.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool Rescue_SDContensInfoTableLoad(Rescue_SDContensInfoTableTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void Rescue_SDContensInfoTableFree(Rescue_SDContensInfoTableTable_t* table);

/* The row with the given Id, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const Rescue_SDContensInfoTableRecord_t* Rescue_SDContensInfoTableFindById(
  const Rescue_SDContensInfoTableTable_t* table, int32_t key);

/* Whether the table holds a row with this Id. */
bool Rescue_SDContensInfoTableContainsId(
  const Rescue_SDContensInfoTableTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* RESCUE_SD_CONTENS_INFO_TABLE_H */
