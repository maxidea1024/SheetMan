/* ------------------------------------------------------------------------------
 * Generated by SheetMan. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#include "tables/Rescue_SDDungeonRewardTable.h"

#include <string.h>
static bool Rescue_SDDungeonRewardTableParse(Rescue_SDDungeonRewardTableTable_t* table, sm_reader* reader) {
  int32_t row;
  int32_t at;
  sm_column* columns = NULL;
  int32_t column_count = 0;

  if (!sm_read_table_header(reader, &table->count, &columns, &column_count))
    return false;

  if (table->count > 0) {
    table->records = (Rescue_SDDungeonRewardTableRecord_t*)sm_arena_alloc(
      &table->arena, (size_t)table->count * sizeof *table->records);

    if (table->records == NULL)
      return sm_fail_with(reader, "out of memory allocating the rows");

    table->by_id = (sm_index_entry*)sm_arena_alloc(
      &table->arena, (size_t)table->count * sizeof *table->by_id);

    if (table->by_id == NULL)
      return sm_fail_with(reader, "out of memory allocating the Id index");

  }


  /* The arena hands back zeroed memory, so every member starts at its zero - which for
   * a string is NULL, and NULL is what reaches printf when a column the file does not
   * carry is read. An empty string instead: a literal, so there is nothing to free and
   * nothing to allocate. */
  for (row = 0; row < table->count; ++row) {
    Rescue_SDDungeonRewardTableRecord_t* record = &table->records[row];

    record->name = "";

    record->reward_name = "";
  }

  /* Column by column, matched by tag rather than by position: a column this build has
   * no member for is skipped by its declared length, and one whose type no longer fits
   * the member stops the read naming the field. Rows arrive with every member at an
   * empty value, so one the file has nothing for keeps it. */
  for (at = 0; at < column_count && !sm_failed(reader); ++at) {
    const sm_column* column = &columns[at];
    int32_t block_end = reader->position + column->byte_length;

    switch (column->tag) {

    case 1:
      (void)sm_check_column(reader, column, "SDDungeonRewardTable.Id", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));


      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_SDDungeonRewardTableRecord_t* record = &table->records[row];

        (void)sm_read_i32_as(reader, column->element, &record->id);
      }

      break;

    case 2:
      (void)sm_check_column(reader, column, "SDDungeonRewardTable.Name", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_STRING));


      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_SDDungeonRewardTableRecord_t* record = &table->records[row];

        (void)sm_read_string(reader, &record->name);
      }

      break;

    case 3:
      (void)sm_check_column(reader, column, "SDDungeonRewardTable.RewardName", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_STRING));


      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_SDDungeonRewardTableRecord_t* record = &table->records[row];

        (void)sm_read_string(reader, &record->reward_name);
      }

      break;

    case 4:
      (void)sm_check_column(reader, column, "SDDungeonRewardTable.ConditionId", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));


      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_SDDungeonRewardTableRecord_t* record = &table->records[row];

        (void)sm_read_i32_as(reader, column->element, &record->condition_id);
      }

      break;

    case 5:
      (void)sm_check_column(reader, column, "SDDungeonRewardTable.RewardCommon", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_VARINT));


      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_SDDungeonRewardTableRecord_t* record = &table->records[row];
        int32_t scratch = 0;

        (void)sm_read_enum(reader, &scratch);
        record->reward_common = (Rescue_CurrencyType_t)scratch;
      }

      break;

    case 6:
      (void)sm_check_column(reader, column, "SDDungeonRewardTable.CommonValue", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));


      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_SDDungeonRewardTableRecord_t* record = &table->records[row];

        (void)sm_read_i32_as(reader, column->element, &record->common_value);
      }

      break;

    case 7:
      (void)sm_check_column(reader, column, "SDDungeonRewardTable.Reward1Type", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_VARINT));


      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_SDDungeonRewardTableRecord_t* record = &table->records[row];
        int32_t scratch = 0;

        (void)sm_read_enum(reader, &scratch);
        record->reward1_type = (Rescue_CurrencyType_t)scratch;
      }

      break;

    case 8:
      (void)sm_check_column(reader, column, "SDDungeonRewardTable.Reward1Value", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));


      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_SDDungeonRewardTableRecord_t* record = &table->records[row];

        (void)sm_read_i32_as(reader, column->element, &record->reward1_value);
      }

      break;

    case 9:
      (void)sm_check_column(reader, column, "SDDungeonRewardTable.Reward2Type", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_VARINT));


      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_SDDungeonRewardTableRecord_t* record = &table->records[row];
        int32_t scratch = 0;

        (void)sm_read_enum(reader, &scratch);
        record->reward2_type = (Rescue_CurrencyType_t)scratch;
      }

      break;

    case 10:
      (void)sm_check_column(reader, column, "SDDungeonRewardTable.Reward2Value", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));


      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_SDDungeonRewardTableRecord_t* record = &table->records[row];

        (void)sm_read_i32_as(reader, column->element, &record->reward2_value);
      }

      break;

    case 11:
      (void)sm_check_column(reader, column, "SDDungeonRewardTable.Reward3Type", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_VARINT));


      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_SDDungeonRewardTableRecord_t* record = &table->records[row];
        int32_t scratch = 0;

        (void)sm_read_enum(reader, &scratch);
        record->reward3_type = (Rescue_CurrencyType_t)scratch;
      }

      break;

    case 12:
      (void)sm_check_column(reader, column, "SDDungeonRewardTable.Reward3Value", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));


      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_SDDungeonRewardTableRecord_t* record = &table->records[row];

        (void)sm_read_i32_as(reader, column->element, &record->reward3_value);
      }

      break;

    case 13:
      (void)sm_check_column(reader, column, "SDDungeonRewardTable.Reward4Type", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_VARINT));


      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_SDDungeonRewardTableRecord_t* record = &table->records[row];
        int32_t scratch = 0;

        (void)sm_read_enum(reader, &scratch);
        record->reward4_type = (Rescue_CurrencyType_t)scratch;
      }

      break;

    case 14:
      (void)sm_check_column(reader, column, "SDDungeonRewardTable.Reward4Value", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));


      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_SDDungeonRewardTableRecord_t* record = &table->records[row];

        (void)sm_read_i32_as(reader, column->element, &record->reward4_value);
      }

      break;

    case 15:
      (void)sm_check_column(reader, column, "SDDungeonRewardTable.IsPerfect", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_BOOL));


      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_SDDungeonRewardTableRecord_t* record = &table->records[row];

        (void)sm_read_bool(reader, &record->is_perfect);
      }

      break;

    case 16:
      (void)sm_check_column(reader, column, "SDDungeonRewardTable.RewardPerfect", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_VARINT));


      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_SDDungeonRewardTableRecord_t* record = &table->records[row];
        int32_t scratch = 0;

        (void)sm_read_enum(reader, &scratch);
        record->reward_perfect = (Rescue_CurrencyType_t)scratch;
      }

      break;

    case 17:
      (void)sm_check_column(reader, column, "SDDungeonRewardTable.PerfectValue", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));


      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_SDDungeonRewardTableRecord_t* record = &table->records[row];

        (void)sm_read_i32_as(reader, column->element, &record->perfect_value);
      }

      break;

    case 18:
      (void)sm_check_column(reader, column, "SDDungeonRewardTable.IsMinReward", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_BOOL));


      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_SDDungeonRewardTableRecord_t* record = &table->records[row];

        (void)sm_read_bool(reader, &record->is_min_reward);
      }

      break;

    case 19:
      (void)sm_check_column(reader, column, "SDDungeonRewardTable.RewardMin", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_VARINT));


      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_SDDungeonRewardTableRecord_t* record = &table->records[row];
        int32_t scratch = 0;

        (void)sm_read_enum(reader, &scratch);
        record->reward_min = (Rescue_CurrencyType_t)scratch;
      }

      break;

    case 20:
      (void)sm_check_column(reader, column, "SDDungeonRewardTable.MinRewardValue", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));


      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_SDDungeonRewardTableRecord_t* record = &table->records[row];

        (void)sm_read_i32_as(reader, column->element, &record->min_reward_value);
      }

      break;

    default:
      /* A column this build has no member for: added to the schema after the code
       * was generated, or removed from the code while the data still carries it.
       * The block's declared length is the whole of what skipping needs to know. */
      (void)sm_skip(reader, column->byte_length);
      break;
    }

    if (!sm_check_block_end(reader, column, block_end))
      return false;
  }

  if (sm_failed(reader))
    return false;

  /* The keys can only be gathered once every column has been read: the column that
   * carries one arrives wherever the file happens to put it. */
  for (row = 0; row < table->count; ++row) {
    table->by_id[row].key = table->records[row].id;
    table->by_id[row].position = row;
  }

  sm_index_sort(table->by_id, table->count);

  return true;
}

bool Rescue_SDDungeonRewardTableLoad(Rescue_SDDungeonRewardTableTable_t* table, const char* filename,
                   char* error, size_t error_size) {
  uint8_t* bytes = NULL;
  int32_t length = 0;
  sm_reader reader;
  bool ok;

  /* Loaded beside whatever the table is already holding, arena and all, and swapped in
   * only if it worked. That is what makes a reload safe: reading a patched file that
   * turns out to be truncated - or to carry a column this build cannot read - leaves the
   * rows the caller has pointers to exactly where they were.
   *
   * It also has to be this way round for the memory. The table owns its arena, and
   * zeroing the table before reading - which is what this did - dropped that arena
   * without freeing it, so every reload leaked the whole of the previous load. */
  Rescue_SDDungeonRewardTableTable_t loaded;

  memset(&loaded, 0, sizeof loaded);

  if (!sm_read_all_bytes(filename, &bytes, &length)) {
    sm_copy_error(error, error_size, filename, "cannot be read");
    return false;
  }

  sm_reader_init(&reader, bytes, length, &loaded.arena);

  ok = Rescue_SDDungeonRewardTableParse(&loaded, &reader);

  if (ok) {
    /* The previous load goes now, and not before: its arena holds the strings and
     * arrays the caller was reading up to this line. */
    Rescue_SDDungeonRewardTableFree(table);
    *table = loaded;
  } else {
    sm_copy_error(error, error_size, filename, sm_error(&reader));

    /* Nothing half-read is kept, and the table is untouched. */
    Rescue_SDDungeonRewardTableFree(&loaded);
  }

  /* The strings were copied into the arena, so the file buffer goes either way. */
  sm_free_bytes(bytes);

  return ok;
}

void Rescue_SDDungeonRewardTableFree(Rescue_SDDungeonRewardTableTable_t* table) {
  sm_arena_free(&table->arena);

  memset(table, 0, sizeof *table);
}

const Rescue_SDDungeonRewardTableRecord_t* Rescue_SDDungeonRewardTableFindById(
  const Rescue_SDDungeonRewardTableTable_t* table, int32_t key) {
  int32_t position = sm_index_find(table->by_id, table->count, key);

  return position < 0 ? NULL : &table->records[position];
}

bool Rescue_SDDungeonRewardTableContainsId(
  const Rescue_SDDungeonRewardTableTable_t* table, int32_t key) {
  return sm_index_find(table->by_id, table->count, key) >= 0;
}
