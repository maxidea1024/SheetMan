/* ------------------------------------------------------------------------------
 * Generated by SheetMan. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef RESCUE_SD_TRAINING_LEVEL_TABLE_H
#define RESCUE_SD_TRAINING_LEVEL_TABLE_H

#include "sheetman/sheetman_lite_binary_reader.h"

#include "Rescue_Forward.h"

#ifdef __cplusplus
extern "C" {
#endif
/* Generated from project-rescue-xlsxs/SD_Contens.xlsx : SDTrainingLevelTable : A1
 */
struct Rescue_SDTrainingLevelTableRecord_t {
  /* SDTrainingLevel_Table */
  int32_t id;
  /* 영문명 */
  const char* name;
  /* 한글명 */
  const char* level_name;
  /* 필요 재료값 */
  int32_t currency_value;
  /* 누적 재료값 */
  int32_t currency_result;
  /* 체력 고정 비율 */
  float mhp_growth;
  /* 체력 비율 누적 값 */
  float mhp_total;
  /* 방어력 고정 비율 */
  float def_growth;
  /* 방어력 비율 누적 값 */
  float def_total;
  /* 공통 훈련 스테이지 진행도 */
  int32_t common_unlock_stage_id;
};

/* Every row of SDTrainingLevelTable.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct Rescue_SDTrainingLevelTableTable_t {
  Rescue_SDTrainingLevelTableRecord_t* records;
  int32_t count;

  sm_index_entry* by_id;
  sm_arena arena;
} Rescue_SDTrainingLevelTableTable_t;

/* Loads the table from a .scb file written by SheetMan.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool Rescue_SDTrainingLevelTableLoad(Rescue_SDTrainingLevelTableTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void Rescue_SDTrainingLevelTableFree(Rescue_SDTrainingLevelTableTable_t* table);

/* The row with the given Id, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const Rescue_SDTrainingLevelTableRecord_t* Rescue_SDTrainingLevelTableFindById(
  const Rescue_SDTrainingLevelTableTable_t* table, int32_t key);

/* Whether the table holds a row with this Id. */
bool Rescue_SDTrainingLevelTableContainsId(
  const Rescue_SDTrainingLevelTableTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* RESCUE_SD_TRAINING_LEVEL_TABLE_H */
