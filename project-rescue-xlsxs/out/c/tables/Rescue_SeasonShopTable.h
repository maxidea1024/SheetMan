/* ------------------------------------------------------------------------------
 * Generated by SheetMan. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef RESCUE_SEASON_SHOP_TABLE_H
#define RESCUE_SEASON_SHOP_TABLE_H

#include "sheetman/sheetman_lite_binary_reader.h"

#include "Rescue_Forward.h"
#include "enums/Rescue_EnumShopType.h"
#include "enums/Rescue_EnumCycleType.h"
#include "enums/Rescue_EnumCurrencyType.h"

#ifdef __cplusplus
extern "C" {
#endif
/* Generated from project-rescue-xlsxs/ShopData.xlsx : SeasonShopTable : A1
 */
struct Rescue_SeasonShopTableRecord_t {
  /* SeasonShop_Table */
  int32_t id;
  /* 영문명 */
  const char* name;
  /* 한글명 */
  const char* product_name;
  /* 상점 타입 */
  Rescue_ShopType_t shop_type;
  /* 슬롯 ID */
  int32_t shop_slot_id;
  /* 출력 우선순위 */
  int32_t priority;
  /* 포함 아이템 ID */
  int32_t* item_id;
  int32_t item_id_count;
  /* 포함 아이템 수 */
  int32_t* item_value;
  int32_t item_value_count;
  /* 구매 주기 타입 */
  Rescue_CycleType_t cycle_type;
  /* 구매 제한 횟수 */
  int32_t limit_value;
  /* 소모 재화 */
  Rescue_CurrencyType_t currency_type;
  /* 가격값 */
  int32_t price_value;
  /* 해금 조건 ID */
  int32_t condition_id;
  /* 아이콘 경로 */
  const char* icon_path;
};

/* Every row of SeasonShopTable.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct Rescue_SeasonShopTableTable_t {
  Rescue_SeasonShopTableRecord_t* records;
  int32_t count;

  sm_index_entry* by_id;
  sm_arena arena;
} Rescue_SeasonShopTableTable_t;

/* Loads the table from a .table file written by SheetMan.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool Rescue_SeasonShopTableLoad(Rescue_SeasonShopTableTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void Rescue_SeasonShopTableFree(Rescue_SeasonShopTableTable_t* table);

/* The row with the given Id, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const Rescue_SeasonShopTableRecord_t* Rescue_SeasonShopTableFindById(
  const Rescue_SeasonShopTableTable_t* table, int32_t key);

/* Whether the table holds a row with this Id. */
bool Rescue_SeasonShopTableContainsId(
  const Rescue_SeasonShopTableTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* RESCUE_SEASON_SHOP_TABLE_H */
