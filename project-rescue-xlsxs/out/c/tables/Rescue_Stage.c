/* ------------------------------------------------------------------------------
 * Generated by SheetMan. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#include "tables/Rescue_Stage.h"

#include <string.h>
static bool Rescue_StageParse(Rescue_StageTable_t* table, sm_reader* reader) {
  int32_t row;
  int32_t at;
  sm_column* columns = NULL;
  int32_t column_count = 0;
  sm_cursor cursor;

  if (!sm_read_table_header(reader, &table->count, &columns, &column_count))
    return false;

  if (table->count > 0) {
    table->records = (Rescue_StageRecord_t*)sm_arena_alloc(
      &table->arena, (size_t)table->count * sizeof *table->records);

    if (table->records == NULL)
      return sm_fail_with(reader, "out of memory allocating the rows");

    table->by_id = (sm_index_entry*)sm_arena_alloc(
      &table->arena, (size_t)table->count * sizeof *table->by_id);

    if (table->by_id == NULL)
      return sm_fail_with(reader, "out of memory allocating the Id index");

  }


  /* The arena hands back zeroed memory, so every member starts at its zero - which for
   * a string is NULL, and NULL is what reaches printf when a column the file does not
   * carry is read. An empty string instead: a literal, so there is nothing to free and
   * nothing to allocate. */
  for (row = 0; row < table->count; ++row) {
    Rescue_StageRecord_t* record = &table->records[row];

    record->name = "";

    record->stage_name = "";

    record->asset_data_path = "";
  }

  /* Column by column, matched by tag rather than by position: a column this build has
   * no member for is skipped by its declared length, and one whose type no longer fits
   * the member stops the read naming the field. Rows arrive with every member at an
   * empty value, so one the file has nothing for keeps it. */
  for (at = 0; at < column_count && !sm_failed(reader); ++at) {
    const sm_column* column = &columns[at];
    int32_t block_end = reader->position + column->byte_length;

    switch (column->tag) {

    case 1:
      (void)sm_check_column(reader, column, "Stage.Id", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "Stage.Id");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_StageRecord_t* record = &table->records[row];

        (void)sm_cursor_next_i32(&cursor, &record->id);
      }

      break;

    case 2:
      (void)sm_check_column(reader, column, "Stage.Name", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_STRING));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "Stage.Name");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_StageRecord_t* record = &table->records[row];

        (void)sm_cursor_next_string(&cursor, &record->name);
      }

      break;

    case 3:
      (void)sm_check_column(reader, column, "Stage.StageName", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_STRING));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "Stage.StageName");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_StageRecord_t* record = &table->records[row];

        (void)sm_cursor_next_string(&cursor, &record->stage_name);
      }

      break;

    case 4:
      (void)sm_check_column(reader, column, "Stage.AssetDataPath", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_STRING));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "Stage.AssetDataPath");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_StageRecord_t* record = &table->records[row];

        (void)sm_cursor_next_string(&cursor, &record->asset_data_path);
      }

      break;

    case 5:
      (void)sm_check_column(reader, column, "Stage.SpawnIds", SM_KIND_VAR_ARRAY, 0, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));


      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_StageRecord_t* record = &table->records[row];
        int32_t element_count = 0;
        int32_t element;

        (void)sm_read_counter32(reader, &element_count);

        /* The count came off the wire, and one element is at least one byte. */
        if (!sm_row_count_is_plausible(reader, element_count, 1))
          return sm_fail_with(reader, "an array is longer than the file can hold");

        record->spawn_ids_count = element_count;
        record->spawn_ids = (int32_t*)sm_arena_alloc(
          &table->arena, (size_t)element_count * sizeof *record->spawn_ids);

        if (element_count > 0 && record->spawn_ids == NULL)
          return sm_fail_with(reader, "out of memory allocating an array");

        for (element = 0; element < element_count && !sm_failed(reader); ++element)
          (void)sm_read_i32_as(reader, column->element, &record->spawn_ids[element]);
      }

      break;

    case 6:
      (void)sm_check_column(reader, column, "Stage.SpawnPointCount", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "Stage.SpawnPointCount");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_StageRecord_t* record = &table->records[row];

        (void)sm_cursor_next_i32(&cursor, &record->spawn_point_count);
      }

      break;

    case 7:
      (void)sm_check_column(reader, column, "Stage.SpawnMaxCount", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "Stage.SpawnMaxCount");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_StageRecord_t* record = &table->records[row];

        (void)sm_cursor_next_i32(&cursor, &record->spawn_max_count);
      }

      break;

    case 8:
      (void)sm_check_column(reader, column, "Stage.SpawnNextTime", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_F32));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "Stage.SpawnNextTime");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_StageRecord_t* record = &table->records[row];

        (void)sm_cursor_next_f32(&cursor, &record->spawn_next_time);
      }

      break;

    case 9:
      (void)sm_check_column(reader, column, "Stage.StageClearCount", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "Stage.StageClearCount");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_StageRecord_t* record = &table->records[row];

        (void)sm_cursor_next_i32(&cursor, &record->stage_clear_count);
      }

      break;

    case 10:
      (void)sm_check_column(reader, column, "Stage.BossID", SM_KIND_VAR_ARRAY, 0, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));


      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_StageRecord_t* record = &table->records[row];
        int32_t element_count = 0;
        int32_t element;

        (void)sm_read_counter32(reader, &element_count);

        /* The count came off the wire, and one element is at least one byte. */
        if (!sm_row_count_is_plausible(reader, element_count, 1))
          return sm_fail_with(reader, "an array is longer than the file can hold");

        record->boss_id_count = element_count;
        record->boss_id = (int32_t*)sm_arena_alloc(
          &table->arena, (size_t)element_count * sizeof *record->boss_id);

        if (element_count > 0 && record->boss_id == NULL)
          return sm_fail_with(reader, "out of memory allocating an array");

        for (element = 0; element < element_count && !sm_failed(reader); ++element)
          (void)sm_read_i32_as(reader, column->element, &record->boss_id[element]);
      }

      break;

    case 11:
      (void)sm_check_column(reader, column, "Stage.AtkUpPercent", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_F32));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "Stage.AtkUpPercent");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_StageRecord_t* record = &table->records[row];

        (void)sm_cursor_next_f32(&cursor, &record->atk_up_percent);
      }

      break;

    case 12:
      (void)sm_check_column(reader, column, "Stage.DefUpPercent", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_F32));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "Stage.DefUpPercent");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_StageRecord_t* record = &table->records[row];

        (void)sm_cursor_next_f32(&cursor, &record->def_up_percent);
      }

      break;

    case 13:
      (void)sm_check_column(reader, column, "Stage.MHPUpPercent", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_F32));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "Stage.MHPUpPercent");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_StageRecord_t* record = &table->records[row];

        (void)sm_cursor_next_f32(&cursor, &record->mhp_up_percent);
      }

      break;

    case 14:
      (void)sm_check_column(reader, column, "Stage.BossAtkUpPercent", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_F32));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "Stage.BossAtkUpPercent");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_StageRecord_t* record = &table->records[row];

        (void)sm_cursor_next_f32(&cursor, &record->boss_atk_up_percent);
      }

      break;

    case 15:
      (void)sm_check_column(reader, column, "Stage.BossDefUpPercent", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_F32));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "Stage.BossDefUpPercent");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_StageRecord_t* record = &table->records[row];

        (void)sm_cursor_next_f32(&cursor, &record->boss_def_up_percent);
      }

      break;

    case 16:
      (void)sm_check_column(reader, column, "Stage.BossMHPUpPercent", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_F32));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "Stage.BossMHPUpPercent");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_StageRecord_t* record = &table->records[row];

        (void)sm_cursor_next_f32(&cursor, &record->boss_mhp_up_percent);
      }

      break;

    case 17:
      (void)sm_check_column(reader, column, "Stage.StageDropListID", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "Stage.StageDropListID");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_StageRecord_t* record = &table->records[row];

        (void)sm_cursor_next_i32(&cursor, &record->stage_drop_list_id);
      }

      break;

    case 18:
      (void)sm_check_column(reader, column, "Stage.StageBGMID", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "Stage.StageBGMID");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_StageRecord_t* record = &table->records[row];

        (void)sm_cursor_next_i32(&cursor, &record->stage_bgmid);
      }

      break;

    case 19:
      (void)sm_check_column(reader, column, "Stage.BossStageBGMID", SM_KIND_SCALAR, 1, SM_ELEMENT_MASK(SM_ELEMENT_I32) | SM_ELEMENT_MASK(SM_ELEMENT_VARINT));

      (void)sm_cursor_init(&cursor, reader, column, table->count, "Stage.BossStageBGMID");

      for (row = 0; row < table->count && !sm_failed(reader); ++row) {
        Rescue_StageRecord_t* record = &table->records[row];

        (void)sm_cursor_next_i32(&cursor, &record->boss_stage_bgmid);
      }

      break;

    default:
      /* A column this build has no member for: added to the schema after the code
       * was generated, or removed from the code while the data still carries it.
       * The block's declared length is the whole of what skipping needs to know. */
      (void)sm_skip(reader, column->byte_length);
      break;
    }

    if (!sm_check_block_end(reader, column, block_end))
      return false;
  }

  if (sm_failed(reader))
    return false;

  /* The keys can only be gathered once every column has been read: the column that
   * carries one arrives wherever the file happens to put it. */
  for (row = 0; row < table->count; ++row) {
    table->by_id[row].key = table->records[row].id;
    table->by_id[row].position = row;
  }

  sm_index_sort(table->by_id, table->count);

  return true;
}

bool Rescue_StageLoad(Rescue_StageTable_t* table, const char* filename,
                   char* error, size_t error_size) {
  uint8_t* bytes = NULL;
  int32_t length = 0;
  sm_reader reader;
  bool ok;

  /* Loaded beside whatever the table is already holding, arena and all, and swapped in
   * only if it worked. That is what makes a reload safe: reading a patched file that
   * turns out to be truncated - or to carry a column this build cannot read - leaves the
   * rows the caller has pointers to exactly where they were.
   *
   * It also has to be this way round for the memory. The table owns its arena, and
   * zeroing the table before reading - which is what this did - dropped that arena
   * without freeing it, so every reload leaked the whole of the previous load. */
  Rescue_StageTable_t loaded;

  memset(&loaded, 0, sizeof loaded);

  if (!sm_read_all_bytes(filename, &bytes, &length)) {
    sm_copy_error(error, error_size, filename, "cannot be read");
    return false;
  }

  sm_reader_init(&reader, bytes, length, &loaded.arena);

  ok = Rescue_StageParse(&loaded, &reader);

  if (ok) {
    /* The previous load goes now, and not before: its arena holds the strings and
     * arrays the caller was reading up to this line. */
    Rescue_StageFree(table);
    *table = loaded;
  } else {
    sm_copy_error(error, error_size, filename, sm_error(&reader));

    /* Nothing half-read is kept, and the table is untouched. */
    Rescue_StageFree(&loaded);
  }

  /* The strings were copied into the arena, so the file buffer goes either way. */
  sm_free_bytes(bytes);

  return ok;
}

void Rescue_StageFree(Rescue_StageTable_t* table) {
  sm_arena_free(&table->arena);

  memset(table, 0, sizeof *table);
}

const Rescue_StageRecord_t* Rescue_StageFindById(
  const Rescue_StageTable_t* table, int32_t key) {
  int32_t position = sm_index_find(table->by_id, table->count, key);

  return position < 0 ? NULL : &table->records[position];
}

bool Rescue_StageContainsId(
  const Rescue_StageTable_t* table, int32_t key) {
  return sm_index_find(table->by_id, table->count, key) >= 0;
}
