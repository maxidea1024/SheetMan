/* ------------------------------------------------------------------------------
 * Generated by SheetMan. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef RESCUE_STAGE_H
#define RESCUE_STAGE_H

#include "sheetman/sheetman_scb_reader.h"

#include "Rescue_Forward.h"

#ifdef __cplusplus
extern "C" {
#endif
/* Generated from project-rescue-xlsxs/MasterData.xlsx : StageTable : A1
 */
struct Rescue_StageRecord_t {
  /* Stage_Table */
  int32_t id;
  /* 영문 */
  const char* name;
  /* 한글 */
  const char* stage_name;
  /* AssetData 경로 */
  const char* asset_data_path;
  /* 생성 몬스터 ID */
  int32_t* spawn_ids;
  int32_t spawn_ids_count;
  /* 위치 생성 가능 횟수 */
  int32_t spawn_point_count;
  /* 최대 생성 수 */
  int32_t spawn_max_count;
  /* 다음 포인터 생성 시간 */
  float spawn_next_time;
  /* 클리어 조건 */
  int32_t stage_clear_count;
  /* 보스 ID */
  int32_t* boss_id;
  int32_t boss_id_count;
  /* 일반 ATK증가 값 */
  float atk_up_percent;
  /* 일반 DEF증가 값 */
  float def_up_percent;
  /* 일반 HP증가 값 */
  float mhp_up_percent;
  /* 보스 ATK증가 값 */
  float boss_atk_up_percent;
  /* 보스 DEF증가 값 */
  float boss_def_up_percent;
  /* 보스 HP증가 값 */
  float boss_mhp_up_percent;
  /* 드랍 테이블 */
  int32_t stage_drop_list_id;
  /* 스테이지 BGM */
  int32_t stage_bgmid;
  /* 보스 스테이지 BGM */
  int32_t boss_stage_bgmid;
};

/* Every row of Stage.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct Rescue_StageTable_t {
  Rescue_StageRecord_t* records;
  int32_t count;

  sm_index_entry* by_id;
  sm_arena arena;
} Rescue_StageTable_t;

/* Loads the table from a .scb file written by SheetMan.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool Rescue_StageLoad(Rescue_StageTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void Rescue_StageFree(Rescue_StageTable_t* table);

/* The row with the given Id, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const Rescue_StageRecord_t* Rescue_StageFindById(
  const Rescue_StageTable_t* table, int32_t key);

/* Whether the table holds a row with this Id. */
bool Rescue_StageContainsId(
  const Rescue_StageTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* RESCUE_STAGE_H */
