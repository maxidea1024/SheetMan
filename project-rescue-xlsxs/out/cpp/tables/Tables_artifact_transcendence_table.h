// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

#ifndef SHEETMAN_GENERATED_TABLES_ARTIFACT_TRANSCENDENCE_TABLE_H
#define SHEETMAN_GENERATED_TABLES_ARTIFACT_TRANSCENDENCE_TABLE_H

#include <cstddef>
#include <cstdint>
#include <string>
#include <unordered_map>
#include <vector>

#include "sheetman/lite_binary_reader.h"
#include "Tables_forward.h"
#include "enums/Tables_enum_grade_type.h"
#include "enums/Tables_enum_currency_type.h"

namespace rescue {
// Generated from project-rescue-xlsxs/MasterData.xlsx : ArtifactTranscendenceTable : A1
struct ArtifactTranscendenceTableRecord {
  /// Artifact_Transcend_ID
  std::int32_t id = 0;
  /// 영문
  std::string name;
  /// 한글이름
  std::string name_kr;
  /// 등급
  GradeType grade_type = static_cast<GradeType>(0);
  /// 초월 단계
  std::int32_t transcend_step = 0;
  /// 초월 최대 레벨
  std::int32_t max_level = 0;
  /// 초월 특수 재료 타입
  CurrencyType material_type = static_cast<CurrencyType>(0);
  /// 초월 특수 재료 개수
  std::int32_t material_count = 0;
  /// 초월시 스탯 증가율
  float power_multiplier = 0.0f;
  /// 다음 성급 Index
  std::int32_t next_step_id = 0;
};

class ArtifactTranscendenceTableTable {
 public:
  const std::vector<ArtifactTranscendenceTableRecord>& records() const { return records_; }

  /// The row with this Id, or nullptr when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  const ArtifactTranscendenceTableRecord* find_by_id(std::int32_t key) const {
    const auto it = by_id_.find(key);
    return it == by_id_.end() ? nullptr : &records_[it->second];
  }

  /// The row with this Id, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading `get_by_id(key)->name` at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  const ArtifactTranscendenceTableRecord& get_by_id_or_throw(std::int32_t key) const {
    const ArtifactTranscendenceTableRecord* found = find_by_id(key);

    if (found == nullptr) {
      throw sheetman::RecordNotFound(
        std::string("there is no record in table `ArtifactTranscendenceTable` that corresponds to "
                    "field `Id` value ") + std::to_string(key));
    }

    return *found;
  }

  /// Whether the table holds a row with this Id.
  bool contains_id(std::int32_t key) const {
    return by_id_.find(key) != by_id_.end();
  }

  /// Loads the table from a .table file written by SheetMan.
  void read(const std::string& filename) {
    const std::vector<std::uint8_t> buffer = sheetman::read_all_bytes(filename);
    sheetman::LiteBinaryReader reader(buffer);

    // Column by column, matched by tag rather than position: a column this build
    // does not know is skipped by its block length, and one whose type changed
    // incompatibly fails naming the field.
    const sheetman::Header header = sheetman::read_table_header(reader);
    const std::size_t row_count = static_cast<std::size_t>(header.row_count);

    // Read into storage of its own and swapped in at the end: reading a table that is
    // already loaded is a refresh, and one that throws part way through - a truncated
    // file, a column this build cannot read - has to leave the rows already there.
    std::vector<ArtifactTranscendenceTableRecord> records;
    records.resize(row_count);

    for (const sheetman::Column& column : header.columns) {
      const std::size_t block_end = reader.position() + static_cast<std::size_t>(column.byte_length);

      switch (column.tag) {
        case 1: {
          sheetman::check_column(column, "ArtifactTranscendenceTable.Id", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.id);
          }
          break;
        }
        case 2: {
          sheetman::check_column(column, "ArtifactTranscendenceTable.Name", sheetman::kKindScalar, 1, {sheetman::kElementString});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.name);
          }
          break;
        }
        case 3: {
          sheetman::check_column(column, "ArtifactTranscendenceTable.NameKR", sheetman::kKindScalar, 1, {sheetman::kElementString});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.name_kr);
          }
          break;
        }
        case 4: {
          sheetman::check_column(column, "ArtifactTranscendenceTable.GradeType", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.grade_type);
          }
          break;
        }
        case 5: {
          sheetman::check_column(column, "ArtifactTranscendenceTable.TranscendStep", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.transcend_step);
          }
          break;
        }
        case 6: {
          sheetman::check_column(column, "ArtifactTranscendenceTable.MaxLevel", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.max_level);
          }
          break;
        }
        case 7: {
          sheetman::check_column(column, "ArtifactTranscendenceTable.MaterialType", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.material_type);
          }
          break;
        }
        case 8: {
          sheetman::check_column(column, "ArtifactTranscendenceTable.MaterialCount", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.material_count);
          }
          break;
        }
        case 9: {
          sheetman::check_column(column, "ArtifactTranscendenceTable.PowerMultiplier", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.power_multiplier);
          }
          break;
        }
        case 10: {
          sheetman::check_column(column, "ArtifactTranscendenceTable.NextStepID", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.next_step_id);
          }
          break;
        }
        default:
          // A column added after this code was generated.
          reader.skip(column.byte_length);
          break;
      }

      sheetman::check_block_end(reader, column, block_end);
    }

    publish(std::move(records));
  }

 private:
  friend class Tables;

  /// One whole load, in place of the previous one.
  ///
  /// The indexes are built here rather than by the caller, because they have to arrive
  /// with the rows: a table holding this load's rows and the last one's index would
  /// answer a lookup with a row that is no longer at that position.
  void publish(std::vector<ArtifactTranscendenceTableRecord>&& records) {
    std::unordered_map<std::int32_t, std::size_t> id_map;
    id_map.reserve(records.size());
    for (std::size_t i = 0; i < records.size(); ++i) {
      id_map.emplace(records[i].id, i);
    }

    records_ = std::move(records);
    by_id_ = std::move(id_map);
  }

  std::vector<ArtifactTranscendenceTableRecord> records_;
  std::unordered_map<std::int32_t, std::size_t> by_id_;
};

}  // namespace rescue

#endif  // SHEETMAN_GENERATED_TABLES_ARTIFACT_TRANSCENDENCE_TABLE_H
