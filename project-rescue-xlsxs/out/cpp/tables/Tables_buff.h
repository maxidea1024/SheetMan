// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

#ifndef SHEETMAN_GENERATED_TABLES_BUFF_H
#define SHEETMAN_GENERATED_TABLES_BUFF_H

#include <cstddef>
#include <cstdint>
#include <string>
#include <unordered_map>
#include <vector>

#include "sheetman/scb_reader.h"
#include "Tables_forward.h"
#include "enums/Tables_enum_skill_type.h"
#include "enums/Tables_enum_stat_type.h"
#include "enums/Tables_enum_attribute_type.h"
#include "enums/Tables_enum_target_type.h"
#include "enums/Tables_enum_buff_condition_type.h"

namespace rescue {
// Generated from project-rescue-xlsxs/MasterData.xlsx : BuffTable : A1
struct BuffRecord {
  /// ID
  std::int32_t id = 0;
  /// SkillTable에서 Name을 불러와서 사용
  std::string buff_name;
  /// 한글 명
  std::string name;
  /// 버프 타입_메모확인
  SkillType skill_type = static_cast<SkillType>(0);
  /// 어떤 종류의 버프 인지
  StatType stat_type = static_cast<StatType>(0);
  /// 버프 우선 적용 순위
  std::int32_t priority = 0;
  /// 버프 속성
  AttributeType attribute_type = static_cast<AttributeType>(0);
  /// 타겟 설정
  TargetType target_type = static_cast<TargetType>(0);
  /// 상태에 따른 발동 조건
  BuffConditionType buff_condition_type = static_cast<BuffConditionType>(0);
  /// 증가되는 값(상수)
  std::int32_t buff_value = 0;
  /// 증가되는 값(%)
  float buff_value_rate = 0.0f;
  /// 버프 시간
  std::int32_t buff_time = 0;
  /// 버프 틱 시간
  std::int32_t buff_tick_time = 0;
  /// 버프 최대 스택
  std::int32_t buff_max_stack = 0;
  /// 버프 쿨타임 1초 = 1000
  float buff_cool_down_time = 0.0f;
  /// 버프 표시 유무 FALSE/TRUE
  bool is_visible = false;
  /// 버프 아이콘 경로
  std::string icon_path;
  /// 버프 튤팁
  std::string description;
};

class BuffTable {
 public:
  const std::vector<BuffRecord>& records() const { return records_; }

  /// The row with this Id, or nullptr when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  const BuffRecord* find_by_id(std::int32_t key) const {
    const auto it = by_id_.find(key);
    return it == by_id_.end() ? nullptr : &records_[it->second];
  }

  /// The row with this Id, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading `get_by_id(key)->name` at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  const BuffRecord& get_by_id_or_throw(std::int32_t key) const {
    const BuffRecord* found = find_by_id(key);

    if (found == nullptr) {
      throw sheetman::RecordNotFound(
        std::string("there is no record in table `Buff` that corresponds to "
                    "field `Id` value ") + std::to_string(key));
    }

    return *found;
  }

  /// Whether the table holds a row with this Id.
  bool contains_id(std::int32_t key) const {
    return by_id_.find(key) != by_id_.end();
  }

  /// Loads the table from a .scb file written by SheetMan.
  void read(const std::string& filename) {
    const std::vector<std::uint8_t> buffer = sheetman::read_all_bytes(filename);
    sheetman::ScbReader reader(buffer);

    // Column by column, matched by tag rather than position: a column this build
    // does not know is skipped by its block length, and one whose type changed
    // incompatibly fails naming the field.
    const sheetman::Header header = sheetman::read_table_header(reader);
    const std::size_t row_count = static_cast<std::size_t>(header.row_count);

    // Read into storage of its own and swapped in at the end: reading a table that is
    // already loaded is a refresh, and one that throws part way through - a truncated
    // file, a column this build cannot read - has to leave the rows already there.
    std::vector<BuffRecord> records;
    records.resize(row_count);

    for (const sheetman::Column& column : header.columns) {
      const std::size_t block_end = reader.position() + static_cast<std::size_t>(column.byte_length);

      switch (column.tag) {
        case 1: {
          sheetman::check_column(column, "Buff.Id", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Buff.Id");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.id = cursor.next_i32();
          }
          break;
        }
        case 2: {
          sheetman::check_column(column, "Buff.BuffName", sheetman::kKindScalar, 1, {sheetman::kElementString});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Buff.BuffName");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.buff_name = cursor.next_string();
          }
          break;
        }
        case 3: {
          sheetman::check_column(column, "Buff.Name", sheetman::kKindScalar, 1, {sheetman::kElementString});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Buff.Name");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.name = cursor.next_string();
          }
          break;
        }
        case 4: {
          sheetman::check_column(column, "Buff.SkillType", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Buff.SkillType");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.skill_type = static_cast<SkillType>(cursor.next_i32());
          }
          break;
        }
        case 5: {
          sheetman::check_column(column, "Buff.StatType", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Buff.StatType");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.stat_type = static_cast<StatType>(cursor.next_i32());
          }
          break;
        }
        case 6: {
          sheetman::check_column(column, "Buff.Priority", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Buff.Priority");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.priority = cursor.next_i32();
          }
          break;
        }
        case 7: {
          sheetman::check_column(column, "Buff.AttributeType", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Buff.AttributeType");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.attribute_type = static_cast<AttributeType>(cursor.next_i32());
          }
          break;
        }
        case 8: {
          sheetman::check_column(column, "Buff.TargetType", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Buff.TargetType");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.target_type = static_cast<TargetType>(cursor.next_i32());
          }
          break;
        }
        case 9: {
          sheetman::check_column(column, "Buff.BuffConditionType", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Buff.BuffConditionType");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.buff_condition_type = static_cast<BuffConditionType>(cursor.next_i32());
          }
          break;
        }
        case 10: {
          sheetman::check_column(column, "Buff.BuffValue", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Buff.BuffValue");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.buff_value = cursor.next_i32();
          }
          break;
        }
        case 11: {
          sheetman::check_column(column, "Buff.BuffValueRate", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.buff_value_rate);
          }
          break;
        }
        case 12: {
          sheetman::check_column(column, "Buff.BuffTime", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Buff.BuffTime");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.buff_time = cursor.next_i32();
          }
          break;
        }
        case 13: {
          sheetman::check_column(column, "Buff.BuffTickTime", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Buff.BuffTickTime");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.buff_tick_time = cursor.next_i32();
          }
          break;
        }
        case 14: {
          sheetman::check_column(column, "Buff.BuffMaxStack", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Buff.BuffMaxStack");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.buff_max_stack = cursor.next_i32();
          }
          break;
        }
        case 15: {
          sheetman::check_column(column, "Buff.BuffCoolDownTime", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.buff_cool_down_time);
          }
          break;
        }
        case 16: {
          sheetman::check_column(column, "Buff.IsVisible", sheetman::kKindScalar, 1, {sheetman::kElementBool});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.is_visible);
          }
          break;
        }
        case 17: {
          sheetman::check_column(column, "Buff.IconPath", sheetman::kKindScalar, 1, {sheetman::kElementString});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Buff.IconPath");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.icon_path = cursor.next_string();
          }
          break;
        }
        case 18: {
          sheetman::check_column(column, "Buff.Description", sheetman::kKindScalar, 1, {sheetman::kElementString});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Buff.Description");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.description = cursor.next_string();
          }
          break;
        }
        default:
          // A column added after this code was generated.
          reader.skip(column.byte_length);
          break;
      }

      sheetman::check_block_end(reader, column, block_end);
    }

    publish(std::move(records));
  }

 private:
  friend class Tables;

  /// One whole load, in place of the previous one.
  ///
  /// The indexes are built here rather than by the caller, because they have to arrive
  /// with the rows: a table holding this load's rows and the last one's index would
  /// answer a lookup with a row that is no longer at that position.
  void publish(std::vector<BuffRecord>&& records) {
    std::unordered_map<std::int32_t, std::size_t> id_map;
    id_map.reserve(records.size());
    for (std::size_t i = 0; i < records.size(); ++i) {
      id_map.emplace(records[i].id, i);
    }

    records_ = std::move(records);
    by_id_ = std::move(id_map);
  }

  std::vector<BuffRecord> records_;
  std::unordered_map<std::int32_t, std::size_t> by_id_;
};

}  // namespace rescue

#endif  // SHEETMAN_GENERATED_TABLES_BUFF_H
