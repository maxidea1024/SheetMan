// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

#ifndef SHEETMAN_GENERATED_TABLES_CHARACTER_TABLE_H
#define SHEETMAN_GENERATED_TABLES_CHARACTER_TABLE_H

#include <cstddef>
#include <cstdint>
#include <string>
#include <unordered_map>
#include <vector>

#include "sheetman/lite_binary_reader.h"
#include "Tables_forward.h"
#include "enums/Tables_enum_character_type.h"
#include "enums/Tables_enum_grade_type.h"
#include "enums/Tables_enum_attribute_type.h"
#include "enums/Tables_enum_job_type.h"
#include "enums/Tables_enum_nation_type.h"

namespace rescue {
// Generated from project-rescue-xlsxs/MasterData.xlsx : CharacterTable : A1
struct CharacterTableRecord {
  /// Character_Table
  std::int32_t id = 0;
  /// 접두사
  std::string title_name;
  /// 후미사
  std::string name;
  /// 캐릭터 타입
  CharacterType character_type = static_cast<CharacterType>(0);
  /// 캐릭터 태생 등급
  GradeType grade = static_cast<GradeType>(0);
  /// 캐릭터 속성 타입
  AttributeType attribute_type = static_cast<AttributeType>(0);
  /// 직업
  JobType job_type = static_cast<JobType>(0);
  /// 태생 국가
  NationType nation = static_cast<NationType>(0);
  /// 기본 공격
  std::int64_t base_atk = 0;
  /// 기본 방어력
  std::int64_t base_def = 0;
  /// 기본 체력
  std::int64_t base_mhp = 0;
  /// 이동 속도
  float move_speed = 0.0f;
  /// 크리티컬 확률
  float crit_rate = 0.0f;
  /// 크리티컬 대미지
  float crit_dmg = 0.0f;
  /// 적 탐색 범위
  float search_range = 0.0f;
  /// 적 재 탐지 시간
  float re_find_time = 0.0f;
  /// 메인 케릭터와 멀어지는 거리
  float max_follow = 0.0f;
  /// 크기 설정
  float size_prefab = 0.0f;
  /// 열림 체크
  bool is_unlocked = false;
  /// 기본 공격1
  std::int32_t basic_attack1 = 0;
  /// 기본 공격2
  std::int32_t basic_attack2 = 0;
  /// 기본 공격3
  std::int32_t basic_attack3 = 0;
  /// 스킬 공격1
  std::int32_t active_skill1 = 0;
  /// 스킬 공격2
  std::int32_t active_skill2 = 0;
  /// 스킬 공격3
  std::int32_t active_skill3 = 0;
  /// 필살기1단계
  std::int32_t special_skill1 = 0;
  /// 필살기2단계
  std::int32_t special_skill2 = 0;
  /// 필살기3단계
  std::int32_t special_skill3 = 0;
  /// 필살기4단계
  std::int32_t special_skill4 = 0;
  /// 필살기5단계
  std::int32_t special_skill5 = 0;
  /// 패시브 버프1
  std::int32_t passive_buff1 = 0;
  /// 패시브 버프2
  std::int32_t passive_buff2 = 0;
  /// 패시브 버프3
  std::int32_t passive_buff3 = 0;
  /// 모델링 경로
  std::string prefab_path;
  /// SD 캐릭터 머테리얼 경로
  std::string sd_material_path;
  /// 아이콘 경로
  std::string icon_path;
  /// 설명
  std::string description;
};

class CharacterTableTable {
 public:
  const std::vector<CharacterTableRecord>& records() const { return records_; }

  /// The row with this Id, or nullptr when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  const CharacterTableRecord* find_by_id(std::int32_t key) const {
    const auto it = by_id_.find(key);
    return it == by_id_.end() ? nullptr : &records_[it->second];
  }

  /// The row with this Id, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading `get_by_id(key)->name` at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  const CharacterTableRecord& get_by_id_or_throw(std::int32_t key) const {
    const CharacterTableRecord* found = find_by_id(key);

    if (found == nullptr) {
      throw sheetman::RecordNotFound(
        std::string("there is no record in table `CharacterTable` that corresponds to "
                    "field `Id` value ") + std::to_string(key));
    }

    return *found;
  }

  /// Whether the table holds a row with this Id.
  bool contains_id(std::int32_t key) const {
    return by_id_.find(key) != by_id_.end();
  }

  /// Loads the table from a .scb file written by SheetMan.
  void read(const std::string& filename) {
    const std::vector<std::uint8_t> buffer = sheetman::read_all_bytes(filename);
    sheetman::LiteBinaryReader reader(buffer);

    // Column by column, matched by tag rather than position: a column this build
    // does not know is skipped by its block length, and one whose type changed
    // incompatibly fails naming the field.
    const sheetman::Header header = sheetman::read_table_header(reader);
    const std::size_t row_count = static_cast<std::size_t>(header.row_count);

    // Read into storage of its own and swapped in at the end: reading a table that is
    // already loaded is a refresh, and one that throws part way through - a truncated
    // file, a column this build cannot read - has to leave the rows already there.
    std::vector<CharacterTableRecord> records;
    records.resize(row_count);

    for (const sheetman::Column& column : header.columns) {
      const std::size_t block_end = reader.position() + static_cast<std::size_t>(column.byte_length);

      switch (column.tag) {
        case 1: {
          sheetman::check_column(column, "CharacterTable.Id", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.id);
          }
          break;
        }
        case 2: {
          sheetman::check_column(column, "CharacterTable.TitleName", sheetman::kKindScalar, 1, {sheetman::kElementString});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.title_name);
          }
          break;
        }
        case 3: {
          sheetman::check_column(column, "CharacterTable.Name", sheetman::kKindScalar, 1, {sheetman::kElementString});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.name);
          }
          break;
        }
        case 4: {
          sheetman::check_column(column, "CharacterTable.CharacterType", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.character_type);
          }
          break;
        }
        case 5: {
          sheetman::check_column(column, "CharacterTable.Grade", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.grade);
          }
          break;
        }
        case 6: {
          sheetman::check_column(column, "CharacterTable.AttributeType", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.attribute_type);
          }
          break;
        }
        case 7: {
          sheetman::check_column(column, "CharacterTable.JobType", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.job_type);
          }
          break;
        }
        case 8: {
          sheetman::check_column(column, "CharacterTable.Nation", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.nation);
          }
          break;
        }
        case 9: {
          sheetman::check_column(column, "CharacterTable.BaseATK", sheetman::kKindScalar, 1, {sheetman::kElementI64, sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i64_as(column.element, record.base_atk);
          }
          break;
        }
        case 10: {
          sheetman::check_column(column, "CharacterTable.BaseDEF", sheetman::kKindScalar, 1, {sheetman::kElementI64, sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i64_as(column.element, record.base_def);
          }
          break;
        }
        case 11: {
          sheetman::check_column(column, "CharacterTable.BaseMHP", sheetman::kKindScalar, 1, {sheetman::kElementI64, sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i64_as(column.element, record.base_mhp);
          }
          break;
        }
        case 12: {
          sheetman::check_column(column, "CharacterTable.MoveSpeed", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.move_speed);
          }
          break;
        }
        case 13: {
          sheetman::check_column(column, "CharacterTable.CritRate", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.crit_rate);
          }
          break;
        }
        case 14: {
          sheetman::check_column(column, "CharacterTable.CritDmg", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.crit_dmg);
          }
          break;
        }
        case 15: {
          sheetman::check_column(column, "CharacterTable.SearchRange", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.search_range);
          }
          break;
        }
        case 16: {
          sheetman::check_column(column, "CharacterTable.ReFindTime", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.re_find_time);
          }
          break;
        }
        case 17: {
          sheetman::check_column(column, "CharacterTable.MaxFollow", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.max_follow);
          }
          break;
        }
        case 18: {
          sheetman::check_column(column, "CharacterTable.SizePrefab", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.size_prefab);
          }
          break;
        }
        case 19: {
          sheetman::check_column(column, "CharacterTable.IsUnlocked", sheetman::kKindScalar, 1, {sheetman::kElementBool});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.is_unlocked);
          }
          break;
        }
        case 20: {
          sheetman::check_column(column, "CharacterTable.BasicAttack1", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.basic_attack1);
          }
          break;
        }
        case 21: {
          sheetman::check_column(column, "CharacterTable.BasicAttack2", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.basic_attack2);
          }
          break;
        }
        case 22: {
          sheetman::check_column(column, "CharacterTable.BasicAttack3", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.basic_attack3);
          }
          break;
        }
        case 23: {
          sheetman::check_column(column, "CharacterTable.ActiveSkill1", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.active_skill1);
          }
          break;
        }
        case 24: {
          sheetman::check_column(column, "CharacterTable.ActiveSkill2", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.active_skill2);
          }
          break;
        }
        case 25: {
          sheetman::check_column(column, "CharacterTable.ActiveSkill3", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.active_skill3);
          }
          break;
        }
        case 26: {
          sheetman::check_column(column, "CharacterTable.SpecialSkill1", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.special_skill1);
          }
          break;
        }
        case 27: {
          sheetman::check_column(column, "CharacterTable.SpecialSkill2", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.special_skill2);
          }
          break;
        }
        case 28: {
          sheetman::check_column(column, "CharacterTable.SpecialSkill3", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.special_skill3);
          }
          break;
        }
        case 29: {
          sheetman::check_column(column, "CharacterTable.SpecialSkill4", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.special_skill4);
          }
          break;
        }
        case 30: {
          sheetman::check_column(column, "CharacterTable.SpecialSkill5", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.special_skill5);
          }
          break;
        }
        case 31: {
          sheetman::check_column(column, "CharacterTable.PassiveBuff1", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.passive_buff1);
          }
          break;
        }
        case 32: {
          sheetman::check_column(column, "CharacterTable.PassiveBuff2", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.passive_buff2);
          }
          break;
        }
        case 33: {
          sheetman::check_column(column, "CharacterTable.PassiveBuff3", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.passive_buff3);
          }
          break;
        }
        case 34: {
          sheetman::check_column(column, "CharacterTable.PrefabPath", sheetman::kKindScalar, 1, {sheetman::kElementString});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.prefab_path);
          }
          break;
        }
        case 35: {
          sheetman::check_column(column, "CharacterTable.SdMaterialPath", sheetman::kKindScalar, 1, {sheetman::kElementString});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.sd_material_path);
          }
          break;
        }
        case 36: {
          sheetman::check_column(column, "CharacterTable.IconPath", sheetman::kKindScalar, 1, {sheetman::kElementString});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.icon_path);
          }
          break;
        }
        case 37: {
          sheetman::check_column(column, "CharacterTable.Description", sheetman::kKindScalar, 1, {sheetman::kElementString});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.description);
          }
          break;
        }
        default:
          // A column added after this code was generated.
          reader.skip(column.byte_length);
          break;
      }

      sheetman::check_block_end(reader, column, block_end);
    }

    publish(std::move(records));
  }

 private:
  friend class Tables;

  /// One whole load, in place of the previous one.
  ///
  /// The indexes are built here rather than by the caller, because they have to arrive
  /// with the rows: a table holding this load's rows and the last one's index would
  /// answer a lookup with a row that is no longer at that position.
  void publish(std::vector<CharacterTableRecord>&& records) {
    std::unordered_map<std::int32_t, std::size_t> id_map;
    id_map.reserve(records.size());
    for (std::size_t i = 0; i < records.size(); ++i) {
      id_map.emplace(records[i].id, i);
    }

    records_ = std::move(records);
    by_id_ = std::move(id_map);
  }

  std::vector<CharacterTableRecord> records_;
  std::unordered_map<std::int32_t, std::size_t> by_id_;
};

}  // namespace rescue

#endif  // SHEETMAN_GENERATED_TABLES_CHARACTER_TABLE_H
