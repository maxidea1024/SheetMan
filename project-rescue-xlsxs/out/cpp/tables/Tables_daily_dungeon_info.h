// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

#ifndef SHEETMAN_GENERATED_TABLES_DAILY_DUNGEON_INFO_H
#define SHEETMAN_GENERATED_TABLES_DAILY_DUNGEON_INFO_H

#include <cstddef>
#include <cstdint>
#include <string>
#include <unordered_map>
#include <vector>

#include "sheetman/scb_reader.h"
#include "Tables_forward.h"
#include "enums/Tables_enum_dungeon_type.h"
#include "enums/Tables_enum_cycle_type.h"
#include "enums/Tables_enum_currency_type.h"

namespace rescue {
// Generated from project-rescue-xlsxs/DailyDungeon.xlsx : DailyDungeonInfoTable : A1
struct DailyDungeonInfoRecord {
  /// DailyDungeonInfo_Table
  std::int32_t id = 0;
  /// 영문명
  std::string name;
  /// 한글명
  std::string dungeon_name;
  /// 던전 타입
  DungeonType dungeon_type = static_cast<DungeonType>(0);
  /// 출력 우선 순위
  std::int32_t priority = 0;
  /// 던전 입장 타입
  CycleType cycle_type = static_cast<CycleType>(0);
  /// 해금 조건
  std::int32_t enter_condition = 0;
  /// 입장 재화
  CurrencyType enter_currency_type = static_cast<CurrencyType>(0);
  /// 던전 버프 ID
  std::int32_t dungeon_buff_id = 0;
  /// 버프 수치 증가값
  float buff_value_rate = 0.0f;
  /// 입장 값
  std::int32_t enter_currency_value = 0;
  /// 무료입장 재화 타입
  CurrencyType free_enter_currency_type = static_cast<CurrencyType>(0);
  /// 일일 광고 입장 횟수
  std::int32_t ad_enter_count = 0;
  /// 일일 입장권 최대 소모 횟수
  std::int32_t enter_currency_count_max = 0;
  /// 던전 이미지
  std::string dungeon_image_path;
  /// 던전 BGM
  std::int32_t dungeon_bgmid = 0;
  /// 설명
  std::string description;
};

class DailyDungeonInfoTable {
 public:
  const std::vector<DailyDungeonInfoRecord>& records() const { return records_; }

  /// The row with this Id, or nullptr when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  const DailyDungeonInfoRecord* find_by_id(std::int32_t key) const {
    const auto it = by_id_.find(key);
    return it == by_id_.end() ? nullptr : &records_[it->second];
  }

  /// The row with this Id, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading `get_by_id(key)->name` at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  const DailyDungeonInfoRecord& get_by_id_or_throw(std::int32_t key) const {
    const DailyDungeonInfoRecord* found = find_by_id(key);

    if (found == nullptr) {
      throw sheetman::RecordNotFound(
        std::string("there is no record in table `DailyDungeonInfo` that corresponds to "
                    "field `Id` value ") + std::to_string(key));
    }

    return *found;
  }

  /// Whether the table holds a row with this Id.
  bool contains_id(std::int32_t key) const {
    return by_id_.find(key) != by_id_.end();
  }

  /// Loads the table from a .scb file written by SheetMan.
  void read(const std::string& filename) {
    const std::vector<std::uint8_t> buffer = sheetman::read_all_bytes(filename);
    sheetman::ScbReader reader(buffer);

    // Column by column, matched by tag rather than position: a column this build
    // does not know is skipped by its block length, and one whose type changed
    // incompatibly fails naming the field.
    const sheetman::Header header = sheetman::read_table_header(reader);
    const std::size_t row_count = static_cast<std::size_t>(header.row_count);

    // Read into storage of its own and swapped in at the end: reading a table that is
    // already loaded is a refresh, and one that throws part way through - a truncated
    // file, a column this build cannot read - has to leave the rows already there.
    std::vector<DailyDungeonInfoRecord> records;
    records.resize(row_count);

    for (const sheetman::Column& column : header.columns) {
      const std::size_t block_end = reader.position() + static_cast<std::size_t>(column.byte_length);

      switch (column.tag) {
        case 1: {
          sheetman::check_column(column, "DailyDungeonInfo.Id", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "DailyDungeonInfo.Id");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.id = cursor.next_i32();
          }
          break;
        }
        case 2: {
          sheetman::check_column(column, "DailyDungeonInfo.Name", sheetman::kKindScalar, 1, {sheetman::kElementString});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "DailyDungeonInfo.Name");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.name = cursor.next_string();
          }
          break;
        }
        case 3: {
          sheetman::check_column(column, "DailyDungeonInfo.DungeonName", sheetman::kKindScalar, 1, {sheetman::kElementString});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "DailyDungeonInfo.DungeonName");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.dungeon_name = cursor.next_string();
          }
          break;
        }
        case 4: {
          sheetman::check_column(column, "DailyDungeonInfo.DungeonType", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "DailyDungeonInfo.DungeonType");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.dungeon_type = static_cast<DungeonType>(cursor.next_i32());
          }
          break;
        }
        case 5: {
          sheetman::check_column(column, "DailyDungeonInfo.Priority", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "DailyDungeonInfo.Priority");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.priority = cursor.next_i32();
          }
          break;
        }
        case 6: {
          sheetman::check_column(column, "DailyDungeonInfo.CycleType", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "DailyDungeonInfo.CycleType");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.cycle_type = static_cast<CycleType>(cursor.next_i32());
          }
          break;
        }
        case 7: {
          sheetman::check_column(column, "DailyDungeonInfo.EnterCondition", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "DailyDungeonInfo.EnterCondition");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.enter_condition = cursor.next_i32();
          }
          break;
        }
        case 8: {
          sheetman::check_column(column, "DailyDungeonInfo.EnterCurrencyType", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "DailyDungeonInfo.EnterCurrencyType");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.enter_currency_type = static_cast<CurrencyType>(cursor.next_i32());
          }
          break;
        }
        case 9: {
          sheetman::check_column(column, "DailyDungeonInfo.DungeonBuffID", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "DailyDungeonInfo.DungeonBuffID");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.dungeon_buff_id = cursor.next_i32();
          }
          break;
        }
        case 10: {
          sheetman::check_column(column, "DailyDungeonInfo.BuffValueRate", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "DailyDungeonInfo.BuffValueRate");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.buff_value_rate = cursor.next_f32();
          }
          break;
        }
        case 11: {
          sheetman::check_column(column, "DailyDungeonInfo.EnterCurrencyValue", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "DailyDungeonInfo.EnterCurrencyValue");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.enter_currency_value = cursor.next_i32();
          }
          break;
        }
        case 12: {
          sheetman::check_column(column, "DailyDungeonInfo.FreeEnterCurrencyType", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "DailyDungeonInfo.FreeEnterCurrencyType");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.free_enter_currency_type = static_cast<CurrencyType>(cursor.next_i32());
          }
          break;
        }
        case 13: {
          sheetman::check_column(column, "DailyDungeonInfo.ADEnterCount", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "DailyDungeonInfo.ADEnterCount");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.ad_enter_count = cursor.next_i32();
          }
          break;
        }
        case 14: {
          sheetman::check_column(column, "DailyDungeonInfo.EnterCurrencyCountMax", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "DailyDungeonInfo.EnterCurrencyCountMax");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.enter_currency_count_max = cursor.next_i32();
          }
          break;
        }
        case 15: {
          sheetman::check_column(column, "DailyDungeonInfo.DungeonImagePath", sheetman::kKindScalar, 1, {sheetman::kElementString});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "DailyDungeonInfo.DungeonImagePath");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.dungeon_image_path = cursor.next_string();
          }
          break;
        }
        case 16: {
          sheetman::check_column(column, "DailyDungeonInfo.DungeonBGMID", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "DailyDungeonInfo.DungeonBGMID");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.dungeon_bgmid = cursor.next_i32();
          }
          break;
        }
        case 17: {
          sheetman::check_column(column, "DailyDungeonInfo.Description", sheetman::kKindScalar, 1, {sheetman::kElementString});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "DailyDungeonInfo.Description");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.description = cursor.next_string();
          }
          break;
        }
        default:
          // A column added after this code was generated.
          reader.skip(column.byte_length);
          break;
      }

      sheetman::check_block_end(reader, column, block_end);
    }

    publish(std::move(records));
  }

 private:
  friend class Tables;

  /// One whole load, in place of the previous one.
  ///
  /// The indexes are built here rather than by the caller, because they have to arrive
  /// with the rows: a table holding this load's rows and the last one's index would
  /// answer a lookup with a row that is no longer at that position.
  void publish(std::vector<DailyDungeonInfoRecord>&& records) {
    std::unordered_map<std::int32_t, std::size_t> id_map;
    id_map.reserve(records.size());
    for (std::size_t i = 0; i < records.size(); ++i) {
      id_map.emplace(records[i].id, i);
    }

    records_ = std::move(records);
    by_id_ = std::move(id_map);
  }

  std::vector<DailyDungeonInfoRecord> records_;
  std::unordered_map<std::int32_t, std::size_t> by_id_;
};

}  // namespace rescue

#endif  // SHEETMAN_GENERATED_TABLES_DAILY_DUNGEON_INFO_H
