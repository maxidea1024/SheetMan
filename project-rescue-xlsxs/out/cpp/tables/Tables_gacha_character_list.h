// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

#ifndef SHEETMAN_GENERATED_TABLES_GACHA_CHARACTER_LIST_H
#define SHEETMAN_GENERATED_TABLES_GACHA_CHARACTER_LIST_H

#include <cstddef>
#include <cstdint>
#include <string>
#include <unordered_map>
#include <vector>

#include "sheetman/scb_reader.h"
#include "Tables_forward.h"
#include "enums/Tables_enum_grade_type.h"

namespace rescue {
// Generated from project-rescue-xlsxs/GachaData.xlsx : GachaCharacterListTable : A1
struct GachaCharacterListRecord {
  /// GachaCharacter_List
  std::int32_t id = 0;
  /// 캐릭터 ID
  std::int32_t character_id = 0;
  /// 태생 등급
  GradeType grade_type = static_cast<GradeType>(0);
  /// 기본 가중치
  std::int32_t base_weight = 0;
  /// 위시리스트 여부
  bool is_wishlist = false;
  /// 위시리스트 가중치
  std::int32_t wishlist_weight = 0;
  /// 성급 재화ID
  std::int32_t class_up_currency_id = 0;
  /// 성급 재화값
  std::int32_t class_up_currency_value = 0;
  /// 변환 조건 ID
  std::int32_t ex_condition_id = 0;
  /// 변환 재화ID
  std::int32_t ex_currency_id = 0;
  /// 변환 재화값
  std::int32_t ex_currency_value = 0;
};

class GachaCharacterListTable {
 public:
  const std::vector<GachaCharacterListRecord>& records() const { return records_; }

  /// The row with this Id, or nullptr when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  const GachaCharacterListRecord* find_by_id(std::int32_t key) const {
    const auto it = by_id_.find(key);
    return it == by_id_.end() ? nullptr : &records_[it->second];
  }

  /// The row with this Id, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading `get_by_id(key)->name` at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  const GachaCharacterListRecord& get_by_id_or_throw(std::int32_t key) const {
    const GachaCharacterListRecord* found = find_by_id(key);

    if (found == nullptr) {
      throw sheetman::RecordNotFound(
        std::string("there is no record in table `GachaCharacterList` that corresponds to "
                    "field `Id` value ") + std::to_string(key));
    }

    return *found;
  }

  /// Whether the table holds a row with this Id.
  bool contains_id(std::int32_t key) const {
    return by_id_.find(key) != by_id_.end();
  }

  /// Loads the table from a .scb file written by SheetMan.
  void read(const std::string& filename) {
    const std::vector<std::uint8_t> buffer = sheetman::read_all_bytes(filename);
    sheetman::ScbReader reader(buffer);

    // Column by column, matched by tag rather than position: a column this build
    // does not know is skipped by its block length, and one whose type changed
    // incompatibly fails naming the field.
    const sheetman::Header header = sheetman::read_table_header(reader);
    const std::size_t row_count = static_cast<std::size_t>(header.row_count);

    // Read into storage of its own and swapped in at the end: reading a table that is
    // already loaded is a refresh, and one that throws part way through - a truncated
    // file, a column this build cannot read - has to leave the rows already there.
    std::vector<GachaCharacterListRecord> records;
    records.resize(row_count);

    for (const sheetman::Column& column : header.columns) {
      const std::size_t block_end = reader.position() + static_cast<std::size_t>(column.byte_length);

      switch (column.tag) {
        case 1: {
          sheetman::check_column(column, "GachaCharacterList.Id", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.id);
          }
          break;
        }
        case 2: {
          sheetman::check_column(column, "GachaCharacterList.CharacterID", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.character_id);
          }
          break;
        }
        case 3: {
          sheetman::check_column(column, "GachaCharacterList.GradeType", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.grade_type);
          }
          break;
        }
        case 4: {
          sheetman::check_column(column, "GachaCharacterList.BaseWeight", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.base_weight);
          }
          break;
        }
        case 5: {
          sheetman::check_column(column, "GachaCharacterList.IsWishlist", sheetman::kKindScalar, 1, {sheetman::kElementBool});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.is_wishlist);
          }
          break;
        }
        case 6: {
          sheetman::check_column(column, "GachaCharacterList.WishlistWeight", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.wishlist_weight);
          }
          break;
        }
        case 7: {
          sheetman::check_column(column, "GachaCharacterList.ClassUpCurrencyID", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.class_up_currency_id);
          }
          break;
        }
        case 8: {
          sheetman::check_column(column, "GachaCharacterList.ClassUpCurrencyValue", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.class_up_currency_value);
          }
          break;
        }
        case 9: {
          sheetman::check_column(column, "GachaCharacterList.ExConditionID", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.ex_condition_id);
          }
          break;
        }
        case 10: {
          sheetman::check_column(column, "GachaCharacterList.ExCurrencyID", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.ex_currency_id);
          }
          break;
        }
        case 11: {
          sheetman::check_column(column, "GachaCharacterList.ExCurrencyValue", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.ex_currency_value);
          }
          break;
        }
        default:
          // A column added after this code was generated.
          reader.skip(column.byte_length);
          break;
      }

      sheetman::check_block_end(reader, column, block_end);
    }

    publish(std::move(records));
  }

 private:
  friend class Tables;

  /// One whole load, in place of the previous one.
  ///
  /// The indexes are built here rather than by the caller, because they have to arrive
  /// with the rows: a table holding this load's rows and the last one's index would
  /// answer a lookup with a row that is no longer at that position.
  void publish(std::vector<GachaCharacterListRecord>&& records) {
    std::unordered_map<std::int32_t, std::size_t> id_map;
    id_map.reserve(records.size());
    for (std::size_t i = 0; i < records.size(); ++i) {
      id_map.emplace(records[i].id, i);
    }

    records_ = std::move(records);
    by_id_ = std::move(id_map);
  }

  std::vector<GachaCharacterListRecord> records_;
  std::unordered_map<std::int32_t, std::size_t> by_id_;
};

}  // namespace rescue

#endif  // SHEETMAN_GENERATED_TABLES_GACHA_CHARACTER_LIST_H
