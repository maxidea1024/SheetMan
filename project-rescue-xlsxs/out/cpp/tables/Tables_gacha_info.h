// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

#ifndef SHEETMAN_GENERATED_TABLES_GACHA_INFO_H
#define SHEETMAN_GENERATED_TABLES_GACHA_INFO_H

#include <cstddef>
#include <cstdint>
#include <string>
#include <unordered_map>
#include <vector>

#include "sheetman/lite_binary_reader.h"
#include "Tables_forward.h"
#include "enums/Tables_enum_gacha_type.h"
#include "enums/Tables_enum_currency_type.h"

namespace rescue {
// Generated from project-rescue-xlsxs/GachaData.xlsx : GachaInfoTable : A1
struct GachaInfoRecord {
  /// HeroGacha_Table
  std::int32_t id = 0;
  /// 영문명
  std::string name;
  /// 카테고리
  GachaType gacha_type = static_cast<GachaType>(0);
  /// 출력 우선순위
  std::int32_t priority = 0;
  /// 해금 조건 ID
  std::int32_t condition_id = 0;
  /// 기본 확률 ID
  std::int32_t rate_id = 0;
  /// 중간 변경 확률 가챠 횟수
  std::int32_t trigger_count = 0;
  /// 중간 변경 확률ID
  std::int32_t trigger_rate_id = 0;
  /// 확정 누적 횟수
  std::int32_t end_count = 0;
  /// 확정 확률ID
  std::int32_t end_rate_id = 0;
  /// 확정 픽업 캐릭터 ID
  std::int32_t end_character_id = 0;
  /// 영입 횟수 초기화
  bool enable_reset = false;
  /// 영입 횟수 공유
  bool is_shared_counter = false;
  /// 위시리스트 해금 조건
  std::int32_t wish_list_condition_id = 0;
  /// 위시리스트 슬롯 최대값
  std::int32_t wish_list_max_value = 0;
  /// 1회 영입
  bool is_single = false;
  /// 10회 영입
  bool is_ten = false;
  /// 재화 타입1
  CurrencyType currency_type1 = static_cast<CurrencyType>(0);
  /// 티켓 소모값
  std::int32_t single_cost1 = 0;
  /// 재화 타입2
  CurrencyType currency_type2 = static_cast<CurrencyType>(0);
  /// 다이아 소모값
  std::int32_t single_cost2 = 0;
  /// 탭 버튼 아이콘
  std::string icon_path;
  /// 건너뛰기 가능
  bool enable_skip = false;
};

class GachaInfoTable {
 public:
  const std::vector<GachaInfoRecord>& records() const { return records_; }

  /// The row with this Id, or nullptr when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  const GachaInfoRecord* find_by_id(std::int32_t key) const {
    const auto it = by_id_.find(key);
    return it == by_id_.end() ? nullptr : &records_[it->second];
  }

  /// The row with this Id, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading `get_by_id(key)->name` at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  const GachaInfoRecord& get_by_id_or_throw(std::int32_t key) const {
    const GachaInfoRecord* found = find_by_id(key);

    if (found == nullptr) {
      throw sheetman::RecordNotFound(
        std::string("there is no record in table `GachaInfo` that corresponds to "
                    "field `Id` value ") + std::to_string(key));
    }

    return *found;
  }

  /// Whether the table holds a row with this Id.
  bool contains_id(std::int32_t key) const {
    return by_id_.find(key) != by_id_.end();
  }

  /// Loads the table from a .scb file written by SheetMan.
  void read(const std::string& filename) {
    const std::vector<std::uint8_t> buffer = sheetman::read_all_bytes(filename);
    sheetman::LiteBinaryReader reader(buffer);

    // Column by column, matched by tag rather than position: a column this build
    // does not know is skipped by its block length, and one whose type changed
    // incompatibly fails naming the field.
    const sheetman::Header header = sheetman::read_table_header(reader);
    const std::size_t row_count = static_cast<std::size_t>(header.row_count);

    // Read into storage of its own and swapped in at the end: reading a table that is
    // already loaded is a refresh, and one that throws part way through - a truncated
    // file, a column this build cannot read - has to leave the rows already there.
    std::vector<GachaInfoRecord> records;
    records.resize(row_count);

    for (const sheetman::Column& column : header.columns) {
      const std::size_t block_end = reader.position() + static_cast<std::size_t>(column.byte_length);

      switch (column.tag) {
        case 1: {
          sheetman::check_column(column, "GachaInfo.Id", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.id);
          }
          break;
        }
        case 2: {
          sheetman::check_column(column, "GachaInfo.Name", sheetman::kKindScalar, 1, {sheetman::kElementString});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.name);
          }
          break;
        }
        case 3: {
          sheetman::check_column(column, "GachaInfo.GachaType", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.gacha_type);
          }
          break;
        }
        case 4: {
          sheetman::check_column(column, "GachaInfo.Priority", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.priority);
          }
          break;
        }
        case 5: {
          sheetman::check_column(column, "GachaInfo.ConditionID", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.condition_id);
          }
          break;
        }
        case 6: {
          sheetman::check_column(column, "GachaInfo.RateId", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.rate_id);
          }
          break;
        }
        case 7: {
          sheetman::check_column(column, "GachaInfo.TriggerCount", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.trigger_count);
          }
          break;
        }
        case 8: {
          sheetman::check_column(column, "GachaInfo.TriggerRateId", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.trigger_rate_id);
          }
          break;
        }
        case 9: {
          sheetman::check_column(column, "GachaInfo.EndCount", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.end_count);
          }
          break;
        }
        case 10: {
          sheetman::check_column(column, "GachaInfo.EndRateId", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.end_rate_id);
          }
          break;
        }
        case 11: {
          sheetman::check_column(column, "GachaInfo.EndCharacterId", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.end_character_id);
          }
          break;
        }
        case 12: {
          sheetman::check_column(column, "GachaInfo.EnableReset", sheetman::kKindScalar, 1, {sheetman::kElementBool});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.enable_reset);
          }
          break;
        }
        case 13: {
          sheetman::check_column(column, "GachaInfo.IsSharedCounter", sheetman::kKindScalar, 1, {sheetman::kElementBool});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.is_shared_counter);
          }
          break;
        }
        case 14: {
          sheetman::check_column(column, "GachaInfo.WishListConditionID", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.wish_list_condition_id);
          }
          break;
        }
        case 15: {
          sheetman::check_column(column, "GachaInfo.WishListMaxValue", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.wish_list_max_value);
          }
          break;
        }
        case 16: {
          sheetman::check_column(column, "GachaInfo.IsSingle", sheetman::kKindScalar, 1, {sheetman::kElementBool});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.is_single);
          }
          break;
        }
        case 17: {
          sheetman::check_column(column, "GachaInfo.IsTen", sheetman::kKindScalar, 1, {sheetman::kElementBool});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.is_ten);
          }
          break;
        }
        case 18: {
          sheetman::check_column(column, "GachaInfo.CurrencyType1", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.currency_type1);
          }
          break;
        }
        case 19: {
          sheetman::check_column(column, "GachaInfo.SingleCost1", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.single_cost1);
          }
          break;
        }
        case 20: {
          sheetman::check_column(column, "GachaInfo.CurrencyType2", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.currency_type2);
          }
          break;
        }
        case 21: {
          sheetman::check_column(column, "GachaInfo.SingleCost2", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.single_cost2);
          }
          break;
        }
        case 22: {
          sheetman::check_column(column, "GachaInfo.IconPath", sheetman::kKindScalar, 1, {sheetman::kElementString});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.icon_path);
          }
          break;
        }
        case 23: {
          sheetman::check_column(column, "GachaInfo.EnableSkip", sheetman::kKindScalar, 1, {sheetman::kElementBool});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.enable_skip);
          }
          break;
        }
        default:
          // A column added after this code was generated.
          reader.skip(column.byte_length);
          break;
      }

      sheetman::check_block_end(reader, column, block_end);
    }

    publish(std::move(records));
  }

 private:
  friend class Tables;

  /// One whole load, in place of the previous one.
  ///
  /// The indexes are built here rather than by the caller, because they have to arrive
  /// with the rows: a table holding this load's rows and the last one's index would
  /// answer a lookup with a row that is no longer at that position.
  void publish(std::vector<GachaInfoRecord>&& records) {
    std::unordered_map<std::int32_t, std::size_t> id_map;
    id_map.reserve(records.size());
    for (std::size_t i = 0; i < records.size(); ++i) {
      id_map.emplace(records[i].id, i);
    }

    records_ = std::move(records);
    by_id_ = std::move(id_map);
  }

  std::vector<GachaInfoRecord> records_;
  std::unordered_map<std::int32_t, std::size_t> by_id_;
};

}  // namespace rescue

#endif  // SHEETMAN_GENERATED_TABLES_GACHA_INFO_H
