// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

#ifndef SHEETMAN_GENERATED_TABLES_PACKAGE_TABLE_H
#define SHEETMAN_GENERATED_TABLES_PACKAGE_TABLE_H

#include <cstddef>
#include <cstdint>
#include <string>
#include <unordered_map>
#include <vector>

#include "sheetman/lite_binary_reader.h"
#include "Tables_forward.h"
#include "enums/Tables_enum_item_type.h"
#include "enums/Tables_enum_consumables_type.h"
#include "enums/Tables_enum_condition_type.h"

namespace rescue {
// Generated from project-rescue-xlsxs/ItemData.xlsx : PackageTable : A1
struct PackageTableRecord {
  /// PackcgeItem_Table
  std::int32_t id = 0;
  /// 영문
  std::string name;
  /// 한글
  std::string class_name;
  /// 카테고리
  ItemType item_type = static_cast<ItemType>(0);
  /// 패키지 타입
  ConsumablesType package_type = static_cast<ConsumablesType>(0);
  /// 중첩 가능 유무
  bool stackable = false;
  /// 최대 소유 값
  std::int64_t max_stack = 0;
  /// 획득조건 타입
  ConditionType package_condition = static_cast<ConditionType>(0);
  /// 아이템 재사용 대기시간
  std::int32_t cooltime = 0;
  /// 아이템 유지 시간
  std::int32_t duration = 0;
  /// 포함 아이템 종류
  std::vector<std::int32_t> currency_id;
  /// 포함 아이템 최소 수량 값
  std::vector<std::int32_t> currency_item_min;
  /// 포함 아이템 최대 수량 값
  std::vector<std::int32_t> currency_item_max;
  /// 포함 아이템 확률
  std::vector<std::int32_t> currency_item_rate;
  /// 아이콘 경로
  std::string icon_path;
  /// 프리펩 위치
  std::string drop_prefab_path;
  /// 아이템 설명
  std::string item_description;
};

class PackageTableTable {
 public:
  const std::vector<PackageTableRecord>& records() const { return records_; }

  /// The row with this Id, or nullptr when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  const PackageTableRecord* find_by_id(std::int32_t key) const {
    const auto it = by_id_.find(key);
    return it == by_id_.end() ? nullptr : &records_[it->second];
  }

  /// The row with this Id, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading `get_by_id(key)->name` at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  const PackageTableRecord& get_by_id_or_throw(std::int32_t key) const {
    const PackageTableRecord* found = find_by_id(key);

    if (found == nullptr) {
      throw sheetman::RecordNotFound(
        std::string("there is no record in table `PackageTable` that corresponds to "
                    "field `Id` value ") + std::to_string(key));
    }

    return *found;
  }

  /// Whether the table holds a row with this Id.
  bool contains_id(std::int32_t key) const {
    return by_id_.find(key) != by_id_.end();
  }

  /// Loads the table from a .scb file written by SheetMan.
  void read(const std::string& filename) {
    const std::vector<std::uint8_t> buffer = sheetman::read_all_bytes(filename);
    sheetman::LiteBinaryReader reader(buffer);

    // Column by column, matched by tag rather than position: a column this build
    // does not know is skipped by its block length, and one whose type changed
    // incompatibly fails naming the field.
    const sheetman::Header header = sheetman::read_table_header(reader);
    const std::size_t row_count = static_cast<std::size_t>(header.row_count);

    // Read into storage of its own and swapped in at the end: reading a table that is
    // already loaded is a refresh, and one that throws part way through - a truncated
    // file, a column this build cannot read - has to leave the rows already there.
    std::vector<PackageTableRecord> records;
    records.resize(row_count);

    for (const sheetman::Column& column : header.columns) {
      const std::size_t block_end = reader.position() + static_cast<std::size_t>(column.byte_length);

      switch (column.tag) {
        case 1: {
          sheetman::check_column(column, "PackageTable.Id", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.id);
          }
          break;
        }
        case 2: {
          sheetman::check_column(column, "PackageTable.Name", sheetman::kKindScalar, 1, {sheetman::kElementString});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.name);
          }
          break;
        }
        case 3: {
          sheetman::check_column(column, "PackageTable.ClassName", sheetman::kKindScalar, 1, {sheetman::kElementString});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.class_name);
          }
          break;
        }
        case 4: {
          sheetman::check_column(column, "PackageTable.ItemType", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.item_type);
          }
          break;
        }
        case 5: {
          sheetman::check_column(column, "PackageTable.PackageType", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.package_type);
          }
          break;
        }
        case 6: {
          sheetman::check_column(column, "PackageTable.Stackable", sheetman::kKindScalar, 1, {sheetman::kElementBool});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.stackable);
          }
          break;
        }
        case 7: {
          sheetman::check_column(column, "PackageTable.MaxStack", sheetman::kKindScalar, 1, {sheetman::kElementI64, sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i64_as(column.element, record.max_stack);
          }
          break;
        }
        case 8: {
          sheetman::check_column(column, "PackageTable.PackageCondition", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.package_condition);
          }
          break;
        }
        case 9: {
          sheetman::check_column(column, "PackageTable.Cooltime", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.cooltime);
          }
          break;
        }
        case 10: {
          sheetman::check_column(column, "PackageTable.Duration", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.duration);
          }
          break;
        }
        case 11: {
          sheetman::check_column(column, "PackageTable.CurrencyID", sheetman::kKindVarArray, 0, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            const std::int32_t element_count = reader.read_counter32();
            record.currency_id.resize(static_cast<std::size_t>(element_count));
            for (std::int32_t j = 0; j < element_count; ++j) {
              reader.read_i32_as(column.element, record.currency_id[static_cast<std::size_t>(j)]);
            }
          }
          break;
        }
        case 12: {
          sheetman::check_column(column, "PackageTable.CurrencyItemMin", sheetman::kKindVarArray, 0, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            const std::int32_t element_count = reader.read_counter32();
            record.currency_item_min.resize(static_cast<std::size_t>(element_count));
            for (std::int32_t j = 0; j < element_count; ++j) {
              reader.read_i32_as(column.element, record.currency_item_min[static_cast<std::size_t>(j)]);
            }
          }
          break;
        }
        case 13: {
          sheetman::check_column(column, "PackageTable.CurrencyItemMax", sheetman::kKindVarArray, 0, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            const std::int32_t element_count = reader.read_counter32();
            record.currency_item_max.resize(static_cast<std::size_t>(element_count));
            for (std::int32_t j = 0; j < element_count; ++j) {
              reader.read_i32_as(column.element, record.currency_item_max[static_cast<std::size_t>(j)]);
            }
          }
          break;
        }
        case 14: {
          sheetman::check_column(column, "PackageTable.CurrencyItemRate", sheetman::kKindVarArray, 0, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            const std::int32_t element_count = reader.read_counter32();
            record.currency_item_rate.resize(static_cast<std::size_t>(element_count));
            for (std::int32_t j = 0; j < element_count; ++j) {
              reader.read_i32_as(column.element, record.currency_item_rate[static_cast<std::size_t>(j)]);
            }
          }
          break;
        }
        case 15: {
          sheetman::check_column(column, "PackageTable.IconPath", sheetman::kKindScalar, 1, {sheetman::kElementString});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.icon_path);
          }
          break;
        }
        case 16: {
          sheetman::check_column(column, "PackageTable.DropPrefabPath", sheetman::kKindScalar, 1, {sheetman::kElementString});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.drop_prefab_path);
          }
          break;
        }
        case 17: {
          sheetman::check_column(column, "PackageTable.ItemDescription", sheetman::kKindScalar, 1, {sheetman::kElementString});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.item_description);
          }
          break;
        }
        default:
          // A column added after this code was generated.
          reader.skip(column.byte_length);
          break;
      }

      sheetman::check_block_end(reader, column, block_end);
    }

    publish(std::move(records));
  }

 private:
  friend class Tables;

  /// One whole load, in place of the previous one.
  ///
  /// The indexes are built here rather than by the caller, because they have to arrive
  /// with the rows: a table holding this load's rows and the last one's index would
  /// answer a lookup with a row that is no longer at that position.
  void publish(std::vector<PackageTableRecord>&& records) {
    std::unordered_map<std::int32_t, std::size_t> id_map;
    id_map.reserve(records.size());
    for (std::size_t i = 0; i < records.size(); ++i) {
      id_map.emplace(records[i].id, i);
    }

    records_ = std::move(records);
    by_id_ = std::move(id_map);
  }

  std::vector<PackageTableRecord> records_;
  std::unordered_map<std::int32_t, std::size_t> by_id_;
};

}  // namespace rescue

#endif  // SHEETMAN_GENERATED_TABLES_PACKAGE_TABLE_H
