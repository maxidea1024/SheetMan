// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

#ifndef SHEETMAN_GENERATED_TABLES_RELIC_H
#define SHEETMAN_GENERATED_TABLES_RELIC_H

#include <cstddef>
#include <cstdint>
#include <string>
#include <unordered_map>
#include <vector>

#include "sheetman/scb_reader.h"
#include "Tables_forward.h"
#include "enums/Tables_enum_stat_type.h"

namespace rescue {
// Generated from project-rescue-xlsxs/StageGrowth.xlsx : RelicTable : A1
struct RelicRecord {
  /// 기본값
  std::int32_t id = 0;
  /// 영문
  std::string name;
  /// 한글
  std::string relic_name;
  /// 영향력 단계
  std::int32_t infuluence_step = 0;
  /// 스탯 타입
  StatType relic_type1 = static_cast<StatType>(0);
  /// 유물 기본 값 1
  float relic_value1 = 0.0f;
  /// 유물 레벨 당 스탯 증가 수치 1
  float relic_inc_value1 = 0.0f;
  /// 유물 기본 값 2
  StatType relic_type2 = static_cast<StatType>(0);
  /// 유물 레벨 당 스탯 증가 수치 2
  float relic_value2 = 0.0f;
  /// 유물 기본 값 3
  float relic_inc_value2 = 0.0f;
  /// 유물 레벨 당 스탯 증가 수치 3
  StatType relic_type3 = static_cast<StatType>(0);
  /// 유물 기본 값 4
  float relic_value3 = 0.0f;
  /// 유물 레벨 당 스탯 증가 수치 4
  float relic_inc_value3 = 0.0f;
  /// 설명
  std::string description;
  /// 아이콘 경로
  std::string icon_path;
};

class RelicTable {
 public:
  const std::vector<RelicRecord>& records() const { return records_; }

  /// The row with this Id, or nullptr when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  const RelicRecord* find_by_id(std::int32_t key) const {
    const auto it = by_id_.find(key);
    return it == by_id_.end() ? nullptr : &records_[it->second];
  }

  /// The row with this Id, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading `get_by_id(key)->name` at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  const RelicRecord& get_by_id_or_throw(std::int32_t key) const {
    const RelicRecord* found = find_by_id(key);

    if (found == nullptr) {
      throw sheetman::RecordNotFound(
        std::string("there is no record in table `Relic` that corresponds to "
                    "field `Id` value ") + std::to_string(key));
    }

    return *found;
  }

  /// Whether the table holds a row with this Id.
  bool contains_id(std::int32_t key) const {
    return by_id_.find(key) != by_id_.end();
  }

  /// Loads the table from a .scb file written by SheetMan.
  void read(const std::string& filename) {
    const std::vector<std::uint8_t> buffer = sheetman::read_all_bytes(filename);
    sheetman::ScbReader reader(buffer);

    // Column by column, matched by tag rather than position: a column this build
    // does not know is skipped by its block length, and one whose type changed
    // incompatibly fails naming the field.
    const sheetman::Header header = sheetman::read_table_header(reader);
    const std::size_t row_count = static_cast<std::size_t>(header.row_count);

    // Read into storage of its own and swapped in at the end: reading a table that is
    // already loaded is a refresh, and one that throws part way through - a truncated
    // file, a column this build cannot read - has to leave the rows already there.
    std::vector<RelicRecord> records;
    records.resize(row_count);

    for (const sheetman::Column& column : header.columns) {
      const std::size_t block_end = reader.position() + static_cast<std::size_t>(column.byte_length);

      switch (column.tag) {
        case 1: {
          sheetman::check_column(column, "Relic.Id", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Relic.Id");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.id = cursor.next_i32();
          }
          break;
        }
        case 2: {
          sheetman::check_column(column, "Relic.Name", sheetman::kKindScalar, 1, {sheetman::kElementString});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Relic.Name");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.name = cursor.next_string();
          }
          break;
        }
        case 3: {
          sheetman::check_column(column, "Relic.RelicName", sheetman::kKindScalar, 1, {sheetman::kElementString});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Relic.RelicName");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.relic_name = cursor.next_string();
          }
          break;
        }
        case 4: {
          sheetman::check_column(column, "Relic.InfuluenceStep", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Relic.InfuluenceStep");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.infuluence_step = cursor.next_i32();
          }
          break;
        }
        case 5: {
          sheetman::check_column(column, "Relic.RelicType1", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Relic.RelicType1");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.relic_type1 = static_cast<StatType>(cursor.next_i32());
          }
          break;
        }
        case 6: {
          sheetman::check_column(column, "Relic.RelicValue1", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Relic.RelicValue1");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.relic_value1 = cursor.next_f32();
          }
          break;
        }
        case 7: {
          sheetman::check_column(column, "Relic.RelicIncValue1", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Relic.RelicIncValue1");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.relic_inc_value1 = cursor.next_f32();
          }
          break;
        }
        case 8: {
          sheetman::check_column(column, "Relic.RelicType2", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Relic.RelicType2");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.relic_type2 = static_cast<StatType>(cursor.next_i32());
          }
          break;
        }
        case 9: {
          sheetman::check_column(column, "Relic.RelicValue2", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Relic.RelicValue2");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.relic_value2 = cursor.next_f32();
          }
          break;
        }
        case 10: {
          sheetman::check_column(column, "Relic.RelicIncValue2", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Relic.RelicIncValue2");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.relic_inc_value2 = cursor.next_f32();
          }
          break;
        }
        case 11: {
          sheetman::check_column(column, "Relic.RelicType3", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Relic.RelicType3");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.relic_type3 = static_cast<StatType>(cursor.next_i32());
          }
          break;
        }
        case 12: {
          sheetman::check_column(column, "Relic.RelicValue3", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Relic.RelicValue3");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.relic_value3 = cursor.next_f32();
          }
          break;
        }
        case 13: {
          sheetman::check_column(column, "Relic.RelicIncValue3", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Relic.RelicIncValue3");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.relic_inc_value3 = cursor.next_f32();
          }
          break;
        }
        case 14: {
          sheetman::check_column(column, "Relic.Description", sheetman::kKindScalar, 1, {sheetman::kElementString});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Relic.Description");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.description = cursor.next_string();
          }
          break;
        }
        case 15: {
          sheetman::check_column(column, "Relic.IconPath", sheetman::kKindScalar, 1, {sheetman::kElementString});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Relic.IconPath");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.icon_path = cursor.next_string();
          }
          break;
        }
        default:
          // A column added after this code was generated.
          reader.skip(column.byte_length);
          break;
      }

      sheetman::check_block_end(reader, column, block_end);
    }

    publish(std::move(records));
  }

 private:
  friend class Tables;

  /// One whole load, in place of the previous one.
  ///
  /// The indexes are built here rather than by the caller, because they have to arrive
  /// with the rows: a table holding this load's rows and the last one's index would
  /// answer a lookup with a row that is no longer at that position.
  void publish(std::vector<RelicRecord>&& records) {
    std::unordered_map<std::int32_t, std::size_t> id_map;
    id_map.reserve(records.size());
    for (std::size_t i = 0; i < records.size(); ++i) {
      id_map.emplace(records[i].id, i);
    }

    records_ = std::move(records);
    by_id_ = std::move(id_map);
  }

  std::vector<RelicRecord> records_;
  std::unordered_map<std::int32_t, std::size_t> by_id_;
};

}  // namespace rescue

#endif  // SHEETMAN_GENERATED_TABLES_RELIC_H
