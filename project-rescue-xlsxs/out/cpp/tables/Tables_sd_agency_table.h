// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

#ifndef SHEETMAN_GENERATED_TABLES_SD_AGENCY_TABLE_H
#define SHEETMAN_GENERATED_TABLES_SD_AGENCY_TABLE_H

#include <cstddef>
#include <cstdint>
#include <string>
#include <unordered_map>
#include <vector>

#include "sheetman/lite_binary_reader.h"
#include "Tables_forward.h"
#include "enums/Tables_enum_agency_type.h"
#include "enums/Tables_enum_agency_grade.h"
#include "enums/Tables_enum_job_type.h"
#include "enums/Tables_enum_grade_type.h"
#include "enums/Tables_enum_condition_type.h"
#include "enums/Tables_enum_currency_type.h"

namespace rescue {
// Generated from project-rescue-xlsxs/SD_Contens.xlsx : SDAgencyTable : A1
struct SDAgencyTableRecord {
  /// SDAgency_Table
  std::int32_t id = 0;
  /// 영문
  std::string name;
  /// 한글
  std::string info_name;
  /// 타입
  AgencyType agency_type = static_cast<AgencyType>(0);
  /// 등급
  AgencyGrade agency_grade = static_cast<AgencyGrade>(0);
  /// 소요 시간(sec)
  std::int32_t time = 0;
  /// 가중치
  std::int32_t agency_weight = 0;
  /// 조건1
  JobType condition1 = static_cast<JobType>(0);
  /// 조건값1
  std::int32_t condition1value = 0;
  /// 조건2
  GradeType condition2 = static_cast<GradeType>(0);
  /// 조건값2
  std::int32_t condition2value = 0;
  /// 조건3
  ConditionType condition3 = static_cast<ConditionType>(0);
  /// 최소 성급
  std::int32_t class_min_value = 0;
  /// 조건값3
  std::int32_t condition3value = 0;
  /// 보상1
  CurrencyType reward_type1 = static_cast<CurrencyType>(0);
  /// 보상 값1
  std::int32_t reward_value1 = 0;
  /// 보상2
  CurrencyType reward_type2 = static_cast<CurrencyType>(0);
  /// 보상 값2
  std::int32_t reward_value2 = 0;
  /// 보상3
  CurrencyType reward_type3 = static_cast<CurrencyType>(0);
  /// 보상 값3
  std::int32_t reward_value3 = 0;
  /// 아이콘 경로
  std::string icon_path;
};

class SDAgencyTableTable {
 public:
  const std::vector<SDAgencyTableRecord>& records() const { return records_; }

  /// The row with this Id, or nullptr when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  const SDAgencyTableRecord* find_by_id(std::int32_t key) const {
    const auto it = by_id_.find(key);
    return it == by_id_.end() ? nullptr : &records_[it->second];
  }

  /// The row with this Id, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading `get_by_id(key)->name` at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  const SDAgencyTableRecord& get_by_id_or_throw(std::int32_t key) const {
    const SDAgencyTableRecord* found = find_by_id(key);

    if (found == nullptr) {
      throw sheetman::RecordNotFound(
        std::string("there is no record in table `SDAgencyTable` that corresponds to "
                    "field `Id` value ") + std::to_string(key));
    }

    return *found;
  }

  /// Whether the table holds a row with this Id.
  bool contains_id(std::int32_t key) const {
    return by_id_.find(key) != by_id_.end();
  }

  /// Loads the table from a .scb file written by SheetMan.
  void read(const std::string& filename) {
    const std::vector<std::uint8_t> buffer = sheetman::read_all_bytes(filename);
    sheetman::LiteBinaryReader reader(buffer);

    // Column by column, matched by tag rather than position: a column this build
    // does not know is skipped by its block length, and one whose type changed
    // incompatibly fails naming the field.
    const sheetman::Header header = sheetman::read_table_header(reader);
    const std::size_t row_count = static_cast<std::size_t>(header.row_count);

    // Read into storage of its own and swapped in at the end: reading a table that is
    // already loaded is a refresh, and one that throws part way through - a truncated
    // file, a column this build cannot read - has to leave the rows already there.
    std::vector<SDAgencyTableRecord> records;
    records.resize(row_count);

    for (const sheetman::Column& column : header.columns) {
      const std::size_t block_end = reader.position() + static_cast<std::size_t>(column.byte_length);

      switch (column.tag) {
        case 1: {
          sheetman::check_column(column, "SDAgencyTable.Id", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.id);
          }
          break;
        }
        case 2: {
          sheetman::check_column(column, "SDAgencyTable.Name", sheetman::kKindScalar, 1, {sheetman::kElementString});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.name);
          }
          break;
        }
        case 3: {
          sheetman::check_column(column, "SDAgencyTable.InfoName", sheetman::kKindScalar, 1, {sheetman::kElementString});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.info_name);
          }
          break;
        }
        case 4: {
          sheetman::check_column(column, "SDAgencyTable.AgencyType", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.agency_type);
          }
          break;
        }
        case 5: {
          sheetman::check_column(column, "SDAgencyTable.AgencyGrade", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.agency_grade);
          }
          break;
        }
        case 6: {
          sheetman::check_column(column, "SDAgencyTable.Time", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.time);
          }
          break;
        }
        case 7: {
          sheetman::check_column(column, "SDAgencyTable.AgencyWeight", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.agency_weight);
          }
          break;
        }
        case 8: {
          sheetman::check_column(column, "SDAgencyTable.Condition1", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.condition1);
          }
          break;
        }
        case 9: {
          sheetman::check_column(column, "SDAgencyTable.Condition1value", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.condition1value);
          }
          break;
        }
        case 10: {
          sheetman::check_column(column, "SDAgencyTable.Condition2", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.condition2);
          }
          break;
        }
        case 11: {
          sheetman::check_column(column, "SDAgencyTable.Condition2value", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.condition2value);
          }
          break;
        }
        case 12: {
          sheetman::check_column(column, "SDAgencyTable.Condition3", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.condition3);
          }
          break;
        }
        case 13: {
          sheetman::check_column(column, "SDAgencyTable.ClassMinValue", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.class_min_value);
          }
          break;
        }
        case 14: {
          sheetman::check_column(column, "SDAgencyTable.Condition3value", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.condition3value);
          }
          break;
        }
        case 15: {
          sheetman::check_column(column, "SDAgencyTable.RewardType1", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.reward_type1);
          }
          break;
        }
        case 16: {
          sheetman::check_column(column, "SDAgencyTable.RewardValue1", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.reward_value1);
          }
          break;
        }
        case 17: {
          sheetman::check_column(column, "SDAgencyTable.RewardType2", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.reward_type2);
          }
          break;
        }
        case 18: {
          sheetman::check_column(column, "SDAgencyTable.RewardValue2", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.reward_value2);
          }
          break;
        }
        case 19: {
          sheetman::check_column(column, "SDAgencyTable.RewardType3", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.reward_type3);
          }
          break;
        }
        case 20: {
          sheetman::check_column(column, "SDAgencyTable.RewardValue3", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.reward_value3);
          }
          break;
        }
        case 21: {
          sheetman::check_column(column, "SDAgencyTable.IconPath", sheetman::kKindScalar, 1, {sheetman::kElementString});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.icon_path);
          }
          break;
        }
        default:
          // A column added after this code was generated.
          reader.skip(column.byte_length);
          break;
      }

      sheetman::check_block_end(reader, column, block_end);
    }

    publish(std::move(records));
  }

 private:
  friend class Tables;

  /// One whole load, in place of the previous one.
  ///
  /// The indexes are built here rather than by the caller, because they have to arrive
  /// with the rows: a table holding this load's rows and the last one's index would
  /// answer a lookup with a row that is no longer at that position.
  void publish(std::vector<SDAgencyTableRecord>&& records) {
    std::unordered_map<std::int32_t, std::size_t> id_map;
    id_map.reserve(records.size());
    for (std::size_t i = 0; i < records.size(); ++i) {
      id_map.emplace(records[i].id, i);
    }

    records_ = std::move(records);
    by_id_ = std::move(id_map);
  }

  std::vector<SDAgencyTableRecord> records_;
  std::unordered_map<std::int32_t, std::size_t> by_id_;
};

}  // namespace rescue

#endif  // SHEETMAN_GENERATED_TABLES_SD_AGENCY_TABLE_H
