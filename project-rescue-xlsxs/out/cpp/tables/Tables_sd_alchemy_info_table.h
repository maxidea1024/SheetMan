// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

#ifndef SHEETMAN_GENERATED_TABLES_SD_ALCHEMY_INFO_TABLE_H
#define SHEETMAN_GENERATED_TABLES_SD_ALCHEMY_INFO_TABLE_H

#include <cstddef>
#include <cstdint>
#include <string>
#include <unordered_map>
#include <vector>

#include "sheetman/lite_binary_reader.h"
#include "Tables_forward.h"
#include "enums/Tables_enum_currency_type.h"
#include "enums/Tables_enum_stat_type.h"
#include "enums/Tables_enum_target_type.h"

namespace rescue {
// Generated from project-rescue-xlsxs/SD_Contens.xlsx : SDAlchemyInfoTable : A1
struct SDAlchemyInfoTableRecord {
  /// SDAlchemyInfo_Table
  std::int32_t id = 0;
  /// 영문명
  std::string name;
  /// 한글명
  std::string name_kr;
  /// 전용 재료 타입
  CurrencyType material_type = static_cast<CurrencyType>(0);
  /// 공용 재료1 타입
  CurrencyType common_material_type1 = static_cast<CurrencyType>(0);
  /// 공용 재료2 타입
  CurrencyType common_material_type2 = static_cast<CurrencyType>(0);
  /// 재료값 증가 비율
  float material_type2_rate = 0.0f;
  /// 스텟 타입
  StatType stat_type = static_cast<StatType>(0);
  /// 타겟 대상
  TargetType target_type = static_cast<TargetType>(0);
  /// 최대 레벨값
  std::int32_t max_level_id = 0;
  /// 가속 시간
  std::int32_t accelerate_time = 0;
  /// 가속권 타입
  CurrencyType accelerate_item_type1 = static_cast<CurrencyType>(0);
  /// 가속권 개수
  std::int32_t accelerate_item_cost1 = 0;
  /// 다이아
  CurrencyType accelerate_item_type2 = static_cast<CurrencyType>(0);
  /// 필요 다이아 개수
  std::int32_t accelerate_item_cost2 = 0;
  /// 아이콘 경로
  std::string icon_path;
};

class SDAlchemyInfoTableTable {
 public:
  const std::vector<SDAlchemyInfoTableRecord>& records() const { return records_; }

  /// The row with this Id, or nullptr when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  const SDAlchemyInfoTableRecord* find_by_id(std::int32_t key) const {
    const auto it = by_id_.find(key);
    return it == by_id_.end() ? nullptr : &records_[it->second];
  }

  /// The row with this Id, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading `get_by_id(key)->name` at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  const SDAlchemyInfoTableRecord& get_by_id_or_throw(std::int32_t key) const {
    const SDAlchemyInfoTableRecord* found = find_by_id(key);

    if (found == nullptr) {
      throw sheetman::RecordNotFound(
        std::string("there is no record in table `SDAlchemyInfoTable` that corresponds to "
                    "field `Id` value ") + std::to_string(key));
    }

    return *found;
  }

  /// Whether the table holds a row with this Id.
  bool contains_id(std::int32_t key) const {
    return by_id_.find(key) != by_id_.end();
  }

  /// Loads the table from a .scb file written by SheetMan.
  void read(const std::string& filename) {
    const std::vector<std::uint8_t> buffer = sheetman::read_all_bytes(filename);
    sheetman::LiteBinaryReader reader(buffer);

    // Column by column, matched by tag rather than position: a column this build
    // does not know is skipped by its block length, and one whose type changed
    // incompatibly fails naming the field.
    const sheetman::Header header = sheetman::read_table_header(reader);
    const std::size_t row_count = static_cast<std::size_t>(header.row_count);

    // Read into storage of its own and swapped in at the end: reading a table that is
    // already loaded is a refresh, and one that throws part way through - a truncated
    // file, a column this build cannot read - has to leave the rows already there.
    std::vector<SDAlchemyInfoTableRecord> records;
    records.resize(row_count);

    for (const sheetman::Column& column : header.columns) {
      const std::size_t block_end = reader.position() + static_cast<std::size_t>(column.byte_length);

      switch (column.tag) {
        case 1: {
          sheetman::check_column(column, "SDAlchemyInfoTable.Id", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.id);
          }
          break;
        }
        case 2: {
          sheetman::check_column(column, "SDAlchemyInfoTable.Name", sheetman::kKindScalar, 1, {sheetman::kElementString});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.name);
          }
          break;
        }
        case 3: {
          sheetman::check_column(column, "SDAlchemyInfoTable.NameKR", sheetman::kKindScalar, 1, {sheetman::kElementString});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.name_kr);
          }
          break;
        }
        case 4: {
          sheetman::check_column(column, "SDAlchemyInfoTable.MaterialType", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.material_type);
          }
          break;
        }
        case 5: {
          sheetman::check_column(column, "SDAlchemyInfoTable.CommonMaterialType1", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.common_material_type1);
          }
          break;
        }
        case 6: {
          sheetman::check_column(column, "SDAlchemyInfoTable.CommonMaterialType2", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.common_material_type2);
          }
          break;
        }
        case 7: {
          sheetman::check_column(column, "SDAlchemyInfoTable.MaterialType2Rate", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.material_type2_rate);
          }
          break;
        }
        case 8: {
          sheetman::check_column(column, "SDAlchemyInfoTable.StatType", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.stat_type);
          }
          break;
        }
        case 9: {
          sheetman::check_column(column, "SDAlchemyInfoTable.TargetType", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.target_type);
          }
          break;
        }
        case 10: {
          sheetman::check_column(column, "SDAlchemyInfoTable.MaxLevelId", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.max_level_id);
          }
          break;
        }
        case 11: {
          sheetman::check_column(column, "SDAlchemyInfoTable.AccelerateTime", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.accelerate_time);
          }
          break;
        }
        case 12: {
          sheetman::check_column(column, "SDAlchemyInfoTable.AccelerateItemType1", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.accelerate_item_type1);
          }
          break;
        }
        case 13: {
          sheetman::check_column(column, "SDAlchemyInfoTable.AccelerateItemCost1", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.accelerate_item_cost1);
          }
          break;
        }
        case 14: {
          sheetman::check_column(column, "SDAlchemyInfoTable.AccelerateItemType2", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.accelerate_item_type2);
          }
          break;
        }
        case 15: {
          sheetman::check_column(column, "SDAlchemyInfoTable.AccelerateItemCost2", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.accelerate_item_cost2);
          }
          break;
        }
        case 16: {
          sheetman::check_column(column, "SDAlchemyInfoTable.IconPath", sheetman::kKindScalar, 1, {sheetman::kElementString});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.icon_path);
          }
          break;
        }
        default:
          // A column added after this code was generated.
          reader.skip(column.byte_length);
          break;
      }

      sheetman::check_block_end(reader, column, block_end);
    }

    publish(std::move(records));
  }

 private:
  friend class Tables;

  /// One whole load, in place of the previous one.
  ///
  /// The indexes are built here rather than by the caller, because they have to arrive
  /// with the rows: a table holding this load's rows and the last one's index would
  /// answer a lookup with a row that is no longer at that position.
  void publish(std::vector<SDAlchemyInfoTableRecord>&& records) {
    std::unordered_map<std::int32_t, std::size_t> id_map;
    id_map.reserve(records.size());
    for (std::size_t i = 0; i < records.size(); ++i) {
      id_map.emplace(records[i].id, i);
    }

    records_ = std::move(records);
    by_id_ = std::move(id_map);
  }

  std::vector<SDAlchemyInfoTableRecord> records_;
  std::unordered_map<std::int32_t, std::size_t> by_id_;
};

}  // namespace rescue

#endif  // SHEETMAN_GENERATED_TABLES_SD_ALCHEMY_INFO_TABLE_H
