// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

#ifndef SHEETMAN_GENERATED_TABLES_SD_DUNGEON_REWARD_TABLE_H
#define SHEETMAN_GENERATED_TABLES_SD_DUNGEON_REWARD_TABLE_H

#include <cstddef>
#include <cstdint>
#include <string>
#include <unordered_map>
#include <vector>

#include "sheetman/lite_binary_reader.h"
#include "Tables_forward.h"
#include "enums/Tables_enum_currency_type.h"

namespace rescue {
// Generated from project-rescue-xlsxs\SD_Contens.xlsx : SDDungeonRewardTable : A1
struct SDDungeonRewardTableRecord {
  /// SDDungeonReward_Table
  std::int32_t id = 0;
  /// 영문명
  std::string name;
  /// 한글명
  std::string reward_name;
  /// 해제 조건
  std::int32_t condition_id = 0;
  /// 공용 재료
  CurrencyType reward_common = static_cast<CurrencyType>(0);
  /// 공용 재료 값
  std::int32_t common_value = 0;
  /// 크리티컬 데미지 재료
  CurrencyType reward1_type = static_cast<CurrencyType>(0);
  /// 크리티컬 데미지 재료값
  std::int32_t reward1_value = 0;
  /// 크리티컬 확률 재료
  CurrencyType reward2_type = static_cast<CurrencyType>(0);
  /// 크리티컬 확률 재료 값
  std::int32_t reward2_value = 0;
  /// 블록 확률 재료
  CurrencyType reward3_type = static_cast<CurrencyType>(0);
  /// 블록 확률 재료 값
  std::int32_t reward3_value = 0;
  /// 명중률 재료
  CurrencyType reward4_type = static_cast<CurrencyType>(0);
  /// 명중률 재료 값
  std::int32_t reward4_value = 0;
  /// 퍼펙트 조건
  bool is_perfect = false;
  /// 퍼펙트 보상
  CurrencyType reward_perfect = static_cast<CurrencyType>(0);
  /// 퍼펙트 보상값
  std::int32_t perfect_value = 0;
  /// 최소 보상 조건
  bool is_min_reward = false;
  /// 최소 보상
  CurrencyType reward_min = static_cast<CurrencyType>(0);
  /// 최소 보상값
  std::int32_t min_reward_value = 0;
};

class SDDungeonRewardTableTable {
 public:
  const std::vector<SDDungeonRewardTableRecord>& records() const { return records_; }

  /// The row with this Id, or nullptr when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  const SDDungeonRewardTableRecord* find_by_id(std::int32_t key) const {
    const auto it = by_id_.find(key);
    return it == by_id_.end() ? nullptr : &records_[it->second];
  }

  /// The row with this Id, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading `get_by_id(key)->name` at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  const SDDungeonRewardTableRecord& get_by_id_or_throw(std::int32_t key) const {
    const SDDungeonRewardTableRecord* found = find_by_id(key);

    if (found == nullptr) {
      throw sheetman::RecordNotFound(
        std::string("there is no record in table `SDDungeonRewardTable` that corresponds to "
                    "field `Id` value ") + std::to_string(key));
    }

    return *found;
  }

  /// Whether the table holds a row with this Id.
  bool contains_id(std::int32_t key) const {
    return by_id_.find(key) != by_id_.end();
  }

  /// Loads the table from a .table file written by SheetMan.
  void read(const std::string& filename) {
    const std::vector<std::uint8_t> buffer = sheetman::read_all_bytes(filename);
    sheetman::LiteBinaryReader reader(buffer);

    // Column by column, matched by tag rather than position: a column this build
    // does not know is skipped by its block length, and one whose type changed
    // incompatibly fails naming the field.
    const sheetman::Header header = sheetman::read_table_header(reader);
    const std::size_t row_count = static_cast<std::size_t>(header.row_count);

    // Read into storage of its own and swapped in at the end: reading a table that is
    // already loaded is a refresh, and one that throws part way through - a truncated
    // file, a column this build cannot read - has to leave the rows already there.
    std::vector<SDDungeonRewardTableRecord> records;
    records.resize(row_count);

    for (const sheetman::Column& column : header.columns) {
      const std::size_t block_end = reader.position() + static_cast<std::size_t>(column.byte_length);

      switch (column.tag) {
        case 1: {
          sheetman::check_column(column, "SDDungeonRewardTable.Id", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.id);
          }
          break;
        }
        case 2: {
          sheetman::check_column(column, "SDDungeonRewardTable.Name", sheetman::kKindScalar, 1, {sheetman::kElementString});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.name);
          }
          break;
        }
        case 3: {
          sheetman::check_column(column, "SDDungeonRewardTable.RewardName", sheetman::kKindScalar, 1, {sheetman::kElementString});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.reward_name);
          }
          break;
        }
        case 4: {
          sheetman::check_column(column, "SDDungeonRewardTable.ConditionId", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.condition_id);
          }
          break;
        }
        case 5: {
          sheetman::check_column(column, "SDDungeonRewardTable.RewardCommon", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.reward_common);
          }
          break;
        }
        case 6: {
          sheetman::check_column(column, "SDDungeonRewardTable.CommonValue", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.common_value);
          }
          break;
        }
        case 7: {
          sheetman::check_column(column, "SDDungeonRewardTable.Reward1Type", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.reward1_type);
          }
          break;
        }
        case 8: {
          sheetman::check_column(column, "SDDungeonRewardTable.Reward1Value", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.reward1_value);
          }
          break;
        }
        case 9: {
          sheetman::check_column(column, "SDDungeonRewardTable.Reward2Type", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.reward2_type);
          }
          break;
        }
        case 10: {
          sheetman::check_column(column, "SDDungeonRewardTable.Reward2Value", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.reward2_value);
          }
          break;
        }
        case 11: {
          sheetman::check_column(column, "SDDungeonRewardTable.Reward3Type", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.reward3_type);
          }
          break;
        }
        case 12: {
          sheetman::check_column(column, "SDDungeonRewardTable.Reward3Value", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.reward3_value);
          }
          break;
        }
        case 13: {
          sheetman::check_column(column, "SDDungeonRewardTable.Reward4Type", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.reward4_type);
          }
          break;
        }
        case 14: {
          sheetman::check_column(column, "SDDungeonRewardTable.Reward4Value", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.reward4_value);
          }
          break;
        }
        case 15: {
          sheetman::check_column(column, "SDDungeonRewardTable.IsPerfect", sheetman::kKindScalar, 1, {sheetman::kElementBool});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.is_perfect);
          }
          break;
        }
        case 16: {
          sheetman::check_column(column, "SDDungeonRewardTable.RewardPerfect", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.reward_perfect);
          }
          break;
        }
        case 17: {
          sheetman::check_column(column, "SDDungeonRewardTable.PerfectValue", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.perfect_value);
          }
          break;
        }
        case 18: {
          sheetman::check_column(column, "SDDungeonRewardTable.IsMinReward", sheetman::kKindScalar, 1, {sheetman::kElementBool});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.is_min_reward);
          }
          break;
        }
        case 19: {
          sheetman::check_column(column, "SDDungeonRewardTable.RewardMin", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.reward_min);
          }
          break;
        }
        case 20: {
          sheetman::check_column(column, "SDDungeonRewardTable.MinRewardValue", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.min_reward_value);
          }
          break;
        }
        default:
          // A column added after this code was generated.
          reader.skip(column.byte_length);
          break;
      }

      sheetman::check_block_end(reader, column, block_end);
    }

    publish(std::move(records));
  }

 private:
  friend class Tables;

  /// One whole load, in place of the previous one.
  ///
  /// The indexes are built here rather than by the caller, because they have to arrive
  /// with the rows: a table holding this load's rows and the last one's index would
  /// answer a lookup with a row that is no longer at that position.
  void publish(std::vector<SDDungeonRewardTableRecord>&& records) {
    std::unordered_map<std::int32_t, std::size_t> id_map;
    id_map.reserve(records.size());
    for (std::size_t i = 0; i < records.size(); ++i) {
      id_map.emplace(records[i].id, i);
    }

    records_ = std::move(records);
    by_id_ = std::move(id_map);
  }

  std::vector<SDDungeonRewardTableRecord> records_;
  std::unordered_map<std::int32_t, std::size_t> by_id_;
};

}  // namespace rescue

#endif  // SHEETMAN_GENERATED_TABLES_SD_DUNGEON_REWARD_TABLE_H
