// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

#ifndef SHEETMAN_GENERATED_TABLES_SD_TRAINING_LEVEL_H
#define SHEETMAN_GENERATED_TABLES_SD_TRAINING_LEVEL_H

#include <cstddef>
#include <cstdint>
#include <string>
#include <unordered_map>
#include <vector>

#include "sheetman/scb_reader.h"
#include "Tables_forward.h"

namespace rescue {
// Generated from project-rescue-xlsxs/SD_Contens.xlsx : SDTrainingLevelTable : A1
struct SDTrainingLevelRecord {
  /// SDTrainingLevel_Table
  std::int32_t id = 0;
  /// 영문명
  std::string name;
  /// 한글명
  std::string level_name;
  /// 필요 재료값
  std::int32_t currency_value = 0;
  /// 누적 재료값
  std::int32_t currency_result = 0;
  /// 체력 고정 비율
  float mhp_growth = 0.0f;
  /// 체력 비율 누적 값
  float mhp_total = 0.0f;
  /// 방어력 고정 비율
  float def_growth = 0.0f;
  /// 방어력 비율 누적 값
  float def_total = 0.0f;
  /// 공통 훈련 스테이지 진행도
  std::int32_t common_unlock_stage_id = 0;
};

class SDTrainingLevelTable {
 public:
  const std::vector<SDTrainingLevelRecord>& records() const { return records_; }

  /// The row with this Id, or nullptr when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  const SDTrainingLevelRecord* find_by_id(std::int32_t key) const {
    const auto it = by_id_.find(key);
    return it == by_id_.end() ? nullptr : &records_[it->second];
  }

  /// The row with this Id, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading `get_by_id(key)->name` at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  const SDTrainingLevelRecord& get_by_id_or_throw(std::int32_t key) const {
    const SDTrainingLevelRecord* found = find_by_id(key);

    if (found == nullptr) {
      throw sheetman::RecordNotFound(
        std::string("there is no record in table `SDTrainingLevel` that corresponds to "
                    "field `Id` value ") + std::to_string(key));
    }

    return *found;
  }

  /// Whether the table holds a row with this Id.
  bool contains_id(std::int32_t key) const {
    return by_id_.find(key) != by_id_.end();
  }

  /// Loads the table from a .scb file written by SheetMan.
  void read(const std::string& filename) {
    const std::vector<std::uint8_t> buffer = sheetman::read_all_bytes(filename);
    sheetman::ScbReader reader(buffer);

    // Column by column, matched by tag rather than position: a column this build
    // does not know is skipped by its block length, and one whose type changed
    // incompatibly fails naming the field.
    const sheetman::Header header = sheetman::read_table_header(reader);
    const std::size_t row_count = static_cast<std::size_t>(header.row_count);

    // Read into storage of its own and swapped in at the end: reading a table that is
    // already loaded is a refresh, and one that throws part way through - a truncated
    // file, a column this build cannot read - has to leave the rows already there.
    std::vector<SDTrainingLevelRecord> records;
    records.resize(row_count);

    for (const sheetman::Column& column : header.columns) {
      const std::size_t block_end = reader.position() + static_cast<std::size_t>(column.byte_length);

      switch (column.tag) {
        case 1: {
          sheetman::check_column(column, "SDTrainingLevel.Id", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "SDTrainingLevel.Id");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.id = cursor.next_i32();
          }
          break;
        }
        case 2: {
          sheetman::check_column(column, "SDTrainingLevel.Name", sheetman::kKindScalar, 1, {sheetman::kElementString});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "SDTrainingLevel.Name");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.name = cursor.next_string();
          }
          break;
        }
        case 3: {
          sheetman::check_column(column, "SDTrainingLevel.LevelName", sheetman::kKindScalar, 1, {sheetman::kElementString});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "SDTrainingLevel.LevelName");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.level_name = cursor.next_string();
          }
          break;
        }
        case 4: {
          sheetman::check_column(column, "SDTrainingLevel.CurrencyValue", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "SDTrainingLevel.CurrencyValue");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.currency_value = cursor.next_i32();
          }
          break;
        }
        case 5: {
          sheetman::check_column(column, "SDTrainingLevel.CurrencyResult", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "SDTrainingLevel.CurrencyResult");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.currency_result = cursor.next_i32();
          }
          break;
        }
        case 6: {
          sheetman::check_column(column, "SDTrainingLevel.MHPGrowth", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "SDTrainingLevel.MHPGrowth");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.mhp_growth = cursor.next_f32();
          }
          break;
        }
        case 7: {
          sheetman::check_column(column, "SDTrainingLevel.MHPTotal", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "SDTrainingLevel.MHPTotal");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.mhp_total = cursor.next_f32();
          }
          break;
        }
        case 8: {
          sheetman::check_column(column, "SDTrainingLevel.DEFGrowth", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "SDTrainingLevel.DEFGrowth");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.def_growth = cursor.next_f32();
          }
          break;
        }
        case 9: {
          sheetman::check_column(column, "SDTrainingLevel.DEFTotal", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "SDTrainingLevel.DEFTotal");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.def_total = cursor.next_f32();
          }
          break;
        }
        case 10: {
          sheetman::check_column(column, "SDTrainingLevel.CommonUnlockStageID", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "SDTrainingLevel.CommonUnlockStageID");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.common_unlock_stage_id = cursor.next_i32();
          }
          break;
        }
        default:
          // A column added after this code was generated.
          reader.skip(column.byte_length);
          break;
      }

      sheetman::check_block_end(reader, column, block_end);
    }

    publish(std::move(records));
  }

 private:
  friend class Tables;

  /// One whole load, in place of the previous one.
  ///
  /// The indexes are built here rather than by the caller, because they have to arrive
  /// with the rows: a table holding this load's rows and the last one's index would
  /// answer a lookup with a row that is no longer at that position.
  void publish(std::vector<SDTrainingLevelRecord>&& records) {
    std::unordered_map<std::int32_t, std::size_t> id_map;
    id_map.reserve(records.size());
    for (std::size_t i = 0; i < records.size(); ++i) {
      id_map.emplace(records[i].id, i);
    }

    records_ = std::move(records);
    by_id_ = std::move(id_map);
  }

  std::vector<SDTrainingLevelRecord> records_;
  std::unordered_map<std::int32_t, std::size_t> by_id_;
};

}  // namespace rescue

#endif  // SHEETMAN_GENERATED_TABLES_SD_TRAINING_LEVEL_H
