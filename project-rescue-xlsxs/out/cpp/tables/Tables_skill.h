// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

#ifndef SHEETMAN_GENERATED_TABLES_SKILL_H
#define SHEETMAN_GENERATED_TABLES_SKILL_H

#include <cstddef>
#include <cstdint>
#include <string>
#include <unordered_map>
#include <vector>

#include "sheetman/scb_reader.h"
#include "Tables_forward.h"
#include "enums/Tables_enum_skill_type.h"
#include "enums/Tables_enum_skill_sub_type.h"
#include "enums/Tables_enum_attribute_type.h"
#include "enums/Tables_enum_target_type.h"

namespace rescue {
// Generated from project-rescue-xlsxs/MasterData.xlsx : SkillTable : A1
struct SkillRecord {
  /// Skill_Table
  std::int32_t id = 0;
  /// 영문
  std::string name;
  /// 한글
  std::string skill_name;
  /// 메모
  SkillType skill_type = static_cast<SkillType>(0);
  /// 메모 참조
  SkillSubType skill_sub_type = static_cast<SkillSubType>(0);
  /// 속성 설정
  AttributeType attribute_type = static_cast<AttributeType>(0);
  /// 메모참조
  TargetType target_type = static_cast<TargetType>(0);
  /// 스킬 경로
  std::string ani_path;
  /// 스킬 사용 거리
  float skill_use_range = 0.0f;
  /// 스킬 쿨타임
  float cool_time = 0.0f;
  /// 버프 ID
  std::vector<std::int32_t> buff_id;
  /// 필살기 아이콘 경로
  std::string skill_icon;
  /// 설명
  std::string description;
};

class SkillTable {
 public:
  const std::vector<SkillRecord>& records() const { return records_; }

  /// The row with this Id, or nullptr when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  const SkillRecord* find_by_id(std::int32_t key) const {
    const auto it = by_id_.find(key);
    return it == by_id_.end() ? nullptr : &records_[it->second];
  }

  /// The row with this Id, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading `get_by_id(key)->name` at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  const SkillRecord& get_by_id_or_throw(std::int32_t key) const {
    const SkillRecord* found = find_by_id(key);

    if (found == nullptr) {
      throw sheetman::RecordNotFound(
        std::string("there is no record in table `Skill` that corresponds to "
                    "field `Id` value ") + std::to_string(key));
    }

    return *found;
  }

  /// Whether the table holds a row with this Id.
  bool contains_id(std::int32_t key) const {
    return by_id_.find(key) != by_id_.end();
  }

  /// Loads the table from a .scb file written by SheetMan.
  void read(const std::string& filename) {
    const std::vector<std::uint8_t> buffer = sheetman::read_all_bytes(filename);
    sheetman::ScbReader reader(buffer);

    // Column by column, matched by tag rather than position: a column this build
    // does not know is skipped by its block length, and one whose type changed
    // incompatibly fails naming the field.
    const sheetman::Header header = sheetman::read_table_header(reader);
    const std::size_t row_count = static_cast<std::size_t>(header.row_count);

    // Read into storage of its own and swapped in at the end: reading a table that is
    // already loaded is a refresh, and one that throws part way through - a truncated
    // file, a column this build cannot read - has to leave the rows already there.
    std::vector<SkillRecord> records;
    records.resize(row_count);

    for (const sheetman::Column& column : header.columns) {
      const std::size_t block_end = reader.position() + static_cast<std::size_t>(column.byte_length);

      switch (column.tag) {
        case 1: {
          sheetman::check_column(column, "Skill.Id", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Skill.Id");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.id = cursor.next_i32();
          }
          break;
        }
        case 2: {
          sheetman::check_column(column, "Skill.Name", sheetman::kKindScalar, 1, {sheetman::kElementString});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Skill.Name");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.name = cursor.next_string();
          }
          break;
        }
        case 3: {
          sheetman::check_column(column, "Skill.SkillName", sheetman::kKindScalar, 1, {sheetman::kElementString});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Skill.SkillName");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.skill_name = cursor.next_string();
          }
          break;
        }
        case 4: {
          sheetman::check_column(column, "Skill.SkillType", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Skill.SkillType");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.skill_type = static_cast<SkillType>(cursor.next_i32());
          }
          break;
        }
        case 5: {
          sheetman::check_column(column, "Skill.SkillSubType", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Skill.SkillSubType");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.skill_sub_type = static_cast<SkillSubType>(cursor.next_i32());
          }
          break;
        }
        case 6: {
          sheetman::check_column(column, "Skill.AttributeType", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Skill.AttributeType");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.attribute_type = static_cast<AttributeType>(cursor.next_i32());
          }
          break;
        }
        case 7: {
          sheetman::check_column(column, "Skill.TargetType", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Skill.TargetType");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.target_type = static_cast<TargetType>(cursor.next_i32());
          }
          break;
        }
        case 8: {
          sheetman::check_column(column, "Skill.AniPath", sheetman::kKindScalar, 1, {sheetman::kElementString});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Skill.AniPath");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.ani_path = cursor.next_string();
          }
          break;
        }
        case 9: {
          sheetman::check_column(column, "Skill.SkillUseRange", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.skill_use_range);
          }
          break;
        }
        case 10: {
          sheetman::check_column(column, "Skill.CoolTime", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.cool_time);
          }
          break;
        }
        case 11: {
          sheetman::check_column(column, "Skill.BuffID", sheetman::kKindVarArray, 0, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            const std::int32_t element_count = reader.read_counter32();
            record.buff_id.resize(static_cast<std::size_t>(element_count));
            for (std::int32_t j = 0; j < element_count; ++j) {
              reader.read_i32_as(column.element, record.buff_id[static_cast<std::size_t>(j)]);
            }
          }
          break;
        }
        case 12: {
          sheetman::check_column(column, "Skill.SkillIcon", sheetman::kKindScalar, 1, {sheetman::kElementString});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Skill.SkillIcon");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.skill_icon = cursor.next_string();
          }
          break;
        }
        case 13: {
          sheetman::check_column(column, "Skill.Description", sheetman::kKindScalar, 1, {sheetman::kElementString});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Skill.Description");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.description = cursor.next_string();
          }
          break;
        }
        default:
          // A column added after this code was generated.
          reader.skip(column.byte_length);
          break;
      }

      sheetman::check_block_end(reader, column, block_end);
    }

    publish(std::move(records));
  }

 private:
  friend class Tables;

  /// One whole load, in place of the previous one.
  ///
  /// The indexes are built here rather than by the caller, because they have to arrive
  /// with the rows: a table holding this load's rows and the last one's index would
  /// answer a lookup with a row that is no longer at that position.
  void publish(std::vector<SkillRecord>&& records) {
    std::unordered_map<std::int32_t, std::size_t> id_map;
    id_map.reserve(records.size());
    for (std::size_t i = 0; i < records.size(); ++i) {
      id_map.emplace(records[i].id, i);
    }

    records_ = std::move(records);
    by_id_ = std::move(id_map);
  }

  std::vector<SkillRecord> records_;
  std::unordered_map<std::int32_t, std::size_t> by_id_;
};

}  // namespace rescue

#endif  // SHEETMAN_GENERATED_TABLES_SKILL_H
