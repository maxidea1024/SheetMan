// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

#ifndef SHEETMAN_GENERATED_TABLES_TRAIT_DUNGEON_STAGE_TABLE_H
#define SHEETMAN_GENERATED_TABLES_TRAIT_DUNGEON_STAGE_TABLE_H

#include <cstddef>
#include <cstdint>
#include <string>
#include <unordered_map>
#include <vector>

#include "sheetman/lite_binary_reader.h"
#include "Tables_forward.h"
#include "enums/Tables_enum_dungeon_type.h"

namespace rescue {
// Generated from project-rescue-xlsxs/DailyDungeon.xlsx : TraitDungeonStageTable : A1
struct TraitDungeonStageTableRecord {
  /// TraitDungeonStage_Table
  std::int32_t id = 0;
  /// 영문
  std::string name;
  /// 한글
  std::string stage_name;
  /// 던전 아이디
  DungeonType dungeon_type = static_cast<DungeonType>(0);
  /// 던전 단계
  std::int32_t dungeon_floor = 0;
  /// 제한 시간
  std::int32_t time_limit = 0;
  /// 생성 몬스터 ID
  std::vector<std::int32_t> spawn_ids;
  /// 보스 ID
  std::vector<std::int32_t> boss_id;
  /// 위치 생성 가능 횟수
  std::int32_t spawn_point_count = 0;
  /// 최대 생성 수
  std::int32_t spawn_max_count = 0;
  /// 다음 포인터 생성 시간
  float spawn_next_time = 0.0f;
  /// 스테이지 클리어 조건
  std::int32_t stage_clear_count = 0;
  /// 권장 전투력
  std::string recommend_power;
  /// 보상 설정
  std::int32_t reward_id = 0;
  /// 던전 공격력 증가 값
  float atk_up_percent = 0.0f;
  /// 던전 방어력 증가 값
  float def_up_percent = 0.0f;
  /// 던전 체력 증가 값
  float mhp_up_percent = 0.0f;
  /// 던전 이미지
  std::string dungeon_image_path;
  /// 몬스터 이미지
  std::string monster_image_path;
  /// AssetData 경로(Stage처럼 에셋만 추가하면 바뀌게)
  std::string asset_data_path;
};

class TraitDungeonStageTableTable {
 public:
  const std::vector<TraitDungeonStageTableRecord>& records() const { return records_; }

  /// The row with this Id, or nullptr when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  const TraitDungeonStageTableRecord* find_by_id(std::int32_t key) const {
    const auto it = by_id_.find(key);
    return it == by_id_.end() ? nullptr : &records_[it->second];
  }

  /// The row with this Id, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading `get_by_id(key)->name` at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  const TraitDungeonStageTableRecord& get_by_id_or_throw(std::int32_t key) const {
    const TraitDungeonStageTableRecord* found = find_by_id(key);

    if (found == nullptr) {
      throw sheetman::RecordNotFound(
        std::string("there is no record in table `TraitDungeonStageTable` that corresponds to "
                    "field `Id` value ") + std::to_string(key));
    }

    return *found;
  }

  /// Whether the table holds a row with this Id.
  bool contains_id(std::int32_t key) const {
    return by_id_.find(key) != by_id_.end();
  }

  /// Loads the table from a .table file written by SheetMan.
  void read(const std::string& filename) {
    const std::vector<std::uint8_t> buffer = sheetman::read_all_bytes(filename);
    sheetman::LiteBinaryReader reader(buffer);

    // Column by column, matched by tag rather than position: a column this build
    // does not know is skipped by its block length, and one whose type changed
    // incompatibly fails naming the field.
    const sheetman::Header header = sheetman::read_table_header(reader);
    const std::size_t row_count = static_cast<std::size_t>(header.row_count);

    // Read into storage of its own and swapped in at the end: reading a table that is
    // already loaded is a refresh, and one that throws part way through - a truncated
    // file, a column this build cannot read - has to leave the rows already there.
    std::vector<TraitDungeonStageTableRecord> records;
    records.resize(row_count);

    for (const sheetman::Column& column : header.columns) {
      const std::size_t block_end = reader.position() + static_cast<std::size_t>(column.byte_length);

      switch (column.tag) {
        case 1: {
          sheetman::check_column(column, "TraitDungeonStageTable.Id", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.id);
          }
          break;
        }
        case 2: {
          sheetman::check_column(column, "TraitDungeonStageTable.Name", sheetman::kKindScalar, 1, {sheetman::kElementString});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.name);
          }
          break;
        }
        case 3: {
          sheetman::check_column(column, "TraitDungeonStageTable.StageName", sheetman::kKindScalar, 1, {sheetman::kElementString});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.stage_name);
          }
          break;
        }
        case 4: {
          sheetman::check_column(column, "TraitDungeonStageTable.DungeonType", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_enum(record.dungeon_type);
          }
          break;
        }
        case 5: {
          sheetman::check_column(column, "TraitDungeonStageTable.DungeonFloor", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.dungeon_floor);
          }
          break;
        }
        case 6: {
          sheetman::check_column(column, "TraitDungeonStageTable.TimeLimit", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.time_limit);
          }
          break;
        }
        case 7: {
          sheetman::check_column(column, "TraitDungeonStageTable.SpawnIds", sheetman::kKindVarArray, 0, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            const std::int32_t element_count = reader.read_counter32();
            record.spawn_ids.resize(static_cast<std::size_t>(element_count));
            for (std::int32_t j = 0; j < element_count; ++j) {
              reader.read_i32_as(column.element, record.spawn_ids[static_cast<std::size_t>(j)]);
            }
          }
          break;
        }
        case 8: {
          sheetman::check_column(column, "TraitDungeonStageTable.BossID", sheetman::kKindVarArray, 0, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            const std::int32_t element_count = reader.read_counter32();
            record.boss_id.resize(static_cast<std::size_t>(element_count));
            for (std::int32_t j = 0; j < element_count; ++j) {
              reader.read_i32_as(column.element, record.boss_id[static_cast<std::size_t>(j)]);
            }
          }
          break;
        }
        case 9: {
          sheetman::check_column(column, "TraitDungeonStageTable.SpawnPointCount", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.spawn_point_count);
          }
          break;
        }
        case 10: {
          sheetman::check_column(column, "TraitDungeonStageTable.SpawnMaxCount", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.spawn_max_count);
          }
          break;
        }
        case 11: {
          sheetman::check_column(column, "TraitDungeonStageTable.SpawnNextTime", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.spawn_next_time);
          }
          break;
        }
        case 12: {
          sheetman::check_column(column, "TraitDungeonStageTable.StageClearCount", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.stage_clear_count);
          }
          break;
        }
        case 13: {
          sheetman::check_column(column, "TraitDungeonStageTable.RecommendPower", sheetman::kKindScalar, 1, {sheetman::kElementString});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.recommend_power);
          }
          break;
        }
        case 14: {
          sheetman::check_column(column, "TraitDungeonStageTable.RewardID", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read_i32_as(column.element, record.reward_id);
          }
          break;
        }
        case 15: {
          sheetman::check_column(column, "TraitDungeonStageTable.AtkUpPercent", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.atk_up_percent);
          }
          break;
        }
        case 16: {
          sheetman::check_column(column, "TraitDungeonStageTable.DefUpPercent", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.def_up_percent);
          }
          break;
        }
        case 17: {
          sheetman::check_column(column, "TraitDungeonStageTable.MHPUpPercent", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.mhp_up_percent);
          }
          break;
        }
        case 18: {
          sheetman::check_column(column, "TraitDungeonStageTable.DungeonImagePath", sheetman::kKindScalar, 1, {sheetman::kElementString});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.dungeon_image_path);
          }
          break;
        }
        case 19: {
          sheetman::check_column(column, "TraitDungeonStageTable.MonsterImagePath", sheetman::kKindScalar, 1, {sheetman::kElementString});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.monster_image_path);
          }
          break;
        }
        case 20: {
          sheetman::check_column(column, "TraitDungeonStageTable.AssetDataPath", sheetman::kKindScalar, 1, {sheetman::kElementString});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            reader.read(record.asset_data_path);
          }
          break;
        }
        default:
          // A column added after this code was generated.
          reader.skip(column.byte_length);
          break;
      }

      sheetman::check_block_end(reader, column, block_end);
    }

    publish(std::move(records));
  }

 private:
  friend class Tables;

  /// One whole load, in place of the previous one.
  ///
  /// The indexes are built here rather than by the caller, because they have to arrive
  /// with the rows: a table holding this load's rows and the last one's index would
  /// answer a lookup with a row that is no longer at that position.
  void publish(std::vector<TraitDungeonStageTableRecord>&& records) {
    std::unordered_map<std::int32_t, std::size_t> id_map;
    id_map.reserve(records.size());
    for (std::size_t i = 0; i < records.size(); ++i) {
      id_map.emplace(records[i].id, i);
    }

    records_ = std::move(records);
    by_id_ = std::move(id_map);
  }

  std::vector<TraitDungeonStageTableRecord> records_;
  std::unordered_map<std::int32_t, std::size_t> by_id_;
};

}  // namespace rescue

#endif  // SHEETMAN_GENERATED_TABLES_TRAIT_DUNGEON_STAGE_TABLE_H
