// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections;
using System.Collections.Generic;
using System.IO;
using System.Threading.Tasks;

// SheetMan's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using SheetMan.Binary;

namespace Rescue.Tables
{
    [System.Serializable]
    public partial class ArtifactTable
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// Artifact_Table
            /// </summary>
            public int Id => _id;

            /// <summary>
            /// 영문
            /// </summary>
            public string Name => _name;

            /// <summary>
            /// 아티팩트 타입
            /// </summary>
            public global::Rescue.Tables.ArtifactJobType ArtifactType => _artifactType;

            /// <summary>
            /// 아티팩트 태생 등급
            /// </summary>
            public global::Rescue.Tables.GradeType Grade => _grade;

            /// <summary>
            /// 장착 버프 효과
            /// </summary>
            public int[] EquipSkillID => _equipSkillID;

            /// <summary>
            /// 장착 스탯 타입
            /// </summary>
            public global::Rescue.Tables.StatType EquipStatType => _equipStatType;

            /// <summary>
            /// 장착 스탯 값
            /// </summary>
            public float EquipStatRate => _equipStatRate;

            /// <summary>
            /// 보유 스탯 타입
            /// </summary>
            public global::Rescue.Tables.StatType CollectionType => _collectionType;

            /// <summary>
            /// 보유 스탯 값
            /// </summary>
            public float CollectionVlaue => _collectionVlaue;

            /// <summary>
            /// 아이콘 경로
            /// </summary>
            public string IconPath => _iconPath;

            /// <summary>
            /// 머티리얼 경로
            /// </summary>
            public string MaterialPath => _materialPath;

            /// <summary>
            /// 설명
            /// </summary>
            public string Description => _description;
            #endregion

            #region Storage
            internal int _id;
            internal string _name = "";
            internal global::Rescue.Tables.ArtifactJobType _artifactType;
            internal global::Rescue.Tables.GradeType _grade;
            internal int[] _equipSkillID = System.Array.Empty<int>();
            internal global::Rescue.Tables.StatType _equipStatType;
            internal float _equipStatRate;
            internal global::Rescue.Tables.StatType _collectionType;
            internal float _collectionVlaue;
            internal string _iconPath = "";
            internal string _materialPath = "";
            internal string _description = "";
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"Id\":"); ToStringHelper.ToString(Id, sb);
                sb.Append(",\"Name\":"); ToStringHelper.ToString(Name, sb);
                sb.Append(",\"ArtifactType\":"); ToStringHelper.ToString(ArtifactType, sb);
                sb.Append(",\"Grade\":"); ToStringHelper.ToString(Grade, sb);
                sb.Append(",\"EquipSkillID\":"); ToStringHelper.ToString(EquipSkillID, sb);
                sb.Append(",\"EquipStatType\":"); ToStringHelper.ToString(EquipStatType, sb);
                sb.Append(",\"EquipStatRate\":"); ToStringHelper.ToString(EquipStatRate, sb);
                sb.Append(",\"CollectionType\":"); ToStringHelper.ToString(CollectionType, sb);
                sb.Append(",\"CollectionVlaue\":"); ToStringHelper.ToString(CollectionVlaue, sb);
                sb.Append(",\"IconPath\":"); ToStringHelper.ToString(IconPath, sb);
                sb.Append(",\"MaterialPath\":"); ToStringHelper.ToString(MaterialPath, sb);
                sb.Append(",\"Description\":"); ToStringHelper.ToString(Description, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "Id", "Name", "ArtifactType", "Grade", "EquipSkillID", "EquipStatType", "EquipStatRate", "CollectionType", "CollectionVlaue", "IconPath", "MaterialPath", "Description" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.Id, r.Name, r.ArtifactType, r.Grade, r.EquipSkillID, r.EquipStatType, r.EquipStatRate, r.CollectionType, r.CollectionVlaue, r.IconPath, r.MaterialPath, r.Description });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        #region Indexing by 'Id'
        public Dictionary<int, Record> RecordsById => _recordsById;
        private Dictionary<int, Record> _recordsById = new Dictionary<int, Record>();

        /// <summary>
        /// The row with this `Id`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language SheetMan generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindById(int key)
            => _recordsById.TryGetValue(key, out Record record) ? record : null;

        /// <summary>
        /// The row with this `Id`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetById(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public Record GetByIdOrThrow(int key)
        {
            if (!_recordsById.TryGetValue(key, out Record record))
                throw new SheetManException($"There is no record in table `Artifact` that corresponds to field `Id` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `Id`.</summary>
        public bool ContainsId(int key) => _recordsById.ContainsKey(key);
        #endregion // Indexing by `Id`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await Tables.ReadAllBytesAsync(filename);
            var reader = new ScbReader(bytes);
            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(ScbReader reader)
        {
            var columns = ScbTable.ReadHeader(reader, out int count);
            ScbColumnCursor cursor;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        ScbTable.CheckColumn(column, "Artifact.Id", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "Artifact.Id");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._id = cursor.NextI32();
                        }
                        break;

                    case 2:
                        ScbTable.CheckColumn(column, "Artifact.Name", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        cursor = new ScbColumnCursor(reader, column, count, "Artifact.Name");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._name = cursor.NextString();
                        }
                        break;

                    case 3:
                        ScbTable.CheckColumn(column, "Artifact.ArtifactType", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "Artifact.ArtifactType");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._artifactType = (global::Rescue.Tables.ArtifactJobType)cursor.NextI32();
                        }
                        break;

                    case 4:
                        ScbTable.CheckColumn(column, "Artifact.Grade", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "Artifact.Grade");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._grade = (global::Rescue.Tables.GradeType)cursor.NextI32();
                        }
                        break;

                    case 5:
                        ScbTable.CheckColumn(column, "Artifact.EquipSkillID", ScbTable.KindVarArray, 0, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.TryReadCounter32(out int elementCount);
                            record._equipSkillID = new int[elementCount];
                            for (int j = 0; j < elementCount; ++j)
                            {
                                record._equipSkillID[j] = reader.ReadI32As(column.Element);
                            }
                        }
                        break;

                    case 6:
                        ScbTable.CheckColumn(column, "Artifact.EquipStatType", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "Artifact.EquipStatType");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._equipStatType = (global::Rescue.Tables.StatType)cursor.NextI32();
                        }
                        break;

                    case 7:
                        ScbTable.CheckColumn(column, "Artifact.EquipStatRate", ScbTable.KindScalar, 1, ScbTable.ElementF32);
                        cursor = new ScbColumnCursor(reader, column, count, "Artifact.EquipStatRate");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._equipStatRate = cursor.NextF32();
                        }
                        break;

                    case 8:
                        ScbTable.CheckColumn(column, "Artifact.CollectionType", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "Artifact.CollectionType");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._collectionType = (global::Rescue.Tables.StatType)cursor.NextI32();
                        }
                        break;

                    case 9:
                        ScbTable.CheckColumn(column, "Artifact.CollectionVlaue", ScbTable.KindScalar, 1, ScbTable.ElementF32);
                        cursor = new ScbColumnCursor(reader, column, count, "Artifact.CollectionVlaue");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._collectionVlaue = cursor.NextF32();
                        }
                        break;

                    case 10:
                        ScbTable.CheckColumn(column, "Artifact.IconPath", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        cursor = new ScbColumnCursor(reader, column, count, "Artifact.IconPath");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._iconPath = cursor.NextString();
                        }
                        break;

                    case 11:
                        ScbTable.CheckColumn(column, "Artifact.MaterialPath", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        cursor = new ScbColumnCursor(reader, column, count, "Artifact.MaterialPath");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._materialPath = cursor.NextString();
                        }
                        break;

                    case 12:
                        ScbTable.CheckColumn(column, "Artifact.Description", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        cursor = new ScbColumnCursor(reader, column, count, "Artifact.Description");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._description = cursor.NextString();
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                ScbTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsById = new Dictionary<int, Record>(count);
            foreach (var record in records)
                recordsById.Add(record.Id, record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsById = recordsById;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }

} // namespace Rescue.Tables
