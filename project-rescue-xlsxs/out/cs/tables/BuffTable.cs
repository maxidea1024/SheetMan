// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections;
using System.Collections.Generic;
using System.IO;
using System.Threading.Tasks;

// SheetMan's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using SheetMan.Binary;

namespace Rescue.Tables
{
    [System.Serializable]
    public partial class BuffTable
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// ID
            /// </summary>
            public int Id => _id;

            /// <summary>
            /// SkillTable에서 Name을 불러와서 사용
            /// </summary>
            public string BuffName => _buffName;

            /// <summary>
            /// 한글 명
            /// </summary>
            public string Name => _name;

            /// <summary>
            /// 버프 타입_메모확인
            /// </summary>
            public global::Rescue.Tables.SkillType SkillType => _skillType;

            /// <summary>
            /// 어떤 종류의 버프 인지
            /// </summary>
            public global::Rescue.Tables.StatType StatType => _statType;

            /// <summary>
            /// 버프 우선 적용 순위
            /// </summary>
            public int Priority => _priority;

            /// <summary>
            /// 버프 속성
            /// </summary>
            public global::Rescue.Tables.AttributeType AttributeType => _attributeType;

            /// <summary>
            /// 타겟 설정
            /// </summary>
            public global::Rescue.Tables.TargetType TargetType => _targetType;

            /// <summary>
            /// 상태에 따른 발동 조건
            /// </summary>
            public global::Rescue.Tables.BuffConditionType BuffConditionType => _buffConditionType;

            /// <summary>
            /// 증가되는 값(상수)
            /// </summary>
            public int BuffValue => _buffValue;

            /// <summary>
            /// 증가되는 값(%)
            /// </summary>
            public float BuffValueRate => _buffValueRate;

            /// <summary>
            /// 버프 시간
            /// </summary>
            public int BuffTime => _buffTime;

            /// <summary>
            /// 버프 틱 시간
            /// </summary>
            public int BuffTickTime => _buffTickTime;

            /// <summary>
            /// 버프 최대 스택
            /// </summary>
            public int BuffMaxStack => _buffMaxStack;

            /// <summary>
            /// 버프 쿨타임 1초 = 1000
            /// </summary>
            public float BuffCoolDownTime => _buffCoolDownTime;

            /// <summary>
            /// 버프 표시 유무 FALSE/TRUE
            /// </summary>
            public bool IsVisible => _isVisible;

            /// <summary>
            /// 버프 아이콘 경로
            /// </summary>
            public string IconPath => _iconPath;

            /// <summary>
            /// 버프 튤팁
            /// </summary>
            public string Description => _description;
            #endregion

            #region Storage
            internal int _id;
            internal string _buffName = "";
            internal string _name = "";
            internal global::Rescue.Tables.SkillType _skillType;
            internal global::Rescue.Tables.StatType _statType;
            internal int _priority;
            internal global::Rescue.Tables.AttributeType _attributeType;
            internal global::Rescue.Tables.TargetType _targetType;
            internal global::Rescue.Tables.BuffConditionType _buffConditionType;
            internal int _buffValue;
            internal float _buffValueRate;
            internal int _buffTime;
            internal int _buffTickTime;
            internal int _buffMaxStack;
            internal float _buffCoolDownTime;
            internal bool _isVisible;
            internal string _iconPath = "";
            internal string _description = "";
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"Id\":"); ToStringHelper.ToString(Id, sb);
                sb.Append(",\"BuffName\":"); ToStringHelper.ToString(BuffName, sb);
                sb.Append(",\"Name\":"); ToStringHelper.ToString(Name, sb);
                sb.Append(",\"SkillType\":"); ToStringHelper.ToString(SkillType, sb);
                sb.Append(",\"StatType\":"); ToStringHelper.ToString(StatType, sb);
                sb.Append(",\"Priority\":"); ToStringHelper.ToString(Priority, sb);
                sb.Append(",\"AttributeType\":"); ToStringHelper.ToString(AttributeType, sb);
                sb.Append(",\"TargetType\":"); ToStringHelper.ToString(TargetType, sb);
                sb.Append(",\"BuffConditionType\":"); ToStringHelper.ToString(BuffConditionType, sb);
                sb.Append(",\"BuffValue\":"); ToStringHelper.ToString(BuffValue, sb);
                sb.Append(",\"BuffValueRate\":"); ToStringHelper.ToString(BuffValueRate, sb);
                sb.Append(",\"BuffTime\":"); ToStringHelper.ToString(BuffTime, sb);
                sb.Append(",\"BuffTickTime\":"); ToStringHelper.ToString(BuffTickTime, sb);
                sb.Append(",\"BuffMaxStack\":"); ToStringHelper.ToString(BuffMaxStack, sb);
                sb.Append(",\"BuffCoolDownTime\":"); ToStringHelper.ToString(BuffCoolDownTime, sb);
                sb.Append(",\"IsVisible\":"); ToStringHelper.ToString(IsVisible, sb);
                sb.Append(",\"IconPath\":"); ToStringHelper.ToString(IconPath, sb);
                sb.Append(",\"Description\":"); ToStringHelper.ToString(Description, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "Id", "BuffName", "Name", "SkillType", "StatType", "Priority", "AttributeType", "TargetType", "BuffConditionType", "BuffValue", "BuffValueRate", "BuffTime", "BuffTickTime", "BuffMaxStack", "BuffCoolDownTime", "IsVisible", "IconPath", "Description" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.Id, r.BuffName, r.Name, r.SkillType, r.StatType, r.Priority, r.AttributeType, r.TargetType, r.BuffConditionType, r.BuffValue, r.BuffValueRate, r.BuffTime, r.BuffTickTime, r.BuffMaxStack, r.BuffCoolDownTime, r.IsVisible, r.IconPath, r.Description });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        #region Indexing by 'Id'
        public Dictionary<int, Record> RecordsById => _recordsById;
        private Dictionary<int, Record> _recordsById = new Dictionary<int, Record>();

        /// <summary>
        /// The row with this `Id`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language SheetMan generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindById(int key)
            => _recordsById.TryGetValue(key, out Record record) ? record : null;

        /// <summary>
        /// The row with this `Id`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetById(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public Record GetByIdOrThrow(int key)
        {
            if (!_recordsById.TryGetValue(key, out Record record))
                throw new SheetManException($"There is no record in table `Buff` that corresponds to field `Id` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `Id`.</summary>
        public bool ContainsId(int key) => _recordsById.ContainsKey(key);
        #endregion // Indexing by `Id`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await Tables.ReadAllBytesAsync(filename);
            var reader = new ScbReader(bytes);
            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(ScbReader reader)
        {
            var columns = ScbTable.ReadHeader(reader, out int count);
            int tempEnumInt = 0;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        ScbTable.CheckColumn(column, "Buff.Id", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._id = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 2:
                        ScbTable.CheckColumn(column, "Buff.BuffName", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._buffName);
                        }
                        break;

                    case 3:
                        ScbTable.CheckColumn(column, "Buff.Name", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._name);
                        }
                        break;

                    case 4:
                        ScbTable.CheckColumn(column, "Buff.SkillType", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._skillType = (global::Rescue.Tables.SkillType)tempEnumInt;
                        }
                        break;

                    case 5:
                        ScbTable.CheckColumn(column, "Buff.StatType", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._statType = (global::Rescue.Tables.StatType)tempEnumInt;
                        }
                        break;

                    case 6:
                        ScbTable.CheckColumn(column, "Buff.Priority", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._priority = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 7:
                        ScbTable.CheckColumn(column, "Buff.AttributeType", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._attributeType = (global::Rescue.Tables.AttributeType)tempEnumInt;
                        }
                        break;

                    case 8:
                        ScbTable.CheckColumn(column, "Buff.TargetType", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._targetType = (global::Rescue.Tables.TargetType)tempEnumInt;
                        }
                        break;

                    case 9:
                        ScbTable.CheckColumn(column, "Buff.BuffConditionType", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._buffConditionType = (global::Rescue.Tables.BuffConditionType)tempEnumInt;
                        }
                        break;

                    case 10:
                        ScbTable.CheckColumn(column, "Buff.BuffValue", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._buffValue = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 11:
                        ScbTable.CheckColumn(column, "Buff.BuffValueRate", ScbTable.KindScalar, 1, ScbTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._buffValueRate);
                        }
                        break;

                    case 12:
                        ScbTable.CheckColumn(column, "Buff.BuffTime", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._buffTime = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 13:
                        ScbTable.CheckColumn(column, "Buff.BuffTickTime", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._buffTickTime = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 14:
                        ScbTable.CheckColumn(column, "Buff.BuffMaxStack", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._buffMaxStack = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 15:
                        ScbTable.CheckColumn(column, "Buff.BuffCoolDownTime", ScbTable.KindScalar, 1, ScbTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._buffCoolDownTime);
                        }
                        break;

                    case 16:
                        ScbTable.CheckColumn(column, "Buff.IsVisible", ScbTable.KindScalar, 1, ScbTable.ElementBool);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._isVisible);
                        }
                        break;

                    case 17:
                        ScbTable.CheckColumn(column, "Buff.IconPath", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._iconPath);
                        }
                        break;

                    case 18:
                        ScbTable.CheckColumn(column, "Buff.Description", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._description);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                ScbTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsById = new Dictionary<int, Record>(count);
            foreach (var record in records)
                recordsById.Add(record.Id, record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsById = recordsById;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }

} // namespace Rescue.Tables
