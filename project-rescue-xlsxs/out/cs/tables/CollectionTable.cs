// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections;
using System.Collections.Generic;
using System.IO;
using System.Threading.Tasks;

// SheetMan's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using SheetMan.Binary;

namespace Rescue.Tables
{
    [System.Serializable]
    public partial class CollectionTable
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// CollectionID
            /// </summary>
            public int Id => _id;

            /// <summary>
            /// 영문
            /// </summary>
            public string Name => _name;

            /// <summary>
            /// 캐릭터ID
            /// </summary>
            public int CharacterID => _characterID;

            /// <summary>
            /// 보상 UI분기 타입
            /// </summary>
            public global::Rescue.Tables.CollectionTabType TabType => _tabType;

            /// <summary>
            /// 조건 값
            /// </summary>
            public int ConditionID => _conditionID;

            /// <summary>
            /// 재화 보상 타입
            /// </summary>
            public global::Rescue.Tables.CurrencyType RewardType => _rewardType;

            /// <summary>
            /// 재화 보상 값
            /// </summary>
            public int RewardValue => _rewardValue;

            /// <summary>
            /// 스탯 보상 타입
            /// </summary>
            public global::Rescue.Tables.StatType RewardStatType1 => _rewardStatType1;

            /// <summary>
            /// 스탯 보상(%)
            /// </summary>
            public float RewardStatRate => _rewardStatRate;

            /// <summary>
            /// 스탯 보상 타입
            /// </summary>
            public global::Rescue.Tables.StatType RewardStatType2 => _rewardStatType2;

            /// <summary>
            /// 스탯 보상(+)
            /// </summary>
            public int RewardStatValue => _rewardStatValue;

            /// <summary>
            /// 바로가기 ID
            /// </summary>
            public int ShortCutID => _shortCutID;
            #endregion

            #region Storage
            internal int _id;
            internal string _name = "";
            internal int _characterID;
            internal global::Rescue.Tables.CollectionTabType _tabType;
            internal int _conditionID;
            internal global::Rescue.Tables.CurrencyType _rewardType;
            internal int _rewardValue;
            internal global::Rescue.Tables.StatType _rewardStatType1;
            internal float _rewardStatRate;
            internal global::Rescue.Tables.StatType _rewardStatType2;
            internal int _rewardStatValue;
            internal int _shortCutID;
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"Id\":"); ToStringHelper.ToString(Id, sb);
                sb.Append(",\"Name\":"); ToStringHelper.ToString(Name, sb);
                sb.Append(",\"CharacterID\":"); ToStringHelper.ToString(CharacterID, sb);
                sb.Append(",\"TabType\":"); ToStringHelper.ToString(TabType, sb);
                sb.Append(",\"ConditionID\":"); ToStringHelper.ToString(ConditionID, sb);
                sb.Append(",\"RewardType\":"); ToStringHelper.ToString(RewardType, sb);
                sb.Append(",\"RewardValue\":"); ToStringHelper.ToString(RewardValue, sb);
                sb.Append(",\"RewardStatType1\":"); ToStringHelper.ToString(RewardStatType1, sb);
                sb.Append(",\"RewardStatRate\":"); ToStringHelper.ToString(RewardStatRate, sb);
                sb.Append(",\"RewardStatType2\":"); ToStringHelper.ToString(RewardStatType2, sb);
                sb.Append(",\"RewardStatValue\":"); ToStringHelper.ToString(RewardStatValue, sb);
                sb.Append(",\"ShortCutID\":"); ToStringHelper.ToString(ShortCutID, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "Id", "Name", "CharacterID", "TabType", "ConditionID", "RewardType", "RewardValue", "RewardStatType1", "RewardStatRate", "RewardStatType2", "RewardStatValue", "ShortCutID" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.Id, r.Name, r.CharacterID, r.TabType, r.ConditionID, r.RewardType, r.RewardValue, r.RewardStatType1, r.RewardStatRate, r.RewardStatType2, r.RewardStatValue, r.ShortCutID });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        #region Indexing by 'Id'
        public Dictionary<int, Record> RecordsById => _recordsById;
        private Dictionary<int, Record> _recordsById = new Dictionary<int, Record>();

        /// <summary>
        /// The row with this `Id`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language SheetMan generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindById(int key)
            => _recordsById.TryGetValue(key, out Record record) ? record : null;

        /// <summary>
        /// The row with this `Id`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetById(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public Record GetByIdOrThrow(int key)
        {
            if (!_recordsById.TryGetValue(key, out Record record))
                throw new SheetManException($"There is no record in table `Collection` that corresponds to field `Id` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `Id`.</summary>
        public bool ContainsId(int key) => _recordsById.ContainsKey(key);
        #endregion // Indexing by `Id`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await Tables.ReadAllBytesAsync(filename);
            var reader = new ScbReader(bytes);
            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(ScbReader reader)
        {
            var columns = ScbTable.ReadHeader(reader, out int count);
            ScbColumnCursor cursor;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        ScbTable.CheckColumn(column, "Collection.Id", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "Collection.Id");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._id = cursor.NextI32();
                        }
                        break;

                    case 2:
                        ScbTable.CheckColumn(column, "Collection.Name", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        cursor = new ScbColumnCursor(reader, column, count, "Collection.Name");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._name = cursor.NextString();
                        }
                        break;

                    case 3:
                        ScbTable.CheckColumn(column, "Collection.CharacterID", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "Collection.CharacterID");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._characterID = cursor.NextI32();
                        }
                        break;

                    case 4:
                        ScbTable.CheckColumn(column, "Collection.TabType", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "Collection.TabType");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._tabType = (global::Rescue.Tables.CollectionTabType)cursor.NextI32();
                        }
                        break;

                    case 5:
                        ScbTable.CheckColumn(column, "Collection.ConditionID", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "Collection.ConditionID");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._conditionID = cursor.NextI32();
                        }
                        break;

                    case 6:
                        ScbTable.CheckColumn(column, "Collection.RewardType", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "Collection.RewardType");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._rewardType = (global::Rescue.Tables.CurrencyType)cursor.NextI32();
                        }
                        break;

                    case 7:
                        ScbTable.CheckColumn(column, "Collection.RewardValue", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "Collection.RewardValue");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._rewardValue = cursor.NextI32();
                        }
                        break;

                    case 8:
                        ScbTable.CheckColumn(column, "Collection.RewardStatType1", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "Collection.RewardStatType1");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._rewardStatType1 = (global::Rescue.Tables.StatType)cursor.NextI32();
                        }
                        break;

                    case 9:
                        ScbTable.CheckColumn(column, "Collection.RewardStatRate", ScbTable.KindScalar, 1, ScbTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._rewardStatRate);
                        }
                        break;

                    case 10:
                        ScbTable.CheckColumn(column, "Collection.RewardStatType2", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "Collection.RewardStatType2");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._rewardStatType2 = (global::Rescue.Tables.StatType)cursor.NextI32();
                        }
                        break;

                    case 11:
                        ScbTable.CheckColumn(column, "Collection.RewardStatValue", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "Collection.RewardStatValue");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._rewardStatValue = cursor.NextI32();
                        }
                        break;

                    case 12:
                        ScbTable.CheckColumn(column, "Collection.ShortCutID", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "Collection.ShortCutID");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._shortCutID = cursor.NextI32();
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                ScbTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsById = new Dictionary<int, Record>(count);
            foreach (var record in records)
                recordsById.Add(record.Id, record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsById = recordsById;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }

} // namespace Rescue.Tables
