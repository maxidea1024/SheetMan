// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections;
using System.Collections.Generic;
using System.IO;
using System.Threading.Tasks;

// SheetMan's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using SheetMan.Binary;

namespace Rescue.Tables
{
    [System.Serializable]
    public partial class DailyDungeonInfoTable
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// DailyDungeonInfo_Table
            /// </summary>
            public int Id => _id;

            /// <summary>
            /// 영문명
            /// </summary>
            public string Name => _name;

            /// <summary>
            /// 한글명
            /// </summary>
            public string DungeonName => _dungeonName;

            /// <summary>
            /// 던전 타입
            /// </summary>
            public global::Rescue.Tables.DungeonType DungeonType => _dungeonType;

            /// <summary>
            /// 출력 우선 순위
            /// </summary>
            public int Priority => _priority;

            /// <summary>
            /// 던전 입장 타입
            /// </summary>
            public global::Rescue.Tables.CycleType CycleType => _cycleType;

            /// <summary>
            /// 해금 조건
            /// </summary>
            public int EnterCondition => _enterCondition;

            /// <summary>
            /// 입장 재화
            /// </summary>
            public global::Rescue.Tables.CurrencyType EnterCurrencyType => _enterCurrencyType;

            /// <summary>
            /// 던전 버프 ID
            /// </summary>
            public int DungeonBuffID => _dungeonBuffID;

            /// <summary>
            /// 버프 수치 증가값
            /// </summary>
            public float BuffValueRate => _buffValueRate;

            /// <summary>
            /// 입장 값
            /// </summary>
            public int EnterCurrencyValue => _enterCurrencyValue;

            /// <summary>
            /// 무료입장 재화 타입
            /// </summary>
            public global::Rescue.Tables.CurrencyType FreeEnterCurrencyType => _freeEnterCurrencyType;

            /// <summary>
            /// 일일 광고 입장 횟수
            /// </summary>
            public int ADEnterCount => _aDEnterCount;

            /// <summary>
            /// 일일 입장권 최대 소모 횟수
            /// </summary>
            public int EnterCurrencyCountMax => _enterCurrencyCountMax;

            /// <summary>
            /// 던전 이미지
            /// </summary>
            public string DungeonImagePath => _dungeonImagePath;

            /// <summary>
            /// 던전 BGM
            /// </summary>
            public int DungeonBGMID => _dungeonBGMID;

            /// <summary>
            /// 설명
            /// </summary>
            public string Description => _description;
            #endregion

            #region Storage
            internal int _id;
            internal string _name = "";
            internal string _dungeonName = "";
            internal global::Rescue.Tables.DungeonType _dungeonType;
            internal int _priority;
            internal global::Rescue.Tables.CycleType _cycleType;
            internal int _enterCondition;
            internal global::Rescue.Tables.CurrencyType _enterCurrencyType;
            internal int _dungeonBuffID;
            internal float _buffValueRate;
            internal int _enterCurrencyValue;
            internal global::Rescue.Tables.CurrencyType _freeEnterCurrencyType;
            internal int _aDEnterCount;
            internal int _enterCurrencyCountMax;
            internal string _dungeonImagePath = "";
            internal int _dungeonBGMID;
            internal string _description = "";
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"Id\":"); ToStringHelper.ToString(Id, sb);
                sb.Append(",\"Name\":"); ToStringHelper.ToString(Name, sb);
                sb.Append(",\"DungeonName\":"); ToStringHelper.ToString(DungeonName, sb);
                sb.Append(",\"DungeonType\":"); ToStringHelper.ToString(DungeonType, sb);
                sb.Append(",\"Priority\":"); ToStringHelper.ToString(Priority, sb);
                sb.Append(",\"CycleType\":"); ToStringHelper.ToString(CycleType, sb);
                sb.Append(",\"EnterCondition\":"); ToStringHelper.ToString(EnterCondition, sb);
                sb.Append(",\"EnterCurrencyType\":"); ToStringHelper.ToString(EnterCurrencyType, sb);
                sb.Append(",\"DungeonBuffID\":"); ToStringHelper.ToString(DungeonBuffID, sb);
                sb.Append(",\"BuffValueRate\":"); ToStringHelper.ToString(BuffValueRate, sb);
                sb.Append(",\"EnterCurrencyValue\":"); ToStringHelper.ToString(EnterCurrencyValue, sb);
                sb.Append(",\"FreeEnterCurrencyType\":"); ToStringHelper.ToString(FreeEnterCurrencyType, sb);
                sb.Append(",\"ADEnterCount\":"); ToStringHelper.ToString(ADEnterCount, sb);
                sb.Append(",\"EnterCurrencyCountMax\":"); ToStringHelper.ToString(EnterCurrencyCountMax, sb);
                sb.Append(",\"DungeonImagePath\":"); ToStringHelper.ToString(DungeonImagePath, sb);
                sb.Append(",\"DungeonBGMID\":"); ToStringHelper.ToString(DungeonBGMID, sb);
                sb.Append(",\"Description\":"); ToStringHelper.ToString(Description, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "Id", "Name", "DungeonName", "DungeonType", "Priority", "CycleType", "EnterCondition", "EnterCurrencyType", "DungeonBuffID", "BuffValueRate", "EnterCurrencyValue", "FreeEnterCurrencyType", "ADEnterCount", "EnterCurrencyCountMax", "DungeonImagePath", "DungeonBGMID", "Description" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.Id, r.Name, r.DungeonName, r.DungeonType, r.Priority, r.CycleType, r.EnterCondition, r.EnterCurrencyType, r.DungeonBuffID, r.BuffValueRate, r.EnterCurrencyValue, r.FreeEnterCurrencyType, r.ADEnterCount, r.EnterCurrencyCountMax, r.DungeonImagePath, r.DungeonBGMID, r.Description });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        #region Indexing by 'Id'
        public Dictionary<int, Record> RecordsById => _recordsById;
        private Dictionary<int, Record> _recordsById = new Dictionary<int, Record>();

        /// <summary>
        /// The row with this `Id`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language SheetMan generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindById(int key)
            => _recordsById.TryGetValue(key, out Record record) ? record : null;

        /// <summary>
        /// The row with this `Id`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetById(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public Record GetByIdOrThrow(int key)
        {
            if (!_recordsById.TryGetValue(key, out Record record))
                throw new SheetManException($"There is no record in table `DailyDungeonInfo` that corresponds to field `Id` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `Id`.</summary>
        public bool ContainsId(int key) => _recordsById.ContainsKey(key);
        #endregion // Indexing by `Id`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await Tables.ReadAllBytesAsync(filename);
            var reader = new LiteBinaryReader(bytes);
            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(LiteBinaryReader reader)
        {
            var columns = LiteBinaryTable.ReadHeader(reader, out int count);
            int tempEnumInt = 0;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        LiteBinaryTable.CheckColumn(column, "DailyDungeonInfo.Id", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementI32, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._id = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 2:
                        LiteBinaryTable.CheckColumn(column, "DailyDungeonInfo.Name", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._name);
                        }
                        break;

                    case 3:
                        LiteBinaryTable.CheckColumn(column, "DailyDungeonInfo.DungeonName", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._dungeonName);
                        }
                        break;

                    case 4:
                        LiteBinaryTable.CheckColumn(column, "DailyDungeonInfo.DungeonType", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._dungeonType = (global::Rescue.Tables.DungeonType)tempEnumInt;
                        }
                        break;

                    case 5:
                        LiteBinaryTable.CheckColumn(column, "DailyDungeonInfo.Priority", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementI32, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._priority = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 6:
                        LiteBinaryTable.CheckColumn(column, "DailyDungeonInfo.CycleType", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._cycleType = (global::Rescue.Tables.CycleType)tempEnumInt;
                        }
                        break;

                    case 7:
                        LiteBinaryTable.CheckColumn(column, "DailyDungeonInfo.EnterCondition", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementI32, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._enterCondition = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 8:
                        LiteBinaryTable.CheckColumn(column, "DailyDungeonInfo.EnterCurrencyType", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._enterCurrencyType = (global::Rescue.Tables.CurrencyType)tempEnumInt;
                        }
                        break;

                    case 9:
                        LiteBinaryTable.CheckColumn(column, "DailyDungeonInfo.DungeonBuffID", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementI32, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._dungeonBuffID = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 10:
                        LiteBinaryTable.CheckColumn(column, "DailyDungeonInfo.BuffValueRate", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._buffValueRate);
                        }
                        break;

                    case 11:
                        LiteBinaryTable.CheckColumn(column, "DailyDungeonInfo.EnterCurrencyValue", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementI32, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._enterCurrencyValue = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 12:
                        LiteBinaryTable.CheckColumn(column, "DailyDungeonInfo.FreeEnterCurrencyType", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._freeEnterCurrencyType = (global::Rescue.Tables.CurrencyType)tempEnumInt;
                        }
                        break;

                    case 13:
                        LiteBinaryTable.CheckColumn(column, "DailyDungeonInfo.ADEnterCount", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementI32, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._aDEnterCount = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 14:
                        LiteBinaryTable.CheckColumn(column, "DailyDungeonInfo.EnterCurrencyCountMax", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementI32, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._enterCurrencyCountMax = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 15:
                        LiteBinaryTable.CheckColumn(column, "DailyDungeonInfo.DungeonImagePath", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._dungeonImagePath);
                        }
                        break;

                    case 16:
                        LiteBinaryTable.CheckColumn(column, "DailyDungeonInfo.DungeonBGMID", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementI32, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._dungeonBGMID = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 17:
                        LiteBinaryTable.CheckColumn(column, "DailyDungeonInfo.Description", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._description);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                LiteBinaryTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsById = new Dictionary<int, Record>(count);
            foreach (var record in records)
                recordsById.Add(record.Id, record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsById = recordsById;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }

} // namespace Rescue.Tables
