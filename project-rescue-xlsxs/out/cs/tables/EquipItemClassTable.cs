// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections;
using System.Collections.Generic;
using System.IO;
using System.Threading.Tasks;

// SheetMan's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using SheetMan.Binary;

namespace Rescue.Tables
{
    [System.Serializable]
    public partial class EquipItemClassTable
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// EquipItemClass_Table
            /// </summary>
            public int Id => _id;

            /// <summary>
            /// 영문명
            /// </summary>
            public string Name => _name;

            /// <summary>
            /// 한글명
            /// </summary>
            public string NameKR => _nameKR;

            /// <summary>
            /// 타입
            /// </summary>
            public global::Rescue.Tables.JobType Type => _type;

            /// <summary>
            /// 등급
            /// </summary>
            public global::Rescue.Tables.GradeType Grade => _grade;

            /// <summary>
            /// 성급 단계
            /// </summary>
            public int TranscendStep => _transcendStep;

            /// <summary>
            /// 장비 성급 재료
            /// </summary>
            public global::Rescue.Tables.CurrencyType MaterialType => _materialType;

            /// <summary>
            /// 장비 성급 재료 개수
            /// </summary>
            public int MaterialValue => _materialValue;

            /// <summary>
            /// 무기 스탯1 증가값
            /// </summary>
            public float WeaponStatRate1 => _weaponStatRate1;

            /// <summary>
            /// 무기 스탯2 증가값
            /// </summary>
            public float WeaponStatRate2 => _weaponStatRate2;

            /// <summary>
            /// 갑옷 스탯1 증가값
            /// </summary>
            public float ArmorStatRate1 => _armorStatRate1;

            /// <summary>
            /// 갑옷 스탯2 증가값
            /// </summary>
            public float ArmorStatRate2 => _armorStatRate2;

            /// <summary>
            /// 장갑 스탯1 증가값
            /// </summary>
            public float GauntletStatRate1 => _gauntletStatRate1;

            /// <summary>
            /// 장갑 스탯2 증가값
            /// </summary>
            public float GauntletStatRate2 => _gauntletStatRate2;

            /// <summary>
            /// 신발 스탯1 증가값
            /// </summary>
            public float BootsStatRate1 => _bootsStatRate1;

            /// <summary>
            /// 신발 스탯2 증가값
            /// </summary>
            public float BootsStatRate2 => _bootsStatRate2;

            /// <summary>
            /// 투구 스탯1 증가값
            /// </summary>
            public float HelmetStatRate1 => _helmetStatRate1;

            /// <summary>
            /// 투구 스탯2 증가값
            /// </summary>
            public float HelmetStatRate2 => _helmetStatRate2;

            /// <summary>
            /// 악세서리 스탯1 증가값
            /// </summary>
            public float AccStatRate1 => _accStatRate1;

            /// <summary>
            /// 악세서리 스탯2 증가값
            /// </summary>
            public float AccStatRate2 => _accStatRate2;
            #endregion

            #region Storage
            internal int _id;
            internal string _name = "";
            internal string _nameKR = "";
            internal global::Rescue.Tables.JobType _type;
            internal global::Rescue.Tables.GradeType _grade;
            internal int _transcendStep;
            internal global::Rescue.Tables.CurrencyType _materialType;
            internal int _materialValue;
            internal float _weaponStatRate1;
            internal float _weaponStatRate2;
            internal float _armorStatRate1;
            internal float _armorStatRate2;
            internal float _gauntletStatRate1;
            internal float _gauntletStatRate2;
            internal float _bootsStatRate1;
            internal float _bootsStatRate2;
            internal float _helmetStatRate1;
            internal float _helmetStatRate2;
            internal float _accStatRate1;
            internal float _accStatRate2;
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"Id\":"); ToStringHelper.ToString(Id, sb);
                sb.Append(",\"Name\":"); ToStringHelper.ToString(Name, sb);
                sb.Append(",\"NameKR\":"); ToStringHelper.ToString(NameKR, sb);
                sb.Append(",\"Type\":"); ToStringHelper.ToString(Type, sb);
                sb.Append(",\"Grade\":"); ToStringHelper.ToString(Grade, sb);
                sb.Append(",\"TranscendStep\":"); ToStringHelper.ToString(TranscendStep, sb);
                sb.Append(",\"MaterialType\":"); ToStringHelper.ToString(MaterialType, sb);
                sb.Append(",\"MaterialValue\":"); ToStringHelper.ToString(MaterialValue, sb);
                sb.Append(",\"WeaponStatRate1\":"); ToStringHelper.ToString(WeaponStatRate1, sb);
                sb.Append(",\"WeaponStatRate2\":"); ToStringHelper.ToString(WeaponStatRate2, sb);
                sb.Append(",\"ArmorStatRate1\":"); ToStringHelper.ToString(ArmorStatRate1, sb);
                sb.Append(",\"ArmorStatRate2\":"); ToStringHelper.ToString(ArmorStatRate2, sb);
                sb.Append(",\"GauntletStatRate1\":"); ToStringHelper.ToString(GauntletStatRate1, sb);
                sb.Append(",\"GauntletStatRate2\":"); ToStringHelper.ToString(GauntletStatRate2, sb);
                sb.Append(",\"BootsStatRate1\":"); ToStringHelper.ToString(BootsStatRate1, sb);
                sb.Append(",\"BootsStatRate2\":"); ToStringHelper.ToString(BootsStatRate2, sb);
                sb.Append(",\"HelmetStatRate1\":"); ToStringHelper.ToString(HelmetStatRate1, sb);
                sb.Append(",\"HelmetStatRate2\":"); ToStringHelper.ToString(HelmetStatRate2, sb);
                sb.Append(",\"AccStatRate1\":"); ToStringHelper.ToString(AccStatRate1, sb);
                sb.Append(",\"AccStatRate2\":"); ToStringHelper.ToString(AccStatRate2, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "Id", "Name", "NameKR", "Type", "Grade", "TranscendStep", "MaterialType", "MaterialValue", "WeaponStatRate1", "WeaponStatRate2", "ArmorStatRate1", "ArmorStatRate2", "GauntletStatRate1", "GauntletStatRate2", "BootsStatRate1", "BootsStatRate2", "HelmetStatRate1", "HelmetStatRate2", "AccStatRate1", "AccStatRate2" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.Id, r.Name, r.NameKR, r.Type, r.Grade, r.TranscendStep, r.MaterialType, r.MaterialValue, r.WeaponStatRate1, r.WeaponStatRate2, r.ArmorStatRate1, r.ArmorStatRate2, r.GauntletStatRate1, r.GauntletStatRate2, r.BootsStatRate1, r.BootsStatRate2, r.HelmetStatRate1, r.HelmetStatRate2, r.AccStatRate1, r.AccStatRate2 });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        #region Indexing by 'Id'
        public Dictionary<int, Record> RecordsById => _recordsById;
        private Dictionary<int, Record> _recordsById = new Dictionary<int, Record>();

        /// <summary>
        /// The row with this `Id`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language SheetMan generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindById(int key)
            => _recordsById.TryGetValue(key, out Record record) ? record : null;

        /// <summary>
        /// The row with this `Id`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetById(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public Record GetByIdOrThrow(int key)
        {
            if (!_recordsById.TryGetValue(key, out Record record))
                throw new SheetManException($"There is no record in table `EquipItemClass` that corresponds to field `Id` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `Id`.</summary>
        public bool ContainsId(int key) => _recordsById.ContainsKey(key);
        #endregion // Indexing by `Id`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await Tables.ReadAllBytesAsync(filename);
            var reader = new LiteBinaryReader(bytes);
            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(LiteBinaryReader reader)
        {
            var columns = LiteBinaryTable.ReadHeader(reader, out int count);
            int tempEnumInt = 0;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        LiteBinaryTable.CheckColumn(column, "EquipItemClass.Id", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementI32, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._id = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 2:
                        LiteBinaryTable.CheckColumn(column, "EquipItemClass.Name", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._name);
                        }
                        break;

                    case 3:
                        LiteBinaryTable.CheckColumn(column, "EquipItemClass.NameKR", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._nameKR);
                        }
                        break;

                    case 4:
                        LiteBinaryTable.CheckColumn(column, "EquipItemClass.Type", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._type = (global::Rescue.Tables.JobType)tempEnumInt;
                        }
                        break;

                    case 5:
                        LiteBinaryTable.CheckColumn(column, "EquipItemClass.Grade", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._grade = (global::Rescue.Tables.GradeType)tempEnumInt;
                        }
                        break;

                    case 6:
                        LiteBinaryTable.CheckColumn(column, "EquipItemClass.TranscendStep", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementI32, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._transcendStep = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 7:
                        LiteBinaryTable.CheckColumn(column, "EquipItemClass.MaterialType", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._materialType = (global::Rescue.Tables.CurrencyType)tempEnumInt;
                        }
                        break;

                    case 8:
                        LiteBinaryTable.CheckColumn(column, "EquipItemClass.MaterialValue", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementI32, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._materialValue = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 9:
                        LiteBinaryTable.CheckColumn(column, "EquipItemClass.WeaponStatRate1", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._weaponStatRate1);
                        }
                        break;

                    case 10:
                        LiteBinaryTable.CheckColumn(column, "EquipItemClass.WeaponStatRate2", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._weaponStatRate2);
                        }
                        break;

                    case 11:
                        LiteBinaryTable.CheckColumn(column, "EquipItemClass.ArmorStatRate1", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._armorStatRate1);
                        }
                        break;

                    case 12:
                        LiteBinaryTable.CheckColumn(column, "EquipItemClass.ArmorStatRate2", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._armorStatRate2);
                        }
                        break;

                    case 13:
                        LiteBinaryTable.CheckColumn(column, "EquipItemClass.GauntletStatRate1", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._gauntletStatRate1);
                        }
                        break;

                    case 14:
                        LiteBinaryTable.CheckColumn(column, "EquipItemClass.GauntletStatRate2", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._gauntletStatRate2);
                        }
                        break;

                    case 15:
                        LiteBinaryTable.CheckColumn(column, "EquipItemClass.BootsStatRate1", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._bootsStatRate1);
                        }
                        break;

                    case 16:
                        LiteBinaryTable.CheckColumn(column, "EquipItemClass.BootsStatRate2", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._bootsStatRate2);
                        }
                        break;

                    case 17:
                        LiteBinaryTable.CheckColumn(column, "EquipItemClass.HelmetStatRate1", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._helmetStatRate1);
                        }
                        break;

                    case 18:
                        LiteBinaryTable.CheckColumn(column, "EquipItemClass.HelmetStatRate2", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._helmetStatRate2);
                        }
                        break;

                    case 19:
                        LiteBinaryTable.CheckColumn(column, "EquipItemClass.AccStatRate1", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._accStatRate1);
                        }
                        break;

                    case 20:
                        LiteBinaryTable.CheckColumn(column, "EquipItemClass.AccStatRate2", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._accStatRate2);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                LiteBinaryTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsById = new Dictionary<int, Record>(count);
            foreach (var record in records)
                recordsById.Add(record.Id, record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsById = recordsById;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }

} // namespace Rescue.Tables
