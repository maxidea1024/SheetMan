// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections;
using System.Collections.Generic;
using System.IO;
using System.Threading.Tasks;

// SheetMan's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using SheetMan.Binary;

namespace Rescue.Tables
{
    [System.Serializable]
    public partial class EquipTable
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// Equipment_Table
            /// </summary>
            public int Id => _id;

            /// <summary>
            /// 한글명
            /// </summary>
            public string Name => _name;

            /// <summary>
            /// 영문명
            /// </summary>
            public string EquipName => _equipName;

            /// <summary>
            /// 타입
            /// </summary>
            public global::Rescue.Tables.JobType Type => _type;

            /// <summary>
            /// 장비 종류
            /// </summary>
            public global::Rescue.Tables.EquipmentType EquipType => _equipType;

            /// <summary>
            /// 등급
            /// </summary>
            public global::Rescue.Tables.GradeType Grade => _grade;

            /// <summary>
            /// 최대 성급
            /// </summary>
            public int MaxClass => _maxClass;

            /// <summary>
            /// 최대 레벨
            /// </summary>
            public int MaxLevel => _maxLevel;

            /// <summary>
            /// 스탯1
            /// </summary>
            public global::Rescue.Tables.StatType StatType1 => _statType1;

            /// <summary>
            /// 스탯2
            /// </summary>
            public global::Rescue.Tables.StatType StatType2 => _statType2;

            /// <summary>
            /// 아이콘 경로
            /// </summary>
            public string IconPath => _iconPath;

            /// <summary>
            /// 프리펩 위치
            /// </summary>
            public string PrefabPath => _prefabPath;

            /// <summary>
            /// 설명
            /// </summary>
            public string Description => _description;
            #endregion

            #region Storage
            internal int _id;
            internal string _name = "";
            internal string _equipName = "";
            internal global::Rescue.Tables.JobType _type;
            internal global::Rescue.Tables.EquipmentType _equipType;
            internal global::Rescue.Tables.GradeType _grade;
            internal int _maxClass;
            internal int _maxLevel;
            internal global::Rescue.Tables.StatType _statType1;
            internal global::Rescue.Tables.StatType _statType2;
            internal string _iconPath = "";
            internal string _prefabPath = "";
            internal string _description = "";
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"Id\":"); ToStringHelper.ToString(Id, sb);
                sb.Append(",\"Name\":"); ToStringHelper.ToString(Name, sb);
                sb.Append(",\"EquipName\":"); ToStringHelper.ToString(EquipName, sb);
                sb.Append(",\"Type\":"); ToStringHelper.ToString(Type, sb);
                sb.Append(",\"EquipType\":"); ToStringHelper.ToString(EquipType, sb);
                sb.Append(",\"Grade\":"); ToStringHelper.ToString(Grade, sb);
                sb.Append(",\"MaxClass\":"); ToStringHelper.ToString(MaxClass, sb);
                sb.Append(",\"MaxLevel\":"); ToStringHelper.ToString(MaxLevel, sb);
                sb.Append(",\"StatType1\":"); ToStringHelper.ToString(StatType1, sb);
                sb.Append(",\"StatType2\":"); ToStringHelper.ToString(StatType2, sb);
                sb.Append(",\"IconPath\":"); ToStringHelper.ToString(IconPath, sb);
                sb.Append(",\"PrefabPath\":"); ToStringHelper.ToString(PrefabPath, sb);
                sb.Append(",\"Description\":"); ToStringHelper.ToString(Description, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "Id", "Name", "EquipName", "Type", "EquipType", "Grade", "MaxClass", "MaxLevel", "StatType1", "StatType2", "IconPath", "PrefabPath", "Description" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.Id, r.Name, r.EquipName, r.Type, r.EquipType, r.Grade, r.MaxClass, r.MaxLevel, r.StatType1, r.StatType2, r.IconPath, r.PrefabPath, r.Description });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        #region Indexing by 'Id'
        public Dictionary<int, Record> RecordsById => _recordsById;
        private Dictionary<int, Record> _recordsById = new Dictionary<int, Record>();

        /// <summary>
        /// The row with this `Id`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language SheetMan generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindById(int key)
            => _recordsById.TryGetValue(key, out Record record) ? record : null;

        /// <summary>
        /// The row with this `Id`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetById(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public Record GetByIdOrThrow(int key)
        {
            if (!_recordsById.TryGetValue(key, out Record record))
                throw new SheetManException($"There is no record in table `Equip` that corresponds to field `Id` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `Id`.</summary>
        public bool ContainsId(int key) => _recordsById.ContainsKey(key);
        #endregion // Indexing by `Id`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await Tables.ReadAllBytesAsync(filename);
            var reader = new LiteBinaryReader(bytes);
            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(LiteBinaryReader reader)
        {
            var columns = LiteBinaryTable.ReadHeader(reader, out int count);
            int tempEnumInt = 0;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        LiteBinaryTable.CheckColumn(column, "Equip.Id", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementI32, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._id = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 2:
                        LiteBinaryTable.CheckColumn(column, "Equip.Name", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._name);
                        }
                        break;

                    case 3:
                        LiteBinaryTable.CheckColumn(column, "Equip.EquipName", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._equipName);
                        }
                        break;

                    case 4:
                        LiteBinaryTable.CheckColumn(column, "Equip.Type", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._type = (global::Rescue.Tables.JobType)tempEnumInt;
                        }
                        break;

                    case 5:
                        LiteBinaryTable.CheckColumn(column, "Equip.EquipType", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._equipType = (global::Rescue.Tables.EquipmentType)tempEnumInt;
                        }
                        break;

                    case 6:
                        LiteBinaryTable.CheckColumn(column, "Equip.Grade", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._grade = (global::Rescue.Tables.GradeType)tempEnumInt;
                        }
                        break;

                    case 7:
                        LiteBinaryTable.CheckColumn(column, "Equip.MaxClass", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementI32, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._maxClass = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 8:
                        LiteBinaryTable.CheckColumn(column, "Equip.MaxLevel", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementI32, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._maxLevel = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 9:
                        LiteBinaryTable.CheckColumn(column, "Equip.StatType1", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._statType1 = (global::Rescue.Tables.StatType)tempEnumInt;
                        }
                        break;

                    case 10:
                        LiteBinaryTable.CheckColumn(column, "Equip.StatType2", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._statType2 = (global::Rescue.Tables.StatType)tempEnumInt;
                        }
                        break;

                    case 11:
                        LiteBinaryTable.CheckColumn(column, "Equip.IconPath", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._iconPath);
                        }
                        break;

                    case 12:
                        LiteBinaryTable.CheckColumn(column, "Equip.PrefabPath", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._prefabPath);
                        }
                        break;

                    case 13:
                        LiteBinaryTable.CheckColumn(column, "Equip.Description", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._description);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                LiteBinaryTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsById = new Dictionary<int, Record>(count);
            foreach (var record in records)
                recordsById.Add(record.Id, record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsById = recordsById;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }

} // namespace Rescue.Tables
