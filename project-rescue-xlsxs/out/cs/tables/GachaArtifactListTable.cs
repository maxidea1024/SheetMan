// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections;
using System.Collections.Generic;
using System.IO;
using System.Threading.Tasks;

// SheetMan's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using SheetMan.Binary;

namespace Rescue.Tables
{
    [System.Serializable]
    public partial class GachaArtifactListTable
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// GachaArtifact_List
            /// </summary>
            public int Id => _id;

            /// <summary>
            /// 아티펙트 ID
            /// </summary>
            public int ArtifactID => _artifactID;

            /// <summary>
            /// 태생 등급
            /// </summary>
            public global::Rescue.Tables.GradeType GradeType => _gradeType;

            /// <summary>
            /// 기본 가중치
            /// </summary>
            public int BaseWeight => _baseWeight;

            /// <summary>
            /// 성급 재화 ID
            /// </summary>
            public int ClassUpCurrencyID => _classUpCurrencyID;

            /// <summary>
            /// 성급 재화값
            /// </summary>
            public int ClassUpCurrencyValue => _classUpCurrencyValue;

            /// <summary>
            /// 변환 조건 ID
            /// </summary>
            public int ExConditionID => _exConditionID;

            /// <summary>
            /// 변환 재화ID
            /// </summary>
            public int ExCurrencyID => _exCurrencyID;

            /// <summary>
            /// 변환 재화값
            /// </summary>
            public int ExCurrencyValue => _exCurrencyValue;
            #endregion

            #region Storage
            internal int _id;
            internal int _artifactID;
            internal global::Rescue.Tables.GradeType _gradeType;
            internal int _baseWeight;
            internal int _classUpCurrencyID;
            internal int _classUpCurrencyValue;
            internal int _exConditionID;
            internal int _exCurrencyID;
            internal int _exCurrencyValue;
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"Id\":"); ToStringHelper.ToString(Id, sb);
                sb.Append(",\"ArtifactID\":"); ToStringHelper.ToString(ArtifactID, sb);
                sb.Append(",\"GradeType\":"); ToStringHelper.ToString(GradeType, sb);
                sb.Append(",\"BaseWeight\":"); ToStringHelper.ToString(BaseWeight, sb);
                sb.Append(",\"ClassUpCurrencyID\":"); ToStringHelper.ToString(ClassUpCurrencyID, sb);
                sb.Append(",\"ClassUpCurrencyValue\":"); ToStringHelper.ToString(ClassUpCurrencyValue, sb);
                sb.Append(",\"ExConditionID\":"); ToStringHelper.ToString(ExConditionID, sb);
                sb.Append(",\"ExCurrencyID\":"); ToStringHelper.ToString(ExCurrencyID, sb);
                sb.Append(",\"ExCurrencyValue\":"); ToStringHelper.ToString(ExCurrencyValue, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "Id", "ArtifactID", "GradeType", "BaseWeight", "ClassUpCurrencyID", "ClassUpCurrencyValue", "ExConditionID", "ExCurrencyID", "ExCurrencyValue" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.Id, r.ArtifactID, r.GradeType, r.BaseWeight, r.ClassUpCurrencyID, r.ClassUpCurrencyValue, r.ExConditionID, r.ExCurrencyID, r.ExCurrencyValue });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        #region Indexing by 'Id'
        public Dictionary<int, Record> RecordsById => _recordsById;
        private Dictionary<int, Record> _recordsById = new Dictionary<int, Record>();

        /// <summary>
        /// The row with this `Id`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language SheetMan generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindById(int key)
            => _recordsById.TryGetValue(key, out Record record) ? record : null;

        /// <summary>
        /// The row with this `Id`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetById(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public Record GetByIdOrThrow(int key)
        {
            if (!_recordsById.TryGetValue(key, out Record record))
                throw new SheetManException($"There is no record in table `GachaArtifactList` that corresponds to field `Id` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `Id`.</summary>
        public bool ContainsId(int key) => _recordsById.ContainsKey(key);
        #endregion // Indexing by `Id`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await Tables.ReadAllBytesAsync(filename);
            var reader = new ScbReader(bytes);
            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(ScbReader reader)
        {
            var columns = ScbTable.ReadHeader(reader, out int count);
            int tempEnumInt = 0;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        ScbTable.CheckColumn(column, "GachaArtifactList.Id", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._id = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 2:
                        ScbTable.CheckColumn(column, "GachaArtifactList.ArtifactID", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._artifactID = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 3:
                        ScbTable.CheckColumn(column, "GachaArtifactList.GradeType", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._gradeType = (global::Rescue.Tables.GradeType)tempEnumInt;
                        }
                        break;

                    case 4:
                        ScbTable.CheckColumn(column, "GachaArtifactList.BaseWeight", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._baseWeight = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 5:
                        ScbTable.CheckColumn(column, "GachaArtifactList.ClassUpCurrencyID", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._classUpCurrencyID = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 6:
                        ScbTable.CheckColumn(column, "GachaArtifactList.ClassUpCurrencyValue", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._classUpCurrencyValue = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 7:
                        ScbTable.CheckColumn(column, "GachaArtifactList.ExConditionID", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._exConditionID = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 8:
                        ScbTable.CheckColumn(column, "GachaArtifactList.ExCurrencyID", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._exCurrencyID = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 9:
                        ScbTable.CheckColumn(column, "GachaArtifactList.ExCurrencyValue", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._exCurrencyValue = reader.ReadI32As(column.Element);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                ScbTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsById = new Dictionary<int, Record>(count);
            foreach (var record in records)
                recordsById.Add(record.Id, record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsById = recordsById;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }

} // namespace Rescue.Tables
