// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections;
using System.Collections.Generic;
using System.IO;
using System.Threading.Tasks;

// SheetMan's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using SheetMan.Binary;

namespace Rescue.Tables
{
    [System.Serializable]
    public partial class OopartsDungeonRewardTable
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// OopartsDungeonReward_Table
            /// </summary>
            public int Id => _id;

            /// <summary>
            /// 영문
            /// </summary>
            public string Name => _name;

            /// <summary>
            /// 한글
            /// </summary>
            public string RewardName => _rewardName;

            /// <summary>
            /// 보상 설정1
            /// </summary>
            public global::Rescue.Tables.CurrencyType RewardType1 => _rewardType1;

            /// <summary>
            /// 보상 값 1
            /// </summary>
            public int RewardValue1 => _rewardValue1;

            /// <summary>
            /// 보상 설정2
            /// </summary>
            public global::Rescue.Tables.CurrencyType RewardType2 => _rewardType2;

            /// <summary>
            /// 보상 값 2
            /// </summary>
            public int RewardValue2 => _rewardValue2;

            /// <summary>
            /// 최초 클리어
            /// </summary>
            public bool IsFirstClear => _isFirstClear;

            /// <summary>
            /// 최초 보상 설정
            /// </summary>
            public global::Rescue.Tables.CurrencyType FirstClearRewardType => _firstClearRewardType;

            /// <summary>
            /// 최초 보상 값
            /// </summary>
            public int FirstClearRewardValue => _firstClearRewardValue;
            #endregion

            #region Storage
            internal int _id;
            internal string _name = "";
            internal string _rewardName = "";
            internal global::Rescue.Tables.CurrencyType _rewardType1;
            internal int _rewardValue1;
            internal global::Rescue.Tables.CurrencyType _rewardType2;
            internal int _rewardValue2;
            internal bool _isFirstClear;
            internal global::Rescue.Tables.CurrencyType _firstClearRewardType;
            internal int _firstClearRewardValue;
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"Id\":"); ToStringHelper.ToString(Id, sb);
                sb.Append(",\"Name\":"); ToStringHelper.ToString(Name, sb);
                sb.Append(",\"RewardName\":"); ToStringHelper.ToString(RewardName, sb);
                sb.Append(",\"RewardType1\":"); ToStringHelper.ToString(RewardType1, sb);
                sb.Append(",\"RewardValue1\":"); ToStringHelper.ToString(RewardValue1, sb);
                sb.Append(",\"RewardType2\":"); ToStringHelper.ToString(RewardType2, sb);
                sb.Append(",\"RewardValue2\":"); ToStringHelper.ToString(RewardValue2, sb);
                sb.Append(",\"IsFirstClear\":"); ToStringHelper.ToString(IsFirstClear, sb);
                sb.Append(",\"FirstClearRewardType\":"); ToStringHelper.ToString(FirstClearRewardType, sb);
                sb.Append(",\"FirstClearRewardValue\":"); ToStringHelper.ToString(FirstClearRewardValue, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "Id", "Name", "RewardName", "RewardType1", "RewardValue1", "RewardType2", "RewardValue2", "IsFirstClear", "FirstClearRewardType", "FirstClearRewardValue" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.Id, r.Name, r.RewardName, r.RewardType1, r.RewardValue1, r.RewardType2, r.RewardValue2, r.IsFirstClear, r.FirstClearRewardType, r.FirstClearRewardValue });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        #region Indexing by 'Id'
        public Dictionary<int, Record> RecordsById => _recordsById;
        private Dictionary<int, Record> _recordsById = new Dictionary<int, Record>();

        /// <summary>
        /// The row with this `Id`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language SheetMan generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindById(int key)
            => _recordsById.TryGetValue(key, out Record record) ? record : null;

        /// <summary>
        /// The row with this `Id`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetById(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public Record GetByIdOrThrow(int key)
        {
            if (!_recordsById.TryGetValue(key, out Record record))
                throw new SheetManException($"There is no record in table `OopartsDungeonReward` that corresponds to field `Id` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `Id`.</summary>
        public bool ContainsId(int key) => _recordsById.ContainsKey(key);
        #endregion // Indexing by `Id`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await Tables.ReadAllBytesAsync(filename);
            var reader = new ScbReader(bytes);
            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(ScbReader reader)
        {
            var columns = ScbTable.ReadHeader(reader, out int count);
            int tempEnumInt = 0;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        ScbTable.CheckColumn(column, "OopartsDungeonReward.Id", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._id = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 2:
                        ScbTable.CheckColumn(column, "OopartsDungeonReward.Name", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._name);
                        }
                        break;

                    case 3:
                        ScbTable.CheckColumn(column, "OopartsDungeonReward.RewardName", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._rewardName);
                        }
                        break;

                    case 4:
                        ScbTable.CheckColumn(column, "OopartsDungeonReward.RewardType1", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._rewardType1 = (global::Rescue.Tables.CurrencyType)tempEnumInt;
                        }
                        break;

                    case 5:
                        ScbTable.CheckColumn(column, "OopartsDungeonReward.RewardValue1", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._rewardValue1 = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 6:
                        ScbTable.CheckColumn(column, "OopartsDungeonReward.RewardType2", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._rewardType2 = (global::Rescue.Tables.CurrencyType)tempEnumInt;
                        }
                        break;

                    case 7:
                        ScbTable.CheckColumn(column, "OopartsDungeonReward.RewardValue2", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._rewardValue2 = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 8:
                        ScbTable.CheckColumn(column, "OopartsDungeonReward.IsFirstClear", ScbTable.KindScalar, 1, ScbTable.ElementBool);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._isFirstClear);
                        }
                        break;

                    case 9:
                        ScbTable.CheckColumn(column, "OopartsDungeonReward.FirstClearRewardType", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._firstClearRewardType = (global::Rescue.Tables.CurrencyType)tempEnumInt;
                        }
                        break;

                    case 10:
                        ScbTable.CheckColumn(column, "OopartsDungeonReward.FirstClearRewardValue", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._firstClearRewardValue = reader.ReadI32As(column.Element);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                ScbTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsById = new Dictionary<int, Record>(count);
            foreach (var record in records)
                recordsById.Add(record.Id, record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsById = recordsById;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }

} // namespace Rescue.Tables
