// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections;
using System.Collections.Generic;
using System.IO;
using System.Threading.Tasks;

// SheetMan's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using SheetMan.Binary;

namespace Rescue.Tables
{
    [System.Serializable]
    public partial class OopartsDungeonStageTable
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// OopartsDungeonStage_Table
            /// </summary>
            public int Id => _id;

            /// <summary>
            /// 영문
            /// </summary>
            public string Name => _name;

            /// <summary>
            /// 한글
            /// </summary>
            public string StageName => _stageName;

            /// <summary>
            /// 던전 아이디
            /// </summary>
            public global::Rescue.Tables.DungeonType DungeonType => _dungeonType;

            /// <summary>
            /// 던전 단계
            /// </summary>
            public int DungeonFloor => _dungeonFloor;

            /// <summary>
            /// 제한 시간
            /// </summary>
            public int TimeLimit => _timeLimit;

            /// <summary>
            /// 생성 몬스터 ID
            /// </summary>
            public int[] SpawnIds => _spawnIds;

            /// <summary>
            /// 보스 ID
            /// </summary>
            public int[] BossID => _bossID;

            /// <summary>
            /// 위치 생성 가능 횟수
            /// </summary>
            public int SpawnPointCount => _spawnPointCount;

            /// <summary>
            /// 최대 생성 수
            /// </summary>
            public int SpawnMaxCount => _spawnMaxCount;

            /// <summary>
            /// 다음 포인터 생성 시간
            /// </summary>
            public float SpawnNextTime => _spawnNextTime;

            /// <summary>
            /// 스테이지 클리어 조건
            /// </summary>
            public int StageClearCount => _stageClearCount;

            /// <summary>
            /// 권장 전투력
            /// </summary>
            public string RecommendPower => _recommendPower;

            /// <summary>
            /// 보상 설정
            /// </summary>
            public int RewardID => _rewardID;

            /// <summary>
            /// 던전 공격력 증가 값
            /// </summary>
            public float AtkUpPercent => _atkUpPercent;

            /// <summary>
            /// 던전 방어력 증가 값
            /// </summary>
            public float DefUpPercent => _defUpPercent;

            /// <summary>
            /// 던전 체력 증가 값
            /// </summary>
            public float MHPUpPercent => _mHPUpPercent;

            /// <summary>
            /// 던전 이미지
            /// </summary>
            public string DungeonImagePath => _dungeonImagePath;

            /// <summary>
            /// 몬스터 이미지
            /// </summary>
            public string MonsterImagePath => _monsterImagePath;

            /// <summary>
            /// AssetData 경로(Stage처럼 에셋만 추가하면 바뀌게)
            /// </summary>
            public string AssetDataPath => _assetDataPath;
            #endregion

            #region Storage
            internal int _id;
            internal string _name = "";
            internal string _stageName = "";
            internal global::Rescue.Tables.DungeonType _dungeonType;
            internal int _dungeonFloor;
            internal int _timeLimit;
            internal int[] _spawnIds = System.Array.Empty<int>();
            internal int[] _bossID = System.Array.Empty<int>();
            internal int _spawnPointCount;
            internal int _spawnMaxCount;
            internal float _spawnNextTime;
            internal int _stageClearCount;
            internal string _recommendPower = "";
            internal int _rewardID;
            internal float _atkUpPercent;
            internal float _defUpPercent;
            internal float _mHPUpPercent;
            internal string _dungeonImagePath = "";
            internal string _monsterImagePath = "";
            internal string _assetDataPath = "";
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"Id\":"); ToStringHelper.ToString(Id, sb);
                sb.Append(",\"Name\":"); ToStringHelper.ToString(Name, sb);
                sb.Append(",\"StageName\":"); ToStringHelper.ToString(StageName, sb);
                sb.Append(",\"DungeonType\":"); ToStringHelper.ToString(DungeonType, sb);
                sb.Append(",\"DungeonFloor\":"); ToStringHelper.ToString(DungeonFloor, sb);
                sb.Append(",\"TimeLimit\":"); ToStringHelper.ToString(TimeLimit, sb);
                sb.Append(",\"SpawnIds\":"); ToStringHelper.ToString(SpawnIds, sb);
                sb.Append(",\"BossID\":"); ToStringHelper.ToString(BossID, sb);
                sb.Append(",\"SpawnPointCount\":"); ToStringHelper.ToString(SpawnPointCount, sb);
                sb.Append(",\"SpawnMaxCount\":"); ToStringHelper.ToString(SpawnMaxCount, sb);
                sb.Append(",\"SpawnNextTime\":"); ToStringHelper.ToString(SpawnNextTime, sb);
                sb.Append(",\"StageClearCount\":"); ToStringHelper.ToString(StageClearCount, sb);
                sb.Append(",\"RecommendPower\":"); ToStringHelper.ToString(RecommendPower, sb);
                sb.Append(",\"RewardID\":"); ToStringHelper.ToString(RewardID, sb);
                sb.Append(",\"AtkUpPercent\":"); ToStringHelper.ToString(AtkUpPercent, sb);
                sb.Append(",\"DefUpPercent\":"); ToStringHelper.ToString(DefUpPercent, sb);
                sb.Append(",\"MHPUpPercent\":"); ToStringHelper.ToString(MHPUpPercent, sb);
                sb.Append(",\"DungeonImagePath\":"); ToStringHelper.ToString(DungeonImagePath, sb);
                sb.Append(",\"MonsterImagePath\":"); ToStringHelper.ToString(MonsterImagePath, sb);
                sb.Append(",\"AssetDataPath\":"); ToStringHelper.ToString(AssetDataPath, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "Id", "Name", "StageName", "DungeonType", "DungeonFloor", "TimeLimit", "SpawnIds", "BossID", "SpawnPointCount", "SpawnMaxCount", "SpawnNextTime", "StageClearCount", "RecommendPower", "RewardID", "AtkUpPercent", "DefUpPercent", "MHPUpPercent", "DungeonImagePath", "MonsterImagePath", "AssetDataPath" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.Id, r.Name, r.StageName, r.DungeonType, r.DungeonFloor, r.TimeLimit, r.SpawnIds, r.BossID, r.SpawnPointCount, r.SpawnMaxCount, r.SpawnNextTime, r.StageClearCount, r.RecommendPower, r.RewardID, r.AtkUpPercent, r.DefUpPercent, r.MHPUpPercent, r.DungeonImagePath, r.MonsterImagePath, r.AssetDataPath });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        #region Indexing by 'Id'
        public Dictionary<int, Record> RecordsById => _recordsById;
        private Dictionary<int, Record> _recordsById = new Dictionary<int, Record>();

        /// <summary>
        /// The row with this `Id`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language SheetMan generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindById(int key)
            => _recordsById.TryGetValue(key, out Record record) ? record : null;

        /// <summary>
        /// The row with this `Id`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetById(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public Record GetByIdOrThrow(int key)
        {
            if (!_recordsById.TryGetValue(key, out Record record))
                throw new SheetManException($"There is no record in table `OopartsDungeonStage` that corresponds to field `Id` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `Id`.</summary>
        public bool ContainsId(int key) => _recordsById.ContainsKey(key);
        #endregion // Indexing by `Id`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await Tables.ReadAllBytesAsync(filename);
            var reader = new ScbReader(bytes);
            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(ScbReader reader)
        {
            var columns = ScbTable.ReadHeader(reader, out int count);
            int tempEnumInt = 0;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        ScbTable.CheckColumn(column, "OopartsDungeonStage.Id", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._id = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 2:
                        ScbTable.CheckColumn(column, "OopartsDungeonStage.Name", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._name);
                        }
                        break;

                    case 3:
                        ScbTable.CheckColumn(column, "OopartsDungeonStage.StageName", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._stageName);
                        }
                        break;

                    case 4:
                        ScbTable.CheckColumn(column, "OopartsDungeonStage.DungeonType", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._dungeonType = (global::Rescue.Tables.DungeonType)tempEnumInt;
                        }
                        break;

                    case 5:
                        ScbTable.CheckColumn(column, "OopartsDungeonStage.DungeonFloor", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._dungeonFloor = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 6:
                        ScbTable.CheckColumn(column, "OopartsDungeonStage.TimeLimit", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._timeLimit = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 7:
                        ScbTable.CheckColumn(column, "OopartsDungeonStage.SpawnIds", ScbTable.KindVarArray, 0, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.TryReadCounter32(out int elementCount);
                            record._spawnIds = new int[elementCount];
                            for (int j = 0; j < elementCount; ++j)
                            {
                                record._spawnIds[j] = reader.ReadI32As(column.Element);
                            }
                        }
                        break;

                    case 8:
                        ScbTable.CheckColumn(column, "OopartsDungeonStage.BossID", ScbTable.KindVarArray, 0, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.TryReadCounter32(out int elementCount);
                            record._bossID = new int[elementCount];
                            for (int j = 0; j < elementCount; ++j)
                            {
                                record._bossID[j] = reader.ReadI32As(column.Element);
                            }
                        }
                        break;

                    case 9:
                        ScbTable.CheckColumn(column, "OopartsDungeonStage.SpawnPointCount", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._spawnPointCount = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 10:
                        ScbTable.CheckColumn(column, "OopartsDungeonStage.SpawnMaxCount", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._spawnMaxCount = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 11:
                        ScbTable.CheckColumn(column, "OopartsDungeonStage.SpawnNextTime", ScbTable.KindScalar, 1, ScbTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._spawnNextTime);
                        }
                        break;

                    case 12:
                        ScbTable.CheckColumn(column, "OopartsDungeonStage.StageClearCount", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._stageClearCount = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 13:
                        ScbTable.CheckColumn(column, "OopartsDungeonStage.RecommendPower", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._recommendPower);
                        }
                        break;

                    case 14:
                        ScbTable.CheckColumn(column, "OopartsDungeonStage.RewardID", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._rewardID = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 15:
                        ScbTable.CheckColumn(column, "OopartsDungeonStage.AtkUpPercent", ScbTable.KindScalar, 1, ScbTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._atkUpPercent);
                        }
                        break;

                    case 16:
                        ScbTable.CheckColumn(column, "OopartsDungeonStage.DefUpPercent", ScbTable.KindScalar, 1, ScbTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._defUpPercent);
                        }
                        break;

                    case 17:
                        ScbTable.CheckColumn(column, "OopartsDungeonStage.MHPUpPercent", ScbTable.KindScalar, 1, ScbTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._mHPUpPercent);
                        }
                        break;

                    case 18:
                        ScbTable.CheckColumn(column, "OopartsDungeonStage.DungeonImagePath", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._dungeonImagePath);
                        }
                        break;

                    case 19:
                        ScbTable.CheckColumn(column, "OopartsDungeonStage.MonsterImagePath", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._monsterImagePath);
                        }
                        break;

                    case 20:
                        ScbTable.CheckColumn(column, "OopartsDungeonStage.AssetDataPath", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._assetDataPath);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                ScbTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsById = new Dictionary<int, Record>(count);
            foreach (var record in records)
                recordsById.Add(record.Id, record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsById = recordsById;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }

} // namespace Rescue.Tables
