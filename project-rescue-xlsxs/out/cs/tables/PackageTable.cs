// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections;
using System.Collections.Generic;
using System.IO;
using System.Threading.Tasks;

// SheetMan's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using SheetMan.Binary;

namespace Rescue.Tables
{
    [System.Serializable]
    public partial class PackageTable
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// PackcgeItem_Table
            /// </summary>
            public int Id => _id;

            /// <summary>
            /// 영문
            /// </summary>
            public string Name => _name;

            /// <summary>
            /// 한글
            /// </summary>
            public string ClassName => _className;

            /// <summary>
            /// 카테고리
            /// </summary>
            public global::Rescue.Tables.ItemType ItemType => _itemType;

            /// <summary>
            /// 패키지 타입
            /// </summary>
            public global::Rescue.Tables.ConsumablesType PackageType => _packageType;

            /// <summary>
            /// 중첩 가능 유무
            /// </summary>
            public bool Stackable => _stackable;

            /// <summary>
            /// 최대 소유 값
            /// </summary>
            public long MaxStack => _maxStack;

            /// <summary>
            /// 획득조건 타입
            /// </summary>
            public global::Rescue.Tables.ConditionType PackageCondition => _packageCondition;

            /// <summary>
            /// 아이템 재사용 대기시간
            /// </summary>
            public int Cooltime => _cooltime;

            /// <summary>
            /// 아이템 유지 시간
            /// </summary>
            public int Duration => _duration;

            /// <summary>
            /// 포함 아이템 종류
            /// </summary>
            public int[] CurrencyID => _currencyID;

            /// <summary>
            /// 포함 아이템 최소 수량 값
            /// </summary>
            public int[] CurrencyItemMin => _currencyItemMin;

            /// <summary>
            /// 포함 아이템 최대 수량 값
            /// </summary>
            public int[] CurrencyItemMax => _currencyItemMax;

            /// <summary>
            /// 포함 아이템 확률
            /// </summary>
            public int[] CurrencyItemRate => _currencyItemRate;

            /// <summary>
            /// 아이콘 경로
            /// </summary>
            public string IconPath => _iconPath;

            /// <summary>
            /// 프리펩 위치
            /// </summary>
            public string DropPrefabPath => _dropPrefabPath;

            /// <summary>
            /// 아이템 설명
            /// </summary>
            public string ItemDescription => _itemDescription;
            #endregion

            #region Storage
            internal int _id;
            internal string _name = "";
            internal string _className = "";
            internal global::Rescue.Tables.ItemType _itemType;
            internal global::Rescue.Tables.ConsumablesType _packageType;
            internal bool _stackable;
            internal long _maxStack;
            internal global::Rescue.Tables.ConditionType _packageCondition;
            internal int _cooltime;
            internal int _duration;
            internal int[] _currencyID = System.Array.Empty<int>();
            internal int[] _currencyItemMin = System.Array.Empty<int>();
            internal int[] _currencyItemMax = System.Array.Empty<int>();
            internal int[] _currencyItemRate = System.Array.Empty<int>();
            internal string _iconPath = "";
            internal string _dropPrefabPath = "";
            internal string _itemDescription = "";
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"Id\":"); ToStringHelper.ToString(Id, sb);
                sb.Append(",\"Name\":"); ToStringHelper.ToString(Name, sb);
                sb.Append(",\"ClassName\":"); ToStringHelper.ToString(ClassName, sb);
                sb.Append(",\"ItemType\":"); ToStringHelper.ToString(ItemType, sb);
                sb.Append(",\"PackageType\":"); ToStringHelper.ToString(PackageType, sb);
                sb.Append(",\"Stackable\":"); ToStringHelper.ToString(Stackable, sb);
                sb.Append(",\"MaxStack\":"); ToStringHelper.ToString(MaxStack, sb);
                sb.Append(",\"PackageCondition\":"); ToStringHelper.ToString(PackageCondition, sb);
                sb.Append(",\"Cooltime\":"); ToStringHelper.ToString(Cooltime, sb);
                sb.Append(",\"Duration\":"); ToStringHelper.ToString(Duration, sb);
                sb.Append(",\"CurrencyID\":"); ToStringHelper.ToString(CurrencyID, sb);
                sb.Append(",\"CurrencyItemMin\":"); ToStringHelper.ToString(CurrencyItemMin, sb);
                sb.Append(",\"CurrencyItemMax\":"); ToStringHelper.ToString(CurrencyItemMax, sb);
                sb.Append(",\"CurrencyItemRate\":"); ToStringHelper.ToString(CurrencyItemRate, sb);
                sb.Append(",\"IconPath\":"); ToStringHelper.ToString(IconPath, sb);
                sb.Append(",\"DropPrefabPath\":"); ToStringHelper.ToString(DropPrefabPath, sb);
                sb.Append(",\"ItemDescription\":"); ToStringHelper.ToString(ItemDescription, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "Id", "Name", "ClassName", "ItemType", "PackageType", "Stackable", "MaxStack", "PackageCondition", "Cooltime", "Duration", "CurrencyID", "CurrencyItemMin", "CurrencyItemMax", "CurrencyItemRate", "IconPath", "DropPrefabPath", "ItemDescription" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.Id, r.Name, r.ClassName, r.ItemType, r.PackageType, r.Stackable, r.MaxStack, r.PackageCondition, r.Cooltime, r.Duration, r.CurrencyID, r.CurrencyItemMin, r.CurrencyItemMax, r.CurrencyItemRate, r.IconPath, r.DropPrefabPath, r.ItemDescription });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        #region Indexing by 'Id'
        public Dictionary<int, Record> RecordsById => _recordsById;
        private Dictionary<int, Record> _recordsById = new Dictionary<int, Record>();

        /// <summary>
        /// The row with this `Id`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language SheetMan generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindById(int key)
            => _recordsById.TryGetValue(key, out Record record) ? record : null;

        /// <summary>
        /// The row with this `Id`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetById(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public Record GetByIdOrThrow(int key)
        {
            if (!_recordsById.TryGetValue(key, out Record record))
                throw new SheetManException($"There is no record in table `Package` that corresponds to field `Id` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `Id`.</summary>
        public bool ContainsId(int key) => _recordsById.ContainsKey(key);
        #endregion // Indexing by `Id`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await Tables.ReadAllBytesAsync(filename);
            var reader = new LiteBinaryReader(bytes);
            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(LiteBinaryReader reader)
        {
            var columns = LiteBinaryTable.ReadHeader(reader, out int count);
            int tempEnumInt = 0;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        LiteBinaryTable.CheckColumn(column, "Package.Id", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementI32, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._id = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 2:
                        LiteBinaryTable.CheckColumn(column, "Package.Name", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._name);
                        }
                        break;

                    case 3:
                        LiteBinaryTable.CheckColumn(column, "Package.ClassName", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._className);
                        }
                        break;

                    case 4:
                        LiteBinaryTable.CheckColumn(column, "Package.ItemType", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._itemType = (global::Rescue.Tables.ItemType)tempEnumInt;
                        }
                        break;

                    case 5:
                        LiteBinaryTable.CheckColumn(column, "Package.PackageType", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._packageType = (global::Rescue.Tables.ConsumablesType)tempEnumInt;
                        }
                        break;

                    case 6:
                        LiteBinaryTable.CheckColumn(column, "Package.Stackable", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementBool);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._stackable);
                        }
                        break;

                    case 7:
                        LiteBinaryTable.CheckColumn(column, "Package.MaxStack", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementI64, LiteBinaryTable.ElementI32, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._maxStack = reader.ReadI64As(column.Element);
                        }
                        break;

                    case 8:
                        LiteBinaryTable.CheckColumn(column, "Package.PackageCondition", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._packageCondition = (global::Rescue.Tables.ConditionType)tempEnumInt;
                        }
                        break;

                    case 9:
                        LiteBinaryTable.CheckColumn(column, "Package.Cooltime", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementI32, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._cooltime = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 10:
                        LiteBinaryTable.CheckColumn(column, "Package.Duration", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementI32, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._duration = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 11:
                        LiteBinaryTable.CheckColumn(column, "Package.CurrencyID", LiteBinaryTable.KindVarArray, 0, LiteBinaryTable.ElementI32, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.TryReadCounter32(out int elementCount);
                            record._currencyID = new int[elementCount];
                            for (int j = 0; j < elementCount; ++j)
                            {
                                record._currencyID[j] = reader.ReadI32As(column.Element);
                            }
                        }
                        break;

                    case 12:
                        LiteBinaryTable.CheckColumn(column, "Package.CurrencyItemMin", LiteBinaryTable.KindVarArray, 0, LiteBinaryTable.ElementI32, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.TryReadCounter32(out int elementCount);
                            record._currencyItemMin = new int[elementCount];
                            for (int j = 0; j < elementCount; ++j)
                            {
                                record._currencyItemMin[j] = reader.ReadI32As(column.Element);
                            }
                        }
                        break;

                    case 13:
                        LiteBinaryTable.CheckColumn(column, "Package.CurrencyItemMax", LiteBinaryTable.KindVarArray, 0, LiteBinaryTable.ElementI32, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.TryReadCounter32(out int elementCount);
                            record._currencyItemMax = new int[elementCount];
                            for (int j = 0; j < elementCount; ++j)
                            {
                                record._currencyItemMax[j] = reader.ReadI32As(column.Element);
                            }
                        }
                        break;

                    case 14:
                        LiteBinaryTable.CheckColumn(column, "Package.CurrencyItemRate", LiteBinaryTable.KindVarArray, 0, LiteBinaryTable.ElementI32, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.TryReadCounter32(out int elementCount);
                            record._currencyItemRate = new int[elementCount];
                            for (int j = 0; j < elementCount; ++j)
                            {
                                record._currencyItemRate[j] = reader.ReadI32As(column.Element);
                            }
                        }
                        break;

                    case 15:
                        LiteBinaryTable.CheckColumn(column, "Package.IconPath", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._iconPath);
                        }
                        break;

                    case 16:
                        LiteBinaryTable.CheckColumn(column, "Package.DropPrefabPath", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._dropPrefabPath);
                        }
                        break;

                    case 17:
                        LiteBinaryTable.CheckColumn(column, "Package.ItemDescription", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._itemDescription);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                LiteBinaryTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsById = new Dictionary<int, Record>(count);
            foreach (var record in records)
                recordsById.Add(record.Id, record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsById = recordsById;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }

} // namespace Rescue.Tables
