// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections;
using System.Collections.Generic;
using System.IO;
using System.Threading.Tasks;

// SheetMan's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using SheetMan.Binary;

namespace Rescue.Tables
{
    [System.Serializable]
    public partial class SDAgencyInfoTable
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// SDAgencyInfo_Table
            /// </summary>
            public int Id => _id;

            /// <summary>
            /// 등급 이름
            /// </summary>
            public string Name => _name;

            /// <summary>
            /// 등급
            /// </summary>
            public global::Rescue.Tables.AgencyGrade AgencyGrade => _agencyGrade;

            /// <summary>
            /// 해금 조건
            /// </summary>
            public int DispatchCount => _dispatchCount;

            /// <summary>
            /// S등장 확률
            /// </summary>
            public float GradeSRate => _gradeSRate;

            /// <summary>
            /// A등장 확률
            /// </summary>
            public float GradeARate => _gradeARate;

            /// <summary>
            /// B등장 확률
            /// </summary>
            public float GradeBRate => _gradeBRate;

            /// <summary>
            /// C등장 확률
            /// </summary>
            public float GradeCRate => _gradeCRate;

            /// <summary>
            /// D등장 확률
            /// </summary>
            public float GradeDRate => _gradeDRate;

            /// <summary>
            /// E등장 확률
            /// </summary>
            public float GradeERate => _gradeERate;

            /// <summary>
            /// F등장 확률
            /// </summary>
            public float GradeFRate => _gradeFRate;

            /// <summary>
            /// 무료 초기화 횟수
            /// </summary>
            public int FreeRefresh => _freeRefresh;

            /// <summary>
            /// 초기화 재화 타입
            /// </summary>
            public global::Rescue.Tables.CurrencyType RefreshCurrencyType => _refreshCurrencyType;

            /// <summary>
            /// 초기화 재화 값
            /// </summary>
            public int RefreshCurrencyValue => _refreshCurrencyValue;
            #endregion

            #region Storage
            internal int _id;
            internal string _name = "";
            internal global::Rescue.Tables.AgencyGrade _agencyGrade;
            internal int _dispatchCount;
            internal float _gradeSRate;
            internal float _gradeARate;
            internal float _gradeBRate;
            internal float _gradeCRate;
            internal float _gradeDRate;
            internal float _gradeERate;
            internal float _gradeFRate;
            internal int _freeRefresh;
            internal global::Rescue.Tables.CurrencyType _refreshCurrencyType;
            internal int _refreshCurrencyValue;
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"Id\":"); ToStringHelper.ToString(Id, sb);
                sb.Append(",\"Name\":"); ToStringHelper.ToString(Name, sb);
                sb.Append(",\"AgencyGrade\":"); ToStringHelper.ToString(AgencyGrade, sb);
                sb.Append(",\"DispatchCount\":"); ToStringHelper.ToString(DispatchCount, sb);
                sb.Append(",\"GradeSRate\":"); ToStringHelper.ToString(GradeSRate, sb);
                sb.Append(",\"GradeARate\":"); ToStringHelper.ToString(GradeARate, sb);
                sb.Append(",\"GradeBRate\":"); ToStringHelper.ToString(GradeBRate, sb);
                sb.Append(",\"GradeCRate\":"); ToStringHelper.ToString(GradeCRate, sb);
                sb.Append(",\"GradeDRate\":"); ToStringHelper.ToString(GradeDRate, sb);
                sb.Append(",\"GradeERate\":"); ToStringHelper.ToString(GradeERate, sb);
                sb.Append(",\"GradeFRate\":"); ToStringHelper.ToString(GradeFRate, sb);
                sb.Append(",\"FreeRefresh\":"); ToStringHelper.ToString(FreeRefresh, sb);
                sb.Append(",\"RefreshCurrencyType\":"); ToStringHelper.ToString(RefreshCurrencyType, sb);
                sb.Append(",\"RefreshCurrencyValue\":"); ToStringHelper.ToString(RefreshCurrencyValue, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "Id", "Name", "AgencyGrade", "DispatchCount", "GradeSRate", "GradeARate", "GradeBRate", "GradeCRate", "GradeDRate", "GradeERate", "GradeFRate", "FreeRefresh", "RefreshCurrencyType", "RefreshCurrencyValue" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.Id, r.Name, r.AgencyGrade, r.DispatchCount, r.GradeSRate, r.GradeARate, r.GradeBRate, r.GradeCRate, r.GradeDRate, r.GradeERate, r.GradeFRate, r.FreeRefresh, r.RefreshCurrencyType, r.RefreshCurrencyValue });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        #region Indexing by 'Id'
        public Dictionary<int, Record> RecordsById => _recordsById;
        private Dictionary<int, Record> _recordsById = new Dictionary<int, Record>();

        /// <summary>
        /// The row with this `Id`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language SheetMan generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindById(int key)
            => _recordsById.TryGetValue(key, out Record record) ? record : null;

        /// <summary>
        /// The row with this `Id`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetById(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public Record GetByIdOrThrow(int key)
        {
            if (!_recordsById.TryGetValue(key, out Record record))
                throw new SheetManException($"There is no record in table `SDAgencyInfo` that corresponds to field `Id` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `Id`.</summary>
        public bool ContainsId(int key) => _recordsById.ContainsKey(key);
        #endregion // Indexing by `Id`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await Tables.ReadAllBytesAsync(filename);
            var reader = new LiteBinaryReader(bytes);
            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(LiteBinaryReader reader)
        {
            var columns = LiteBinaryTable.ReadHeader(reader, out int count);
            int tempEnumInt = 0;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        LiteBinaryTable.CheckColumn(column, "SDAgencyInfo.Id", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementI32, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._id = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 2:
                        LiteBinaryTable.CheckColumn(column, "SDAgencyInfo.Name", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._name);
                        }
                        break;

                    case 3:
                        LiteBinaryTable.CheckColumn(column, "SDAgencyInfo.AgencyGrade", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._agencyGrade = (global::Rescue.Tables.AgencyGrade)tempEnumInt;
                        }
                        break;

                    case 4:
                        LiteBinaryTable.CheckColumn(column, "SDAgencyInfo.DispatchCount", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementI32, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._dispatchCount = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 5:
                        LiteBinaryTable.CheckColumn(column, "SDAgencyInfo.GradeSRate", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._gradeSRate);
                        }
                        break;

                    case 6:
                        LiteBinaryTable.CheckColumn(column, "SDAgencyInfo.GradeARate", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._gradeARate);
                        }
                        break;

                    case 7:
                        LiteBinaryTable.CheckColumn(column, "SDAgencyInfo.GradeBRate", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._gradeBRate);
                        }
                        break;

                    case 8:
                        LiteBinaryTable.CheckColumn(column, "SDAgencyInfo.GradeCRate", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._gradeCRate);
                        }
                        break;

                    case 9:
                        LiteBinaryTable.CheckColumn(column, "SDAgencyInfo.GradeDRate", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._gradeDRate);
                        }
                        break;

                    case 10:
                        LiteBinaryTable.CheckColumn(column, "SDAgencyInfo.GradeERate", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._gradeERate);
                        }
                        break;

                    case 11:
                        LiteBinaryTable.CheckColumn(column, "SDAgencyInfo.GradeFRate", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._gradeFRate);
                        }
                        break;

                    case 12:
                        LiteBinaryTable.CheckColumn(column, "SDAgencyInfo.FreeRefresh", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementI32, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._freeRefresh = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 13:
                        LiteBinaryTable.CheckColumn(column, "SDAgencyInfo.RefreshCurrencyType", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._refreshCurrencyType = (global::Rescue.Tables.CurrencyType)tempEnumInt;
                        }
                        break;

                    case 14:
                        LiteBinaryTable.CheckColumn(column, "SDAgencyInfo.RefreshCurrencyValue", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementI32, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._refreshCurrencyValue = reader.ReadI32As(column.Element);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                LiteBinaryTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsById = new Dictionary<int, Record>(count);
            foreach (var record in records)
                recordsById.Add(record.Id, record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsById = recordsById;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }

} // namespace Rescue.Tables
