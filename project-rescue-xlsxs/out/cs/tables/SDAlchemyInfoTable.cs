// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections;
using System.Collections.Generic;
using System.IO;
using System.Threading.Tasks;

// SheetMan's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using SheetMan.Binary;

namespace Rescue.Tables
{
    [System.Serializable]
    public partial class SDAlchemyInfoTable
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// SDAlchemyInfo_Table
            /// </summary>
            public int Id => _id;

            /// <summary>
            /// 영문명
            /// </summary>
            public string Name => _name;

            /// <summary>
            /// 한글명
            /// </summary>
            public string NameKR => _nameKR;

            /// <summary>
            /// 전용 재료 타입
            /// </summary>
            public global::Rescue.Tables.CurrencyType MaterialType => _materialType;

            /// <summary>
            /// 공용 재료1 타입
            /// </summary>
            public global::Rescue.Tables.CurrencyType CommonMaterialType1 => _commonMaterialType1;

            /// <summary>
            /// 공용 재료2 타입
            /// </summary>
            public global::Rescue.Tables.CurrencyType CommonMaterialType2 => _commonMaterialType2;

            /// <summary>
            /// 재료값 증가 비율
            /// </summary>
            public float MaterialType2Rate => _materialType2Rate;

            /// <summary>
            /// 스텟 타입
            /// </summary>
            public global::Rescue.Tables.StatType StatType => _statType;

            /// <summary>
            /// 타겟 대상
            /// </summary>
            public global::Rescue.Tables.TargetType TargetType => _targetType;

            /// <summary>
            /// 최대 레벨값
            /// </summary>
            public int MaxLevelId => _maxLevelId;

            /// <summary>
            /// 가속 시간
            /// </summary>
            public int AccelerateTime => _accelerateTime;

            /// <summary>
            /// 가속권 타입
            /// </summary>
            public global::Rescue.Tables.CurrencyType AccelerateItemType1 => _accelerateItemType1;

            /// <summary>
            /// 가속권 개수
            /// </summary>
            public int AccelerateItemCost1 => _accelerateItemCost1;

            /// <summary>
            /// 다이아
            /// </summary>
            public global::Rescue.Tables.CurrencyType AccelerateItemType2 => _accelerateItemType2;

            /// <summary>
            /// 필요 다이아 개수
            /// </summary>
            public int AccelerateItemCost2 => _accelerateItemCost2;

            /// <summary>
            /// 아이콘 경로
            /// </summary>
            public string IconPath => _iconPath;
            #endregion

            #region Storage
            internal int _id;
            internal string _name = "";
            internal string _nameKR = "";
            internal global::Rescue.Tables.CurrencyType _materialType;
            internal global::Rescue.Tables.CurrencyType _commonMaterialType1;
            internal global::Rescue.Tables.CurrencyType _commonMaterialType2;
            internal float _materialType2Rate;
            internal global::Rescue.Tables.StatType _statType;
            internal global::Rescue.Tables.TargetType _targetType;
            internal int _maxLevelId;
            internal int _accelerateTime;
            internal global::Rescue.Tables.CurrencyType _accelerateItemType1;
            internal int _accelerateItemCost1;
            internal global::Rescue.Tables.CurrencyType _accelerateItemType2;
            internal int _accelerateItemCost2;
            internal string _iconPath = "";
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"Id\":"); ToStringHelper.ToString(Id, sb);
                sb.Append(",\"Name\":"); ToStringHelper.ToString(Name, sb);
                sb.Append(",\"NameKR\":"); ToStringHelper.ToString(NameKR, sb);
                sb.Append(",\"MaterialType\":"); ToStringHelper.ToString(MaterialType, sb);
                sb.Append(",\"CommonMaterialType1\":"); ToStringHelper.ToString(CommonMaterialType1, sb);
                sb.Append(",\"CommonMaterialType2\":"); ToStringHelper.ToString(CommonMaterialType2, sb);
                sb.Append(",\"MaterialType2Rate\":"); ToStringHelper.ToString(MaterialType2Rate, sb);
                sb.Append(",\"StatType\":"); ToStringHelper.ToString(StatType, sb);
                sb.Append(",\"TargetType\":"); ToStringHelper.ToString(TargetType, sb);
                sb.Append(",\"MaxLevelId\":"); ToStringHelper.ToString(MaxLevelId, sb);
                sb.Append(",\"AccelerateTime\":"); ToStringHelper.ToString(AccelerateTime, sb);
                sb.Append(",\"AccelerateItemType1\":"); ToStringHelper.ToString(AccelerateItemType1, sb);
                sb.Append(",\"AccelerateItemCost1\":"); ToStringHelper.ToString(AccelerateItemCost1, sb);
                sb.Append(",\"AccelerateItemType2\":"); ToStringHelper.ToString(AccelerateItemType2, sb);
                sb.Append(",\"AccelerateItemCost2\":"); ToStringHelper.ToString(AccelerateItemCost2, sb);
                sb.Append(",\"IconPath\":"); ToStringHelper.ToString(IconPath, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "Id", "Name", "NameKR", "MaterialType", "CommonMaterialType1", "CommonMaterialType2", "MaterialType2Rate", "StatType", "TargetType", "MaxLevelId", "AccelerateTime", "AccelerateItemType1", "AccelerateItemCost1", "AccelerateItemType2", "AccelerateItemCost2", "IconPath" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.Id, r.Name, r.NameKR, r.MaterialType, r.CommonMaterialType1, r.CommonMaterialType2, r.MaterialType2Rate, r.StatType, r.TargetType, r.MaxLevelId, r.AccelerateTime, r.AccelerateItemType1, r.AccelerateItemCost1, r.AccelerateItemType2, r.AccelerateItemCost2, r.IconPath });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        #region Indexing by 'Id'
        public Dictionary<int, Record> RecordsById => _recordsById;
        private Dictionary<int, Record> _recordsById = new Dictionary<int, Record>();

        /// <summary>
        /// The row with this `Id`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language SheetMan generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindById(int key)
            => _recordsById.TryGetValue(key, out Record record) ? record : null;

        /// <summary>
        /// The row with this `Id`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetById(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public Record GetByIdOrThrow(int key)
        {
            if (!_recordsById.TryGetValue(key, out Record record))
                throw new SheetManException($"There is no record in table `SDAlchemyInfo` that corresponds to field `Id` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `Id`.</summary>
        public bool ContainsId(int key) => _recordsById.ContainsKey(key);
        #endregion // Indexing by `Id`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await Tables.ReadAllBytesAsync(filename);
            var reader = new ScbReader(bytes);
            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(ScbReader reader)
        {
            var columns = ScbTable.ReadHeader(reader, out int count);
            ScbColumnCursor cursor;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        ScbTable.CheckColumn(column, "SDAlchemyInfo.Id", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDAlchemyInfo.Id");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._id = cursor.NextI32();
                        }
                        break;

                    case 2:
                        ScbTable.CheckColumn(column, "SDAlchemyInfo.Name", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        cursor = new ScbColumnCursor(reader, column, count, "SDAlchemyInfo.Name");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._name = cursor.NextString();
                        }
                        break;

                    case 3:
                        ScbTable.CheckColumn(column, "SDAlchemyInfo.NameKR", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        cursor = new ScbColumnCursor(reader, column, count, "SDAlchemyInfo.NameKR");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._nameKR = cursor.NextString();
                        }
                        break;

                    case 4:
                        ScbTable.CheckColumn(column, "SDAlchemyInfo.MaterialType", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDAlchemyInfo.MaterialType");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._materialType = (global::Rescue.Tables.CurrencyType)cursor.NextI32();
                        }
                        break;

                    case 5:
                        ScbTable.CheckColumn(column, "SDAlchemyInfo.CommonMaterialType1", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDAlchemyInfo.CommonMaterialType1");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._commonMaterialType1 = (global::Rescue.Tables.CurrencyType)cursor.NextI32();
                        }
                        break;

                    case 6:
                        ScbTable.CheckColumn(column, "SDAlchemyInfo.CommonMaterialType2", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDAlchemyInfo.CommonMaterialType2");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._commonMaterialType2 = (global::Rescue.Tables.CurrencyType)cursor.NextI32();
                        }
                        break;

                    case 7:
                        ScbTable.CheckColumn(column, "SDAlchemyInfo.MaterialType2Rate", ScbTable.KindScalar, 1, ScbTable.ElementF32);
                        cursor = new ScbColumnCursor(reader, column, count, "SDAlchemyInfo.MaterialType2Rate");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._materialType2Rate = cursor.NextF32();
                        }
                        break;

                    case 8:
                        ScbTable.CheckColumn(column, "SDAlchemyInfo.StatType", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDAlchemyInfo.StatType");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._statType = (global::Rescue.Tables.StatType)cursor.NextI32();
                        }
                        break;

                    case 9:
                        ScbTable.CheckColumn(column, "SDAlchemyInfo.TargetType", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDAlchemyInfo.TargetType");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._targetType = (global::Rescue.Tables.TargetType)cursor.NextI32();
                        }
                        break;

                    case 10:
                        ScbTable.CheckColumn(column, "SDAlchemyInfo.MaxLevelId", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDAlchemyInfo.MaxLevelId");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._maxLevelId = cursor.NextI32();
                        }
                        break;

                    case 11:
                        ScbTable.CheckColumn(column, "SDAlchemyInfo.AccelerateTime", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDAlchemyInfo.AccelerateTime");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._accelerateTime = cursor.NextI32();
                        }
                        break;

                    case 12:
                        ScbTable.CheckColumn(column, "SDAlchemyInfo.AccelerateItemType1", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDAlchemyInfo.AccelerateItemType1");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._accelerateItemType1 = (global::Rescue.Tables.CurrencyType)cursor.NextI32();
                        }
                        break;

                    case 13:
                        ScbTable.CheckColumn(column, "SDAlchemyInfo.AccelerateItemCost1", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDAlchemyInfo.AccelerateItemCost1");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._accelerateItemCost1 = cursor.NextI32();
                        }
                        break;

                    case 14:
                        ScbTable.CheckColumn(column, "SDAlchemyInfo.AccelerateItemType2", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDAlchemyInfo.AccelerateItemType2");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._accelerateItemType2 = (global::Rescue.Tables.CurrencyType)cursor.NextI32();
                        }
                        break;

                    case 15:
                        ScbTable.CheckColumn(column, "SDAlchemyInfo.AccelerateItemCost2", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDAlchemyInfo.AccelerateItemCost2");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._accelerateItemCost2 = cursor.NextI32();
                        }
                        break;

                    case 16:
                        ScbTable.CheckColumn(column, "SDAlchemyInfo.IconPath", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        cursor = new ScbColumnCursor(reader, column, count, "SDAlchemyInfo.IconPath");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._iconPath = cursor.NextString();
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                ScbTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsById = new Dictionary<int, Record>(count);
            foreach (var record in records)
                recordsById.Add(record.Id, record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsById = recordsById;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }

} // namespace Rescue.Tables
