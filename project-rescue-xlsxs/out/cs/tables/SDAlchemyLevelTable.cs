// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections;
using System.Collections.Generic;
using System.IO;
using System.Threading.Tasks;

// SheetMan's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using SheetMan.Binary;

namespace Rescue.Tables
{
    [System.Serializable]
    public partial class SDAlchemyLevelTable
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// SDAlchemyLevel_Table
            /// </summary>
            public int Id => _id;

            /// <summary>
            /// 영문명
            /// </summary>
            public string Name => _name;

            /// <summary>
            /// 한글명
            /// </summary>
            public string NameKR => _nameKR;

            /// <summary>
            /// 필요 재화값
            /// </summary>
            public int LevelUpCost => _levelUpCost;

            /// <summary>
            /// 누적 재화값
            /// </summary>
            public int LevelUpTotal => _levelUpTotal;

            /// <summary>
            /// 소요 시간
            /// </summary>
            public long Time => _time;

            /// <summary>
            /// 필요 다이아 개수
            /// </summary>
            public int UseDiaCost => _useDiaCost;

            /// <summary>
            /// 필요 가속권 개수
            /// </summary>
            public int UseItemCost => _useItemCost;

            /// <summary>
            /// 크리티컬 데미지 증가값
            /// </summary>
            public float CritDMGValue => _critDMGValue;

            /// <summary>
            /// 크리티컬 데미지 누적값
            /// </summary>
            public float CritDMGTotal => _critDMGTotal;

            /// <summary>
            /// 크리티컬 확률 증가값
            /// </summary>
            public float CritRateValue => _critRateValue;

            /// <summary>
            /// 크리티컬 확률 누적값
            /// </summary>
            public float CritRateTotal => _critRateTotal;

            /// <summary>
            /// 블록 확률 증가값
            /// </summary>
            public float BlockRateValue => _blockRateValue;

            /// <summary>
            /// 블록 확률 누적값
            /// </summary>
            public float BlockRateTotal => _blockRateTotal;

            /// <summary>
            /// 명중률 증가값
            /// </summary>
            public float HITValue => _hITValue;

            /// <summary>
            /// 명중률 누적값
            /// </summary>
            public float HITTotal => _hITTotal;
            #endregion

            #region Storage
            internal int _id;
            internal string _name = "";
            internal string _nameKR = "";
            internal int _levelUpCost;
            internal int _levelUpTotal;
            internal long _time;
            internal int _useDiaCost;
            internal int _useItemCost;
            internal float _critDMGValue;
            internal float _critDMGTotal;
            internal float _critRateValue;
            internal float _critRateTotal;
            internal float _blockRateValue;
            internal float _blockRateTotal;
            internal float _hITValue;
            internal float _hITTotal;
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"Id\":"); ToStringHelper.ToString(Id, sb);
                sb.Append(",\"Name\":"); ToStringHelper.ToString(Name, sb);
                sb.Append(",\"NameKR\":"); ToStringHelper.ToString(NameKR, sb);
                sb.Append(",\"LevelUpCost\":"); ToStringHelper.ToString(LevelUpCost, sb);
                sb.Append(",\"LevelUpTotal\":"); ToStringHelper.ToString(LevelUpTotal, sb);
                sb.Append(",\"Time\":"); ToStringHelper.ToString(Time, sb);
                sb.Append(",\"UseDiaCost\":"); ToStringHelper.ToString(UseDiaCost, sb);
                sb.Append(",\"UseItemCost\":"); ToStringHelper.ToString(UseItemCost, sb);
                sb.Append(",\"CritDMGValue\":"); ToStringHelper.ToString(CritDMGValue, sb);
                sb.Append(",\"CritDMGTotal\":"); ToStringHelper.ToString(CritDMGTotal, sb);
                sb.Append(",\"CritRateValue\":"); ToStringHelper.ToString(CritRateValue, sb);
                sb.Append(",\"CritRateTotal\":"); ToStringHelper.ToString(CritRateTotal, sb);
                sb.Append(",\"BlockRateValue\":"); ToStringHelper.ToString(BlockRateValue, sb);
                sb.Append(",\"BlockRateTotal\":"); ToStringHelper.ToString(BlockRateTotal, sb);
                sb.Append(",\"HITValue\":"); ToStringHelper.ToString(HITValue, sb);
                sb.Append(",\"HITTotal\":"); ToStringHelper.ToString(HITTotal, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "Id", "Name", "NameKR", "LevelUpCost", "LevelUpTotal", "Time", "UseDiaCost", "UseItemCost", "CritDMGValue", "CritDMGTotal", "CritRateValue", "CritRateTotal", "BlockRateValue", "BlockRateTotal", "HITValue", "HITTotal" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.Id, r.Name, r.NameKR, r.LevelUpCost, r.LevelUpTotal, r.Time, r.UseDiaCost, r.UseItemCost, r.CritDMGValue, r.CritDMGTotal, r.CritRateValue, r.CritRateTotal, r.BlockRateValue, r.BlockRateTotal, r.HITValue, r.HITTotal });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        #region Indexing by 'Id'
        public Dictionary<int, Record> RecordsById => _recordsById;
        private Dictionary<int, Record> _recordsById = new Dictionary<int, Record>();

        /// <summary>
        /// The row with this `Id`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language SheetMan generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindById(int key)
            => _recordsById.TryGetValue(key, out Record record) ? record : null;

        /// <summary>
        /// The row with this `Id`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetById(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public Record GetByIdOrThrow(int key)
        {
            if (!_recordsById.TryGetValue(key, out Record record))
                throw new SheetManException($"There is no record in table `SDAlchemyLevel` that corresponds to field `Id` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `Id`.</summary>
        public bool ContainsId(int key) => _recordsById.ContainsKey(key);
        #endregion // Indexing by `Id`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await Tables.ReadAllBytesAsync(filename);
            var reader = new ScbReader(bytes);
            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(ScbReader reader)
        {
            var columns = ScbTable.ReadHeader(reader, out int count);

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        ScbTable.CheckColumn(column, "SDAlchemyLevel.Id", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._id = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 2:
                        ScbTable.CheckColumn(column, "SDAlchemyLevel.Name", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._name);
                        }
                        break;

                    case 3:
                        ScbTable.CheckColumn(column, "SDAlchemyLevel.NameKR", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._nameKR);
                        }
                        break;

                    case 4:
                        ScbTable.CheckColumn(column, "SDAlchemyLevel.LevelUpCost", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._levelUpCost = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 5:
                        ScbTable.CheckColumn(column, "SDAlchemyLevel.LevelUpTotal", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._levelUpTotal = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 6:
                        ScbTable.CheckColumn(column, "SDAlchemyLevel.Time", ScbTable.KindScalar, 1, ScbTable.ElementI64, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._time = reader.ReadI64As(column.Element);
                        }
                        break;

                    case 7:
                        ScbTable.CheckColumn(column, "SDAlchemyLevel.UseDiaCost", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._useDiaCost = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 8:
                        ScbTable.CheckColumn(column, "SDAlchemyLevel.UseItemCost", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._useItemCost = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 9:
                        ScbTable.CheckColumn(column, "SDAlchemyLevel.CritDMGValue", ScbTable.KindScalar, 1, ScbTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._critDMGValue);
                        }
                        break;

                    case 10:
                        ScbTable.CheckColumn(column, "SDAlchemyLevel.CritDMGTotal", ScbTable.KindScalar, 1, ScbTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._critDMGTotal);
                        }
                        break;

                    case 11:
                        ScbTable.CheckColumn(column, "SDAlchemyLevel.CritRateValue", ScbTable.KindScalar, 1, ScbTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._critRateValue);
                        }
                        break;

                    case 12:
                        ScbTable.CheckColumn(column, "SDAlchemyLevel.CritRateTotal", ScbTable.KindScalar, 1, ScbTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._critRateTotal);
                        }
                        break;

                    case 13:
                        ScbTable.CheckColumn(column, "SDAlchemyLevel.BlockRateValue", ScbTable.KindScalar, 1, ScbTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._blockRateValue);
                        }
                        break;

                    case 14:
                        ScbTable.CheckColumn(column, "SDAlchemyLevel.BlockRateTotal", ScbTable.KindScalar, 1, ScbTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._blockRateTotal);
                        }
                        break;

                    case 15:
                        ScbTable.CheckColumn(column, "SDAlchemyLevel.HITValue", ScbTable.KindScalar, 1, ScbTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._hITValue);
                        }
                        break;

                    case 16:
                        ScbTable.CheckColumn(column, "SDAlchemyLevel.HITTotal", ScbTable.KindScalar, 1, ScbTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._hITTotal);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                ScbTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsById = new Dictionary<int, Record>(count);
            foreach (var record in records)
                recordsById.Add(record.Id, record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsById = recordsById;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }

} // namespace Rescue.Tables
