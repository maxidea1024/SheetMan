// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections;
using System.Collections.Generic;
using System.IO;
using System.Threading.Tasks;

// SheetMan's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using SheetMan.Binary;

namespace Rescue.Tables
{
    [System.Serializable]
    public partial class SDContensInfoTable
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// SDContensInfo_Table
            /// </summary>
            public int Id => _id;

            /// <summary>
            /// 영문
            /// </summary>
            public string Name => _name;

            /// <summary>
            /// 한글
            /// </summary>
            public string InfoName => _infoName;

            /// <summary>
            /// 실제 데이터가 있는 시트 이름
            /// </summary>
            public string SheetName => _sheetName;

            /// <summary>
            /// SD 콘텐츠 타입
            /// </summary>
            public global::Rescue.Tables.SdContensType SdContensType => _sdContensType;

            /// <summary>
            /// 콘텐츠 해금 조건 ID
            /// </summary>
            public int ConditionID => _conditionID;

            /// <summary>
            /// 소모 재화
            /// </summary>
            public global::Rescue.Tables.CurrencyType CurrencyType => _currencyType;

            /// <summary>
            /// 초기화 적용 유무
            /// </summary>
            public bool EnableReset => _enableReset;

            /// <summary>
            /// 초기화 타입
            /// </summary>
            public global::Rescue.Tables.CycleType CycleType => _cycleType;

            /// <summary>
            /// SD  캐릭터 프리펩 경로
            /// </summary>
            public string SDCharacterPath => _sDCharacterPath;

            /// <summary>
            /// UI 3d프리펩 경로
            /// </summary>
            public string PrefabPath => _prefabPath;
            #endregion

            #region Storage
            internal int _id;
            internal string _name = "";
            internal string _infoName = "";
            internal string _sheetName = "";
            internal global::Rescue.Tables.SdContensType _sdContensType;
            internal int _conditionID;
            internal global::Rescue.Tables.CurrencyType _currencyType;
            internal bool _enableReset;
            internal global::Rescue.Tables.CycleType _cycleType;
            internal string _sDCharacterPath = "";
            internal string _prefabPath = "";
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"Id\":"); ToStringHelper.ToString(Id, sb);
                sb.Append(",\"Name\":"); ToStringHelper.ToString(Name, sb);
                sb.Append(",\"InfoName\":"); ToStringHelper.ToString(InfoName, sb);
                sb.Append(",\"SheetName\":"); ToStringHelper.ToString(SheetName, sb);
                sb.Append(",\"SdContensType\":"); ToStringHelper.ToString(SdContensType, sb);
                sb.Append(",\"ConditionID\":"); ToStringHelper.ToString(ConditionID, sb);
                sb.Append(",\"CurrencyType\":"); ToStringHelper.ToString(CurrencyType, sb);
                sb.Append(",\"EnableReset\":"); ToStringHelper.ToString(EnableReset, sb);
                sb.Append(",\"CycleType\":"); ToStringHelper.ToString(CycleType, sb);
                sb.Append(",\"SDCharacterPath\":"); ToStringHelper.ToString(SDCharacterPath, sb);
                sb.Append(",\"PrefabPath\":"); ToStringHelper.ToString(PrefabPath, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "Id", "Name", "InfoName", "SheetName", "SdContensType", "ConditionID", "CurrencyType", "EnableReset", "CycleType", "SDCharacterPath", "PrefabPath" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.Id, r.Name, r.InfoName, r.SheetName, r.SdContensType, r.ConditionID, r.CurrencyType, r.EnableReset, r.CycleType, r.SDCharacterPath, r.PrefabPath });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        #region Indexing by 'Id'
        public Dictionary<int, Record> RecordsById => _recordsById;
        private Dictionary<int, Record> _recordsById = new Dictionary<int, Record>();

        /// <summary>
        /// The row with this `Id`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language SheetMan generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindById(int key)
            => _recordsById.TryGetValue(key, out Record record) ? record : null;

        /// <summary>
        /// The row with this `Id`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetById(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public Record GetByIdOrThrow(int key)
        {
            if (!_recordsById.TryGetValue(key, out Record record))
                throw new SheetManException($"There is no record in table `SDContensInfo` that corresponds to field `Id` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `Id`.</summary>
        public bool ContainsId(int key) => _recordsById.ContainsKey(key);
        #endregion // Indexing by `Id`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await Tables.ReadAllBytesAsync(filename);
            var reader = new ScbReader(bytes);
            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(ScbReader reader)
        {
            var columns = ScbTable.ReadHeader(reader, out int count);
            ScbColumnCursor cursor;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        ScbTable.CheckColumn(column, "SDContensInfo.Id", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDContensInfo.Id");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._id = cursor.NextI32();
                        }
                        break;

                    case 2:
                        ScbTable.CheckColumn(column, "SDContensInfo.Name", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        cursor = new ScbColumnCursor(reader, column, count, "SDContensInfo.Name");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._name = cursor.NextString();
                        }
                        break;

                    case 3:
                        ScbTable.CheckColumn(column, "SDContensInfo.InfoName", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        cursor = new ScbColumnCursor(reader, column, count, "SDContensInfo.InfoName");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._infoName = cursor.NextString();
                        }
                        break;

                    case 4:
                        ScbTable.CheckColumn(column, "SDContensInfo.SheetName", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        cursor = new ScbColumnCursor(reader, column, count, "SDContensInfo.SheetName");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._sheetName = cursor.NextString();
                        }
                        break;

                    case 5:
                        ScbTable.CheckColumn(column, "SDContensInfo.SdContensType", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDContensInfo.SdContensType");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._sdContensType = (global::Rescue.Tables.SdContensType)cursor.NextI32();
                        }
                        break;

                    case 6:
                        ScbTable.CheckColumn(column, "SDContensInfo.ConditionID", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDContensInfo.ConditionID");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._conditionID = cursor.NextI32();
                        }
                        break;

                    case 7:
                        ScbTable.CheckColumn(column, "SDContensInfo.CurrencyType", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDContensInfo.CurrencyType");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._currencyType = (global::Rescue.Tables.CurrencyType)cursor.NextI32();
                        }
                        break;

                    case 8:
                        ScbTable.CheckColumn(column, "SDContensInfo.EnableReset", ScbTable.KindScalar, 1, ScbTable.ElementBool);
                        cursor = new ScbColumnCursor(reader, column, count, "SDContensInfo.EnableReset");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._enableReset = cursor.NextBool();
                        }
                        break;

                    case 9:
                        ScbTable.CheckColumn(column, "SDContensInfo.CycleType", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDContensInfo.CycleType");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._cycleType = (global::Rescue.Tables.CycleType)cursor.NextI32();
                        }
                        break;

                    case 10:
                        ScbTable.CheckColumn(column, "SDContensInfo.SDCharacterPath", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        cursor = new ScbColumnCursor(reader, column, count, "SDContensInfo.SDCharacterPath");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._sDCharacterPath = cursor.NextString();
                        }
                        break;

                    case 11:
                        ScbTable.CheckColumn(column, "SDContensInfo.PrefabPath", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        cursor = new ScbColumnCursor(reader, column, count, "SDContensInfo.PrefabPath");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._prefabPath = cursor.NextString();
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                ScbTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsById = new Dictionary<int, Record>(count);
            foreach (var record in records)
                recordsById.Add(record.Id, record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsById = recordsById;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }

} // namespace Rescue.Tables
