// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections;
using System.Collections.Generic;
using System.IO;
using System.Threading.Tasks;

// SheetMan's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using SheetMan.Binary;

namespace Rescue.Tables
{
    [System.Serializable]
    public partial class SDDungeonInfoTable
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// SDDungeonInfo_Table
            /// </summary>
            public int Id => _id;

            /// <summary>
            /// 영문명
            /// </summary>
            public string Name => _name;

            /// <summary>
            /// 한글명
            /// </summary>
            public string NameKR => _nameKR;

            /// <summary>
            /// 총 카드 수
            /// </summary>
            public int TotalCount => _totalCount;

            /// <summary>
            /// 함정 카드 수
            /// </summary>
            public int TrapCount => _trapCount;

            /// <summary>
            /// 행동 수
            /// </summary>
            public int ActionCount => _actionCount;

            /// <summary>
            /// 성공 차감수
            /// </summary>
            public int MatchCost => _matchCost;

            /// <summary>
            /// 실패 차감수
            /// </summary>
            public int MismatchCost => _mismatchCost;

            /// <summary>
            /// 함정 차감수
            /// </summary>
            public int TrapCost => _trapCost;

            /// <summary>
            /// 미리 보기 시간
            /// </summary>
            public int ViewTime => _viewTime;

            /// <summary>
            /// 오답 체크 시간
            /// </summary>
            public int Time => _time;

            /// <summary>
            /// 광고 횟수
            /// </summary>
            public int ADCount => _aDCount;

            /// <summary>
            /// 일일 무료 횟수
            /// </summary>
            public int DailyFreeCount => _dailyFreeCount;
            #endregion

            #region Storage
            internal int _id;
            internal string _name = "";
            internal string _nameKR = "";
            internal int _totalCount;
            internal int _trapCount;
            internal int _actionCount;
            internal int _matchCost;
            internal int _mismatchCost;
            internal int _trapCost;
            internal int _viewTime;
            internal int _time;
            internal int _aDCount;
            internal int _dailyFreeCount;
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"Id\":"); ToStringHelper.ToString(Id, sb);
                sb.Append(",\"Name\":"); ToStringHelper.ToString(Name, sb);
                sb.Append(",\"NameKR\":"); ToStringHelper.ToString(NameKR, sb);
                sb.Append(",\"TotalCount\":"); ToStringHelper.ToString(TotalCount, sb);
                sb.Append(",\"TrapCount\":"); ToStringHelper.ToString(TrapCount, sb);
                sb.Append(",\"ActionCount\":"); ToStringHelper.ToString(ActionCount, sb);
                sb.Append(",\"MatchCost\":"); ToStringHelper.ToString(MatchCost, sb);
                sb.Append(",\"MismatchCost\":"); ToStringHelper.ToString(MismatchCost, sb);
                sb.Append(",\"TrapCost\":"); ToStringHelper.ToString(TrapCost, sb);
                sb.Append(",\"ViewTime\":"); ToStringHelper.ToString(ViewTime, sb);
                sb.Append(",\"Time\":"); ToStringHelper.ToString(Time, sb);
                sb.Append(",\"ADCount\":"); ToStringHelper.ToString(ADCount, sb);
                sb.Append(",\"DailyFreeCount\":"); ToStringHelper.ToString(DailyFreeCount, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "Id", "Name", "NameKR", "TotalCount", "TrapCount", "ActionCount", "MatchCost", "MismatchCost", "TrapCost", "ViewTime", "Time", "ADCount", "DailyFreeCount" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.Id, r.Name, r.NameKR, r.TotalCount, r.TrapCount, r.ActionCount, r.MatchCost, r.MismatchCost, r.TrapCost, r.ViewTime, r.Time, r.ADCount, r.DailyFreeCount });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        #region Indexing by 'Id'
        public Dictionary<int, Record> RecordsById => _recordsById;
        private Dictionary<int, Record> _recordsById = new Dictionary<int, Record>();

        /// <summary>
        /// The row with this `Id`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language SheetMan generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindById(int key)
            => _recordsById.TryGetValue(key, out Record record) ? record : null;

        /// <summary>
        /// The row with this `Id`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetById(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public Record GetByIdOrThrow(int key)
        {
            if (!_recordsById.TryGetValue(key, out Record record))
                throw new SheetManException($"There is no record in table `SDDungeonInfo` that corresponds to field `Id` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `Id`.</summary>
        public bool ContainsId(int key) => _recordsById.ContainsKey(key);
        #endregion // Indexing by `Id`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await Tables.ReadAllBytesAsync(filename);
            var reader = new ScbReader(bytes);
            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(ScbReader reader)
        {
            var columns = ScbTable.ReadHeader(reader, out int count);

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        ScbTable.CheckColumn(column, "SDDungeonInfo.Id", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._id = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 2:
                        ScbTable.CheckColumn(column, "SDDungeonInfo.Name", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._name);
                        }
                        break;

                    case 3:
                        ScbTable.CheckColumn(column, "SDDungeonInfo.NameKR", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._nameKR);
                        }
                        break;

                    case 4:
                        ScbTable.CheckColumn(column, "SDDungeonInfo.TotalCount", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._totalCount = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 5:
                        ScbTable.CheckColumn(column, "SDDungeonInfo.TrapCount", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._trapCount = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 6:
                        ScbTable.CheckColumn(column, "SDDungeonInfo.ActionCount", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._actionCount = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 7:
                        ScbTable.CheckColumn(column, "SDDungeonInfo.MatchCost", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._matchCost = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 8:
                        ScbTable.CheckColumn(column, "SDDungeonInfo.MismatchCost", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._mismatchCost = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 9:
                        ScbTable.CheckColumn(column, "SDDungeonInfo.TrapCost", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._trapCost = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 10:
                        ScbTable.CheckColumn(column, "SDDungeonInfo.ViewTime", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._viewTime = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 11:
                        ScbTable.CheckColumn(column, "SDDungeonInfo.Time", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._time = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 12:
                        ScbTable.CheckColumn(column, "SDDungeonInfo.ADCount", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._aDCount = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 13:
                        ScbTable.CheckColumn(column, "SDDungeonInfo.DailyFreeCount", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._dailyFreeCount = reader.ReadI32As(column.Element);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                ScbTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsById = new Dictionary<int, Record>(count);
            foreach (var record in records)
                recordsById.Add(record.Id, record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsById = recordsById;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }

} // namespace Rescue.Tables
