// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections;
using System.Collections.Generic;
using System.IO;
using System.Threading.Tasks;

// SheetMan's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using SheetMan.Binary;

namespace Rescue.Tables
{
    [System.Serializable]
    public partial class SDTrainingInfoTable
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// SDTrainingInfo_Table
            /// </summary>
            public int Id => _id;

            /// <summary>
            /// 영문명
            /// </summary>
            public string Name => _name;

            /// <summary>
            /// 한글명
            /// </summary>
            public string TrainingName => _trainingName;

            /// <summary>
            /// 적용 속성 타입
            /// </summary>
            public global::Rescue.Tables.AttributeType AttributeType => _attributeType;

            /// <summary>
            /// 스탯 타입
            /// </summary>
            public global::Rescue.Tables.StatType StatType => _statType;

            /// <summary>
            /// 해제 조건 대상
            /// </summary>
            public int UnlockCondition => _unlockCondition;

            /// <summary>
            /// 레벨업 제한 타입
            /// </summary>
            public int LevelUpCondition => _levelUpCondition;

            /// <summary>
            /// 최대 레벨
            /// </summary>
            public int MaxLevel => _maxLevel;

            /// <summary>
            /// 레벨 초기화 여부
            /// </summary>
            public bool IsLvReset => _isLvReset;

            /// <summary>
            /// 레벨 초기화 재화
            /// </summary>
            public global::Rescue.Tables.CurrencyType LvResetCurrencyType => _lvResetCurrencyType;

            /// <summary>
            /// 레벨 초기화 재화값
            /// </summary>
            public int LvResetCurrencyValue => _lvResetCurrencyValue;

            /// <summary>
            /// 설명
            /// </summary>
            public string Description => _description;
            #endregion

            #region Storage
            internal int _id;
            internal string _name = "";
            internal string _trainingName = "";
            internal global::Rescue.Tables.AttributeType _attributeType;
            internal global::Rescue.Tables.StatType _statType;
            internal int _unlockCondition;
            internal int _levelUpCondition;
            internal int _maxLevel;
            internal bool _isLvReset;
            internal global::Rescue.Tables.CurrencyType _lvResetCurrencyType;
            internal int _lvResetCurrencyValue;
            internal string _description = "";
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"Id\":"); ToStringHelper.ToString(Id, sb);
                sb.Append(",\"Name\":"); ToStringHelper.ToString(Name, sb);
                sb.Append(",\"TrainingName\":"); ToStringHelper.ToString(TrainingName, sb);
                sb.Append(",\"AttributeType\":"); ToStringHelper.ToString(AttributeType, sb);
                sb.Append(",\"StatType\":"); ToStringHelper.ToString(StatType, sb);
                sb.Append(",\"UnlockCondition\":"); ToStringHelper.ToString(UnlockCondition, sb);
                sb.Append(",\"LevelUpCondition\":"); ToStringHelper.ToString(LevelUpCondition, sb);
                sb.Append(",\"MaxLevel\":"); ToStringHelper.ToString(MaxLevel, sb);
                sb.Append(",\"IsLvReset\":"); ToStringHelper.ToString(IsLvReset, sb);
                sb.Append(",\"LvResetCurrencyType\":"); ToStringHelper.ToString(LvResetCurrencyType, sb);
                sb.Append(",\"LvResetCurrencyValue\":"); ToStringHelper.ToString(LvResetCurrencyValue, sb);
                sb.Append(",\"Description\":"); ToStringHelper.ToString(Description, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "Id", "Name", "TrainingName", "AttributeType", "StatType", "UnlockCondition", "LevelUpCondition", "MaxLevel", "IsLvReset", "LvResetCurrencyType", "LvResetCurrencyValue", "Description" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.Id, r.Name, r.TrainingName, r.AttributeType, r.StatType, r.UnlockCondition, r.LevelUpCondition, r.MaxLevel, r.IsLvReset, r.LvResetCurrencyType, r.LvResetCurrencyValue, r.Description });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        #region Indexing by 'Id'
        public Dictionary<int, Record> RecordsById => _recordsById;
        private Dictionary<int, Record> _recordsById = new Dictionary<int, Record>();

        /// <summary>
        /// The row with this `Id`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language SheetMan generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindById(int key)
            => _recordsById.TryGetValue(key, out Record record) ? record : null;

        /// <summary>
        /// The row with this `Id`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetById(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public Record GetByIdOrThrow(int key)
        {
            if (!_recordsById.TryGetValue(key, out Record record))
                throw new SheetManException($"There is no record in table `SDTrainingInfo` that corresponds to field `Id` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `Id`.</summary>
        public bool ContainsId(int key) => _recordsById.ContainsKey(key);
        #endregion // Indexing by `Id`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await Tables.ReadAllBytesAsync(filename);
            var reader = new ScbReader(bytes);
            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(ScbReader reader)
        {
            var columns = ScbTable.ReadHeader(reader, out int count);
            int tempEnumInt = 0;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        ScbTable.CheckColumn(column, "SDTrainingInfo.Id", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._id = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 2:
                        ScbTable.CheckColumn(column, "SDTrainingInfo.Name", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._name);
                        }
                        break;

                    case 3:
                        ScbTable.CheckColumn(column, "SDTrainingInfo.TrainingName", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._trainingName);
                        }
                        break;

                    case 4:
                        ScbTable.CheckColumn(column, "SDTrainingInfo.AttributeType", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._attributeType = (global::Rescue.Tables.AttributeType)tempEnumInt;
                        }
                        break;

                    case 5:
                        ScbTable.CheckColumn(column, "SDTrainingInfo.StatType", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._statType = (global::Rescue.Tables.StatType)tempEnumInt;
                        }
                        break;

                    case 6:
                        ScbTable.CheckColumn(column, "SDTrainingInfo.UnlockCondition", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._unlockCondition = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 7:
                        ScbTable.CheckColumn(column, "SDTrainingInfo.LevelUpCondition", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._levelUpCondition = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 8:
                        ScbTable.CheckColumn(column, "SDTrainingInfo.MaxLevel", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._maxLevel = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 9:
                        ScbTable.CheckColumn(column, "SDTrainingInfo.IsLvReset", ScbTable.KindScalar, 1, ScbTable.ElementBool);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._isLvReset);
                        }
                        break;

                    case 10:
                        ScbTable.CheckColumn(column, "SDTrainingInfo.LvResetCurrencyType", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._lvResetCurrencyType = (global::Rescue.Tables.CurrencyType)tempEnumInt;
                        }
                        break;

                    case 11:
                        ScbTable.CheckColumn(column, "SDTrainingInfo.LvResetCurrencyValue", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._lvResetCurrencyValue = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 12:
                        ScbTable.CheckColumn(column, "SDTrainingInfo.Description", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._description);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                ScbTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsById = new Dictionary<int, Record>(count);
            foreach (var record in records)
                recordsById.Add(record.Id, record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsById = recordsById;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }

} // namespace Rescue.Tables
