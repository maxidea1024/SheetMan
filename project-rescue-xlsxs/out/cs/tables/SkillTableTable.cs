// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections;
using System.Collections.Generic;
using System.IO;
using System.Threading.Tasks;

// SheetMan's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using SheetMan.Binary;

namespace Rescue.Tables
{
    [System.Serializable]
    public partial class SkillTableTable
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// Skill_Table
            /// </summary>
            public int Id => _id;

            /// <summary>
            /// 영문
            /// </summary>
            public string Name => _name;

            /// <summary>
            /// 한글
            /// </summary>
            public string SkillName => _skillName;

            /// <summary>
            /// 메모
            /// </summary>
            public global::Rescue.Tables.SkillType SkillType => _skillType;

            /// <summary>
            /// 메모 참조
            /// </summary>
            public global::Rescue.Tables.SkillSubType SkillSubType => _skillSubType;

            /// <summary>
            /// 속성 설정
            /// </summary>
            public global::Rescue.Tables.AttributeType AttributeType => _attributeType;

            /// <summary>
            /// 메모참조
            /// </summary>
            public global::Rescue.Tables.TargetType TargetType => _targetType;

            /// <summary>
            /// 스킬 경로
            /// </summary>
            public string AniPath => _aniPath;

            /// <summary>
            /// 스킬 사용 거리
            /// </summary>
            public float SkillUseRange => _skillUseRange;

            /// <summary>
            /// 스킬 쿨타임
            /// </summary>
            public float CoolTime => _coolTime;

            /// <summary>
            /// 버프 ID
            /// </summary>
            public int[] BuffID => _buffID;

            /// <summary>
            /// 필살기 아이콘 경로
            /// </summary>
            public string SkillIcon => _skillIcon;

            /// <summary>
            /// 설명
            /// </summary>
            public string Description => _description;
            #endregion

            #region Storage
            internal int _id;
            internal string _name = "";
            internal string _skillName = "";
            internal global::Rescue.Tables.SkillType _skillType;
            internal global::Rescue.Tables.SkillSubType _skillSubType;
            internal global::Rescue.Tables.AttributeType _attributeType;
            internal global::Rescue.Tables.TargetType _targetType;
            internal string _aniPath = "";
            internal float _skillUseRange;
            internal float _coolTime;
            internal int[] _buffID = System.Array.Empty<int>();
            internal string _skillIcon = "";
            internal string _description = "";
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"Id\":"); ToStringHelper.ToString(Id, sb);
                sb.Append(",\"Name\":"); ToStringHelper.ToString(Name, sb);
                sb.Append(",\"SkillName\":"); ToStringHelper.ToString(SkillName, sb);
                sb.Append(",\"SkillType\":"); ToStringHelper.ToString(SkillType, sb);
                sb.Append(",\"SkillSubType\":"); ToStringHelper.ToString(SkillSubType, sb);
                sb.Append(",\"AttributeType\":"); ToStringHelper.ToString(AttributeType, sb);
                sb.Append(",\"TargetType\":"); ToStringHelper.ToString(TargetType, sb);
                sb.Append(",\"AniPath\":"); ToStringHelper.ToString(AniPath, sb);
                sb.Append(",\"SkillUseRange\":"); ToStringHelper.ToString(SkillUseRange, sb);
                sb.Append(",\"CoolTime\":"); ToStringHelper.ToString(CoolTime, sb);
                sb.Append(",\"BuffID\":"); ToStringHelper.ToString(BuffID, sb);
                sb.Append(",\"SkillIcon\":"); ToStringHelper.ToString(SkillIcon, sb);
                sb.Append(",\"Description\":"); ToStringHelper.ToString(Description, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "Id", "Name", "SkillName", "SkillType", "SkillSubType", "AttributeType", "TargetType", "AniPath", "SkillUseRange", "CoolTime", "BuffID", "SkillIcon", "Description" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.Id, r.Name, r.SkillName, r.SkillType, r.SkillSubType, r.AttributeType, r.TargetType, r.AniPath, r.SkillUseRange, r.CoolTime, r.BuffID, r.SkillIcon, r.Description });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        #region Indexing by 'Id'
        public Dictionary<int, Record> RecordsById => _recordsById;
        private Dictionary<int, Record> _recordsById = new Dictionary<int, Record>();

        /// <summary>
        /// The row with this `Id`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language SheetMan generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindById(int key)
            => _recordsById.TryGetValue(key, out Record record) ? record : null;

        /// <summary>
        /// The row with this `Id`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetById(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public Record GetByIdOrThrow(int key)
        {
            if (!_recordsById.TryGetValue(key, out Record record))
                throw new SheetManException($"There is no record in table `SkillTable` that corresponds to field `Id` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `Id`.</summary>
        public bool ContainsId(int key) => _recordsById.ContainsKey(key);
        #endregion // Indexing by `Id`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await Tables.ReadAllBytesAsync(filename);
            var reader = new LiteBinaryReader(bytes);
            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(LiteBinaryReader reader)
        {
            var columns = LiteBinaryTable.ReadHeader(reader, out int count);
            int tempEnumInt = 0;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        LiteBinaryTable.CheckColumn(column, "SkillTable.Id", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementI32, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._id = reader.ReadI32As(column.Element);
                        }
                        break;

                    case 2:
                        LiteBinaryTable.CheckColumn(column, "SkillTable.Name", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._name);
                        }
                        break;

                    case 3:
                        LiteBinaryTable.CheckColumn(column, "SkillTable.SkillName", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._skillName);
                        }
                        break;

                    case 4:
                        LiteBinaryTable.CheckColumn(column, "SkillTable.SkillType", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._skillType = (global::Rescue.Tables.SkillType)tempEnumInt;
                        }
                        break;

                    case 5:
                        LiteBinaryTable.CheckColumn(column, "SkillTable.SkillSubType", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._skillSubType = (global::Rescue.Tables.SkillSubType)tempEnumInt;
                        }
                        break;

                    case 6:
                        LiteBinaryTable.CheckColumn(column, "SkillTable.AttributeType", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._attributeType = (global::Rescue.Tables.AttributeType)tempEnumInt;
                        }
                        break;

                    case 7:
                        LiteBinaryTable.CheckColumn(column, "SkillTable.TargetType", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.ReadOptimalInt32(out tempEnumInt);
                            record._targetType = (global::Rescue.Tables.TargetType)tempEnumInt;
                        }
                        break;

                    case 8:
                        LiteBinaryTable.CheckColumn(column, "SkillTable.AniPath", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._aniPath);
                        }
                        break;

                    case 9:
                        LiteBinaryTable.CheckColumn(column, "SkillTable.SkillUseRange", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._skillUseRange);
                        }
                        break;

                    case 10:
                        LiteBinaryTable.CheckColumn(column, "SkillTable.CoolTime", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementF32);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._coolTime);
                        }
                        break;

                    case 11:
                        LiteBinaryTable.CheckColumn(column, "SkillTable.BuffID", LiteBinaryTable.KindVarArray, 0, LiteBinaryTable.ElementI32, LiteBinaryTable.ElementVarint);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.TryReadCounter32(out int elementCount);
                            record._buffID = new int[elementCount];
                            for (int j = 0; j < elementCount; ++j)
                            {
                                record._buffID[j] = reader.ReadI32As(column.Element);
                            }
                        }
                        break;

                    case 12:
                        LiteBinaryTable.CheckColumn(column, "SkillTable.SkillIcon", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._skillIcon);
                        }
                        break;

                    case 13:
                        LiteBinaryTable.CheckColumn(column, "SkillTable.Description", LiteBinaryTable.KindScalar, 1, LiteBinaryTable.ElementString);
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            reader.Read(out record._description);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                LiteBinaryTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsById = new Dictionary<int, Record>(count);
            foreach (var record in records)
                recordsById.Add(record.Id, record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsById = recordsById;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }

} // namespace Rescue.Tables
