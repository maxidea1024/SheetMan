// ------------------------------------------------------------------------------
// Generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

part of '../tables.dart';
// Generated from project-rescue-xlsxs/SoundData.xlsx : SFXSoundTable : A1
class SFXSoundRecord {
  /// SFXSound_Table
  int id = 0;
  /// 영문
  String name = '';
  /// 카테고리
  SFXCategoryType category = SFXCategoryType.of(0);
  /// 사운드 주소
  String path = '';
  /// 프리로드
  String preloadGroup = '';
  /// 설명
  String description = '';

}

/// Every row of SFXSound.
class SFXSoundTable {
  /// Every row, in the order the sheet declared them.
  ///
  /// The list a read published, not one a read fills. A refresh replaces the reference
  /// rather than the contents, so whoever holds this holds one whole load.
  List<SFXSoundRecord> get records => _records;
  List<SFXSoundRecord> _records = [];

  Map<int, SFXSoundRecord> _byId = {};

  /// The row with this Id, or null when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  SFXSoundRecord? findById(int key) => _byId[key];

  /// The row with this Id, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading getById(key).name at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  SFXSoundRecord getByIdOrThrow(int key) {
    final record = _byId[key];

    if (record == null) {
      throw RecordNotFoundException(
          'there is no record in table `SFXSound` that corresponds to '
          'field `Id` value $key');
    }

    return record;
  }

  /// Whether the table holds a row with this Id.
  bool containsId(int key) => _byId.containsKey(key);

  /// Loads the table from a .scb file written by SheetMan.
  /// Column by column, matched by tag rather than position: a column this build does
  /// not know is skipped by its block length, and one whose type changed incompatibly
  /// fails naming the field.
  void read(String filename) {
    final reader = ScbReader(readAllBytes(filename));
    final header = readTableHeader(reader);
    final count = header.rowCount;
    late ScbColumnCursor cursor;

    // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
    final loaded = <SFXSoundRecord>[];
    final loadedById = <int, SFXSoundRecord>{};

    for (var i = 0; i < count; i++) {
      loaded.add(SFXSoundRecord());
    }

    for (final column in header.columns) {
      final blockEnd = reader.position + column.byteLength;

      switch (column.tag) {
        case 1:
          checkColumn(column, 'SFXSound.Id', kindScalar, 1, [elementI32, elementVarint]);
          cursor = ScbColumnCursor(reader, column, count, 'SFXSound.Id');
          for (final record in loaded) {
            record.id = cursor.nextI32();
          }
          break;
        case 2:
          checkColumn(column, 'SFXSound.Name', kindScalar, 1, [elementString]);
          cursor = ScbColumnCursor(reader, column, count, 'SFXSound.Name');
          for (final record in loaded) {
            record.name = cursor.nextString();
          }
          break;
        case 3:
          checkColumn(column, 'SFXSound.Category', kindScalar, 1, [elementVarint]);
          cursor = ScbColumnCursor(reader, column, count, 'SFXSound.Category');
          for (final record in loaded) {
            record.category = SFXCategoryType.of(cursor.nextI32());
          }
          break;
        case 4:
          checkColumn(column, 'SFXSound.Path', kindScalar, 1, [elementString]);
          cursor = ScbColumnCursor(reader, column, count, 'SFXSound.Path');
          for (final record in loaded) {
            record.path = cursor.nextString();
          }
          break;
        case 5:
          checkColumn(column, 'SFXSound.PreloadGroup', kindScalar, 1, [elementString]);
          cursor = ScbColumnCursor(reader, column, count, 'SFXSound.PreloadGroup');
          for (final record in loaded) {
            record.preloadGroup = cursor.nextString();
          }
          break;
        case 6:
          checkColumn(column, 'SFXSound.Description', kindScalar, 1, [elementString]);
          cursor = ScbColumnCursor(reader, column, count, 'SFXSound.Description');
          for (final record in loaded) {
            record.description = cursor.nextString();
          }
          break;
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength);
          break;
      }

      checkBlockEnd(reader, column, blockEnd);
    }

    for (final record in loaded) {
      loadedById[record.id] = record;
    }

    // Published, now that every column read.
    _records = loaded;
    _byId = loadedById;
  }
}
