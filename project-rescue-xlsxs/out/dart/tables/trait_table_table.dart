// ------------------------------------------------------------------------------
// Generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

part of '../tables.dart';
// Generated from project-rescue-xlsxs/StageGrowth.xlsx : TraitTable : A1
class TraitTableRecord {
  /// 기본값
  int id = 0;
  /// 영문
  String name = '';
  /// 한글
  String traitName = '';
  /// 스탯 타입
  StatType statType = StatType.of(0);
  /// 증가 수치
  double statRate = 0.0;
  /// 해제 조건
  List<int> unlockCondition = [];
  /// 아이콘 경로
  String iconPath = '';

}

/// Every row of TraitTable.
class TraitTableTable {
  /// Every row, in the order the sheet declared them.
  ///
  /// The list a read published, not one a read fills. A refresh replaces the reference
  /// rather than the contents, so whoever holds this holds one whole load.
  List<TraitTableRecord> get records => _records;
  List<TraitTableRecord> _records = [];

  Map<int, TraitTableRecord> _byId = {};

  /// The row with this Id, or null when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  TraitTableRecord? findById(int key) => _byId[key];

  /// The row with this Id, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading getById(key).name at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  TraitTableRecord getByIdOrThrow(int key) {
    final record = _byId[key];

    if (record == null) {
      throw RecordNotFoundException(
          'there is no record in table `TraitTable` that corresponds to '
          'field `Id` value $key');
    }

    return record;
  }

  /// Whether the table holds a row with this Id.
  bool containsId(int key) => _byId.containsKey(key);

  /// Loads the table from a .scb file written by SheetMan.
  /// Column by column, matched by tag rather than position: a column this build does
  /// not know is skipped by its block length, and one whose type changed incompatibly
  /// fails naming the field.
  void read(String filename) {
    final reader = LiteBinaryReader(readAllBytes(filename));
    final header = readTableHeader(reader);
    final count = header.rowCount;

    // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
    final loaded = <TraitTableRecord>[];
    final loadedById = <int, TraitTableRecord>{};

    for (var i = 0; i < count; i++) {
      loaded.add(TraitTableRecord());
    }

    for (final column in header.columns) {
      final blockEnd = reader.position + column.byteLength;

      switch (column.tag) {
        case 1:
          checkColumn(column, 'TraitTable.Id', kindScalar, 1, [elementI32, elementVarint]);
          for (final record in loaded) {
            record.id = reader.readI32As(column.element);
          }
          break;
        case 2:
          checkColumn(column, 'TraitTable.Name', kindScalar, 1, [elementString]);
          for (final record in loaded) {
            record.name = reader.readString();
          }
          break;
        case 3:
          checkColumn(column, 'TraitTable.TraitName', kindScalar, 1, [elementString]);
          for (final record in loaded) {
            record.traitName = reader.readString();
          }
          break;
        case 4:
          checkColumn(column, 'TraitTable.StatType', kindScalar, 1, [elementVarint]);
          for (final record in loaded) {
            record.statType = StatType.of(reader.readEnum());
          }
          break;
        case 5:
          checkColumn(column, 'TraitTable.StatRate', kindScalar, 1, [elementF32]);
          for (final record in loaded) {
            record.statRate = reader.readFloat();
          }
          break;
        case 6:
          checkColumn(column, 'TraitTable.UnlockCondition', kindVarArray, 0, [elementI32, elementVarint]);
          for (final record in loaded) {
            final elementCount = reader.readCounter32();
            record.unlockCondition = List.generate(elementCount, (_) => reader.readI32As(column.element));
          }
          break;
        case 7:
          checkColumn(column, 'TraitTable.IconPath', kindScalar, 1, [elementString]);
          for (final record in loaded) {
            record.iconPath = reader.readString();
          }
          break;
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength);
          break;
      }

      checkBlockEnd(reader, column, blockEnd);
    }

    for (final record in loaded) {
      loadedById[record.id] = record;
    }

    // Published, now that every column read.
    _records = loaded;
    _byId = loadedById;
  }
}
