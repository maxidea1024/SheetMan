// ------------------------------------------------------------------------------
// Code generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package rescue

import (
	"fmt"

	"rescue/sheetman"
)

// ArtifactLevelRecord was generated from project-rescue-xlsxs/MasterData.xlsx : ArtifactLevelTable : A1.
type ArtifactLevelRecord struct {
	// ArtifactLevelTable
	Id int32
	// 영문
	Name string
	// 한글이름
	NameKR string
	// 레벨
	Level int32
	// 필요 경험치
	CharacterEXP int64
	// 누적 경험치
	AccumulatedEXP int64
	// 공격력 고정 비율
	ATKGrowth float32
	// 방어력 고정 비율
	DEFGrowth float32
	// 체력 증가 비율
	HPGrowth float32
}

// ArtifactLevelTable holds every row of ArtifactLevel.
type ArtifactLevelTable struct {
	records []ArtifactLevelRecord
	byId map[int32]int
}

// Records returns every row, in the order the sheet declared them.
func (t *ArtifactLevelTable) Records() []ArtifactLevelRecord { return t.records }

// FindById returns the row with this Id, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *ArtifactLevelTable) FindById(key int32) *ArtifactLevelRecord {
	position, found := t.byId[key]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByIdOrError returns the row with this Id, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *ArtifactLevelTable) GetByIdOrError(key int32) (*ArtifactLevelRecord, error) {
	position, found := t.byId[key]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `ArtifactLevel` that corresponds to field `Id` value %v", key)
	}

	return &t.records[position], nil
}

// ContainsId reports whether the table holds a row with this Id.
func (t *ArtifactLevelTable) ContainsId(key int32) bool {
	_, found := t.byId[key]
	return found
}

// Read loads the table from a .scb file written by SheetMan.
func (t *ArtifactLevelTable) Read(filename string) error {
	data, err := sheetman.ReadAllBytes(filename)
	if err != nil {
		return err
	}

	reader := sheetman.NewReader(data)
	count, columns := sheetman.ReadTableHeader(reader)

	// Column by column, matched by tag rather than position: a column this build does
	// not know is skipped by its block length, and one whose type changed incompatibly
	// fails naming the field.
	//
	// Read into rows of its own and assigned at the end. Reading a table that is already
	// loaded is a refresh, and a refresh that turns out to be unreadable has to leave what
	// the table holds alone - the previous rows and an error, not an empty table.
	records := make([]ArtifactLevelRecord, count)

	for _, column := range columns {
		blockEnd := reader.Position() + int(column.ByteLength)

		switch column.Tag {
		case 1:
			if sheetman.CheckColumn(reader, column, "ArtifactLevel.Id", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "ArtifactLevel.Id")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Id = cursor.NextI32()
				}
			}
		case 2:
			if sheetman.CheckColumn(reader, column, "ArtifactLevel.Name", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "ArtifactLevel.Name")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Name = cursor.NextString()
				}
			}
		case 3:
			if sheetman.CheckColumn(reader, column, "ArtifactLevel.NameKR", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "ArtifactLevel.NameKR")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.NameKR = cursor.NextString()
				}
			}
		case 4:
			if sheetman.CheckColumn(reader, column, "ArtifactLevel.Level", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "ArtifactLevel.Level")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Level = cursor.NextI32()
				}
			}
		case 5:
			if sheetman.CheckColumn(reader, column, "ArtifactLevel.CharacterEXP", sheetman.KindScalar, 1, sheetman.ElementI64, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "ArtifactLevel.CharacterEXP")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.CharacterEXP = cursor.NextI64()
				}
			}
		case 6:
			if sheetman.CheckColumn(reader, column, "ArtifactLevel.AccumulatedEXP", sheetman.KindScalar, 1, sheetman.ElementI64, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "ArtifactLevel.AccumulatedEXP")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.AccumulatedEXP = cursor.NextI64()
				}
			}
		case 7:
			if sheetman.CheckColumn(reader, column, "ArtifactLevel.ATKGrowth", sheetman.KindScalar, 1, sheetman.ElementF32) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.ATKGrowth = reader.ReadFloat32()
				}
			}
		case 8:
			if sheetman.CheckColumn(reader, column, "ArtifactLevel.DEFGrowth", sheetman.KindScalar, 1, sheetman.ElementF32) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.DEFGrowth = reader.ReadFloat32()
				}
			}
		case 9:
			if sheetman.CheckColumn(reader, column, "ArtifactLevel.HPGrowth", sheetman.KindScalar, 1, sheetman.ElementF32) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.HPGrowth = reader.ReadFloat32()
				}
			}
		default:
			// A column added after this code was generated.
			reader.Skip(column.ByteLength)
		}

		sheetman.CheckBlockEnd(reader, column, blockEnd)
	}

	if err := reader.Err(); err != nil {
		return fmt.Errorf("ArtifactLevel: %w", err)
	}

	byId := make(map[int32]int, len(records))
	for i := range records {
		byId[records[i].Id] = i
	}

	// Published, now that every column read and the indexes are built.
	t.records = records
	t.byId = byId

	return nil
}
