// ------------------------------------------------------------------------------
// Code generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package rescue

import (
	"fmt"

	"rescue/sheetman"
)

// ArtifactRecord was generated from project-rescue-xlsxs/MasterData.xlsx : ArtifactTable : A1.
type ArtifactRecord struct {
	// Artifact_Table
	Id int32
	// 영문
	Name string
	// 아티팩트 타입
	ArtifactType ArtifactJobType
	// 아티팩트 태생 등급
	Grade GradeType
	// 장착 버프 효과
	EquipSkillID []int32
	// 장착 스탯 타입
	EquipStatType StatType
	// 장착 스탯 값
	EquipStatRate float32
	// 보유 스탯 타입
	CollectionType StatType
	// 보유 스탯 값
	CollectionVlaue float32
	// 아이콘 경로
	IconPath string
	// 머티리얼 경로
	MaterialPath string
	// 설명
	Description string
}

// ArtifactTable holds every row of Artifact.
type ArtifactTable struct {
	records []ArtifactRecord
	byId map[int32]int
}

// Records returns every row, in the order the sheet declared them.
func (t *ArtifactTable) Records() []ArtifactRecord { return t.records }

// FindById returns the row with this Id, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *ArtifactTable) FindById(key int32) *ArtifactRecord {
	position, found := t.byId[key]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByIdOrError returns the row with this Id, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *ArtifactTable) GetByIdOrError(key int32) (*ArtifactRecord, error) {
	position, found := t.byId[key]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `Artifact` that corresponds to field `Id` value %v", key)
	}

	return &t.records[position], nil
}

// ContainsId reports whether the table holds a row with this Id.
func (t *ArtifactTable) ContainsId(key int32) bool {
	_, found := t.byId[key]
	return found
}

// Read loads the table from a .scb file written by SheetMan.
func (t *ArtifactTable) Read(filename string) error {
	data, err := sheetman.ReadAllBytes(filename)
	if err != nil {
		return err
	}

	reader := sheetman.NewReader(data)
	count, columns := sheetman.ReadTableHeader(reader)

	// Column by column, matched by tag rather than position: a column this build does
	// not know is skipped by its block length, and one whose type changed incompatibly
	// fails naming the field.
	//
	// Read into rows of its own and assigned at the end. Reading a table that is already
	// loaded is a refresh, and a refresh that turns out to be unreadable has to leave what
	// the table holds alone - the previous rows and an error, not an empty table.
	records := make([]ArtifactRecord, count)

	for _, column := range columns {
		blockEnd := reader.Position() + int(column.ByteLength)

		switch column.Tag {
		case 1:
			if sheetman.CheckColumn(reader, column, "Artifact.Id", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Artifact.Id")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Id = cursor.NextI32()
				}
			}
		case 2:
			if sheetman.CheckColumn(reader, column, "Artifact.Name", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Artifact.Name")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Name = cursor.NextString()
				}
			}
		case 3:
			if sheetman.CheckColumn(reader, column, "Artifact.ArtifactType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Artifact.ArtifactType")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.ArtifactType = ArtifactJobType(cursor.NextI32())
				}
			}
		case 4:
			if sheetman.CheckColumn(reader, column, "Artifact.Grade", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Artifact.Grade")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Grade = GradeType(cursor.NextI32())
				}
			}
		case 5:
			if sheetman.CheckColumn(reader, column, "Artifact.EquipSkillID", sheetman.KindVarArray, 0, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					elementCount := int(reader.ReadCounter32())
					r.EquipSkillID = make([]int32, elementCount)
					for j := 0; j < elementCount; j++ {
						r.EquipSkillID[j] = reader.ReadI32As(column.Element)
					}
				}
			}
		case 6:
			if sheetman.CheckColumn(reader, column, "Artifact.EquipStatType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Artifact.EquipStatType")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.EquipStatType = StatType(cursor.NextI32())
				}
			}
		case 7:
			if sheetman.CheckColumn(reader, column, "Artifact.EquipStatRate", sheetman.KindScalar, 1, sheetman.ElementF32) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.EquipStatRate = reader.ReadFloat32()
				}
			}
		case 8:
			if sheetman.CheckColumn(reader, column, "Artifact.CollectionType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Artifact.CollectionType")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.CollectionType = StatType(cursor.NextI32())
				}
			}
		case 9:
			if sheetman.CheckColumn(reader, column, "Artifact.CollectionVlaue", sheetman.KindScalar, 1, sheetman.ElementF32) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.CollectionVlaue = reader.ReadFloat32()
				}
			}
		case 10:
			if sheetman.CheckColumn(reader, column, "Artifact.IconPath", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Artifact.IconPath")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.IconPath = cursor.NextString()
				}
			}
		case 11:
			if sheetman.CheckColumn(reader, column, "Artifact.MaterialPath", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Artifact.MaterialPath")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.MaterialPath = cursor.NextString()
				}
			}
		case 12:
			if sheetman.CheckColumn(reader, column, "Artifact.Description", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Artifact.Description")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Description = cursor.NextString()
				}
			}
		default:
			// A column added after this code was generated.
			reader.Skip(column.ByteLength)
		}

		sheetman.CheckBlockEnd(reader, column, blockEnd)
	}

	if err := reader.Err(); err != nil {
		return fmt.Errorf("Artifact: %w", err)
	}

	byId := make(map[int32]int, len(records))
	for i := range records {
		byId[records[i].Id] = i
	}

	// Published, now that every column read and the indexes are built.
	t.records = records
	t.byId = byId

	return nil
}
