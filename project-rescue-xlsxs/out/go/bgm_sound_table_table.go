// ------------------------------------------------------------------------------
// Code generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package rescue

import (
	"fmt"

	"rescue/sheetman"
)

// BGMSoundTableRecord was generated from project-rescue-xlsxs\SoundData.xlsx : BGMSoundTable : A1.
type BGMSoundTableRecord struct {
	// BGMSound_Table
	Id int32
	// 영문
	Name string
	// 사운드 주소
	Path string
	// 루프
	Loop bool
	// 페이드인/아웃
	FadeTime float32
	// 설명
	Description string
}

// BGMSoundTableTable holds every row of BGMSoundTable.
type BGMSoundTableTable struct {
	records []BGMSoundTableRecord
	byId map[int32]int
}

// Records returns every row, in the order the sheet declared them.
func (t *BGMSoundTableTable) Records() []BGMSoundTableRecord { return t.records }

// FindById returns the row with this Id, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *BGMSoundTableTable) FindById(key int32) *BGMSoundTableRecord {
	position, found := t.byId[key]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByIdOrError returns the row with this Id, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *BGMSoundTableTable) GetByIdOrError(key int32) (*BGMSoundTableRecord, error) {
	position, found := t.byId[key]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `BGMSoundTable` that corresponds to field `Id` value %v", key)
	}

	return &t.records[position], nil
}

// ContainsId reports whether the table holds a row with this Id.
func (t *BGMSoundTableTable) ContainsId(key int32) bool {
	_, found := t.byId[key]
	return found
}

// Read loads the table from a .table file written by SheetMan.
func (t *BGMSoundTableTable) Read(filename string) error {
	data, err := sheetman.ReadAllBytes(filename)
	if err != nil {
		return err
	}

	reader := sheetman.NewReader(data)
	count, columns := sheetman.ReadTableHeader(reader)

	// Column by column, matched by tag rather than position: a column this build does
	// not know is skipped by its block length, and one whose type changed incompatibly
	// fails naming the field.
	//
	// Read into rows of its own and assigned at the end. Reading a table that is already
	// loaded is a refresh, and a refresh that turns out to be unreadable has to leave what
	// the table holds alone - the previous rows and an error, not an empty table.
	records := make([]BGMSoundTableRecord, count)

	for _, column := range columns {
		blockEnd := reader.Position() + int(column.ByteLength)

		switch column.Tag {
		case 1:
			if sheetman.CheckColumn(reader, column, "BGMSoundTable.Id", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Id = reader.ReadI32As(column.Element)
				}
			}
		case 2:
			if sheetman.CheckColumn(reader, column, "BGMSoundTable.Name", sheetman.KindScalar, 1, sheetman.ElementString) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Name = reader.ReadString()
				}
			}
		case 3:
			if sheetman.CheckColumn(reader, column, "BGMSoundTable.Path", sheetman.KindScalar, 1, sheetman.ElementString) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Path = reader.ReadString()
				}
			}
		case 4:
			if sheetman.CheckColumn(reader, column, "BGMSoundTable.Loop", sheetman.KindScalar, 1, sheetman.ElementBool) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Loop = reader.ReadBool()
				}
			}
		case 5:
			if sheetman.CheckColumn(reader, column, "BGMSoundTable.FadeTime", sheetman.KindScalar, 1, sheetman.ElementF32) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.FadeTime = reader.ReadFloat32()
				}
			}
		case 6:
			if sheetman.CheckColumn(reader, column, "BGMSoundTable.Description", sheetman.KindScalar, 1, sheetman.ElementString) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Description = reader.ReadString()
				}
			}
		default:
			// A column added after this code was generated.
			reader.Skip(column.ByteLength)
		}

		sheetman.CheckBlockEnd(reader, column, blockEnd)
	}

	if err := reader.Err(); err != nil {
		return fmt.Errorf("BGMSoundTable: %w", err)
	}

	byId := make(map[int32]int, len(records))
	for i := range records {
		byId[records[i].Id] = i
	}

	// Published, now that every column read and the indexes are built.
	t.records = records
	t.byId = byId

	return nil
}
