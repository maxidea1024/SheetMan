// ------------------------------------------------------------------------------
// Code generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package rescue

import (
	"fmt"

	"rescue/sheetman"
)

// BuffSelectRecord was generated from project-rescue-xlsxs/MasterData.xlsx : BuffSelectTable : A1.
type BuffSelectRecord struct {
	// ID
	Id int32
	// 버프 이름
	BuffName string
	// 버프 ID
	BuffID int32
	// 등급
	Grade GradeType
	// 확률
	BuffRate float32
	// 버프 튤팁
	BuffTooltip string
	// 버프 아이콘 경로
	IconPath string
}

// BuffSelectTable holds every row of BuffSelect.
type BuffSelectTable struct {
	records []BuffSelectRecord
	byId map[int32]int
}

// Records returns every row, in the order the sheet declared them.
func (t *BuffSelectTable) Records() []BuffSelectRecord { return t.records }

// FindById returns the row with this Id, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *BuffSelectTable) FindById(key int32) *BuffSelectRecord {
	position, found := t.byId[key]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByIdOrError returns the row with this Id, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *BuffSelectTable) GetByIdOrError(key int32) (*BuffSelectRecord, error) {
	position, found := t.byId[key]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `BuffSelect` that corresponds to field `Id` value %v", key)
	}

	return &t.records[position], nil
}

// ContainsId reports whether the table holds a row with this Id.
func (t *BuffSelectTable) ContainsId(key int32) bool {
	_, found := t.byId[key]
	return found
}

// Read loads the table from a .scb file written by SheetMan.
func (t *BuffSelectTable) Read(filename string) error {
	data, err := sheetman.ReadAllBytes(filename)
	if err != nil {
		return err
	}

	reader := sheetman.NewReader(data)
	count, columns := sheetman.ReadTableHeader(reader)

	// Column by column, matched by tag rather than position: a column this build does
	// not know is skipped by its block length, and one whose type changed incompatibly
	// fails naming the field.
	//
	// Read into rows of its own and assigned at the end. Reading a table that is already
	// loaded is a refresh, and a refresh that turns out to be unreadable has to leave what
	// the table holds alone - the previous rows and an error, not an empty table.
	records := make([]BuffSelectRecord, count)

	for _, column := range columns {
		blockEnd := reader.Position() + int(column.ByteLength)

		switch column.Tag {
		case 1:
			if sheetman.CheckColumn(reader, column, "BuffSelect.Id", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "BuffSelect.Id")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Id = cursor.NextI32()
				}
			}
		case 2:
			if sheetman.CheckColumn(reader, column, "BuffSelect.BuffName", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "BuffSelect.BuffName")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.BuffName = cursor.NextString()
				}
			}
		case 3:
			if sheetman.CheckColumn(reader, column, "BuffSelect.BuffID", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "BuffSelect.BuffID")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.BuffID = cursor.NextI32()
				}
			}
		case 4:
			if sheetman.CheckColumn(reader, column, "BuffSelect.Grade", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "BuffSelect.Grade")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Grade = GradeType(cursor.NextI32())
				}
			}
		case 5:
			if sheetman.CheckColumn(reader, column, "BuffSelect.BuffRate", sheetman.KindScalar, 1, sheetman.ElementF32) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.BuffRate = reader.ReadFloat32()
				}
			}
		case 6:
			if sheetman.CheckColumn(reader, column, "BuffSelect.BuffTooltip", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "BuffSelect.BuffTooltip")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.BuffTooltip = cursor.NextString()
				}
			}
		case 7:
			if sheetman.CheckColumn(reader, column, "BuffSelect.IconPath", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "BuffSelect.IconPath")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.IconPath = cursor.NextString()
				}
			}
		default:
			// A column added after this code was generated.
			reader.Skip(column.ByteLength)
		}

		sheetman.CheckBlockEnd(reader, column, blockEnd)
	}

	if err := reader.Err(); err != nil {
		return fmt.Errorf("BuffSelect: %w", err)
	}

	byId := make(map[int32]int, len(records))
	for i := range records {
		byId[records[i].Id] = i
	}

	// Published, now that every column read and the indexes are built.
	t.records = records
	t.byId = byId

	return nil
}
