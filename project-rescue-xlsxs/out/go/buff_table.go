// ------------------------------------------------------------------------------
// Code generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package rescue

import (
	"fmt"

	"rescue/sheetman"
)

// BuffRecord was generated from project-rescue-xlsxs/MasterData.xlsx : BuffTable : A1.
type BuffRecord struct {
	// ID
	Id int32
	// SkillTable에서 Name을 불러와서 사용
	BuffName string
	// 한글 명
	Name string
	// 버프 타입_메모확인
	SkillType SkillType
	// 어떤 종류의 버프 인지
	StatType StatType
	// 버프 우선 적용 순위
	Priority int32
	// 버프 속성
	AttributeType AttributeType
	// 타겟 설정
	TargetType TargetType
	// 상태에 따른 발동 조건
	BuffConditionType BuffConditionType
	// 증가되는 값(상수)
	BuffValue int32
	// 증가되는 값(%)
	BuffValueRate float32
	// 버프 시간
	BuffTime int32
	// 버프 틱 시간
	BuffTickTime int32
	// 버프 최대 스택
	BuffMaxStack int32
	// 버프 쿨타임 1초 = 1000
	BuffCoolDownTime float32
	// 버프 표시 유무 FALSE/TRUE
	IsVisible bool
	// 버프 아이콘 경로
	IconPath string
	// 버프 튤팁
	Description string
}

// BuffTable holds every row of Buff.
type BuffTable struct {
	records []BuffRecord
	byId map[int32]int
}

// Records returns every row, in the order the sheet declared them.
func (t *BuffTable) Records() []BuffRecord { return t.records }

// FindById returns the row with this Id, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *BuffTable) FindById(key int32) *BuffRecord {
	position, found := t.byId[key]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByIdOrError returns the row with this Id, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *BuffTable) GetByIdOrError(key int32) (*BuffRecord, error) {
	position, found := t.byId[key]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `Buff` that corresponds to field `Id` value %v", key)
	}

	return &t.records[position], nil
}

// ContainsId reports whether the table holds a row with this Id.
func (t *BuffTable) ContainsId(key int32) bool {
	_, found := t.byId[key]
	return found
}

// Read loads the table from a .scb file written by SheetMan.
func (t *BuffTable) Read(filename string) error {
	data, err := sheetman.ReadAllBytes(filename)
	if err != nil {
		return err
	}

	reader := sheetman.NewReader(data)
	count, columns := sheetman.ReadTableHeader(reader)

	// Column by column, matched by tag rather than position: a column this build does
	// not know is skipped by its block length, and one whose type changed incompatibly
	// fails naming the field.
	//
	// Read into rows of its own and assigned at the end. Reading a table that is already
	// loaded is a refresh, and a refresh that turns out to be unreadable has to leave what
	// the table holds alone - the previous rows and an error, not an empty table.
	records := make([]BuffRecord, count)

	for _, column := range columns {
		blockEnd := reader.Position() + int(column.ByteLength)

		switch column.Tag {
		case 1:
			if sheetman.CheckColumn(reader, column, "Buff.Id", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Id = reader.ReadI32As(column.Element)
				}
			}
		case 2:
			if sheetman.CheckColumn(reader, column, "Buff.BuffName", sheetman.KindScalar, 1, sheetman.ElementString) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.BuffName = reader.ReadString()
				}
			}
		case 3:
			if sheetman.CheckColumn(reader, column, "Buff.Name", sheetman.KindScalar, 1, sheetman.ElementString) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Name = reader.ReadString()
				}
			}
		case 4:
			if sheetman.CheckColumn(reader, column, "Buff.SkillType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.SkillType = SkillType(reader.ReadEnum())
				}
			}
		case 5:
			if sheetman.CheckColumn(reader, column, "Buff.StatType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.StatType = StatType(reader.ReadEnum())
				}
			}
		case 6:
			if sheetman.CheckColumn(reader, column, "Buff.Priority", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Priority = reader.ReadI32As(column.Element)
				}
			}
		case 7:
			if sheetman.CheckColumn(reader, column, "Buff.AttributeType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.AttributeType = AttributeType(reader.ReadEnum())
				}
			}
		case 8:
			if sheetman.CheckColumn(reader, column, "Buff.TargetType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.TargetType = TargetType(reader.ReadEnum())
				}
			}
		case 9:
			if sheetman.CheckColumn(reader, column, "Buff.BuffConditionType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.BuffConditionType = BuffConditionType(reader.ReadEnum())
				}
			}
		case 10:
			if sheetman.CheckColumn(reader, column, "Buff.BuffValue", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.BuffValue = reader.ReadI32As(column.Element)
				}
			}
		case 11:
			if sheetman.CheckColumn(reader, column, "Buff.BuffValueRate", sheetman.KindScalar, 1, sheetman.ElementF32) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.BuffValueRate = reader.ReadFloat32()
				}
			}
		case 12:
			if sheetman.CheckColumn(reader, column, "Buff.BuffTime", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.BuffTime = reader.ReadI32As(column.Element)
				}
			}
		case 13:
			if sheetman.CheckColumn(reader, column, "Buff.BuffTickTime", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.BuffTickTime = reader.ReadI32As(column.Element)
				}
			}
		case 14:
			if sheetman.CheckColumn(reader, column, "Buff.BuffMaxStack", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.BuffMaxStack = reader.ReadI32As(column.Element)
				}
			}
		case 15:
			if sheetman.CheckColumn(reader, column, "Buff.BuffCoolDownTime", sheetman.KindScalar, 1, sheetman.ElementF32) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.BuffCoolDownTime = reader.ReadFloat32()
				}
			}
		case 16:
			if sheetman.CheckColumn(reader, column, "Buff.IsVisible", sheetman.KindScalar, 1, sheetman.ElementBool) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.IsVisible = reader.ReadBool()
				}
			}
		case 17:
			if sheetman.CheckColumn(reader, column, "Buff.IconPath", sheetman.KindScalar, 1, sheetman.ElementString) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.IconPath = reader.ReadString()
				}
			}
		case 18:
			if sheetman.CheckColumn(reader, column, "Buff.Description", sheetman.KindScalar, 1, sheetman.ElementString) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Description = reader.ReadString()
				}
			}
		default:
			// A column added after this code was generated.
			reader.Skip(column.ByteLength)
		}

		sheetman.CheckBlockEnd(reader, column, blockEnd)
	}

	if err := reader.Err(); err != nil {
		return fmt.Errorf("Buff: %w", err)
	}

	byId := make(map[int32]int, len(records))
	for i := range records {
		byId[records[i].Id] = i
	}

	// Published, now that every column read and the indexes are built.
	t.records = records
	t.byId = byId

	return nil
}
