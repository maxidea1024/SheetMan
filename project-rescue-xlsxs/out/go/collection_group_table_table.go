// ------------------------------------------------------------------------------
// Code generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package rescue

import (
	"fmt"

	"rescue/sheetman"
)

// CollectionGroupTableRecord was generated from project-rescue-xlsxs\CollectionData.xlsx : CollectionGroupTable : A1.
type CollectionGroupTableRecord struct {
	// ID
	Id int32
	// 영문
	Name string
	// 출력 순서
	Index int32
	// 캐릭터 Index(프리펩에 있는 캐릭터와 같아야 함)
	CharacterList []int32
	// 프리펩 위치
	PrefabPath string
}

// CollectionGroupTableTable holds every row of CollectionGroupTable.
type CollectionGroupTableTable struct {
	records []CollectionGroupTableRecord
	byId map[int32]int
}

// Records returns every row, in the order the sheet declared them.
func (t *CollectionGroupTableTable) Records() []CollectionGroupTableRecord { return t.records }

// FindById returns the row with this Id, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *CollectionGroupTableTable) FindById(key int32) *CollectionGroupTableRecord {
	position, found := t.byId[key]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByIdOrError returns the row with this Id, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *CollectionGroupTableTable) GetByIdOrError(key int32) (*CollectionGroupTableRecord, error) {
	position, found := t.byId[key]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `CollectionGroupTable` that corresponds to field `Id` value %v", key)
	}

	return &t.records[position], nil
}

// ContainsId reports whether the table holds a row with this Id.
func (t *CollectionGroupTableTable) ContainsId(key int32) bool {
	_, found := t.byId[key]
	return found
}

// Read loads the table from a .table file written by SheetMan.
func (t *CollectionGroupTableTable) Read(filename string) error {
	data, err := sheetman.ReadAllBytes(filename)
	if err != nil {
		return err
	}

	reader := sheetman.NewReader(data)
	count, columns := sheetman.ReadTableHeader(reader)

	// Column by column, matched by tag rather than position: a column this build does
	// not know is skipped by its block length, and one whose type changed incompatibly
	// fails naming the field.
	//
	// Read into rows of its own and assigned at the end. Reading a table that is already
	// loaded is a refresh, and a refresh that turns out to be unreadable has to leave what
	// the table holds alone - the previous rows and an error, not an empty table.
	records := make([]CollectionGroupTableRecord, count)

	for _, column := range columns {
		blockEnd := reader.Position() + int(column.ByteLength)

		switch column.Tag {
		case 1:
			if sheetman.CheckColumn(reader, column, "CollectionGroupTable.Id", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Id = reader.ReadI32As(column.Element)
				}
			}
		case 2:
			if sheetman.CheckColumn(reader, column, "CollectionGroupTable.Name", sheetman.KindScalar, 1, sheetman.ElementString) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Name = reader.ReadString()
				}
			}
		case 3:
			if sheetman.CheckColumn(reader, column, "CollectionGroupTable.Index", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Index = reader.ReadI32As(column.Element)
				}
			}
		case 4:
			if sheetman.CheckColumn(reader, column, "CollectionGroupTable.CharacterList", sheetman.KindVarArray, 0, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					elementCount := int(reader.ReadCounter32())
					r.CharacterList = make([]int32, elementCount)
					for j := 0; j < elementCount; j++ {
						r.CharacterList[j] = reader.ReadI32As(column.Element)
					}
				}
			}
		case 5:
			if sheetman.CheckColumn(reader, column, "CollectionGroupTable.PrefabPath", sheetman.KindScalar, 1, sheetman.ElementString) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.PrefabPath = reader.ReadString()
				}
			}
		default:
			// A column added after this code was generated.
			reader.Skip(column.ByteLength)
		}

		sheetman.CheckBlockEnd(reader, column, blockEnd)
	}

	if err := reader.Err(); err != nil {
		return fmt.Errorf("CollectionGroupTable: %w", err)
	}

	byId := make(map[int32]int, len(records))
	for i := range records {
		byId[records[i].Id] = i
	}

	// Published, now that every column read and the indexes are built.
	t.records = records
	t.byId = byId

	return nil
}
