// ------------------------------------------------------------------------------
// Code generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package rescue

import (
	"fmt"

	"rescue/sheetman"
)

// CollectionRecord was generated from project-rescue-xlsxs/CollectionData.xlsx : CollectionTable : A1.
type CollectionRecord struct {
	// CollectionID
	Id int32
	// 영문
	Name string
	// 캐릭터ID
	CharacterID int32
	// 보상 UI분기 타입
	TabType CollectionTabType
	// 조건 값
	ConditionID int32
	// 재화 보상 타입
	RewardType CurrencyType
	// 재화 보상 값
	RewardValue int32
	// 스탯 보상 타입
	RewardStatType1 StatType
	// 스탯 보상(%)
	RewardStatRate float32
	// 스탯 보상 타입
	RewardStatType2 StatType
	// 스탯 보상(+)
	RewardStatValue int32
	// 바로가기 ID
	ShortCutID int32
}

// CollectionTable holds every row of Collection.
type CollectionTable struct {
	records []CollectionRecord
	byId map[int32]int
}

// Records returns every row, in the order the sheet declared them.
func (t *CollectionTable) Records() []CollectionRecord { return t.records }

// FindById returns the row with this Id, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *CollectionTable) FindById(key int32) *CollectionRecord {
	position, found := t.byId[key]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByIdOrError returns the row with this Id, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *CollectionTable) GetByIdOrError(key int32) (*CollectionRecord, error) {
	position, found := t.byId[key]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `Collection` that corresponds to field `Id` value %v", key)
	}

	return &t.records[position], nil
}

// ContainsId reports whether the table holds a row with this Id.
func (t *CollectionTable) ContainsId(key int32) bool {
	_, found := t.byId[key]
	return found
}

// Read loads the table from a .scb file written by SheetMan.
func (t *CollectionTable) Read(filename string) error {
	data, err := sheetman.ReadAllBytes(filename)
	if err != nil {
		return err
	}

	reader := sheetman.NewReader(data)
	count, columns := sheetman.ReadTableHeader(reader)

	// Column by column, matched by tag rather than position: a column this build does
	// not know is skipped by its block length, and one whose type changed incompatibly
	// fails naming the field.
	//
	// Read into rows of its own and assigned at the end. Reading a table that is already
	// loaded is a refresh, and a refresh that turns out to be unreadable has to leave what
	// the table holds alone - the previous rows and an error, not an empty table.
	records := make([]CollectionRecord, count)

	for _, column := range columns {
		blockEnd := reader.Position() + int(column.ByteLength)

		switch column.Tag {
		case 1:
			if sheetman.CheckColumn(reader, column, "Collection.Id", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Collection.Id")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Id = cursor.NextI32()
				}
			}
		case 2:
			if sheetman.CheckColumn(reader, column, "Collection.Name", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Collection.Name")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Name = cursor.NextString()
				}
			}
		case 3:
			if sheetman.CheckColumn(reader, column, "Collection.CharacterID", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Collection.CharacterID")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.CharacterID = cursor.NextI32()
				}
			}
		case 4:
			if sheetman.CheckColumn(reader, column, "Collection.TabType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Collection.TabType")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.TabType = CollectionTabType(cursor.NextI32())
				}
			}
		case 5:
			if sheetman.CheckColumn(reader, column, "Collection.ConditionID", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Collection.ConditionID")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.ConditionID = cursor.NextI32()
				}
			}
		case 6:
			if sheetman.CheckColumn(reader, column, "Collection.RewardType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Collection.RewardType")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.RewardType = CurrencyType(cursor.NextI32())
				}
			}
		case 7:
			if sheetman.CheckColumn(reader, column, "Collection.RewardValue", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Collection.RewardValue")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.RewardValue = cursor.NextI32()
				}
			}
		case 8:
			if sheetman.CheckColumn(reader, column, "Collection.RewardStatType1", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Collection.RewardStatType1")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.RewardStatType1 = StatType(cursor.NextI32())
				}
			}
		case 9:
			if sheetman.CheckColumn(reader, column, "Collection.RewardStatRate", sheetman.KindScalar, 1, sheetman.ElementF32) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Collection.RewardStatRate")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.RewardStatRate = cursor.NextF32()
				}
			}
		case 10:
			if sheetman.CheckColumn(reader, column, "Collection.RewardStatType2", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Collection.RewardStatType2")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.RewardStatType2 = StatType(cursor.NextI32())
				}
			}
		case 11:
			if sheetman.CheckColumn(reader, column, "Collection.RewardStatValue", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Collection.RewardStatValue")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.RewardStatValue = cursor.NextI32()
				}
			}
		case 12:
			if sheetman.CheckColumn(reader, column, "Collection.ShortCutID", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Collection.ShortCutID")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.ShortCutID = cursor.NextI32()
				}
			}
		default:
			// A column added after this code was generated.
			reader.Skip(column.ByteLength)
		}

		sheetman.CheckBlockEnd(reader, column, blockEnd)
	}

	if err := reader.Err(); err != nil {
		return fmt.Errorf("Collection: %w", err)
	}

	byId := make(map[int32]int, len(records))
	for i := range records {
		byId[records[i].Id] = i
	}

	// Published, now that every column read and the indexes are built.
	t.records = records
	t.byId = byId

	return nil
}
