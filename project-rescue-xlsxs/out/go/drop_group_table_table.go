// ------------------------------------------------------------------------------
// Code generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package rescue

import (
	"fmt"

	"rescue/sheetman"
)

// DropGroupTableRecord was generated from project-rescue-xlsxs/MasterData.xlsx : DropGroupTable : A1.
type DropGroupTableRecord struct {
	// 기본값
	Id int32
	// 영문
	Name string
	// 한글
	DropName string
	// 드랍 구역
	DropArea string
	// 아이템 종류1
	DropItemIds []int32
	// 아이템 최소 수량 값
	DropItemMin []int32
	// 아이템 최대 수량 값
	DropItemMax []int32
	// 확률
	ItemDropRate []float32
}

// DropGroupTableTable holds every row of DropGroupTable.
type DropGroupTableTable struct {
	records []DropGroupTableRecord
	byId map[int32]int
}

// Records returns every row, in the order the sheet declared them.
func (t *DropGroupTableTable) Records() []DropGroupTableRecord { return t.records }

// FindById returns the row with this Id, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *DropGroupTableTable) FindById(key int32) *DropGroupTableRecord {
	position, found := t.byId[key]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByIdOrError returns the row with this Id, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *DropGroupTableTable) GetByIdOrError(key int32) (*DropGroupTableRecord, error) {
	position, found := t.byId[key]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `DropGroupTable` that corresponds to field `Id` value %v", key)
	}

	return &t.records[position], nil
}

// ContainsId reports whether the table holds a row with this Id.
func (t *DropGroupTableTable) ContainsId(key int32) bool {
	_, found := t.byId[key]
	return found
}

// Read loads the table from a .scb file written by SheetMan.
func (t *DropGroupTableTable) Read(filename string) error {
	data, err := sheetman.ReadAllBytes(filename)
	if err != nil {
		return err
	}

	reader := sheetman.NewReader(data)
	count, columns := sheetman.ReadTableHeader(reader)

	// Column by column, matched by tag rather than position: a column this build does
	// not know is skipped by its block length, and one whose type changed incompatibly
	// fails naming the field.
	//
	// Read into rows of its own and assigned at the end. Reading a table that is already
	// loaded is a refresh, and a refresh that turns out to be unreadable has to leave what
	// the table holds alone - the previous rows and an error, not an empty table.
	records := make([]DropGroupTableRecord, count)

	for _, column := range columns {
		blockEnd := reader.Position() + int(column.ByteLength)

		switch column.Tag {
		case 1:
			if sheetman.CheckColumn(reader, column, "DropGroupTable.Id", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Id = reader.ReadI32As(column.Element)
				}
			}
		case 2:
			if sheetman.CheckColumn(reader, column, "DropGroupTable.Name", sheetman.KindScalar, 1, sheetman.ElementString) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Name = reader.ReadString()
				}
			}
		case 3:
			if sheetman.CheckColumn(reader, column, "DropGroupTable.DropName", sheetman.KindScalar, 1, sheetman.ElementString) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.DropName = reader.ReadString()
				}
			}
		case 4:
			if sheetman.CheckColumn(reader, column, "DropGroupTable.DropArea", sheetman.KindScalar, 1, sheetman.ElementString) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.DropArea = reader.ReadString()
				}
			}
		case 5:
			if sheetman.CheckColumn(reader, column, "DropGroupTable.DropItemIds", sheetman.KindVarArray, 0, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					elementCount := int(reader.ReadCounter32())
					r.DropItemIds = make([]int32, elementCount)
					for j := 0; j < elementCount; j++ {
						r.DropItemIds[j] = reader.ReadI32As(column.Element)
					}
				}
			}
		case 6:
			if sheetman.CheckColumn(reader, column, "DropGroupTable.DropItemMin", sheetman.KindVarArray, 0, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					elementCount := int(reader.ReadCounter32())
					r.DropItemMin = make([]int32, elementCount)
					for j := 0; j < elementCount; j++ {
						r.DropItemMin[j] = reader.ReadI32As(column.Element)
					}
				}
			}
		case 7:
			if sheetman.CheckColumn(reader, column, "DropGroupTable.DropItemMax", sheetman.KindVarArray, 0, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					elementCount := int(reader.ReadCounter32())
					r.DropItemMax = make([]int32, elementCount)
					for j := 0; j < elementCount; j++ {
						r.DropItemMax[j] = reader.ReadI32As(column.Element)
					}
				}
			}
		case 8:
			if sheetman.CheckColumn(reader, column, "DropGroupTable.ItemDropRate", sheetman.KindVarArray, 0, sheetman.ElementF32) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					elementCount := int(reader.ReadCounter32())
					r.ItemDropRate = make([]float32, elementCount)
					for j := 0; j < elementCount; j++ {
						r.ItemDropRate[j] = reader.ReadFloat32()
					}
				}
			}
		default:
			// A column added after this code was generated.
			reader.Skip(column.ByteLength)
		}

		sheetman.CheckBlockEnd(reader, column, blockEnd)
	}

	if err := reader.Err(); err != nil {
		return fmt.Errorf("DropGroupTable: %w", err)
	}

	byId := make(map[int32]int, len(records))
	for i := range records {
		byId[records[i].Id] = i
	}

	// Published, now that every column read and the indexes are built.
	t.records = records
	t.byId = byId

	return nil
}
