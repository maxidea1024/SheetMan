// ------------------------------------------------------------------------------
// Code generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package rescue

import (
	"fmt"

	"rescue/sheetman"
)

// EquipTableRecord was generated from project-rescue-xlsxs/ItemData.xlsx : EquipTable : A1.
type EquipTableRecord struct {
	// Equipment_Table
	Id int32
	// 한글명
	Name string
	// 영문명
	EquipName string
	// 타입
	Type JobType
	// 장비 종류
	EquipType EquipmentType
	// 등급
	Grade GradeType
	// 최대 성급
	MaxClass int32
	// 최대 레벨
	MaxLevel int32
	// 스탯1
	StatType1 StatType
	// 스탯2
	StatType2 StatType
	// 아이콘 경로
	IconPath string
	// 프리펩 위치
	PrefabPath string
	// 설명
	Description string
}

// EquipTableTable holds every row of EquipTable.
type EquipTableTable struct {
	records []EquipTableRecord
	byId map[int32]int
}

// Records returns every row, in the order the sheet declared them.
func (t *EquipTableTable) Records() []EquipTableRecord { return t.records }

// FindById returns the row with this Id, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *EquipTableTable) FindById(key int32) *EquipTableRecord {
	position, found := t.byId[key]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByIdOrError returns the row with this Id, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *EquipTableTable) GetByIdOrError(key int32) (*EquipTableRecord, error) {
	position, found := t.byId[key]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `EquipTable` that corresponds to field `Id` value %v", key)
	}

	return &t.records[position], nil
}

// ContainsId reports whether the table holds a row with this Id.
func (t *EquipTableTable) ContainsId(key int32) bool {
	_, found := t.byId[key]
	return found
}

// Read loads the table from a .scb file written by SheetMan.
func (t *EquipTableTable) Read(filename string) error {
	data, err := sheetman.ReadAllBytes(filename)
	if err != nil {
		return err
	}

	reader := sheetman.NewReader(data)
	count, columns := sheetman.ReadTableHeader(reader)

	// Column by column, matched by tag rather than position: a column this build does
	// not know is skipped by its block length, and one whose type changed incompatibly
	// fails naming the field.
	//
	// Read into rows of its own and assigned at the end. Reading a table that is already
	// loaded is a refresh, and a refresh that turns out to be unreadable has to leave what
	// the table holds alone - the previous rows and an error, not an empty table.
	records := make([]EquipTableRecord, count)

	for _, column := range columns {
		blockEnd := reader.Position() + int(column.ByteLength)

		switch column.Tag {
		case 1:
			if sheetman.CheckColumn(reader, column, "EquipTable.Id", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Id = reader.ReadI32As(column.Element)
				}
			}
		case 2:
			if sheetman.CheckColumn(reader, column, "EquipTable.Name", sheetman.KindScalar, 1, sheetman.ElementString) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Name = reader.ReadString()
				}
			}
		case 3:
			if sheetman.CheckColumn(reader, column, "EquipTable.EquipName", sheetman.KindScalar, 1, sheetman.ElementString) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.EquipName = reader.ReadString()
				}
			}
		case 4:
			if sheetman.CheckColumn(reader, column, "EquipTable.Type", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Type = JobType(reader.ReadEnum())
				}
			}
		case 5:
			if sheetman.CheckColumn(reader, column, "EquipTable.EquipType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.EquipType = EquipmentType(reader.ReadEnum())
				}
			}
		case 6:
			if sheetman.CheckColumn(reader, column, "EquipTable.Grade", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Grade = GradeType(reader.ReadEnum())
				}
			}
		case 7:
			if sheetman.CheckColumn(reader, column, "EquipTable.MaxClass", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.MaxClass = reader.ReadI32As(column.Element)
				}
			}
		case 8:
			if sheetman.CheckColumn(reader, column, "EquipTable.MaxLevel", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.MaxLevel = reader.ReadI32As(column.Element)
				}
			}
		case 9:
			if sheetman.CheckColumn(reader, column, "EquipTable.StatType1", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.StatType1 = StatType(reader.ReadEnum())
				}
			}
		case 10:
			if sheetman.CheckColumn(reader, column, "EquipTable.StatType2", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.StatType2 = StatType(reader.ReadEnum())
				}
			}
		case 11:
			if sheetman.CheckColumn(reader, column, "EquipTable.IconPath", sheetman.KindScalar, 1, sheetman.ElementString) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.IconPath = reader.ReadString()
				}
			}
		case 12:
			if sheetman.CheckColumn(reader, column, "EquipTable.PrefabPath", sheetman.KindScalar, 1, sheetman.ElementString) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.PrefabPath = reader.ReadString()
				}
			}
		case 13:
			if sheetman.CheckColumn(reader, column, "EquipTable.Description", sheetman.KindScalar, 1, sheetman.ElementString) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Description = reader.ReadString()
				}
			}
		default:
			// A column added after this code was generated.
			reader.Skip(column.ByteLength)
		}

		sheetman.CheckBlockEnd(reader, column, blockEnd)
	}

	if err := reader.Err(); err != nil {
		return fmt.Errorf("EquipTable: %w", err)
	}

	byId := make(map[int32]int, len(records))
	for i := range records {
		byId[records[i].Id] = i
	}

	// Published, now that every column read and the indexes are built.
	t.records = records
	t.byId = byId

	return nil
}
