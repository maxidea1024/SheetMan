// ------------------------------------------------------------------------------
// Code generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package rescue

import (
	"fmt"

	"rescue/sheetman"
)

// GachaRateRecord was generated from project-rescue-xlsxs/GachaData.xlsx : GachaRateTable : A1.
type GachaRateRecord struct {
	// GachaRate_Table
	Id int32
	// UR 등장 확률
	URRate float32
	// SSR 등장 확률
	SSRRate float32
	// SR 등장 확률
	SRRate float32
	// R 등장 확률
	RRate float32
	// N 등장 확률
	NRate float32
}

// GachaRateTable holds every row of GachaRate.
type GachaRateTable struct {
	records []GachaRateRecord
	byId map[int32]int
}

// Records returns every row, in the order the sheet declared them.
func (t *GachaRateTable) Records() []GachaRateRecord { return t.records }

// FindById returns the row with this Id, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *GachaRateTable) FindById(key int32) *GachaRateRecord {
	position, found := t.byId[key]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByIdOrError returns the row with this Id, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *GachaRateTable) GetByIdOrError(key int32) (*GachaRateRecord, error) {
	position, found := t.byId[key]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `GachaRate` that corresponds to field `Id` value %v", key)
	}

	return &t.records[position], nil
}

// ContainsId reports whether the table holds a row with this Id.
func (t *GachaRateTable) ContainsId(key int32) bool {
	_, found := t.byId[key]
	return found
}

// Read loads the table from a .scb file written by SheetMan.
func (t *GachaRateTable) Read(filename string) error {
	data, err := sheetman.ReadAllBytes(filename)
	if err != nil {
		return err
	}

	reader := sheetman.NewReader(data)
	count, columns := sheetman.ReadTableHeader(reader)

	// Column by column, matched by tag rather than position: a column this build does
	// not know is skipped by its block length, and one whose type changed incompatibly
	// fails naming the field.
	//
	// Read into rows of its own and assigned at the end. Reading a table that is already
	// loaded is a refresh, and a refresh that turns out to be unreadable has to leave what
	// the table holds alone - the previous rows and an error, not an empty table.
	records := make([]GachaRateRecord, count)

	for _, column := range columns {
		blockEnd := reader.Position() + int(column.ByteLength)

		switch column.Tag {
		case 1:
			if sheetman.CheckColumn(reader, column, "GachaRate.Id", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "GachaRate.Id")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Id = cursor.NextI32()
				}
			}
		case 2:
			if sheetman.CheckColumn(reader, column, "GachaRate.URRate", sheetman.KindScalar, 1, sheetman.ElementF32) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "GachaRate.URRate")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.URRate = cursor.NextF32()
				}
			}
		case 3:
			if sheetman.CheckColumn(reader, column, "GachaRate.SSRRate", sheetman.KindScalar, 1, sheetman.ElementF32) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "GachaRate.SSRRate")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.SSRRate = cursor.NextF32()
				}
			}
		case 4:
			if sheetman.CheckColumn(reader, column, "GachaRate.SRRate", sheetman.KindScalar, 1, sheetman.ElementF32) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "GachaRate.SRRate")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.SRRate = cursor.NextF32()
				}
			}
		case 5:
			if sheetman.CheckColumn(reader, column, "GachaRate.RRate", sheetman.KindScalar, 1, sheetman.ElementF32) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "GachaRate.RRate")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.RRate = cursor.NextF32()
				}
			}
		case 6:
			if sheetman.CheckColumn(reader, column, "GachaRate.NRate", sheetman.KindScalar, 1, sheetman.ElementF32) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "GachaRate.NRate")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.NRate = cursor.NextF32()
				}
			}
		default:
			// A column added after this code was generated.
			reader.Skip(column.ByteLength)
		}

		sheetman.CheckBlockEnd(reader, column, blockEnd)
	}

	if err := reader.Err(); err != nil {
		return fmt.Errorf("GachaRate: %w", err)
	}

	byId := make(map[int32]int, len(records))
	for i := range records {
		byId[records[i].Id] = i
	}

	// Published, now that every column read and the indexes are built.
	t.records = records
	t.byId = byId

	return nil
}
