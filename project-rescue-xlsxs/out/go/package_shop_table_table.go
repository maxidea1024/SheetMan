// ------------------------------------------------------------------------------
// Code generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package rescue

import (
	"fmt"

	"rescue/sheetman"
)

// PackageShopTableRecord was generated from project-rescue-xlsxs/ShopData.xlsx : PackageShopTable : A1.
type PackageShopTableRecord struct {
	// PackageShop_Table
	Id int32
	// 영문명
	Name string
	// 한글명
	ProductName string
	// 상점 타입
	ShopType ShopType
	// 슬롯 ID
	ShopSlotID int32
	// 출력 우선순위
	Priority int32
	// 포함 아이템 ID
	ItemID []int32
	// 포함 아이템 수
	ItemValue []int32
	// 구매 주기 타입
	CycleType CycleType
	// 구매 제한 횟수
	LimitValue int32
	// 소모 재화
	CurrencyType CurrencyType
	// 가격값
	PriceValue int32
	// 해금 조건 ID
	ConditionID int32
	// 아이콘 경로
	IconPath string
}

// PackageShopTableTable holds every row of PackageShopTable.
type PackageShopTableTable struct {
	records []PackageShopTableRecord
	byId map[int32]int
}

// Records returns every row, in the order the sheet declared them.
func (t *PackageShopTableTable) Records() []PackageShopTableRecord { return t.records }

// FindById returns the row with this Id, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *PackageShopTableTable) FindById(key int32) *PackageShopTableRecord {
	position, found := t.byId[key]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByIdOrError returns the row with this Id, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *PackageShopTableTable) GetByIdOrError(key int32) (*PackageShopTableRecord, error) {
	position, found := t.byId[key]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `PackageShopTable` that corresponds to field `Id` value %v", key)
	}

	return &t.records[position], nil
}

// ContainsId reports whether the table holds a row with this Id.
func (t *PackageShopTableTable) ContainsId(key int32) bool {
	_, found := t.byId[key]
	return found
}

// Read loads the table from a .table file written by SheetMan.
func (t *PackageShopTableTable) Read(filename string) error {
	data, err := sheetman.ReadAllBytes(filename)
	if err != nil {
		return err
	}

	reader := sheetman.NewReader(data)
	count, columns := sheetman.ReadTableHeader(reader)

	// Column by column, matched by tag rather than position: a column this build does
	// not know is skipped by its block length, and one whose type changed incompatibly
	// fails naming the field.
	//
	// Read into rows of its own and assigned at the end. Reading a table that is already
	// loaded is a refresh, and a refresh that turns out to be unreadable has to leave what
	// the table holds alone - the previous rows and an error, not an empty table.
	records := make([]PackageShopTableRecord, count)

	for _, column := range columns {
		blockEnd := reader.Position() + int(column.ByteLength)

		switch column.Tag {
		case 1:
			if sheetman.CheckColumn(reader, column, "PackageShopTable.Id", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Id = reader.ReadI32As(column.Element)
				}
			}
		case 2:
			if sheetman.CheckColumn(reader, column, "PackageShopTable.Name", sheetman.KindScalar, 1, sheetman.ElementString) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Name = reader.ReadString()
				}
			}
		case 3:
			if sheetman.CheckColumn(reader, column, "PackageShopTable.ProductName", sheetman.KindScalar, 1, sheetman.ElementString) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.ProductName = reader.ReadString()
				}
			}
		case 4:
			if sheetman.CheckColumn(reader, column, "PackageShopTable.ShopType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.ShopType = ShopType(reader.ReadEnum())
				}
			}
		case 5:
			if sheetman.CheckColumn(reader, column, "PackageShopTable.ShopSlotID", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.ShopSlotID = reader.ReadI32As(column.Element)
				}
			}
		case 6:
			if sheetman.CheckColumn(reader, column, "PackageShopTable.Priority", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Priority = reader.ReadI32As(column.Element)
				}
			}
		case 7:
			if sheetman.CheckColumn(reader, column, "PackageShopTable.ItemID", sheetman.KindVarArray, 0, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					elementCount := int(reader.ReadCounter32())
					r.ItemID = make([]int32, elementCount)
					for j := 0; j < elementCount; j++ {
						r.ItemID[j] = reader.ReadI32As(column.Element)
					}
				}
			}
		case 8:
			if sheetman.CheckColumn(reader, column, "PackageShopTable.ItemValue", sheetman.KindVarArray, 0, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					elementCount := int(reader.ReadCounter32())
					r.ItemValue = make([]int32, elementCount)
					for j := 0; j < elementCount; j++ {
						r.ItemValue[j] = reader.ReadI32As(column.Element)
					}
				}
			}
		case 9:
			if sheetman.CheckColumn(reader, column, "PackageShopTable.CycleType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.CycleType = CycleType(reader.ReadEnum())
				}
			}
		case 10:
			if sheetman.CheckColumn(reader, column, "PackageShopTable.LimitValue", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.LimitValue = reader.ReadI32As(column.Element)
				}
			}
		case 11:
			if sheetman.CheckColumn(reader, column, "PackageShopTable.CurrencyType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.CurrencyType = CurrencyType(reader.ReadEnum())
				}
			}
		case 12:
			if sheetman.CheckColumn(reader, column, "PackageShopTable.PriceValue", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.PriceValue = reader.ReadI32As(column.Element)
				}
			}
		case 13:
			if sheetman.CheckColumn(reader, column, "PackageShopTable.ConditionID", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.ConditionID = reader.ReadI32As(column.Element)
				}
			}
		case 14:
			if sheetman.CheckColumn(reader, column, "PackageShopTable.IconPath", sheetman.KindScalar, 1, sheetman.ElementString) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.IconPath = reader.ReadString()
				}
			}
		default:
			// A column added after this code was generated.
			reader.Skip(column.ByteLength)
		}

		sheetman.CheckBlockEnd(reader, column, blockEnd)
	}

	if err := reader.Err(); err != nil {
		return fmt.Errorf("PackageShopTable: %w", err)
	}

	byId := make(map[int32]int, len(records))
	for i := range records {
		byId[records[i].Id] = i
	}

	// Published, now that every column read and the indexes are built.
	t.records = records
	t.byId = byId

	return nil
}
