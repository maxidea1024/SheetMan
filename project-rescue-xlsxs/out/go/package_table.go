// ------------------------------------------------------------------------------
// Code generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package rescue

import (
	"fmt"

	"rescue/sheetman"
)

// PackageRecord was generated from project-rescue-xlsxs/ItemData.xlsx : PackageTable : A1.
type PackageRecord struct {
	// PackcgeItem_Table
	Id int32
	// 영문
	Name string
	// 한글
	ClassName string
	// 카테고리
	ItemType ItemType
	// 패키지 타입
	PackageType ConsumablesType
	// 중첩 가능 유무
	Stackable bool
	// 최대 소유 값
	MaxStack int64
	// 획득조건 타입
	PackageCondition ConditionType
	// 아이템 재사용 대기시간
	Cooltime int32
	// 아이템 유지 시간
	Duration int32
	// 포함 아이템 종류
	CurrencyID []int32
	// 포함 아이템 최소 수량 값
	CurrencyItemMin []int32
	// 포함 아이템 최대 수량 값
	CurrencyItemMax []int32
	// 포함 아이템 확률
	CurrencyItemRate []int32
	// 아이콘 경로
	IconPath string
	// 프리펩 위치
	DropPrefabPath string
	// 아이템 설명
	ItemDescription string
}

// PackageTable holds every row of Package.
type PackageTable struct {
	records []PackageRecord
	byId map[int32]int
}

// Records returns every row, in the order the sheet declared them.
func (t *PackageTable) Records() []PackageRecord { return t.records }

// FindById returns the row with this Id, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *PackageTable) FindById(key int32) *PackageRecord {
	position, found := t.byId[key]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByIdOrError returns the row with this Id, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *PackageTable) GetByIdOrError(key int32) (*PackageRecord, error) {
	position, found := t.byId[key]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `Package` that corresponds to field `Id` value %v", key)
	}

	return &t.records[position], nil
}

// ContainsId reports whether the table holds a row with this Id.
func (t *PackageTable) ContainsId(key int32) bool {
	_, found := t.byId[key]
	return found
}

// Read loads the table from a .scb file written by SheetMan.
func (t *PackageTable) Read(filename string) error {
	data, err := sheetman.ReadAllBytes(filename)
	if err != nil {
		return err
	}

	reader := sheetman.NewReader(data)
	count, columns := sheetman.ReadTableHeader(reader)

	// Column by column, matched by tag rather than position: a column this build does
	// not know is skipped by its block length, and one whose type changed incompatibly
	// fails naming the field.
	//
	// Read into rows of its own and assigned at the end. Reading a table that is already
	// loaded is a refresh, and a refresh that turns out to be unreadable has to leave what
	// the table holds alone - the previous rows and an error, not an empty table.
	records := make([]PackageRecord, count)

	for _, column := range columns {
		blockEnd := reader.Position() + int(column.ByteLength)

		switch column.Tag {
		case 1:
			if sheetman.CheckColumn(reader, column, "Package.Id", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Package.Id")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Id = cursor.NextI32()
				}
			}
		case 2:
			if sheetman.CheckColumn(reader, column, "Package.Name", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Package.Name")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Name = cursor.NextString()
				}
			}
		case 3:
			if sheetman.CheckColumn(reader, column, "Package.ClassName", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Package.ClassName")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.ClassName = cursor.NextString()
				}
			}
		case 4:
			if sheetman.CheckColumn(reader, column, "Package.ItemType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Package.ItemType")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.ItemType = ItemType(cursor.NextI32())
				}
			}
		case 5:
			if sheetman.CheckColumn(reader, column, "Package.PackageType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Package.PackageType")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.PackageType = ConsumablesType(cursor.NextI32())
				}
			}
		case 6:
			if sheetman.CheckColumn(reader, column, "Package.Stackable", sheetman.KindScalar, 1, sheetman.ElementBool) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Package.Stackable")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Stackable = cursor.NextBool()
				}
			}
		case 7:
			if sheetman.CheckColumn(reader, column, "Package.MaxStack", sheetman.KindScalar, 1, sheetman.ElementI64, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Package.MaxStack")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.MaxStack = cursor.NextI64()
				}
			}
		case 8:
			if sheetman.CheckColumn(reader, column, "Package.PackageCondition", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Package.PackageCondition")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.PackageCondition = ConditionType(cursor.NextI32())
				}
			}
		case 9:
			if sheetman.CheckColumn(reader, column, "Package.Cooltime", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Package.Cooltime")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Cooltime = cursor.NextI32()
				}
			}
		case 10:
			if sheetman.CheckColumn(reader, column, "Package.Duration", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Package.Duration")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Duration = cursor.NextI32()
				}
			}
		case 11:
			if sheetman.CheckColumn(reader, column, "Package.CurrencyID", sheetman.KindVarArray, 0, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					elementCount := int(reader.ReadCounter32())
					r.CurrencyID = make([]int32, elementCount)
					for j := 0; j < elementCount; j++ {
						r.CurrencyID[j] = reader.ReadI32As(column.Element)
					}
				}
			}
		case 12:
			if sheetman.CheckColumn(reader, column, "Package.CurrencyItemMin", sheetman.KindVarArray, 0, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					elementCount := int(reader.ReadCounter32())
					r.CurrencyItemMin = make([]int32, elementCount)
					for j := 0; j < elementCount; j++ {
						r.CurrencyItemMin[j] = reader.ReadI32As(column.Element)
					}
				}
			}
		case 13:
			if sheetman.CheckColumn(reader, column, "Package.CurrencyItemMax", sheetman.KindVarArray, 0, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					elementCount := int(reader.ReadCounter32())
					r.CurrencyItemMax = make([]int32, elementCount)
					for j := 0; j < elementCount; j++ {
						r.CurrencyItemMax[j] = reader.ReadI32As(column.Element)
					}
				}
			}
		case 14:
			if sheetman.CheckColumn(reader, column, "Package.CurrencyItemRate", sheetman.KindVarArray, 0, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					elementCount := int(reader.ReadCounter32())
					r.CurrencyItemRate = make([]int32, elementCount)
					for j := 0; j < elementCount; j++ {
						r.CurrencyItemRate[j] = reader.ReadI32As(column.Element)
					}
				}
			}
		case 15:
			if sheetman.CheckColumn(reader, column, "Package.IconPath", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Package.IconPath")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.IconPath = cursor.NextString()
				}
			}
		case 16:
			if sheetman.CheckColumn(reader, column, "Package.DropPrefabPath", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Package.DropPrefabPath")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.DropPrefabPath = cursor.NextString()
				}
			}
		case 17:
			if sheetman.CheckColumn(reader, column, "Package.ItemDescription", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Package.ItemDescription")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.ItemDescription = cursor.NextString()
				}
			}
		default:
			// A column added after this code was generated.
			reader.Skip(column.ByteLength)
		}

		sheetman.CheckBlockEnd(reader, column, blockEnd)
	}

	if err := reader.Err(); err != nil {
		return fmt.Errorf("Package: %w", err)
	}

	byId := make(map[int32]int, len(records))
	for i := range records {
		byId[records[i].Id] = i
	}

	// Published, now that every column read and the indexes are built.
	t.records = records
	t.byId = byId

	return nil
}
