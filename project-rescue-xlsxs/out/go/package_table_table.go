// ------------------------------------------------------------------------------
// Code generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package rescue

import (
	"fmt"

	"rescue/sheetman"
)

// PackageTableRecord was generated from project-rescue-xlsxs\ItemData.xlsx : PackageTable : A1.
type PackageTableRecord struct {
	// PackcgeItem_Table
	Id int32
	// 영문
	Name string
	// 한글
	ClassName string
	// 카테고리
	ItemType ItemType
	// 패키지 타입
	PackageType ConsumablesType
	// 중첩 가능 유무
	Stackable bool
	// 최대 소유 값
	MaxStack int64
	// 획득조건 타입
	PackageCondition ConditionType
	// 아이템 재사용 대기시간
	Cooltime int32
	// 아이템 유지 시간
	Duration int32
	// 포함 아이템 종류
	CurrencyID []int32
	// 포함 아이템 최소 수량 값
	CurrencyItemMin []int32
	// 포함 아이템 최대 수량 값
	CurrencyItemMax []int32
	// 포함 아이템 확률
	CurrencyItemRate []int32
	// 아이콘 경로
	IconPath string
	// 프리펩 위치
	DropPrefabPath string
	// 아이템 설명
	ItemDescription string
}

// PackageTableTable holds every row of PackageTable.
type PackageTableTable struct {
	records []PackageTableRecord
	byId map[int32]int
}

// Records returns every row, in the order the sheet declared them.
func (t *PackageTableTable) Records() []PackageTableRecord { return t.records }

// FindById returns the row with this Id, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *PackageTableTable) FindById(key int32) *PackageTableRecord {
	position, found := t.byId[key]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByIdOrError returns the row with this Id, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *PackageTableTable) GetByIdOrError(key int32) (*PackageTableRecord, error) {
	position, found := t.byId[key]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `PackageTable` that corresponds to field `Id` value %v", key)
	}

	return &t.records[position], nil
}

// ContainsId reports whether the table holds a row with this Id.
func (t *PackageTableTable) ContainsId(key int32) bool {
	_, found := t.byId[key]
	return found
}

// Read loads the table from a .table file written by SheetMan.
func (t *PackageTableTable) Read(filename string) error {
	data, err := sheetman.ReadAllBytes(filename)
	if err != nil {
		return err
	}

	reader := sheetman.NewReader(data)
	count, columns := sheetman.ReadTableHeader(reader)

	// Column by column, matched by tag rather than position: a column this build does
	// not know is skipped by its block length, and one whose type changed incompatibly
	// fails naming the field.
	//
	// Read into rows of its own and assigned at the end. Reading a table that is already
	// loaded is a refresh, and a refresh that turns out to be unreadable has to leave what
	// the table holds alone - the previous rows and an error, not an empty table.
	records := make([]PackageTableRecord, count)

	for _, column := range columns {
		blockEnd := reader.Position() + int(column.ByteLength)

		switch column.Tag {
		case 1:
			if sheetman.CheckColumn(reader, column, "PackageTable.Id", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Id = reader.ReadI32As(column.Element)
				}
			}
		case 2:
			if sheetman.CheckColumn(reader, column, "PackageTable.Name", sheetman.KindScalar, 1, sheetman.ElementString) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Name = reader.ReadString()
				}
			}
		case 3:
			if sheetman.CheckColumn(reader, column, "PackageTable.ClassName", sheetman.KindScalar, 1, sheetman.ElementString) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.ClassName = reader.ReadString()
				}
			}
		case 4:
			if sheetman.CheckColumn(reader, column, "PackageTable.ItemType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.ItemType = ItemType(reader.ReadEnum())
				}
			}
		case 5:
			if sheetman.CheckColumn(reader, column, "PackageTable.PackageType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.PackageType = ConsumablesType(reader.ReadEnum())
				}
			}
		case 6:
			if sheetman.CheckColumn(reader, column, "PackageTable.Stackable", sheetman.KindScalar, 1, sheetman.ElementBool) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Stackable = reader.ReadBool()
				}
			}
		case 7:
			if sheetman.CheckColumn(reader, column, "PackageTable.MaxStack", sheetman.KindScalar, 1, sheetman.ElementI64, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.MaxStack = reader.ReadI64As(column.Element)
				}
			}
		case 8:
			if sheetman.CheckColumn(reader, column, "PackageTable.PackageCondition", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.PackageCondition = ConditionType(reader.ReadEnum())
				}
			}
		case 9:
			if sheetman.CheckColumn(reader, column, "PackageTable.Cooltime", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Cooltime = reader.ReadI32As(column.Element)
				}
			}
		case 10:
			if sheetman.CheckColumn(reader, column, "PackageTable.Duration", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Duration = reader.ReadI32As(column.Element)
				}
			}
		case 11:
			if sheetman.CheckColumn(reader, column, "PackageTable.CurrencyID", sheetman.KindVarArray, 0, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					elementCount := int(reader.ReadCounter32())
					r.CurrencyID = make([]int32, elementCount)
					for j := 0; j < elementCount; j++ {
						r.CurrencyID[j] = reader.ReadI32As(column.Element)
					}
				}
			}
		case 12:
			if sheetman.CheckColumn(reader, column, "PackageTable.CurrencyItemMin", sheetman.KindVarArray, 0, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					elementCount := int(reader.ReadCounter32())
					r.CurrencyItemMin = make([]int32, elementCount)
					for j := 0; j < elementCount; j++ {
						r.CurrencyItemMin[j] = reader.ReadI32As(column.Element)
					}
				}
			}
		case 13:
			if sheetman.CheckColumn(reader, column, "PackageTable.CurrencyItemMax", sheetman.KindVarArray, 0, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					elementCount := int(reader.ReadCounter32())
					r.CurrencyItemMax = make([]int32, elementCount)
					for j := 0; j < elementCount; j++ {
						r.CurrencyItemMax[j] = reader.ReadI32As(column.Element)
					}
				}
			}
		case 14:
			if sheetman.CheckColumn(reader, column, "PackageTable.CurrencyItemRate", sheetman.KindVarArray, 0, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					elementCount := int(reader.ReadCounter32())
					r.CurrencyItemRate = make([]int32, elementCount)
					for j := 0; j < elementCount; j++ {
						r.CurrencyItemRate[j] = reader.ReadI32As(column.Element)
					}
				}
			}
		case 15:
			if sheetman.CheckColumn(reader, column, "PackageTable.IconPath", sheetman.KindScalar, 1, sheetman.ElementString) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.IconPath = reader.ReadString()
				}
			}
		case 16:
			if sheetman.CheckColumn(reader, column, "PackageTable.DropPrefabPath", sheetman.KindScalar, 1, sheetman.ElementString) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.DropPrefabPath = reader.ReadString()
				}
			}
		case 17:
			if sheetman.CheckColumn(reader, column, "PackageTable.ItemDescription", sheetman.KindScalar, 1, sheetman.ElementString) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.ItemDescription = reader.ReadString()
				}
			}
		default:
			// A column added after this code was generated.
			reader.Skip(column.ByteLength)
		}

		sheetman.CheckBlockEnd(reader, column, blockEnd)
	}

	if err := reader.Err(); err != nil {
		return fmt.Errorf("PackageTable: %w", err)
	}

	byId := make(map[int32]int, len(records))
	for i := range records {
		byId[records[i].Id] = i
	}

	// Published, now that every column read and the indexes are built.
	t.records = records
	t.byId = byId

	return nil
}
