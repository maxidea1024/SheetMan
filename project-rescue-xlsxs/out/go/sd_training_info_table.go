// ------------------------------------------------------------------------------
// Code generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package rescue

import (
	"fmt"

	"rescue/sheetman"
)

// SDTrainingInfoRecord was generated from project-rescue-xlsxs/SD_Contens.xlsx : SDTrainingInfoTable : A1.
type SDTrainingInfoRecord struct {
	// SDTrainingInfo_Table
	Id int32
	// 영문명
	Name string
	// 한글명
	TrainingName string
	// 적용 속성 타입
	AttributeType AttributeType
	// 스탯 타입
	StatType StatType
	// 해제 조건 대상
	UnlockCondition int32
	// 레벨업 제한 타입
	LevelUpCondition int32
	// 최대 레벨
	MaxLevel int32
	// 레벨 초기화 여부
	IsLvReset bool
	// 레벨 초기화 재화
	LvResetCurrencyType CurrencyType
	// 레벨 초기화 재화값
	LvResetCurrencyValue int32
	// 설명
	Description string
}

// SDTrainingInfoTable holds every row of SDTrainingInfo.
type SDTrainingInfoTable struct {
	records []SDTrainingInfoRecord
	byId map[int32]int
}

// Records returns every row, in the order the sheet declared them.
func (t *SDTrainingInfoTable) Records() []SDTrainingInfoRecord { return t.records }

// FindById returns the row with this Id, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *SDTrainingInfoTable) FindById(key int32) *SDTrainingInfoRecord {
	position, found := t.byId[key]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByIdOrError returns the row with this Id, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *SDTrainingInfoTable) GetByIdOrError(key int32) (*SDTrainingInfoRecord, error) {
	position, found := t.byId[key]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `SDTrainingInfo` that corresponds to field `Id` value %v", key)
	}

	return &t.records[position], nil
}

// ContainsId reports whether the table holds a row with this Id.
func (t *SDTrainingInfoTable) ContainsId(key int32) bool {
	_, found := t.byId[key]
	return found
}

// Read loads the table from a .scb file written by SheetMan.
func (t *SDTrainingInfoTable) Read(filename string) error {
	data, err := sheetman.ReadAllBytes(filename)
	if err != nil {
		return err
	}

	reader := sheetman.NewReader(data)
	count, columns := sheetman.ReadTableHeader(reader)

	// Column by column, matched by tag rather than position: a column this build does
	// not know is skipped by its block length, and one whose type changed incompatibly
	// fails naming the field.
	//
	// Read into rows of its own and assigned at the end. Reading a table that is already
	// loaded is a refresh, and a refresh that turns out to be unreadable has to leave what
	// the table holds alone - the previous rows and an error, not an empty table.
	records := make([]SDTrainingInfoRecord, count)

	for _, column := range columns {
		blockEnd := reader.Position() + int(column.ByteLength)

		switch column.Tag {
		case 1:
			if sheetman.CheckColumn(reader, column, "SDTrainingInfo.Id", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "SDTrainingInfo.Id")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Id = cursor.NextI32()
				}
			}
		case 2:
			if sheetman.CheckColumn(reader, column, "SDTrainingInfo.Name", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "SDTrainingInfo.Name")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Name = cursor.NextString()
				}
			}
		case 3:
			if sheetman.CheckColumn(reader, column, "SDTrainingInfo.TrainingName", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "SDTrainingInfo.TrainingName")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.TrainingName = cursor.NextString()
				}
			}
		case 4:
			if sheetman.CheckColumn(reader, column, "SDTrainingInfo.AttributeType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "SDTrainingInfo.AttributeType")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.AttributeType = AttributeType(cursor.NextI32())
				}
			}
		case 5:
			if sheetman.CheckColumn(reader, column, "SDTrainingInfo.StatType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "SDTrainingInfo.StatType")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.StatType = StatType(cursor.NextI32())
				}
			}
		case 6:
			if sheetman.CheckColumn(reader, column, "SDTrainingInfo.UnlockCondition", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "SDTrainingInfo.UnlockCondition")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.UnlockCondition = cursor.NextI32()
				}
			}
		case 7:
			if sheetman.CheckColumn(reader, column, "SDTrainingInfo.LevelUpCondition", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "SDTrainingInfo.LevelUpCondition")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.LevelUpCondition = cursor.NextI32()
				}
			}
		case 8:
			if sheetman.CheckColumn(reader, column, "SDTrainingInfo.MaxLevel", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "SDTrainingInfo.MaxLevel")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.MaxLevel = cursor.NextI32()
				}
			}
		case 9:
			if sheetman.CheckColumn(reader, column, "SDTrainingInfo.IsLvReset", sheetman.KindScalar, 1, sheetman.ElementBool) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "SDTrainingInfo.IsLvReset")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.IsLvReset = cursor.NextBool()
				}
			}
		case 10:
			if sheetman.CheckColumn(reader, column, "SDTrainingInfo.LvResetCurrencyType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "SDTrainingInfo.LvResetCurrencyType")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.LvResetCurrencyType = CurrencyType(cursor.NextI32())
				}
			}
		case 11:
			if sheetman.CheckColumn(reader, column, "SDTrainingInfo.LvResetCurrencyValue", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "SDTrainingInfo.LvResetCurrencyValue")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.LvResetCurrencyValue = cursor.NextI32()
				}
			}
		case 12:
			if sheetman.CheckColumn(reader, column, "SDTrainingInfo.Description", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "SDTrainingInfo.Description")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Description = cursor.NextString()
				}
			}
		default:
			// A column added after this code was generated.
			reader.Skip(column.ByteLength)
		}

		sheetman.CheckBlockEnd(reader, column, blockEnd)
	}

	if err := reader.Err(); err != nil {
		return fmt.Errorf("SDTrainingInfo: %w", err)
	}

	byId := make(map[int32]int, len(records))
	for i := range records {
		byId[records[i].Id] = i
	}

	// Published, now that every column read and the indexes are built.
	t.records = records
	t.byId = byId

	return nil
}
