// ------------------------------------------------------------------------------
// Code generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package rescue

import (
	"fmt"

	"rescue/sheetman"
)

// SkillTableRecord was generated from project-rescue-xlsxs\MasterData.xlsx : SkillTable : A1.
type SkillTableRecord struct {
	// Skill_Table
	Id int32
	// 영문
	Name string
	// 한글
	SkillName string
	// 메모
	SkillType SkillType
	// 메모 참조
	SkillSubType SkillSubType
	// 속성 설정
	AttributeType AttributeType
	// 메모참조
	TargetType TargetType
	// 스킬 경로
	AniPath string
	// 스킬 사용 거리
	SkillUseRange float32
	// 스킬 쿨타임
	CoolTime float32
	// 버프 ID
	BuffID []int32
	// 필살기 아이콘 경로
	SkillIcon string
	// 설명
	Description string
}

// SkillTableTable holds every row of SkillTable.
type SkillTableTable struct {
	records []SkillTableRecord
	byId map[int32]int
}

// Records returns every row, in the order the sheet declared them.
func (t *SkillTableTable) Records() []SkillTableRecord { return t.records }

// FindById returns the row with this Id, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *SkillTableTable) FindById(key int32) *SkillTableRecord {
	position, found := t.byId[key]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByIdOrError returns the row with this Id, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *SkillTableTable) GetByIdOrError(key int32) (*SkillTableRecord, error) {
	position, found := t.byId[key]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `SkillTable` that corresponds to field `Id` value %v", key)
	}

	return &t.records[position], nil
}

// ContainsId reports whether the table holds a row with this Id.
func (t *SkillTableTable) ContainsId(key int32) bool {
	_, found := t.byId[key]
	return found
}

// Read loads the table from a .table file written by SheetMan.
func (t *SkillTableTable) Read(filename string) error {
	data, err := sheetman.ReadAllBytes(filename)
	if err != nil {
		return err
	}

	reader := sheetman.NewReader(data)
	count, columns := sheetman.ReadTableHeader(reader)

	// Column by column, matched by tag rather than position: a column this build does
	// not know is skipped by its block length, and one whose type changed incompatibly
	// fails naming the field.
	//
	// Read into rows of its own and assigned at the end. Reading a table that is already
	// loaded is a refresh, and a refresh that turns out to be unreadable has to leave what
	// the table holds alone - the previous rows and an error, not an empty table.
	records := make([]SkillTableRecord, count)

	for _, column := range columns {
		blockEnd := reader.Position() + int(column.ByteLength)

		switch column.Tag {
		case 1:
			if sheetman.CheckColumn(reader, column, "SkillTable.Id", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Id = reader.ReadI32As(column.Element)
				}
			}
		case 2:
			if sheetman.CheckColumn(reader, column, "SkillTable.Name", sheetman.KindScalar, 1, sheetman.ElementString) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Name = reader.ReadString()
				}
			}
		case 3:
			if sheetman.CheckColumn(reader, column, "SkillTable.SkillName", sheetman.KindScalar, 1, sheetman.ElementString) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.SkillName = reader.ReadString()
				}
			}
		case 4:
			if sheetman.CheckColumn(reader, column, "SkillTable.SkillType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.SkillType = SkillType(reader.ReadEnum())
				}
			}
		case 5:
			if sheetman.CheckColumn(reader, column, "SkillTable.SkillSubType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.SkillSubType = SkillSubType(reader.ReadEnum())
				}
			}
		case 6:
			if sheetman.CheckColumn(reader, column, "SkillTable.AttributeType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.AttributeType = AttributeType(reader.ReadEnum())
				}
			}
		case 7:
			if sheetman.CheckColumn(reader, column, "SkillTable.TargetType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.TargetType = TargetType(reader.ReadEnum())
				}
			}
		case 8:
			if sheetman.CheckColumn(reader, column, "SkillTable.AniPath", sheetman.KindScalar, 1, sheetman.ElementString) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.AniPath = reader.ReadString()
				}
			}
		case 9:
			if sheetman.CheckColumn(reader, column, "SkillTable.SkillUseRange", sheetman.KindScalar, 1, sheetman.ElementF32) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.SkillUseRange = reader.ReadFloat32()
				}
			}
		case 10:
			if sheetman.CheckColumn(reader, column, "SkillTable.CoolTime", sheetman.KindScalar, 1, sheetman.ElementF32) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.CoolTime = reader.ReadFloat32()
				}
			}
		case 11:
			if sheetman.CheckColumn(reader, column, "SkillTable.BuffID", sheetman.KindVarArray, 0, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					elementCount := int(reader.ReadCounter32())
					r.BuffID = make([]int32, elementCount)
					for j := 0; j < elementCount; j++ {
						r.BuffID[j] = reader.ReadI32As(column.Element)
					}
				}
			}
		case 12:
			if sheetman.CheckColumn(reader, column, "SkillTable.SkillIcon", sheetman.KindScalar, 1, sheetman.ElementString) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.SkillIcon = reader.ReadString()
				}
			}
		case 13:
			if sheetman.CheckColumn(reader, column, "SkillTable.Description", sheetman.KindScalar, 1, sheetman.ElementString) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Description = reader.ReadString()
				}
			}
		default:
			// A column added after this code was generated.
			reader.Skip(column.ByteLength)
		}

		sheetman.CheckBlockEnd(reader, column, blockEnd)
	}

	if err := reader.Err(); err != nil {
		return fmt.Errorf("SkillTable: %w", err)
	}

	byId := make(map[int32]int, len(records))
	for i := range records {
		byId[records[i].Id] = i
	}

	// Published, now that every column read and the indexes are built.
	t.records = records
	t.byId = byId

	return nil
}
