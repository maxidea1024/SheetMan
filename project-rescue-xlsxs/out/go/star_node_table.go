// ------------------------------------------------------------------------------
// Code generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package rescue

import (
	"fmt"

	"rescue/sheetman"
)

// StarNodeRecord was generated from project-rescue-xlsxs/StageGrowth.xlsx : StarNodeTable : A1.
type StarNodeRecord struct {
	// 기본값
	Id int32
	// 영문
	Name string
	// 영향력 단계
	InfuluenceStep int32
	// 한글
	StarNodeName string
	// 스탯 타입
	StatType StatType
	// 증가 Value
	StarNodeValue int32
	// 증가 Rate
	StarNodeRate float32
	// 해제 조건
	UnlockCondition []int32
	// 설명
	Description string
	// 아이콘
	IconPath string
}

// StarNodeTable holds every row of StarNode.
type StarNodeTable struct {
	records []StarNodeRecord
	byId map[int32]int
}

// Records returns every row, in the order the sheet declared them.
func (t *StarNodeTable) Records() []StarNodeRecord { return t.records }

// FindById returns the row with this Id, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *StarNodeTable) FindById(key int32) *StarNodeRecord {
	position, found := t.byId[key]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByIdOrError returns the row with this Id, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *StarNodeTable) GetByIdOrError(key int32) (*StarNodeRecord, error) {
	position, found := t.byId[key]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `StarNode` that corresponds to field `Id` value %v", key)
	}

	return &t.records[position], nil
}

// ContainsId reports whether the table holds a row with this Id.
func (t *StarNodeTable) ContainsId(key int32) bool {
	_, found := t.byId[key]
	return found
}

// Read loads the table from a .scb file written by SheetMan.
func (t *StarNodeTable) Read(filename string) error {
	data, err := sheetman.ReadAllBytes(filename)
	if err != nil {
		return err
	}

	reader := sheetman.NewReader(data)
	count, columns := sheetman.ReadTableHeader(reader)

	// Column by column, matched by tag rather than position: a column this build does
	// not know is skipped by its block length, and one whose type changed incompatibly
	// fails naming the field.
	//
	// Read into rows of its own and assigned at the end. Reading a table that is already
	// loaded is a refresh, and a refresh that turns out to be unreadable has to leave what
	// the table holds alone - the previous rows and an error, not an empty table.
	records := make([]StarNodeRecord, count)

	for _, column := range columns {
		blockEnd := reader.Position() + int(column.ByteLength)

		switch column.Tag {
		case 1:
			if sheetman.CheckColumn(reader, column, "StarNode.Id", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "StarNode.Id")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Id = cursor.NextI32()
				}
			}
		case 2:
			if sheetman.CheckColumn(reader, column, "StarNode.Name", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "StarNode.Name")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Name = cursor.NextString()
				}
			}
		case 3:
			if sheetman.CheckColumn(reader, column, "StarNode.InfuluenceStep", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "StarNode.InfuluenceStep")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.InfuluenceStep = cursor.NextI32()
				}
			}
		case 4:
			if sheetman.CheckColumn(reader, column, "StarNode.StarNodeName", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "StarNode.StarNodeName")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.StarNodeName = cursor.NextString()
				}
			}
		case 5:
			if sheetman.CheckColumn(reader, column, "StarNode.StatType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "StarNode.StatType")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.StatType = StatType(cursor.NextI32())
				}
			}
		case 6:
			if sheetman.CheckColumn(reader, column, "StarNode.StarNodeValue", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "StarNode.StarNodeValue")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.StarNodeValue = cursor.NextI32()
				}
			}
		case 7:
			if sheetman.CheckColumn(reader, column, "StarNode.StarNodeRate", sheetman.KindScalar, 1, sheetman.ElementF32) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.StarNodeRate = reader.ReadFloat32()
				}
			}
		case 8:
			if sheetman.CheckColumn(reader, column, "StarNode.UnlockCondition", sheetman.KindVarArray, 0, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					elementCount := int(reader.ReadCounter32())
					r.UnlockCondition = make([]int32, elementCount)
					for j := 0; j < elementCount; j++ {
						r.UnlockCondition[j] = reader.ReadI32As(column.Element)
					}
				}
			}
		case 9:
			if sheetman.CheckColumn(reader, column, "StarNode.Description", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "StarNode.Description")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Description = cursor.NextString()
				}
			}
		case 10:
			if sheetman.CheckColumn(reader, column, "StarNode.IconPath", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "StarNode.IconPath")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.IconPath = cursor.NextString()
				}
			}
		default:
			// A column added after this code was generated.
			reader.Skip(column.ByteLength)
		}

		sheetman.CheckBlockEnd(reader, column, blockEnd)
	}

	if err := reader.Err(); err != nil {
		return fmt.Errorf("StarNode: %w", err)
	}

	byId := make(map[int32]int, len(records))
	for i := range records {
		byId[records[i].Id] = i
	}

	// Published, now that every column read and the indexes are built.
	t.records = records
	t.byId = byId

	return nil
}
