// ------------------------------------------------------------------------------
// Generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

@file:Suppress("unused", "RedundantVisibilityModifier", "UNUSED_IMPORT")

package rescue

import java.io.File
import sheetman.ScbReader
import sheetman.RecordNotFoundException
import sheetman.Uuid
import sheetman.readAllBytes
import sheetman.readTableHeader
import sheetman.checkColumn
import sheetman.checkBlockEnd
import sheetman.ColumnCursor
import sheetman.ELEMENT_VARINT
import sheetman.ELEMENT_BOOL
import sheetman.ELEMENT_I32
import sheetman.ELEMENT_I64
import sheetman.ELEMENT_F32
import sheetman.ELEMENT_F64
import sheetman.ELEMENT_STRING
import sheetman.ELEMENT_UUID
import sheetman.KIND_SCALAR
import sheetman.KIND_FIXED_ARRAY
import sheetman.KIND_VAR_ARRAY

// Generated from project-rescue-xlsxs/MasterData.xlsx : ArtifactTable : A1
class ArtifactRecord {
    /** Artifact_Table */
    var id: Int = 0
    /** 영문 */
    var name: String = ""
    /** 아티팩트 타입 */
    var artifactType: ArtifactJobType = ArtifactJobType.of(0)
    /** 아티팩트 태생 등급 */
    var grade: GradeType = GradeType.of(0)
    /** 장착 버프 효과 */
    var equipSkillID: MutableList<Int> = ArrayList()
    /** 장착 스탯 타입 */
    var equipStatType: StatType = StatType.of(0)
    /** 장착 스탯 값 */
    var equipStatRate: Float = 0.0f
    /** 보유 스탯 타입 */
    var collectionType: StatType = StatType.of(0)
    /** 보유 스탯 값 */
    var collectionVlaue: Float = 0.0f
    /** 아이콘 경로 */
    var iconPath: String = ""
    /** 머티리얼 경로 */
    var materialPath: String = ""
    /** 설명 */
    var description: String = ""

}

/** Every row of Artifact. */
class ArtifactTable {

    /**
     * Every row, in the order the sheet declared them.
     *
     * The list a read published, not one a read fills. A refresh replaces the reference
     * rather than the contents, so whoever holds this holds one whole load.
     */
    var records: MutableList<ArtifactRecord> = ArrayList()
        private set

    private var byId: HashMap<Int, ArtifactRecord> = HashMap()

    /**
     * The row with this Id, or null when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    fun findById(key: Int): ArtifactRecord? = byId[key]

    /**
     * The row with this Id, or a thrown exception naming what was missing.
     *
     * For a key that has to be there - one from another table, or a constant. The name says
     * it throws, because a caller reading getById(key).name at a glance
     * cannot otherwise tell whether the next line is a null check or a catch.
     */
    fun getByIdOrThrow(key: Int): ArtifactRecord =
        byId[key] ?: throw RecordNotFoundException(
            "there is no record in table `Artifact` that corresponds to " +
                "field `Id` value $key")

    /** Whether the table holds a row with this Id. */
    fun containsId(key: Int): Boolean = byId.containsKey(key)

    /** Loads the table from a .scb file written by SheetMan. */
    /**
     * Column by column, matched by tag rather than position: a column this build does not
     * know is skipped by its block length, and one whose type changed incompatibly fails
     * naming the field.
     */
    fun read(filename: String) {
        val reader = ScbReader(readAllBytes(filename))
        val header = readTableHeader(reader)
        val count = header.rowCount

        // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
        val loaded = ArrayList<ArtifactRecord>(count)
        val loadedById = HashMap<Int, ArtifactRecord>(count * 2)

        repeat(count) { loaded.add(ArtifactRecord()) }

        for (column in header.columns) {
            val blockEnd = reader.position + column.byteLength

            when (column.tag) {
                1 -> {
                    checkColumn(column, "Artifact.Id", KIND_SCALAR, 1, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "Artifact.Id")
                    for (record in loaded) {
                        record.id = cursor.nextI32()
                    }
                }
                2 -> {
                    checkColumn(column, "Artifact.Name", KIND_SCALAR, 1, ELEMENT_STRING)
                    val cursor = ColumnCursor(reader, column, count, "Artifact.Name")
                    for (record in loaded) {
                        record.name = cursor.nextString()
                    }
                }
                3 -> {
                    checkColumn(column, "Artifact.ArtifactType", KIND_SCALAR, 1, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "Artifact.ArtifactType")
                    for (record in loaded) {
                        record.artifactType = ArtifactJobType.of(cursor.nextI32())
                    }
                }
                4 -> {
                    checkColumn(column, "Artifact.Grade", KIND_SCALAR, 1, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "Artifact.Grade")
                    for (record in loaded) {
                        record.grade = GradeType.of(cursor.nextI32())
                    }
                }
                5 -> {
                    checkColumn(column, "Artifact.EquipSkillID", KIND_VAR_ARRAY, 0, ELEMENT_I32, ELEMENT_VARINT)
                    for (record in loaded) {
                        val elementCount = reader.readCounter32()
                        record.equipSkillID = ArrayList(elementCount.coerceAtLeast(0))
                        repeat(elementCount) { record.equipSkillID.add(reader.readI32As(column.element)) }
                    }
                }
                6 -> {
                    checkColumn(column, "Artifact.EquipStatType", KIND_SCALAR, 1, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "Artifact.EquipStatType")
                    for (record in loaded) {
                        record.equipStatType = StatType.of(cursor.nextI32())
                    }
                }
                7 -> {
                    checkColumn(column, "Artifact.EquipStatRate", KIND_SCALAR, 1, ELEMENT_F32)
                    val cursor = ColumnCursor(reader, column, count, "Artifact.EquipStatRate")
                    for (record in loaded) {
                        record.equipStatRate = cursor.nextF32()
                    }
                }
                8 -> {
                    checkColumn(column, "Artifact.CollectionType", KIND_SCALAR, 1, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "Artifact.CollectionType")
                    for (record in loaded) {
                        record.collectionType = StatType.of(cursor.nextI32())
                    }
                }
                9 -> {
                    checkColumn(column, "Artifact.CollectionVlaue", KIND_SCALAR, 1, ELEMENT_F32)
                    val cursor = ColumnCursor(reader, column, count, "Artifact.CollectionVlaue")
                    for (record in loaded) {
                        record.collectionVlaue = cursor.nextF32()
                    }
                }
                10 -> {
                    checkColumn(column, "Artifact.IconPath", KIND_SCALAR, 1, ELEMENT_STRING)
                    val cursor = ColumnCursor(reader, column, count, "Artifact.IconPath")
                    for (record in loaded) {
                        record.iconPath = cursor.nextString()
                    }
                }
                11 -> {
                    checkColumn(column, "Artifact.MaterialPath", KIND_SCALAR, 1, ELEMENT_STRING)
                    val cursor = ColumnCursor(reader, column, count, "Artifact.MaterialPath")
                    for (record in loaded) {
                        record.materialPath = cursor.nextString()
                    }
                }
                12 -> {
                    checkColumn(column, "Artifact.Description", KIND_SCALAR, 1, ELEMENT_STRING)
                    val cursor = ColumnCursor(reader, column, count, "Artifact.Description")
                    for (record in loaded) {
                        record.description = cursor.nextString()
                    }
                }
                else ->
                    // A column added after this code was generated.
                    reader.skip(column.byteLength)
            }

            checkBlockEnd(reader, column, blockEnd)
        }

        for (record in loaded) {
            loadedById[record.id] = record
        }

        // Published, now that every column read.
        records = loaded
        byId = loadedById
    }
}
