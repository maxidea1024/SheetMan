// ------------------------------------------------------------------------------
// Generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

@file:Suppress("unused", "RedundantVisibilityModifier", "UNUSED_IMPORT")

package rescue

import java.io.File
import sheetman.ScbReader
import sheetman.RecordNotFoundException
import sheetman.Uuid
import sheetman.readAllBytes
import sheetman.readTableHeader
import sheetman.checkColumn
import sheetman.checkBlockEnd
import sheetman.ColumnCursor
import sheetman.ELEMENT_VARINT
import sheetman.ELEMENT_BOOL
import sheetman.ELEMENT_I32
import sheetman.ELEMENT_I64
import sheetman.ELEMENT_F32
import sheetman.ELEMENT_F64
import sheetman.ELEMENT_STRING
import sheetman.ELEMENT_UUID
import sheetman.KIND_SCALAR
import sheetman.KIND_FIXED_ARRAY
import sheetman.KIND_VAR_ARRAY

// Generated from project-rescue-xlsxs/MasterData.xlsx : CharacterTranscendenceTable : A1
class CharacterTranscendenceRecord {
    /** Transcend_ID */
    var id: Int = 0
    /** 영문 */
    var name: String = ""
    /** 한글이름 */
    var nameKR: String = ""
    /** 등급 */
    var gradeType: GradeType = GradeType.of(0)
    /** 초월 단계 */
    var transcendStep: Int = 0
    /** 초월 최대 레벨 */
    var maxLevel: Int = 0
    /** 초월 특수 재료 타입 */
    var materialType: CurrencyType = CurrencyType.of(0)
    /** 초월 특수 재료 개수 */
    var materialCount: Int = 0
    /** 초월시 스탯 증가율 */
    var powerMultiplier: Float = 0.0f
    /** 다음 성급 Index */
    var nextStepID: Int = 0

}

/** Every row of CharacterTranscendence. */
class CharacterTranscendenceTable {

    /**
     * Every row, in the order the sheet declared them.
     *
     * The list a read published, not one a read fills. A refresh replaces the reference
     * rather than the contents, so whoever holds this holds one whole load.
     */
    var records: MutableList<CharacterTranscendenceRecord> = ArrayList()
        private set

    private var byId: HashMap<Int, CharacterTranscendenceRecord> = HashMap()

    /**
     * The row with this Id, or null when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    fun findById(key: Int): CharacterTranscendenceRecord? = byId[key]

    /**
     * The row with this Id, or a thrown exception naming what was missing.
     *
     * For a key that has to be there - one from another table, or a constant. The name says
     * it throws, because a caller reading getById(key).name at a glance
     * cannot otherwise tell whether the next line is a null check or a catch.
     */
    fun getByIdOrThrow(key: Int): CharacterTranscendenceRecord =
        byId[key] ?: throw RecordNotFoundException(
            "there is no record in table `CharacterTranscendence` that corresponds to " +
                "field `Id` value $key")

    /** Whether the table holds a row with this Id. */
    fun containsId(key: Int): Boolean = byId.containsKey(key)

    /** Loads the table from a .scb file written by SheetMan. */
    /**
     * Column by column, matched by tag rather than position: a column this build does not
     * know is skipped by its block length, and one whose type changed incompatibly fails
     * naming the field.
     */
    fun read(filename: String) {
        val reader = ScbReader(readAllBytes(filename))
        val header = readTableHeader(reader)
        val count = header.rowCount

        // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
        val loaded = ArrayList<CharacterTranscendenceRecord>(count)
        val loadedById = HashMap<Int, CharacterTranscendenceRecord>(count * 2)

        repeat(count) { loaded.add(CharacterTranscendenceRecord()) }

        for (column in header.columns) {
            val blockEnd = reader.position + column.byteLength

            when (column.tag) {
                1 -> {
                    checkColumn(column, "CharacterTranscendence.Id", KIND_SCALAR, 1, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "CharacterTranscendence.Id")
                    for (record in loaded) {
                        record.id = cursor.nextI32()
                    }
                }
                2 -> {
                    checkColumn(column, "CharacterTranscendence.Name", KIND_SCALAR, 1, ELEMENT_STRING)
                    val cursor = ColumnCursor(reader, column, count, "CharacterTranscendence.Name")
                    for (record in loaded) {
                        record.name = cursor.nextString()
                    }
                }
                3 -> {
                    checkColumn(column, "CharacterTranscendence.NameKR", KIND_SCALAR, 1, ELEMENT_STRING)
                    val cursor = ColumnCursor(reader, column, count, "CharacterTranscendence.NameKR")
                    for (record in loaded) {
                        record.nameKR = cursor.nextString()
                    }
                }
                4 -> {
                    checkColumn(column, "CharacterTranscendence.GradeType", KIND_SCALAR, 1, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "CharacterTranscendence.GradeType")
                    for (record in loaded) {
                        record.gradeType = GradeType.of(cursor.nextI32())
                    }
                }
                5 -> {
                    checkColumn(column, "CharacterTranscendence.TranscendStep", KIND_SCALAR, 1, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "CharacterTranscendence.TranscendStep")
                    for (record in loaded) {
                        record.transcendStep = cursor.nextI32()
                    }
                }
                6 -> {
                    checkColumn(column, "CharacterTranscendence.MaxLevel", KIND_SCALAR, 1, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "CharacterTranscendence.MaxLevel")
                    for (record in loaded) {
                        record.maxLevel = cursor.nextI32()
                    }
                }
                7 -> {
                    checkColumn(column, "CharacterTranscendence.MaterialType", KIND_SCALAR, 1, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "CharacterTranscendence.MaterialType")
                    for (record in loaded) {
                        record.materialType = CurrencyType.of(cursor.nextI32())
                    }
                }
                8 -> {
                    checkColumn(column, "CharacterTranscendence.MaterialCount", KIND_SCALAR, 1, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "CharacterTranscendence.MaterialCount")
                    for (record in loaded) {
                        record.materialCount = cursor.nextI32()
                    }
                }
                9 -> {
                    checkColumn(column, "CharacterTranscendence.PowerMultiplier", KIND_SCALAR, 1, ELEMENT_F32)
                    val cursor = ColumnCursor(reader, column, count, "CharacterTranscendence.PowerMultiplier")
                    for (record in loaded) {
                        record.powerMultiplier = cursor.nextF32()
                    }
                }
                10 -> {
                    checkColumn(column, "CharacterTranscendence.NextStepID", KIND_SCALAR, 1, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "CharacterTranscendence.NextStepID")
                    for (record in loaded) {
                        record.nextStepID = cursor.nextI32()
                    }
                }
                else ->
                    // A column added after this code was generated.
                    reader.skip(column.byteLength)
            }

            checkBlockEnd(reader, column, blockEnd)
        }

        for (record in loaded) {
            loadedById[record.id] = record
        }

        // Published, now that every column read.
        records = loaded
        byId = loadedById
    }
}
