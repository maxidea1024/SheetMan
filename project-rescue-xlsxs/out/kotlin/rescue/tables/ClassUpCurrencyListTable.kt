// ------------------------------------------------------------------------------
// Generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

@file:Suppress("unused", "RedundantVisibilityModifier", "UNUSED_IMPORT")

package rescue

import java.io.File
import sheetman.ScbReader
import sheetman.RecordNotFoundException
import sheetman.Uuid
import sheetman.readAllBytes
import sheetman.readTableHeader
import sheetman.checkColumn
import sheetman.checkBlockEnd
import sheetman.ColumnCursor
import sheetman.ELEMENT_VARINT
import sheetman.ELEMENT_BOOL
import sheetman.ELEMENT_I32
import sheetman.ELEMENT_I64
import sheetman.ELEMENT_F32
import sheetman.ELEMENT_F64
import sheetman.ELEMENT_STRING
import sheetman.ELEMENT_UUID
import sheetman.KIND_SCALAR
import sheetman.KIND_FIXED_ARRAY
import sheetman.KIND_VAR_ARRAY

// Generated from project-rescue-xlsxs/ItemData.xlsx : ClassUpCurrencyListTable : A1
class ClassUpCurrencyListRecord {
    /** Currency_Table */
    var id: Int = 0
    /** 한글 */
    var name: String = ""
    /** 재화 타입 */
    var type: CurrencyType = CurrencyType.of(0)
    /** 타겟 ID */
    var targetId: Int = 0
    /** 최대 획득 */
    var maxCount: Int = 0
    /** 최대 소유 값 */
    var maxStack: Long = 0L
    /** 아이콘 경로 */
    var iconPath: String = ""
    /** 바로가기 */
    var shortCutIndex: MutableList<Int> = ArrayList()

}

/** Every row of ClassUpCurrencyList. */
class ClassUpCurrencyListTable {

    /**
     * Every row, in the order the sheet declared them.
     *
     * The list a read published, not one a read fills. A refresh replaces the reference
     * rather than the contents, so whoever holds this holds one whole load.
     */
    var records: MutableList<ClassUpCurrencyListRecord> = ArrayList()
        private set

    private var byId: HashMap<Int, ClassUpCurrencyListRecord> = HashMap()

    /**
     * The row with this Id, or null when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    fun findById(key: Int): ClassUpCurrencyListRecord? = byId[key]

    /**
     * The row with this Id, or a thrown exception naming what was missing.
     *
     * For a key that has to be there - one from another table, or a constant. The name says
     * it throws, because a caller reading getById(key).name at a glance
     * cannot otherwise tell whether the next line is a null check or a catch.
     */
    fun getByIdOrThrow(key: Int): ClassUpCurrencyListRecord =
        byId[key] ?: throw RecordNotFoundException(
            "there is no record in table `ClassUpCurrencyList` that corresponds to " +
                "field `Id` value $key")

    /** Whether the table holds a row with this Id. */
    fun containsId(key: Int): Boolean = byId.containsKey(key)

    /** Loads the table from a .scb file written by SheetMan. */
    /**
     * Column by column, matched by tag rather than position: a column this build does not
     * know is skipped by its block length, and one whose type changed incompatibly fails
     * naming the field.
     */
    fun read(filename: String) {
        val reader = ScbReader(readAllBytes(filename))
        val header = readTableHeader(reader)
        val count = header.rowCount

        // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
        val loaded = ArrayList<ClassUpCurrencyListRecord>(count)
        val loadedById = HashMap<Int, ClassUpCurrencyListRecord>(count * 2)

        repeat(count) { loaded.add(ClassUpCurrencyListRecord()) }

        for (column in header.columns) {
            val blockEnd = reader.position + column.byteLength

            when (column.tag) {
                1 -> {
                    checkColumn(column, "ClassUpCurrencyList.Id", KIND_SCALAR, 1, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "ClassUpCurrencyList.Id")
                    for (record in loaded) {
                        record.id = cursor.nextI32()
                    }
                }
                2 -> {
                    checkColumn(column, "ClassUpCurrencyList.Name", KIND_SCALAR, 1, ELEMENT_STRING)
                    val cursor = ColumnCursor(reader, column, count, "ClassUpCurrencyList.Name")
                    for (record in loaded) {
                        record.name = cursor.nextString()
                    }
                }
                3 -> {
                    checkColumn(column, "ClassUpCurrencyList.Type", KIND_SCALAR, 1, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "ClassUpCurrencyList.Type")
                    for (record in loaded) {
                        record.type = CurrencyType.of(cursor.nextI32())
                    }
                }
                4 -> {
                    checkColumn(column, "ClassUpCurrencyList.TargetId", KIND_SCALAR, 1, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "ClassUpCurrencyList.TargetId")
                    for (record in loaded) {
                        record.targetId = cursor.nextI32()
                    }
                }
                5 -> {
                    checkColumn(column, "ClassUpCurrencyList.MaxCount", KIND_SCALAR, 1, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "ClassUpCurrencyList.MaxCount")
                    for (record in loaded) {
                        record.maxCount = cursor.nextI32()
                    }
                }
                6 -> {
                    checkColumn(column, "ClassUpCurrencyList.MaxStack", KIND_SCALAR, 1, ELEMENT_I64, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "ClassUpCurrencyList.MaxStack")
                    for (record in loaded) {
                        record.maxStack = cursor.nextI64()
                    }
                }
                7 -> {
                    checkColumn(column, "ClassUpCurrencyList.IconPath", KIND_SCALAR, 1, ELEMENT_STRING)
                    val cursor = ColumnCursor(reader, column, count, "ClassUpCurrencyList.IconPath")
                    for (record in loaded) {
                        record.iconPath = cursor.nextString()
                    }
                }
                8 -> {
                    checkColumn(column, "ClassUpCurrencyList.ShortCutIndex", KIND_VAR_ARRAY, 0, ELEMENT_I32, ELEMENT_VARINT)
                    for (record in loaded) {
                        val elementCount = reader.readCounter32()
                        record.shortCutIndex = ArrayList(elementCount.coerceAtLeast(0))
                        repeat(elementCount) { record.shortCutIndex.add(reader.readI32As(column.element)) }
                    }
                }
                else ->
                    // A column added after this code was generated.
                    reader.skip(column.byteLength)
            }

            checkBlockEnd(reader, column, blockEnd)
        }

        for (record in loaded) {
            loadedById[record.id] = record
        }

        // Published, now that every column read.
        records = loaded
        byId = loadedById
    }
}
