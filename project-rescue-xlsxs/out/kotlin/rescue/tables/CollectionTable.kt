// ------------------------------------------------------------------------------
// Generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

@file:Suppress("unused", "RedundantVisibilityModifier", "UNUSED_IMPORT")

package rescue

import java.io.File
import sheetman.ScbReader
import sheetman.RecordNotFoundException
import sheetman.Uuid
import sheetman.readAllBytes
import sheetman.readTableHeader
import sheetman.checkColumn
import sheetman.checkBlockEnd
import sheetman.ColumnCursor
import sheetman.ELEMENT_VARINT
import sheetman.ELEMENT_BOOL
import sheetman.ELEMENT_I32
import sheetman.ELEMENT_I64
import sheetman.ELEMENT_F32
import sheetman.ELEMENT_F64
import sheetman.ELEMENT_STRING
import sheetman.ELEMENT_UUID
import sheetman.KIND_SCALAR
import sheetman.KIND_FIXED_ARRAY
import sheetman.KIND_VAR_ARRAY

// Generated from project-rescue-xlsxs/CollectionData.xlsx : CollectionTable : A1
class CollectionRecord {
    /** CollectionID */
    var id: Int = 0
    /** 영문 */
    var name: String = ""
    /** 캐릭터ID */
    var characterID: Int = 0
    /** 보상 UI분기 타입 */
    var tabType: CollectionTabType = CollectionTabType.of(0)
    /** 조건 값 */
    var conditionID: Int = 0
    /** 재화 보상 타입 */
    var rewardType: CurrencyType = CurrencyType.of(0)
    /** 재화 보상 값 */
    var rewardValue: Int = 0
    /** 스탯 보상 타입 */
    var rewardStatType1: StatType = StatType.of(0)
    /** 스탯 보상(%) */
    var rewardStatRate: Float = 0.0f
    /** 스탯 보상 타입 */
    var rewardStatType2: StatType = StatType.of(0)
    /** 스탯 보상(+) */
    var rewardStatValue: Int = 0
    /** 바로가기 ID */
    var shortCutID: Int = 0

}

/** Every row of Collection. */
class CollectionTable {

    /**
     * Every row, in the order the sheet declared them.
     *
     * The list a read published, not one a read fills. A refresh replaces the reference
     * rather than the contents, so whoever holds this holds one whole load.
     */
    var records: MutableList<CollectionRecord> = ArrayList()
        private set

    private var byId: HashMap<Int, CollectionRecord> = HashMap()

    /**
     * The row with this Id, or null when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    fun findById(key: Int): CollectionRecord? = byId[key]

    /**
     * The row with this Id, or a thrown exception naming what was missing.
     *
     * For a key that has to be there - one from another table, or a constant. The name says
     * it throws, because a caller reading getById(key).name at a glance
     * cannot otherwise tell whether the next line is a null check or a catch.
     */
    fun getByIdOrThrow(key: Int): CollectionRecord =
        byId[key] ?: throw RecordNotFoundException(
            "there is no record in table `Collection` that corresponds to " +
                "field `Id` value $key")

    /** Whether the table holds a row with this Id. */
    fun containsId(key: Int): Boolean = byId.containsKey(key)

    /** Loads the table from a .scb file written by SheetMan. */
    /**
     * Column by column, matched by tag rather than position: a column this build does not
     * know is skipped by its block length, and one whose type changed incompatibly fails
     * naming the field.
     */
    fun read(filename: String) {
        val reader = ScbReader(readAllBytes(filename))
        val header = readTableHeader(reader)
        val count = header.rowCount

        // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
        val loaded = ArrayList<CollectionRecord>(count)
        val loadedById = HashMap<Int, CollectionRecord>(count * 2)

        repeat(count) { loaded.add(CollectionRecord()) }

        for (column in header.columns) {
            val blockEnd = reader.position + column.byteLength

            when (column.tag) {
                1 -> {
                    checkColumn(column, "Collection.Id", KIND_SCALAR, 1, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "Collection.Id")
                    for (record in loaded) {
                        record.id = cursor.nextI32()
                    }
                }
                2 -> {
                    checkColumn(column, "Collection.Name", KIND_SCALAR, 1, ELEMENT_STRING)
                    val cursor = ColumnCursor(reader, column, count, "Collection.Name")
                    for (record in loaded) {
                        record.name = cursor.nextString()
                    }
                }
                3 -> {
                    checkColumn(column, "Collection.CharacterID", KIND_SCALAR, 1, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "Collection.CharacterID")
                    for (record in loaded) {
                        record.characterID = cursor.nextI32()
                    }
                }
                4 -> {
                    checkColumn(column, "Collection.TabType", KIND_SCALAR, 1, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "Collection.TabType")
                    for (record in loaded) {
                        record.tabType = CollectionTabType.of(cursor.nextI32())
                    }
                }
                5 -> {
                    checkColumn(column, "Collection.ConditionID", KIND_SCALAR, 1, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "Collection.ConditionID")
                    for (record in loaded) {
                        record.conditionID = cursor.nextI32()
                    }
                }
                6 -> {
                    checkColumn(column, "Collection.RewardType", KIND_SCALAR, 1, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "Collection.RewardType")
                    for (record in loaded) {
                        record.rewardType = CurrencyType.of(cursor.nextI32())
                    }
                }
                7 -> {
                    checkColumn(column, "Collection.RewardValue", KIND_SCALAR, 1, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "Collection.RewardValue")
                    for (record in loaded) {
                        record.rewardValue = cursor.nextI32()
                    }
                }
                8 -> {
                    checkColumn(column, "Collection.RewardStatType1", KIND_SCALAR, 1, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "Collection.RewardStatType1")
                    for (record in loaded) {
                        record.rewardStatType1 = StatType.of(cursor.nextI32())
                    }
                }
                9 -> {
                    checkColumn(column, "Collection.RewardStatRate", KIND_SCALAR, 1, ELEMENT_F32)
                    val cursor = ColumnCursor(reader, column, count, "Collection.RewardStatRate")
                    for (record in loaded) {
                        record.rewardStatRate = cursor.nextF32()
                    }
                }
                10 -> {
                    checkColumn(column, "Collection.RewardStatType2", KIND_SCALAR, 1, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "Collection.RewardStatType2")
                    for (record in loaded) {
                        record.rewardStatType2 = StatType.of(cursor.nextI32())
                    }
                }
                11 -> {
                    checkColumn(column, "Collection.RewardStatValue", KIND_SCALAR, 1, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "Collection.RewardStatValue")
                    for (record in loaded) {
                        record.rewardStatValue = cursor.nextI32()
                    }
                }
                12 -> {
                    checkColumn(column, "Collection.ShortCutID", KIND_SCALAR, 1, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "Collection.ShortCutID")
                    for (record in loaded) {
                        record.shortCutID = cursor.nextI32()
                    }
                }
                else ->
                    // A column added after this code was generated.
                    reader.skip(column.byteLength)
            }

            checkBlockEnd(reader, column, blockEnd)
        }

        for (record in loaded) {
            loadedById[record.id] = record
        }

        // Published, now that every column read.
        records = loaded
        byId = loadedById
    }
}
