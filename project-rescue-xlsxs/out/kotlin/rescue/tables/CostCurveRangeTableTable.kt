// ------------------------------------------------------------------------------
// Generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

@file:Suppress("unused", "RedundantVisibilityModifier", "UNUSED_IMPORT")

package rescue

import java.io.File
import sheetman.LiteBinaryReader
import sheetman.RecordNotFoundException
import sheetman.Uuid
import sheetman.readAllBytes
import sheetman.readTableHeader
import sheetman.checkColumn
import sheetman.checkBlockEnd
import sheetman.ELEMENT_VARINT
import sheetman.ELEMENT_BOOL
import sheetman.ELEMENT_I32
import sheetman.ELEMENT_I64
import sheetman.ELEMENT_F32
import sheetman.ELEMENT_F64
import sheetman.ELEMENT_STRING
import sheetman.ELEMENT_UUID
import sheetman.KIND_SCALAR
import sheetman.KIND_FIXED_ARRAY
import sheetman.KIND_VAR_ARRAY

// Generated from project-rescue-xlsxs/StageGrowth.xlsx : CostCurveRangeTable : A1
class CostCurveRangeTableRecord {
    /** 숫자 */
    var id: Int = 0
    /** 비용 콘텐츠 ID */
    var growthType: GrowthType = GrowthType.of(0)
    /** 범위 */
    var rangeIndex: Int = 0
    /** 시작 단계 */
    var startStep: Int = 0
    /** 끝 단계 */
    var endStep: Int = 0
    /** 가격 증가 배율 */
    var addMultiplier: Float = 0.0f

}

/** Every row of CostCurveRangeTable. */
class CostCurveRangeTableTable {

    /**
     * Every row, in the order the sheet declared them.
     *
     * The list a read published, not one a read fills. A refresh replaces the reference
     * rather than the contents, so whoever holds this holds one whole load.
     */
    var records: MutableList<CostCurveRangeTableRecord> = ArrayList()
        private set

    private var byId: HashMap<Int, CostCurveRangeTableRecord> = HashMap()

    /**
     * The row with this Id, or null when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    fun findById(key: Int): CostCurveRangeTableRecord? = byId[key]

    /**
     * The row with this Id, or a thrown exception naming what was missing.
     *
     * For a key that has to be there - one from another table, or a constant. The name says
     * it throws, because a caller reading getById(key).name at a glance
     * cannot otherwise tell whether the next line is a null check or a catch.
     */
    fun getByIdOrThrow(key: Int): CostCurveRangeTableRecord =
        byId[key] ?: throw RecordNotFoundException(
            "there is no record in table `CostCurveRangeTable` that corresponds to " +
                "field `Id` value $key")

    /** Whether the table holds a row with this Id. */
    fun containsId(key: Int): Boolean = byId.containsKey(key)

    /** Loads the table from a .table file written by SheetMan. */
    /**
     * Column by column, matched by tag rather than position: a column this build does not
     * know is skipped by its block length, and one whose type changed incompatibly fails
     * naming the field.
     */
    fun read(filename: String) {
        val reader = LiteBinaryReader(readAllBytes(filename))
        val header = readTableHeader(reader)
        val count = header.rowCount

        // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
        val loaded = ArrayList<CostCurveRangeTableRecord>(count)
        val loadedById = HashMap<Int, CostCurveRangeTableRecord>(count * 2)

        repeat(count) { loaded.add(CostCurveRangeTableRecord()) }

        for (column in header.columns) {
            val blockEnd = reader.position + column.byteLength

            when (column.tag) {
                1 -> {
                    checkColumn(column, "CostCurveRangeTable.Id", KIND_SCALAR, 1, ELEMENT_I32, ELEMENT_VARINT)
                    for (record in loaded) {
                        record.id = reader.readI32As(column.element)
                    }
                }
                2 -> {
                    checkColumn(column, "CostCurveRangeTable.GrowthType", KIND_SCALAR, 1, ELEMENT_VARINT)
                    for (record in loaded) {
                        record.growthType = GrowthType.of(reader.readEnum())
                    }
                }
                3 -> {
                    checkColumn(column, "CostCurveRangeTable.RangeIndex", KIND_SCALAR, 1, ELEMENT_I32, ELEMENT_VARINT)
                    for (record in loaded) {
                        record.rangeIndex = reader.readI32As(column.element)
                    }
                }
                4 -> {
                    checkColumn(column, "CostCurveRangeTable.StartStep", KIND_SCALAR, 1, ELEMENT_I32, ELEMENT_VARINT)
                    for (record in loaded) {
                        record.startStep = reader.readI32As(column.element)
                    }
                }
                5 -> {
                    checkColumn(column, "CostCurveRangeTable.EndStep", KIND_SCALAR, 1, ELEMENT_I32, ELEMENT_VARINT)
                    for (record in loaded) {
                        record.endStep = reader.readI32As(column.element)
                    }
                }
                6 -> {
                    checkColumn(column, "CostCurveRangeTable.AddMultiplier", KIND_SCALAR, 1, ELEMENT_F32)
                    for (record in loaded) {
                        record.addMultiplier = reader.readFloat()
                    }
                }
                else ->
                    // A column added after this code was generated.
                    reader.skip(column.byteLength)
            }

            checkBlockEnd(reader, column, blockEnd)
        }

        for (record in loaded) {
            loadedById[record.id] = record
        }

        // Published, now that every column read.
        records = loaded
        byId = loadedById
    }
}
