// ------------------------------------------------------------------------------
// Generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

@file:Suppress("unused", "RedundantVisibilityModifier", "UNUSED_IMPORT")

package rescue

import java.io.File
import sheetman.LiteBinaryReader
import sheetman.RecordNotFoundException
import sheetman.Uuid
import sheetman.readAllBytes
import sheetman.readTableHeader
import sheetman.checkColumn
import sheetman.checkBlockEnd
import sheetman.ELEMENT_VARINT
import sheetman.ELEMENT_BOOL
import sheetman.ELEMENT_I32
import sheetman.ELEMENT_I64
import sheetman.ELEMENT_F32
import sheetman.ELEMENT_F64
import sheetman.ELEMENT_STRING
import sheetman.ELEMENT_UUID
import sheetman.KIND_SCALAR
import sheetman.KIND_FIXED_ARRAY
import sheetman.KIND_VAR_ARRAY

// Generated from project-rescue-xlsxs/ShopData.xlsx : PackageShopTable : A1
class PackageShopTableRecord {
    /** PackageShop_Table */
    var id: Int = 0
    /** 영문명 */
    var name: String = ""
    /** 한글명 */
    var productName: String = ""
    /** 상점 타입 */
    var shopType: ShopType = ShopType.of(0)
    /** 슬롯 ID */
    var shopSlotID: Int = 0
    /** 출력 우선순위 */
    var priority: Int = 0
    /** 포함 아이템 ID */
    var itemID: MutableList<Int> = ArrayList()
    /** 포함 아이템 수 */
    var itemValue: MutableList<Int> = ArrayList()
    /** 구매 주기 타입 */
    var cycleType: CycleType = CycleType.of(0)
    /** 구매 제한 횟수 */
    var limitValue: Int = 0
    /** 소모 재화 */
    var currencyType: CurrencyType = CurrencyType.of(0)
    /** 가격값 */
    var priceValue: Int = 0
    /** 해금 조건 ID */
    var conditionID: Int = 0
    /** 아이콘 경로 */
    var iconPath: String = ""

}

/** Every row of PackageShopTable. */
class PackageShopTableTable {

    /**
     * Every row, in the order the sheet declared them.
     *
     * The list a read published, not one a read fills. A refresh replaces the reference
     * rather than the contents, so whoever holds this holds one whole load.
     */
    var records: MutableList<PackageShopTableRecord> = ArrayList()
        private set

    private var byId: HashMap<Int, PackageShopTableRecord> = HashMap()

    /**
     * The row with this Id, or null when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    fun findById(key: Int): PackageShopTableRecord? = byId[key]

    /**
     * The row with this Id, or a thrown exception naming what was missing.
     *
     * For a key that has to be there - one from another table, or a constant. The name says
     * it throws, because a caller reading getById(key).name at a glance
     * cannot otherwise tell whether the next line is a null check or a catch.
     */
    fun getByIdOrThrow(key: Int): PackageShopTableRecord =
        byId[key] ?: throw RecordNotFoundException(
            "there is no record in table `PackageShopTable` that corresponds to " +
                "field `Id` value $key")

    /** Whether the table holds a row with this Id. */
    fun containsId(key: Int): Boolean = byId.containsKey(key)

    /** Loads the table from a .scb file written by SheetMan. */
    /**
     * Column by column, matched by tag rather than position: a column this build does not
     * know is skipped by its block length, and one whose type changed incompatibly fails
     * naming the field.
     */
    fun read(filename: String) {
        val reader = LiteBinaryReader(readAllBytes(filename))
        val header = readTableHeader(reader)
        val count = header.rowCount

        // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
        val loaded = ArrayList<PackageShopTableRecord>(count)
        val loadedById = HashMap<Int, PackageShopTableRecord>(count * 2)

        repeat(count) { loaded.add(PackageShopTableRecord()) }

        for (column in header.columns) {
            val blockEnd = reader.position + column.byteLength

            when (column.tag) {
                1 -> {
                    checkColumn(column, "PackageShopTable.Id", KIND_SCALAR, 1, ELEMENT_I32, ELEMENT_VARINT)
                    for (record in loaded) {
                        record.id = reader.readI32As(column.element)
                    }
                }
                2 -> {
                    checkColumn(column, "PackageShopTable.Name", KIND_SCALAR, 1, ELEMENT_STRING)
                    for (record in loaded) {
                        record.name = reader.readString()
                    }
                }
                3 -> {
                    checkColumn(column, "PackageShopTable.ProductName", KIND_SCALAR, 1, ELEMENT_STRING)
                    for (record in loaded) {
                        record.productName = reader.readString()
                    }
                }
                4 -> {
                    checkColumn(column, "PackageShopTable.ShopType", KIND_SCALAR, 1, ELEMENT_VARINT)
                    for (record in loaded) {
                        record.shopType = ShopType.of(reader.readEnum())
                    }
                }
                5 -> {
                    checkColumn(column, "PackageShopTable.ShopSlotID", KIND_SCALAR, 1, ELEMENT_I32, ELEMENT_VARINT)
                    for (record in loaded) {
                        record.shopSlotID = reader.readI32As(column.element)
                    }
                }
                6 -> {
                    checkColumn(column, "PackageShopTable.Priority", KIND_SCALAR, 1, ELEMENT_I32, ELEMENT_VARINT)
                    for (record in loaded) {
                        record.priority = reader.readI32As(column.element)
                    }
                }
                7 -> {
                    checkColumn(column, "PackageShopTable.ItemID", KIND_VAR_ARRAY, 0, ELEMENT_I32, ELEMENT_VARINT)
                    for (record in loaded) {
                        val elementCount = reader.readCounter32()
                        record.itemID = ArrayList(elementCount.coerceAtLeast(0))
                        repeat(elementCount) { record.itemID.add(reader.readI32As(column.element)) }
                    }
                }
                8 -> {
                    checkColumn(column, "PackageShopTable.ItemValue", KIND_VAR_ARRAY, 0, ELEMENT_I32, ELEMENT_VARINT)
                    for (record in loaded) {
                        val elementCount = reader.readCounter32()
                        record.itemValue = ArrayList(elementCount.coerceAtLeast(0))
                        repeat(elementCount) { record.itemValue.add(reader.readI32As(column.element)) }
                    }
                }
                9 -> {
                    checkColumn(column, "PackageShopTable.CycleType", KIND_SCALAR, 1, ELEMENT_VARINT)
                    for (record in loaded) {
                        record.cycleType = CycleType.of(reader.readEnum())
                    }
                }
                10 -> {
                    checkColumn(column, "PackageShopTable.LimitValue", KIND_SCALAR, 1, ELEMENT_I32, ELEMENT_VARINT)
                    for (record in loaded) {
                        record.limitValue = reader.readI32As(column.element)
                    }
                }
                11 -> {
                    checkColumn(column, "PackageShopTable.CurrencyType", KIND_SCALAR, 1, ELEMENT_VARINT)
                    for (record in loaded) {
                        record.currencyType = CurrencyType.of(reader.readEnum())
                    }
                }
                12 -> {
                    checkColumn(column, "PackageShopTable.PriceValue", KIND_SCALAR, 1, ELEMENT_I32, ELEMENT_VARINT)
                    for (record in loaded) {
                        record.priceValue = reader.readI32As(column.element)
                    }
                }
                13 -> {
                    checkColumn(column, "PackageShopTable.ConditionID", KIND_SCALAR, 1, ELEMENT_I32, ELEMENT_VARINT)
                    for (record in loaded) {
                        record.conditionID = reader.readI32As(column.element)
                    }
                }
                14 -> {
                    checkColumn(column, "PackageShopTable.IconPath", KIND_SCALAR, 1, ELEMENT_STRING)
                    for (record in loaded) {
                        record.iconPath = reader.readString()
                    }
                }
                else ->
                    // A column added after this code was generated.
                    reader.skip(column.byteLength)
            }

            checkBlockEnd(reader, column, blockEnd)
        }

        for (record in loaded) {
            loadedById[record.id] = record
        }

        // Published, now that every column read.
        records = loaded
        byId = loadedById
    }
}
