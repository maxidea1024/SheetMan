// ------------------------------------------------------------------------------
// Generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

@file:Suppress("unused", "RedundantVisibilityModifier", "UNUSED_IMPORT")

package rescue

import java.io.File
import sheetman.ScbReader
import sheetman.RecordNotFoundException
import sheetman.Uuid
import sheetman.readAllBytes
import sheetman.readTableHeader
import sheetman.checkColumn
import sheetman.checkBlockEnd
import sheetman.ColumnCursor
import sheetman.ELEMENT_VARINT
import sheetman.ELEMENT_BOOL
import sheetman.ELEMENT_I32
import sheetman.ELEMENT_I64
import sheetman.ELEMENT_F32
import sheetman.ELEMENT_F64
import sheetman.ELEMENT_STRING
import sheetman.ELEMENT_UUID
import sheetman.KIND_SCALAR
import sheetman.KIND_FIXED_ARRAY
import sheetman.KIND_VAR_ARRAY

// Generated from project-rescue-xlsxs/ItemData.xlsx : PackageTable : A1
class PackageRecord {
    /** PackcgeItem_Table */
    var id: Int = 0
    /** 영문 */
    var name: String = ""
    /** 한글 */
    var className: String = ""
    /** 카테고리 */
    var itemType: ItemType = ItemType.of(0)
    /** 패키지 타입 */
    var packageType: ConsumablesType = ConsumablesType.of(0)
    /** 중첩 가능 유무 */
    var stackable: Boolean = false
    /** 최대 소유 값 */
    var maxStack: Long = 0L
    /** 획득조건 타입 */
    var packageCondition: ConditionType = ConditionType.of(0)
    /** 아이템 재사용 대기시간 */
    var cooltime: Int = 0
    /** 아이템 유지 시간 */
    var duration: Int = 0
    /** 포함 아이템 종류 */
    var currencyID: MutableList<Int> = ArrayList()
    /** 포함 아이템 최소 수량 값 */
    var currencyItemMin: MutableList<Int> = ArrayList()
    /** 포함 아이템 최대 수량 값 */
    var currencyItemMax: MutableList<Int> = ArrayList()
    /** 포함 아이템 확률 */
    var currencyItemRate: MutableList<Int> = ArrayList()
    /** 아이콘 경로 */
    var iconPath: String = ""
    /** 프리펩 위치 */
    var dropPrefabPath: String = ""
    /** 아이템 설명 */
    var itemDescription: String = ""

}

/** Every row of Package. */
class PackageTable {

    /**
     * Every row, in the order the sheet declared them.
     *
     * The list a read published, not one a read fills. A refresh replaces the reference
     * rather than the contents, so whoever holds this holds one whole load.
     */
    var records: MutableList<PackageRecord> = ArrayList()
        private set

    private var byId: HashMap<Int, PackageRecord> = HashMap()

    /**
     * The row with this Id, or null when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    fun findById(key: Int): PackageRecord? = byId[key]

    /**
     * The row with this Id, or a thrown exception naming what was missing.
     *
     * For a key that has to be there - one from another table, or a constant. The name says
     * it throws, because a caller reading getById(key).name at a glance
     * cannot otherwise tell whether the next line is a null check or a catch.
     */
    fun getByIdOrThrow(key: Int): PackageRecord =
        byId[key] ?: throw RecordNotFoundException(
            "there is no record in table `Package` that corresponds to " +
                "field `Id` value $key")

    /** Whether the table holds a row with this Id. */
    fun containsId(key: Int): Boolean = byId.containsKey(key)

    /** Loads the table from a .scb file written by SheetMan. */
    /**
     * Column by column, matched by tag rather than position: a column this build does not
     * know is skipped by its block length, and one whose type changed incompatibly fails
     * naming the field.
     */
    fun read(filename: String) {
        val reader = ScbReader(readAllBytes(filename))
        val header = readTableHeader(reader)
        val count = header.rowCount

        // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
        val loaded = ArrayList<PackageRecord>(count)
        val loadedById = HashMap<Int, PackageRecord>(count * 2)

        repeat(count) { loaded.add(PackageRecord()) }

        for (column in header.columns) {
            val blockEnd = reader.position + column.byteLength

            when (column.tag) {
                1 -> {
                    checkColumn(column, "Package.Id", KIND_SCALAR, 1, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "Package.Id")
                    for (record in loaded) {
                        record.id = cursor.nextI32()
                    }
                }
                2 -> {
                    checkColumn(column, "Package.Name", KIND_SCALAR, 1, ELEMENT_STRING)
                    val cursor = ColumnCursor(reader, column, count, "Package.Name")
                    for (record in loaded) {
                        record.name = cursor.nextString()
                    }
                }
                3 -> {
                    checkColumn(column, "Package.ClassName", KIND_SCALAR, 1, ELEMENT_STRING)
                    val cursor = ColumnCursor(reader, column, count, "Package.ClassName")
                    for (record in loaded) {
                        record.className = cursor.nextString()
                    }
                }
                4 -> {
                    checkColumn(column, "Package.ItemType", KIND_SCALAR, 1, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "Package.ItemType")
                    for (record in loaded) {
                        record.itemType = ItemType.of(cursor.nextI32())
                    }
                }
                5 -> {
                    checkColumn(column, "Package.PackageType", KIND_SCALAR, 1, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "Package.PackageType")
                    for (record in loaded) {
                        record.packageType = ConsumablesType.of(cursor.nextI32())
                    }
                }
                6 -> {
                    checkColumn(column, "Package.Stackable", KIND_SCALAR, 1, ELEMENT_BOOL)
                    for (record in loaded) {
                        record.stackable = reader.readBool()
                    }
                }
                7 -> {
                    checkColumn(column, "Package.MaxStack", KIND_SCALAR, 1, ELEMENT_I64, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "Package.MaxStack")
                    for (record in loaded) {
                        record.maxStack = cursor.nextI64()
                    }
                }
                8 -> {
                    checkColumn(column, "Package.PackageCondition", KIND_SCALAR, 1, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "Package.PackageCondition")
                    for (record in loaded) {
                        record.packageCondition = ConditionType.of(cursor.nextI32())
                    }
                }
                9 -> {
                    checkColumn(column, "Package.Cooltime", KIND_SCALAR, 1, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "Package.Cooltime")
                    for (record in loaded) {
                        record.cooltime = cursor.nextI32()
                    }
                }
                10 -> {
                    checkColumn(column, "Package.Duration", KIND_SCALAR, 1, ELEMENT_I32, ELEMENT_VARINT)
                    val cursor = ColumnCursor(reader, column, count, "Package.Duration")
                    for (record in loaded) {
                        record.duration = cursor.nextI32()
                    }
                }
                11 -> {
                    checkColumn(column, "Package.CurrencyID", KIND_VAR_ARRAY, 0, ELEMENT_I32, ELEMENT_VARINT)
                    for (record in loaded) {
                        val elementCount = reader.readCounter32()
                        record.currencyID = ArrayList(elementCount.coerceAtLeast(0))
                        repeat(elementCount) { record.currencyID.add(reader.readI32As(column.element)) }
                    }
                }
                12 -> {
                    checkColumn(column, "Package.CurrencyItemMin", KIND_VAR_ARRAY, 0, ELEMENT_I32, ELEMENT_VARINT)
                    for (record in loaded) {
                        val elementCount = reader.readCounter32()
                        record.currencyItemMin = ArrayList(elementCount.coerceAtLeast(0))
                        repeat(elementCount) { record.currencyItemMin.add(reader.readI32As(column.element)) }
                    }
                }
                13 -> {
                    checkColumn(column, "Package.CurrencyItemMax", KIND_VAR_ARRAY, 0, ELEMENT_I32, ELEMENT_VARINT)
                    for (record in loaded) {
                        val elementCount = reader.readCounter32()
                        record.currencyItemMax = ArrayList(elementCount.coerceAtLeast(0))
                        repeat(elementCount) { record.currencyItemMax.add(reader.readI32As(column.element)) }
                    }
                }
                14 -> {
                    checkColumn(column, "Package.CurrencyItemRate", KIND_VAR_ARRAY, 0, ELEMENT_I32, ELEMENT_VARINT)
                    for (record in loaded) {
                        val elementCount = reader.readCounter32()
                        record.currencyItemRate = ArrayList(elementCount.coerceAtLeast(0))
                        repeat(elementCount) { record.currencyItemRate.add(reader.readI32As(column.element)) }
                    }
                }
                15 -> {
                    checkColumn(column, "Package.IconPath", KIND_SCALAR, 1, ELEMENT_STRING)
                    val cursor = ColumnCursor(reader, column, count, "Package.IconPath")
                    for (record in loaded) {
                        record.iconPath = cursor.nextString()
                    }
                }
                16 -> {
                    checkColumn(column, "Package.DropPrefabPath", KIND_SCALAR, 1, ELEMENT_STRING)
                    val cursor = ColumnCursor(reader, column, count, "Package.DropPrefabPath")
                    for (record in loaded) {
                        record.dropPrefabPath = cursor.nextString()
                    }
                }
                17 -> {
                    checkColumn(column, "Package.ItemDescription", KIND_SCALAR, 1, ELEMENT_STRING)
                    val cursor = ColumnCursor(reader, column, count, "Package.ItemDescription")
                    for (record in loaded) {
                        record.itemDescription = cursor.nextString()
                    }
                }
                else ->
                    // A column added after this code was generated.
                    reader.skip(column.byteLength)
            }

            checkBlockEnd(reader, column, blockEnd)
        }

        for (record in loaded) {
            loadedById[record.id] = record
        }

        // Published, now that every column read.
        records = loaded
        byId = loadedById
    }
}
