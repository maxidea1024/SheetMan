// ------------------------------------------------------------------------------
// Generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

@file:Suppress("unused", "RedundantVisibilityModifier", "UNUSED_IMPORT")

package rescue

import java.io.File
import sheetman.LiteBinaryReader
import sheetman.RecordNotFoundException
import sheetman.Uuid
import sheetman.readAllBytes
import sheetman.readTableHeader
import sheetman.checkColumn
import sheetman.checkBlockEnd
import sheetman.ELEMENT_VARINT
import sheetman.ELEMENT_BOOL
import sheetman.ELEMENT_I32
import sheetman.ELEMENT_I64
import sheetman.ELEMENT_F32
import sheetman.ELEMENT_F64
import sheetman.ELEMENT_STRING
import sheetman.ELEMENT_UUID
import sheetman.KIND_SCALAR
import sheetman.KIND_FIXED_ARRAY
import sheetman.KIND_VAR_ARRAY

// Generated from project-rescue-xlsxs\ItemData.xlsx : PackageTable : A1
class PackageTableRecord {
    /** PackcgeItem_Table */
    var id: Int = 0
    /** 영문 */
    var name: String = ""
    /** 한글 */
    var className: String = ""
    /** 카테고리 */
    var itemType: ItemType = ItemType.of(0)
    /** 패키지 타입 */
    var packageType: ConsumablesType = ConsumablesType.of(0)
    /** 중첩 가능 유무 */
    var stackable: Boolean = false
    /** 최대 소유 값 */
    var maxStack: Long = 0L
    /** 획득조건 타입 */
    var packageCondition: ConditionType = ConditionType.of(0)
    /** 아이템 재사용 대기시간 */
    var cooltime: Int = 0
    /** 아이템 유지 시간 */
    var duration: Int = 0
    /** 포함 아이템 종류 */
    var currencyID: MutableList<Int> = ArrayList()
    /** 포함 아이템 최소 수량 값 */
    var currencyItemMin: MutableList<Int> = ArrayList()
    /** 포함 아이템 최대 수량 값 */
    var currencyItemMax: MutableList<Int> = ArrayList()
    /** 포함 아이템 확률 */
    var currencyItemRate: MutableList<Int> = ArrayList()
    /** 아이콘 경로 */
    var iconPath: String = ""
    /** 프리펩 위치 */
    var dropPrefabPath: String = ""
    /** 아이템 설명 */
    var itemDescription: String = ""

}

/** Every row of PackageTable. */
class PackageTableTable {

    /**
     * Every row, in the order the sheet declared them.
     *
     * The list a read published, not one a read fills. A refresh replaces the reference
     * rather than the contents, so whoever holds this holds one whole load.
     */
    var records: MutableList<PackageTableRecord> = ArrayList()
        private set

    private var byId: HashMap<Int, PackageTableRecord> = HashMap()

    /**
     * The row with this Id, or null when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    fun findById(key: Int): PackageTableRecord? = byId[key]

    /**
     * The row with this Id, or a thrown exception naming what was missing.
     *
     * For a key that has to be there - one from another table, or a constant. The name says
     * it throws, because a caller reading getById(key).name at a glance
     * cannot otherwise tell whether the next line is a null check or a catch.
     */
    fun getByIdOrThrow(key: Int): PackageTableRecord =
        byId[key] ?: throw RecordNotFoundException(
            "there is no record in table `PackageTable` that corresponds to " +
                "field `Id` value $key")

    /** Whether the table holds a row with this Id. */
    fun containsId(key: Int): Boolean = byId.containsKey(key)

    /** Loads the table from a .table file written by SheetMan. */
    /**
     * Column by column, matched by tag rather than position: a column this build does not
     * know is skipped by its block length, and one whose type changed incompatibly fails
     * naming the field.
     */
    fun read(filename: String) {
        val reader = LiteBinaryReader(readAllBytes(filename))
        val header = readTableHeader(reader)
        val count = header.rowCount

        // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
        val loaded = ArrayList<PackageTableRecord>(count)
        val loadedById = HashMap<Int, PackageTableRecord>(count * 2)

        repeat(count) { loaded.add(PackageTableRecord()) }

        for (column in header.columns) {
            val blockEnd = reader.position + column.byteLength

            when (column.tag) {
                1 -> {
                    checkColumn(column, "PackageTable.Id", KIND_SCALAR, 1, ELEMENT_I32, ELEMENT_VARINT)
                    for (record in loaded) {
                        record.id = reader.readI32As(column.element)
                    }
                }
                2 -> {
                    checkColumn(column, "PackageTable.Name", KIND_SCALAR, 1, ELEMENT_STRING)
                    for (record in loaded) {
                        record.name = reader.readString()
                    }
                }
                3 -> {
                    checkColumn(column, "PackageTable.ClassName", KIND_SCALAR, 1, ELEMENT_STRING)
                    for (record in loaded) {
                        record.className = reader.readString()
                    }
                }
                4 -> {
                    checkColumn(column, "PackageTable.ItemType", KIND_SCALAR, 1, ELEMENT_VARINT)
                    for (record in loaded) {
                        record.itemType = ItemType.of(reader.readEnum())
                    }
                }
                5 -> {
                    checkColumn(column, "PackageTable.PackageType", KIND_SCALAR, 1, ELEMENT_VARINT)
                    for (record in loaded) {
                        record.packageType = ConsumablesType.of(reader.readEnum())
                    }
                }
                6 -> {
                    checkColumn(column, "PackageTable.Stackable", KIND_SCALAR, 1, ELEMENT_BOOL)
                    for (record in loaded) {
                        record.stackable = reader.readBool()
                    }
                }
                7 -> {
                    checkColumn(column, "PackageTable.MaxStack", KIND_SCALAR, 1, ELEMENT_I64, ELEMENT_I32, ELEMENT_VARINT)
                    for (record in loaded) {
                        record.maxStack = reader.readI64As(column.element)
                    }
                }
                8 -> {
                    checkColumn(column, "PackageTable.PackageCondition", KIND_SCALAR, 1, ELEMENT_VARINT)
                    for (record in loaded) {
                        record.packageCondition = ConditionType.of(reader.readEnum())
                    }
                }
                9 -> {
                    checkColumn(column, "PackageTable.Cooltime", KIND_SCALAR, 1, ELEMENT_I32, ELEMENT_VARINT)
                    for (record in loaded) {
                        record.cooltime = reader.readI32As(column.element)
                    }
                }
                10 -> {
                    checkColumn(column, "PackageTable.Duration", KIND_SCALAR, 1, ELEMENT_I32, ELEMENT_VARINT)
                    for (record in loaded) {
                        record.duration = reader.readI32As(column.element)
                    }
                }
                11 -> {
                    checkColumn(column, "PackageTable.CurrencyID", KIND_VAR_ARRAY, 0, ELEMENT_I32, ELEMENT_VARINT)
                    for (record in loaded) {
                        val elementCount = reader.readCounter32()
                        record.currencyID = ArrayList(elementCount.coerceAtLeast(0))
                        repeat(elementCount) { record.currencyID.add(reader.readI32As(column.element)) }
                    }
                }
                12 -> {
                    checkColumn(column, "PackageTable.CurrencyItemMin", KIND_VAR_ARRAY, 0, ELEMENT_I32, ELEMENT_VARINT)
                    for (record in loaded) {
                        val elementCount = reader.readCounter32()
                        record.currencyItemMin = ArrayList(elementCount.coerceAtLeast(0))
                        repeat(elementCount) { record.currencyItemMin.add(reader.readI32As(column.element)) }
                    }
                }
                13 -> {
                    checkColumn(column, "PackageTable.CurrencyItemMax", KIND_VAR_ARRAY, 0, ELEMENT_I32, ELEMENT_VARINT)
                    for (record in loaded) {
                        val elementCount = reader.readCounter32()
                        record.currencyItemMax = ArrayList(elementCount.coerceAtLeast(0))
                        repeat(elementCount) { record.currencyItemMax.add(reader.readI32As(column.element)) }
                    }
                }
                14 -> {
                    checkColumn(column, "PackageTable.CurrencyItemRate", KIND_VAR_ARRAY, 0, ELEMENT_I32, ELEMENT_VARINT)
                    for (record in loaded) {
                        val elementCount = reader.readCounter32()
                        record.currencyItemRate = ArrayList(elementCount.coerceAtLeast(0))
                        repeat(elementCount) { record.currencyItemRate.add(reader.readI32As(column.element)) }
                    }
                }
                15 -> {
                    checkColumn(column, "PackageTable.IconPath", KIND_SCALAR, 1, ELEMENT_STRING)
                    for (record in loaded) {
                        record.iconPath = reader.readString()
                    }
                }
                16 -> {
                    checkColumn(column, "PackageTable.DropPrefabPath", KIND_SCALAR, 1, ELEMENT_STRING)
                    for (record in loaded) {
                        record.dropPrefabPath = reader.readString()
                    }
                }
                17 -> {
                    checkColumn(column, "PackageTable.ItemDescription", KIND_SCALAR, 1, ELEMENT_STRING)
                    for (record in loaded) {
                        record.itemDescription = reader.readString()
                    }
                }
                else ->
                    // A column added after this code was generated.
                    reader.skip(column.byteLength)
            }

            checkBlockEnd(reader, column, blockEnd)
        }

        for (record in loaded) {
            loadedById[record.id] = record
        }

        // Published, now that every column read.
        records = loaded
        byId = loadedById
    }
}
