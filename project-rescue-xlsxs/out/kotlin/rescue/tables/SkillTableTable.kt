// ------------------------------------------------------------------------------
// Generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

@file:Suppress("unused", "RedundantVisibilityModifier", "UNUSED_IMPORT")

package rescue

import java.io.File
import sheetman.LiteBinaryReader
import sheetman.RecordNotFoundException
import sheetman.Uuid
import sheetman.readAllBytes
import sheetman.readTableHeader
import sheetman.checkColumn
import sheetman.checkBlockEnd
import sheetman.ELEMENT_VARINT
import sheetman.ELEMENT_BOOL
import sheetman.ELEMENT_I32
import sheetman.ELEMENT_I64
import sheetman.ELEMENT_F32
import sheetman.ELEMENT_F64
import sheetman.ELEMENT_STRING
import sheetman.ELEMENT_UUID
import sheetman.KIND_SCALAR
import sheetman.KIND_FIXED_ARRAY
import sheetman.KIND_VAR_ARRAY

// Generated from project-rescue-xlsxs/MasterData.xlsx : SkillTable : A1
class SkillTableRecord {
    /** Skill_Table */
    var id: Int = 0
    /** 영문 */
    var name: String = ""
    /** 한글 */
    var skillName: String = ""
    /** 메모 */
    var skillType: SkillType = SkillType.of(0)
    /** 메모 참조 */
    var skillSubType: SkillSubType = SkillSubType.of(0)
    /** 속성 설정 */
    var attributeType: AttributeType = AttributeType.of(0)
    /** 메모참조 */
    var targetType: TargetType = TargetType.of(0)
    /** 스킬 경로 */
    var aniPath: String = ""
    /** 스킬 사용 거리 */
    var skillUseRange: Float = 0.0f
    /** 스킬 쿨타임 */
    var coolTime: Float = 0.0f
    /** 버프 ID */
    var buffID: MutableList<Int> = ArrayList()
    /** 필살기 아이콘 경로 */
    var skillIcon: String = ""
    /** 설명 */
    var description: String = ""

}

/** Every row of SkillTable. */
class SkillTableTable {

    /**
     * Every row, in the order the sheet declared them.
     *
     * The list a read published, not one a read fills. A refresh replaces the reference
     * rather than the contents, so whoever holds this holds one whole load.
     */
    var records: MutableList<SkillTableRecord> = ArrayList()
        private set

    private var byId: HashMap<Int, SkillTableRecord> = HashMap()

    /**
     * The row with this Id, or null when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    fun findById(key: Int): SkillTableRecord? = byId[key]

    /**
     * The row with this Id, or a thrown exception naming what was missing.
     *
     * For a key that has to be there - one from another table, or a constant. The name says
     * it throws, because a caller reading getById(key).name at a glance
     * cannot otherwise tell whether the next line is a null check or a catch.
     */
    fun getByIdOrThrow(key: Int): SkillTableRecord =
        byId[key] ?: throw RecordNotFoundException(
            "there is no record in table `SkillTable` that corresponds to " +
                "field `Id` value $key")

    /** Whether the table holds a row with this Id. */
    fun containsId(key: Int): Boolean = byId.containsKey(key)

    /** Loads the table from a .scb file written by SheetMan. */
    /**
     * Column by column, matched by tag rather than position: a column this build does not
     * know is skipped by its block length, and one whose type changed incompatibly fails
     * naming the field.
     */
    fun read(filename: String) {
        val reader = LiteBinaryReader(readAllBytes(filename))
        val header = readTableHeader(reader)
        val count = header.rowCount

        // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
        val loaded = ArrayList<SkillTableRecord>(count)
        val loadedById = HashMap<Int, SkillTableRecord>(count * 2)

        repeat(count) { loaded.add(SkillTableRecord()) }

        for (column in header.columns) {
            val blockEnd = reader.position + column.byteLength

            when (column.tag) {
                1 -> {
                    checkColumn(column, "SkillTable.Id", KIND_SCALAR, 1, ELEMENT_I32, ELEMENT_VARINT)
                    for (record in loaded) {
                        record.id = reader.readI32As(column.element)
                    }
                }
                2 -> {
                    checkColumn(column, "SkillTable.Name", KIND_SCALAR, 1, ELEMENT_STRING)
                    for (record in loaded) {
                        record.name = reader.readString()
                    }
                }
                3 -> {
                    checkColumn(column, "SkillTable.SkillName", KIND_SCALAR, 1, ELEMENT_STRING)
                    for (record in loaded) {
                        record.skillName = reader.readString()
                    }
                }
                4 -> {
                    checkColumn(column, "SkillTable.SkillType", KIND_SCALAR, 1, ELEMENT_VARINT)
                    for (record in loaded) {
                        record.skillType = SkillType.of(reader.readEnum())
                    }
                }
                5 -> {
                    checkColumn(column, "SkillTable.SkillSubType", KIND_SCALAR, 1, ELEMENT_VARINT)
                    for (record in loaded) {
                        record.skillSubType = SkillSubType.of(reader.readEnum())
                    }
                }
                6 -> {
                    checkColumn(column, "SkillTable.AttributeType", KIND_SCALAR, 1, ELEMENT_VARINT)
                    for (record in loaded) {
                        record.attributeType = AttributeType.of(reader.readEnum())
                    }
                }
                7 -> {
                    checkColumn(column, "SkillTable.TargetType", KIND_SCALAR, 1, ELEMENT_VARINT)
                    for (record in loaded) {
                        record.targetType = TargetType.of(reader.readEnum())
                    }
                }
                8 -> {
                    checkColumn(column, "SkillTable.AniPath", KIND_SCALAR, 1, ELEMENT_STRING)
                    for (record in loaded) {
                        record.aniPath = reader.readString()
                    }
                }
                9 -> {
                    checkColumn(column, "SkillTable.SkillUseRange", KIND_SCALAR, 1, ELEMENT_F32)
                    for (record in loaded) {
                        record.skillUseRange = reader.readFloat()
                    }
                }
                10 -> {
                    checkColumn(column, "SkillTable.CoolTime", KIND_SCALAR, 1, ELEMENT_F32)
                    for (record in loaded) {
                        record.coolTime = reader.readFloat()
                    }
                }
                11 -> {
                    checkColumn(column, "SkillTable.BuffID", KIND_VAR_ARRAY, 0, ELEMENT_I32, ELEMENT_VARINT)
                    for (record in loaded) {
                        val elementCount = reader.readCounter32()
                        record.buffID = ArrayList(elementCount.coerceAtLeast(0))
                        repeat(elementCount) { record.buffID.add(reader.readI32As(column.element)) }
                    }
                }
                12 -> {
                    checkColumn(column, "SkillTable.SkillIcon", KIND_SCALAR, 1, ELEMENT_STRING)
                    for (record in loaded) {
                        record.skillIcon = reader.readString()
                    }
                }
                13 -> {
                    checkColumn(column, "SkillTable.Description", KIND_SCALAR, 1, ELEMENT_STRING)
                    for (record in loaded) {
                        record.description = reader.readString()
                    }
                }
                else ->
                    // A column added after this code was generated.
                    reader.skip(column.byteLength)
            }

            checkBlockEnd(reader, column, blockEnd)
        }

        for (record in loaded) {
            loadedById[record.id] = record
        }

        // Published, now that every column read.
        records = loaded
        byId = loadedById
    }
}
