<?php

// ------------------------------------------------------------------------------
// Generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

declare(strict_types=1);

namespace Rescue;

require_once __DIR__ . '/../sheetman/ScbReader.php';

use SheetMan\ScbReader;
use SheetMan\RecordNotFoundException;
use SheetMan\Uuid;

/**
 * Generated from project-rescue-xlsxs/MasterData.xlsx : ConfigTable : A1
 */
final class ConfigRecord
{
    /** Row number, added by SheetMan because `Id` cannot be a primary index. */
    public int $index = 0;
    /** ConfigTable */
    public string $id = '';
    /** 카테고리 */
    public string $category = '';
    /** 테이터타입 */
    public string $dataType = '';
    /** 기본값 */
    public string $defaultValue = '';
    /** 설명 */
    public string $description = '';
}

/** Every row of Config. */
final class ConfigTable
{
    /** @var list<ConfigRecord> */
    public array $records = [];

    /** @var array<int, ConfigRecord> */
    private array $byIndex = [];

    /** @var array<string, ConfigRecord> */
    private array $byId = [];

    /**
     * The row with this Index, or null when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    public function findByIndex(int $key): ?ConfigRecord
    {
        return $this->byIndex[$key] ?? null;
    }

    /**
     * The row with this Index, or a thrown exception naming what was missing.
     *
     * For a key that has to be there - one from another table, or a constant. The name says
     * it throws, because a caller reading getByIndex($key)->name at a glance
     * cannot otherwise tell whether the next line is a null check or a catch.
     */
    public function getByIndexOrThrow(int $key): ConfigRecord
    {
        $record = $this->byIndex[$key] ?? null;

        if ($record === null) {
            throw new RecordNotFoundException(
                "There is no record in table `Config` that corresponds to " .
                "field `Index` value {$key}.");
        }

        return $record;
    }

    /** Whether the table holds a row with this Index. */
    public function containsIndex(int $key): bool
    {
        return isset($this->byIndex[$key]);
    }

    /**
     * The row with this Id, or null when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    public function findById(string $key): ?ConfigRecord
    {
        return $this->byId[$key] ?? null;
    }

    /**
     * The row with this Id, or a thrown exception naming what was missing.
     *
     * For a key that has to be there - one from another table, or a constant. The name says
     * it throws, because a caller reading getById($key)->name at a glance
     * cannot otherwise tell whether the next line is a null check or a catch.
     */
    public function getByIdOrThrow(string $key): ConfigRecord
    {
        $record = $this->byId[$key] ?? null;

        if ($record === null) {
            throw new RecordNotFoundException(
                "There is no record in table `Config` that corresponds to " .
                "field `Id` value {$key}.");
        }

        return $record;
    }

    /** Whether the table holds a row with this Id. */
    public function containsId(string $key): bool
    {
        return isset($this->byId[$key]);
    }

    /** Loads the table from a .scb file written by SheetMan. */
    /**
     * Column by column, matched by tag rather than position: a column this build does not
     * know is skipped by its block length, and one whose type changed incompatibly fails
     * naming the field.
     */
    public function read(string $filename): void
    {
        $reader = ScbReader::fromFile($filename);
        [$count, $columns] = $reader->readTableHeader();

        // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
        $records = [];
        $byIndex = [];
        $byId = [];

        for ($i = 0; $i < $count; $i++) {
            $records[] = new ConfigRecord();
        }

        foreach ($columns as $column) {
            $blockEnd = $reader->position() + $column['byteLength'];

            switch ($column['tag']) {
                case 1:
                    ScbReader::checkColumn($column, 'Config.Index', ScbReader::KIND_SCALAR, 1, [ScbReader::ELEMENT_I32, ScbReader::ELEMENT_VARINT]);
                    foreach ($records as $record) {
                        $record->index = $reader->readI32As($column['element']);
                    }
                    break;

                case 2:
                    ScbReader::checkColumn($column, 'Config.Id', ScbReader::KIND_SCALAR, 1, [ScbReader::ELEMENT_STRING]);
                    foreach ($records as $record) {
                        $record->id = $reader->readString();
                    }
                    break;

                case 3:
                    ScbReader::checkColumn($column, 'Config.Category', ScbReader::KIND_SCALAR, 1, [ScbReader::ELEMENT_STRING]);
                    foreach ($records as $record) {
                        $record->category = $reader->readString();
                    }
                    break;

                case 4:
                    ScbReader::checkColumn($column, 'Config.DataType', ScbReader::KIND_SCALAR, 1, [ScbReader::ELEMENT_STRING]);
                    foreach ($records as $record) {
                        $record->dataType = $reader->readString();
                    }
                    break;

                case 5:
                    ScbReader::checkColumn($column, 'Config.DefaultValue', ScbReader::KIND_SCALAR, 1, [ScbReader::ELEMENT_STRING]);
                    foreach ($records as $record) {
                        $record->defaultValue = $reader->readString();
                    }
                    break;

                case 6:
                    ScbReader::checkColumn($column, 'Config.Description', ScbReader::KIND_SCALAR, 1, [ScbReader::ELEMENT_STRING]);
                    foreach ($records as $record) {
                        $record->description = $reader->readString();
                    }
                    break;

                default:
                    // A column added after this code was generated.
                    $reader->skip($column['byteLength']);
                    break;
            }

            $reader->checkBlockEnd($column, $blockEnd);
        }

        foreach ($records as $record) {
            $byIndex[$record->index] = $record;
            $byId[$record->id] = $record;
        }

        // Published, now that every column read.
        $this->records = $records;
        $this->byIndex = $byIndex;
        $this->byId = $byId;
    }
}
