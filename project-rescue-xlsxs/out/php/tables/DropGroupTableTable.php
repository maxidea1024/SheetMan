<?php

// ------------------------------------------------------------------------------
// Generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

declare(strict_types=1);

namespace Rescue;

require_once __DIR__ . '/../sheetman/LiteBinaryReader.php';

use SheetMan\LiteBinaryReader;
use SheetMan\RecordNotFoundException;
use SheetMan\Uuid;

/**
 * Generated from project-rescue-xlsxs/MasterData.xlsx : DropGroupTable : A1
 */
final class DropGroupTableRecord
{
    /** 기본값 */
    public int $id = 0;
    /** 영문 */
    public string $name = '';
    /** 한글 */
    public string $dropName = '';
    /** 드랍 구역 */
    public string $dropArea = '';
    /** 아이템 종류1 */
    /** @var list<int> */
    public array $dropItemIds = [];
    /** 아이템 최소 수량 값 */
    /** @var list<int> */
    public array $dropItemMin = [];
    /** 아이템 최대 수량 값 */
    /** @var list<int> */
    public array $dropItemMax = [];
    /** 확률 */
    /** @var list<float> */
    public array $itemDropRate = [];
}

/** Every row of DropGroupTable. */
final class DropGroupTableTable
{
    /** @var list<DropGroupTableRecord> */
    public array $records = [];

    /** @var array<int, DropGroupTableRecord> */
    private array $byId = [];

    /**
     * The row with this Id, or null when the table has none.
     *
     * The lookup to reach for when a missing row is an ordinary answer - an optional
     * reference, a key that came from user input.
     */
    public function findById(int $key): ?DropGroupTableRecord
    {
        return $this->byId[$key] ?? null;
    }

    /**
     * The row with this Id, or a thrown exception naming what was missing.
     *
     * For a key that has to be there - one from another table, or a constant. The name says
     * it throws, because a caller reading getById($key)->name at a glance
     * cannot otherwise tell whether the next line is a null check or a catch.
     */
    public function getByIdOrThrow(int $key): DropGroupTableRecord
    {
        $record = $this->byId[$key] ?? null;

        if ($record === null) {
            throw new RecordNotFoundException(
                "There is no record in table `DropGroupTable` that corresponds to " .
                "field `Id` value {$key}.");
        }

        return $record;
    }

    /** Whether the table holds a row with this Id. */
    public function containsId(int $key): bool
    {
        return isset($this->byId[$key]);
    }

    /** Loads the table from a .table file written by SheetMan. */
    /**
     * Column by column, matched by tag rather than position: a column this build does not
     * know is skipped by its block length, and one whose type changed incompatibly fails
     * naming the field.
     */
    public function read(string $filename): void
    {
        $reader = LiteBinaryReader::fromFile($filename);
        [$count, $columns] = $reader->readTableHeader();

        // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
        $records = [];
        $byId = [];

        for ($i = 0; $i < $count; $i++) {
            $records[] = new DropGroupTableRecord();
        }

        foreach ($columns as $column) {
            $blockEnd = $reader->position() + $column['byteLength'];

            switch ($column['tag']) {
                case 1:
                    LiteBinaryReader::checkColumn($column, 'DropGroupTable.Id', LiteBinaryReader::KIND_SCALAR, 1, [LiteBinaryReader::ELEMENT_I32, LiteBinaryReader::ELEMENT_VARINT]);
                    foreach ($records as $record) {
                        $record->id = $reader->readI32As($column['element']);
                    }
                    break;

                case 2:
                    LiteBinaryReader::checkColumn($column, 'DropGroupTable.Name', LiteBinaryReader::KIND_SCALAR, 1, [LiteBinaryReader::ELEMENT_STRING]);
                    foreach ($records as $record) {
                        $record->name = $reader->readString();
                    }
                    break;

                case 3:
                    LiteBinaryReader::checkColumn($column, 'DropGroupTable.DropName', LiteBinaryReader::KIND_SCALAR, 1, [LiteBinaryReader::ELEMENT_STRING]);
                    foreach ($records as $record) {
                        $record->dropName = $reader->readString();
                    }
                    break;

                case 4:
                    LiteBinaryReader::checkColumn($column, 'DropGroupTable.DropArea', LiteBinaryReader::KIND_SCALAR, 1, [LiteBinaryReader::ELEMENT_STRING]);
                    foreach ($records as $record) {
                        $record->dropArea = $reader->readString();
                    }
                    break;

                case 5:
                    LiteBinaryReader::checkColumn($column, 'DropGroupTable.DropItemIds', LiteBinaryReader::KIND_VAR_ARRAY, 0, [LiteBinaryReader::ELEMENT_I32, LiteBinaryReader::ELEMENT_VARINT]);
                    foreach ($records as $record) {
                        $elementCount = $reader->readCounter32();
                        $record->dropItemIds = [];
                        for ($j = 0; $j < $elementCount; $j++) {
                            $record->dropItemIds[] = $reader->readI32As($column['element']);
                        }
                    }
                    break;

                case 6:
                    LiteBinaryReader::checkColumn($column, 'DropGroupTable.DropItemMin', LiteBinaryReader::KIND_VAR_ARRAY, 0, [LiteBinaryReader::ELEMENT_I32, LiteBinaryReader::ELEMENT_VARINT]);
                    foreach ($records as $record) {
                        $elementCount = $reader->readCounter32();
                        $record->dropItemMin = [];
                        for ($j = 0; $j < $elementCount; $j++) {
                            $record->dropItemMin[] = $reader->readI32As($column['element']);
                        }
                    }
                    break;

                case 7:
                    LiteBinaryReader::checkColumn($column, 'DropGroupTable.DropItemMax', LiteBinaryReader::KIND_VAR_ARRAY, 0, [LiteBinaryReader::ELEMENT_I32, LiteBinaryReader::ELEMENT_VARINT]);
                    foreach ($records as $record) {
                        $elementCount = $reader->readCounter32();
                        $record->dropItemMax = [];
                        for ($j = 0; $j < $elementCount; $j++) {
                            $record->dropItemMax[] = $reader->readI32As($column['element']);
                        }
                    }
                    break;

                case 8:
                    LiteBinaryReader::checkColumn($column, 'DropGroupTable.ItemDropRate', LiteBinaryReader::KIND_VAR_ARRAY, 0, [LiteBinaryReader::ELEMENT_F32]);
                    foreach ($records as $record) {
                        $elementCount = $reader->readCounter32();
                        $record->itemDropRate = [];
                        for ($j = 0; $j < $elementCount; $j++) {
                            $record->itemDropRate[] = $reader->readFloat();
                        }
                    }
                    break;

                default:
                    // A column added after this code was generated.
                    $reader->skip($column['byteLength']);
                    break;
            }

            $reader->checkBlockEnd($column, $blockEnd);
        }

        foreach ($records as $record) {
            $byId[$record->id] = $record;
        }

        // Published, now that every column read.
        $this->records = $records;
        $this->byId = $byId;
    }
}
