# ------------------------------------------------------------------------------
# Generated by SheetMan. DO NOT EDIT.
#
# Changes to this file may cause incorrect behavior and will be lost if the code is
# regenerated.
# ------------------------------------------------------------------------------

import enum
import os

from . import sheetman
from .enum_collection_tab_type import CollectionTabType
from .enum_currency_type import CurrencyType
from .enum_stat_type import StatType


class CollectionTableRecord:
    """Generated from project-rescue-xlsxs/CollectionData.xlsx : CollectionTable : A1.
    """

    __slots__ = ("id", "name", "character_id", "tab_type", "condition_id", "reward_type", "reward_value", "reward_stat_type1", "reward_stat_rate", "reward_stat_type2", "reward_stat_value", "short_cut_id")

    def __init__(self):
        self.id = 0
        self.name = ""
        self.character_id = 0
        self.tab_type = CollectionTabType(0)
        self.condition_id = 0
        self.reward_type = CurrencyType(0)
        self.reward_value = 0
        self.reward_stat_type1 = StatType(0)
        self.reward_stat_rate = 0.0
        self.reward_stat_type2 = StatType(0)
        self.reward_stat_value = 0
        self.short_cut_id = 0

    def __repr__(self):
        return "CollectionTableRecord(id=%r, name=%r, character_id=%r, tab_type=%r, condition_id=%r, reward_type=%r, reward_value=%r, reward_stat_type1=%r, reward_stat_rate=%r, reward_stat_type2=%r, reward_stat_value=%r, short_cut_id=%r)" % (self.id, self.name, self.character_id, self.tab_type, self.condition_id, self.reward_type, self.reward_value, self.reward_stat_type1, self.reward_stat_rate, self.reward_stat_type2, self.reward_stat_value, self.short_cut_id)


class CollectionTableTable:
    """Every row of CollectionTable."""

    __slots__ = ("records", "by_id")

    def __init__(self):
        self.records = []
        self.by_id = {}

    def find_by_id(self, key):
        """The row with this Id, or None when the table has none.

        The lookup to reach for when a missing row is an ordinary answer - an optional
        reference, a key that came from user input.
        """
        return self.by_id.get(key)

    def get_by_id_or_throw(self, key):
        """The row with this Id, or a raised error naming what was missing.

        For a key that has to be there - one from another table, or a constant. The name
        says it raises, because a caller reading `get_by_id(key).name` at a
        glance cannot otherwise tell whether the next line is a check or a try.
        """
        record = self.by_id.get(key)

        if record is None:
            raise sheetman.RecordNotFoundError(
                "there is no record in table `CollectionTable` that corresponds to "
                "field `Id` value %r" % (key,))

        return record

    def contains_id(self, key):
        """Whether the table holds a row with this Id."""
        return key in self.by_id

    def read(self, filename):
        """Loads the table from a .table file written by SheetMan.

        Column by column, matched by tag rather than position: a column this build does
        not know is skipped by its block length, and one whose type changed incompatibly
        fails naming the field.
        """
        reader = sheetman.Reader(sheetman.read_all_bytes(filename))
        count, columns = sheetman.read_table_header(reader)

        # Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
        records = [CollectionTableRecord() for _ in range(count)]

        for column in columns:
            block_end = reader.position + column.byte_length
            if column.tag == 1:
                sheetman.check_column(column, "CollectionTable.Id", sheetman.KIND_SCALAR, 1, (sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT))
                for record in records:
                    record.id = reader.read_i32_as(column.element)
            elif column.tag == 2:
                sheetman.check_column(column, "CollectionTable.Name", sheetman.KIND_SCALAR, 1, (sheetman.ELEMENT_STRING,))
                for record in records:
                    record.name = reader.read_string()
            elif column.tag == 3:
                sheetman.check_column(column, "CollectionTable.CharacterID", sheetman.KIND_SCALAR, 1, (sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT))
                for record in records:
                    record.character_id = reader.read_i32_as(column.element)
            elif column.tag == 4:
                sheetman.check_column(column, "CollectionTable.TabType", sheetman.KIND_SCALAR, 1, (sheetman.ELEMENT_VARINT,))
                for record in records:
                    record.tab_type = CollectionTabType(reader.read_enum())
            elif column.tag == 5:
                sheetman.check_column(column, "CollectionTable.ConditionID", sheetman.KIND_SCALAR, 1, (sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT))
                for record in records:
                    record.condition_id = reader.read_i32_as(column.element)
            elif column.tag == 6:
                sheetman.check_column(column, "CollectionTable.RewardType", sheetman.KIND_SCALAR, 1, (sheetman.ELEMENT_VARINT,))
                for record in records:
                    record.reward_type = CurrencyType(reader.read_enum())
            elif column.tag == 7:
                sheetman.check_column(column, "CollectionTable.RewardValue", sheetman.KIND_SCALAR, 1, (sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT))
                for record in records:
                    record.reward_value = reader.read_i32_as(column.element)
            elif column.tag == 8:
                sheetman.check_column(column, "CollectionTable.RewardStatType1", sheetman.KIND_SCALAR, 1, (sheetman.ELEMENT_VARINT,))
                for record in records:
                    record.reward_stat_type1 = StatType(reader.read_enum())
            elif column.tag == 9:
                sheetman.check_column(column, "CollectionTable.RewardStatRate", sheetman.KIND_SCALAR, 1, (sheetman.ELEMENT_F32,))
                for record in records:
                    record.reward_stat_rate = reader.read_float()
            elif column.tag == 10:
                sheetman.check_column(column, "CollectionTable.RewardStatType2", sheetman.KIND_SCALAR, 1, (sheetman.ELEMENT_VARINT,))
                for record in records:
                    record.reward_stat_type2 = StatType(reader.read_enum())
            elif column.tag == 11:
                sheetman.check_column(column, "CollectionTable.RewardStatValue", sheetman.KIND_SCALAR, 1, (sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT))
                for record in records:
                    record.reward_stat_value = reader.read_i32_as(column.element)
            elif column.tag == 12:
                sheetman.check_column(column, "CollectionTable.ShortCutID", sheetman.KIND_SCALAR, 1, (sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT))
                for record in records:
                    record.short_cut_id = reader.read_i32_as(column.element)
            else:
                # A column added after this code was generated.
                reader.skip(column.byte_length)

            sheetman.check_block_end(reader, column, block_end)

        by_id = {record.id: record for record in records}

        # Published, now that every column read.
        self.records = records
        self.by_id = by_id
