# ------------------------------------------------------------------------------
# Generated by SheetMan. DO NOT EDIT.
#
# Changes to this file may cause incorrect behavior and will be lost if the code is
# regenerated.
# ------------------------------------------------------------------------------

import enum
import os

from . import sheetman
from .enum_short_cut_type import ShortCutType


class ShortCutTableRecord:
    """Generated from project-rescue-xlsxs\\MasterData.xlsx : ShortCutTable : A1.
    """

    __slots__ = ("id", "name", "type_", "sub_index", "description")

    def __init__(self):
        self.id = 0
        self.name = ""
        self.type_ = ShortCutType(0)
        self.sub_index = 0
        self.description = ""

    def __repr__(self):
        return "ShortCutTableRecord(id=%r, name=%r, type_=%r, sub_index=%r, description=%r)" % (self.id, self.name, self.type_, self.sub_index, self.description)


class ShortCutTableTable:
    """Every row of ShortCutTable."""

    __slots__ = ("records", "by_id")

    def __init__(self):
        self.records = []
        self.by_id = {}

    def find_by_id(self, key):
        """The row with this ID, or None when the table has none.

        The lookup to reach for when a missing row is an ordinary answer - an optional
        reference, a key that came from user input.
        """
        return self.by_id.get(key)

    def get_by_id_or_throw(self, key):
        """The row with this ID, or a raised error naming what was missing.

        For a key that has to be there - one from another table, or a constant. The name
        says it raises, because a caller reading `get_by_id(key).name` at a
        glance cannot otherwise tell whether the next line is a check or a try.
        """
        record = self.by_id.get(key)

        if record is None:
            raise sheetman.RecordNotFoundError(
                "there is no record in table `ShortCutTable` that corresponds to "
                "field `ID` value %r" % (key,))

        return record

    def contains_id(self, key):
        """Whether the table holds a row with this ID."""
        return key in self.by_id

    def read(self, filename):
        """Loads the table from a .table file written by SheetMan.

        Column by column, matched by tag rather than position: a column this build does
        not know is skipped by its block length, and one whose type changed incompatibly
        fails naming the field.
        """
        reader = sheetman.Reader(sheetman.read_all_bytes(filename))
        count, columns = sheetman.read_table_header(reader)

        # Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
        records = [ShortCutTableRecord() for _ in range(count)]

        for column in columns:
            block_end = reader.position + column.byte_length
            if column.tag == 1:
                sheetman.check_column(column, "ShortCutTable.ID", sheetman.KIND_SCALAR, 1, (sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT))
                for record in records:
                    record.id = reader.read_i32_as(column.element)
            elif column.tag == 2:
                sheetman.check_column(column, "ShortCutTable.Name", sheetman.KIND_SCALAR, 1, (sheetman.ELEMENT_STRING,))
                for record in records:
                    record.name = reader.read_string()
            elif column.tag == 3:
                sheetman.check_column(column, "ShortCutTable.Type", sheetman.KIND_SCALAR, 1, (sheetman.ELEMENT_VARINT,))
                for record in records:
                    record.type_ = ShortCutType(reader.read_enum())
            elif column.tag == 4:
                sheetman.check_column(column, "ShortCutTable.SubIndex", sheetman.KIND_SCALAR, 1, (sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT))
                for record in records:
                    record.sub_index = reader.read_i32_as(column.element)
            elif column.tag == 5:
                sheetman.check_column(column, "ShortCutTable.Description", sheetman.KIND_SCALAR, 1, (sheetman.ELEMENT_STRING,))
                for record in records:
                    record.description = reader.read_string()
            else:
                # A column added after this code was generated.
                reader.skip(column.byte_length)

            sheetman.check_block_end(reader, column, block_end)

        by_id = {record.id: record for record in records}

        # Published, now that every column read.
        self.records = records
        self.by_id = by_id
