// ------------------------------------------------------------------------------
// Generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

use std::collections::HashMap;
use std::path::Path;
use crate::sheetman;

// Generated from project-rescue-xlsxs/MasterData.xlsx : DropGroupTable : A1
#[derive(Clone, Debug, Default)]
pub struct DropGroupTableRecord {
    /// 기본값
    pub id: i32,
    /// 영문
    pub name: String,
    /// 한글
    pub drop_name: String,
    /// 드랍 구역
    pub drop_area: String,
    /// 아이템 종류1
    pub drop_item_ids: Vec<i32>,
    /// 아이템 최소 수량 값
    pub drop_item_min: Vec<i32>,
    /// 아이템 최대 수량 값
    pub drop_item_max: Vec<i32>,
    /// 확률
    pub item_drop_rate: Vec<f32>,
}

impl DropGroupTableRecord {
}

/// Every row of DropGroupTable.
#[derive(Clone, Debug, Default)]
pub struct DropGroupTableTable {
    records: Vec<DropGroupTableRecord>,
    by_id: HashMap<i32, usize>,
}

impl DropGroupTableTable {
    /// Every row, in the order the sheet declared them.
    pub fn records(&self) -> &[DropGroupTableRecord] {
        &self.records
    }

    /// The row with this Id, or None when the table has none.
    ///
    /// The lookup to reach for when a missing row is an ordinary answer - an optional
    /// reference, a key that came from user input.
    pub fn find_by_id(&self, key: i32) -> Option<&DropGroupTableRecord> {
        self.by_id.get(&key).map(|position| &self.records[*position])
    }

    /// The row with this Id, or an error naming what was missing.
    ///
    /// For a key that has to be there - one from another table, or a constant. Rust has no
    /// exception to throw, so where the other languages name a throw this one names the
    /// error it returns.
    pub fn get_by_id_or_error(&self, key: i32) -> sheetman::Result<&DropGroupTableRecord> {
        self.by_id
            .get(&key)
            .map(|position| &self.records[*position])
            .ok_or_else(|| sheetman::Error::RecordNotFound {
                table: "DropGroupTable",
                field: "Id",
                key: format!("{:?}", key),
            })
    }

    /// Whether the table holds a row with this Id.
    pub fn contains_id(&self, key: i32) -> bool {
        self.by_id.contains_key(&key)
    }

    /// Loads the table from a .table file written by SheetMan.
    pub fn read(&mut self, filename: &Path) -> sheetman::Result<()> {
        let data = sheetman::read_all_bytes(filename)?;
        let mut reader = sheetman::Reader::new(&data);

        // Column by column, matched by tag rather than position: a column this build
        // does not know is skipped by its block length, and one whose type changed
        // incompatibly fails naming the field.
        let header = sheetman::read_table_header(&mut reader)?;
        let count = header.row_count.max(0) as usize;

        // Read into rows of its own and assigned at the end. Reading a table that is
        // already loaded is a refresh, and one that turns out to be unreadable has to
        // leave the rows already there alone - `?` returns before anything is replaced.
        let mut records = vec![DropGroupTableRecord::default(); count];

        for column in &header.columns {
            let block_end = reader.position() + column.byte_length.max(0) as usize;

            match column.tag {
                1 => {
                    sheetman::check_column(column, "DropGroupTable.Id", sheetman::KIND_SCALAR, 1, &[sheetman::ELEMENT_I32, sheetman::ELEMENT_VARINT])?;
                    for record in records.iter_mut() {
                        record.id = reader.read_i32_as(column.element)?;
                    }
                }
                2 => {
                    sheetman::check_column(column, "DropGroupTable.Name", sheetman::KIND_SCALAR, 1, &[sheetman::ELEMENT_STRING])?;
                    for record in records.iter_mut() {
                        record.name = reader.read_string()?;
                    }
                }
                3 => {
                    sheetman::check_column(column, "DropGroupTable.DropName", sheetman::KIND_SCALAR, 1, &[sheetman::ELEMENT_STRING])?;
                    for record in records.iter_mut() {
                        record.drop_name = reader.read_string()?;
                    }
                }
                4 => {
                    sheetman::check_column(column, "DropGroupTable.DropArea", sheetman::KIND_SCALAR, 1, &[sheetman::ELEMENT_STRING])?;
                    for record in records.iter_mut() {
                        record.drop_area = reader.read_string()?;
                    }
                }
                5 => {
                    sheetman::check_column(column, "DropGroupTable.DropItemIds", sheetman::KIND_VAR_ARRAY, 0, &[sheetman::ELEMENT_I32, sheetman::ELEMENT_VARINT])?;
                    for record in records.iter_mut() {
                        let element_count = reader.read_counter32()?.max(0) as usize;
                        record.drop_item_ids = Vec::with_capacity(element_count.min(65536));
                        for _ in 0..element_count {
                            record.drop_item_ids.push(reader.read_i32_as(column.element)?);
                        }
                    }
                }
                6 => {
                    sheetman::check_column(column, "DropGroupTable.DropItemMin", sheetman::KIND_VAR_ARRAY, 0, &[sheetman::ELEMENT_I32, sheetman::ELEMENT_VARINT])?;
                    for record in records.iter_mut() {
                        let element_count = reader.read_counter32()?.max(0) as usize;
                        record.drop_item_min = Vec::with_capacity(element_count.min(65536));
                        for _ in 0..element_count {
                            record.drop_item_min.push(reader.read_i32_as(column.element)?);
                        }
                    }
                }
                7 => {
                    sheetman::check_column(column, "DropGroupTable.DropItemMax", sheetman::KIND_VAR_ARRAY, 0, &[sheetman::ELEMENT_I32, sheetman::ELEMENT_VARINT])?;
                    for record in records.iter_mut() {
                        let element_count = reader.read_counter32()?.max(0) as usize;
                        record.drop_item_max = Vec::with_capacity(element_count.min(65536));
                        for _ in 0..element_count {
                            record.drop_item_max.push(reader.read_i32_as(column.element)?);
                        }
                    }
                }
                8 => {
                    sheetman::check_column(column, "DropGroupTable.ItemDropRate", sheetman::KIND_VAR_ARRAY, 0, &[sheetman::ELEMENT_F32])?;
                    for record in records.iter_mut() {
                        let element_count = reader.read_counter32()?.max(0) as usize;
                        record.item_drop_rate = Vec::with_capacity(element_count.min(65536));
                        for _ in 0..element_count {
                            record.item_drop_rate.push(reader.read_f32()?);
                        }
                    }
                }
                _ => {
                    // A column added after this code was generated.
                    reader.skip(column.byte_length)?;
                }
            }

            sheetman::check_block_end(&reader, column, block_end)?;
        }

        let by_id = records
            .iter()
            .enumerate()
            .map(|(position, record)| (record.id.clone(), position))
            .collect();

        // Published, now that every column read.
        self.records = records;
        self.by_id = by_id;

        Ok(())
    }
}
