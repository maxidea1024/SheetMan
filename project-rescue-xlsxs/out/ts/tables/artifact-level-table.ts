// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/lite-binary-reader'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  name: string
  nameKR: string
  level: number
  characterEXP: string
  accumulatedEXP: string
  aTKGrowth: number
  dEFGrowth: number
  hPGrowth: number
}

// Generated from project-rescue-xlsxs/MasterData.xlsx : ArtifactLevelTable : A1
export class ArtifactLevelTableRecord {
  /** Default constructor */
  constructor() {
  }

  /** ArtifactLevelTable */
  public get id(): number { return this._id }

  /** 영문 */
  public get name(): string { return this._name }

  /** 한글이름 */
  public get nameKR(): string { return this._nameKR }

  /** 레벨 */
  public get level(): number { return this._level }

  /** 필요 경험치 */
  public get characterEXP(): bigint { return this._characterEXP }

  /** 누적 경험치 */
  public get accumulatedEXP(): bigint { return this._accumulatedEXP }

  /** 공격력 고정 비율 */
  public get aTKGrowth(): number { return this._aTKGrowth }

  /** 방어력 고정 비율 */
  public get dEFGrowth(): number { return this._dEFGrowth }

  /** 체력 증가 비율 */
  public get hPGrowth(): number { return this._hPGrowth }

  public _id: number = 0
  public _name: string = ''
  public _nameKR: string = ''
  public _level: number = 0
  public _characterEXP: bigint = 0n
  public _accumulatedEXP: bigint = 0n
  public _aTKGrowth: number = 0
  public _dEFGrowth: number = 0
  public _hPGrowth: number = 0

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._name = dataRow.name
    this._nameKR = dataRow.nameKR
    this._level = dataRow.level
    this._characterEXP = BigInt(dataRow.characterEXP)
    this._accumulatedEXP = BigInt(dataRow.accumulatedEXP)
    this._aTKGrowth = Math.fround(dataRow.aTKGrowth)
    this._dEFGrowth = Math.fround(dataRow.dEFGrowth)
    this._hPGrowth = Math.fround(dataRow.hPGrowth)
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._name = dataRow[offset++]
    this._nameKR = dataRow[offset++]
    this._level = dataRow[offset++]
    this._characterEXP = BigInt(dataRow[offset++])
    this._accumulatedEXP = BigInt(dataRow[offset++])
    this._aTKGrowth = Math.fround(dataRow[offset++])
    this._dEFGrowth = Math.fround(dataRow[offset++])
    this._hPGrowth = Math.fround(dataRow[offset++])
  }
}

// Generated from project-rescue-xlsxs/MasterData.xlsx : ArtifactLevelTable : A1
export class ArtifactLevelTableTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): ArtifactLevelTableRecord[] { return this._records }
  private _records: ArtifactLevelTableRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, ArtifactLevelTableRecord> { return this._recordsById }
  private _recordsById: Map<number, ArtifactLevelTableRecord> = new Map<number, ArtifactLevelTableRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): ArtifactLevelTableRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): ArtifactLevelTableRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "ArtifactLevelTable" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: ArtifactLevelTableRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new ArtifactLevelTableRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new ArtifactLevelTableRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .table file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.LiteBinaryReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: ArtifactLevelTableRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new ArtifactLevelTableRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'ArtifactLevelTable.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = reader.readI32As(column.element)
          }
          break
        case 2:
          sheetman.checkColumn(column, 'ArtifactLevelTable.Name', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._name = reader.readString()
          }
          break
        case 3:
          sheetman.checkColumn(column, 'ArtifactLevelTable.NameKR', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._nameKR = reader.readString()
          }
          break
        case 4:
          sheetman.checkColumn(column, 'ArtifactLevelTable.Level', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._level = reader.readI32As(column.element)
          }
          break
        case 5:
          sheetman.checkColumn(column, 'ArtifactLevelTable.CharacterEXP', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I64, sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._characterEXP = reader.readI64As(column.element)
          }
          break
        case 6:
          sheetman.checkColumn(column, 'ArtifactLevelTable.AccumulatedEXP', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I64, sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._accumulatedEXP = reader.readI64As(column.element)
          }
          break
        case 7:
          sheetman.checkColumn(column, 'ArtifactLevelTable.ATKGrowth', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._aTKGrowth = reader.readFloat()
          }
          break
        case 8:
          sheetman.checkColumn(column, 'ArtifactLevelTable.DEFGrowth', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._dEFGrowth = reader.readFloat()
          }
          break
        case 9:
          sheetman.checkColumn(column, 'ArtifactLevelTable.HPGrowth', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._hPGrowth = reader.readFloat()
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: ArtifactLevelTableRecord[]): void {
    const recordsById = new Map<number, ArtifactLevelTableRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
