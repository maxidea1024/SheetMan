// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/lite-binary-reader'

// Automatically import to handle external type references.
import { GradeType } from '../enums/grade-type'
import { CurrencyType } from '../enums/currency-type'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  name: string
  nameKR: string
  gradeType: GradeType
  transcendStep: number
  maxLevel: number
  materialType: CurrencyType
  materialCount: number
  powerMultiplier: number
  nextStepID: number
}

// Generated from project-rescue-xlsxs\MasterData.xlsx : ArtifactTranscendenceTable : A1
export class ArtifactTranscendenceTableRecord {
  /** Default constructor */
  constructor() {
  }

  /** Artifact_Transcend_ID */
  public get id(): number { return this._id }

  /** 영문 */
  public get name(): string { return this._name }

  /** 한글이름 */
  public get nameKR(): string { return this._nameKR }

  /** 등급 */
  public get gradeType(): GradeType { return this._gradeType }

  /** 초월 단계 */
  public get transcendStep(): number { return this._transcendStep }

  /** 초월 최대 레벨 */
  public get maxLevel(): number { return this._maxLevel }

  /** 초월 특수 재료 타입 */
  public get materialType(): CurrencyType { return this._materialType }

  /** 초월 특수 재료 개수 */
  public get materialCount(): number { return this._materialCount }

  /** 초월시 스탯 증가율 */
  public get powerMultiplier(): number { return this._powerMultiplier }

  /** 다음 성급 Index */
  public get nextStepID(): number { return this._nextStepID }

  public _id: number = 0
  public _name: string = ''
  public _nameKR: string = ''
  public _gradeType: GradeType = 0 as GradeType
  public _transcendStep: number = 0
  public _maxLevel: number = 0
  public _materialType: CurrencyType = 0 as CurrencyType
  public _materialCount: number = 0
  public _powerMultiplier: number = 0
  public _nextStepID: number = 0

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._name = dataRow.name
    this._nameKR = dataRow.nameKR
    this._gradeType = dataRow.gradeType
    this._transcendStep = dataRow.transcendStep
    this._maxLevel = dataRow.maxLevel
    this._materialType = dataRow.materialType
    this._materialCount = dataRow.materialCount
    this._powerMultiplier = Math.fround(dataRow.powerMultiplier)
    this._nextStepID = dataRow.nextStepID
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._name = dataRow[offset++]
    this._nameKR = dataRow[offset++]
    this._gradeType = dataRow[offset++]
    this._transcendStep = dataRow[offset++]
    this._maxLevel = dataRow[offset++]
    this._materialType = dataRow[offset++]
    this._materialCount = dataRow[offset++]
    this._powerMultiplier = Math.fround(dataRow[offset++])
    this._nextStepID = dataRow[offset++]
  }
}

// Generated from project-rescue-xlsxs\MasterData.xlsx : ArtifactTranscendenceTable : A1
export class ArtifactTranscendenceTableTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): ArtifactTranscendenceTableRecord[] { return this._records }
  private _records: ArtifactTranscendenceTableRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, ArtifactTranscendenceTableRecord> { return this._recordsById }
  private _recordsById: Map<number, ArtifactTranscendenceTableRecord> = new Map<number, ArtifactTranscendenceTableRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): ArtifactTranscendenceTableRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): ArtifactTranscendenceTableRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "ArtifactTranscendenceTable" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: ArtifactTranscendenceTableRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new ArtifactTranscendenceTableRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new ArtifactTranscendenceTableRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .table file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.LiteBinaryReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: ArtifactTranscendenceTableRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new ArtifactTranscendenceTableRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'ArtifactTranscendenceTable.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = reader.readI32As(column.element)
          }
          break
        case 2:
          sheetman.checkColumn(column, 'ArtifactTranscendenceTable.Name', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._name = reader.readString()
          }
          break
        case 3:
          sheetman.checkColumn(column, 'ArtifactTranscendenceTable.NameKR', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._nameKR = reader.readString()
          }
          break
        case 4:
          sheetman.checkColumn(column, 'ArtifactTranscendenceTable.GradeType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._gradeType = reader.readEnum() as GradeType
          }
          break
        case 5:
          sheetman.checkColumn(column, 'ArtifactTranscendenceTable.TranscendStep', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._transcendStep = reader.readI32As(column.element)
          }
          break
        case 6:
          sheetman.checkColumn(column, 'ArtifactTranscendenceTable.MaxLevel', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._maxLevel = reader.readI32As(column.element)
          }
          break
        case 7:
          sheetman.checkColumn(column, 'ArtifactTranscendenceTable.MaterialType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._materialType = reader.readEnum() as CurrencyType
          }
          break
        case 8:
          sheetman.checkColumn(column, 'ArtifactTranscendenceTable.MaterialCount', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._materialCount = reader.readI32As(column.element)
          }
          break
        case 9:
          sheetman.checkColumn(column, 'ArtifactTranscendenceTable.PowerMultiplier', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._powerMultiplier = reader.readFloat()
          }
          break
        case 10:
          sheetman.checkColumn(column, 'ArtifactTranscendenceTable.NextStepID', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._nextStepID = reader.readI32As(column.element)
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: ArtifactTranscendenceTableRecord[]): void {
    const recordsById = new Map<number, ArtifactTranscendenceTableRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
