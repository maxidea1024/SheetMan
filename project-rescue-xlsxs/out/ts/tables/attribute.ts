// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/scb-reader'

// Automatically import to handle external type references.
import { AttributeType } from '../enums/attribute-type'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  name: string
  attributeName: string
  attributeType: AttributeType
  targetAttributeType: AttributeType
  damageValueRate: number
  isVisible: boolean
  iconPath: string
}

// Generated from project-rescue-xlsxs/MasterData.xlsx : AttributeTable : A1
export class AttributeRecord {
  /** Default constructor */
  constructor() {
  }

  /** Attribute_Table */
  public get id(): number { return this._id }

  /** 영문명 */
  public get name(): string { return this._name }

  /** 한글명 */
  public get attributeName(): string { return this._attributeName }

  /** 내 속성 타입 */
  public get attributeType(): AttributeType { return this._attributeType }

  /** 타겟 속성 타입 */
  public get targetAttributeType(): AttributeType { return this._targetAttributeType }

  /** 증가되는 값(%) */
  public get damageValueRate(): number { return this._damageValueRate }

  /** 속성 표시 유무 */
  public get isVisible(): boolean { return this._isVisible }

  /** 속성 아이콘 경로 */
  public get iconPath(): string { return this._iconPath }

  public _id: number = 0
  public _name: string = ''
  public _attributeName: string = ''
  public _attributeType: AttributeType = 0 as AttributeType
  public _targetAttributeType: AttributeType = 0 as AttributeType
  public _damageValueRate: number = 0
  public _isVisible: boolean = false
  public _iconPath: string = ''

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._name = dataRow.name
    this._attributeName = dataRow.attributeName
    this._attributeType = dataRow.attributeType
    this._targetAttributeType = dataRow.targetAttributeType
    this._damageValueRate = Math.fround(dataRow.damageValueRate)
    this._isVisible = dataRow.isVisible
    this._iconPath = dataRow.iconPath
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._name = dataRow[offset++]
    this._attributeName = dataRow[offset++]
    this._attributeType = dataRow[offset++]
    this._targetAttributeType = dataRow[offset++]
    this._damageValueRate = Math.fround(dataRow[offset++])
    this._isVisible = dataRow[offset++]
    this._iconPath = dataRow[offset++]
  }
}

// Generated from project-rescue-xlsxs/MasterData.xlsx : AttributeTable : A1
export class AttributeTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): AttributeRecord[] { return this._records }
  private _records: AttributeRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, AttributeRecord> { return this._recordsById }
  private _recordsById: Map<number, AttributeRecord> = new Map<number, AttributeRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): AttributeRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): AttributeRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "Attribute" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: AttributeRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new AttributeRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new AttributeRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .scb file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.ScbReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)
    let cursor: sheetman.ScbColumnCursor

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: AttributeRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new AttributeRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'Attribute.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Attribute.Id')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = cursor.nextI32()
          }
          break
        case 2:
          sheetman.checkColumn(column, 'Attribute.Name', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Attribute.Name')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._name = cursor.nextString()
          }
          break
        case 3:
          sheetman.checkColumn(column, 'Attribute.AttributeName', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Attribute.AttributeName')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._attributeName = cursor.nextString()
          }
          break
        case 4:
          sheetman.checkColumn(column, 'Attribute.AttributeType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Attribute.AttributeType')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._attributeType = cursor.nextI32() as AttributeType
          }
          break
        case 5:
          sheetman.checkColumn(column, 'Attribute.TargetAttributeType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Attribute.TargetAttributeType')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._targetAttributeType = cursor.nextI32() as AttributeType
          }
          break
        case 6:
          sheetman.checkColumn(column, 'Attribute.DamageValueRate', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Attribute.DamageValueRate')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._damageValueRate = cursor.nextF32()
          }
          break
        case 7:
          sheetman.checkColumn(column, 'Attribute.IsVisible', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_BOOL])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Attribute.IsVisible')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._isVisible = cursor.nextBool()
          }
          break
        case 8:
          sheetman.checkColumn(column, 'Attribute.IconPath', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Attribute.IconPath')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._iconPath = cursor.nextString()
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: AttributeRecord[]): void {
    const recordsById = new Map<number, AttributeRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
