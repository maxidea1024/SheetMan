// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/lite-binary-reader'

// Automatically import to handle external type references.
import { SkillType } from '../enums/skill-type'
import { StatType } from '../enums/stat-type'
import { AttributeType } from '../enums/attribute-type'
import { TargetType } from '../enums/target-type'
import { BuffConditionType } from '../enums/buff-condition-type'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  buffName: string
  name: string
  skillType: SkillType
  statType: StatType
  priority: number
  attributeType: AttributeType
  targetType: TargetType
  buffConditionType: BuffConditionType
  buffValue: number
  buffValueRate: number
  buffTime: number
  buffTickTime: number
  buffMaxStack: number
  buffCoolDownTime: number
  isVisible: boolean
  iconPath: string
  description: string
}

// Generated from project-rescue-xlsxs\MasterData.xlsx : BuffTable : A1
export class BuffTableRecord {
  /** Default constructor */
  constructor() {
  }

  /** ID */
  public get id(): number { return this._id }

  /** SkillTable에서 Name을 불러와서 사용 */
  public get buffName(): string { return this._buffName }

  /** 한글 명 */
  public get name(): string { return this._name }

  /** 버프 타입_메모확인 */
  public get skillType(): SkillType { return this._skillType }

  /** 어떤 종류의 버프 인지 */
  public get statType(): StatType { return this._statType }

  /** 버프 우선 적용 순위 */
  public get priority(): number { return this._priority }

  /** 버프 속성 */
  public get attributeType(): AttributeType { return this._attributeType }

  /** 타겟 설정 */
  public get targetType(): TargetType { return this._targetType }

  /** 상태에 따른 발동 조건 */
  public get buffConditionType(): BuffConditionType { return this._buffConditionType }

  /** 증가되는 값(상수) */
  public get buffValue(): number { return this._buffValue }

  /** 증가되는 값(%) */
  public get buffValueRate(): number { return this._buffValueRate }

  /** 버프 시간 */
  public get buffTime(): number { return this._buffTime }

  /** 버프 틱 시간 */
  public get buffTickTime(): number { return this._buffTickTime }

  /** 버프 최대 스택 */
  public get buffMaxStack(): number { return this._buffMaxStack }

  /** 버프 쿨타임 1초 = 1000 */
  public get buffCoolDownTime(): number { return this._buffCoolDownTime }

  /** 버프 표시 유무 FALSE/TRUE */
  public get isVisible(): boolean { return this._isVisible }

  /** 버프 아이콘 경로 */
  public get iconPath(): string { return this._iconPath }

  /** 버프 튤팁 */
  public get description(): string { return this._description }

  public _id: number = 0
  public _buffName: string = ''
  public _name: string = ''
  public _skillType: SkillType = 0 as SkillType
  public _statType: StatType = 0 as StatType
  public _priority: number = 0
  public _attributeType: AttributeType = 0 as AttributeType
  public _targetType: TargetType = 0 as TargetType
  public _buffConditionType: BuffConditionType = 0 as BuffConditionType
  public _buffValue: number = 0
  public _buffValueRate: number = 0
  public _buffTime: number = 0
  public _buffTickTime: number = 0
  public _buffMaxStack: number = 0
  public _buffCoolDownTime: number = 0
  public _isVisible: boolean = false
  public _iconPath: string = ''
  public _description: string = ''

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._buffName = dataRow.buffName
    this._name = dataRow.name
    this._skillType = dataRow.skillType
    this._statType = dataRow.statType
    this._priority = dataRow.priority
    this._attributeType = dataRow.attributeType
    this._targetType = dataRow.targetType
    this._buffConditionType = dataRow.buffConditionType
    this._buffValue = dataRow.buffValue
    this._buffValueRate = Math.fround(dataRow.buffValueRate)
    this._buffTime = dataRow.buffTime
    this._buffTickTime = dataRow.buffTickTime
    this._buffMaxStack = dataRow.buffMaxStack
    this._buffCoolDownTime = Math.fround(dataRow.buffCoolDownTime)
    this._isVisible = dataRow.isVisible
    this._iconPath = dataRow.iconPath
    this._description = dataRow.description
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._buffName = dataRow[offset++]
    this._name = dataRow[offset++]
    this._skillType = dataRow[offset++]
    this._statType = dataRow[offset++]
    this._priority = dataRow[offset++]
    this._attributeType = dataRow[offset++]
    this._targetType = dataRow[offset++]
    this._buffConditionType = dataRow[offset++]
    this._buffValue = dataRow[offset++]
    this._buffValueRate = Math.fround(dataRow[offset++])
    this._buffTime = dataRow[offset++]
    this._buffTickTime = dataRow[offset++]
    this._buffMaxStack = dataRow[offset++]
    this._buffCoolDownTime = Math.fround(dataRow[offset++])
    this._isVisible = dataRow[offset++]
    this._iconPath = dataRow[offset++]
    this._description = dataRow[offset++]
  }
}

// Generated from project-rescue-xlsxs\MasterData.xlsx : BuffTable : A1
export class BuffTableTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): BuffTableRecord[] { return this._records }
  private _records: BuffTableRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, BuffTableRecord> { return this._recordsById }
  private _recordsById: Map<number, BuffTableRecord> = new Map<number, BuffTableRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): BuffTableRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): BuffTableRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "BuffTable" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: BuffTableRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new BuffTableRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new BuffTableRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .table file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.LiteBinaryReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: BuffTableRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new BuffTableRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'BuffTable.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = reader.readI32As(column.element)
          }
          break
        case 2:
          sheetman.checkColumn(column, 'BuffTable.BuffName', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._buffName = reader.readString()
          }
          break
        case 3:
          sheetman.checkColumn(column, 'BuffTable.Name', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._name = reader.readString()
          }
          break
        case 4:
          sheetman.checkColumn(column, 'BuffTable.SkillType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._skillType = reader.readEnum() as SkillType
          }
          break
        case 5:
          sheetman.checkColumn(column, 'BuffTable.StatType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._statType = reader.readEnum() as StatType
          }
          break
        case 6:
          sheetman.checkColumn(column, 'BuffTable.Priority', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._priority = reader.readI32As(column.element)
          }
          break
        case 7:
          sheetman.checkColumn(column, 'BuffTable.AttributeType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._attributeType = reader.readEnum() as AttributeType
          }
          break
        case 8:
          sheetman.checkColumn(column, 'BuffTable.TargetType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._targetType = reader.readEnum() as TargetType
          }
          break
        case 9:
          sheetman.checkColumn(column, 'BuffTable.BuffConditionType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._buffConditionType = reader.readEnum() as BuffConditionType
          }
          break
        case 10:
          sheetman.checkColumn(column, 'BuffTable.BuffValue', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._buffValue = reader.readI32As(column.element)
          }
          break
        case 11:
          sheetman.checkColumn(column, 'BuffTable.BuffValueRate', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._buffValueRate = reader.readFloat()
          }
          break
        case 12:
          sheetman.checkColumn(column, 'BuffTable.BuffTime', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._buffTime = reader.readI32As(column.element)
          }
          break
        case 13:
          sheetman.checkColumn(column, 'BuffTable.BuffTickTime', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._buffTickTime = reader.readI32As(column.element)
          }
          break
        case 14:
          sheetman.checkColumn(column, 'BuffTable.BuffMaxStack', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._buffMaxStack = reader.readI32As(column.element)
          }
          break
        case 15:
          sheetman.checkColumn(column, 'BuffTable.BuffCoolDownTime', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._buffCoolDownTime = reader.readFloat()
          }
          break
        case 16:
          sheetman.checkColumn(column, 'BuffTable.IsVisible', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_BOOL])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._isVisible = reader.readBool()
          }
          break
        case 17:
          sheetman.checkColumn(column, 'BuffTable.IconPath', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._iconPath = reader.readString()
          }
          break
        case 18:
          sheetman.checkColumn(column, 'BuffTable.Description', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._description = reader.readString()
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: BuffTableRecord[]): void {
    const recordsById = new Map<number, BuffTableRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
