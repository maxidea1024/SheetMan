// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/lite-binary-reader'

// Automatically import to handle external type references.
import { ShopType } from '../enums/shop-type'
import { CycleType } from '../enums/cycle-type'
import { CurrencyType } from '../enums/currency-type'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  name: string
  productName: string
  shopType: ShopType
  shopSlotID: number
  priority: number
  itemID: number[]
  itemValue: number[]
  cycleType: CycleType
  limitValue: number
  currencyType: CurrencyType
  priceValue: number
  conditionID: number
  iconPath: string
}

// Generated from project-rescue-xlsxs/ShopData.xlsx : CashShopTable : A1
export class CashShopRecord {
  /** Default constructor */
  constructor() {
  }

  /** CashShop_Table */
  public get id(): number { return this._id }

  /** 영문명 */
  public get name(): string { return this._name }

  /** 한글명 */
  public get productName(): string { return this._productName }

  /** 상점 타입 */
  public get shopType(): ShopType { return this._shopType }

  /** 슬롯 ID */
  public get shopSlotID(): number { return this._shopSlotID }

  /** 출력 우선순위 */
  public get priority(): number { return this._priority }

  /** 포함 아이템 ID */
  public get itemID(): number[] { return this._itemID }

  /** 포함 아이템 수 */
  public get itemValue(): number[] { return this._itemValue }

  /** 구매 주기 타입 */
  public get cycleType(): CycleType { return this._cycleType }

  /** 구매 제한 횟수 */
  public get limitValue(): number { return this._limitValue }

  /** 소모 재화 */
  public get currencyType(): CurrencyType { return this._currencyType }

  /** 가격값 */
  public get priceValue(): number { return this._priceValue }

  /** 해금 조건 ID */
  public get conditionID(): number { return this._conditionID }

  /** 아이콘 경로 */
  public get iconPath(): string { return this._iconPath }

  public _id: number = 0
  public _name: string = ''
  public _productName: string = ''
  public _shopType: ShopType = 0 as ShopType
  public _shopSlotID: number = 0
  public _priority: number = 0
  public _itemID: number[] = []
  public _itemValue: number[] = []
  public _cycleType: CycleType = 0 as CycleType
  public _limitValue: number = 0
  public _currencyType: CurrencyType = 0 as CurrencyType
  public _priceValue: number = 0
  public _conditionID: number = 0
  public _iconPath: string = ''

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._name = dataRow.name
    this._productName = dataRow.productName
    this._shopType = dataRow.shopType
    this._shopSlotID = dataRow.shopSlotID
    this._priority = dataRow.priority
    this._itemID = dataRow.itemID
    this._itemValue = dataRow.itemValue
    this._cycleType = dataRow.cycleType
    this._limitValue = dataRow.limitValue
    this._currencyType = dataRow.currencyType
    this._priceValue = dataRow.priceValue
    this._conditionID = dataRow.conditionID
    this._iconPath = dataRow.iconPath
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._name = dataRow[offset++]
    this._productName = dataRow[offset++]
    this._shopType = dataRow[offset++]
    this._shopSlotID = dataRow[offset++]
    this._priority = dataRow[offset++]
    this._itemID = dataRow[offset++]
    this._itemValue = dataRow[offset++]
    this._cycleType = dataRow[offset++]
    this._limitValue = dataRow[offset++]
    this._currencyType = dataRow[offset++]
    this._priceValue = dataRow[offset++]
    this._conditionID = dataRow[offset++]
    this._iconPath = dataRow[offset++]
  }
}

// Generated from project-rescue-xlsxs/ShopData.xlsx : CashShopTable : A1
export class CashShopTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): CashShopRecord[] { return this._records }
  private _records: CashShopRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, CashShopRecord> { return this._recordsById }
  private _recordsById: Map<number, CashShopRecord> = new Map<number, CashShopRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): CashShopRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): CashShopRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "CashShop" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: CashShopRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new CashShopRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new CashShopRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .scb file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.LiteBinaryReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: CashShopRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new CashShopRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'CashShop.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = reader.readI32As(column.element)
          }
          break
        case 2:
          sheetman.checkColumn(column, 'CashShop.Name', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._name = reader.readString()
          }
          break
        case 3:
          sheetman.checkColumn(column, 'CashShop.ProductName', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._productName = reader.readString()
          }
          break
        case 4:
          sheetman.checkColumn(column, 'CashShop.ShopType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._shopType = reader.readEnum() as ShopType
          }
          break
        case 5:
          sheetman.checkColumn(column, 'CashShop.ShopSlotID', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._shopSlotID = reader.readI32As(column.element)
          }
          break
        case 6:
          sheetman.checkColumn(column, 'CashShop.Priority', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._priority = reader.readI32As(column.element)
          }
          break
        case 7:
          sheetman.checkColumn(column, 'CashShop.ItemID', sheetman.KIND_VAR_ARRAY, 0, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            const elementCount = reader.readCounter32()
            record._itemID = []
            for (let j = 0; j < elementCount; ++j)
              record._itemID.push(reader.readI32As(column.element))
          }
          break
        case 8:
          sheetman.checkColumn(column, 'CashShop.ItemValue', sheetman.KIND_VAR_ARRAY, 0, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            const elementCount = reader.readCounter32()
            record._itemValue = []
            for (let j = 0; j < elementCount; ++j)
              record._itemValue.push(reader.readI32As(column.element))
          }
          break
        case 9:
          sheetman.checkColumn(column, 'CashShop.CycleType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._cycleType = reader.readEnum() as CycleType
          }
          break
        case 10:
          sheetman.checkColumn(column, 'CashShop.LimitValue', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._limitValue = reader.readI32As(column.element)
          }
          break
        case 11:
          sheetman.checkColumn(column, 'CashShop.CurrencyType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._currencyType = reader.readEnum() as CurrencyType
          }
          break
        case 12:
          sheetman.checkColumn(column, 'CashShop.PriceValue', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._priceValue = reader.readI32As(column.element)
          }
          break
        case 13:
          sheetman.checkColumn(column, 'CashShop.ConditionID', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._conditionID = reader.readI32As(column.element)
          }
          break
        case 14:
          sheetman.checkColumn(column, 'CashShop.IconPath', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._iconPath = reader.readString()
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: CashShopRecord[]): void {
    const recordsById = new Map<number, CashShopRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
