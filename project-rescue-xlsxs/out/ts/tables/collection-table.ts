// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/lite-binary-reader'

// Automatically import to handle external type references.
import { CollectionTabType } from '../enums/collection-tab-type'
import { CurrencyType } from '../enums/currency-type'
import { StatType } from '../enums/stat-type'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  name: string
  characterID: number
  tabType: CollectionTabType
  conditionID: number
  rewardType: CurrencyType
  rewardValue: number
  rewardStatType1: StatType
  rewardStatRate: number
  rewardStatType2: StatType
  rewardStatValue: number
  shortCutID: number
}

// Generated from project-rescue-xlsxs/CollectionData.xlsx : CollectionTable : A1
export class CollectionTableRecord {
  /** Default constructor */
  constructor() {
  }

  /** CollectionID */
  public get id(): number { return this._id }

  /** 영문 */
  public get name(): string { return this._name }

  /** 캐릭터ID */
  public get characterID(): number { return this._characterID }

  /** 보상 UI분기 타입 */
  public get tabType(): CollectionTabType { return this._tabType }

  /** 조건 값 */
  public get conditionID(): number { return this._conditionID }

  /** 재화 보상 타입 */
  public get rewardType(): CurrencyType { return this._rewardType }

  /** 재화 보상 값 */
  public get rewardValue(): number { return this._rewardValue }

  /** 스탯 보상 타입 */
  public get rewardStatType1(): StatType { return this._rewardStatType1 }

  /** 스탯 보상(%) */
  public get rewardStatRate(): number { return this._rewardStatRate }

  /** 스탯 보상 타입 */
  public get rewardStatType2(): StatType { return this._rewardStatType2 }

  /** 스탯 보상(+) */
  public get rewardStatValue(): number { return this._rewardStatValue }

  /** 바로가기 ID */
  public get shortCutID(): number { return this._shortCutID }

  public _id: number = 0
  public _name: string = ''
  public _characterID: number = 0
  public _tabType: CollectionTabType = 0 as CollectionTabType
  public _conditionID: number = 0
  public _rewardType: CurrencyType = 0 as CurrencyType
  public _rewardValue: number = 0
  public _rewardStatType1: StatType = 0 as StatType
  public _rewardStatRate: number = 0
  public _rewardStatType2: StatType = 0 as StatType
  public _rewardStatValue: number = 0
  public _shortCutID: number = 0

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._name = dataRow.name
    this._characterID = dataRow.characterID
    this._tabType = dataRow.tabType
    this._conditionID = dataRow.conditionID
    this._rewardType = dataRow.rewardType
    this._rewardValue = dataRow.rewardValue
    this._rewardStatType1 = dataRow.rewardStatType1
    this._rewardStatRate = Math.fround(dataRow.rewardStatRate)
    this._rewardStatType2 = dataRow.rewardStatType2
    this._rewardStatValue = dataRow.rewardStatValue
    this._shortCutID = dataRow.shortCutID
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._name = dataRow[offset++]
    this._characterID = dataRow[offset++]
    this._tabType = dataRow[offset++]
    this._conditionID = dataRow[offset++]
    this._rewardType = dataRow[offset++]
    this._rewardValue = dataRow[offset++]
    this._rewardStatType1 = dataRow[offset++]
    this._rewardStatRate = Math.fround(dataRow[offset++])
    this._rewardStatType2 = dataRow[offset++]
    this._rewardStatValue = dataRow[offset++]
    this._shortCutID = dataRow[offset++]
  }
}

// Generated from project-rescue-xlsxs/CollectionData.xlsx : CollectionTable : A1
export class CollectionTableTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): CollectionTableRecord[] { return this._records }
  private _records: CollectionTableRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, CollectionTableRecord> { return this._recordsById }
  private _recordsById: Map<number, CollectionTableRecord> = new Map<number, CollectionTableRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): CollectionTableRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): CollectionTableRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "CollectionTable" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: CollectionTableRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new CollectionTableRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new CollectionTableRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .table file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.LiteBinaryReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: CollectionTableRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new CollectionTableRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'CollectionTable.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = reader.readI32As(column.element)
          }
          break
        case 2:
          sheetman.checkColumn(column, 'CollectionTable.Name', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._name = reader.readString()
          }
          break
        case 3:
          sheetman.checkColumn(column, 'CollectionTable.CharacterID', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._characterID = reader.readI32As(column.element)
          }
          break
        case 4:
          sheetman.checkColumn(column, 'CollectionTable.TabType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._tabType = reader.readEnum() as CollectionTabType
          }
          break
        case 5:
          sheetman.checkColumn(column, 'CollectionTable.ConditionID', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._conditionID = reader.readI32As(column.element)
          }
          break
        case 6:
          sheetman.checkColumn(column, 'CollectionTable.RewardType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._rewardType = reader.readEnum() as CurrencyType
          }
          break
        case 7:
          sheetman.checkColumn(column, 'CollectionTable.RewardValue', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._rewardValue = reader.readI32As(column.element)
          }
          break
        case 8:
          sheetman.checkColumn(column, 'CollectionTable.RewardStatType1', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._rewardStatType1 = reader.readEnum() as StatType
          }
          break
        case 9:
          sheetman.checkColumn(column, 'CollectionTable.RewardStatRate', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._rewardStatRate = reader.readFloat()
          }
          break
        case 10:
          sheetman.checkColumn(column, 'CollectionTable.RewardStatType2', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._rewardStatType2 = reader.readEnum() as StatType
          }
          break
        case 11:
          sheetman.checkColumn(column, 'CollectionTable.RewardStatValue', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._rewardStatValue = reader.readI32As(column.element)
          }
          break
        case 12:
          sheetman.checkColumn(column, 'CollectionTable.ShortCutID', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._shortCutID = reader.readI32As(column.element)
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: CollectionTableRecord[]): void {
    const recordsById = new Map<number, CollectionTableRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
