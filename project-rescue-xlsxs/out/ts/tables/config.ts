// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/scb-reader'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  index: number
  id: string
  category: string
  dataType: string
  defaultValue: string
  description: string
}

// Generated from project-rescue-xlsxs/MasterData.xlsx : ConfigTable : A1
export class ConfigRecord {
  /** Default constructor */
  constructor() {
  }

  /** Row number, added by SheetMan because `Id` cannot be a primary index. */
  public get index(): number { return this._index }

  /** ConfigTable */
  public get id(): string { return this._id }

  /** 카테고리 */
  public get category(): string { return this._category }

  /** 테이터타입 */
  public get dataType(): string { return this._dataType }

  /** 기본값 */
  public get defaultValue(): string { return this._defaultValue }

  /** 설명 */
  public get description(): string { return this._description }

  public _index: number = 0
  public _id: string = ''
  public _category: string = ''
  public _dataType: string = ''
  public _defaultValue: string = ''
  public _description: string = ''

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._index = dataRow.index
    this._id = dataRow.id
    this._category = dataRow.category
    this._dataType = dataRow.dataType
    this._defaultValue = dataRow.defaultValue
    this._description = dataRow.description
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._index = dataRow[offset++]
    this._id = dataRow[offset++]
    this._category = dataRow[offset++]
    this._dataType = dataRow[offset++]
    this._defaultValue = dataRow[offset++]
    this._description = dataRow[offset++]
  }
}

// Generated from project-rescue-xlsxs/MasterData.xlsx : ConfigTable : A1
export class ConfigTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): ConfigRecord[] { return this._records }
  private _records: ConfigRecord[] = []

  // Indexing by 'index'
  public get recordsByIndex(): Map<number, ConfigRecord> { return this._recordsByIndex }
  private _recordsByIndex: Map<number, ConfigRecord> = new Map<number, ConfigRecord>()

  /**
   * The row with this index, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findByIndex(key: number): ConfigRecord | undefined {
    return this._recordsByIndex.get(key)
  }

  /**
   * The row with this index, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getByIndex(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIndexOrThrow(key: number): ConfigRecord {
    const found = this._recordsByIndex.get(key)
    if (!found)
      throw new Error(`There is no record in table "Config" that corresponds to field "index" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this index. */
  public containsIndex(key: number): boolean {
    return this._recordsByIndex.has(key)
  }

  // Indexing by 'id'
  public get recordsById(): Map<string, ConfigRecord> { return this._recordsById }
  private _recordsById: Map<string, ConfigRecord> = new Map<string, ConfigRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: string): ConfigRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: string): ConfigRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "Config" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: string): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: ConfigRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new ConfigRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new ConfigRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .scb file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.ScbReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)
    let cursor: sheetman.ScbColumnCursor

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: ConfigRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new ConfigRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'Config.Index', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Config.Index')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._index = cursor.nextI32()
          }
          break
        case 2:
          sheetman.checkColumn(column, 'Config.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Config.Id')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = cursor.nextString()
          }
          break
        case 3:
          sheetman.checkColumn(column, 'Config.Category', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Config.Category')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._category = cursor.nextString()
          }
          break
        case 4:
          sheetman.checkColumn(column, 'Config.DataType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Config.DataType')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._dataType = cursor.nextString()
          }
          break
        case 5:
          sheetman.checkColumn(column, 'Config.DefaultValue', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Config.DefaultValue')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._defaultValue = cursor.nextString()
          }
          break
        case 6:
          sheetman.checkColumn(column, 'Config.Description', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Config.Description')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._description = cursor.nextString()
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: ConfigRecord[]): void {
    const recordsByIndex = new Map<number, ConfigRecord>()
    const recordsById = new Map<string, ConfigRecord>()

    for (const record of records) {
      recordsByIndex.set(record.index, record)
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsByIndex = recordsByIndex
    this._recordsById = recordsById
  }
}
