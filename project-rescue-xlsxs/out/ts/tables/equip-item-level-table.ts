// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/lite-binary-reader'

// Automatically import to handle external type references.
import { JobType } from '../enums/job-type'
import { CurrencyType } from '../enums/currency-type'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  name: string
  nameKR: string
  type: JobType
  materialType: CurrencyType
  materialValue: number
  materialValue1: number
  weaponStatRate1: number
  weaponStatRate2: number
  armorStatRate1: number
  armorStatRate2: number
  gauntletStatRate1: number
  gauntletStatRate2: number
  bootsStatRate1: number
  bootsStatRate2: number
  helmetStatRate1: number
  helmetStatRate2: number
  accStatRate1: number
  accStatRate2: number
}

// Generated from project-rescue-xlsxs/ItemData.xlsx : EquipItemLevelTable : A1
export class EquipItemLevelTableRecord {
  /** Default constructor */
  constructor() {
  }

  /** EquipItemLevel_Table */
  public get id(): number { return this._id }

  /** 영문명 */
  public get name(): string { return this._name }

  /** 한글명 */
  public get nameKR(): string { return this._nameKR }

  /** 타입 */
  public get type(): JobType { return this._type }

  /** 필요 재료 종류 */
  public get materialType(): CurrencyType { return this._materialType }

  /** 필요 재료 값 */
  public get materialValue(): number { return this._materialValue }

  /** 필요 재료 누적값 */
  public get materialValue1(): number { return this._materialValue1 }

  /** 무기 스탯1 증가값 */
  public get weaponStatRate1(): number { return this._weaponStatRate1 }

  /** 무기 스탯2 증가값 */
  public get weaponStatRate2(): number { return this._weaponStatRate2 }

  /** 갑옷 스탯1 증가값 */
  public get armorStatRate1(): number { return this._armorStatRate1 }

  /** 갑옷 스탯2 증가값 */
  public get armorStatRate2(): number { return this._armorStatRate2 }

  /** 장갑 스탯1 증가값 */
  public get gauntletStatRate1(): number { return this._gauntletStatRate1 }

  /** 장갑 스탯2 증가값 */
  public get gauntletStatRate2(): number { return this._gauntletStatRate2 }

  /** 신발 스탯1 증가값 */
  public get bootsStatRate1(): number { return this._bootsStatRate1 }

  /** 신발 스탯2 증가값 */
  public get bootsStatRate2(): number { return this._bootsStatRate2 }

  /** 투구 스탯1 증가값 */
  public get helmetStatRate1(): number { return this._helmetStatRate1 }

  /** 투구 스탯2 증가값 */
  public get helmetStatRate2(): number { return this._helmetStatRate2 }

  /** 악세서리 스탯1 증가값 */
  public get accStatRate1(): number { return this._accStatRate1 }

  /** 악세서리 스탯2 증가값 */
  public get accStatRate2(): number { return this._accStatRate2 }

  public _id: number = 0
  public _name: string = ''
  public _nameKR: string = ''
  public _type: JobType = 0 as JobType
  public _materialType: CurrencyType = 0 as CurrencyType
  public _materialValue: number = 0
  public _materialValue1: number = 0
  public _weaponStatRate1: number = 0
  public _weaponStatRate2: number = 0
  public _armorStatRate1: number = 0
  public _armorStatRate2: number = 0
  public _gauntletStatRate1: number = 0
  public _gauntletStatRate2: number = 0
  public _bootsStatRate1: number = 0
  public _bootsStatRate2: number = 0
  public _helmetStatRate1: number = 0
  public _helmetStatRate2: number = 0
  public _accStatRate1: number = 0
  public _accStatRate2: number = 0

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._name = dataRow.name
    this._nameKR = dataRow.nameKR
    this._type = dataRow.type
    this._materialType = dataRow.materialType
    this._materialValue = dataRow.materialValue
    this._materialValue1 = dataRow.materialValue1
    this._weaponStatRate1 = Math.fround(dataRow.weaponStatRate1)
    this._weaponStatRate2 = Math.fround(dataRow.weaponStatRate2)
    this._armorStatRate1 = Math.fround(dataRow.armorStatRate1)
    this._armorStatRate2 = Math.fround(dataRow.armorStatRate2)
    this._gauntletStatRate1 = Math.fround(dataRow.gauntletStatRate1)
    this._gauntletStatRate2 = Math.fround(dataRow.gauntletStatRate2)
    this._bootsStatRate1 = Math.fround(dataRow.bootsStatRate1)
    this._bootsStatRate2 = Math.fround(dataRow.bootsStatRate2)
    this._helmetStatRate1 = Math.fround(dataRow.helmetStatRate1)
    this._helmetStatRate2 = Math.fround(dataRow.helmetStatRate2)
    this._accStatRate1 = Math.fround(dataRow.accStatRate1)
    this._accStatRate2 = Math.fround(dataRow.accStatRate2)
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._name = dataRow[offset++]
    this._nameKR = dataRow[offset++]
    this._type = dataRow[offset++]
    this._materialType = dataRow[offset++]
    this._materialValue = dataRow[offset++]
    this._materialValue1 = dataRow[offset++]
    this._weaponStatRate1 = Math.fround(dataRow[offset++])
    this._weaponStatRate2 = Math.fround(dataRow[offset++])
    this._armorStatRate1 = Math.fround(dataRow[offset++])
    this._armorStatRate2 = Math.fround(dataRow[offset++])
    this._gauntletStatRate1 = Math.fround(dataRow[offset++])
    this._gauntletStatRate2 = Math.fround(dataRow[offset++])
    this._bootsStatRate1 = Math.fround(dataRow[offset++])
    this._bootsStatRate2 = Math.fround(dataRow[offset++])
    this._helmetStatRate1 = Math.fround(dataRow[offset++])
    this._helmetStatRate2 = Math.fround(dataRow[offset++])
    this._accStatRate1 = Math.fround(dataRow[offset++])
    this._accStatRate2 = Math.fround(dataRow[offset++])
  }
}

// Generated from project-rescue-xlsxs/ItemData.xlsx : EquipItemLevelTable : A1
export class EquipItemLevelTableTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): EquipItemLevelTableRecord[] { return this._records }
  private _records: EquipItemLevelTableRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, EquipItemLevelTableRecord> { return this._recordsById }
  private _recordsById: Map<number, EquipItemLevelTableRecord> = new Map<number, EquipItemLevelTableRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): EquipItemLevelTableRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): EquipItemLevelTableRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "EquipItemLevelTable" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: EquipItemLevelTableRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new EquipItemLevelTableRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new EquipItemLevelTableRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .table file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.LiteBinaryReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: EquipItemLevelTableRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new EquipItemLevelTableRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'EquipItemLevelTable.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = reader.readI32As(column.element)
          }
          break
        case 2:
          sheetman.checkColumn(column, 'EquipItemLevelTable.Name', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._name = reader.readString()
          }
          break
        case 3:
          sheetman.checkColumn(column, 'EquipItemLevelTable.NameKR', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._nameKR = reader.readString()
          }
          break
        case 4:
          sheetman.checkColumn(column, 'EquipItemLevelTable.Type', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._type = reader.readEnum() as JobType
          }
          break
        case 5:
          sheetman.checkColumn(column, 'EquipItemLevelTable.MaterialType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._materialType = reader.readEnum() as CurrencyType
          }
          break
        case 6:
          sheetman.checkColumn(column, 'EquipItemLevelTable.MaterialValue', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._materialValue = reader.readI32As(column.element)
          }
          break
        case 7:
          sheetman.checkColumn(column, 'EquipItemLevelTable.MaterialValue1', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._materialValue1 = reader.readI32As(column.element)
          }
          break
        case 8:
          sheetman.checkColumn(column, 'EquipItemLevelTable.WeaponStatRate1', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._weaponStatRate1 = reader.readFloat()
          }
          break
        case 9:
          sheetman.checkColumn(column, 'EquipItemLevelTable.WeaponStatRate2', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._weaponStatRate2 = reader.readFloat()
          }
          break
        case 10:
          sheetman.checkColumn(column, 'EquipItemLevelTable.ArmorStatRate1', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._armorStatRate1 = reader.readFloat()
          }
          break
        case 11:
          sheetman.checkColumn(column, 'EquipItemLevelTable.ArmorStatRate2', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._armorStatRate2 = reader.readFloat()
          }
          break
        case 12:
          sheetman.checkColumn(column, 'EquipItemLevelTable.GauntletStatRate1', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._gauntletStatRate1 = reader.readFloat()
          }
          break
        case 13:
          sheetman.checkColumn(column, 'EquipItemLevelTable.GauntletStatRate2', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._gauntletStatRate2 = reader.readFloat()
          }
          break
        case 14:
          sheetman.checkColumn(column, 'EquipItemLevelTable.BootsStatRate1', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._bootsStatRate1 = reader.readFloat()
          }
          break
        case 15:
          sheetman.checkColumn(column, 'EquipItemLevelTable.BootsStatRate2', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._bootsStatRate2 = reader.readFloat()
          }
          break
        case 16:
          sheetman.checkColumn(column, 'EquipItemLevelTable.HelmetStatRate1', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._helmetStatRate1 = reader.readFloat()
          }
          break
        case 17:
          sheetman.checkColumn(column, 'EquipItemLevelTable.HelmetStatRate2', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._helmetStatRate2 = reader.readFloat()
          }
          break
        case 18:
          sheetman.checkColumn(column, 'EquipItemLevelTable.AccStatRate1', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._accStatRate1 = reader.readFloat()
          }
          break
        case 19:
          sheetman.checkColumn(column, 'EquipItemLevelTable.AccStatRate2', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._accStatRate2 = reader.readFloat()
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: EquipItemLevelTableRecord[]): void {
    const recordsById = new Map<number, EquipItemLevelTableRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
