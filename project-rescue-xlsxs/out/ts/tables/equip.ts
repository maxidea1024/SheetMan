// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/lite-binary-reader'

// Automatically import to handle external type references.
import { JobType } from '../enums/job-type'
import { EquipmentType } from '../enums/equipment-type'
import { GradeType } from '../enums/grade-type'
import { StatType } from '../enums/stat-type'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  name: string
  equipName: string
  type: JobType
  equipType: EquipmentType
  grade: GradeType
  maxClass: number
  maxLevel: number
  statType1: StatType
  statType2: StatType
  iconPath: string
  prefabPath: string
  description: string
}

// Generated from project-rescue-xlsxs/ItemData.xlsx : EquipTable : A1
export class EquipRecord {
  /** Default constructor */
  constructor() {
  }

  /** Equipment_Table */
  public get id(): number { return this._id }

  /** 한글명 */
  public get name(): string { return this._name }

  /** 영문명 */
  public get equipName(): string { return this._equipName }

  /** 타입 */
  public get type(): JobType { return this._type }

  /** 장비 종류 */
  public get equipType(): EquipmentType { return this._equipType }

  /** 등급 */
  public get grade(): GradeType { return this._grade }

  /** 최대 성급 */
  public get maxClass(): number { return this._maxClass }

  /** 최대 레벨 */
  public get maxLevel(): number { return this._maxLevel }

  /** 스탯1 */
  public get statType1(): StatType { return this._statType1 }

  /** 스탯2 */
  public get statType2(): StatType { return this._statType2 }

  /** 아이콘 경로 */
  public get iconPath(): string { return this._iconPath }

  /** 프리펩 위치 */
  public get prefabPath(): string { return this._prefabPath }

  /** 설명 */
  public get description(): string { return this._description }

  public _id: number = 0
  public _name: string = ''
  public _equipName: string = ''
  public _type: JobType = 0 as JobType
  public _equipType: EquipmentType = 0 as EquipmentType
  public _grade: GradeType = 0 as GradeType
  public _maxClass: number = 0
  public _maxLevel: number = 0
  public _statType1: StatType = 0 as StatType
  public _statType2: StatType = 0 as StatType
  public _iconPath: string = ''
  public _prefabPath: string = ''
  public _description: string = ''

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._name = dataRow.name
    this._equipName = dataRow.equipName
    this._type = dataRow.type
    this._equipType = dataRow.equipType
    this._grade = dataRow.grade
    this._maxClass = dataRow.maxClass
    this._maxLevel = dataRow.maxLevel
    this._statType1 = dataRow.statType1
    this._statType2 = dataRow.statType2
    this._iconPath = dataRow.iconPath
    this._prefabPath = dataRow.prefabPath
    this._description = dataRow.description
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._name = dataRow[offset++]
    this._equipName = dataRow[offset++]
    this._type = dataRow[offset++]
    this._equipType = dataRow[offset++]
    this._grade = dataRow[offset++]
    this._maxClass = dataRow[offset++]
    this._maxLevel = dataRow[offset++]
    this._statType1 = dataRow[offset++]
    this._statType2 = dataRow[offset++]
    this._iconPath = dataRow[offset++]
    this._prefabPath = dataRow[offset++]
    this._description = dataRow[offset++]
  }
}

// Generated from project-rescue-xlsxs/ItemData.xlsx : EquipTable : A1
export class EquipTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): EquipRecord[] { return this._records }
  private _records: EquipRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, EquipRecord> { return this._recordsById }
  private _recordsById: Map<number, EquipRecord> = new Map<number, EquipRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): EquipRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): EquipRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "Equip" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: EquipRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new EquipRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new EquipRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .scb file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.LiteBinaryReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: EquipRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new EquipRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'Equip.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = reader.readI32As(column.element)
          }
          break
        case 2:
          sheetman.checkColumn(column, 'Equip.Name', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._name = reader.readString()
          }
          break
        case 3:
          sheetman.checkColumn(column, 'Equip.EquipName', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._equipName = reader.readString()
          }
          break
        case 4:
          sheetman.checkColumn(column, 'Equip.Type', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._type = reader.readEnum() as JobType
          }
          break
        case 5:
          sheetman.checkColumn(column, 'Equip.EquipType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._equipType = reader.readEnum() as EquipmentType
          }
          break
        case 6:
          sheetman.checkColumn(column, 'Equip.Grade', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._grade = reader.readEnum() as GradeType
          }
          break
        case 7:
          sheetman.checkColumn(column, 'Equip.MaxClass', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._maxClass = reader.readI32As(column.element)
          }
          break
        case 8:
          sheetman.checkColumn(column, 'Equip.MaxLevel', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._maxLevel = reader.readI32As(column.element)
          }
          break
        case 9:
          sheetman.checkColumn(column, 'Equip.StatType1', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._statType1 = reader.readEnum() as StatType
          }
          break
        case 10:
          sheetman.checkColumn(column, 'Equip.StatType2', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._statType2 = reader.readEnum() as StatType
          }
          break
        case 11:
          sheetman.checkColumn(column, 'Equip.IconPath', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._iconPath = reader.readString()
          }
          break
        case 12:
          sheetman.checkColumn(column, 'Equip.PrefabPath', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._prefabPath = reader.readString()
          }
          break
        case 13:
          sheetman.checkColumn(column, 'Equip.Description', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._description = reader.readString()
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: EquipRecord[]): void {
    const recordsById = new Map<number, EquipRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
