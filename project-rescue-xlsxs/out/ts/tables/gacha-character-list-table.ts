// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/lite-binary-reader'

// Automatically import to handle external type references.
import { GradeType } from '../enums/grade-type'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  characterID: number
  gradeType: GradeType
  baseWeight: number
  isWishlist: boolean
  wishlistWeight: number
  classUpCurrencyID: number
  classUpCurrencyValue: number
  exConditionID: number
  exCurrencyID: number
  exCurrencyValue: number
}

// Generated from project-rescue-xlsxs/GachaData.xlsx : GachaCharacterListTable : A1
export class GachaCharacterListTableRecord {
  /** Default constructor */
  constructor() {
  }

  /** GachaCharacter_List */
  public get id(): number { return this._id }

  /** 캐릭터 ID */
  public get characterID(): number { return this._characterID }

  /** 태생 등급 */
  public get gradeType(): GradeType { return this._gradeType }

  /** 기본 가중치 */
  public get baseWeight(): number { return this._baseWeight }

  /** 위시리스트 여부 */
  public get isWishlist(): boolean { return this._isWishlist }

  /** 위시리스트 가중치 */
  public get wishlistWeight(): number { return this._wishlistWeight }

  /** 성급 재화ID */
  public get classUpCurrencyID(): number { return this._classUpCurrencyID }

  /** 성급 재화값 */
  public get classUpCurrencyValue(): number { return this._classUpCurrencyValue }

  /** 변환 조건 ID */
  public get exConditionID(): number { return this._exConditionID }

  /** 변환 재화ID */
  public get exCurrencyID(): number { return this._exCurrencyID }

  /** 변환 재화값 */
  public get exCurrencyValue(): number { return this._exCurrencyValue }

  public _id: number = 0
  public _characterID: number = 0
  public _gradeType: GradeType = 0 as GradeType
  public _baseWeight: number = 0
  public _isWishlist: boolean = false
  public _wishlistWeight: number = 0
  public _classUpCurrencyID: number = 0
  public _classUpCurrencyValue: number = 0
  public _exConditionID: number = 0
  public _exCurrencyID: number = 0
  public _exCurrencyValue: number = 0

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._characterID = dataRow.characterID
    this._gradeType = dataRow.gradeType
    this._baseWeight = dataRow.baseWeight
    this._isWishlist = dataRow.isWishlist
    this._wishlistWeight = dataRow.wishlistWeight
    this._classUpCurrencyID = dataRow.classUpCurrencyID
    this._classUpCurrencyValue = dataRow.classUpCurrencyValue
    this._exConditionID = dataRow.exConditionID
    this._exCurrencyID = dataRow.exCurrencyID
    this._exCurrencyValue = dataRow.exCurrencyValue
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._characterID = dataRow[offset++]
    this._gradeType = dataRow[offset++]
    this._baseWeight = dataRow[offset++]
    this._isWishlist = dataRow[offset++]
    this._wishlistWeight = dataRow[offset++]
    this._classUpCurrencyID = dataRow[offset++]
    this._classUpCurrencyValue = dataRow[offset++]
    this._exConditionID = dataRow[offset++]
    this._exCurrencyID = dataRow[offset++]
    this._exCurrencyValue = dataRow[offset++]
  }
}

// Generated from project-rescue-xlsxs/GachaData.xlsx : GachaCharacterListTable : A1
export class GachaCharacterListTableTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): GachaCharacterListTableRecord[] { return this._records }
  private _records: GachaCharacterListTableRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, GachaCharacterListTableRecord> { return this._recordsById }
  private _recordsById: Map<number, GachaCharacterListTableRecord> = new Map<number, GachaCharacterListTableRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): GachaCharacterListTableRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): GachaCharacterListTableRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "GachaCharacterListTable" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: GachaCharacterListTableRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new GachaCharacterListTableRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new GachaCharacterListTableRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .table file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.LiteBinaryReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: GachaCharacterListTableRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new GachaCharacterListTableRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'GachaCharacterListTable.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = reader.readI32As(column.element)
          }
          break
        case 2:
          sheetman.checkColumn(column, 'GachaCharacterListTable.CharacterID', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._characterID = reader.readI32As(column.element)
          }
          break
        case 3:
          sheetman.checkColumn(column, 'GachaCharacterListTable.GradeType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._gradeType = reader.readEnum() as GradeType
          }
          break
        case 4:
          sheetman.checkColumn(column, 'GachaCharacterListTable.BaseWeight', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._baseWeight = reader.readI32As(column.element)
          }
          break
        case 5:
          sheetman.checkColumn(column, 'GachaCharacterListTable.IsWishlist', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_BOOL])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._isWishlist = reader.readBool()
          }
          break
        case 6:
          sheetman.checkColumn(column, 'GachaCharacterListTable.WishlistWeight', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._wishlistWeight = reader.readI32As(column.element)
          }
          break
        case 7:
          sheetman.checkColumn(column, 'GachaCharacterListTable.ClassUpCurrencyID', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._classUpCurrencyID = reader.readI32As(column.element)
          }
          break
        case 8:
          sheetman.checkColumn(column, 'GachaCharacterListTable.ClassUpCurrencyValue', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._classUpCurrencyValue = reader.readI32As(column.element)
          }
          break
        case 9:
          sheetman.checkColumn(column, 'GachaCharacterListTable.ExConditionID', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._exConditionID = reader.readI32As(column.element)
          }
          break
        case 10:
          sheetman.checkColumn(column, 'GachaCharacterListTable.ExCurrencyID', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._exCurrencyID = reader.readI32As(column.element)
          }
          break
        case 11:
          sheetman.checkColumn(column, 'GachaCharacterListTable.ExCurrencyValue', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._exCurrencyValue = reader.readI32As(column.element)
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: GachaCharacterListTableRecord[]): void {
    const recordsById = new Map<number, GachaCharacterListTableRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
