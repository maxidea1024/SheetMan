// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/scb-reader'

// Automatically import to handle external type references.
import { GrowthType } from '../enums/growth-type'
import { CurrencyType } from '../enums/currency-type'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  name: string
  infoName: string
  sheetName: string
  growthType: GrowthType
  reset: boolean
  resetCostType: CurrencyType
  resetCostValue: number
  maxStep: number
  maxLevel: number
  unlockConditionType: string
  unlockConditionValue: number
  description: string
  iconPath: string
}

// Generated from project-rescue-xlsxs/StageGrowth.xlsx : InfoGrowthTable : A1
export class InfoGrowthRecord {
  /** Default constructor */
  constructor() {
  }

  /** 기본값 */
  public get id(): number { return this._id }

  /** 영문 */
  public get name(): string { return this._name }

  /** 한글 */
  public get infoName(): string { return this._infoName }

  /** 실제 데이터가 있는 시트 이름 (AccountGrowth/TraitGrowth/RelicGrowth) */
  public get sheetName(): string { return this._sheetName }

  /** 시스템 분류 (Account/Trait/Relic) */
  public get growthType(): GrowthType { return this._growthType }

  /** 초기화 가능 불가능 체크 */
  public get reset(): boolean { return this._reset }

  /** 초기화 재화 타입 */
  public get resetCostType(): CurrencyType { return this._resetCostType }

  /** 초기화 비용 값 */
  public get resetCostValue(): number { return this._resetCostValue }

  /** 루프형 시스템만 사용 (예: 유물 20단계) */
  public get maxStep(): number { return this._maxStep }

  /** 단일 레벨 최대치 (루프형 시스템은 100 고정) */
  public get maxLevel(): number { return this._maxLevel }

  /** 해금 조건 */
  public get unlockConditionType(): string { return this._unlockConditionType }

  /** 해금 제한 */
  public get unlockConditionValue(): number { return this._unlockConditionValue }

  /** 설명 */
  public get description(): string { return this._description }

  /** 아이콘 */
  public get iconPath(): string { return this._iconPath }

  public _id: number = 0
  public _name: string = ''
  public _infoName: string = ''
  public _sheetName: string = ''
  public _growthType: GrowthType = 0 as GrowthType
  public _reset: boolean = false
  public _resetCostType: CurrencyType = 0 as CurrencyType
  public _resetCostValue: number = 0
  public _maxStep: number = 0
  public _maxLevel: number = 0
  public _unlockConditionType: string = ''
  public _unlockConditionValue: number = 0
  public _description: string = ''
  public _iconPath: string = ''

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._name = dataRow.name
    this._infoName = dataRow.infoName
    this._sheetName = dataRow.sheetName
    this._growthType = dataRow.growthType
    this._reset = dataRow.reset
    this._resetCostType = dataRow.resetCostType
    this._resetCostValue = dataRow.resetCostValue
    this._maxStep = dataRow.maxStep
    this._maxLevel = dataRow.maxLevel
    this._unlockConditionType = dataRow.unlockConditionType
    this._unlockConditionValue = dataRow.unlockConditionValue
    this._description = dataRow.description
    this._iconPath = dataRow.iconPath
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._name = dataRow[offset++]
    this._infoName = dataRow[offset++]
    this._sheetName = dataRow[offset++]
    this._growthType = dataRow[offset++]
    this._reset = dataRow[offset++]
    this._resetCostType = dataRow[offset++]
    this._resetCostValue = dataRow[offset++]
    this._maxStep = dataRow[offset++]
    this._maxLevel = dataRow[offset++]
    this._unlockConditionType = dataRow[offset++]
    this._unlockConditionValue = dataRow[offset++]
    this._description = dataRow[offset++]
    this._iconPath = dataRow[offset++]
  }
}

// Generated from project-rescue-xlsxs/StageGrowth.xlsx : InfoGrowthTable : A1
export class InfoGrowthTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): InfoGrowthRecord[] { return this._records }
  private _records: InfoGrowthRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, InfoGrowthRecord> { return this._recordsById }
  private _recordsById: Map<number, InfoGrowthRecord> = new Map<number, InfoGrowthRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): InfoGrowthRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): InfoGrowthRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "InfoGrowth" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: InfoGrowthRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new InfoGrowthRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new InfoGrowthRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .scb file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.ScbReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: InfoGrowthRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new InfoGrowthRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'InfoGrowth.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = reader.readI32As(column.element)
          }
          break
        case 2:
          sheetman.checkColumn(column, 'InfoGrowth.Name', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._name = reader.readString()
          }
          break
        case 3:
          sheetman.checkColumn(column, 'InfoGrowth.InfoName', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._infoName = reader.readString()
          }
          break
        case 4:
          sheetman.checkColumn(column, 'InfoGrowth.SheetName', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._sheetName = reader.readString()
          }
          break
        case 5:
          sheetman.checkColumn(column, 'InfoGrowth.GrowthType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._growthType = reader.readEnum() as GrowthType
          }
          break
        case 6:
          sheetman.checkColumn(column, 'InfoGrowth.Reset', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_BOOL])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._reset = reader.readBool()
          }
          break
        case 7:
          sheetman.checkColumn(column, 'InfoGrowth.ResetCostType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._resetCostType = reader.readEnum() as CurrencyType
          }
          break
        case 8:
          sheetman.checkColumn(column, 'InfoGrowth.ResetCostValue', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._resetCostValue = reader.readI32As(column.element)
          }
          break
        case 9:
          sheetman.checkColumn(column, 'InfoGrowth.MaxStep', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._maxStep = reader.readI32As(column.element)
          }
          break
        case 10:
          sheetman.checkColumn(column, 'InfoGrowth.MaxLevel', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._maxLevel = reader.readI32As(column.element)
          }
          break
        case 11:
          sheetman.checkColumn(column, 'InfoGrowth.UnlockConditionType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._unlockConditionType = reader.readString()
          }
          break
        case 12:
          sheetman.checkColumn(column, 'InfoGrowth.UnlockConditionValue', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._unlockConditionValue = reader.readI32As(column.element)
          }
          break
        case 13:
          sheetman.checkColumn(column, 'InfoGrowth.Description', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._description = reader.readString()
          }
          break
        case 14:
          sheetman.checkColumn(column, 'InfoGrowth.IconPath', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._iconPath = reader.readString()
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: InfoGrowthRecord[]): void {
    const recordsById = new Map<number, InfoGrowthRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
