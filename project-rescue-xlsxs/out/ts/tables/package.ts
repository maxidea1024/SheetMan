// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/scb-reader'

// Automatically import to handle external type references.
import { ItemType } from '../enums/item-type'
import { ConsumablesType } from '../enums/consumables-type'
import { ConditionType } from '../enums/condition-type'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  name: string
  className: string
  itemType: ItemType
  packageType: ConsumablesType
  stackable: boolean
  maxStack: string
  packageCondition: ConditionType
  cooltime: number
  duration: number
  currencyID: number[]
  currencyItemMin: number[]
  currencyItemMax: number[]
  currencyItemRate: number[]
  iconPath: string
  dropPrefabPath: string
  itemDescription: string
}

// Generated from project-rescue-xlsxs/ItemData.xlsx : PackageTable : A1
export class PackageRecord {
  /** Default constructor */
  constructor() {
  }

  /** PackcgeItem_Table */
  public get id(): number { return this._id }

  /** 영문 */
  public get name(): string { return this._name }

  /** 한글 */
  public get className(): string { return this._className }

  /** 카테고리 */
  public get itemType(): ItemType { return this._itemType }

  /** 패키지 타입 */
  public get packageType(): ConsumablesType { return this._packageType }

  /** 중첩 가능 유무 */
  public get stackable(): boolean { return this._stackable }

  /** 최대 소유 값 */
  public get maxStack(): bigint { return this._maxStack }

  /** 획득조건 타입 */
  public get packageCondition(): ConditionType { return this._packageCondition }

  /** 아이템 재사용 대기시간 */
  public get cooltime(): number { return this._cooltime }

  /** 아이템 유지 시간 */
  public get duration(): number { return this._duration }

  /** 포함 아이템 종류 */
  public get currencyID(): number[] { return this._currencyID }

  /** 포함 아이템 최소 수량 값 */
  public get currencyItemMin(): number[] { return this._currencyItemMin }

  /** 포함 아이템 최대 수량 값 */
  public get currencyItemMax(): number[] { return this._currencyItemMax }

  /** 포함 아이템 확률 */
  public get currencyItemRate(): number[] { return this._currencyItemRate }

  /** 아이콘 경로 */
  public get iconPath(): string { return this._iconPath }

  /** 프리펩 위치 */
  public get dropPrefabPath(): string { return this._dropPrefabPath }

  /** 아이템 설명 */
  public get itemDescription(): string { return this._itemDescription }

  public _id: number = 0
  public _name: string = ''
  public _className: string = ''
  public _itemType: ItemType = 0 as ItemType
  public _packageType: ConsumablesType = 0 as ConsumablesType
  public _stackable: boolean = false
  public _maxStack: bigint = 0n
  public _packageCondition: ConditionType = 0 as ConditionType
  public _cooltime: number = 0
  public _duration: number = 0
  public _currencyID: number[] = []
  public _currencyItemMin: number[] = []
  public _currencyItemMax: number[] = []
  public _currencyItemRate: number[] = []
  public _iconPath: string = ''
  public _dropPrefabPath: string = ''
  public _itemDescription: string = ''

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._name = dataRow.name
    this._className = dataRow.className
    this._itemType = dataRow.itemType
    this._packageType = dataRow.packageType
    this._stackable = dataRow.stackable
    this._maxStack = BigInt(dataRow.maxStack)
    this._packageCondition = dataRow.packageCondition
    this._cooltime = dataRow.cooltime
    this._duration = dataRow.duration
    this._currencyID = dataRow.currencyID
    this._currencyItemMin = dataRow.currencyItemMin
    this._currencyItemMax = dataRow.currencyItemMax
    this._currencyItemRate = dataRow.currencyItemRate
    this._iconPath = dataRow.iconPath
    this._dropPrefabPath = dataRow.dropPrefabPath
    this._itemDescription = dataRow.itemDescription
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._name = dataRow[offset++]
    this._className = dataRow[offset++]
    this._itemType = dataRow[offset++]
    this._packageType = dataRow[offset++]
    this._stackable = dataRow[offset++]
    this._maxStack = BigInt(dataRow[offset++])
    this._packageCondition = dataRow[offset++]
    this._cooltime = dataRow[offset++]
    this._duration = dataRow[offset++]
    this._currencyID = dataRow[offset++]
    this._currencyItemMin = dataRow[offset++]
    this._currencyItemMax = dataRow[offset++]
    this._currencyItemRate = dataRow[offset++]
    this._iconPath = dataRow[offset++]
    this._dropPrefabPath = dataRow[offset++]
    this._itemDescription = dataRow[offset++]
  }
}

// Generated from project-rescue-xlsxs/ItemData.xlsx : PackageTable : A1
export class PackageTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): PackageRecord[] { return this._records }
  private _records: PackageRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, PackageRecord> { return this._recordsById }
  private _recordsById: Map<number, PackageRecord> = new Map<number, PackageRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): PackageRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): PackageRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "Package" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: PackageRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new PackageRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new PackageRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .scb file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.ScbReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)
    let cursor: sheetman.ScbColumnCursor

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: PackageRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new PackageRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'Package.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Package.Id')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = cursor.nextI32()
          }
          break
        case 2:
          sheetman.checkColumn(column, 'Package.Name', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Package.Name')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._name = cursor.nextString()
          }
          break
        case 3:
          sheetman.checkColumn(column, 'Package.ClassName', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Package.ClassName')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._className = cursor.nextString()
          }
          break
        case 4:
          sheetman.checkColumn(column, 'Package.ItemType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Package.ItemType')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._itemType = cursor.nextI32() as ItemType
          }
          break
        case 5:
          sheetman.checkColumn(column, 'Package.PackageType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Package.PackageType')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._packageType = cursor.nextI32() as ConsumablesType
          }
          break
        case 6:
          sheetman.checkColumn(column, 'Package.Stackable', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_BOOL])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Package.Stackable')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._stackable = cursor.nextBool()
          }
          break
        case 7:
          sheetman.checkColumn(column, 'Package.MaxStack', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I64, sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Package.MaxStack')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._maxStack = cursor.nextI64()
          }
          break
        case 8:
          sheetman.checkColumn(column, 'Package.PackageCondition', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Package.PackageCondition')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._packageCondition = cursor.nextI32() as ConditionType
          }
          break
        case 9:
          sheetman.checkColumn(column, 'Package.Cooltime', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Package.Cooltime')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._cooltime = cursor.nextI32()
          }
          break
        case 10:
          sheetman.checkColumn(column, 'Package.Duration', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Package.Duration')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._duration = cursor.nextI32()
          }
          break
        case 11:
          sheetman.checkColumn(column, 'Package.CurrencyID', sheetman.KIND_VAR_ARRAY, 0, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            const elementCount = reader.readCounter32()
            record._currencyID = []
            for (let j = 0; j < elementCount; ++j)
              record._currencyID.push(reader.readI32As(column.element))
          }
          break
        case 12:
          sheetman.checkColumn(column, 'Package.CurrencyItemMin', sheetman.KIND_VAR_ARRAY, 0, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            const elementCount = reader.readCounter32()
            record._currencyItemMin = []
            for (let j = 0; j < elementCount; ++j)
              record._currencyItemMin.push(reader.readI32As(column.element))
          }
          break
        case 13:
          sheetman.checkColumn(column, 'Package.CurrencyItemMax', sheetman.KIND_VAR_ARRAY, 0, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            const elementCount = reader.readCounter32()
            record._currencyItemMax = []
            for (let j = 0; j < elementCount; ++j)
              record._currencyItemMax.push(reader.readI32As(column.element))
          }
          break
        case 14:
          sheetman.checkColumn(column, 'Package.CurrencyItemRate', sheetman.KIND_VAR_ARRAY, 0, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            const elementCount = reader.readCounter32()
            record._currencyItemRate = []
            for (let j = 0; j < elementCount; ++j)
              record._currencyItemRate.push(reader.readI32As(column.element))
          }
          break
        case 15:
          sheetman.checkColumn(column, 'Package.IconPath', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Package.IconPath')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._iconPath = cursor.nextString()
          }
          break
        case 16:
          sheetman.checkColumn(column, 'Package.DropPrefabPath', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Package.DropPrefabPath')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._dropPrefabPath = cursor.nextString()
          }
          break
        case 17:
          sheetman.checkColumn(column, 'Package.ItemDescription', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Package.ItemDescription')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._itemDescription = cursor.nextString()
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: PackageRecord[]): void {
    const recordsById = new Map<number, PackageRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
