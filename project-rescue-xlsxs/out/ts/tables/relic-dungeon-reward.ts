// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/scb-reader'

// Automatically import to handle external type references.
import { CurrencyType } from '../enums/currency-type'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  name: string
  rewardName: string
  rewardType1: CurrencyType
  rewardValue1: number
  rewardType2: CurrencyType
  rewardValue2: number
  isFirstClear: boolean
  firstClearRewardType: CurrencyType
  firstClearRewardValue: number
}

// Generated from project-rescue-xlsxs/DailyDungeon.xlsx : RelicDungeonRewardTable : A1
export class RelicDungeonRewardRecord {
  /** Default constructor */
  constructor() {
  }

  /** RelicDungeonReward_Table */
  public get id(): number { return this._id }

  /** 영문 */
  public get name(): string { return this._name }

  /** 한글 */
  public get rewardName(): string { return this._rewardName }

  /** 보상 설정1 */
  public get rewardType1(): CurrencyType { return this._rewardType1 }

  /** 보상 값 1 */
  public get rewardValue1(): number { return this._rewardValue1 }

  /** 보상 설정2 */
  public get rewardType2(): CurrencyType { return this._rewardType2 }

  /** 보상 값 2 */
  public get rewardValue2(): number { return this._rewardValue2 }

  /** 최초 클리어 */
  public get isFirstClear(): boolean { return this._isFirstClear }

  /** 최초 보상 설정 */
  public get firstClearRewardType(): CurrencyType { return this._firstClearRewardType }

  /** 최초 보상 값 */
  public get firstClearRewardValue(): number { return this._firstClearRewardValue }

  public _id: number = 0
  public _name: string = ''
  public _rewardName: string = ''
  public _rewardType1: CurrencyType = 0 as CurrencyType
  public _rewardValue1: number = 0
  public _rewardType2: CurrencyType = 0 as CurrencyType
  public _rewardValue2: number = 0
  public _isFirstClear: boolean = false
  public _firstClearRewardType: CurrencyType = 0 as CurrencyType
  public _firstClearRewardValue: number = 0

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._name = dataRow.name
    this._rewardName = dataRow.rewardName
    this._rewardType1 = dataRow.rewardType1
    this._rewardValue1 = dataRow.rewardValue1
    this._rewardType2 = dataRow.rewardType2
    this._rewardValue2 = dataRow.rewardValue2
    this._isFirstClear = dataRow.isFirstClear
    this._firstClearRewardType = dataRow.firstClearRewardType
    this._firstClearRewardValue = dataRow.firstClearRewardValue
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._name = dataRow[offset++]
    this._rewardName = dataRow[offset++]
    this._rewardType1 = dataRow[offset++]
    this._rewardValue1 = dataRow[offset++]
    this._rewardType2 = dataRow[offset++]
    this._rewardValue2 = dataRow[offset++]
    this._isFirstClear = dataRow[offset++]
    this._firstClearRewardType = dataRow[offset++]
    this._firstClearRewardValue = dataRow[offset++]
  }
}

// Generated from project-rescue-xlsxs/DailyDungeon.xlsx : RelicDungeonRewardTable : A1
export class RelicDungeonRewardTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): RelicDungeonRewardRecord[] { return this._records }
  private _records: RelicDungeonRewardRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, RelicDungeonRewardRecord> { return this._recordsById }
  private _recordsById: Map<number, RelicDungeonRewardRecord> = new Map<number, RelicDungeonRewardRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): RelicDungeonRewardRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): RelicDungeonRewardRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "RelicDungeonReward" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: RelicDungeonRewardRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new RelicDungeonRewardRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new RelicDungeonRewardRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .scb file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.ScbReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: RelicDungeonRewardRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new RelicDungeonRewardRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'RelicDungeonReward.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = reader.readI32As(column.element)
          }
          break
        case 2:
          sheetman.checkColumn(column, 'RelicDungeonReward.Name', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._name = reader.readString()
          }
          break
        case 3:
          sheetman.checkColumn(column, 'RelicDungeonReward.RewardName', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._rewardName = reader.readString()
          }
          break
        case 4:
          sheetman.checkColumn(column, 'RelicDungeonReward.RewardType1', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._rewardType1 = reader.readEnum() as CurrencyType
          }
          break
        case 5:
          sheetman.checkColumn(column, 'RelicDungeonReward.RewardValue1', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._rewardValue1 = reader.readI32As(column.element)
          }
          break
        case 6:
          sheetman.checkColumn(column, 'RelicDungeonReward.RewardType2', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._rewardType2 = reader.readEnum() as CurrencyType
          }
          break
        case 7:
          sheetman.checkColumn(column, 'RelicDungeonReward.RewardValue2', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._rewardValue2 = reader.readI32As(column.element)
          }
          break
        case 8:
          sheetman.checkColumn(column, 'RelicDungeonReward.IsFirstClear', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_BOOL])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._isFirstClear = reader.readBool()
          }
          break
        case 9:
          sheetman.checkColumn(column, 'RelicDungeonReward.FirstClearRewardType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._firstClearRewardType = reader.readEnum() as CurrencyType
          }
          break
        case 10:
          sheetman.checkColumn(column, 'RelicDungeonReward.FirstClearRewardValue', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._firstClearRewardValue = reader.readI32As(column.element)
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: RelicDungeonRewardRecord[]): void {
    const recordsById = new Map<number, RelicDungeonRewardRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
