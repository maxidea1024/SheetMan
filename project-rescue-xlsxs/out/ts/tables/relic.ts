// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/scb-reader'

// Automatically import to handle external type references.
import { StatType } from '../enums/stat-type'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  name: string
  relicName: string
  infuluenceStep: number
  relicType1: StatType
  relicValue1: number
  relicIncValue1: number
  relicType2: StatType
  relicValue2: number
  relicIncValue2: number
  relicType3: StatType
  relicValue3: number
  relicIncValue3: number
  description: string
  iconPath: string
}

// Generated from project-rescue-xlsxs/StageGrowth.xlsx : RelicTable : A1
export class RelicRecord {
  /** Default constructor */
  constructor() {
  }

  /** 기본값 */
  public get id(): number { return this._id }

  /** 영문 */
  public get name(): string { return this._name }

  /** 한글 */
  public get relicName(): string { return this._relicName }

  /** 영향력 단계 */
  public get infuluenceStep(): number { return this._infuluenceStep }

  /** 스탯 타입 */
  public get relicType1(): StatType { return this._relicType1 }

  /** 유물 기본 값 1 */
  public get relicValue1(): number { return this._relicValue1 }

  /** 유물 레벨 당 스탯 증가 수치 1 */
  public get relicIncValue1(): number { return this._relicIncValue1 }

  /** 유물 기본 값 2 */
  public get relicType2(): StatType { return this._relicType2 }

  /** 유물 레벨 당 스탯 증가 수치 2 */
  public get relicValue2(): number { return this._relicValue2 }

  /** 유물 기본 값 3 */
  public get relicIncValue2(): number { return this._relicIncValue2 }

  /** 유물 레벨 당 스탯 증가 수치 3 */
  public get relicType3(): StatType { return this._relicType3 }

  /** 유물 기본 값 4 */
  public get relicValue3(): number { return this._relicValue3 }

  /** 유물 레벨 당 스탯 증가 수치 4 */
  public get relicIncValue3(): number { return this._relicIncValue3 }

  /** 설명 */
  public get description(): string { return this._description }

  /** 아이콘 경로 */
  public get iconPath(): string { return this._iconPath }

  public _id: number = 0
  public _name: string = ''
  public _relicName: string = ''
  public _infuluenceStep: number = 0
  public _relicType1: StatType = 0 as StatType
  public _relicValue1: number = 0
  public _relicIncValue1: number = 0
  public _relicType2: StatType = 0 as StatType
  public _relicValue2: number = 0
  public _relicIncValue2: number = 0
  public _relicType3: StatType = 0 as StatType
  public _relicValue3: number = 0
  public _relicIncValue3: number = 0
  public _description: string = ''
  public _iconPath: string = ''

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._name = dataRow.name
    this._relicName = dataRow.relicName
    this._infuluenceStep = dataRow.infuluenceStep
    this._relicType1 = dataRow.relicType1
    this._relicValue1 = Math.fround(dataRow.relicValue1)
    this._relicIncValue1 = Math.fround(dataRow.relicIncValue1)
    this._relicType2 = dataRow.relicType2
    this._relicValue2 = Math.fround(dataRow.relicValue2)
    this._relicIncValue2 = Math.fround(dataRow.relicIncValue2)
    this._relicType3 = dataRow.relicType3
    this._relicValue3 = Math.fround(dataRow.relicValue3)
    this._relicIncValue3 = Math.fround(dataRow.relicIncValue3)
    this._description = dataRow.description
    this._iconPath = dataRow.iconPath
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._name = dataRow[offset++]
    this._relicName = dataRow[offset++]
    this._infuluenceStep = dataRow[offset++]
    this._relicType1 = dataRow[offset++]
    this._relicValue1 = Math.fround(dataRow[offset++])
    this._relicIncValue1 = Math.fround(dataRow[offset++])
    this._relicType2 = dataRow[offset++]
    this._relicValue2 = Math.fround(dataRow[offset++])
    this._relicIncValue2 = Math.fround(dataRow[offset++])
    this._relicType3 = dataRow[offset++]
    this._relicValue3 = Math.fround(dataRow[offset++])
    this._relicIncValue3 = Math.fround(dataRow[offset++])
    this._description = dataRow[offset++]
    this._iconPath = dataRow[offset++]
  }
}

// Generated from project-rescue-xlsxs/StageGrowth.xlsx : RelicTable : A1
export class RelicTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): RelicRecord[] { return this._records }
  private _records: RelicRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, RelicRecord> { return this._recordsById }
  private _recordsById: Map<number, RelicRecord> = new Map<number, RelicRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): RelicRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): RelicRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "Relic" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: RelicRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new RelicRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new RelicRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .scb file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.ScbReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)
    let cursor: sheetman.ScbColumnCursor

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: RelicRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new RelicRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'Relic.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Relic.Id')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = cursor.nextI32()
          }
          break
        case 2:
          sheetman.checkColumn(column, 'Relic.Name', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Relic.Name')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._name = cursor.nextString()
          }
          break
        case 3:
          sheetman.checkColumn(column, 'Relic.RelicName', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Relic.RelicName')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._relicName = cursor.nextString()
          }
          break
        case 4:
          sheetman.checkColumn(column, 'Relic.InfuluenceStep', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Relic.InfuluenceStep')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._infuluenceStep = cursor.nextI32()
          }
          break
        case 5:
          sheetman.checkColumn(column, 'Relic.RelicType1', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Relic.RelicType1')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._relicType1 = cursor.nextI32() as StatType
          }
          break
        case 6:
          sheetman.checkColumn(column, 'Relic.RelicValue1', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._relicValue1 = reader.readFloat()
          }
          break
        case 7:
          sheetman.checkColumn(column, 'Relic.RelicIncValue1', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._relicIncValue1 = reader.readFloat()
          }
          break
        case 8:
          sheetman.checkColumn(column, 'Relic.RelicType2', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Relic.RelicType2')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._relicType2 = cursor.nextI32() as StatType
          }
          break
        case 9:
          sheetman.checkColumn(column, 'Relic.RelicValue2', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._relicValue2 = reader.readFloat()
          }
          break
        case 10:
          sheetman.checkColumn(column, 'Relic.RelicIncValue2', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._relicIncValue2 = reader.readFloat()
          }
          break
        case 11:
          sheetman.checkColumn(column, 'Relic.RelicType3', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Relic.RelicType3')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._relicType3 = cursor.nextI32() as StatType
          }
          break
        case 12:
          sheetman.checkColumn(column, 'Relic.RelicValue3', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._relicValue3 = reader.readFloat()
          }
          break
        case 13:
          sheetman.checkColumn(column, 'Relic.RelicIncValue3', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._relicIncValue3 = reader.readFloat()
          }
          break
        case 14:
          sheetman.checkColumn(column, 'Relic.Description', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Relic.Description')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._description = cursor.nextString()
          }
          break
        case 15:
          sheetman.checkColumn(column, 'Relic.IconPath', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Relic.IconPath')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._iconPath = cursor.nextString()
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: RelicRecord[]): void {
    const recordsById = new Map<number, RelicRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
