// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/lite-binary-reader'

// Automatically import to handle external type references.
import { AgencyGrade } from '../enums/agency-grade'
import { CurrencyType } from '../enums/currency-type'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  name: string
  agencyGrade: AgencyGrade
  dispatchCount: number
  gradeSRate: number
  gradeARate: number
  gradeBRate: number
  gradeCRate: number
  gradeDRate: number
  gradeERate: number
  gradeFRate: number
  freeRefresh: number
  refreshCurrencyType: CurrencyType
  refreshCurrencyValue: number
}

// Generated from project-rescue-xlsxs/SD_Contens.xlsx : SDAgencyInfoTable : A1
export class SDAgencyInfoTableRecord {
  /** Default constructor */
  constructor() {
  }

  /** SDAgencyInfo_Table */
  public get id(): number { return this._id }

  /** 등급 이름 */
  public get name(): string { return this._name }

  /** 등급 */
  public get agencyGrade(): AgencyGrade { return this._agencyGrade }

  /** 해금 조건 */
  public get dispatchCount(): number { return this._dispatchCount }

  /** S등장 확률 */
  public get gradeSRate(): number { return this._gradeSRate }

  /** A등장 확률 */
  public get gradeARate(): number { return this._gradeARate }

  /** B등장 확률 */
  public get gradeBRate(): number { return this._gradeBRate }

  /** C등장 확률 */
  public get gradeCRate(): number { return this._gradeCRate }

  /** D등장 확률 */
  public get gradeDRate(): number { return this._gradeDRate }

  /** E등장 확률 */
  public get gradeERate(): number { return this._gradeERate }

  /** F등장 확률 */
  public get gradeFRate(): number { return this._gradeFRate }

  /** 무료 초기화 횟수 */
  public get freeRefresh(): number { return this._freeRefresh }

  /** 초기화 재화 타입 */
  public get refreshCurrencyType(): CurrencyType { return this._refreshCurrencyType }

  /** 초기화 재화 값 */
  public get refreshCurrencyValue(): number { return this._refreshCurrencyValue }

  public _id: number = 0
  public _name: string = ''
  public _agencyGrade: AgencyGrade = 0 as AgencyGrade
  public _dispatchCount: number = 0
  public _gradeSRate: number = 0
  public _gradeARate: number = 0
  public _gradeBRate: number = 0
  public _gradeCRate: number = 0
  public _gradeDRate: number = 0
  public _gradeERate: number = 0
  public _gradeFRate: number = 0
  public _freeRefresh: number = 0
  public _refreshCurrencyType: CurrencyType = 0 as CurrencyType
  public _refreshCurrencyValue: number = 0

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._name = dataRow.name
    this._agencyGrade = dataRow.agencyGrade
    this._dispatchCount = dataRow.dispatchCount
    this._gradeSRate = Math.fround(dataRow.gradeSRate)
    this._gradeARate = Math.fround(dataRow.gradeARate)
    this._gradeBRate = Math.fround(dataRow.gradeBRate)
    this._gradeCRate = Math.fround(dataRow.gradeCRate)
    this._gradeDRate = Math.fround(dataRow.gradeDRate)
    this._gradeERate = Math.fround(dataRow.gradeERate)
    this._gradeFRate = Math.fround(dataRow.gradeFRate)
    this._freeRefresh = dataRow.freeRefresh
    this._refreshCurrencyType = dataRow.refreshCurrencyType
    this._refreshCurrencyValue = dataRow.refreshCurrencyValue
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._name = dataRow[offset++]
    this._agencyGrade = dataRow[offset++]
    this._dispatchCount = dataRow[offset++]
    this._gradeSRate = Math.fround(dataRow[offset++])
    this._gradeARate = Math.fround(dataRow[offset++])
    this._gradeBRate = Math.fround(dataRow[offset++])
    this._gradeCRate = Math.fround(dataRow[offset++])
    this._gradeDRate = Math.fround(dataRow[offset++])
    this._gradeERate = Math.fround(dataRow[offset++])
    this._gradeFRate = Math.fround(dataRow[offset++])
    this._freeRefresh = dataRow[offset++]
    this._refreshCurrencyType = dataRow[offset++]
    this._refreshCurrencyValue = dataRow[offset++]
  }
}

// Generated from project-rescue-xlsxs/SD_Contens.xlsx : SDAgencyInfoTable : A1
export class SDAgencyInfoTableTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): SDAgencyInfoTableRecord[] { return this._records }
  private _records: SDAgencyInfoTableRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, SDAgencyInfoTableRecord> { return this._recordsById }
  private _recordsById: Map<number, SDAgencyInfoTableRecord> = new Map<number, SDAgencyInfoTableRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): SDAgencyInfoTableRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): SDAgencyInfoTableRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "SDAgencyInfoTable" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: SDAgencyInfoTableRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new SDAgencyInfoTableRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new SDAgencyInfoTableRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .scb file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.LiteBinaryReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: SDAgencyInfoTableRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new SDAgencyInfoTableRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'SDAgencyInfoTable.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = reader.readI32As(column.element)
          }
          break
        case 2:
          sheetman.checkColumn(column, 'SDAgencyInfoTable.Name', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._name = reader.readString()
          }
          break
        case 3:
          sheetman.checkColumn(column, 'SDAgencyInfoTable.AgencyGrade', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._agencyGrade = reader.readEnum() as AgencyGrade
          }
          break
        case 4:
          sheetman.checkColumn(column, 'SDAgencyInfoTable.DispatchCount', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._dispatchCount = reader.readI32As(column.element)
          }
          break
        case 5:
          sheetman.checkColumn(column, 'SDAgencyInfoTable.GradeSRate', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._gradeSRate = reader.readFloat()
          }
          break
        case 6:
          sheetman.checkColumn(column, 'SDAgencyInfoTable.GradeARate', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._gradeARate = reader.readFloat()
          }
          break
        case 7:
          sheetman.checkColumn(column, 'SDAgencyInfoTable.GradeBRate', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._gradeBRate = reader.readFloat()
          }
          break
        case 8:
          sheetman.checkColumn(column, 'SDAgencyInfoTable.GradeCRate', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._gradeCRate = reader.readFloat()
          }
          break
        case 9:
          sheetman.checkColumn(column, 'SDAgencyInfoTable.GradeDRate', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._gradeDRate = reader.readFloat()
          }
          break
        case 10:
          sheetman.checkColumn(column, 'SDAgencyInfoTable.GradeERate', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._gradeERate = reader.readFloat()
          }
          break
        case 11:
          sheetman.checkColumn(column, 'SDAgencyInfoTable.GradeFRate', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._gradeFRate = reader.readFloat()
          }
          break
        case 12:
          sheetman.checkColumn(column, 'SDAgencyInfoTable.FreeRefresh', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._freeRefresh = reader.readI32As(column.element)
          }
          break
        case 13:
          sheetman.checkColumn(column, 'SDAgencyInfoTable.RefreshCurrencyType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._refreshCurrencyType = reader.readEnum() as CurrencyType
          }
          break
        case 14:
          sheetman.checkColumn(column, 'SDAgencyInfoTable.RefreshCurrencyValue', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._refreshCurrencyValue = reader.readI32As(column.element)
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: SDAgencyInfoTableRecord[]): void {
    const recordsById = new Map<number, SDAgencyInfoTableRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
