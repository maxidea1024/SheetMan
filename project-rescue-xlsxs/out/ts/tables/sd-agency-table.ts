// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/lite-binary-reader'

// Automatically import to handle external type references.
import { AgencyType } from '../enums/agency-type'
import { AgencyGrade } from '../enums/agency-grade'
import { JobType } from '../enums/job-type'
import { GradeType } from '../enums/grade-type'
import { ConditionType } from '../enums/condition-type'
import { CurrencyType } from '../enums/currency-type'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  name: string
  infoName: string
  agencyType: AgencyType
  agencyGrade: AgencyGrade
  time: number
  agencyWeight: number
  condition1: JobType
  condition1value: number
  condition2: GradeType
  condition2value: number
  condition3: ConditionType
  classMinValue: number
  condition3value: number
  rewardType1: CurrencyType
  rewardValue1: number
  rewardType2: CurrencyType
  rewardValue2: number
  rewardType3: CurrencyType
  rewardValue3: number
  iconPath: string
}

// Generated from project-rescue-xlsxs\SD_Contens.xlsx : SDAgencyTable : A1
export class SDAgencyTableRecord {
  /** Default constructor */
  constructor() {
  }

  /** SDAgency_Table */
  public get id(): number { return this._id }

  /** 영문 */
  public get name(): string { return this._name }

  /** 한글 */
  public get infoName(): string { return this._infoName }

  /** 타입 */
  public get agencyType(): AgencyType { return this._agencyType }

  /** 등급 */
  public get agencyGrade(): AgencyGrade { return this._agencyGrade }

  /** 소요 시간(sec) */
  public get time(): number { return this._time }

  /** 가중치 */
  public get agencyWeight(): number { return this._agencyWeight }

  /** 조건1 */
  public get condition1(): JobType { return this._condition1 }

  /** 조건값1 */
  public get condition1value(): number { return this._condition1value }

  /** 조건2 */
  public get condition2(): GradeType { return this._condition2 }

  /** 조건값2 */
  public get condition2value(): number { return this._condition2value }

  /** 조건3 */
  public get condition3(): ConditionType { return this._condition3 }

  /** 최소 성급 */
  public get classMinValue(): number { return this._classMinValue }

  /** 조건값3 */
  public get condition3value(): number { return this._condition3value }

  /** 보상1 */
  public get rewardType1(): CurrencyType { return this._rewardType1 }

  /** 보상 값1 */
  public get rewardValue1(): number { return this._rewardValue1 }

  /** 보상2 */
  public get rewardType2(): CurrencyType { return this._rewardType2 }

  /** 보상 값2 */
  public get rewardValue2(): number { return this._rewardValue2 }

  /** 보상3 */
  public get rewardType3(): CurrencyType { return this._rewardType3 }

  /** 보상 값3 */
  public get rewardValue3(): number { return this._rewardValue3 }

  /** 아이콘 경로 */
  public get iconPath(): string { return this._iconPath }

  public _id: number = 0
  public _name: string = ''
  public _infoName: string = ''
  public _agencyType: AgencyType = 0 as AgencyType
  public _agencyGrade: AgencyGrade = 0 as AgencyGrade
  public _time: number = 0
  public _agencyWeight: number = 0
  public _condition1: JobType = 0 as JobType
  public _condition1value: number = 0
  public _condition2: GradeType = 0 as GradeType
  public _condition2value: number = 0
  public _condition3: ConditionType = 0 as ConditionType
  public _classMinValue: number = 0
  public _condition3value: number = 0
  public _rewardType1: CurrencyType = 0 as CurrencyType
  public _rewardValue1: number = 0
  public _rewardType2: CurrencyType = 0 as CurrencyType
  public _rewardValue2: number = 0
  public _rewardType3: CurrencyType = 0 as CurrencyType
  public _rewardValue3: number = 0
  public _iconPath: string = ''

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._name = dataRow.name
    this._infoName = dataRow.infoName
    this._agencyType = dataRow.agencyType
    this._agencyGrade = dataRow.agencyGrade
    this._time = dataRow.time
    this._agencyWeight = dataRow.agencyWeight
    this._condition1 = dataRow.condition1
    this._condition1value = dataRow.condition1value
    this._condition2 = dataRow.condition2
    this._condition2value = dataRow.condition2value
    this._condition3 = dataRow.condition3
    this._classMinValue = dataRow.classMinValue
    this._condition3value = dataRow.condition3value
    this._rewardType1 = dataRow.rewardType1
    this._rewardValue1 = dataRow.rewardValue1
    this._rewardType2 = dataRow.rewardType2
    this._rewardValue2 = dataRow.rewardValue2
    this._rewardType3 = dataRow.rewardType3
    this._rewardValue3 = dataRow.rewardValue3
    this._iconPath = dataRow.iconPath
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._name = dataRow[offset++]
    this._infoName = dataRow[offset++]
    this._agencyType = dataRow[offset++]
    this._agencyGrade = dataRow[offset++]
    this._time = dataRow[offset++]
    this._agencyWeight = dataRow[offset++]
    this._condition1 = dataRow[offset++]
    this._condition1value = dataRow[offset++]
    this._condition2 = dataRow[offset++]
    this._condition2value = dataRow[offset++]
    this._condition3 = dataRow[offset++]
    this._classMinValue = dataRow[offset++]
    this._condition3value = dataRow[offset++]
    this._rewardType1 = dataRow[offset++]
    this._rewardValue1 = dataRow[offset++]
    this._rewardType2 = dataRow[offset++]
    this._rewardValue2 = dataRow[offset++]
    this._rewardType3 = dataRow[offset++]
    this._rewardValue3 = dataRow[offset++]
    this._iconPath = dataRow[offset++]
  }
}

// Generated from project-rescue-xlsxs\SD_Contens.xlsx : SDAgencyTable : A1
export class SDAgencyTableTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): SDAgencyTableRecord[] { return this._records }
  private _records: SDAgencyTableRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, SDAgencyTableRecord> { return this._recordsById }
  private _recordsById: Map<number, SDAgencyTableRecord> = new Map<number, SDAgencyTableRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): SDAgencyTableRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): SDAgencyTableRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "SDAgencyTable" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: SDAgencyTableRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new SDAgencyTableRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new SDAgencyTableRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .table file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.LiteBinaryReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: SDAgencyTableRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new SDAgencyTableRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'SDAgencyTable.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = reader.readI32As(column.element)
          }
          break
        case 2:
          sheetman.checkColumn(column, 'SDAgencyTable.Name', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._name = reader.readString()
          }
          break
        case 3:
          sheetman.checkColumn(column, 'SDAgencyTable.InfoName', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._infoName = reader.readString()
          }
          break
        case 4:
          sheetman.checkColumn(column, 'SDAgencyTable.AgencyType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._agencyType = reader.readEnum() as AgencyType
          }
          break
        case 5:
          sheetman.checkColumn(column, 'SDAgencyTable.AgencyGrade', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._agencyGrade = reader.readEnum() as AgencyGrade
          }
          break
        case 6:
          sheetman.checkColumn(column, 'SDAgencyTable.Time', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._time = reader.readI32As(column.element)
          }
          break
        case 7:
          sheetman.checkColumn(column, 'SDAgencyTable.AgencyWeight', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._agencyWeight = reader.readI32As(column.element)
          }
          break
        case 8:
          sheetman.checkColumn(column, 'SDAgencyTable.Condition1', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._condition1 = reader.readEnum() as JobType
          }
          break
        case 9:
          sheetman.checkColumn(column, 'SDAgencyTable.Condition1value', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._condition1value = reader.readI32As(column.element)
          }
          break
        case 10:
          sheetman.checkColumn(column, 'SDAgencyTable.Condition2', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._condition2 = reader.readEnum() as GradeType
          }
          break
        case 11:
          sheetman.checkColumn(column, 'SDAgencyTable.Condition2value', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._condition2value = reader.readI32As(column.element)
          }
          break
        case 12:
          sheetman.checkColumn(column, 'SDAgencyTable.Condition3', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._condition3 = reader.readEnum() as ConditionType
          }
          break
        case 13:
          sheetman.checkColumn(column, 'SDAgencyTable.ClassMinValue', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._classMinValue = reader.readI32As(column.element)
          }
          break
        case 14:
          sheetman.checkColumn(column, 'SDAgencyTable.Condition3value', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._condition3value = reader.readI32As(column.element)
          }
          break
        case 15:
          sheetman.checkColumn(column, 'SDAgencyTable.RewardType1', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._rewardType1 = reader.readEnum() as CurrencyType
          }
          break
        case 16:
          sheetman.checkColumn(column, 'SDAgencyTable.RewardValue1', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._rewardValue1 = reader.readI32As(column.element)
          }
          break
        case 17:
          sheetman.checkColumn(column, 'SDAgencyTable.RewardType2', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._rewardType2 = reader.readEnum() as CurrencyType
          }
          break
        case 18:
          sheetman.checkColumn(column, 'SDAgencyTable.RewardValue2', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._rewardValue2 = reader.readI32As(column.element)
          }
          break
        case 19:
          sheetman.checkColumn(column, 'SDAgencyTable.RewardType3', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._rewardType3 = reader.readEnum() as CurrencyType
          }
          break
        case 20:
          sheetman.checkColumn(column, 'SDAgencyTable.RewardValue3', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._rewardValue3 = reader.readI32As(column.element)
          }
          break
        case 21:
          sheetman.checkColumn(column, 'SDAgencyTable.IconPath', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._iconPath = reader.readString()
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: SDAgencyTableRecord[]): void {
    const recordsById = new Map<number, SDAgencyTableRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
