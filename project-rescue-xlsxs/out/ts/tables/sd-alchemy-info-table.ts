// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/lite-binary-reader'

// Automatically import to handle external type references.
import { CurrencyType } from '../enums/currency-type'
import { StatType } from '../enums/stat-type'
import { TargetType } from '../enums/target-type'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  name: string
  nameKR: string
  materialType: CurrencyType
  commonMaterialType1: CurrencyType
  commonMaterialType2: CurrencyType
  materialType2Rate: number
  statType: StatType
  targetType: TargetType
  maxLevelId: number
  accelerateTime: number
  accelerateItemType1: CurrencyType
  accelerateItemCost1: number
  accelerateItemType2: CurrencyType
  accelerateItemCost2: number
  iconPath: string
}

// Generated from project-rescue-xlsxs\SD_Contens.xlsx : SDAlchemyInfoTable : A1
export class SDAlchemyInfoTableRecord {
  /** Default constructor */
  constructor() {
  }

  /** SDAlchemyInfo_Table */
  public get id(): number { return this._id }

  /** 영문명 */
  public get name(): string { return this._name }

  /** 한글명 */
  public get nameKR(): string { return this._nameKR }

  /** 전용 재료 타입 */
  public get materialType(): CurrencyType { return this._materialType }

  /** 공용 재료1 타입 */
  public get commonMaterialType1(): CurrencyType { return this._commonMaterialType1 }

  /** 공용 재료2 타입 */
  public get commonMaterialType2(): CurrencyType { return this._commonMaterialType2 }

  /** 재료값 증가 비율 */
  public get materialType2Rate(): number { return this._materialType2Rate }

  /** 스텟 타입 */
  public get statType(): StatType { return this._statType }

  /** 타겟 대상 */
  public get targetType(): TargetType { return this._targetType }

  /** 최대 레벨값 */
  public get maxLevelId(): number { return this._maxLevelId }

  /** 가속 시간 */
  public get accelerateTime(): number { return this._accelerateTime }

  /** 가속권 타입 */
  public get accelerateItemType1(): CurrencyType { return this._accelerateItemType1 }

  /** 가속권 개수 */
  public get accelerateItemCost1(): number { return this._accelerateItemCost1 }

  /** 다이아 */
  public get accelerateItemType2(): CurrencyType { return this._accelerateItemType2 }

  /** 필요 다이아 개수 */
  public get accelerateItemCost2(): number { return this._accelerateItemCost2 }

  /** 아이콘 경로 */
  public get iconPath(): string { return this._iconPath }

  public _id: number = 0
  public _name: string = ''
  public _nameKR: string = ''
  public _materialType: CurrencyType = 0 as CurrencyType
  public _commonMaterialType1: CurrencyType = 0 as CurrencyType
  public _commonMaterialType2: CurrencyType = 0 as CurrencyType
  public _materialType2Rate: number = 0
  public _statType: StatType = 0 as StatType
  public _targetType: TargetType = 0 as TargetType
  public _maxLevelId: number = 0
  public _accelerateTime: number = 0
  public _accelerateItemType1: CurrencyType = 0 as CurrencyType
  public _accelerateItemCost1: number = 0
  public _accelerateItemType2: CurrencyType = 0 as CurrencyType
  public _accelerateItemCost2: number = 0
  public _iconPath: string = ''

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._name = dataRow.name
    this._nameKR = dataRow.nameKR
    this._materialType = dataRow.materialType
    this._commonMaterialType1 = dataRow.commonMaterialType1
    this._commonMaterialType2 = dataRow.commonMaterialType2
    this._materialType2Rate = Math.fround(dataRow.materialType2Rate)
    this._statType = dataRow.statType
    this._targetType = dataRow.targetType
    this._maxLevelId = dataRow.maxLevelId
    this._accelerateTime = dataRow.accelerateTime
    this._accelerateItemType1 = dataRow.accelerateItemType1
    this._accelerateItemCost1 = dataRow.accelerateItemCost1
    this._accelerateItemType2 = dataRow.accelerateItemType2
    this._accelerateItemCost2 = dataRow.accelerateItemCost2
    this._iconPath = dataRow.iconPath
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._name = dataRow[offset++]
    this._nameKR = dataRow[offset++]
    this._materialType = dataRow[offset++]
    this._commonMaterialType1 = dataRow[offset++]
    this._commonMaterialType2 = dataRow[offset++]
    this._materialType2Rate = Math.fround(dataRow[offset++])
    this._statType = dataRow[offset++]
    this._targetType = dataRow[offset++]
    this._maxLevelId = dataRow[offset++]
    this._accelerateTime = dataRow[offset++]
    this._accelerateItemType1 = dataRow[offset++]
    this._accelerateItemCost1 = dataRow[offset++]
    this._accelerateItemType2 = dataRow[offset++]
    this._accelerateItemCost2 = dataRow[offset++]
    this._iconPath = dataRow[offset++]
  }
}

// Generated from project-rescue-xlsxs\SD_Contens.xlsx : SDAlchemyInfoTable : A1
export class SDAlchemyInfoTableTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): SDAlchemyInfoTableRecord[] { return this._records }
  private _records: SDAlchemyInfoTableRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, SDAlchemyInfoTableRecord> { return this._recordsById }
  private _recordsById: Map<number, SDAlchemyInfoTableRecord> = new Map<number, SDAlchemyInfoTableRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): SDAlchemyInfoTableRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): SDAlchemyInfoTableRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "SDAlchemyInfoTable" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: SDAlchemyInfoTableRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new SDAlchemyInfoTableRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new SDAlchemyInfoTableRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .table file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.LiteBinaryReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: SDAlchemyInfoTableRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new SDAlchemyInfoTableRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'SDAlchemyInfoTable.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = reader.readI32As(column.element)
          }
          break
        case 2:
          sheetman.checkColumn(column, 'SDAlchemyInfoTable.Name', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._name = reader.readString()
          }
          break
        case 3:
          sheetman.checkColumn(column, 'SDAlchemyInfoTable.NameKR', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._nameKR = reader.readString()
          }
          break
        case 4:
          sheetman.checkColumn(column, 'SDAlchemyInfoTable.MaterialType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._materialType = reader.readEnum() as CurrencyType
          }
          break
        case 5:
          sheetman.checkColumn(column, 'SDAlchemyInfoTable.CommonMaterialType1', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._commonMaterialType1 = reader.readEnum() as CurrencyType
          }
          break
        case 6:
          sheetman.checkColumn(column, 'SDAlchemyInfoTable.CommonMaterialType2', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._commonMaterialType2 = reader.readEnum() as CurrencyType
          }
          break
        case 7:
          sheetman.checkColumn(column, 'SDAlchemyInfoTable.MaterialType2Rate', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._materialType2Rate = reader.readFloat()
          }
          break
        case 8:
          sheetman.checkColumn(column, 'SDAlchemyInfoTable.StatType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._statType = reader.readEnum() as StatType
          }
          break
        case 9:
          sheetman.checkColumn(column, 'SDAlchemyInfoTable.TargetType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._targetType = reader.readEnum() as TargetType
          }
          break
        case 10:
          sheetman.checkColumn(column, 'SDAlchemyInfoTable.MaxLevelId', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._maxLevelId = reader.readI32As(column.element)
          }
          break
        case 11:
          sheetman.checkColumn(column, 'SDAlchemyInfoTable.AccelerateTime', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._accelerateTime = reader.readI32As(column.element)
          }
          break
        case 12:
          sheetman.checkColumn(column, 'SDAlchemyInfoTable.AccelerateItemType1', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._accelerateItemType1 = reader.readEnum() as CurrencyType
          }
          break
        case 13:
          sheetman.checkColumn(column, 'SDAlchemyInfoTable.AccelerateItemCost1', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._accelerateItemCost1 = reader.readI32As(column.element)
          }
          break
        case 14:
          sheetman.checkColumn(column, 'SDAlchemyInfoTable.AccelerateItemType2', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._accelerateItemType2 = reader.readEnum() as CurrencyType
          }
          break
        case 15:
          sheetman.checkColumn(column, 'SDAlchemyInfoTable.AccelerateItemCost2', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._accelerateItemCost2 = reader.readI32As(column.element)
          }
          break
        case 16:
          sheetman.checkColumn(column, 'SDAlchemyInfoTable.IconPath', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._iconPath = reader.readString()
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: SDAlchemyInfoTableRecord[]): void {
    const recordsById = new Map<number, SDAlchemyInfoTableRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
