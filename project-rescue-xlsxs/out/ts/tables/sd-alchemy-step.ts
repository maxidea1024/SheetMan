// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/scb-reader'

// Automatically import to handle external type references.
import { CurrencyType } from '../enums/currency-type'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  name: string
  nameKR: string
  maxLevel: number
  rewardType: CurrencyType
  rewardValue: number
}

// Generated from project-rescue-xlsxs/SD_Contens.xlsx : SDAlchemyStepTable : A1
export class SDAlchemyStepRecord {
  /** Default constructor */
  constructor() {
  }

  /** SDAlchemyStep_Table */
  public get id(): number { return this._id }

  /** 영문명 */
  public get name(): string { return this._name }

  /** 한글명 */
  public get nameKR(): string { return this._nameKR }

  /** 최대 레벨 */
  public get maxLevel(): number { return this._maxLevel }

  /** 보상 타입 */
  public get rewardType(): CurrencyType { return this._rewardType }

  /** 보상 값 */
  public get rewardValue(): number { return this._rewardValue }

  public _id: number = 0
  public _name: string = ''
  public _nameKR: string = ''
  public _maxLevel: number = 0
  public _rewardType: CurrencyType = 0 as CurrencyType
  public _rewardValue: number = 0

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._name = dataRow.name
    this._nameKR = dataRow.nameKR
    this._maxLevel = dataRow.maxLevel
    this._rewardType = dataRow.rewardType
    this._rewardValue = dataRow.rewardValue
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._name = dataRow[offset++]
    this._nameKR = dataRow[offset++]
    this._maxLevel = dataRow[offset++]
    this._rewardType = dataRow[offset++]
    this._rewardValue = dataRow[offset++]
  }
}

// Generated from project-rescue-xlsxs/SD_Contens.xlsx : SDAlchemyStepTable : A1
export class SDAlchemyStepTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): SDAlchemyStepRecord[] { return this._records }
  private _records: SDAlchemyStepRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, SDAlchemyStepRecord> { return this._recordsById }
  private _recordsById: Map<number, SDAlchemyStepRecord> = new Map<number, SDAlchemyStepRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): SDAlchemyStepRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): SDAlchemyStepRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "SDAlchemyStep" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: SDAlchemyStepRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new SDAlchemyStepRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new SDAlchemyStepRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .scb file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.ScbReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)
    let cursor: sheetman.ScbColumnCursor

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: SDAlchemyStepRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new SDAlchemyStepRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'SDAlchemyStep.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDAlchemyStep.Id')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = cursor.nextI32()
          }
          break
        case 2:
          sheetman.checkColumn(column, 'SDAlchemyStep.Name', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDAlchemyStep.Name')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._name = cursor.nextString()
          }
          break
        case 3:
          sheetman.checkColumn(column, 'SDAlchemyStep.NameKR', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDAlchemyStep.NameKR')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._nameKR = cursor.nextString()
          }
          break
        case 4:
          sheetman.checkColumn(column, 'SDAlchemyStep.MaxLevel', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDAlchemyStep.MaxLevel')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._maxLevel = cursor.nextI32()
          }
          break
        case 5:
          sheetman.checkColumn(column, 'SDAlchemyStep.RewardType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDAlchemyStep.RewardType')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._rewardType = cursor.nextI32() as CurrencyType
          }
          break
        case 6:
          sheetman.checkColumn(column, 'SDAlchemyStep.RewardValue', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDAlchemyStep.RewardValue')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._rewardValue = cursor.nextI32()
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: SDAlchemyStepRecord[]): void {
    const recordsById = new Map<number, SDAlchemyStepRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
