// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/scb-reader'

// Automatically import to handle external type references.
import { SdContensType } from '../enums/sd-contens-type'
import { CurrencyType } from '../enums/currency-type'
import { CycleType } from '../enums/cycle-type'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  name: string
  infoName: string
  sheetName: string
  sdContensType: SdContensType
  conditionID: number
  currencyType: CurrencyType
  enableReset: boolean
  cycleType: CycleType
  sDCharacterPath: string
  prefabPath: string
}

// Generated from project-rescue-xlsxs/SD_Contens.xlsx : SDContensInfoTable : A1
export class SDContensInfoRecord {
  /** Default constructor */
  constructor() {
  }

  /** SDContensInfo_Table */
  public get id(): number { return this._id }

  /** 영문 */
  public get name(): string { return this._name }

  /** 한글 */
  public get infoName(): string { return this._infoName }

  /** 실제 데이터가 있는 시트 이름 */
  public get sheetName(): string { return this._sheetName }

  /** SD 콘텐츠 타입 */
  public get sdContensType(): SdContensType { return this._sdContensType }

  /** 콘텐츠 해금 조건 ID */
  public get conditionID(): number { return this._conditionID }

  /** 소모 재화 */
  public get currencyType(): CurrencyType { return this._currencyType }

  /** 초기화 적용 유무 */
  public get enableReset(): boolean { return this._enableReset }

  /** 초기화 타입 */
  public get cycleType(): CycleType { return this._cycleType }

  /** SD  캐릭터 프리펩 경로 */
  public get sDCharacterPath(): string { return this._sDCharacterPath }

  /** UI 3d프리펩 경로 */
  public get prefabPath(): string { return this._prefabPath }

  public _id: number = 0
  public _name: string = ''
  public _infoName: string = ''
  public _sheetName: string = ''
  public _sdContensType: SdContensType = 0 as SdContensType
  public _conditionID: number = 0
  public _currencyType: CurrencyType = 0 as CurrencyType
  public _enableReset: boolean = false
  public _cycleType: CycleType = 0 as CycleType
  public _sDCharacterPath: string = ''
  public _prefabPath: string = ''

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._name = dataRow.name
    this._infoName = dataRow.infoName
    this._sheetName = dataRow.sheetName
    this._sdContensType = dataRow.sdContensType
    this._conditionID = dataRow.conditionID
    this._currencyType = dataRow.currencyType
    this._enableReset = dataRow.enableReset
    this._cycleType = dataRow.cycleType
    this._sDCharacterPath = dataRow.sDCharacterPath
    this._prefabPath = dataRow.prefabPath
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._name = dataRow[offset++]
    this._infoName = dataRow[offset++]
    this._sheetName = dataRow[offset++]
    this._sdContensType = dataRow[offset++]
    this._conditionID = dataRow[offset++]
    this._currencyType = dataRow[offset++]
    this._enableReset = dataRow[offset++]
    this._cycleType = dataRow[offset++]
    this._sDCharacterPath = dataRow[offset++]
    this._prefabPath = dataRow[offset++]
  }
}

// Generated from project-rescue-xlsxs/SD_Contens.xlsx : SDContensInfoTable : A1
export class SDContensInfoTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): SDContensInfoRecord[] { return this._records }
  private _records: SDContensInfoRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, SDContensInfoRecord> { return this._recordsById }
  private _recordsById: Map<number, SDContensInfoRecord> = new Map<number, SDContensInfoRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): SDContensInfoRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): SDContensInfoRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "SDContensInfo" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: SDContensInfoRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new SDContensInfoRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new SDContensInfoRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .scb file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.ScbReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)
    let cursor: sheetman.ScbColumnCursor

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: SDContensInfoRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new SDContensInfoRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'SDContensInfo.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDContensInfo.Id')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = cursor.nextI32()
          }
          break
        case 2:
          sheetman.checkColumn(column, 'SDContensInfo.Name', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDContensInfo.Name')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._name = cursor.nextString()
          }
          break
        case 3:
          sheetman.checkColumn(column, 'SDContensInfo.InfoName', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDContensInfo.InfoName')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._infoName = cursor.nextString()
          }
          break
        case 4:
          sheetman.checkColumn(column, 'SDContensInfo.SheetName', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDContensInfo.SheetName')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._sheetName = cursor.nextString()
          }
          break
        case 5:
          sheetman.checkColumn(column, 'SDContensInfo.SdContensType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDContensInfo.SdContensType')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._sdContensType = cursor.nextI32() as SdContensType
          }
          break
        case 6:
          sheetman.checkColumn(column, 'SDContensInfo.ConditionID', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDContensInfo.ConditionID')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._conditionID = cursor.nextI32()
          }
          break
        case 7:
          sheetman.checkColumn(column, 'SDContensInfo.CurrencyType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDContensInfo.CurrencyType')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._currencyType = cursor.nextI32() as CurrencyType
          }
          break
        case 8:
          sheetman.checkColumn(column, 'SDContensInfo.EnableReset', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_BOOL])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._enableReset = reader.readBool()
          }
          break
        case 9:
          sheetman.checkColumn(column, 'SDContensInfo.CycleType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDContensInfo.CycleType')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._cycleType = cursor.nextI32() as CycleType
          }
          break
        case 10:
          sheetman.checkColumn(column, 'SDContensInfo.SDCharacterPath', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDContensInfo.SDCharacterPath')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._sDCharacterPath = cursor.nextString()
          }
          break
        case 11:
          sheetman.checkColumn(column, 'SDContensInfo.PrefabPath', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDContensInfo.PrefabPath')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._prefabPath = cursor.nextString()
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: SDContensInfoRecord[]): void {
    const recordsById = new Map<number, SDContensInfoRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
