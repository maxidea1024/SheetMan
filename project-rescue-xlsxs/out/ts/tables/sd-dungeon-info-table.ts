// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/lite-binary-reader'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  name: string
  nameKR: string
  totalCount: number
  trapCount: number
  actionCount: number
  matchCost: number
  mismatchCost: number
  trapCost: number
  viewTime: number
  time: number
  aDCount: number
  dailyFreeCount: number
}

// Generated from project-rescue-xlsxs\SD_Contens.xlsx : SDDungeonInfoTable : A1
export class SDDungeonInfoTableRecord {
  /** Default constructor */
  constructor() {
  }

  /** SDDungeonInfo_Table */
  public get id(): number { return this._id }

  /** 영문명 */
  public get name(): string { return this._name }

  /** 한글명 */
  public get nameKR(): string { return this._nameKR }

  /** 총 카드 수 */
  public get totalCount(): number { return this._totalCount }

  /** 함정 카드 수 */
  public get trapCount(): number { return this._trapCount }

  /** 행동 수 */
  public get actionCount(): number { return this._actionCount }

  /** 성공 차감수 */
  public get matchCost(): number { return this._matchCost }

  /** 실패 차감수 */
  public get mismatchCost(): number { return this._mismatchCost }

  /** 함정 차감수 */
  public get trapCost(): number { return this._trapCost }

  /** 미리 보기 시간 */
  public get viewTime(): number { return this._viewTime }

  /** 오답 체크 시간 */
  public get time(): number { return this._time }

  /** 광고 횟수 */
  public get aDCount(): number { return this._aDCount }

  /** 일일 무료 횟수 */
  public get dailyFreeCount(): number { return this._dailyFreeCount }

  public _id: number = 0
  public _name: string = ''
  public _nameKR: string = ''
  public _totalCount: number = 0
  public _trapCount: number = 0
  public _actionCount: number = 0
  public _matchCost: number = 0
  public _mismatchCost: number = 0
  public _trapCost: number = 0
  public _viewTime: number = 0
  public _time: number = 0
  public _aDCount: number = 0
  public _dailyFreeCount: number = 0

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._name = dataRow.name
    this._nameKR = dataRow.nameKR
    this._totalCount = dataRow.totalCount
    this._trapCount = dataRow.trapCount
    this._actionCount = dataRow.actionCount
    this._matchCost = dataRow.matchCost
    this._mismatchCost = dataRow.mismatchCost
    this._trapCost = dataRow.trapCost
    this._viewTime = dataRow.viewTime
    this._time = dataRow.time
    this._aDCount = dataRow.aDCount
    this._dailyFreeCount = dataRow.dailyFreeCount
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._name = dataRow[offset++]
    this._nameKR = dataRow[offset++]
    this._totalCount = dataRow[offset++]
    this._trapCount = dataRow[offset++]
    this._actionCount = dataRow[offset++]
    this._matchCost = dataRow[offset++]
    this._mismatchCost = dataRow[offset++]
    this._trapCost = dataRow[offset++]
    this._viewTime = dataRow[offset++]
    this._time = dataRow[offset++]
    this._aDCount = dataRow[offset++]
    this._dailyFreeCount = dataRow[offset++]
  }
}

// Generated from project-rescue-xlsxs\SD_Contens.xlsx : SDDungeonInfoTable : A1
export class SDDungeonInfoTableTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): SDDungeonInfoTableRecord[] { return this._records }
  private _records: SDDungeonInfoTableRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, SDDungeonInfoTableRecord> { return this._recordsById }
  private _recordsById: Map<number, SDDungeonInfoTableRecord> = new Map<number, SDDungeonInfoTableRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): SDDungeonInfoTableRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): SDDungeonInfoTableRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "SDDungeonInfoTable" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: SDDungeonInfoTableRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new SDDungeonInfoTableRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new SDDungeonInfoTableRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .table file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.LiteBinaryReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: SDDungeonInfoTableRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new SDDungeonInfoTableRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'SDDungeonInfoTable.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = reader.readI32As(column.element)
          }
          break
        case 2:
          sheetman.checkColumn(column, 'SDDungeonInfoTable.Name', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._name = reader.readString()
          }
          break
        case 3:
          sheetman.checkColumn(column, 'SDDungeonInfoTable.NameKR', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._nameKR = reader.readString()
          }
          break
        case 4:
          sheetman.checkColumn(column, 'SDDungeonInfoTable.TotalCount', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._totalCount = reader.readI32As(column.element)
          }
          break
        case 5:
          sheetman.checkColumn(column, 'SDDungeonInfoTable.TrapCount', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._trapCount = reader.readI32As(column.element)
          }
          break
        case 6:
          sheetman.checkColumn(column, 'SDDungeonInfoTable.ActionCount', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._actionCount = reader.readI32As(column.element)
          }
          break
        case 7:
          sheetman.checkColumn(column, 'SDDungeonInfoTable.MatchCost', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._matchCost = reader.readI32As(column.element)
          }
          break
        case 8:
          sheetman.checkColumn(column, 'SDDungeonInfoTable.MismatchCost', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._mismatchCost = reader.readI32As(column.element)
          }
          break
        case 9:
          sheetman.checkColumn(column, 'SDDungeonInfoTable.TrapCost', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._trapCost = reader.readI32As(column.element)
          }
          break
        case 10:
          sheetman.checkColumn(column, 'SDDungeonInfoTable.ViewTime', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._viewTime = reader.readI32As(column.element)
          }
          break
        case 11:
          sheetman.checkColumn(column, 'SDDungeonInfoTable.Time', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._time = reader.readI32As(column.element)
          }
          break
        case 12:
          sheetman.checkColumn(column, 'SDDungeonInfoTable.ADCount', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._aDCount = reader.readI32As(column.element)
          }
          break
        case 13:
          sheetman.checkColumn(column, 'SDDungeonInfoTable.DailyFreeCount', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._dailyFreeCount = reader.readI32As(column.element)
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: SDDungeonInfoTableRecord[]): void {
    const recordsById = new Map<number, SDDungeonInfoTableRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
