// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/lite-binary-reader'

// Automatically import to handle external type references.
import { AttributeType } from '../enums/attribute-type'
import { StatType } from '../enums/stat-type'
import { CurrencyType } from '../enums/currency-type'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  name: string
  trainingName: string
  attributeType: AttributeType
  statType: StatType
  unlockCondition: number
  levelUpCondition: number
  maxLevel: number
  isLvReset: boolean
  lvResetCurrencyType: CurrencyType
  lvResetCurrencyValue: number
  description: string
}

// Generated from project-rescue-xlsxs\SD_Contens.xlsx : SDTrainingInfoTable : A1
export class SDTrainingInfoTableRecord {
  /** Default constructor */
  constructor() {
  }

  /** SDTrainingInfo_Table */
  public get id(): number { return this._id }

  /** 영문명 */
  public get name(): string { return this._name }

  /** 한글명 */
  public get trainingName(): string { return this._trainingName }

  /** 적용 속성 타입 */
  public get attributeType(): AttributeType { return this._attributeType }

  /** 스탯 타입 */
  public get statType(): StatType { return this._statType }

  /** 해제 조건 대상 */
  public get unlockCondition(): number { return this._unlockCondition }

  /** 레벨업 제한 타입 */
  public get levelUpCondition(): number { return this._levelUpCondition }

  /** 최대 레벨 */
  public get maxLevel(): number { return this._maxLevel }

  /** 레벨 초기화 여부 */
  public get isLvReset(): boolean { return this._isLvReset }

  /** 레벨 초기화 재화 */
  public get lvResetCurrencyType(): CurrencyType { return this._lvResetCurrencyType }

  /** 레벨 초기화 재화값 */
  public get lvResetCurrencyValue(): number { return this._lvResetCurrencyValue }

  /** 설명 */
  public get description(): string { return this._description }

  public _id: number = 0
  public _name: string = ''
  public _trainingName: string = ''
  public _attributeType: AttributeType = 0 as AttributeType
  public _statType: StatType = 0 as StatType
  public _unlockCondition: number = 0
  public _levelUpCondition: number = 0
  public _maxLevel: number = 0
  public _isLvReset: boolean = false
  public _lvResetCurrencyType: CurrencyType = 0 as CurrencyType
  public _lvResetCurrencyValue: number = 0
  public _description: string = ''

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._name = dataRow.name
    this._trainingName = dataRow.trainingName
    this._attributeType = dataRow.attributeType
    this._statType = dataRow.statType
    this._unlockCondition = dataRow.unlockCondition
    this._levelUpCondition = dataRow.levelUpCondition
    this._maxLevel = dataRow.maxLevel
    this._isLvReset = dataRow.isLvReset
    this._lvResetCurrencyType = dataRow.lvResetCurrencyType
    this._lvResetCurrencyValue = dataRow.lvResetCurrencyValue
    this._description = dataRow.description
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._name = dataRow[offset++]
    this._trainingName = dataRow[offset++]
    this._attributeType = dataRow[offset++]
    this._statType = dataRow[offset++]
    this._unlockCondition = dataRow[offset++]
    this._levelUpCondition = dataRow[offset++]
    this._maxLevel = dataRow[offset++]
    this._isLvReset = dataRow[offset++]
    this._lvResetCurrencyType = dataRow[offset++]
    this._lvResetCurrencyValue = dataRow[offset++]
    this._description = dataRow[offset++]
  }
}

// Generated from project-rescue-xlsxs\SD_Contens.xlsx : SDTrainingInfoTable : A1
export class SDTrainingInfoTableTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): SDTrainingInfoTableRecord[] { return this._records }
  private _records: SDTrainingInfoTableRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, SDTrainingInfoTableRecord> { return this._recordsById }
  private _recordsById: Map<number, SDTrainingInfoTableRecord> = new Map<number, SDTrainingInfoTableRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): SDTrainingInfoTableRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): SDTrainingInfoTableRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "SDTrainingInfoTable" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: SDTrainingInfoTableRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new SDTrainingInfoTableRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new SDTrainingInfoTableRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .table file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.LiteBinaryReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: SDTrainingInfoTableRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new SDTrainingInfoTableRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'SDTrainingInfoTable.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = reader.readI32As(column.element)
          }
          break
        case 2:
          sheetman.checkColumn(column, 'SDTrainingInfoTable.Name', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._name = reader.readString()
          }
          break
        case 3:
          sheetman.checkColumn(column, 'SDTrainingInfoTable.TrainingName', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._trainingName = reader.readString()
          }
          break
        case 4:
          sheetman.checkColumn(column, 'SDTrainingInfoTable.AttributeType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._attributeType = reader.readEnum() as AttributeType
          }
          break
        case 5:
          sheetman.checkColumn(column, 'SDTrainingInfoTable.StatType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._statType = reader.readEnum() as StatType
          }
          break
        case 6:
          sheetman.checkColumn(column, 'SDTrainingInfoTable.UnlockCondition', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._unlockCondition = reader.readI32As(column.element)
          }
          break
        case 7:
          sheetman.checkColumn(column, 'SDTrainingInfoTable.LevelUpCondition', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._levelUpCondition = reader.readI32As(column.element)
          }
          break
        case 8:
          sheetman.checkColumn(column, 'SDTrainingInfoTable.MaxLevel', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._maxLevel = reader.readI32As(column.element)
          }
          break
        case 9:
          sheetman.checkColumn(column, 'SDTrainingInfoTable.IsLvReset', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_BOOL])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._isLvReset = reader.readBool()
          }
          break
        case 10:
          sheetman.checkColumn(column, 'SDTrainingInfoTable.LvResetCurrencyType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._lvResetCurrencyType = reader.readEnum() as CurrencyType
          }
          break
        case 11:
          sheetman.checkColumn(column, 'SDTrainingInfoTable.LvResetCurrencyValue', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._lvResetCurrencyValue = reader.readI32As(column.element)
          }
          break
        case 12:
          sheetman.checkColumn(column, 'SDTrainingInfoTable.Description', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._description = reader.readString()
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: SDTrainingInfoTableRecord[]): void {
    const recordsById = new Map<number, SDTrainingInfoTableRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
