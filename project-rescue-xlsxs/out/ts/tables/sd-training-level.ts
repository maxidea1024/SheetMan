// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/scb-reader'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  name: string
  levelName: string
  currencyValue: number
  currencyResult: number
  mHPGrowth: number
  mHPTotal: number
  dEFGrowth: number
  dEFTotal: number
  commonUnlockStageID: number
}

// Generated from project-rescue-xlsxs/SD_Contens.xlsx : SDTrainingLevelTable : A1
export class SDTrainingLevelRecord {
  /** Default constructor */
  constructor() {
  }

  /** SDTrainingLevel_Table */
  public get id(): number { return this._id }

  /** 영문명 */
  public get name(): string { return this._name }

  /** 한글명 */
  public get levelName(): string { return this._levelName }

  /** 필요 재료값 */
  public get currencyValue(): number { return this._currencyValue }

  /** 누적 재료값 */
  public get currencyResult(): number { return this._currencyResult }

  /** 체력 고정 비율 */
  public get mHPGrowth(): number { return this._mHPGrowth }

  /** 체력 비율 누적 값 */
  public get mHPTotal(): number { return this._mHPTotal }

  /** 방어력 고정 비율 */
  public get dEFGrowth(): number { return this._dEFGrowth }

  /** 방어력 비율 누적 값 */
  public get dEFTotal(): number { return this._dEFTotal }

  /** 공통 훈련 스테이지 진행도 */
  public get commonUnlockStageID(): number { return this._commonUnlockStageID }

  public _id: number = 0
  public _name: string = ''
  public _levelName: string = ''
  public _currencyValue: number = 0
  public _currencyResult: number = 0
  public _mHPGrowth: number = 0
  public _mHPTotal: number = 0
  public _dEFGrowth: number = 0
  public _dEFTotal: number = 0
  public _commonUnlockStageID: number = 0

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._name = dataRow.name
    this._levelName = dataRow.levelName
    this._currencyValue = dataRow.currencyValue
    this._currencyResult = dataRow.currencyResult
    this._mHPGrowth = Math.fround(dataRow.mHPGrowth)
    this._mHPTotal = Math.fround(dataRow.mHPTotal)
    this._dEFGrowth = Math.fround(dataRow.dEFGrowth)
    this._dEFTotal = Math.fround(dataRow.dEFTotal)
    this._commonUnlockStageID = dataRow.commonUnlockStageID
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._name = dataRow[offset++]
    this._levelName = dataRow[offset++]
    this._currencyValue = dataRow[offset++]
    this._currencyResult = dataRow[offset++]
    this._mHPGrowth = Math.fround(dataRow[offset++])
    this._mHPTotal = Math.fround(dataRow[offset++])
    this._dEFGrowth = Math.fround(dataRow[offset++])
    this._dEFTotal = Math.fround(dataRow[offset++])
    this._commonUnlockStageID = dataRow[offset++]
  }
}

// Generated from project-rescue-xlsxs/SD_Contens.xlsx : SDTrainingLevelTable : A1
export class SDTrainingLevelTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): SDTrainingLevelRecord[] { return this._records }
  private _records: SDTrainingLevelRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, SDTrainingLevelRecord> { return this._recordsById }
  private _recordsById: Map<number, SDTrainingLevelRecord> = new Map<number, SDTrainingLevelRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): SDTrainingLevelRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): SDTrainingLevelRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "SDTrainingLevel" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: SDTrainingLevelRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new SDTrainingLevelRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new SDTrainingLevelRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .scb file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.ScbReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: SDTrainingLevelRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new SDTrainingLevelRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'SDTrainingLevel.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = reader.readI32As(column.element)
          }
          break
        case 2:
          sheetman.checkColumn(column, 'SDTrainingLevel.Name', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._name = reader.readString()
          }
          break
        case 3:
          sheetman.checkColumn(column, 'SDTrainingLevel.LevelName', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._levelName = reader.readString()
          }
          break
        case 4:
          sheetman.checkColumn(column, 'SDTrainingLevel.CurrencyValue', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._currencyValue = reader.readI32As(column.element)
          }
          break
        case 5:
          sheetman.checkColumn(column, 'SDTrainingLevel.CurrencyResult', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._currencyResult = reader.readI32As(column.element)
          }
          break
        case 6:
          sheetman.checkColumn(column, 'SDTrainingLevel.MHPGrowth', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._mHPGrowth = reader.readFloat()
          }
          break
        case 7:
          sheetman.checkColumn(column, 'SDTrainingLevel.MHPTotal', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._mHPTotal = reader.readFloat()
          }
          break
        case 8:
          sheetman.checkColumn(column, 'SDTrainingLevel.DEFGrowth', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._dEFGrowth = reader.readFloat()
          }
          break
        case 9:
          sheetman.checkColumn(column, 'SDTrainingLevel.DEFTotal', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._dEFTotal = reader.readFloat()
          }
          break
        case 10:
          sheetman.checkColumn(column, 'SDTrainingLevel.CommonUnlockStageID', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._commonUnlockStageID = reader.readI32As(column.element)
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: SDTrainingLevelRecord[]): void {
    const recordsById = new Map<number, SDTrainingLevelRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
