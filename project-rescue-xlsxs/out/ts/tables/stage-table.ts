// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/lite-binary-reader'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  name: string
  stageName: string
  assetDataPath: string
  spawnIds: number[]
  spawnPointCount: number
  spawnMaxCount: number
  spawnNextTime: number
  stageClearCount: number
  bossID: number[]
  atkUpPercent: number
  defUpPercent: number
  mHPUpPercent: number
  bossAtkUpPercent: number
  bossDefUpPercent: number
  bossMHPUpPercent: number
  stageDropListID: number
  stageBGMID: number
  bossStageBGMID: number
}

// Generated from project-rescue-xlsxs/MasterData.xlsx : StageTable : A1
export class StageTableRecord {
  /** Default constructor */
  constructor() {
  }

  /** Stage_Table */
  public get id(): number { return this._id }

  /** 영문 */
  public get name(): string { return this._name }

  /** 한글 */
  public get stageName(): string { return this._stageName }

  /** AssetData 경로 */
  public get assetDataPath(): string { return this._assetDataPath }

  /** 생성 몬스터 ID */
  public get spawnIds(): number[] { return this._spawnIds }

  /** 위치 생성 가능 횟수 */
  public get spawnPointCount(): number { return this._spawnPointCount }

  /** 최대 생성 수 */
  public get spawnMaxCount(): number { return this._spawnMaxCount }

  /** 다음 포인터 생성 시간 */
  public get spawnNextTime(): number { return this._spawnNextTime }

  /** 클리어 조건 */
  public get stageClearCount(): number { return this._stageClearCount }

  /** 보스 ID */
  public get bossID(): number[] { return this._bossID }

  /** 일반 ATK증가 값 */
  public get atkUpPercent(): number { return this._atkUpPercent }

  /** 일반 DEF증가 값 */
  public get defUpPercent(): number { return this._defUpPercent }

  /** 일반 HP증가 값 */
  public get mHPUpPercent(): number { return this._mHPUpPercent }

  /** 보스 ATK증가 값 */
  public get bossAtkUpPercent(): number { return this._bossAtkUpPercent }

  /** 보스 DEF증가 값 */
  public get bossDefUpPercent(): number { return this._bossDefUpPercent }

  /** 보스 HP증가 값 */
  public get bossMHPUpPercent(): number { return this._bossMHPUpPercent }

  /** 드랍 테이블 */
  public get stageDropListID(): number { return this._stageDropListID }

  /** 스테이지 BGM */
  public get stageBGMID(): number { return this._stageBGMID }

  /** 보스 스테이지 BGM */
  public get bossStageBGMID(): number { return this._bossStageBGMID }

  public _id: number = 0
  public _name: string = ''
  public _stageName: string = ''
  public _assetDataPath: string = ''
  public _spawnIds: number[] = []
  public _spawnPointCount: number = 0
  public _spawnMaxCount: number = 0
  public _spawnNextTime: number = 0
  public _stageClearCount: number = 0
  public _bossID: number[] = []
  public _atkUpPercent: number = 0
  public _defUpPercent: number = 0
  public _mHPUpPercent: number = 0
  public _bossAtkUpPercent: number = 0
  public _bossDefUpPercent: number = 0
  public _bossMHPUpPercent: number = 0
  public _stageDropListID: number = 0
  public _stageBGMID: number = 0
  public _bossStageBGMID: number = 0

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._name = dataRow.name
    this._stageName = dataRow.stageName
    this._assetDataPath = dataRow.assetDataPath
    this._spawnIds = dataRow.spawnIds
    this._spawnPointCount = dataRow.spawnPointCount
    this._spawnMaxCount = dataRow.spawnMaxCount
    this._spawnNextTime = Math.fround(dataRow.spawnNextTime)
    this._stageClearCount = dataRow.stageClearCount
    this._bossID = dataRow.bossID
    this._atkUpPercent = Math.fround(dataRow.atkUpPercent)
    this._defUpPercent = Math.fround(dataRow.defUpPercent)
    this._mHPUpPercent = Math.fround(dataRow.mHPUpPercent)
    this._bossAtkUpPercent = Math.fround(dataRow.bossAtkUpPercent)
    this._bossDefUpPercent = Math.fround(dataRow.bossDefUpPercent)
    this._bossMHPUpPercent = Math.fround(dataRow.bossMHPUpPercent)
    this._stageDropListID = dataRow.stageDropListID
    this._stageBGMID = dataRow.stageBGMID
    this._bossStageBGMID = dataRow.bossStageBGMID
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._name = dataRow[offset++]
    this._stageName = dataRow[offset++]
    this._assetDataPath = dataRow[offset++]
    this._spawnIds = dataRow[offset++]
    this._spawnPointCount = dataRow[offset++]
    this._spawnMaxCount = dataRow[offset++]
    this._spawnNextTime = Math.fround(dataRow[offset++])
    this._stageClearCount = dataRow[offset++]
    this._bossID = dataRow[offset++]
    this._atkUpPercent = Math.fround(dataRow[offset++])
    this._defUpPercent = Math.fround(dataRow[offset++])
    this._mHPUpPercent = Math.fround(dataRow[offset++])
    this._bossAtkUpPercent = Math.fround(dataRow[offset++])
    this._bossDefUpPercent = Math.fround(dataRow[offset++])
    this._bossMHPUpPercent = Math.fround(dataRow[offset++])
    this._stageDropListID = dataRow[offset++]
    this._stageBGMID = dataRow[offset++]
    this._bossStageBGMID = dataRow[offset++]
  }
}

// Generated from project-rescue-xlsxs/MasterData.xlsx : StageTable : A1
export class StageTableTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): StageTableRecord[] { return this._records }
  private _records: StageTableRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, StageTableRecord> { return this._recordsById }
  private _recordsById: Map<number, StageTableRecord> = new Map<number, StageTableRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): StageTableRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): StageTableRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "StageTable" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: StageTableRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new StageTableRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new StageTableRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .scb file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.LiteBinaryReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: StageTableRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new StageTableRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'StageTable.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = reader.readI32As(column.element)
          }
          break
        case 2:
          sheetman.checkColumn(column, 'StageTable.Name', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._name = reader.readString()
          }
          break
        case 3:
          sheetman.checkColumn(column, 'StageTable.StageName', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._stageName = reader.readString()
          }
          break
        case 4:
          sheetman.checkColumn(column, 'StageTable.AssetDataPath', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._assetDataPath = reader.readString()
          }
          break
        case 5:
          sheetman.checkColumn(column, 'StageTable.SpawnIds', sheetman.KIND_VAR_ARRAY, 0, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            const elementCount = reader.readCounter32()
            record._spawnIds = []
            for (let j = 0; j < elementCount; ++j)
              record._spawnIds.push(reader.readI32As(column.element))
          }
          break
        case 6:
          sheetman.checkColumn(column, 'StageTable.SpawnPointCount', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._spawnPointCount = reader.readI32As(column.element)
          }
          break
        case 7:
          sheetman.checkColumn(column, 'StageTable.SpawnMaxCount', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._spawnMaxCount = reader.readI32As(column.element)
          }
          break
        case 8:
          sheetman.checkColumn(column, 'StageTable.SpawnNextTime', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._spawnNextTime = reader.readFloat()
          }
          break
        case 9:
          sheetman.checkColumn(column, 'StageTable.StageClearCount', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._stageClearCount = reader.readI32As(column.element)
          }
          break
        case 10:
          sheetman.checkColumn(column, 'StageTable.BossID', sheetman.KIND_VAR_ARRAY, 0, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            const elementCount = reader.readCounter32()
            record._bossID = []
            for (let j = 0; j < elementCount; ++j)
              record._bossID.push(reader.readI32As(column.element))
          }
          break
        case 11:
          sheetman.checkColumn(column, 'StageTable.AtkUpPercent', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._atkUpPercent = reader.readFloat()
          }
          break
        case 12:
          sheetman.checkColumn(column, 'StageTable.DefUpPercent', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._defUpPercent = reader.readFloat()
          }
          break
        case 13:
          sheetman.checkColumn(column, 'StageTable.MHPUpPercent', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._mHPUpPercent = reader.readFloat()
          }
          break
        case 14:
          sheetman.checkColumn(column, 'StageTable.BossAtkUpPercent', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._bossAtkUpPercent = reader.readFloat()
          }
          break
        case 15:
          sheetman.checkColumn(column, 'StageTable.BossDefUpPercent', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._bossDefUpPercent = reader.readFloat()
          }
          break
        case 16:
          sheetman.checkColumn(column, 'StageTable.BossMHPUpPercent', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._bossMHPUpPercent = reader.readFloat()
          }
          break
        case 17:
          sheetman.checkColumn(column, 'StageTable.StageDropListID', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._stageDropListID = reader.readI32As(column.element)
          }
          break
        case 18:
          sheetman.checkColumn(column, 'StageTable.StageBGMID', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._stageBGMID = reader.readI32As(column.element)
          }
          break
        case 19:
          sheetman.checkColumn(column, 'StageTable.BossStageBGMID', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._bossStageBGMID = reader.readI32As(column.element)
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: StageTableRecord[]): void {
    const recordsById = new Map<number, StageTableRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
