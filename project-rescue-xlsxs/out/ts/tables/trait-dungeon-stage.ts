// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/lite-binary-reader'

// Automatically import to handle external type references.
import { DungeonType } from '../enums/dungeon-type'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  name: string
  stageName: string
  dungeonType: DungeonType
  dungeonFloor: number
  timeLimit: number
  spawnIds: number[]
  bossID: number[]
  spawnPointCount: number
  spawnMaxCount: number
  spawnNextTime: number
  stageClearCount: number
  recommendPower: string
  rewardID: number
  atkUpPercent: number
  defUpPercent: number
  mHPUpPercent: number
  dungeonImagePath: string
  monsterImagePath: string
  assetDataPath: string
}

// Generated from project-rescue-xlsxs/DailyDungeon.xlsx : TraitDungeonStageTable : A1
export class TraitDungeonStageRecord {
  /** Default constructor */
  constructor() {
  }

  /** TraitDungeonStage_Table */
  public get id(): number { return this._id }

  /** 영문 */
  public get name(): string { return this._name }

  /** 한글 */
  public get stageName(): string { return this._stageName }

  /** 던전 아이디 */
  public get dungeonType(): DungeonType { return this._dungeonType }

  /** 던전 단계 */
  public get dungeonFloor(): number { return this._dungeonFloor }

  /** 제한 시간 */
  public get timeLimit(): number { return this._timeLimit }

  /** 생성 몬스터 ID */
  public get spawnIds(): number[] { return this._spawnIds }

  /** 보스 ID */
  public get bossID(): number[] { return this._bossID }

  /** 위치 생성 가능 횟수 */
  public get spawnPointCount(): number { return this._spawnPointCount }

  /** 최대 생성 수 */
  public get spawnMaxCount(): number { return this._spawnMaxCount }

  /** 다음 포인터 생성 시간 */
  public get spawnNextTime(): number { return this._spawnNextTime }

  /** 스테이지 클리어 조건 */
  public get stageClearCount(): number { return this._stageClearCount }

  /** 권장 전투력 */
  public get recommendPower(): string { return this._recommendPower }

  /** 보상 설정 */
  public get rewardID(): number { return this._rewardID }

  /** 던전 공격력 증가 값 */
  public get atkUpPercent(): number { return this._atkUpPercent }

  /** 던전 방어력 증가 값 */
  public get defUpPercent(): number { return this._defUpPercent }

  /** 던전 체력 증가 값 */
  public get mHPUpPercent(): number { return this._mHPUpPercent }

  /** 던전 이미지 */
  public get dungeonImagePath(): string { return this._dungeonImagePath }

  /** 몬스터 이미지 */
  public get monsterImagePath(): string { return this._monsterImagePath }

  /** AssetData 경로(Stage처럼 에셋만 추가하면 바뀌게) */
  public get assetDataPath(): string { return this._assetDataPath }

  public _id: number = 0
  public _name: string = ''
  public _stageName: string = ''
  public _dungeonType: DungeonType = 0 as DungeonType
  public _dungeonFloor: number = 0
  public _timeLimit: number = 0
  public _spawnIds: number[] = []
  public _bossID: number[] = []
  public _spawnPointCount: number = 0
  public _spawnMaxCount: number = 0
  public _spawnNextTime: number = 0
  public _stageClearCount: number = 0
  public _recommendPower: string = ''
  public _rewardID: number = 0
  public _atkUpPercent: number = 0
  public _defUpPercent: number = 0
  public _mHPUpPercent: number = 0
  public _dungeonImagePath: string = ''
  public _monsterImagePath: string = ''
  public _assetDataPath: string = ''

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._name = dataRow.name
    this._stageName = dataRow.stageName
    this._dungeonType = dataRow.dungeonType
    this._dungeonFloor = dataRow.dungeonFloor
    this._timeLimit = dataRow.timeLimit
    this._spawnIds = dataRow.spawnIds
    this._bossID = dataRow.bossID
    this._spawnPointCount = dataRow.spawnPointCount
    this._spawnMaxCount = dataRow.spawnMaxCount
    this._spawnNextTime = Math.fround(dataRow.spawnNextTime)
    this._stageClearCount = dataRow.stageClearCount
    this._recommendPower = dataRow.recommendPower
    this._rewardID = dataRow.rewardID
    this._atkUpPercent = Math.fround(dataRow.atkUpPercent)
    this._defUpPercent = Math.fround(dataRow.defUpPercent)
    this._mHPUpPercent = Math.fround(dataRow.mHPUpPercent)
    this._dungeonImagePath = dataRow.dungeonImagePath
    this._monsterImagePath = dataRow.monsterImagePath
    this._assetDataPath = dataRow.assetDataPath
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._name = dataRow[offset++]
    this._stageName = dataRow[offset++]
    this._dungeonType = dataRow[offset++]
    this._dungeonFloor = dataRow[offset++]
    this._timeLimit = dataRow[offset++]
    this._spawnIds = dataRow[offset++]
    this._bossID = dataRow[offset++]
    this._spawnPointCount = dataRow[offset++]
    this._spawnMaxCount = dataRow[offset++]
    this._spawnNextTime = Math.fround(dataRow[offset++])
    this._stageClearCount = dataRow[offset++]
    this._recommendPower = dataRow[offset++]
    this._rewardID = dataRow[offset++]
    this._atkUpPercent = Math.fround(dataRow[offset++])
    this._defUpPercent = Math.fround(dataRow[offset++])
    this._mHPUpPercent = Math.fround(dataRow[offset++])
    this._dungeonImagePath = dataRow[offset++]
    this._monsterImagePath = dataRow[offset++]
    this._assetDataPath = dataRow[offset++]
  }
}

// Generated from project-rescue-xlsxs/DailyDungeon.xlsx : TraitDungeonStageTable : A1
export class TraitDungeonStageTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): TraitDungeonStageRecord[] { return this._records }
  private _records: TraitDungeonStageRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, TraitDungeonStageRecord> { return this._recordsById }
  private _recordsById: Map<number, TraitDungeonStageRecord> = new Map<number, TraitDungeonStageRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): TraitDungeonStageRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): TraitDungeonStageRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "TraitDungeonStage" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: TraitDungeonStageRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new TraitDungeonStageRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new TraitDungeonStageRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .scb file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.LiteBinaryReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: TraitDungeonStageRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new TraitDungeonStageRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'TraitDungeonStage.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = reader.readI32As(column.element)
          }
          break
        case 2:
          sheetman.checkColumn(column, 'TraitDungeonStage.Name', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._name = reader.readString()
          }
          break
        case 3:
          sheetman.checkColumn(column, 'TraitDungeonStage.StageName', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._stageName = reader.readString()
          }
          break
        case 4:
          sheetman.checkColumn(column, 'TraitDungeonStage.DungeonType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._dungeonType = reader.readEnum() as DungeonType
          }
          break
        case 5:
          sheetman.checkColumn(column, 'TraitDungeonStage.DungeonFloor', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._dungeonFloor = reader.readI32As(column.element)
          }
          break
        case 6:
          sheetman.checkColumn(column, 'TraitDungeonStage.TimeLimit', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._timeLimit = reader.readI32As(column.element)
          }
          break
        case 7:
          sheetman.checkColumn(column, 'TraitDungeonStage.SpawnIds', sheetman.KIND_VAR_ARRAY, 0, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            const elementCount = reader.readCounter32()
            record._spawnIds = []
            for (let j = 0; j < elementCount; ++j)
              record._spawnIds.push(reader.readI32As(column.element))
          }
          break
        case 8:
          sheetman.checkColumn(column, 'TraitDungeonStage.BossID', sheetman.KIND_VAR_ARRAY, 0, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            const elementCount = reader.readCounter32()
            record._bossID = []
            for (let j = 0; j < elementCount; ++j)
              record._bossID.push(reader.readI32As(column.element))
          }
          break
        case 9:
          sheetman.checkColumn(column, 'TraitDungeonStage.SpawnPointCount', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._spawnPointCount = reader.readI32As(column.element)
          }
          break
        case 10:
          sheetman.checkColumn(column, 'TraitDungeonStage.SpawnMaxCount', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._spawnMaxCount = reader.readI32As(column.element)
          }
          break
        case 11:
          sheetman.checkColumn(column, 'TraitDungeonStage.SpawnNextTime', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._spawnNextTime = reader.readFloat()
          }
          break
        case 12:
          sheetman.checkColumn(column, 'TraitDungeonStage.StageClearCount', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._stageClearCount = reader.readI32As(column.element)
          }
          break
        case 13:
          sheetman.checkColumn(column, 'TraitDungeonStage.RecommendPower', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._recommendPower = reader.readString()
          }
          break
        case 14:
          sheetman.checkColumn(column, 'TraitDungeonStage.RewardID', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._rewardID = reader.readI32As(column.element)
          }
          break
        case 15:
          sheetman.checkColumn(column, 'TraitDungeonStage.AtkUpPercent', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._atkUpPercent = reader.readFloat()
          }
          break
        case 16:
          sheetman.checkColumn(column, 'TraitDungeonStage.DefUpPercent', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._defUpPercent = reader.readFloat()
          }
          break
        case 17:
          sheetman.checkColumn(column, 'TraitDungeonStage.MHPUpPercent', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._mHPUpPercent = reader.readFloat()
          }
          break
        case 18:
          sheetman.checkColumn(column, 'TraitDungeonStage.DungeonImagePath', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._dungeonImagePath = reader.readString()
          }
          break
        case 19:
          sheetman.checkColumn(column, 'TraitDungeonStage.MonsterImagePath', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._monsterImagePath = reader.readString()
          }
          break
        case 20:
          sheetman.checkColumn(column, 'TraitDungeonStage.AssetDataPath', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._assetDataPath = reader.readString()
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: TraitDungeonStageRecord[]): void {
    const recordsById = new Map<number, TraitDungeonStageRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
