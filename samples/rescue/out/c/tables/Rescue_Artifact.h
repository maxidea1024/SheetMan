/* ------------------------------------------------------------------------------
 * Generated by SheetMan. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef RESCUE_ARTIFACT_H
#define RESCUE_ARTIFACT_H

#include "sheetman/sheetman_scb_reader.h"

#include "Rescue_Forward.h"
#include "enums/Rescue_EnumArtifactJobType.h"
#include "enums/Rescue_EnumGradeType.h"
#include "enums/Rescue_EnumStatType.h"

#ifdef __cplusplus
extern "C" {
#endif
/* Generated from samples/rescue/MasterData.xlsx : ArtifactTable : A1
 */
struct Rescue_ArtifactRecord_t {
  /* Artifact_Table */
  int32_t id;
  /* 영문 */
  const char* name;
  /* 아티팩트 타입 */
  Rescue_ArtifactJobType_t artifact_type;
  /* 아티팩트 태생 등급 */
  Rescue_GradeType_t grade;
  /* 장착 버프 효과 */
  int32_t* equip_skill_id;
  int32_t equip_skill_id_count;
  /* 장착 스탯 타입 */
  Rescue_StatType_t equip_stat_type;
  /* 장착 스탯 값 */
  float equip_stat_rate;
  /* 보유 스탯 타입 */
  Rescue_StatType_t collection_type;
  /* 보유 스탯 값 */
  float collection_vlaue;
  /* 아이콘 경로 */
  const char* icon_path;
  /* 머티리얼 경로 */
  const char* material_path;
  /* 설명 */
  const char* description;
};

/* Every row of Artifact.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct Rescue_ArtifactTable_t {
  Rescue_ArtifactRecord_t* records;
  int32_t count;

  sm_index_entry* by_id;
  sm_arena arena;
} Rescue_ArtifactTable_t;

/* Loads the table from a .scb file written by SheetMan.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool Rescue_ArtifactLoad(Rescue_ArtifactTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void Rescue_ArtifactFree(Rescue_ArtifactTable_t* table);

/* The row with the given Id, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const Rescue_ArtifactRecord_t* Rescue_ArtifactFindById(
  const Rescue_ArtifactTable_t* table, int32_t key);

/* Whether the table holds a row with this Id. */
bool Rescue_ArtifactContainsId(
  const Rescue_ArtifactTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* RESCUE_ARTIFACT_H */
