/* ------------------------------------------------------------------------------
 * Generated by SheetMan. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef RESCUE_DAILY_DUNGEON_INFO_H
#define RESCUE_DAILY_DUNGEON_INFO_H

#include "sheetman/sheetman_scb_reader.h"

#include "Rescue_Forward.h"
#include "enums/Rescue_EnumDungeonType.h"
#include "enums/Rescue_EnumCycleType.h"
#include "enums/Rescue_EnumCurrencyType.h"

#ifdef __cplusplus
extern "C" {
#endif
/* Generated from samples/rescue/DailyDungeon.xlsx : DailyDungeonInfoTable : A1
 */
struct Rescue_DailyDungeonInfoRecord_t {
  /* DailyDungeonInfo_Table */
  int32_t id;
  /* 영문명 */
  const char* name;
  /* 한글명 */
  const char* dungeon_name;
  /* 던전 타입 */
  Rescue_DungeonType_t dungeon_type;
  /* 출력 우선 순위 */
  int32_t priority;
  /* 던전 입장 타입 */
  Rescue_CycleType_t cycle_type;
  /* 해금 조건 */
  int32_t enter_condition;
  /* 입장 재화 */
  Rescue_CurrencyType_t enter_currency_type;
  /* 던전 버프 ID */
  int32_t dungeon_buff_id;
  /* 버프 수치 증가값 */
  float buff_value_rate;
  /* 입장 값 */
  int32_t enter_currency_value;
  /* 무료입장 재화 타입 */
  Rescue_CurrencyType_t free_enter_currency_type;
  /* 일일 광고 입장 횟수 */
  int32_t ad_enter_count;
  /* 일일 입장권 최대 소모 횟수 */
  int32_t enter_currency_count_max;
  /* 던전 이미지 */
  const char* dungeon_image_path;
  /* 던전 BGM */
  int32_t dungeon_bgmid;
  /* 설명 */
  const char* description;
};

/* Every row of DailyDungeonInfo.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct Rescue_DailyDungeonInfoTable_t {
  Rescue_DailyDungeonInfoRecord_t* records;
  int32_t count;

  sm_index_entry* by_id;
  sm_arena arena;
} Rescue_DailyDungeonInfoTable_t;

/* Loads the table from a .scb file written by SheetMan.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool Rescue_DailyDungeonInfoLoad(Rescue_DailyDungeonInfoTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void Rescue_DailyDungeonInfoFree(Rescue_DailyDungeonInfoTable_t* table);

/* The row with the given Id, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const Rescue_DailyDungeonInfoRecord_t* Rescue_DailyDungeonInfoFindById(
  const Rescue_DailyDungeonInfoTable_t* table, int32_t key);

/* Whether the table holds a row with this Id. */
bool Rescue_DailyDungeonInfoContainsId(
  const Rescue_DailyDungeonInfoTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* RESCUE_DAILY_DUNGEON_INFO_H */
