/* ------------------------------------------------------------------------------
 * Generated by SheetMan. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef RESCUE_PACKAGE_H
#define RESCUE_PACKAGE_H

#include "sheetman/sheetman_scb_reader.h"

#include "Rescue_Forward.h"
#include "enums/Rescue_EnumItemType.h"
#include "enums/Rescue_EnumConsumablesType.h"
#include "enums/Rescue_EnumConditionType.h"

#ifdef __cplusplus
extern "C" {
#endif
/* Generated from samples/rescue/ItemData.xlsx : PackageTable : A1
 */
struct Rescue_PackageRecord_t {
  /* PackcgeItem_Table */
  int32_t id;
  /* 영문 */
  const char* name;
  /* 한글 */
  const char* class_name;
  /* 카테고리 */
  Rescue_ItemType_t item_type;
  /* 패키지 타입 */
  Rescue_ConsumablesType_t package_type;
  /* 중첩 가능 유무 */
  bool stackable;
  /* 최대 소유 값 */
  int64_t max_stack;
  /* 획득조건 타입 */
  Rescue_ConditionType_t package_condition;
  /* 아이템 재사용 대기시간 */
  int32_t cooltime;
  /* 아이템 유지 시간 */
  int32_t duration;
  /* 포함 아이템 종류 */
  int32_t* currency_id;
  int32_t currency_id_count;
  /* 포함 아이템 최소 수량 값 */
  int32_t* currency_item_min;
  int32_t currency_item_min_count;
  /* 포함 아이템 최대 수량 값 */
  int32_t* currency_item_max;
  int32_t currency_item_max_count;
  /* 포함 아이템 확률 */
  int32_t* currency_item_rate;
  int32_t currency_item_rate_count;
  /* 아이콘 경로 */
  const char* icon_path;
  /* 프리펩 위치 */
  const char* drop_prefab_path;
  /* 아이템 설명 */
  const char* item_description;
};

/* Every row of Package.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct Rescue_PackageTable_t {
  Rescue_PackageRecord_t* records;
  int32_t count;

  sm_index_entry* by_id;
  sm_arena arena;
} Rescue_PackageTable_t;

/* Loads the table from a .scb file written by SheetMan.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool Rescue_PackageLoad(Rescue_PackageTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void Rescue_PackageFree(Rescue_PackageTable_t* table);

/* The row with the given Id, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const Rescue_PackageRecord_t* Rescue_PackageFindById(
  const Rescue_PackageTable_t* table, int32_t key);

/* Whether the table holds a row with this Id. */
bool Rescue_PackageContainsId(
  const Rescue_PackageTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* RESCUE_PACKAGE_H */
