/* ------------------------------------------------------------------------------
 * Generated by SheetMan. DO NOT EDIT.
 *
 * Changes to this file may cause incorrect behavior and will be lost if the code is
 * regenerated.
 * ------------------------------------------------------------------------------
 */

#ifndef RESCUE_SD_TRAINING_INFO_H
#define RESCUE_SD_TRAINING_INFO_H

#include "sheetman/sheetman_scb_reader.h"

#include "Rescue_Forward.h"
#include "enums/Rescue_EnumAttributeType.h"
#include "enums/Rescue_EnumStatType.h"
#include "enums/Rescue_EnumCurrencyType.h"

#ifdef __cplusplus
extern "C" {
#endif
/* Generated from samples/rescue/SD_Contens.xlsx : SDTrainingInfoTable : A1
 */
struct Rescue_SDTrainingInfoRecord_t {
  /* SDTrainingInfo_Table */
  int32_t id;
  /* 영문명 */
  const char* name;
  /* 한글명 */
  const char* training_name;
  /* 적용 속성 타입 */
  Rescue_AttributeType_t attribute_type;
  /* 스탯 타입 */
  Rescue_StatType_t stat_type;
  /* 해제 조건 대상 */
  int32_t unlock_condition;
  /* 레벨업 제한 타입 */
  int32_t level_up_condition;
  /* 최대 레벨 */
  int32_t max_level;
  /* 레벨 초기화 여부 */
  bool is_lv_reset;
  /* 레벨 초기화 재화 */
  Rescue_CurrencyType_t lv_reset_currency_type;
  /* 레벨 초기화 재화값 */
  int32_t lv_reset_currency_value;
  /* 설명 */
  const char* description;
};

/* Every row of SDTrainingInfo.
 *
 * The arena owns every string and every array the records point into, so the
 * whole table is released in one call and no record's pointer outlives it. */
typedef struct Rescue_SDTrainingInfoTable_t {
  Rescue_SDTrainingInfoRecord_t* records;
  int32_t count;

  sm_index_entry* by_id;
  sm_arena arena;
} Rescue_SDTrainingInfoTable_t;

/* Loads the table from a .scb file written by SheetMan.
 *
 * Returns false and leaves the table empty when the file is missing, truncated
 * or malformed; the reason goes to `error` when one is passed. */
bool Rescue_SDTrainingInfoLoad(Rescue_SDTrainingInfoTable_t* table, const char* filename,
                   char* error, size_t error_size);

/* Releases everything the table owns. Safe on a zeroed table, and safe twice. */
void Rescue_SDTrainingInfoFree(Rescue_SDTrainingInfoTable_t* table);

/* The row with the given Id, or NULL when the table has none.
 *
 * The lookup to reach for when a missing row is an ordinary answer - an optional
 * reference, a key that came from user input. C has nothing to throw, so a caller
 * that needs the row to be there checks this NULL and says so itself. */
const Rescue_SDTrainingInfoRecord_t* Rescue_SDTrainingInfoFindById(
  const Rescue_SDTrainingInfoTable_t* table, int32_t key);

/* Whether the table holds a row with this Id. */
bool Rescue_SDTrainingInfoContainsId(
  const Rescue_SDTrainingInfoTable_t* table, int32_t key);

#ifdef __cplusplus
}
#endif

#endif /* RESCUE_SD_TRAINING_INFO_H */
