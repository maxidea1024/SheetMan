// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

#ifndef SHEETMAN_GENERATED_TABLES_RELIC_DUNGEON_REWARD_H
#define SHEETMAN_GENERATED_TABLES_RELIC_DUNGEON_REWARD_H

#include <cstddef>
#include <cstdint>
#include <string>
#include <unordered_map>
#include <vector>

#include "sheetman/scb_reader.h"
#include "Tables_forward.h"
#include "enums/Tables_enum_currency_type.h"

namespace rescue {
// Generated from samples/rescue/DailyDungeon.xlsx : RelicDungeonRewardTable : A1
struct RelicDungeonRewardRecord {
  /// RelicDungeonReward_Table
  std::int32_t id = 0;
  /// 영문
  std::string name;
  /// 한글
  std::string reward_name;
  /// 보상 설정1
  CurrencyType reward_type1 = static_cast<CurrencyType>(0);
  /// 보상 값 1
  std::int32_t reward_value1 = 0;
  /// 보상 설정2
  CurrencyType reward_type2 = static_cast<CurrencyType>(0);
  /// 보상 값 2
  std::int32_t reward_value2 = 0;
  /// 최초 클리어
  bool is_first_clear = false;
  /// 최초 보상 설정
  CurrencyType first_clear_reward_type = static_cast<CurrencyType>(0);
  /// 최초 보상 값
  std::int32_t first_clear_reward_value = 0;
};

class RelicDungeonRewardTable {
 public:
  const std::vector<RelicDungeonRewardRecord>& records() const { return records_; }

  /// The row with this Id, or nullptr when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  const RelicDungeonRewardRecord* find_by_id(std::int32_t key) const {
    const auto it = by_id_.find(key);
    return it == by_id_.end() ? nullptr : &records_[it->second];
  }

  /// The row with this Id, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading `get_by_id(key)->name` at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  const RelicDungeonRewardRecord& get_by_id_or_throw(std::int32_t key) const {
    const RelicDungeonRewardRecord* found = find_by_id(key);

    if (found == nullptr) {
      throw sheetman::RecordNotFound(
        std::string("there is no record in table `RelicDungeonReward` that corresponds to "
                    "field `Id` value ") + std::to_string(key));
    }

    return *found;
  }

  /// Whether the table holds a row with this Id.
  bool contains_id(std::int32_t key) const {
    return by_id_.find(key) != by_id_.end();
  }

  /// Loads the table from a .scb file written by SheetMan.
  void read(const std::string& filename) {
    const std::vector<std::uint8_t> buffer = sheetman::read_all_bytes(filename);
    sheetman::ScbReader reader(buffer);

    // Column by column, matched by tag rather than position: a column this build
    // does not know is skipped by its block length, and one whose type changed
    // incompatibly fails naming the field.
    const sheetman::Header header = sheetman::read_table_header(reader);
    const std::size_t row_count = static_cast<std::size_t>(header.row_count);

    // Read into storage of its own and swapped in at the end: reading a table that is
    // already loaded is a refresh, and one that throws part way through - a truncated
    // file, a column this build cannot read - has to leave the rows already there.
    std::vector<RelicDungeonRewardRecord> records;
    records.resize(row_count);

    for (const sheetman::Column& column : header.columns) {
      const std::size_t block_end = reader.position() + static_cast<std::size_t>(column.byte_length);

      switch (column.tag) {
        case 1: {
          sheetman::check_column(column, "RelicDungeonReward.Id", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "RelicDungeonReward.Id");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.id = cursor.next_i32();
          }
          break;
        }
        case 2: {
          sheetman::check_column(column, "RelicDungeonReward.Name", sheetman::kKindScalar, 1, {sheetman::kElementString});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "RelicDungeonReward.Name");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.name = cursor.next_string();
          }
          break;
        }
        case 3: {
          sheetman::check_column(column, "RelicDungeonReward.RewardName", sheetman::kKindScalar, 1, {sheetman::kElementString});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "RelicDungeonReward.RewardName");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.reward_name = cursor.next_string();
          }
          break;
        }
        case 4: {
          sheetman::check_column(column, "RelicDungeonReward.RewardType1", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "RelicDungeonReward.RewardType1");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.reward_type1 = static_cast<CurrencyType>(cursor.next_i32());
          }
          break;
        }
        case 5: {
          sheetman::check_column(column, "RelicDungeonReward.RewardValue1", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "RelicDungeonReward.RewardValue1");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.reward_value1 = cursor.next_i32();
          }
          break;
        }
        case 6: {
          sheetman::check_column(column, "RelicDungeonReward.RewardType2", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "RelicDungeonReward.RewardType2");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.reward_type2 = static_cast<CurrencyType>(cursor.next_i32());
          }
          break;
        }
        case 7: {
          sheetman::check_column(column, "RelicDungeonReward.RewardValue2", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "RelicDungeonReward.RewardValue2");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.reward_value2 = cursor.next_i32();
          }
          break;
        }
        case 8: {
          sheetman::check_column(column, "RelicDungeonReward.IsFirstClear", sheetman::kKindScalar, 1, {sheetman::kElementBool});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "RelicDungeonReward.IsFirstClear");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.is_first_clear = cursor.next_bool();
          }
          break;
        }
        case 9: {
          sheetman::check_column(column, "RelicDungeonReward.FirstClearRewardType", sheetman::kKindScalar, 1, {sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "RelicDungeonReward.FirstClearRewardType");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.first_clear_reward_type = static_cast<CurrencyType>(cursor.next_i32());
          }
          break;
        }
        case 10: {
          sheetman::check_column(column, "RelicDungeonReward.FirstClearRewardValue", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "RelicDungeonReward.FirstClearRewardValue");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.first_clear_reward_value = cursor.next_i32();
          }
          break;
        }
        default:
          // A column added after this code was generated.
          reader.skip(column.byte_length);
          break;
      }

      sheetman::check_block_end(reader, column, block_end);
    }

    publish(std::move(records));
  }

 private:
  friend class Tables;

  /// One whole load, in place of the previous one.
  ///
  /// The indexes are built here rather than by the caller, because they have to arrive
  /// with the rows: a table holding this load's rows and the last one's index would
  /// answer a lookup with a row that is no longer at that position.
  void publish(std::vector<RelicDungeonRewardRecord>&& records) {
    std::unordered_map<std::int32_t, std::size_t> id_map;
    id_map.reserve(records.size());
    for (std::size_t i = 0; i < records.size(); ++i) {
      id_map.emplace(records[i].id, i);
    }

    records_ = std::move(records);
    by_id_ = std::move(id_map);
  }

  std::vector<RelicDungeonRewardRecord> records_;
  std::unordered_map<std::int32_t, std::size_t> by_id_;
};

}  // namespace rescue

#endif  // SHEETMAN_GENERATED_TABLES_RELIC_DUNGEON_REWARD_H
