// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

#ifndef SHEETMAN_GENERATED_TABLES_STAGE_H
#define SHEETMAN_GENERATED_TABLES_STAGE_H

#include <cstddef>
#include <cstdint>
#include <string>
#include <unordered_map>
#include <vector>

#include "sheetman/scb_reader.h"
#include "Tables_forward.h"

namespace rescue {
// Generated from samples/rescue/MasterData.xlsx : StageTable : A1
struct StageRecord {
  /// Stage_Table
  std::int32_t id = 0;
  /// 영문
  std::string name;
  /// 한글
  std::string stage_name;
  /// AssetData 경로
  std::string asset_data_path;
  /// 생성 몬스터 ID
  std::vector<std::int32_t> spawn_ids;
  /// 위치 생성 가능 횟수
  std::int32_t spawn_point_count = 0;
  /// 최대 생성 수
  std::int32_t spawn_max_count = 0;
  /// 다음 포인터 생성 시간
  float spawn_next_time = 0.0f;
  /// 클리어 조건
  std::int32_t stage_clear_count = 0;
  /// 보스 ID
  std::vector<std::int32_t> boss_id;
  /// 일반 ATK증가 값
  float atk_up_percent = 0.0f;
  /// 일반 DEF증가 값
  float def_up_percent = 0.0f;
  /// 일반 HP증가 값
  float mhp_up_percent = 0.0f;
  /// 보스 ATK증가 값
  float boss_atk_up_percent = 0.0f;
  /// 보스 DEF증가 값
  float boss_def_up_percent = 0.0f;
  /// 보스 HP증가 값
  float boss_mhp_up_percent = 0.0f;
  /// 드랍 테이블
  std::int32_t stage_drop_list_id = 0;
  /// 스테이지 BGM
  std::int32_t stage_bgmid = 0;
  /// 보스 스테이지 BGM
  std::int32_t boss_stage_bgmid = 0;
};

class StageTable {
 public:
  const std::vector<StageRecord>& records() const { return records_; }

  /// The row with this Id, or nullptr when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  const StageRecord* find_by_id(std::int32_t key) const {
    const auto it = by_id_.find(key);
    return it == by_id_.end() ? nullptr : &records_[it->second];
  }

  /// The row with this Id, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading `get_by_id(key)->name` at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  const StageRecord& get_by_id_or_throw(std::int32_t key) const {
    const StageRecord* found = find_by_id(key);

    if (found == nullptr) {
      throw sheetman::RecordNotFound(
        std::string("there is no record in table `Stage` that corresponds to "
                    "field `Id` value ") + std::to_string(key));
    }

    return *found;
  }

  /// Whether the table holds a row with this Id.
  bool contains_id(std::int32_t key) const {
    return by_id_.find(key) != by_id_.end();
  }

  /// Loads the table from a .scb file written by SheetMan.
  void read(const std::string& filename) {
    const std::vector<std::uint8_t> buffer = sheetman::read_all_bytes(filename);
    sheetman::ScbReader reader(buffer);

    // Column by column, matched by tag rather than position: a column this build
    // does not know is skipped by its block length, and one whose type changed
    // incompatibly fails naming the field.
    const sheetman::Header header = sheetman::read_table_header(reader);
    const std::size_t row_count = static_cast<std::size_t>(header.row_count);

    // Read into storage of its own and swapped in at the end: reading a table that is
    // already loaded is a refresh, and one that throws part way through - a truncated
    // file, a column this build cannot read - has to leave the rows already there.
    std::vector<StageRecord> records;
    records.resize(row_count);

    for (const sheetman::Column& column : header.columns) {
      const std::size_t block_end = reader.position() + static_cast<std::size_t>(column.byte_length);

      switch (column.tag) {
        case 1: {
          sheetman::check_column(column, "Stage.Id", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Stage.Id");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.id = cursor.next_i32();
          }
          break;
        }
        case 2: {
          sheetman::check_column(column, "Stage.Name", sheetman::kKindScalar, 1, {sheetman::kElementString});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Stage.Name");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.name = cursor.next_string();
          }
          break;
        }
        case 3: {
          sheetman::check_column(column, "Stage.StageName", sheetman::kKindScalar, 1, {sheetman::kElementString});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Stage.StageName");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.stage_name = cursor.next_string();
          }
          break;
        }
        case 4: {
          sheetman::check_column(column, "Stage.AssetDataPath", sheetman::kKindScalar, 1, {sheetman::kElementString});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Stage.AssetDataPath");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.asset_data_path = cursor.next_string();
          }
          break;
        }
        case 5: {
          sheetman::check_column(column, "Stage.SpawnIds", sheetman::kKindVarArray, 0, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            const std::int32_t element_count = reader.read_counter32();
            record.spawn_ids.resize(static_cast<std::size_t>(element_count));
            for (std::int32_t j = 0; j < element_count; ++j) {
              reader.read_i32_as(column.element, record.spawn_ids[static_cast<std::size_t>(j)]);
            }
          }
          break;
        }
        case 6: {
          sheetman::check_column(column, "Stage.SpawnPointCount", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Stage.SpawnPointCount");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.spawn_point_count = cursor.next_i32();
          }
          break;
        }
        case 7: {
          sheetman::check_column(column, "Stage.SpawnMaxCount", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Stage.SpawnMaxCount");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.spawn_max_count = cursor.next_i32();
          }
          break;
        }
        case 8: {
          sheetman::check_column(column, "Stage.SpawnNextTime", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Stage.SpawnNextTime");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.spawn_next_time = cursor.next_f32();
          }
          break;
        }
        case 9: {
          sheetman::check_column(column, "Stage.StageClearCount", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Stage.StageClearCount");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.stage_clear_count = cursor.next_i32();
          }
          break;
        }
        case 10: {
          sheetman::check_column(column, "Stage.BossID", sheetman::kKindVarArray, 0, {sheetman::kElementI32, sheetman::kElementVarint});
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            const std::int32_t element_count = reader.read_counter32();
            record.boss_id.resize(static_cast<std::size_t>(element_count));
            for (std::int32_t j = 0; j < element_count; ++j) {
              reader.read_i32_as(column.element, record.boss_id[static_cast<std::size_t>(j)]);
            }
          }
          break;
        }
        case 11: {
          sheetman::check_column(column, "Stage.AtkUpPercent", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Stage.AtkUpPercent");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.atk_up_percent = cursor.next_f32();
          }
          break;
        }
        case 12: {
          sheetman::check_column(column, "Stage.DefUpPercent", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Stage.DefUpPercent");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.def_up_percent = cursor.next_f32();
          }
          break;
        }
        case 13: {
          sheetman::check_column(column, "Stage.MHPUpPercent", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Stage.MHPUpPercent");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.mhp_up_percent = cursor.next_f32();
          }
          break;
        }
        case 14: {
          sheetman::check_column(column, "Stage.BossAtkUpPercent", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Stage.BossAtkUpPercent");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.boss_atk_up_percent = cursor.next_f32();
          }
          break;
        }
        case 15: {
          sheetman::check_column(column, "Stage.BossDefUpPercent", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Stage.BossDefUpPercent");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.boss_def_up_percent = cursor.next_f32();
          }
          break;
        }
        case 16: {
          sheetman::check_column(column, "Stage.BossMHPUpPercent", sheetman::kKindScalar, 1, {sheetman::kElementF32});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Stage.BossMHPUpPercent");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.boss_mhp_up_percent = cursor.next_f32();
          }
          break;
        }
        case 17: {
          sheetman::check_column(column, "Stage.StageDropListID", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Stage.StageDropListID");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.stage_drop_list_id = cursor.next_i32();
          }
          break;
        }
        case 18: {
          sheetman::check_column(column, "Stage.StageBGMID", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Stage.StageBGMID");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.stage_bgmid = cursor.next_i32();
          }
          break;
        }
        case 19: {
          sheetman::check_column(column, "Stage.BossStageBGMID", sheetman::kKindScalar, 1, {sheetman::kElementI32, sheetman::kElementVarint});
          sheetman::ScbColumnCursor cursor(reader, column, header.row_count, "Stage.BossStageBGMID");
          for (std::size_t i = 0; i < row_count; ++i) {
            auto& record = records[i];
            record.boss_stage_bgmid = cursor.next_i32();
          }
          break;
        }
        default:
          // A column added after this code was generated.
          reader.skip(column.byte_length);
          break;
      }

      sheetman::check_block_end(reader, column, block_end);
    }

    publish(std::move(records));
  }

 private:
  friend class Tables;

  /// One whole load, in place of the previous one.
  ///
  /// The indexes are built here rather than by the caller, because they have to arrive
  /// with the rows: a table holding this load's rows and the last one's index would
  /// answer a lookup with a row that is no longer at that position.
  void publish(std::vector<StageRecord>&& records) {
    std::unordered_map<std::int32_t, std::size_t> id_map;
    id_map.reserve(records.size());
    for (std::size_t i = 0; i < records.size(); ++i) {
      id_map.emplace(records[i].id, i);
    }

    records_ = std::move(records);
    by_id_ = std::move(id_map);
  }

  std::vector<StageRecord> records_;
  std::unordered_map<std::int32_t, std::size_t> by_id_;
};

}  // namespace rescue

#endif  // SHEETMAN_GENERATED_TABLES_STAGE_H
