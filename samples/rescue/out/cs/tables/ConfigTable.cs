// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections;
using System.Collections.Generic;
using System.IO;
using System.Threading.Tasks;

// SheetMan's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using SheetMan.Binary;

namespace Rescue.Tables
{
    [System.Serializable]
    public partial class ConfigTable
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// Row number, added by SheetMan because `Id` cannot be a primary index.
            /// </summary>
            public int Index => _index;

            /// <summary>
            /// ConfigTable
            /// </summary>
            public string Id => _id;

            /// <summary>
            /// 카테고리
            /// </summary>
            public string Category => _category;

            /// <summary>
            /// 테이터타입
            /// </summary>
            public string DataType => _dataType;

            /// <summary>
            /// 기본값
            /// </summary>
            public string DefaultValue => _defaultValue;

            /// <summary>
            /// 설명
            /// </summary>
            public string Description => _description;
            #endregion

            #region Storage
            internal int _index;
            internal string _id = "";
            internal string _category = "";
            internal string _dataType = "";
            internal string _defaultValue = "";
            internal string _description = "";
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"Index\":"); ToStringHelper.ToString(Index, sb);
                sb.Append(",\"Id\":"); ToStringHelper.ToString(Id, sb);
                sb.Append(",\"Category\":"); ToStringHelper.ToString(Category, sb);
                sb.Append(",\"DataType\":"); ToStringHelper.ToString(DataType, sb);
                sb.Append(",\"DefaultValue\":"); ToStringHelper.ToString(DefaultValue, sb);
                sb.Append(",\"Description\":"); ToStringHelper.ToString(Description, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "Index", "Id", "Category", "DataType", "DefaultValue", "Description" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.Index, r.Id, r.Category, r.DataType, r.DefaultValue, r.Description });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        #region Indexing by 'Index'
        public Dictionary<int, Record> RecordsByIndex => _recordsByIndex;
        private Dictionary<int, Record> _recordsByIndex = new Dictionary<int, Record>();

        /// <summary>
        /// The row with this `Index`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language SheetMan generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindByIndex(int key)
            => _recordsByIndex.TryGetValue(key, out Record record) ? record : null;

        /// <summary>
        /// The row with this `Index`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetByIndex(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public Record GetByIndexOrThrow(int key)
        {
            if (!_recordsByIndex.TryGetValue(key, out Record record))
                throw new SheetManException($"There is no record in table `Config` that corresponds to field `Index` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `Index`.</summary>
        public bool ContainsIndex(int key) => _recordsByIndex.ContainsKey(key);
        #endregion // Indexing by `Index`

        #region Indexing by 'Id'
        public Dictionary<string, Record> RecordsById => _recordsById;
        private Dictionary<string, Record> _recordsById = new Dictionary<string, Record>();

        /// <summary>
        /// The row with this `Id`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language SheetMan generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindById(string key)
            => _recordsById.TryGetValue(key, out Record record) ? record : null;

        /// <summary>
        /// The row with this `Id`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetById(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public Record GetByIdOrThrow(string key)
        {
            if (!_recordsById.TryGetValue(key, out Record record))
                throw new SheetManException($"There is no record in table `Config` that corresponds to field `Id` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `Id`.</summary>
        public bool ContainsId(string key) => _recordsById.ContainsKey(key);
        #endregion // Indexing by `Id`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await Tables.ReadAllBytesAsync(filename);
            var reader = new ScbReader(bytes);
            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(ScbReader reader)
        {
            var columns = ScbTable.ReadHeader(reader, out int count);
            ScbColumnCursor cursor;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        ScbTable.CheckColumn(column, "Config.Index", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "Config.Index");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._index = value;
                            } while (--n > 0);
                        }
                        break;

                    case 2:
                        ScbTable.CheckColumn(column, "Config.Id", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        cursor = new ScbColumnCursor(reader, column, count, "Config.Id");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._id = value;
                            } while (--n > 0);
                        }
                        break;

                    case 3:
                        ScbTable.CheckColumn(column, "Config.Category", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        cursor = new ScbColumnCursor(reader, column, count, "Config.Category");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._category = value;
                            } while (--n > 0);
                        }
                        break;

                    case 4:
                        ScbTable.CheckColumn(column, "Config.DataType", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        cursor = new ScbColumnCursor(reader, column, count, "Config.DataType");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._dataType = value;
                            } while (--n > 0);
                        }
                        break;

                    case 5:
                        ScbTable.CheckColumn(column, "Config.DefaultValue", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        cursor = new ScbColumnCursor(reader, column, count, "Config.DefaultValue");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._defaultValue = value;
                            } while (--n > 0);
                        }
                        break;

                    case 6:
                        ScbTable.CheckColumn(column, "Config.Description", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        cursor = new ScbColumnCursor(reader, column, count, "Config.Description");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._description = value;
                            } while (--n > 0);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                ScbTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsByIndex = new Dictionary<int, Record>(count);
            foreach (var record in records)
                recordsByIndex.Add(record.Index, record);
            var recordsById = new Dictionary<string, Record>(count);
            foreach (var record in records)
                recordsById.Add(record.Id, record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsByIndex = recordsByIndex;
            _recordsById = recordsById;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }

} // namespace Rescue.Tables
