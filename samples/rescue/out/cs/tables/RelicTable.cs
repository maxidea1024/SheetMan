// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections;
using System.Collections.Generic;
using System.IO;
using System.Threading.Tasks;

// SheetMan's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using SheetMan.Binary;

namespace Rescue.Tables
{
    [System.Serializable]
    public partial class RelicTable
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// 기본값
            /// </summary>
            public int Id => _id;

            /// <summary>
            /// 영문
            /// </summary>
            public string Name => _name;

            /// <summary>
            /// 한글
            /// </summary>
            public string RelicName => _relicName;

            /// <summary>
            /// 영향력 단계
            /// </summary>
            public int InfuluenceStep => _infuluenceStep;

            /// <summary>
            /// 스탯 타입
            /// </summary>
            public global::Rescue.Tables.StatType RelicType1 => _relicType1;

            /// <summary>
            /// 유물 기본 값 1
            /// </summary>
            public float RelicValue1 => _relicValue1;

            /// <summary>
            /// 유물 레벨 당 스탯 증가 수치 1
            /// </summary>
            public float RelicIncValue1 => _relicIncValue1;

            /// <summary>
            /// 유물 기본 값 2
            /// </summary>
            public global::Rescue.Tables.StatType RelicType2 => _relicType2;

            /// <summary>
            /// 유물 레벨 당 스탯 증가 수치 2
            /// </summary>
            public float RelicValue2 => _relicValue2;

            /// <summary>
            /// 유물 기본 값 3
            /// </summary>
            public float RelicIncValue2 => _relicIncValue2;

            /// <summary>
            /// 유물 레벨 당 스탯 증가 수치 3
            /// </summary>
            public global::Rescue.Tables.StatType RelicType3 => _relicType3;

            /// <summary>
            /// 유물 기본 값 4
            /// </summary>
            public float RelicValue3 => _relicValue3;

            /// <summary>
            /// 유물 레벨 당 스탯 증가 수치 4
            /// </summary>
            public float RelicIncValue3 => _relicIncValue3;

            /// <summary>
            /// 설명
            /// </summary>
            public string Description => _description;

            /// <summary>
            /// 아이콘 경로
            /// </summary>
            public string IconPath => _iconPath;
            #endregion

            #region Storage
            internal int _id;
            internal string _name = "";
            internal string _relicName = "";
            internal int _infuluenceStep;
            internal global::Rescue.Tables.StatType _relicType1;
            internal float _relicValue1;
            internal float _relicIncValue1;
            internal global::Rescue.Tables.StatType _relicType2;
            internal float _relicValue2;
            internal float _relicIncValue2;
            internal global::Rescue.Tables.StatType _relicType3;
            internal float _relicValue3;
            internal float _relicIncValue3;
            internal string _description = "";
            internal string _iconPath = "";
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"Id\":"); ToStringHelper.ToString(Id, sb);
                sb.Append(",\"Name\":"); ToStringHelper.ToString(Name, sb);
                sb.Append(",\"RelicName\":"); ToStringHelper.ToString(RelicName, sb);
                sb.Append(",\"InfuluenceStep\":"); ToStringHelper.ToString(InfuluenceStep, sb);
                sb.Append(",\"RelicType1\":"); ToStringHelper.ToString(RelicType1, sb);
                sb.Append(",\"RelicValue1\":"); ToStringHelper.ToString(RelicValue1, sb);
                sb.Append(",\"RelicIncValue1\":"); ToStringHelper.ToString(RelicIncValue1, sb);
                sb.Append(",\"RelicType2\":"); ToStringHelper.ToString(RelicType2, sb);
                sb.Append(",\"RelicValue2\":"); ToStringHelper.ToString(RelicValue2, sb);
                sb.Append(",\"RelicIncValue2\":"); ToStringHelper.ToString(RelicIncValue2, sb);
                sb.Append(",\"RelicType3\":"); ToStringHelper.ToString(RelicType3, sb);
                sb.Append(",\"RelicValue3\":"); ToStringHelper.ToString(RelicValue3, sb);
                sb.Append(",\"RelicIncValue3\":"); ToStringHelper.ToString(RelicIncValue3, sb);
                sb.Append(",\"Description\":"); ToStringHelper.ToString(Description, sb);
                sb.Append(",\"IconPath\":"); ToStringHelper.ToString(IconPath, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "Id", "Name", "RelicName", "InfuluenceStep", "RelicType1", "RelicValue1", "RelicIncValue1", "RelicType2", "RelicValue2", "RelicIncValue2", "RelicType3", "RelicValue3", "RelicIncValue3", "Description", "IconPath" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.Id, r.Name, r.RelicName, r.InfuluenceStep, r.RelicType1, r.RelicValue1, r.RelicIncValue1, r.RelicType2, r.RelicValue2, r.RelicIncValue2, r.RelicType3, r.RelicValue3, r.RelicIncValue3, r.Description, r.IconPath });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        #region Indexing by 'Id'
        public Dictionary<int, Record> RecordsById => _recordsById;
        private Dictionary<int, Record> _recordsById = new Dictionary<int, Record>();

        /// <summary>
        /// The row with this `Id`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language SheetMan generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindById(int key)
            => _recordsById.TryGetValue(key, out Record record) ? record : null;

        /// <summary>
        /// The row with this `Id`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetById(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public Record GetByIdOrThrow(int key)
        {
            if (!_recordsById.TryGetValue(key, out Record record))
                throw new SheetManException($"There is no record in table `Relic` that corresponds to field `Id` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `Id`.</summary>
        public bool ContainsId(int key) => _recordsById.ContainsKey(key);
        #endregion // Indexing by `Id`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await Tables.ReadAllBytesAsync(filename);
            var reader = new ScbReader(bytes);
            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(ScbReader reader)
        {
            var columns = ScbTable.ReadHeader(reader, out int count);
            ScbColumnCursor cursor;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        ScbTable.CheckColumn(column, "Relic.Id", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "Relic.Id");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._id = value;
                            } while (--n > 0);
                        }
                        break;

                    case 2:
                        ScbTable.CheckColumn(column, "Relic.Name", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        cursor = new ScbColumnCursor(reader, column, count, "Relic.Name");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._name = value;
                            } while (--n > 0);
                        }
                        break;

                    case 3:
                        ScbTable.CheckColumn(column, "Relic.RelicName", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        cursor = new ScbColumnCursor(reader, column, count, "Relic.RelicName");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._relicName = value;
                            } while (--n > 0);
                        }
                        break;

                    case 4:
                        ScbTable.CheckColumn(column, "Relic.InfuluenceStep", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "Relic.InfuluenceStep");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._infuluenceStep = value;
                            } while (--n > 0);
                        }
                        break;

                    case 5:
                        ScbTable.CheckColumn(column, "Relic.RelicType1", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "Relic.RelicType1");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._relicType1 = (global::Rescue.Tables.StatType)value;
                            } while (--n > 0);
                        }
                        break;

                    case 6:
                        ScbTable.CheckColumn(column, "Relic.RelicValue1", ScbTable.KindScalar, 1, ScbTable.ElementF32);
                        cursor = new ScbColumnCursor(reader, column, count, "Relic.RelicValue1");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._relicValue1 = cursor.NextF32();
                        }
                        break;

                    case 7:
                        ScbTable.CheckColumn(column, "Relic.RelicIncValue1", ScbTable.KindScalar, 1, ScbTable.ElementF32);
                        cursor = new ScbColumnCursor(reader, column, count, "Relic.RelicIncValue1");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._relicIncValue1 = cursor.NextF32();
                        }
                        break;

                    case 8:
                        ScbTable.CheckColumn(column, "Relic.RelicType2", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "Relic.RelicType2");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._relicType2 = (global::Rescue.Tables.StatType)value;
                            } while (--n > 0);
                        }
                        break;

                    case 9:
                        ScbTable.CheckColumn(column, "Relic.RelicValue2", ScbTable.KindScalar, 1, ScbTable.ElementF32);
                        cursor = new ScbColumnCursor(reader, column, count, "Relic.RelicValue2");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._relicValue2 = cursor.NextF32();
                        }
                        break;

                    case 10:
                        ScbTable.CheckColumn(column, "Relic.RelicIncValue2", ScbTable.KindScalar, 1, ScbTable.ElementF32);
                        cursor = new ScbColumnCursor(reader, column, count, "Relic.RelicIncValue2");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._relicIncValue2 = cursor.NextF32();
                        }
                        break;

                    case 11:
                        ScbTable.CheckColumn(column, "Relic.RelicType3", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "Relic.RelicType3");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._relicType3 = (global::Rescue.Tables.StatType)value;
                            } while (--n > 0);
                        }
                        break;

                    case 12:
                        ScbTable.CheckColumn(column, "Relic.RelicValue3", ScbTable.KindScalar, 1, ScbTable.ElementF32);
                        cursor = new ScbColumnCursor(reader, column, count, "Relic.RelicValue3");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._relicValue3 = cursor.NextF32();
                        }
                        break;

                    case 13:
                        ScbTable.CheckColumn(column, "Relic.RelicIncValue3", ScbTable.KindScalar, 1, ScbTable.ElementF32);
                        cursor = new ScbColumnCursor(reader, column, count, "Relic.RelicIncValue3");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._relicIncValue3 = cursor.NextF32();
                        }
                        break;

                    case 14:
                        ScbTable.CheckColumn(column, "Relic.Description", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        cursor = new ScbColumnCursor(reader, column, count, "Relic.Description");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._description = value;
                            } while (--n > 0);
                        }
                        break;

                    case 15:
                        ScbTable.CheckColumn(column, "Relic.IconPath", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        cursor = new ScbColumnCursor(reader, column, count, "Relic.IconPath");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._iconPath = value;
                            } while (--n > 0);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                ScbTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsById = new Dictionary<int, Record>(count);
            foreach (var record in records)
                recordsById.Add(record.Id, record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsById = recordsById;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }

} // namespace Rescue.Tables
