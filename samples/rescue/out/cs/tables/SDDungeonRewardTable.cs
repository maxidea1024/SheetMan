// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

using System;
using System.Text;
using System.Collections;
using System.Collections.Generic;
using System.IO;
using System.Threading.Tasks;

// SheetMan's binary reader, written into this directory beside the accessor.
// Nothing has to be installed for the generated code to compile.
using SheetMan.Binary;

namespace Rescue.Tables
{
    [System.Serializable]
    public partial class SDDungeonRewardTable
    {
        #region Record
        [System.Serializable]
        public partial class Record
        {
            #region Values
            /// <summary>
            /// SDDungeonReward_Table
            /// </summary>
            public int Id => _id;

            /// <summary>
            /// 영문명
            /// </summary>
            public string Name => _name;

            /// <summary>
            /// 한글명
            /// </summary>
            public string RewardName => _rewardName;

            /// <summary>
            /// 해제 조건
            /// </summary>
            public int ConditionId => _conditionId;

            /// <summary>
            /// 공용 재료
            /// </summary>
            public global::Rescue.Tables.CurrencyType RewardCommon => _rewardCommon;

            /// <summary>
            /// 공용 재료 값
            /// </summary>
            public int CommonValue => _commonValue;

            /// <summary>
            /// 크리티컬 데미지 재료
            /// </summary>
            public global::Rescue.Tables.CurrencyType Reward1Type => _reward1Type;

            /// <summary>
            /// 크리티컬 데미지 재료값
            /// </summary>
            public int Reward1Value => _reward1Value;

            /// <summary>
            /// 크리티컬 확률 재료
            /// </summary>
            public global::Rescue.Tables.CurrencyType Reward2Type => _reward2Type;

            /// <summary>
            /// 크리티컬 확률 재료 값
            /// </summary>
            public int Reward2Value => _reward2Value;

            /// <summary>
            /// 블록 확률 재료
            /// </summary>
            public global::Rescue.Tables.CurrencyType Reward3Type => _reward3Type;

            /// <summary>
            /// 블록 확률 재료 값
            /// </summary>
            public int Reward3Value => _reward3Value;

            /// <summary>
            /// 명중률 재료
            /// </summary>
            public global::Rescue.Tables.CurrencyType Reward4Type => _reward4Type;

            /// <summary>
            /// 명중률 재료 값
            /// </summary>
            public int Reward4Value => _reward4Value;

            /// <summary>
            /// 퍼펙트 조건
            /// </summary>
            public bool IsPerfect => _isPerfect;

            /// <summary>
            /// 퍼펙트 보상
            /// </summary>
            public global::Rescue.Tables.CurrencyType RewardPerfect => _rewardPerfect;

            /// <summary>
            /// 퍼펙트 보상값
            /// </summary>
            public int PerfectValue => _perfectValue;

            /// <summary>
            /// 최소 보상 조건
            /// </summary>
            public bool IsMinReward => _isMinReward;

            /// <summary>
            /// 최소 보상
            /// </summary>
            public global::Rescue.Tables.CurrencyType RewardMin => _rewardMin;

            /// <summary>
            /// 최소 보상값
            /// </summary>
            public int MinRewardValue => _minRewardValue;
            #endregion

            #region Storage
            internal int _id;
            internal string _name = "";
            internal string _rewardName = "";
            internal int _conditionId;
            internal global::Rescue.Tables.CurrencyType _rewardCommon;
            internal int _commonValue;
            internal global::Rescue.Tables.CurrencyType _reward1Type;
            internal int _reward1Value;
            internal global::Rescue.Tables.CurrencyType _reward2Type;
            internal int _reward2Value;
            internal global::Rescue.Tables.CurrencyType _reward3Type;
            internal int _reward3Value;
            internal global::Rescue.Tables.CurrencyType _reward4Type;
            internal int _reward4Value;
            internal bool _isPerfect;
            internal global::Rescue.Tables.CurrencyType _rewardPerfect;
            internal int _perfectValue;
            internal bool _isMinReward;
            internal global::Rescue.Tables.CurrencyType _rewardMin;
            internal int _minRewardValue;
            #endregion

            #region ToString
            public override string ToString()
            {
                var sb = new StringBuilder("{");
                sb.Append("\"Id\":"); ToStringHelper.ToString(Id, sb);
                sb.Append(",\"Name\":"); ToStringHelper.ToString(Name, sb);
                sb.Append(",\"RewardName\":"); ToStringHelper.ToString(RewardName, sb);
                sb.Append(",\"ConditionId\":"); ToStringHelper.ToString(ConditionId, sb);
                sb.Append(",\"RewardCommon\":"); ToStringHelper.ToString(RewardCommon, sb);
                sb.Append(",\"CommonValue\":"); ToStringHelper.ToString(CommonValue, sb);
                sb.Append(",\"Reward1Type\":"); ToStringHelper.ToString(Reward1Type, sb);
                sb.Append(",\"Reward1Value\":"); ToStringHelper.ToString(Reward1Value, sb);
                sb.Append(",\"Reward2Type\":"); ToStringHelper.ToString(Reward2Type, sb);
                sb.Append(",\"Reward2Value\":"); ToStringHelper.ToString(Reward2Value, sb);
                sb.Append(",\"Reward3Type\":"); ToStringHelper.ToString(Reward3Type, sb);
                sb.Append(",\"Reward3Value\":"); ToStringHelper.ToString(Reward3Value, sb);
                sb.Append(",\"Reward4Type\":"); ToStringHelper.ToString(Reward4Type, sb);
                sb.Append(",\"Reward4Value\":"); ToStringHelper.ToString(Reward4Value, sb);
                sb.Append(",\"IsPerfect\":"); ToStringHelper.ToString(IsPerfect, sb);
                sb.Append(",\"RewardPerfect\":"); ToStringHelper.ToString(RewardPerfect, sb);
                sb.Append(",\"PerfectValue\":"); ToStringHelper.ToString(PerfectValue, sb);
                sb.Append(",\"IsMinReward\":"); ToStringHelper.ToString(IsMinReward, sb);
                sb.Append(",\"RewardMin\":"); ToStringHelper.ToString(RewardMin, sb);
                sb.Append(",\"MinRewardValue\":"); ToStringHelper.ToString(MinRewardValue, sb);
                sb.Append("}");
                return sb.ToString();
            }
            #endregion
        }
        #endregion

        /// <summary>
        /// Field names.
        /// </summary>
        public static readonly string[] FieldNames = { "Id", "Name", "RewardName", "ConditionId", "RewardCommon", "CommonValue", "Reward1Type", "Reward1Value", "Reward2Type", "Reward2Value", "Reward3Type", "Reward3Value", "Reward4Type", "Reward4Value", "IsPerfect", "RewardPerfect", "PerfectValue", "IsMinReward", "RewardMin", "MinRewardValue" };

        /// <summary>
        /// Build object value map.
        /// </summary>
        public List<object[]> BuildObjectValueMap()
        {
            var result = new List<object[]>();
            foreach (var r in _records)
                result.Add(new object[] { r.Id, r.Name, r.RewardName, r.ConditionId, r.RewardCommon, r.CommonValue, r.Reward1Type, r.Reward1Value, r.Reward2Type, r.Reward2Value, r.Reward3Type, r.Reward3Value, r.Reward4Type, r.Reward4Value, r.IsPerfect, r.RewardPerfect, r.PerfectValue, r.IsMinReward, r.RewardMin, r.MinRewardValue });

            return result;
        }

        /// <summary>
        /// All records.
        /// </summary>
        /// <remarks>
        /// The list a read publishes, not one a read fills. Whoever holds this reference holds
        /// the rows as they were when they took it, and a refresh underneath them replaces the
        /// reference rather than the contents - so an iteration in progress neither tears nor
        /// throws, and a read that fails leaves the previous rows exactly where they were.
        /// </remarks>
        public List<Record> Records => _records;
        private List<Record> _records = new List<Record>();

        #region Indexing by 'Id'
        public Dictionary<int, Record> RecordsById => _recordsById;
        private Dictionary<int, Record> _recordsById = new Dictionary<int, Record>();

        /// <summary>
        /// The row with this `Id`, or null when the table has none.
        /// </summary>
        /// <remarks>
        /// The lookup to reach for when a missing row is an ordinary answer - an optional
        /// reference, a key that came from user input. Every language SheetMan generates has
        /// this one under the same name.
        /// </remarks>
        public Record FindById(int key)
            => _recordsById.TryGetValue(key, out Record record) ? record : null;

        /// <summary>
        /// The row with this `Id`, or a thrown exception naming what was
        /// missing.
        /// </summary>
        /// <remarks>
        /// For a key that has to be there - one from another table, or a constant. The name
        /// says it throws, because a caller reading `GetById(id).Name` at
        /// a glance cannot otherwise tell whether the next line is a null check or a catch.
        /// </remarks>
        public Record GetByIdOrThrow(int key)
        {
            if (!_recordsById.TryGetValue(key, out Record record))
                throw new SheetManException($"There is no record in table `SDDungeonReward` that corresponds to field `Id` value {key}");

            return record;
        }

        /// <summary>Whether the table holds a row with this `Id`.</summary>
        public bool ContainsId(int key) => _recordsById.ContainsKey(key);
        #endregion // Indexing by `Id`

        /// <summary>
        /// Read a table from specified file.
        /// </summary>
        public async Task ReadAsync(string filename)
        {
            var bytes = await Tables.ReadAllBytesAsync(filename);
            var reader = new ScbReader(bytes);
            await ReadAsync(reader);
        }

        /// <summary>
        /// Read a table from specified reader.
        /// </summary>
        /// <remarks>
        /// Column by column, matched by tag rather than position. A column this build does
        /// not know is skipped by its block length; one it knows but cannot read - the type
        /// changed incompatibly - fails naming the field. Order, names and columns added or
        /// removed on either side are therefore all survivable.
        /// </remarks>
        public Task ReadAsync(ScbReader reader)
        {
            var columns = ScbTable.ReadHeader(reader, out int count);
            ScbColumnCursor cursor;

            // Read into storage of its own and published at the end, which is what makes a
            // refresh atomic: nothing here touches what the table is currently holding, so a
            // file that turns out to be truncated - or a column this build cannot read -
            // leaves the previous rows in place and reports why.
            //
            // Sized once. The row count was checked against what the columns declare before
            // this point, so it is a number the file could actually hold rows for - and a
            // list that grows into twenty thousand rows reallocates fifteen times to get
            // there, copying everything each time.
            var records = new List<Record>(count);

            for (int i = 0; i < count; i++)
                records.Add(new Record());

            foreach (var column in columns)
            {
                int blockEnd = reader.Position + column.ByteLength;

                switch (column.Tag)
                {
                    case 1:
                        ScbTable.CheckColumn(column, "SDDungeonReward.Id", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDDungeonReward.Id");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._id = value;
                            } while (--n > 0);
                        }
                        break;

                    case 2:
                        ScbTable.CheckColumn(column, "SDDungeonReward.Name", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        cursor = new ScbColumnCursor(reader, column, count, "SDDungeonReward.Name");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._name = value;
                            } while (--n > 0);
                        }
                        break;

                    case 3:
                        ScbTable.CheckColumn(column, "SDDungeonReward.RewardName", ScbTable.KindScalar, 1, ScbTable.ElementString);
                        cursor = new ScbColumnCursor(reader, column, count, "SDDungeonReward.RewardName");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameString(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._rewardName = value;
                            } while (--n > 0);
                        }
                        break;

                    case 4:
                        ScbTable.CheckColumn(column, "SDDungeonReward.ConditionId", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDDungeonReward.ConditionId");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._conditionId = value;
                            } while (--n > 0);
                        }
                        break;

                    case 5:
                        ScbTable.CheckColumn(column, "SDDungeonReward.RewardCommon", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDDungeonReward.RewardCommon");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._rewardCommon = (global::Rescue.Tables.CurrencyType)value;
                            } while (--n > 0);
                        }
                        break;

                    case 6:
                        ScbTable.CheckColumn(column, "SDDungeonReward.CommonValue", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDDungeonReward.CommonValue");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._commonValue = value;
                            } while (--n > 0);
                        }
                        break;

                    case 7:
                        ScbTable.CheckColumn(column, "SDDungeonReward.Reward1Type", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDDungeonReward.Reward1Type");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._reward1Type = (global::Rescue.Tables.CurrencyType)value;
                            } while (--n > 0);
                        }
                        break;

                    case 8:
                        ScbTable.CheckColumn(column, "SDDungeonReward.Reward1Value", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDDungeonReward.Reward1Value");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._reward1Value = value;
                            } while (--n > 0);
                        }
                        break;

                    case 9:
                        ScbTable.CheckColumn(column, "SDDungeonReward.Reward2Type", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDDungeonReward.Reward2Type");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._reward2Type = (global::Rescue.Tables.CurrencyType)value;
                            } while (--n > 0);
                        }
                        break;

                    case 10:
                        ScbTable.CheckColumn(column, "SDDungeonReward.Reward2Value", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDDungeonReward.Reward2Value");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._reward2Value = value;
                            } while (--n > 0);
                        }
                        break;

                    case 11:
                        ScbTable.CheckColumn(column, "SDDungeonReward.Reward3Type", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDDungeonReward.Reward3Type");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._reward3Type = (global::Rescue.Tables.CurrencyType)value;
                            } while (--n > 0);
                        }
                        break;

                    case 12:
                        ScbTable.CheckColumn(column, "SDDungeonReward.Reward3Value", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDDungeonReward.Reward3Value");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._reward3Value = value;
                            } while (--n > 0);
                        }
                        break;

                    case 13:
                        ScbTable.CheckColumn(column, "SDDungeonReward.Reward4Type", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDDungeonReward.Reward4Type");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._reward4Type = (global::Rescue.Tables.CurrencyType)value;
                            } while (--n > 0);
                        }
                        break;

                    case 14:
                        ScbTable.CheckColumn(column, "SDDungeonReward.Reward4Value", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDDungeonReward.Reward4Value");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._reward4Value = value;
                            } while (--n > 0);
                        }
                        break;

                    case 15:
                        ScbTable.CheckColumn(column, "SDDungeonReward.IsPerfect", ScbTable.KindScalar, 1, ScbTable.ElementBool);
                        cursor = new ScbColumnCursor(reader, column, count, "SDDungeonReward.IsPerfect");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._isPerfect = cursor.NextBool();
                        }
                        break;

                    case 16:
                        ScbTable.CheckColumn(column, "SDDungeonReward.RewardPerfect", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDDungeonReward.RewardPerfect");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._rewardPerfect = (global::Rescue.Tables.CurrencyType)value;
                            } while (--n > 0);
                        }
                        break;

                    case 17:
                        ScbTable.CheckColumn(column, "SDDungeonReward.PerfectValue", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDDungeonReward.PerfectValue");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._perfectValue = value;
                            } while (--n > 0);
                        }
                        break;

                    case 18:
                        ScbTable.CheckColumn(column, "SDDungeonReward.IsMinReward", ScbTable.KindScalar, 1, ScbTable.ElementBool);
                        cursor = new ScbColumnCursor(reader, column, count, "SDDungeonReward.IsMinReward");
                        for (int i = 0; i < count; i++)
                        {
                            var record = records[i];
                            record._isMinReward = cursor.NextBool();
                        }
                        break;

                    case 19:
                        ScbTable.CheckColumn(column, "SDDungeonReward.RewardMin", ScbTable.KindScalar, 1, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDDungeonReward.RewardMin");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._rewardMin = (global::Rescue.Tables.CurrencyType)value;
                            } while (--n > 0);
                        }
                        break;

                    case 20:
                        ScbTable.CheckColumn(column, "SDDungeonReward.MinRewardValue", ScbTable.KindScalar, 1, ScbTable.ElementI32, ScbTable.ElementVarint);
                        cursor = new ScbColumnCursor(reader, column, count, "SDDungeonReward.MinRewardValue");
                        for (int i = 0; i < count; )
                        {
                            // One call per run of equal values, not one per row - over a
                            // run-length encoded column this is most of the decode.
                            int n = cursor.NextSameI32(count - i, out var value);
                            do
                            {
                                var record = records[i++];
                                record._minRewardValue = value;
                            } while (--n > 0);
                        }
                        break;

                    default:
                        // A column added after this code was generated. Its block length
                        // says how far to move on.
                        reader.Skip(column.ByteLength);
                        break;
                }

                ScbTable.CheckBlockEnd(reader, column, blockEnd);
            }

            // Index mapping. Sized to the rows, so nothing rehashes on the way in, and a
            // duplicate key throws here - before any of this is visible.
            var recordsById = new Dictionary<int, Record>(count);
            foreach (var record in records)
                recordsById.Add(record.Id, record);

            // Published. Everything above succeeded, and the barrier is what keeps the writes
            // that built these from being seen after the references to them - a reader on
            // another thread gets the whole table or the previous one, never half of either.
            System.Threading.Thread.MemoryBarrier();

            _records = records;
            _recordsById = recordsById;

            return Task.CompletedTask;
        }

        public override string ToString()
        {
            var sb = new StringBuilder();
            ToStringHelper.ToString(_records, sb);
            return sb.ToString();
        }
    }

} // namespace Rescue.Tables
