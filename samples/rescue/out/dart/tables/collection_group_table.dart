// ------------------------------------------------------------------------------
// Generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

part of '../tables.dart';
// Generated from samples/rescue/CollectionData.xlsx : CollectionGroupTable : A1
class CollectionGroupRecord {
  /// ID
  int id = 0;
  /// 영문
  String name = '';
  /// 출력 순서
  int index = 0;
  /// 캐릭터 Index(프리펩에 있는 캐릭터와 같아야 함)
  List<int> characterList = [];
  /// 프리펩 위치
  String prefabPath = '';

}

/// Every row of CollectionGroup.
class CollectionGroupTable {
  /// Every row, in the order the sheet declared them.
  ///
  /// The list a read published, not one a read fills. A refresh replaces the reference
  /// rather than the contents, so whoever holds this holds one whole load.
  List<CollectionGroupRecord> get records => _records;
  List<CollectionGroupRecord> _records = [];

  Map<int, CollectionGroupRecord> _byId = {};

  /// The row with this Id, or null when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  CollectionGroupRecord? findById(int key) => _byId[key];

  /// The row with this Id, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading getById(key).name at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  CollectionGroupRecord getByIdOrThrow(int key) {
    final record = _byId[key];

    if (record == null) {
      throw RecordNotFoundException(
          'there is no record in table `CollectionGroup` that corresponds to '
          'field `Id` value $key');
    }

    return record;
  }

  /// Whether the table holds a row with this Id.
  bool containsId(int key) => _byId.containsKey(key);

  /// Loads the table from a .scb file written by SheetMan.
  /// Column by column, matched by tag rather than position: a column this build does
  /// not know is skipped by its block length, and one whose type changed incompatibly
  /// fails naming the field.
  void read(String filename) {
    final reader = ScbReader(readAllBytes(filename));
    final header = readTableHeader(reader);
    final count = header.rowCount;
    late ScbColumnCursor cursor;

    // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
    final loaded = <CollectionGroupRecord>[];
    final loadedById = <int, CollectionGroupRecord>{};

    for (var i = 0; i < count; i++) {
      loaded.add(CollectionGroupRecord());
    }

    for (final column in header.columns) {
      final blockEnd = reader.position + column.byteLength;

      switch (column.tag) {
        case 1:
          checkColumn(column, 'CollectionGroup.Id', kindScalar, 1, [elementI32, elementVarint]);
          cursor = ScbColumnCursor(reader, column, count, 'CollectionGroup.Id');
          for (final record in loaded) {
            record.id = cursor.nextI32();
          }
          break;
        case 2:
          checkColumn(column, 'CollectionGroup.Name', kindScalar, 1, [elementString]);
          cursor = ScbColumnCursor(reader, column, count, 'CollectionGroup.Name');
          for (final record in loaded) {
            record.name = cursor.nextString();
          }
          break;
        case 3:
          checkColumn(column, 'CollectionGroup.Index', kindScalar, 1, [elementI32, elementVarint]);
          cursor = ScbColumnCursor(reader, column, count, 'CollectionGroup.Index');
          for (final record in loaded) {
            record.index = cursor.nextI32();
          }
          break;
        case 4:
          checkColumn(column, 'CollectionGroup.CharacterList', kindVarArray, 0, [elementI32, elementVarint]);
          for (final record in loaded) {
            final elementCount = reader.readCounter32();
            record.characterList = List.generate(elementCount, (_) => reader.readI32As(column.element));
          }
          break;
        case 5:
          checkColumn(column, 'CollectionGroup.PrefabPath', kindScalar, 1, [elementString]);
          cursor = ScbColumnCursor(reader, column, count, 'CollectionGroup.PrefabPath');
          for (final record in loaded) {
            record.prefabPath = cursor.nextString();
          }
          break;
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength);
          break;
      }

      checkBlockEnd(reader, column, blockEnd);
    }

    for (final record in loaded) {
      loadedById[record.id] = record;
    }

    // Published, now that every column read.
    _records = loaded;
    _byId = loadedById;
  }
}
