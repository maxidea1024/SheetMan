// ------------------------------------------------------------------------------
// Generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

part of '../tables.dart';
// Generated from samples/rescue/MasterData.xlsx : ConfigTable : A1
class ConfigRecord {
  /// Row number, added by SheetMan because `Id` cannot be a primary index.
  int index = 0;
  /// ConfigTable
  String id = '';
  /// 카테고리
  String category = '';
  /// 테이터타입
  String dataType = '';
  /// 기본값
  String defaultValue = '';
  /// 설명
  String description = '';

}

/// Every row of Config.
class ConfigTable {
  /// Every row, in the order the sheet declared them.
  ///
  /// The list a read published, not one a read fills. A refresh replaces the reference
  /// rather than the contents, so whoever holds this holds one whole load.
  List<ConfigRecord> get records => _records;
  List<ConfigRecord> _records = [];

  Map<int, ConfigRecord> _byIndex = {};
  Map<String, ConfigRecord> _byId = {};

  /// The row with this Index, or null when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  ConfigRecord? findByIndex(int key) => _byIndex[key];

  /// The row with this Index, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading getByIndex(key).name at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  ConfigRecord getByIndexOrThrow(int key) {
    final record = _byIndex[key];

    if (record == null) {
      throw RecordNotFoundException(
          'there is no record in table `Config` that corresponds to '
          'field `Index` value $key');
    }

    return record;
  }

  /// Whether the table holds a row with this Index.
  bool containsIndex(int key) => _byIndex.containsKey(key);

  /// The row with this Id, or null when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  ConfigRecord? findById(String key) => _byId[key];

  /// The row with this Id, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading getById(key).name at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  ConfigRecord getByIdOrThrow(String key) {
    final record = _byId[key];

    if (record == null) {
      throw RecordNotFoundException(
          'there is no record in table `Config` that corresponds to '
          'field `Id` value $key');
    }

    return record;
  }

  /// Whether the table holds a row with this Id.
  bool containsId(String key) => _byId.containsKey(key);

  /// Loads the table from a .scb file written by SheetMan.
  /// Column by column, matched by tag rather than position: a column this build does
  /// not know is skipped by its block length, and one whose type changed incompatibly
  /// fails naming the field.
  void read(String filename) {
    final reader = ScbReader(readAllBytes(filename));
    final header = readTableHeader(reader);
    final count = header.rowCount;
    late ScbColumnCursor cursor;

    // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
    final loaded = <ConfigRecord>[];
    final loadedByIndex = <int, ConfigRecord>{};
    final loadedById = <String, ConfigRecord>{};

    for (var i = 0; i < count; i++) {
      loaded.add(ConfigRecord());
    }

    for (final column in header.columns) {
      final blockEnd = reader.position + column.byteLength;

      switch (column.tag) {
        case 1:
          checkColumn(column, 'Config.Index', kindScalar, 1, [elementI32, elementVarint]);
          cursor = ScbColumnCursor(reader, column, count, 'Config.Index');
          for (final record in loaded) {
            record.index = cursor.nextI32();
          }
          break;
        case 2:
          checkColumn(column, 'Config.Id', kindScalar, 1, [elementString]);
          cursor = ScbColumnCursor(reader, column, count, 'Config.Id');
          for (final record in loaded) {
            record.id = cursor.nextString();
          }
          break;
        case 3:
          checkColumn(column, 'Config.Category', kindScalar, 1, [elementString]);
          cursor = ScbColumnCursor(reader, column, count, 'Config.Category');
          for (final record in loaded) {
            record.category = cursor.nextString();
          }
          break;
        case 4:
          checkColumn(column, 'Config.DataType', kindScalar, 1, [elementString]);
          cursor = ScbColumnCursor(reader, column, count, 'Config.DataType');
          for (final record in loaded) {
            record.dataType = cursor.nextString();
          }
          break;
        case 5:
          checkColumn(column, 'Config.DefaultValue', kindScalar, 1, [elementString]);
          cursor = ScbColumnCursor(reader, column, count, 'Config.DefaultValue');
          for (final record in loaded) {
            record.defaultValue = cursor.nextString();
          }
          break;
        case 6:
          checkColumn(column, 'Config.Description', kindScalar, 1, [elementString]);
          cursor = ScbColumnCursor(reader, column, count, 'Config.Description');
          for (final record in loaded) {
            record.description = cursor.nextString();
          }
          break;
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength);
          break;
      }

      checkBlockEnd(reader, column, blockEnd);
    }

    for (final record in loaded) {
      loadedByIndex[record.index] = record;
      loadedById[record.id] = record;
    }

    // Published, now that every column read.
    _records = loaded;
    _byIndex = loadedByIndex;
    _byId = loadedById;
  }
}
