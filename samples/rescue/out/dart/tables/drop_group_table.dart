// ------------------------------------------------------------------------------
// Generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

part of '../tables.dart';
// Generated from samples/rescue/MasterData.xlsx : DropGroupTable : A1
class DropGroupRecord {
  /// 기본값
  int id = 0;
  /// 영문
  String name = '';
  /// 한글
  String dropName = '';
  /// 드랍 구역
  String dropArea = '';
  /// 아이템 종류1
  List<int> dropItemIds = [];
  /// 아이템 최소 수량 값
  List<int> dropItemMin = [];
  /// 아이템 최대 수량 값
  List<int> dropItemMax = [];
  /// 확률
  List<double> itemDropRate = [];

}

/// Every row of DropGroup.
class DropGroupTable {
  /// Every row, in the order the sheet declared them.
  ///
  /// The list a read published, not one a read fills. A refresh replaces the reference
  /// rather than the contents, so whoever holds this holds one whole load.
  List<DropGroupRecord> get records => _records;
  List<DropGroupRecord> _records = [];

  Map<int, DropGroupRecord> _byId = {};

  /// The row with this Id, or null when the table has none.
  ///
  /// The lookup to reach for when a missing row is an ordinary answer - an optional
  /// reference, a key that came from user input.
  DropGroupRecord? findById(int key) => _byId[key];

  /// The row with this Id, or a thrown exception naming what was missing.
  ///
  /// For a key that has to be there - one from another table, or a constant. The name says
  /// it throws, because a caller reading getById(key).name at a glance
  /// cannot otherwise tell whether the next line is a null check or a catch.
  DropGroupRecord getByIdOrThrow(int key) {
    final record = _byId[key];

    if (record == null) {
      throw RecordNotFoundException(
          'there is no record in table `DropGroup` that corresponds to '
          'field `Id` value $key');
    }

    return record;
  }

  /// Whether the table holds a row with this Id.
  bool containsId(int key) => _byId.containsKey(key);

  /// Loads the table from a .scb file written by SheetMan.
  /// Column by column, matched by tag rather than position: a column this build does
  /// not know is skipped by its block length, and one whose type changed incompatibly
  /// fails naming the field.
  void read(String filename) {
    final reader = ScbReader(readAllBytes(filename));
    final header = readTableHeader(reader);
    final count = header.rowCount;
    late ScbColumnCursor cursor;

    // Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
    final loaded = <DropGroupRecord>[];
    final loadedById = <int, DropGroupRecord>{};

    for (var i = 0; i < count; i++) {
      loaded.add(DropGroupRecord());
    }

    for (final column in header.columns) {
      final blockEnd = reader.position + column.byteLength;

      switch (column.tag) {
        case 1:
          checkColumn(column, 'DropGroup.Id', kindScalar, 1, [elementI32, elementVarint]);
          cursor = ScbColumnCursor(reader, column, count, 'DropGroup.Id');
          for (final record in loaded) {
            record.id = cursor.nextI32();
          }
          break;
        case 2:
          checkColumn(column, 'DropGroup.Name', kindScalar, 1, [elementString]);
          cursor = ScbColumnCursor(reader, column, count, 'DropGroup.Name');
          for (final record in loaded) {
            record.name = cursor.nextString();
          }
          break;
        case 3:
          checkColumn(column, 'DropGroup.DropName', kindScalar, 1, [elementString]);
          cursor = ScbColumnCursor(reader, column, count, 'DropGroup.DropName');
          for (final record in loaded) {
            record.dropName = cursor.nextString();
          }
          break;
        case 4:
          checkColumn(column, 'DropGroup.DropArea', kindScalar, 1, [elementString]);
          cursor = ScbColumnCursor(reader, column, count, 'DropGroup.DropArea');
          for (final record in loaded) {
            record.dropArea = cursor.nextString();
          }
          break;
        case 5:
          checkColumn(column, 'DropGroup.DropItemIds', kindVarArray, 0, [elementI32, elementVarint]);
          for (final record in loaded) {
            final elementCount = reader.readCounter32();
            record.dropItemIds = List.generate(elementCount, (_) => reader.readI32As(column.element));
          }
          break;
        case 6:
          checkColumn(column, 'DropGroup.DropItemMin', kindVarArray, 0, [elementI32, elementVarint]);
          for (final record in loaded) {
            final elementCount = reader.readCounter32();
            record.dropItemMin = List.generate(elementCount, (_) => reader.readI32As(column.element));
          }
          break;
        case 7:
          checkColumn(column, 'DropGroup.DropItemMax', kindVarArray, 0, [elementI32, elementVarint]);
          for (final record in loaded) {
            final elementCount = reader.readCounter32();
            record.dropItemMax = List.generate(elementCount, (_) => reader.readI32As(column.element));
          }
          break;
        case 8:
          checkColumn(column, 'DropGroup.ItemDropRate', kindVarArray, 0, [elementF32]);
          for (final record in loaded) {
            final elementCount = reader.readCounter32();
            record.itemDropRate = List.generate(elementCount, (_) => reader.readFloat());
          }
          break;
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength);
          break;
      }

      checkBlockEnd(reader, column, blockEnd);
    }

    for (final record in loaded) {
      loadedById[record.id] = record;
    }

    // Published, now that every column read.
    _records = loaded;
    _byId = loadedById;
  }
}
