// ------------------------------------------------------------------------------
// Code generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package rescue

import (
	"fmt"

	"rescue/sheetman"
)

// ConfigRecord was generated from samples/rescue/MasterData.xlsx : ConfigTable : A1.
type ConfigRecord struct {
	// Row number, added by SheetMan because `Id` cannot be a primary index.
	Index int32
	// ConfigTable
	Id string
	// 카테고리
	Category string
	// 테이터타입
	DataType string
	// 기본값
	DefaultValue string
	// 설명
	Description string
}

// ConfigTable holds every row of Config.
type ConfigTable struct {
	records []ConfigRecord
	byIndex map[int32]int
	byId map[string]int
}

// Records returns every row, in the order the sheet declared them.
func (t *ConfigTable) Records() []ConfigRecord { return t.records }

// FindByIndex returns the row with this Index, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *ConfigTable) FindByIndex(key int32) *ConfigRecord {
	position, found := t.byIndex[key]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByIndexOrError returns the row with this Index, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *ConfigTable) GetByIndexOrError(key int32) (*ConfigRecord, error) {
	position, found := t.byIndex[key]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `Config` that corresponds to field `Index` value %v", key)
	}

	return &t.records[position], nil
}

// ContainsIndex reports whether the table holds a row with this Index.
func (t *ConfigTable) ContainsIndex(key int32) bool {
	_, found := t.byIndex[key]
	return found
}

// FindById returns the row with this Id, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *ConfigTable) FindById(key string) *ConfigRecord {
	position, found := t.byId[key]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByIdOrError returns the row with this Id, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *ConfigTable) GetByIdOrError(key string) (*ConfigRecord, error) {
	position, found := t.byId[key]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `Config` that corresponds to field `Id` value %v", key)
	}

	return &t.records[position], nil
}

// ContainsId reports whether the table holds a row with this Id.
func (t *ConfigTable) ContainsId(key string) bool {
	_, found := t.byId[key]
	return found
}

// Read loads the table from a .scb file written by SheetMan.
func (t *ConfigTable) Read(filename string) error {
	data, err := sheetman.ReadAllBytes(filename)
	if err != nil {
		return err
	}

	reader := sheetman.NewReader(data)
	count, columns := sheetman.ReadTableHeader(reader)

	// Column by column, matched by tag rather than position: a column this build does
	// not know is skipped by its block length, and one whose type changed incompatibly
	// fails naming the field.
	//
	// Read into rows of its own and assigned at the end. Reading a table that is already
	// loaded is a refresh, and a refresh that turns out to be unreadable has to leave what
	// the table holds alone - the previous rows and an error, not an empty table.
	records := make([]ConfigRecord, count)

	for _, column := range columns {
		blockEnd := reader.Position() + int(column.ByteLength)

		switch column.Tag {
		case 1:
			if sheetman.CheckColumn(reader, column, "Config.Index", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Config.Index")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Index = cursor.NextI32()
				}
			}
		case 2:
			if sheetman.CheckColumn(reader, column, "Config.Id", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Config.Id")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Id = cursor.NextString()
				}
			}
		case 3:
			if sheetman.CheckColumn(reader, column, "Config.Category", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Config.Category")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Category = cursor.NextString()
				}
			}
		case 4:
			if sheetman.CheckColumn(reader, column, "Config.DataType", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Config.DataType")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.DataType = cursor.NextString()
				}
			}
		case 5:
			if sheetman.CheckColumn(reader, column, "Config.DefaultValue", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Config.DefaultValue")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.DefaultValue = cursor.NextString()
				}
			}
		case 6:
			if sheetman.CheckColumn(reader, column, "Config.Description", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Config.Description")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Description = cursor.NextString()
				}
			}
		default:
			// A column added after this code was generated.
			reader.Skip(column.ByteLength)
		}

		sheetman.CheckBlockEnd(reader, column, blockEnd)
	}

	if err := reader.Err(); err != nil {
		return fmt.Errorf("Config: %w", err)
	}

	byIndex := make(map[int32]int, len(records))
	for i := range records {
		byIndex[records[i].Index] = i
	}

	byId := make(map[string]int, len(records))
	for i := range records {
		byId[records[i].Id] = i
	}

	// Published, now that every column read and the indexes are built.
	t.records = records
	t.byIndex = byIndex
	t.byId = byId

	return nil
}
