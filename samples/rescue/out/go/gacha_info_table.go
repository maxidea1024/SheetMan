// ------------------------------------------------------------------------------
// Code generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package rescue

import (
	"fmt"

	"rescue/sheetman"
)

// GachaInfoRecord was generated from samples/rescue/GachaData.xlsx : GachaInfoTable : A1.
type GachaInfoRecord struct {
	// HeroGacha_Table
	Id int32
	// 영문명
	Name string
	// 카테고리
	GachaType GachaType
	// 출력 우선순위
	Priority int32
	// 해금 조건 ID
	ConditionID int32
	// 기본 확률 ID
	RateId int32
	// 중간 변경 확률 가챠 횟수
	TriggerCount int32
	// 중간 변경 확률ID
	TriggerRateId int32
	// 확정 누적 횟수
	EndCount int32
	// 확정 확률ID
	EndRateId int32
	// 확정 픽업 캐릭터 ID
	EndCharacterId int32
	// 영입 횟수 초기화
	EnableReset bool
	// 영입 횟수 공유
	IsSharedCounter bool
	// 위시리스트 해금 조건
	WishListConditionID int32
	// 위시리스트 슬롯 최대값
	WishListMaxValue int32
	// 1회 영입
	IsSingle bool
	// 10회 영입
	IsTen bool
	// 재화 타입1
	CurrencyType1 CurrencyType
	// 티켓 소모값
	SingleCost1 int32
	// 재화 타입2
	CurrencyType2 CurrencyType
	// 다이아 소모값
	SingleCost2 int32
	// 탭 버튼 아이콘
	IconPath string
	// 건너뛰기 가능
	EnableSkip bool
}

// GachaInfoTable holds every row of GachaInfo.
type GachaInfoTable struct {
	records []GachaInfoRecord
	byId map[int32]int
}

// Records returns every row, in the order the sheet declared them.
func (t *GachaInfoTable) Records() []GachaInfoRecord { return t.records }

// FindById returns the row with this Id, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *GachaInfoTable) FindById(key int32) *GachaInfoRecord {
	position, found := t.byId[key]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByIdOrError returns the row with this Id, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *GachaInfoTable) GetByIdOrError(key int32) (*GachaInfoRecord, error) {
	position, found := t.byId[key]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `GachaInfo` that corresponds to field `Id` value %v", key)
	}

	return &t.records[position], nil
}

// ContainsId reports whether the table holds a row with this Id.
func (t *GachaInfoTable) ContainsId(key int32) bool {
	_, found := t.byId[key]
	return found
}

// Read loads the table from a .scb file written by SheetMan.
func (t *GachaInfoTable) Read(filename string) error {
	data, err := sheetman.ReadAllBytes(filename)
	if err != nil {
		return err
	}

	reader := sheetman.NewReader(data)
	count, columns := sheetman.ReadTableHeader(reader)

	// Column by column, matched by tag rather than position: a column this build does
	// not know is skipped by its block length, and one whose type changed incompatibly
	// fails naming the field.
	//
	// Read into rows of its own and assigned at the end. Reading a table that is already
	// loaded is a refresh, and a refresh that turns out to be unreadable has to leave what
	// the table holds alone - the previous rows and an error, not an empty table.
	records := make([]GachaInfoRecord, count)

	for _, column := range columns {
		blockEnd := reader.Position() + int(column.ByteLength)

		switch column.Tag {
		case 1:
			if sheetman.CheckColumn(reader, column, "GachaInfo.Id", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "GachaInfo.Id")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Id = cursor.NextI32()
				}
			}
		case 2:
			if sheetman.CheckColumn(reader, column, "GachaInfo.Name", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "GachaInfo.Name")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Name = cursor.NextString()
				}
			}
		case 3:
			if sheetman.CheckColumn(reader, column, "GachaInfo.GachaType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "GachaInfo.GachaType")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.GachaType = GachaType(cursor.NextI32())
				}
			}
		case 4:
			if sheetman.CheckColumn(reader, column, "GachaInfo.Priority", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "GachaInfo.Priority")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Priority = cursor.NextI32()
				}
			}
		case 5:
			if sheetman.CheckColumn(reader, column, "GachaInfo.ConditionID", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "GachaInfo.ConditionID")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.ConditionID = cursor.NextI32()
				}
			}
		case 6:
			if sheetman.CheckColumn(reader, column, "GachaInfo.RateId", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "GachaInfo.RateId")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.RateId = cursor.NextI32()
				}
			}
		case 7:
			if sheetman.CheckColumn(reader, column, "GachaInfo.TriggerCount", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "GachaInfo.TriggerCount")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.TriggerCount = cursor.NextI32()
				}
			}
		case 8:
			if sheetman.CheckColumn(reader, column, "GachaInfo.TriggerRateId", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "GachaInfo.TriggerRateId")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.TriggerRateId = cursor.NextI32()
				}
			}
		case 9:
			if sheetman.CheckColumn(reader, column, "GachaInfo.EndCount", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "GachaInfo.EndCount")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.EndCount = cursor.NextI32()
				}
			}
		case 10:
			if sheetman.CheckColumn(reader, column, "GachaInfo.EndRateId", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "GachaInfo.EndRateId")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.EndRateId = cursor.NextI32()
				}
			}
		case 11:
			if sheetman.CheckColumn(reader, column, "GachaInfo.EndCharacterId", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "GachaInfo.EndCharacterId")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.EndCharacterId = cursor.NextI32()
				}
			}
		case 12:
			if sheetman.CheckColumn(reader, column, "GachaInfo.EnableReset", sheetman.KindScalar, 1, sheetman.ElementBool) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "GachaInfo.EnableReset")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.EnableReset = cursor.NextBool()
				}
			}
		case 13:
			if sheetman.CheckColumn(reader, column, "GachaInfo.IsSharedCounter", sheetman.KindScalar, 1, sheetman.ElementBool) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "GachaInfo.IsSharedCounter")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.IsSharedCounter = cursor.NextBool()
				}
			}
		case 14:
			if sheetman.CheckColumn(reader, column, "GachaInfo.WishListConditionID", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "GachaInfo.WishListConditionID")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.WishListConditionID = cursor.NextI32()
				}
			}
		case 15:
			if sheetman.CheckColumn(reader, column, "GachaInfo.WishListMaxValue", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "GachaInfo.WishListMaxValue")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.WishListMaxValue = cursor.NextI32()
				}
			}
		case 16:
			if sheetman.CheckColumn(reader, column, "GachaInfo.IsSingle", sheetman.KindScalar, 1, sheetman.ElementBool) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "GachaInfo.IsSingle")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.IsSingle = cursor.NextBool()
				}
			}
		case 17:
			if sheetman.CheckColumn(reader, column, "GachaInfo.IsTen", sheetman.KindScalar, 1, sheetman.ElementBool) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "GachaInfo.IsTen")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.IsTen = cursor.NextBool()
				}
			}
		case 18:
			if sheetman.CheckColumn(reader, column, "GachaInfo.CurrencyType1", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "GachaInfo.CurrencyType1")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.CurrencyType1 = CurrencyType(cursor.NextI32())
				}
			}
		case 19:
			if sheetman.CheckColumn(reader, column, "GachaInfo.SingleCost1", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "GachaInfo.SingleCost1")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.SingleCost1 = cursor.NextI32()
				}
			}
		case 20:
			if sheetman.CheckColumn(reader, column, "GachaInfo.CurrencyType2", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "GachaInfo.CurrencyType2")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.CurrencyType2 = CurrencyType(cursor.NextI32())
				}
			}
		case 21:
			if sheetman.CheckColumn(reader, column, "GachaInfo.SingleCost2", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "GachaInfo.SingleCost2")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.SingleCost2 = cursor.NextI32()
				}
			}
		case 22:
			if sheetman.CheckColumn(reader, column, "GachaInfo.IconPath", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "GachaInfo.IconPath")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.IconPath = cursor.NextString()
				}
			}
		case 23:
			if sheetman.CheckColumn(reader, column, "GachaInfo.EnableSkip", sheetman.KindScalar, 1, sheetman.ElementBool) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "GachaInfo.EnableSkip")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.EnableSkip = cursor.NextBool()
				}
			}
		default:
			// A column added after this code was generated.
			reader.Skip(column.ByteLength)
		}

		sheetman.CheckBlockEnd(reader, column, blockEnd)
	}

	if err := reader.Err(); err != nil {
		return fmt.Errorf("GachaInfo: %w", err)
	}

	byId := make(map[int32]int, len(records))
	for i := range records {
		byId[records[i].Id] = i
	}

	// Published, now that every column read and the indexes are built.
	t.records = records
	t.byId = byId

	return nil
}
