// ------------------------------------------------------------------------------
// Code generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package rescue

import (
	"fmt"

	"rescue/sheetman"
)

// InfoGrowthRecord was generated from samples/rescue/StageGrowth.xlsx : InfoGrowthTable : A1.
type InfoGrowthRecord struct {
	// 기본값
	Id int32
	// 영문
	Name string
	// 한글
	InfoName string
	// 실제 데이터가 있는 시트 이름 (AccountGrowth/TraitGrowth/RelicGrowth)
	SheetName string
	// 시스템 분류 (Account/Trait/Relic)
	GrowthType GrowthType
	// 초기화 가능 불가능 체크
	Reset bool
	// 초기화 재화 타입
	ResetCostType CurrencyType
	// 초기화 비용 값
	ResetCostValue int32
	// 루프형 시스템만 사용 (예: 유물 20단계)
	MaxStep int32
	// 단일 레벨 최대치 (루프형 시스템은 100 고정)
	MaxLevel int32
	// 해금 조건
	UnlockConditionType string
	// 해금 제한
	UnlockConditionValue int32
	// 설명
	Description string
	// 아이콘
	IconPath string
}

// InfoGrowthTable holds every row of InfoGrowth.
type InfoGrowthTable struct {
	records []InfoGrowthRecord
	byId map[int32]int
}

// Records returns every row, in the order the sheet declared them.
func (t *InfoGrowthTable) Records() []InfoGrowthRecord { return t.records }

// FindById returns the row with this Id, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *InfoGrowthTable) FindById(key int32) *InfoGrowthRecord {
	position, found := t.byId[key]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByIdOrError returns the row with this Id, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *InfoGrowthTable) GetByIdOrError(key int32) (*InfoGrowthRecord, error) {
	position, found := t.byId[key]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `InfoGrowth` that corresponds to field `Id` value %v", key)
	}

	return &t.records[position], nil
}

// ContainsId reports whether the table holds a row with this Id.
func (t *InfoGrowthTable) ContainsId(key int32) bool {
	_, found := t.byId[key]
	return found
}

// Read loads the table from a .scb file written by SheetMan.
func (t *InfoGrowthTable) Read(filename string) error {
	data, err := sheetman.ReadAllBytes(filename)
	if err != nil {
		return err
	}

	reader := sheetman.NewReader(data)
	count, columns := sheetman.ReadTableHeader(reader)

	// Column by column, matched by tag rather than position: a column this build does
	// not know is skipped by its block length, and one whose type changed incompatibly
	// fails naming the field.
	//
	// Read into rows of its own and assigned at the end. Reading a table that is already
	// loaded is a refresh, and a refresh that turns out to be unreadable has to leave what
	// the table holds alone - the previous rows and an error, not an empty table.
	records := make([]InfoGrowthRecord, count)

	for _, column := range columns {
		blockEnd := reader.Position() + int(column.ByteLength)

		switch column.Tag {
		case 1:
			if sheetman.CheckColumn(reader, column, "InfoGrowth.Id", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "InfoGrowth.Id")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Id = cursor.NextI32()
				}
			}
		case 2:
			if sheetman.CheckColumn(reader, column, "InfoGrowth.Name", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "InfoGrowth.Name")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Name = cursor.NextString()
				}
			}
		case 3:
			if sheetman.CheckColumn(reader, column, "InfoGrowth.InfoName", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "InfoGrowth.InfoName")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.InfoName = cursor.NextString()
				}
			}
		case 4:
			if sheetman.CheckColumn(reader, column, "InfoGrowth.SheetName", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "InfoGrowth.SheetName")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.SheetName = cursor.NextString()
				}
			}
		case 5:
			if sheetman.CheckColumn(reader, column, "InfoGrowth.GrowthType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "InfoGrowth.GrowthType")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.GrowthType = GrowthType(cursor.NextI32())
				}
			}
		case 6:
			if sheetman.CheckColumn(reader, column, "InfoGrowth.Reset", sheetman.KindScalar, 1, sheetman.ElementBool) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "InfoGrowth.Reset")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Reset = cursor.NextBool()
				}
			}
		case 7:
			if sheetman.CheckColumn(reader, column, "InfoGrowth.ResetCostType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "InfoGrowth.ResetCostType")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.ResetCostType = CurrencyType(cursor.NextI32())
				}
			}
		case 8:
			if sheetman.CheckColumn(reader, column, "InfoGrowth.ResetCostValue", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "InfoGrowth.ResetCostValue")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.ResetCostValue = cursor.NextI32()
				}
			}
		case 9:
			if sheetman.CheckColumn(reader, column, "InfoGrowth.MaxStep", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "InfoGrowth.MaxStep")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.MaxStep = cursor.NextI32()
				}
			}
		case 10:
			if sheetman.CheckColumn(reader, column, "InfoGrowth.MaxLevel", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "InfoGrowth.MaxLevel")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.MaxLevel = cursor.NextI32()
				}
			}
		case 11:
			if sheetman.CheckColumn(reader, column, "InfoGrowth.UnlockConditionType", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "InfoGrowth.UnlockConditionType")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.UnlockConditionType = cursor.NextString()
				}
			}
		case 12:
			if sheetman.CheckColumn(reader, column, "InfoGrowth.UnlockConditionValue", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "InfoGrowth.UnlockConditionValue")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.UnlockConditionValue = cursor.NextI32()
				}
			}
		case 13:
			if sheetman.CheckColumn(reader, column, "InfoGrowth.Description", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "InfoGrowth.Description")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Description = cursor.NextString()
				}
			}
		case 14:
			if sheetman.CheckColumn(reader, column, "InfoGrowth.IconPath", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "InfoGrowth.IconPath")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.IconPath = cursor.NextString()
				}
			}
		default:
			// A column added after this code was generated.
			reader.Skip(column.ByteLength)
		}

		sheetman.CheckBlockEnd(reader, column, blockEnd)
	}

	if err := reader.Err(); err != nil {
		return fmt.Errorf("InfoGrowth: %w", err)
	}

	byId := make(map[int32]int, len(records))
	for i := range records {
		byId[records[i].Id] = i
	}

	// Published, now that every column read and the indexes are built.
	t.records = records
	t.byId = byId

	return nil
}
