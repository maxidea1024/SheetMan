// ------------------------------------------------------------------------------
// Code generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package rescue

import (
	"fmt"

	"rescue/sheetman"
)

// MaterialRecord was generated from samples/rescue/ItemData.xlsx : MaterialTable : A1.
type MaterialRecord struct {
	// Material_Table
	Id int32
	// 영문
	Name string
	// 한글
	ItemName string
	// 카테고리
	ItemType ItemType
	// 세부 재화 분류
	Type CurrencyType
	// 중첩 가능 유무
	Stackable bool
	// 최대 소유 값
	MaxStack int64
	// 아이템 사용 대기시간
	Cooltime int32
	// 아이템 유지 시간
	Duration int32
	// 아이콘 경로
	IconPath string
	// 프리펩 위치
	DropPrefabPath string
	// 아이템 설명
	Description string
	// 바로가기
	ShortCutIndex []int32
}

// MaterialTable holds every row of Material.
type MaterialTable struct {
	records []MaterialRecord
	byId map[int32]int
}

// Records returns every row, in the order the sheet declared them.
func (t *MaterialTable) Records() []MaterialRecord { return t.records }

// FindById returns the row with this Id, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *MaterialTable) FindById(key int32) *MaterialRecord {
	position, found := t.byId[key]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByIdOrError returns the row with this Id, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *MaterialTable) GetByIdOrError(key int32) (*MaterialRecord, error) {
	position, found := t.byId[key]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `Material` that corresponds to field `Id` value %v", key)
	}

	return &t.records[position], nil
}

// ContainsId reports whether the table holds a row with this Id.
func (t *MaterialTable) ContainsId(key int32) bool {
	_, found := t.byId[key]
	return found
}

// Read loads the table from a .scb file written by SheetMan.
func (t *MaterialTable) Read(filename string) error {
	data, err := sheetman.ReadAllBytes(filename)
	if err != nil {
		return err
	}

	reader := sheetman.NewReader(data)
	count, columns := sheetman.ReadTableHeader(reader)

	// Column by column, matched by tag rather than position: a column this build does
	// not know is skipped by its block length, and one whose type changed incompatibly
	// fails naming the field.
	//
	// Read into rows of its own and assigned at the end. Reading a table that is already
	// loaded is a refresh, and a refresh that turns out to be unreadable has to leave what
	// the table holds alone - the previous rows and an error, not an empty table.
	records := make([]MaterialRecord, count)

	for _, column := range columns {
		blockEnd := reader.Position() + int(column.ByteLength)

		switch column.Tag {
		case 1:
			if sheetman.CheckColumn(reader, column, "Material.Id", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Material.Id")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Id = cursor.NextI32()
				}
			}
		case 2:
			if sheetman.CheckColumn(reader, column, "Material.Name", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Material.Name")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Name = cursor.NextString()
				}
			}
		case 3:
			if sheetman.CheckColumn(reader, column, "Material.ItemName", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Material.ItemName")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.ItemName = cursor.NextString()
				}
			}
		case 4:
			if sheetman.CheckColumn(reader, column, "Material.ItemType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Material.ItemType")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.ItemType = ItemType(cursor.NextI32())
				}
			}
		case 5:
			if sheetman.CheckColumn(reader, column, "Material.Type", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Material.Type")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Type = CurrencyType(cursor.NextI32())
				}
			}
		case 6:
			if sheetman.CheckColumn(reader, column, "Material.Stackable", sheetman.KindScalar, 1, sheetman.ElementBool) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Material.Stackable")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Stackable = cursor.NextBool()
				}
			}
		case 7:
			if sheetman.CheckColumn(reader, column, "Material.MaxStack", sheetman.KindScalar, 1, sheetman.ElementI64, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Material.MaxStack")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.MaxStack = cursor.NextI64()
				}
			}
		case 8:
			if sheetman.CheckColumn(reader, column, "Material.Cooltime", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Material.Cooltime")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Cooltime = cursor.NextI32()
				}
			}
		case 9:
			if sheetman.CheckColumn(reader, column, "Material.Duration", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Material.Duration")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Duration = cursor.NextI32()
				}
			}
		case 10:
			if sheetman.CheckColumn(reader, column, "Material.IconPath", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Material.IconPath")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.IconPath = cursor.NextString()
				}
			}
		case 11:
			if sheetman.CheckColumn(reader, column, "Material.DropPrefabPath", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Material.DropPrefabPath")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.DropPrefabPath = cursor.NextString()
				}
			}
		case 12:
			if sheetman.CheckColumn(reader, column, "Material.Description", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Material.Description")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Description = cursor.NextString()
				}
			}
		case 13:
			if sheetman.CheckColumn(reader, column, "Material.ShortCutIndex", sheetman.KindVarArray, 0, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					elementCount := int(reader.ReadCounter32())
					r.ShortCutIndex = make([]int32, elementCount)
					for j := 0; j < elementCount; j++ {
						r.ShortCutIndex[j] = reader.ReadI32As(column.Element)
					}
				}
			}
		default:
			// A column added after this code was generated.
			reader.Skip(column.ByteLength)
		}

		sheetman.CheckBlockEnd(reader, column, blockEnd)
	}

	if err := reader.Err(); err != nil {
		return fmt.Errorf("Material: %w", err)
	}

	byId := make(map[int32]int, len(records))
	for i := range records {
		byId[records[i].Id] = i
	}

	// Published, now that every column read and the indexes are built.
	t.records = records
	t.byId = byId

	return nil
}
