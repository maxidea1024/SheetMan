// ------------------------------------------------------------------------------
// Code generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package rescue

import (
	"fmt"

	"rescue/sheetman"
)

// RelicRecord was generated from samples/rescue/StageGrowth.xlsx : RelicTable : A1.
type RelicRecord struct {
	// 기본값
	Id int32
	// 영문
	Name string
	// 한글
	RelicName string
	// 영향력 단계
	InfuluenceStep int32
	// 스탯 타입
	RelicType1 StatType
	// 유물 기본 값 1
	RelicValue1 float32
	// 유물 레벨 당 스탯 증가 수치 1
	RelicIncValue1 float32
	// 유물 기본 값 2
	RelicType2 StatType
	// 유물 레벨 당 스탯 증가 수치 2
	RelicValue2 float32
	// 유물 기본 값 3
	RelicIncValue2 float32
	// 유물 레벨 당 스탯 증가 수치 3
	RelicType3 StatType
	// 유물 기본 값 4
	RelicValue3 float32
	// 유물 레벨 당 스탯 증가 수치 4
	RelicIncValue3 float32
	// 설명
	Description string
	// 아이콘 경로
	IconPath string
}

// RelicTable holds every row of Relic.
type RelicTable struct {
	records []RelicRecord
	byId map[int32]int
}

// Records returns every row, in the order the sheet declared them.
func (t *RelicTable) Records() []RelicRecord { return t.records }

// FindById returns the row with this Id, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *RelicTable) FindById(key int32) *RelicRecord {
	position, found := t.byId[key]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByIdOrError returns the row with this Id, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *RelicTable) GetByIdOrError(key int32) (*RelicRecord, error) {
	position, found := t.byId[key]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `Relic` that corresponds to field `Id` value %v", key)
	}

	return &t.records[position], nil
}

// ContainsId reports whether the table holds a row with this Id.
func (t *RelicTable) ContainsId(key int32) bool {
	_, found := t.byId[key]
	return found
}

// Read loads the table from a .scb file written by SheetMan.
func (t *RelicTable) Read(filename string) error {
	data, err := sheetman.ReadAllBytes(filename)
	if err != nil {
		return err
	}

	reader := sheetman.NewReader(data)
	count, columns := sheetman.ReadTableHeader(reader)

	// Column by column, matched by tag rather than position: a column this build does
	// not know is skipped by its block length, and one whose type changed incompatibly
	// fails naming the field.
	//
	// Read into rows of its own and assigned at the end. Reading a table that is already
	// loaded is a refresh, and a refresh that turns out to be unreadable has to leave what
	// the table holds alone - the previous rows and an error, not an empty table.
	records := make([]RelicRecord, count)

	for _, column := range columns {
		blockEnd := reader.Position() + int(column.ByteLength)

		switch column.Tag {
		case 1:
			if sheetman.CheckColumn(reader, column, "Relic.Id", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Relic.Id")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Id = cursor.NextI32()
				}
			}
		case 2:
			if sheetman.CheckColumn(reader, column, "Relic.Name", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Relic.Name")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Name = cursor.NextString()
				}
			}
		case 3:
			if sheetman.CheckColumn(reader, column, "Relic.RelicName", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Relic.RelicName")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.RelicName = cursor.NextString()
				}
			}
		case 4:
			if sheetman.CheckColumn(reader, column, "Relic.InfuluenceStep", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Relic.InfuluenceStep")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.InfuluenceStep = cursor.NextI32()
				}
			}
		case 5:
			if sheetman.CheckColumn(reader, column, "Relic.RelicType1", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Relic.RelicType1")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.RelicType1 = StatType(cursor.NextI32())
				}
			}
		case 6:
			if sheetman.CheckColumn(reader, column, "Relic.RelicValue1", sheetman.KindScalar, 1, sheetman.ElementF32) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Relic.RelicValue1")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.RelicValue1 = cursor.NextF32()
				}
			}
		case 7:
			if sheetman.CheckColumn(reader, column, "Relic.RelicIncValue1", sheetman.KindScalar, 1, sheetman.ElementF32) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Relic.RelicIncValue1")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.RelicIncValue1 = cursor.NextF32()
				}
			}
		case 8:
			if sheetman.CheckColumn(reader, column, "Relic.RelicType2", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Relic.RelicType2")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.RelicType2 = StatType(cursor.NextI32())
				}
			}
		case 9:
			if sheetman.CheckColumn(reader, column, "Relic.RelicValue2", sheetman.KindScalar, 1, sheetman.ElementF32) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Relic.RelicValue2")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.RelicValue2 = cursor.NextF32()
				}
			}
		case 10:
			if sheetman.CheckColumn(reader, column, "Relic.RelicIncValue2", sheetman.KindScalar, 1, sheetman.ElementF32) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Relic.RelicIncValue2")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.RelicIncValue2 = cursor.NextF32()
				}
			}
		case 11:
			if sheetman.CheckColumn(reader, column, "Relic.RelicType3", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Relic.RelicType3")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.RelicType3 = StatType(cursor.NextI32())
				}
			}
		case 12:
			if sheetman.CheckColumn(reader, column, "Relic.RelicValue3", sheetman.KindScalar, 1, sheetman.ElementF32) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Relic.RelicValue3")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.RelicValue3 = cursor.NextF32()
				}
			}
		case 13:
			if sheetman.CheckColumn(reader, column, "Relic.RelicIncValue3", sheetman.KindScalar, 1, sheetman.ElementF32) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Relic.RelicIncValue3")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.RelicIncValue3 = cursor.NextF32()
				}
			}
		case 14:
			if sheetman.CheckColumn(reader, column, "Relic.Description", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Relic.Description")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Description = cursor.NextString()
				}
			}
		case 15:
			if sheetman.CheckColumn(reader, column, "Relic.IconPath", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Relic.IconPath")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.IconPath = cursor.NextString()
				}
			}
		default:
			// A column added after this code was generated.
			reader.Skip(column.ByteLength)
		}

		sheetman.CheckBlockEnd(reader, column, blockEnd)
	}

	if err := reader.Err(); err != nil {
		return fmt.Errorf("Relic: %w", err)
	}

	byId := make(map[int32]int, len(records))
	for i := range records {
		byId[records[i].Id] = i
	}

	// Published, now that every column read and the indexes are built.
	t.records = records
	t.byId = byId

	return nil
}
