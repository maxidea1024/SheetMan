// ------------------------------------------------------------------------------
// Code generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package rescue

import (
	"fmt"

	"rescue/sheetman"
)

// SDAlchemyStepRecord was generated from samples/rescue/SD_Contens.xlsx : SDAlchemyStepTable : A1.
type SDAlchemyStepRecord struct {
	// SDAlchemyStep_Table
	Id int32
	// 영문명
	Name string
	// 한글명
	NameKR string
	// 최대 레벨
	MaxLevel int32
	// 보상 타입
	RewardType CurrencyType
	// 보상 값
	RewardValue int32
}

// SDAlchemyStepTable holds every row of SDAlchemyStep.
type SDAlchemyStepTable struct {
	records []SDAlchemyStepRecord
	byId map[int32]int
}

// Records returns every row, in the order the sheet declared them.
func (t *SDAlchemyStepTable) Records() []SDAlchemyStepRecord { return t.records }

// FindById returns the row with this Id, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *SDAlchemyStepTable) FindById(key int32) *SDAlchemyStepRecord {
	position, found := t.byId[key]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByIdOrError returns the row with this Id, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *SDAlchemyStepTable) GetByIdOrError(key int32) (*SDAlchemyStepRecord, error) {
	position, found := t.byId[key]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `SDAlchemyStep` that corresponds to field `Id` value %v", key)
	}

	return &t.records[position], nil
}

// ContainsId reports whether the table holds a row with this Id.
func (t *SDAlchemyStepTable) ContainsId(key int32) bool {
	_, found := t.byId[key]
	return found
}

// Read loads the table from a .scb file written by SheetMan.
func (t *SDAlchemyStepTable) Read(filename string) error {
	data, err := sheetman.ReadAllBytes(filename)
	if err != nil {
		return err
	}

	reader := sheetman.NewReader(data)
	count, columns := sheetman.ReadTableHeader(reader)

	// Column by column, matched by tag rather than position: a column this build does
	// not know is skipped by its block length, and one whose type changed incompatibly
	// fails naming the field.
	//
	// Read into rows of its own and assigned at the end. Reading a table that is already
	// loaded is a refresh, and a refresh that turns out to be unreadable has to leave what
	// the table holds alone - the previous rows and an error, not an empty table.
	records := make([]SDAlchemyStepRecord, count)

	for _, column := range columns {
		blockEnd := reader.Position() + int(column.ByteLength)

		switch column.Tag {
		case 1:
			if sheetman.CheckColumn(reader, column, "SDAlchemyStep.Id", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "SDAlchemyStep.Id")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Id = cursor.NextI32()
				}
			}
		case 2:
			if sheetman.CheckColumn(reader, column, "SDAlchemyStep.Name", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "SDAlchemyStep.Name")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Name = cursor.NextString()
				}
			}
		case 3:
			if sheetman.CheckColumn(reader, column, "SDAlchemyStep.NameKR", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "SDAlchemyStep.NameKR")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.NameKR = cursor.NextString()
				}
			}
		case 4:
			if sheetman.CheckColumn(reader, column, "SDAlchemyStep.MaxLevel", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "SDAlchemyStep.MaxLevel")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.MaxLevel = cursor.NextI32()
				}
			}
		case 5:
			if sheetman.CheckColumn(reader, column, "SDAlchemyStep.RewardType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "SDAlchemyStep.RewardType")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.RewardType = CurrencyType(cursor.NextI32())
				}
			}
		case 6:
			if sheetman.CheckColumn(reader, column, "SDAlchemyStep.RewardValue", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "SDAlchemyStep.RewardValue")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.RewardValue = cursor.NextI32()
				}
			}
		default:
			// A column added after this code was generated.
			reader.Skip(column.ByteLength)
		}

		sheetman.CheckBlockEnd(reader, column, blockEnd)
	}

	if err := reader.Err(); err != nil {
		return fmt.Errorf("SDAlchemyStep: %w", err)
	}

	byId := make(map[int32]int, len(records))
	for i := range records {
		byId[records[i].Id] = i
	}

	// Published, now that every column read and the indexes are built.
	t.records = records
	t.byId = byId

	return nil
}
