// ------------------------------------------------------------------------------
// Code generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package rescue

import (
	"fmt"

	"rescue/sheetman"
)

// ShortCutRecord was generated from samples/rescue/MasterData.xlsx : ShortCutTable : A1.
type ShortCutRecord struct {
	// ShortCut_Table
	ID int32
	// 영문명
	Name string
	// 콘텐츠 카테고리
	Type ShortCutType
	// 하위 카테고리
	SubIndex int32
	// 설명
	Description string
}

// ShortCutTable holds every row of ShortCut.
type ShortCutTable struct {
	records []ShortCutRecord
	byID map[int32]int
}

// Records returns every row, in the order the sheet declared them.
func (t *ShortCutTable) Records() []ShortCutRecord { return t.records }

// FindByID returns the row with this ID, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *ShortCutTable) FindByID(key int32) *ShortCutRecord {
	position, found := t.byID[key]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByIDOrError returns the row with this ID, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *ShortCutTable) GetByIDOrError(key int32) (*ShortCutRecord, error) {
	position, found := t.byID[key]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `ShortCut` that corresponds to field `ID` value %v", key)
	}

	return &t.records[position], nil
}

// ContainsID reports whether the table holds a row with this ID.
func (t *ShortCutTable) ContainsID(key int32) bool {
	_, found := t.byID[key]
	return found
}

// Read loads the table from a .scb file written by SheetMan.
func (t *ShortCutTable) Read(filename string) error {
	data, err := sheetman.ReadAllBytes(filename)
	if err != nil {
		return err
	}

	reader := sheetman.NewReader(data)
	count, columns := sheetman.ReadTableHeader(reader)

	// Column by column, matched by tag rather than position: a column this build does
	// not know is skipped by its block length, and one whose type changed incompatibly
	// fails naming the field.
	//
	// Read into rows of its own and assigned at the end. Reading a table that is already
	// loaded is a refresh, and a refresh that turns out to be unreadable has to leave what
	// the table holds alone - the previous rows and an error, not an empty table.
	records := make([]ShortCutRecord, count)

	for _, column := range columns {
		blockEnd := reader.Position() + int(column.ByteLength)

		switch column.Tag {
		case 1:
			if sheetman.CheckColumn(reader, column, "ShortCut.ID", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "ShortCut.ID")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.ID = cursor.NextI32()
				}
			}
		case 2:
			if sheetman.CheckColumn(reader, column, "ShortCut.Name", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "ShortCut.Name")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Name = cursor.NextString()
				}
			}
		case 3:
			if sheetman.CheckColumn(reader, column, "ShortCut.Type", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "ShortCut.Type")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Type = ShortCutType(cursor.NextI32())
				}
			}
		case 4:
			if sheetman.CheckColumn(reader, column, "ShortCut.SubIndex", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "ShortCut.SubIndex")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.SubIndex = cursor.NextI32()
				}
			}
		case 5:
			if sheetman.CheckColumn(reader, column, "ShortCut.Description", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "ShortCut.Description")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Description = cursor.NextString()
				}
			}
		default:
			// A column added after this code was generated.
			reader.Skip(column.ByteLength)
		}

		sheetman.CheckBlockEnd(reader, column, blockEnd)
	}

	if err := reader.Err(); err != nil {
		return fmt.Errorf("ShortCut: %w", err)
	}

	byID := make(map[int32]int, len(records))
	for i := range records {
		byID[records[i].ID] = i
	}

	// Published, now that every column read and the indexes are built.
	t.records = records
	t.byID = byID

	return nil
}
