// ------------------------------------------------------------------------------
// Code generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package rescue

import (
	"fmt"

	"rescue/sheetman"
)

// SkillRecord was generated from samples/rescue/MasterData.xlsx : SkillTable : A1.
type SkillRecord struct {
	// Skill_Table
	Id int32
	// 영문
	Name string
	// 한글
	SkillName string
	// 메모
	SkillType SkillType
	// 메모 참조
	SkillSubType SkillSubType
	// 속성 설정
	AttributeType AttributeType
	// 메모참조
	TargetType TargetType
	// 스킬 경로
	AniPath string
	// 스킬 사용 거리
	SkillUseRange float32
	// 스킬 쿨타임
	CoolTime float32
	// 버프 ID
	BuffID []int32
	// 필살기 아이콘 경로
	SkillIcon string
	// 설명
	Description string
}

// SkillTable holds every row of Skill.
type SkillTable struct {
	records []SkillRecord
	byId map[int32]int
}

// Records returns every row, in the order the sheet declared them.
func (t *SkillTable) Records() []SkillRecord { return t.records }

// FindById returns the row with this Id, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *SkillTable) FindById(key int32) *SkillRecord {
	position, found := t.byId[key]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByIdOrError returns the row with this Id, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *SkillTable) GetByIdOrError(key int32) (*SkillRecord, error) {
	position, found := t.byId[key]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `Skill` that corresponds to field `Id` value %v", key)
	}

	return &t.records[position], nil
}

// ContainsId reports whether the table holds a row with this Id.
func (t *SkillTable) ContainsId(key int32) bool {
	_, found := t.byId[key]
	return found
}

// Read loads the table from a .scb file written by SheetMan.
func (t *SkillTable) Read(filename string) error {
	data, err := sheetman.ReadAllBytes(filename)
	if err != nil {
		return err
	}

	reader := sheetman.NewReader(data)
	count, columns := sheetman.ReadTableHeader(reader)

	// Column by column, matched by tag rather than position: a column this build does
	// not know is skipped by its block length, and one whose type changed incompatibly
	// fails naming the field.
	//
	// Read into rows of its own and assigned at the end. Reading a table that is already
	// loaded is a refresh, and a refresh that turns out to be unreadable has to leave what
	// the table holds alone - the previous rows and an error, not an empty table.
	records := make([]SkillRecord, count)

	for _, column := range columns {
		blockEnd := reader.Position() + int(column.ByteLength)

		switch column.Tag {
		case 1:
			if sheetman.CheckColumn(reader, column, "Skill.Id", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Skill.Id")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Id = cursor.NextI32()
				}
			}
		case 2:
			if sheetman.CheckColumn(reader, column, "Skill.Name", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Skill.Name")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Name = cursor.NextString()
				}
			}
		case 3:
			if sheetman.CheckColumn(reader, column, "Skill.SkillName", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Skill.SkillName")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.SkillName = cursor.NextString()
				}
			}
		case 4:
			if sheetman.CheckColumn(reader, column, "Skill.SkillType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Skill.SkillType")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.SkillType = SkillType(cursor.NextI32())
				}
			}
		case 5:
			if sheetman.CheckColumn(reader, column, "Skill.SkillSubType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Skill.SkillSubType")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.SkillSubType = SkillSubType(cursor.NextI32())
				}
			}
		case 6:
			if sheetman.CheckColumn(reader, column, "Skill.AttributeType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Skill.AttributeType")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.AttributeType = AttributeType(cursor.NextI32())
				}
			}
		case 7:
			if sheetman.CheckColumn(reader, column, "Skill.TargetType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Skill.TargetType")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.TargetType = TargetType(cursor.NextI32())
				}
			}
		case 8:
			if sheetman.CheckColumn(reader, column, "Skill.AniPath", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Skill.AniPath")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.AniPath = cursor.NextString()
				}
			}
		case 9:
			if sheetman.CheckColumn(reader, column, "Skill.SkillUseRange", sheetman.KindScalar, 1, sheetman.ElementF32) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Skill.SkillUseRange")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.SkillUseRange = cursor.NextF32()
				}
			}
		case 10:
			if sheetman.CheckColumn(reader, column, "Skill.CoolTime", sheetman.KindScalar, 1, sheetman.ElementF32) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Skill.CoolTime")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.CoolTime = cursor.NextF32()
				}
			}
		case 11:
			if sheetman.CheckColumn(reader, column, "Skill.BuffID", sheetman.KindVarArray, 0, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					elementCount := int(reader.ReadCounter32())
					r.BuffID = make([]int32, elementCount)
					for j := 0; j < elementCount; j++ {
						r.BuffID[j] = reader.ReadI32As(column.Element)
					}
				}
			}
		case 12:
			if sheetman.CheckColumn(reader, column, "Skill.SkillIcon", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Skill.SkillIcon")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.SkillIcon = cursor.NextString()
				}
			}
		case 13:
			if sheetman.CheckColumn(reader, column, "Skill.Description", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "Skill.Description")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Description = cursor.NextString()
				}
			}
		default:
			// A column added after this code was generated.
			reader.Skip(column.ByteLength)
		}

		sheetman.CheckBlockEnd(reader, column, blockEnd)
	}

	if err := reader.Err(); err != nil {
		return fmt.Errorf("Skill: %w", err)
	}

	byId := make(map[int32]int, len(records))
	for i := range records {
		byId[records[i].Id] = i
	}

	// Published, now that every column read and the indexes are built.
	t.records = records
	t.byId = byId

	return nil
}
