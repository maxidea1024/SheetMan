// ------------------------------------------------------------------------------
// Code generated by SheetMan. DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost if the code is
// regenerated.
// ------------------------------------------------------------------------------

package rescue

import (
	"fmt"

	"rescue/sheetman"
)

// TraitDungeonStageRecord was generated from samples/rescue/DailyDungeon.xlsx : TraitDungeonStageTable : A1.
type TraitDungeonStageRecord struct {
	// TraitDungeonStage_Table
	Id int32
	// 영문
	Name string
	// 한글
	StageName string
	// 던전 아이디
	DungeonType DungeonType
	// 던전 단계
	DungeonFloor int32
	// 제한 시간
	TimeLimit int32
	// 생성 몬스터 ID
	SpawnIds []int32
	// 보스 ID
	BossID []int32
	// 위치 생성 가능 횟수
	SpawnPointCount int32
	// 최대 생성 수
	SpawnMaxCount int32
	// 다음 포인터 생성 시간
	SpawnNextTime float32
	// 스테이지 클리어 조건
	StageClearCount int32
	// 권장 전투력
	RecommendPower string
	// 보상 설정
	RewardID int32
	// 던전 공격력 증가 값
	AtkUpPercent float32
	// 던전 방어력 증가 값
	DefUpPercent float32
	// 던전 체력 증가 값
	MHPUpPercent float32
	// 던전 이미지
	DungeonImagePath string
	// 몬스터 이미지
	MonsterImagePath string
	// AssetData 경로(Stage처럼 에셋만 추가하면 바뀌게)
	AssetDataPath string
}

// TraitDungeonStageTable holds every row of TraitDungeonStage.
type TraitDungeonStageTable struct {
	records []TraitDungeonStageRecord
	byId map[int32]int
}

// Records returns every row, in the order the sheet declared them.
func (t *TraitDungeonStageTable) Records() []TraitDungeonStageRecord { return t.records }

// FindById returns the row with this Id, or nil when the table has none.
//
// The lookup to reach for when a missing row is an ordinary answer - an optional
// reference, a key that came from user input.
func (t *TraitDungeonStageTable) FindById(key int32) *TraitDungeonStageRecord {
	position, found := t.byId[key]
	if !found {
		return nil
	}

	return &t.records[position]
}

// GetByIdOrError returns the row with this Id, or an error naming
// what was missing.
//
// For a key that has to be there - one from another table, or a constant. Go has no
// exception to throw, so where the other languages name a throw this one names the error
// it returns; either way the call site can tell without going to the definition.
func (t *TraitDungeonStageTable) GetByIdOrError(key int32) (*TraitDungeonStageRecord, error) {
	position, found := t.byId[key]
	if !found {
		return nil, fmt.Errorf(
			"there is no record in table `TraitDungeonStage` that corresponds to field `Id` value %v", key)
	}

	return &t.records[position], nil
}

// ContainsId reports whether the table holds a row with this Id.
func (t *TraitDungeonStageTable) ContainsId(key int32) bool {
	_, found := t.byId[key]
	return found
}

// Read loads the table from a .scb file written by SheetMan.
func (t *TraitDungeonStageTable) Read(filename string) error {
	data, err := sheetman.ReadAllBytes(filename)
	if err != nil {
		return err
	}

	reader := sheetman.NewReader(data)
	count, columns := sheetman.ReadTableHeader(reader)

	// Column by column, matched by tag rather than position: a column this build does
	// not know is skipped by its block length, and one whose type changed incompatibly
	// fails naming the field.
	//
	// Read into rows of its own and assigned at the end. Reading a table that is already
	// loaded is a refresh, and a refresh that turns out to be unreadable has to leave what
	// the table holds alone - the previous rows and an error, not an empty table.
	records := make([]TraitDungeonStageRecord, count)

	for _, column := range columns {
		blockEnd := reader.Position() + int(column.ByteLength)

		switch column.Tag {
		case 1:
			if sheetman.CheckColumn(reader, column, "TraitDungeonStage.Id", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "TraitDungeonStage.Id")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Id = cursor.NextI32()
				}
			}
		case 2:
			if sheetman.CheckColumn(reader, column, "TraitDungeonStage.Name", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "TraitDungeonStage.Name")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.Name = cursor.NextString()
				}
			}
		case 3:
			if sheetman.CheckColumn(reader, column, "TraitDungeonStage.StageName", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "TraitDungeonStage.StageName")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.StageName = cursor.NextString()
				}
			}
		case 4:
			if sheetman.CheckColumn(reader, column, "TraitDungeonStage.DungeonType", sheetman.KindScalar, 1, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "TraitDungeonStage.DungeonType")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.DungeonType = DungeonType(cursor.NextI32())
				}
			}
		case 5:
			if sheetman.CheckColumn(reader, column, "TraitDungeonStage.DungeonFloor", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "TraitDungeonStage.DungeonFloor")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.DungeonFloor = cursor.NextI32()
				}
			}
		case 6:
			if sheetman.CheckColumn(reader, column, "TraitDungeonStage.TimeLimit", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "TraitDungeonStage.TimeLimit")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.TimeLimit = cursor.NextI32()
				}
			}
		case 7:
			if sheetman.CheckColumn(reader, column, "TraitDungeonStage.SpawnIds", sheetman.KindVarArray, 0, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					elementCount := int(reader.ReadCounter32())
					r.SpawnIds = make([]int32, elementCount)
					for j := 0; j < elementCount; j++ {
						r.SpawnIds[j] = reader.ReadI32As(column.Element)
					}
				}
			}
		case 8:
			if sheetman.CheckColumn(reader, column, "TraitDungeonStage.BossID", sheetman.KindVarArray, 0, sheetman.ElementI32, sheetman.ElementVarint) {
				for i := int32(0); i < count; i++ {
					r := &records[i]
					elementCount := int(reader.ReadCounter32())
					r.BossID = make([]int32, elementCount)
					for j := 0; j < elementCount; j++ {
						r.BossID[j] = reader.ReadI32As(column.Element)
					}
				}
			}
		case 9:
			if sheetman.CheckColumn(reader, column, "TraitDungeonStage.SpawnPointCount", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "TraitDungeonStage.SpawnPointCount")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.SpawnPointCount = cursor.NextI32()
				}
			}
		case 10:
			if sheetman.CheckColumn(reader, column, "TraitDungeonStage.SpawnMaxCount", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "TraitDungeonStage.SpawnMaxCount")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.SpawnMaxCount = cursor.NextI32()
				}
			}
		case 11:
			if sheetman.CheckColumn(reader, column, "TraitDungeonStage.SpawnNextTime", sheetman.KindScalar, 1, sheetman.ElementF32) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "TraitDungeonStage.SpawnNextTime")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.SpawnNextTime = cursor.NextF32()
				}
			}
		case 12:
			if sheetman.CheckColumn(reader, column, "TraitDungeonStage.StageClearCount", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "TraitDungeonStage.StageClearCount")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.StageClearCount = cursor.NextI32()
				}
			}
		case 13:
			if sheetman.CheckColumn(reader, column, "TraitDungeonStage.RecommendPower", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "TraitDungeonStage.RecommendPower")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.RecommendPower = cursor.NextString()
				}
			}
		case 14:
			if sheetman.CheckColumn(reader, column, "TraitDungeonStage.RewardID", sheetman.KindScalar, 1, sheetman.ElementI32, sheetman.ElementVarint) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "TraitDungeonStage.RewardID")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.RewardID = cursor.NextI32()
				}
			}
		case 15:
			if sheetman.CheckColumn(reader, column, "TraitDungeonStage.AtkUpPercent", sheetman.KindScalar, 1, sheetman.ElementF32) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "TraitDungeonStage.AtkUpPercent")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.AtkUpPercent = cursor.NextF32()
				}
			}
		case 16:
			if sheetman.CheckColumn(reader, column, "TraitDungeonStage.DefUpPercent", sheetman.KindScalar, 1, sheetman.ElementF32) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "TraitDungeonStage.DefUpPercent")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.DefUpPercent = cursor.NextF32()
				}
			}
		case 17:
			if sheetman.CheckColumn(reader, column, "TraitDungeonStage.MHPUpPercent", sheetman.KindScalar, 1, sheetman.ElementF32) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "TraitDungeonStage.MHPUpPercent")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.MHPUpPercent = cursor.NextF32()
				}
			}
		case 18:
			if sheetman.CheckColumn(reader, column, "TraitDungeonStage.DungeonImagePath", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "TraitDungeonStage.DungeonImagePath")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.DungeonImagePath = cursor.NextString()
				}
			}
		case 19:
			if sheetman.CheckColumn(reader, column, "TraitDungeonStage.MonsterImagePath", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "TraitDungeonStage.MonsterImagePath")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.MonsterImagePath = cursor.NextString()
				}
			}
		case 20:
			if sheetman.CheckColumn(reader, column, "TraitDungeonStage.AssetDataPath", sheetman.KindScalar, 1, sheetman.ElementString) {
				cursor := sheetman.NewColumnCursor(reader, column, count, "TraitDungeonStage.AssetDataPath")
				for i := int32(0); i < count; i++ {
					r := &records[i]
					r.AssetDataPath = cursor.NextString()
				}
			}
		default:
			// A column added after this code was generated.
			reader.Skip(column.ByteLength)
		}

		sheetman.CheckBlockEnd(reader, column, blockEnd)
	}

	if err := reader.Err(); err != nil {
		return fmt.Errorf("TraitDungeonStage: %w", err)
	}

	byId := make(map[int32]int, len(records))
	for i := range records {
		byId[records[i].Id] = i
	}

	// Published, now that every column read and the indexes are built.
	t.records = records
	t.byId = byId

	return nil
}
