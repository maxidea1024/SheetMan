# ------------------------------------------------------------------------------
# Generated by SheetMan. DO NOT EDIT.
#
# Changes to this file may cause incorrect behavior and will be lost if the code is
# regenerated.
# ------------------------------------------------------------------------------

import enum
import os

from . import sheetman


class BGMSoundRecord:
    """Generated from samples/rescue/SoundData.xlsx : BGMSoundTable : A1.
    """

    __slots__ = ("id", "name", "path", "loop", "fade_time", "description")

    def __init__(self):
        self.id = 0
        self.name = ""
        self.path = ""
        self.loop = False
        self.fade_time = 0.0
        self.description = ""

    def __repr__(self):
        return "BGMSoundRecord(id=%r, name=%r, path=%r, loop=%r, fade_time=%r, description=%r)" % (self.id, self.name, self.path, self.loop, self.fade_time, self.description)


class BGMSoundTable:
    """Every row of BGMSound."""

    __slots__ = ("records", "by_id")

    def __init__(self):
        self.records = []
        self.by_id = {}

    def find_by_id(self, key):
        """The row with this Id, or None when the table has none.

        The lookup to reach for when a missing row is an ordinary answer - an optional
        reference, a key that came from user input.
        """
        return self.by_id.get(key)

    def get_by_id_or_throw(self, key):
        """The row with this Id, or a raised error naming what was missing.

        For a key that has to be there - one from another table, or a constant. The name
        says it raises, because a caller reading `get_by_id(key).name` at a
        glance cannot otherwise tell whether the next line is a check or a try.
        """
        record = self.by_id.get(key)

        if record is None:
            raise sheetman.RecordNotFoundError(
                "there is no record in table `BGMSound` that corresponds to "
                "field `Id` value %r" % (key,))

        return record

    def contains_id(self, key):
        """Whether the table holds a row with this Id."""
        return key in self.by_id

    def read(self, filename):
        """Loads the table from a .scb file written by SheetMan.

        Column by column, matched by tag rather than position: a column this build does
        not know is skipped by its block length, and one whose type changed incompatibly
        fails naming the field.
        """
        reader = sheetman.Reader(sheetman.read_all_bytes(filename))
        count, columns = sheetman.read_table_header(reader)

        # Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
        records = [BGMSoundRecord() for _ in range(count)]

        for column in columns:
            block_end = reader.position + column.byte_length
            if column.tag == 1:
                sheetman.check_column(column, "BGMSound.Id", sheetman.KIND_SCALAR, 1, (sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT))
                cursor = sheetman.ColumnCursor(reader, column, count, "BGMSound.Id")
                for record in records:
                    record.id = cursor.next_i32()
            elif column.tag == 2:
                sheetman.check_column(column, "BGMSound.Name", sheetman.KIND_SCALAR, 1, (sheetman.ELEMENT_STRING,))
                cursor = sheetman.ColumnCursor(reader, column, count, "BGMSound.Name")
                for record in records:
                    record.name = cursor.next_string()
            elif column.tag == 3:
                sheetman.check_column(column, "BGMSound.Path", sheetman.KIND_SCALAR, 1, (sheetman.ELEMENT_STRING,))
                cursor = sheetman.ColumnCursor(reader, column, count, "BGMSound.Path")
                for record in records:
                    record.path = cursor.next_string()
            elif column.tag == 4:
                sheetman.check_column(column, "BGMSound.Loop", sheetman.KIND_SCALAR, 1, (sheetman.ELEMENT_BOOL,))
                cursor = sheetman.ColumnCursor(reader, column, count, "BGMSound.Loop")
                for record in records:
                    record.loop = cursor.next_bool()
            elif column.tag == 5:
                sheetman.check_column(column, "BGMSound.FadeTime", sheetman.KIND_SCALAR, 1, (sheetman.ELEMENT_F32,))
                cursor = sheetman.ColumnCursor(reader, column, count, "BGMSound.FadeTime")
                for record in records:
                    record.fade_time = cursor.next_f32()
            elif column.tag == 6:
                sheetman.check_column(column, "BGMSound.Description", sheetman.KIND_SCALAR, 1, (sheetman.ELEMENT_STRING,))
                cursor = sheetman.ColumnCursor(reader, column, count, "BGMSound.Description")
                for record in records:
                    record.description = cursor.next_string()
            else:
                # A column added after this code was generated.
                reader.skip(column.byte_length)

            sheetman.check_block_end(reader, column, block_end)

        by_id = {record.id: record for record in records}

        # Published, now that every column read.
        self.records = records
        self.by_id = by_id
