# ------------------------------------------------------------------------------
# Generated by SheetMan. DO NOT EDIT.
#
# Changes to this file may cause incorrect behavior and will be lost if the code is
# regenerated.
# ------------------------------------------------------------------------------

import enum
import os

from . import sheetman


class DropGroupRecord:
    """Generated from samples/rescue/MasterData.xlsx : DropGroupTable : A1.
    """

    __slots__ = ("id", "name", "drop_name", "drop_area", "drop_item_ids", "drop_item_min", "drop_item_max", "item_drop_rate")

    def __init__(self):
        self.id = 0
        self.name = ""
        self.drop_name = ""
        self.drop_area = ""
        self.drop_item_ids = []
        self.drop_item_min = []
        self.drop_item_max = []
        self.item_drop_rate = []

    def __repr__(self):
        return "DropGroupRecord(id=%r, name=%r, drop_name=%r, drop_area=%r, drop_item_ids=%r, drop_item_min=%r, drop_item_max=%r, item_drop_rate=%r)" % (self.id, self.name, self.drop_name, self.drop_area, self.drop_item_ids, self.drop_item_min, self.drop_item_max, self.item_drop_rate)


class DropGroupTable:
    """Every row of DropGroup."""

    __slots__ = ("records", "by_id")

    def __init__(self):
        self.records = []
        self.by_id = {}

    def find_by_id(self, key):
        """The row with this Id, or None when the table has none.

        The lookup to reach for when a missing row is an ordinary answer - an optional
        reference, a key that came from user input.
        """
        return self.by_id.get(key)

    def get_by_id_or_throw(self, key):
        """The row with this Id, or a raised error naming what was missing.

        For a key that has to be there - one from another table, or a constant. The name
        says it raises, because a caller reading `get_by_id(key).name` at a
        glance cannot otherwise tell whether the next line is a check or a try.
        """
        record = self.by_id.get(key)

        if record is None:
            raise sheetman.RecordNotFoundError(
                "there is no record in table `DropGroup` that corresponds to "
                "field `Id` value %r" % (key,))

        return record

    def contains_id(self, key):
        """Whether the table holds a row with this Id."""
        return key in self.by_id

    def read(self, filename):
        """Loads the table from a .scb file written by SheetMan.

        Column by column, matched by tag rather than position: a column this build does
        not know is skipped by its block length, and one whose type changed incompatibly
        fails naming the field.
        """
        reader = sheetman.Reader(sheetman.read_all_bytes(filename))
        count, columns = sheetman.read_table_header(reader)

        # Read into storage of its own and published at the end: reading a table that is already loaded is a refresh, and one that turns out to be unreadable has to leave the rows already there alone.
        records = [DropGroupRecord() for _ in range(count)]

        for column in columns:
            block_end = reader.position + column.byte_length
            if column.tag == 1:
                sheetman.check_column(column, "DropGroup.Id", sheetman.KIND_SCALAR, 1, (sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT))
                cursor = sheetman.ColumnCursor(reader, column, count, "DropGroup.Id")
                for record in records:
                    record.id = cursor.next_i32()
            elif column.tag == 2:
                sheetman.check_column(column, "DropGroup.Name", sheetman.KIND_SCALAR, 1, (sheetman.ELEMENT_STRING,))
                cursor = sheetman.ColumnCursor(reader, column, count, "DropGroup.Name")
                for record in records:
                    record.name = cursor.next_string()
            elif column.tag == 3:
                sheetman.check_column(column, "DropGroup.DropName", sheetman.KIND_SCALAR, 1, (sheetman.ELEMENT_STRING,))
                cursor = sheetman.ColumnCursor(reader, column, count, "DropGroup.DropName")
                for record in records:
                    record.drop_name = cursor.next_string()
            elif column.tag == 4:
                sheetman.check_column(column, "DropGroup.DropArea", sheetman.KIND_SCALAR, 1, (sheetman.ELEMENT_STRING,))
                cursor = sheetman.ColumnCursor(reader, column, count, "DropGroup.DropArea")
                for record in records:
                    record.drop_area = cursor.next_string()
            elif column.tag == 5:
                sheetman.check_column(column, "DropGroup.DropItemIds", sheetman.KIND_VAR_ARRAY, 0, (sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT))
                for record in records:
                    element_count = reader.read_counter32()
                    record.drop_item_ids = [reader.read_i32_as(column.element) for _ in range(element_count)]
            elif column.tag == 6:
                sheetman.check_column(column, "DropGroup.DropItemMin", sheetman.KIND_VAR_ARRAY, 0, (sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT))
                for record in records:
                    element_count = reader.read_counter32()
                    record.drop_item_min = [reader.read_i32_as(column.element) for _ in range(element_count)]
            elif column.tag == 7:
                sheetman.check_column(column, "DropGroup.DropItemMax", sheetman.KIND_VAR_ARRAY, 0, (sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT))
                for record in records:
                    element_count = reader.read_counter32()
                    record.drop_item_max = [reader.read_i32_as(column.element) for _ in range(element_count)]
            elif column.tag == 8:
                sheetman.check_column(column, "DropGroup.ItemDropRate", sheetman.KIND_VAR_ARRAY, 0, (sheetman.ELEMENT_F32,))
                for record in records:
                    element_count = reader.read_counter32()
                    record.item_drop_rate = [reader.read_float() for _ in range(element_count)]
            else:
                # A column added after this code was generated.
                reader.skip(column.byte_length)

            sheetman.check_block_end(reader, column, block_end)

        by_id = {record.id: record for record in records}

        # Published, now that every column read.
        self.records = records
        self.by_id = by_id
