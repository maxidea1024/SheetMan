// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/scb-reader'

// Automatically import to handle external type references.
import { ArtifactJobType } from '../enums/artifact-job-type'
import { GradeType } from '../enums/grade-type'
import { StatType } from '../enums/stat-type'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  name: string
  artifactType: ArtifactJobType
  grade: GradeType
  equipSkillID: number[]
  equipStatType: StatType
  equipStatRate: number
  collectionType: StatType
  collectionVlaue: number
  iconPath: string
  materialPath: string
  description: string
}

// Generated from samples/rescue/MasterData.xlsx : ArtifactTable : A1
export class ArtifactRecord {
  /** Default constructor */
  constructor() {
  }

  /** Artifact_Table */
  public get id(): number { return this._id }

  /** 영문 */
  public get name(): string { return this._name }

  /** 아티팩트 타입 */
  public get artifactType(): ArtifactJobType { return this._artifactType }

  /** 아티팩트 태생 등급 */
  public get grade(): GradeType { return this._grade }

  /** 장착 버프 효과 */
  public get equipSkillID(): number[] { return this._equipSkillID }

  /** 장착 스탯 타입 */
  public get equipStatType(): StatType { return this._equipStatType }

  /** 장착 스탯 값 */
  public get equipStatRate(): number { return this._equipStatRate }

  /** 보유 스탯 타입 */
  public get collectionType(): StatType { return this._collectionType }

  /** 보유 스탯 값 */
  public get collectionVlaue(): number { return this._collectionVlaue }

  /** 아이콘 경로 */
  public get iconPath(): string { return this._iconPath }

  /** 머티리얼 경로 */
  public get materialPath(): string { return this._materialPath }

  /** 설명 */
  public get description(): string { return this._description }

  public _id: number = 0
  public _name: string = ''
  public _artifactType: ArtifactJobType = 0 as ArtifactJobType
  public _grade: GradeType = 0 as GradeType
  public _equipSkillID: number[] = []
  public _equipStatType: StatType = 0 as StatType
  public _equipStatRate: number = 0
  public _collectionType: StatType = 0 as StatType
  public _collectionVlaue: number = 0
  public _iconPath: string = ''
  public _materialPath: string = ''
  public _description: string = ''

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._name = dataRow.name
    this._artifactType = dataRow.artifactType
    this._grade = dataRow.grade
    this._equipSkillID = dataRow.equipSkillID
    this._equipStatType = dataRow.equipStatType
    this._equipStatRate = Math.fround(dataRow.equipStatRate)
    this._collectionType = dataRow.collectionType
    this._collectionVlaue = Math.fround(dataRow.collectionVlaue)
    this._iconPath = dataRow.iconPath
    this._materialPath = dataRow.materialPath
    this._description = dataRow.description
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._name = dataRow[offset++]
    this._artifactType = dataRow[offset++]
    this._grade = dataRow[offset++]
    this._equipSkillID = dataRow[offset++]
    this._equipStatType = dataRow[offset++]
    this._equipStatRate = Math.fround(dataRow[offset++])
    this._collectionType = dataRow[offset++]
    this._collectionVlaue = Math.fround(dataRow[offset++])
    this._iconPath = dataRow[offset++]
    this._materialPath = dataRow[offset++]
    this._description = dataRow[offset++]
  }
}

// Generated from samples/rescue/MasterData.xlsx : ArtifactTable : A1
export class ArtifactTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): ArtifactRecord[] { return this._records }
  private _records: ArtifactRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, ArtifactRecord> { return this._recordsById }
  private _recordsById: Map<number, ArtifactRecord> = new Map<number, ArtifactRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): ArtifactRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): ArtifactRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "Artifact" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: ArtifactRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new ArtifactRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new ArtifactRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .scb file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.ScbReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)
    let cursor: sheetman.ScbColumnCursor

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: ArtifactRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new ArtifactRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'Artifact.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Artifact.Id')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = cursor.nextI32()
          }
          break
        case 2:
          sheetman.checkColumn(column, 'Artifact.Name', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Artifact.Name')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._name = cursor.nextString()
          }
          break
        case 3:
          sheetman.checkColumn(column, 'Artifact.ArtifactType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Artifact.ArtifactType')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._artifactType = cursor.nextI32() as ArtifactJobType
          }
          break
        case 4:
          sheetman.checkColumn(column, 'Artifact.Grade', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Artifact.Grade')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._grade = cursor.nextI32() as GradeType
          }
          break
        case 5:
          sheetman.checkColumn(column, 'Artifact.EquipSkillID', sheetman.KIND_VAR_ARRAY, 0, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            const elementCount = reader.readCounter32()
            record._equipSkillID = []
            for (let j = 0; j < elementCount; ++j)
              record._equipSkillID.push(reader.readI32As(column.element))
          }
          break
        case 6:
          sheetman.checkColumn(column, 'Artifact.EquipStatType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Artifact.EquipStatType')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._equipStatType = cursor.nextI32() as StatType
          }
          break
        case 7:
          sheetman.checkColumn(column, 'Artifact.EquipStatRate', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Artifact.EquipStatRate')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._equipStatRate = cursor.nextF32()
          }
          break
        case 8:
          sheetman.checkColumn(column, 'Artifact.CollectionType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Artifact.CollectionType')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._collectionType = cursor.nextI32() as StatType
          }
          break
        case 9:
          sheetman.checkColumn(column, 'Artifact.CollectionVlaue', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Artifact.CollectionVlaue')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._collectionVlaue = cursor.nextF32()
          }
          break
        case 10:
          sheetman.checkColumn(column, 'Artifact.IconPath', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Artifact.IconPath')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._iconPath = cursor.nextString()
          }
          break
        case 11:
          sheetman.checkColumn(column, 'Artifact.MaterialPath', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Artifact.MaterialPath')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._materialPath = cursor.nextString()
          }
          break
        case 12:
          sheetman.checkColumn(column, 'Artifact.Description', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Artifact.Description')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._description = cursor.nextString()
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: ArtifactRecord[]): void {
    const recordsById = new Map<number, ArtifactRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
