// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/scb-reader'

// Automatically import to handle external type references.
import { CharacterType } from '../enums/character-type'
import { GradeType } from '../enums/grade-type'
import { AttributeType } from '../enums/attribute-type'
import { JobType } from '../enums/job-type'
import { NationType } from '../enums/nation-type'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  titleName: string
  name: string
  characterType: CharacterType
  grade: GradeType
  attributeType: AttributeType
  jobType: JobType
  nation: NationType
  baseATK: string
  baseDEF: string
  baseMHP: string
  moveSpeed: number
  critRate: number
  critDmg: number
  searchRange: number
  reFindTime: number
  maxFollow: number
  sizePrefab: number
  isUnlocked: boolean
  basicAttack1: number
  basicAttack2: number
  basicAttack3: number
  activeSkill1: number
  activeSkill2: number
  activeSkill3: number
  specialSkill1: number
  specialSkill2: number
  specialSkill3: number
  specialSkill4: number
  specialSkill5: number
  passiveBuff1: number
  passiveBuff2: number
  passiveBuff3: number
  prefabPath: string
  sdMaterialPath: string
  iconPath: string
  description: string
}

// Generated from samples/rescue/MasterData.xlsx : CharacterTable : A1
export class CharacterRecord {
  /** Default constructor */
  constructor() {
  }

  /** Character_Table */
  public get id(): number { return this._id }

  /** 접두사 */
  public get titleName(): string { return this._titleName }

  /** 후미사 */
  public get name(): string { return this._name }

  /** 캐릭터 타입 */
  public get characterType(): CharacterType { return this._characterType }

  /** 캐릭터 태생 등급 */
  public get grade(): GradeType { return this._grade }

  /** 캐릭터 속성 타입 */
  public get attributeType(): AttributeType { return this._attributeType }

  /** 직업 */
  public get jobType(): JobType { return this._jobType }

  /** 태생 국가 */
  public get nation(): NationType { return this._nation }

  /** 기본 공격 */
  public get baseATK(): bigint { return this._baseATK }

  /** 기본 방어력 */
  public get baseDEF(): bigint { return this._baseDEF }

  /** 기본 체력 */
  public get baseMHP(): bigint { return this._baseMHP }

  /** 이동 속도 */
  public get moveSpeed(): number { return this._moveSpeed }

  /** 크리티컬 확률 */
  public get critRate(): number { return this._critRate }

  /** 크리티컬 대미지 */
  public get critDmg(): number { return this._critDmg }

  /** 적 탐색 범위 */
  public get searchRange(): number { return this._searchRange }

  /** 적 재 탐지 시간 */
  public get reFindTime(): number { return this._reFindTime }

  /** 메인 케릭터와 멀어지는 거리 */
  public get maxFollow(): number { return this._maxFollow }

  /** 크기 설정 */
  public get sizePrefab(): number { return this._sizePrefab }

  /** 열림 체크 */
  public get isUnlocked(): boolean { return this._isUnlocked }

  /** 기본 공격1 */
  public get basicAttack1(): number { return this._basicAttack1 }

  /** 기본 공격2 */
  public get basicAttack2(): number { return this._basicAttack2 }

  /** 기본 공격3 */
  public get basicAttack3(): number { return this._basicAttack3 }

  /** 스킬 공격1 */
  public get activeSkill1(): number { return this._activeSkill1 }

  /** 스킬 공격2 */
  public get activeSkill2(): number { return this._activeSkill2 }

  /** 스킬 공격3 */
  public get activeSkill3(): number { return this._activeSkill3 }

  /** 필살기1단계 */
  public get specialSkill1(): number { return this._specialSkill1 }

  /** 필살기2단계 */
  public get specialSkill2(): number { return this._specialSkill2 }

  /** 필살기3단계 */
  public get specialSkill3(): number { return this._specialSkill3 }

  /** 필살기4단계 */
  public get specialSkill4(): number { return this._specialSkill4 }

  /** 필살기5단계 */
  public get specialSkill5(): number { return this._specialSkill5 }

  /** 패시브 버프1 */
  public get passiveBuff1(): number { return this._passiveBuff1 }

  /** 패시브 버프2 */
  public get passiveBuff2(): number { return this._passiveBuff2 }

  /** 패시브 버프3 */
  public get passiveBuff3(): number { return this._passiveBuff3 }

  /** 모델링 경로 */
  public get prefabPath(): string { return this._prefabPath }

  /** SD 캐릭터 머테리얼 경로 */
  public get sdMaterialPath(): string { return this._sdMaterialPath }

  /** 아이콘 경로 */
  public get iconPath(): string { return this._iconPath }

  /** 설명 */
  public get description(): string { return this._description }

  public _id: number = 0
  public _titleName: string = ''
  public _name: string = ''
  public _characterType: CharacterType = 0 as CharacterType
  public _grade: GradeType = 0 as GradeType
  public _attributeType: AttributeType = 0 as AttributeType
  public _jobType: JobType = 0 as JobType
  public _nation: NationType = 0 as NationType
  public _baseATK: bigint = 0n
  public _baseDEF: bigint = 0n
  public _baseMHP: bigint = 0n
  public _moveSpeed: number = 0
  public _critRate: number = 0
  public _critDmg: number = 0
  public _searchRange: number = 0
  public _reFindTime: number = 0
  public _maxFollow: number = 0
  public _sizePrefab: number = 0
  public _isUnlocked: boolean = false
  public _basicAttack1: number = 0
  public _basicAttack2: number = 0
  public _basicAttack3: number = 0
  public _activeSkill1: number = 0
  public _activeSkill2: number = 0
  public _activeSkill3: number = 0
  public _specialSkill1: number = 0
  public _specialSkill2: number = 0
  public _specialSkill3: number = 0
  public _specialSkill4: number = 0
  public _specialSkill5: number = 0
  public _passiveBuff1: number = 0
  public _passiveBuff2: number = 0
  public _passiveBuff3: number = 0
  public _prefabPath: string = ''
  public _sdMaterialPath: string = ''
  public _iconPath: string = ''
  public _description: string = ''

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._titleName = dataRow.titleName
    this._name = dataRow.name
    this._characterType = dataRow.characterType
    this._grade = dataRow.grade
    this._attributeType = dataRow.attributeType
    this._jobType = dataRow.jobType
    this._nation = dataRow.nation
    this._baseATK = BigInt(dataRow.baseATK)
    this._baseDEF = BigInt(dataRow.baseDEF)
    this._baseMHP = BigInt(dataRow.baseMHP)
    this._moveSpeed = Math.fround(dataRow.moveSpeed)
    this._critRate = Math.fround(dataRow.critRate)
    this._critDmg = Math.fround(dataRow.critDmg)
    this._searchRange = Math.fround(dataRow.searchRange)
    this._reFindTime = Math.fround(dataRow.reFindTime)
    this._maxFollow = Math.fround(dataRow.maxFollow)
    this._sizePrefab = Math.fround(dataRow.sizePrefab)
    this._isUnlocked = dataRow.isUnlocked
    this._basicAttack1 = dataRow.basicAttack1
    this._basicAttack2 = dataRow.basicAttack2
    this._basicAttack3 = dataRow.basicAttack3
    this._activeSkill1 = dataRow.activeSkill1
    this._activeSkill2 = dataRow.activeSkill2
    this._activeSkill3 = dataRow.activeSkill3
    this._specialSkill1 = dataRow.specialSkill1
    this._specialSkill2 = dataRow.specialSkill2
    this._specialSkill3 = dataRow.specialSkill3
    this._specialSkill4 = dataRow.specialSkill4
    this._specialSkill5 = dataRow.specialSkill5
    this._passiveBuff1 = dataRow.passiveBuff1
    this._passiveBuff2 = dataRow.passiveBuff2
    this._passiveBuff3 = dataRow.passiveBuff3
    this._prefabPath = dataRow.prefabPath
    this._sdMaterialPath = dataRow.sdMaterialPath
    this._iconPath = dataRow.iconPath
    this._description = dataRow.description
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._titleName = dataRow[offset++]
    this._name = dataRow[offset++]
    this._characterType = dataRow[offset++]
    this._grade = dataRow[offset++]
    this._attributeType = dataRow[offset++]
    this._jobType = dataRow[offset++]
    this._nation = dataRow[offset++]
    this._baseATK = BigInt(dataRow[offset++])
    this._baseDEF = BigInt(dataRow[offset++])
    this._baseMHP = BigInt(dataRow[offset++])
    this._moveSpeed = Math.fround(dataRow[offset++])
    this._critRate = Math.fround(dataRow[offset++])
    this._critDmg = Math.fround(dataRow[offset++])
    this._searchRange = Math.fround(dataRow[offset++])
    this._reFindTime = Math.fround(dataRow[offset++])
    this._maxFollow = Math.fround(dataRow[offset++])
    this._sizePrefab = Math.fround(dataRow[offset++])
    this._isUnlocked = dataRow[offset++]
    this._basicAttack1 = dataRow[offset++]
    this._basicAttack2 = dataRow[offset++]
    this._basicAttack3 = dataRow[offset++]
    this._activeSkill1 = dataRow[offset++]
    this._activeSkill2 = dataRow[offset++]
    this._activeSkill3 = dataRow[offset++]
    this._specialSkill1 = dataRow[offset++]
    this._specialSkill2 = dataRow[offset++]
    this._specialSkill3 = dataRow[offset++]
    this._specialSkill4 = dataRow[offset++]
    this._specialSkill5 = dataRow[offset++]
    this._passiveBuff1 = dataRow[offset++]
    this._passiveBuff2 = dataRow[offset++]
    this._passiveBuff3 = dataRow[offset++]
    this._prefabPath = dataRow[offset++]
    this._sdMaterialPath = dataRow[offset++]
    this._iconPath = dataRow[offset++]
    this._description = dataRow[offset++]
  }
}

// Generated from samples/rescue/MasterData.xlsx : CharacterTable : A1
export class CharacterTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): CharacterRecord[] { return this._records }
  private _records: CharacterRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, CharacterRecord> { return this._recordsById }
  private _recordsById: Map<number, CharacterRecord> = new Map<number, CharacterRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): CharacterRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): CharacterRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "Character" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: CharacterRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new CharacterRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new CharacterRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .scb file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.ScbReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)
    let cursor: sheetman.ScbColumnCursor

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: CharacterRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new CharacterRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'Character.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.Id')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = cursor.nextI32()
          }
          break
        case 2:
          sheetman.checkColumn(column, 'Character.TitleName', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.TitleName')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._titleName = cursor.nextString()
          }
          break
        case 3:
          sheetman.checkColumn(column, 'Character.Name', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.Name')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._name = cursor.nextString()
          }
          break
        case 4:
          sheetman.checkColumn(column, 'Character.CharacterType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.CharacterType')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._characterType = cursor.nextI32() as CharacterType
          }
          break
        case 5:
          sheetman.checkColumn(column, 'Character.Grade', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.Grade')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._grade = cursor.nextI32() as GradeType
          }
          break
        case 6:
          sheetman.checkColumn(column, 'Character.AttributeType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.AttributeType')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._attributeType = cursor.nextI32() as AttributeType
          }
          break
        case 7:
          sheetman.checkColumn(column, 'Character.JobType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.JobType')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._jobType = cursor.nextI32() as JobType
          }
          break
        case 8:
          sheetman.checkColumn(column, 'Character.Nation', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.Nation')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._nation = cursor.nextI32() as NationType
          }
          break
        case 9:
          sheetman.checkColumn(column, 'Character.BaseATK', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I64, sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.BaseATK')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._baseATK = cursor.nextI64()
          }
          break
        case 10:
          sheetman.checkColumn(column, 'Character.BaseDEF', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I64, sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.BaseDEF')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._baseDEF = cursor.nextI64()
          }
          break
        case 11:
          sheetman.checkColumn(column, 'Character.BaseMHP', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I64, sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.BaseMHP')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._baseMHP = cursor.nextI64()
          }
          break
        case 12:
          sheetman.checkColumn(column, 'Character.MoveSpeed', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.MoveSpeed')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._moveSpeed = cursor.nextF32()
          }
          break
        case 13:
          sheetman.checkColumn(column, 'Character.CritRate', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.CritRate')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._critRate = cursor.nextF32()
          }
          break
        case 14:
          sheetman.checkColumn(column, 'Character.CritDmg', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.CritDmg')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._critDmg = cursor.nextF32()
          }
          break
        case 15:
          sheetman.checkColumn(column, 'Character.SearchRange', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.SearchRange')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._searchRange = cursor.nextF32()
          }
          break
        case 16:
          sheetman.checkColumn(column, 'Character.ReFindTime', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.ReFindTime')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._reFindTime = cursor.nextF32()
          }
          break
        case 17:
          sheetman.checkColumn(column, 'Character.MaxFollow', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.MaxFollow')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._maxFollow = cursor.nextF32()
          }
          break
        case 18:
          sheetman.checkColumn(column, 'Character.SizePrefab', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.SizePrefab')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._sizePrefab = cursor.nextF32()
          }
          break
        case 19:
          sheetman.checkColumn(column, 'Character.IsUnlocked', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_BOOL])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.IsUnlocked')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._isUnlocked = cursor.nextBool()
          }
          break
        case 20:
          sheetman.checkColumn(column, 'Character.BasicAttack1', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.BasicAttack1')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._basicAttack1 = cursor.nextI32()
          }
          break
        case 21:
          sheetman.checkColumn(column, 'Character.BasicAttack2', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.BasicAttack2')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._basicAttack2 = cursor.nextI32()
          }
          break
        case 22:
          sheetman.checkColumn(column, 'Character.BasicAttack3', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.BasicAttack3')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._basicAttack3 = cursor.nextI32()
          }
          break
        case 23:
          sheetman.checkColumn(column, 'Character.ActiveSkill1', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.ActiveSkill1')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._activeSkill1 = cursor.nextI32()
          }
          break
        case 24:
          sheetman.checkColumn(column, 'Character.ActiveSkill2', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.ActiveSkill2')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._activeSkill2 = cursor.nextI32()
          }
          break
        case 25:
          sheetman.checkColumn(column, 'Character.ActiveSkill3', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.ActiveSkill3')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._activeSkill3 = cursor.nextI32()
          }
          break
        case 26:
          sheetman.checkColumn(column, 'Character.SpecialSkill1', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.SpecialSkill1')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._specialSkill1 = cursor.nextI32()
          }
          break
        case 27:
          sheetman.checkColumn(column, 'Character.SpecialSkill2', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.SpecialSkill2')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._specialSkill2 = cursor.nextI32()
          }
          break
        case 28:
          sheetman.checkColumn(column, 'Character.SpecialSkill3', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.SpecialSkill3')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._specialSkill3 = cursor.nextI32()
          }
          break
        case 29:
          sheetman.checkColumn(column, 'Character.SpecialSkill4', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.SpecialSkill4')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._specialSkill4 = cursor.nextI32()
          }
          break
        case 30:
          sheetman.checkColumn(column, 'Character.SpecialSkill5', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.SpecialSkill5')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._specialSkill5 = cursor.nextI32()
          }
          break
        case 31:
          sheetman.checkColumn(column, 'Character.PassiveBuff1', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.PassiveBuff1')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._passiveBuff1 = cursor.nextI32()
          }
          break
        case 32:
          sheetman.checkColumn(column, 'Character.PassiveBuff2', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.PassiveBuff2')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._passiveBuff2 = cursor.nextI32()
          }
          break
        case 33:
          sheetman.checkColumn(column, 'Character.PassiveBuff3', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.PassiveBuff3')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._passiveBuff3 = cursor.nextI32()
          }
          break
        case 34:
          sheetman.checkColumn(column, 'Character.PrefabPath', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.PrefabPath')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._prefabPath = cursor.nextString()
          }
          break
        case 35:
          sheetman.checkColumn(column, 'Character.SdMaterialPath', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.SdMaterialPath')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._sdMaterialPath = cursor.nextString()
          }
          break
        case 36:
          sheetman.checkColumn(column, 'Character.IconPath', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.IconPath')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._iconPath = cursor.nextString()
          }
          break
        case 37:
          sheetman.checkColumn(column, 'Character.Description', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Character.Description')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._description = cursor.nextString()
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: CharacterRecord[]): void {
    const recordsById = new Map<number, CharacterRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
