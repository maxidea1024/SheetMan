// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/scb-reader'

// Automatically import to handle external type references.
import { DungeonType } from '../enums/dungeon-type'
import { CycleType } from '../enums/cycle-type'
import { CurrencyType } from '../enums/currency-type'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  name: string
  dungeonName: string
  dungeonType: DungeonType
  priority: number
  cycleType: CycleType
  enterCondition: number
  enterCurrencyType: CurrencyType
  dungeonBuffID: number
  buffValueRate: number
  enterCurrencyValue: number
  freeEnterCurrencyType: CurrencyType
  aDEnterCount: number
  enterCurrencyCountMax: number
  dungeonImagePath: string
  dungeonBGMID: number
  description: string
}

// Generated from samples/rescue/DailyDungeon.xlsx : DailyDungeonInfoTable : A1
export class DailyDungeonInfoRecord {
  /** Default constructor */
  constructor() {
  }

  /** DailyDungeonInfo_Table */
  public get id(): number { return this._id }

  /** 영문명 */
  public get name(): string { return this._name }

  /** 한글명 */
  public get dungeonName(): string { return this._dungeonName }

  /** 던전 타입 */
  public get dungeonType(): DungeonType { return this._dungeonType }

  /** 출력 우선 순위 */
  public get priority(): number { return this._priority }

  /** 던전 입장 타입 */
  public get cycleType(): CycleType { return this._cycleType }

  /** 해금 조건 */
  public get enterCondition(): number { return this._enterCondition }

  /** 입장 재화 */
  public get enterCurrencyType(): CurrencyType { return this._enterCurrencyType }

  /** 던전 버프 ID */
  public get dungeonBuffID(): number { return this._dungeonBuffID }

  /** 버프 수치 증가값 */
  public get buffValueRate(): number { return this._buffValueRate }

  /** 입장 값 */
  public get enterCurrencyValue(): number { return this._enterCurrencyValue }

  /** 무료입장 재화 타입 */
  public get freeEnterCurrencyType(): CurrencyType { return this._freeEnterCurrencyType }

  /** 일일 광고 입장 횟수 */
  public get aDEnterCount(): number { return this._aDEnterCount }

  /** 일일 입장권 최대 소모 횟수 */
  public get enterCurrencyCountMax(): number { return this._enterCurrencyCountMax }

  /** 던전 이미지 */
  public get dungeonImagePath(): string { return this._dungeonImagePath }

  /** 던전 BGM */
  public get dungeonBGMID(): number { return this._dungeonBGMID }

  /** 설명 */
  public get description(): string { return this._description }

  public _id: number = 0
  public _name: string = ''
  public _dungeonName: string = ''
  public _dungeonType: DungeonType = 0 as DungeonType
  public _priority: number = 0
  public _cycleType: CycleType = 0 as CycleType
  public _enterCondition: number = 0
  public _enterCurrencyType: CurrencyType = 0 as CurrencyType
  public _dungeonBuffID: number = 0
  public _buffValueRate: number = 0
  public _enterCurrencyValue: number = 0
  public _freeEnterCurrencyType: CurrencyType = 0 as CurrencyType
  public _aDEnterCount: number = 0
  public _enterCurrencyCountMax: number = 0
  public _dungeonImagePath: string = ''
  public _dungeonBGMID: number = 0
  public _description: string = ''

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._name = dataRow.name
    this._dungeonName = dataRow.dungeonName
    this._dungeonType = dataRow.dungeonType
    this._priority = dataRow.priority
    this._cycleType = dataRow.cycleType
    this._enterCondition = dataRow.enterCondition
    this._enterCurrencyType = dataRow.enterCurrencyType
    this._dungeonBuffID = dataRow.dungeonBuffID
    this._buffValueRate = Math.fround(dataRow.buffValueRate)
    this._enterCurrencyValue = dataRow.enterCurrencyValue
    this._freeEnterCurrencyType = dataRow.freeEnterCurrencyType
    this._aDEnterCount = dataRow.aDEnterCount
    this._enterCurrencyCountMax = dataRow.enterCurrencyCountMax
    this._dungeonImagePath = dataRow.dungeonImagePath
    this._dungeonBGMID = dataRow.dungeonBGMID
    this._description = dataRow.description
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._name = dataRow[offset++]
    this._dungeonName = dataRow[offset++]
    this._dungeonType = dataRow[offset++]
    this._priority = dataRow[offset++]
    this._cycleType = dataRow[offset++]
    this._enterCondition = dataRow[offset++]
    this._enterCurrencyType = dataRow[offset++]
    this._dungeonBuffID = dataRow[offset++]
    this._buffValueRate = Math.fround(dataRow[offset++])
    this._enterCurrencyValue = dataRow[offset++]
    this._freeEnterCurrencyType = dataRow[offset++]
    this._aDEnterCount = dataRow[offset++]
    this._enterCurrencyCountMax = dataRow[offset++]
    this._dungeonImagePath = dataRow[offset++]
    this._dungeonBGMID = dataRow[offset++]
    this._description = dataRow[offset++]
  }
}

// Generated from samples/rescue/DailyDungeon.xlsx : DailyDungeonInfoTable : A1
export class DailyDungeonInfoTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): DailyDungeonInfoRecord[] { return this._records }
  private _records: DailyDungeonInfoRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, DailyDungeonInfoRecord> { return this._recordsById }
  private _recordsById: Map<number, DailyDungeonInfoRecord> = new Map<number, DailyDungeonInfoRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): DailyDungeonInfoRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): DailyDungeonInfoRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "DailyDungeonInfo" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: DailyDungeonInfoRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new DailyDungeonInfoRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new DailyDungeonInfoRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .scb file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.ScbReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)
    let cursor: sheetman.ScbColumnCursor

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: DailyDungeonInfoRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new DailyDungeonInfoRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'DailyDungeonInfo.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'DailyDungeonInfo.Id')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = cursor.nextI32()
          }
          break
        case 2:
          sheetman.checkColumn(column, 'DailyDungeonInfo.Name', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'DailyDungeonInfo.Name')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._name = cursor.nextString()
          }
          break
        case 3:
          sheetman.checkColumn(column, 'DailyDungeonInfo.DungeonName', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'DailyDungeonInfo.DungeonName')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._dungeonName = cursor.nextString()
          }
          break
        case 4:
          sheetman.checkColumn(column, 'DailyDungeonInfo.DungeonType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'DailyDungeonInfo.DungeonType')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._dungeonType = cursor.nextI32() as DungeonType
          }
          break
        case 5:
          sheetman.checkColumn(column, 'DailyDungeonInfo.Priority', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'DailyDungeonInfo.Priority')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._priority = cursor.nextI32()
          }
          break
        case 6:
          sheetman.checkColumn(column, 'DailyDungeonInfo.CycleType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'DailyDungeonInfo.CycleType')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._cycleType = cursor.nextI32() as CycleType
          }
          break
        case 7:
          sheetman.checkColumn(column, 'DailyDungeonInfo.EnterCondition', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'DailyDungeonInfo.EnterCondition')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._enterCondition = cursor.nextI32()
          }
          break
        case 8:
          sheetman.checkColumn(column, 'DailyDungeonInfo.EnterCurrencyType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'DailyDungeonInfo.EnterCurrencyType')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._enterCurrencyType = cursor.nextI32() as CurrencyType
          }
          break
        case 9:
          sheetman.checkColumn(column, 'DailyDungeonInfo.DungeonBuffID', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'DailyDungeonInfo.DungeonBuffID')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._dungeonBuffID = cursor.nextI32()
          }
          break
        case 10:
          sheetman.checkColumn(column, 'DailyDungeonInfo.BuffValueRate', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'DailyDungeonInfo.BuffValueRate')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._buffValueRate = cursor.nextF32()
          }
          break
        case 11:
          sheetman.checkColumn(column, 'DailyDungeonInfo.EnterCurrencyValue', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'DailyDungeonInfo.EnterCurrencyValue')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._enterCurrencyValue = cursor.nextI32()
          }
          break
        case 12:
          sheetman.checkColumn(column, 'DailyDungeonInfo.FreeEnterCurrencyType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'DailyDungeonInfo.FreeEnterCurrencyType')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._freeEnterCurrencyType = cursor.nextI32() as CurrencyType
          }
          break
        case 13:
          sheetman.checkColumn(column, 'DailyDungeonInfo.ADEnterCount', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'DailyDungeonInfo.ADEnterCount')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._aDEnterCount = cursor.nextI32()
          }
          break
        case 14:
          sheetman.checkColumn(column, 'DailyDungeonInfo.EnterCurrencyCountMax', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'DailyDungeonInfo.EnterCurrencyCountMax')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._enterCurrencyCountMax = cursor.nextI32()
          }
          break
        case 15:
          sheetman.checkColumn(column, 'DailyDungeonInfo.DungeonImagePath', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'DailyDungeonInfo.DungeonImagePath')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._dungeonImagePath = cursor.nextString()
          }
          break
        case 16:
          sheetman.checkColumn(column, 'DailyDungeonInfo.DungeonBGMID', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'DailyDungeonInfo.DungeonBGMID')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._dungeonBGMID = cursor.nextI32()
          }
          break
        case 17:
          sheetman.checkColumn(column, 'DailyDungeonInfo.Description', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'DailyDungeonInfo.Description')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._description = cursor.nextString()
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: DailyDungeonInfoRecord[]): void {
    const recordsById = new Map<number, DailyDungeonInfoRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
