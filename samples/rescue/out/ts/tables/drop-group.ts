// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/scb-reader'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  name: string
  dropName: string
  dropArea: string
  dropItemIds: number[]
  dropItemMin: number[]
  dropItemMax: number[]
  itemDropRate: number[]
}

// Generated from samples/rescue/MasterData.xlsx : DropGroupTable : A1
export class DropGroupRecord {
  /** Default constructor */
  constructor() {
  }

  /** 기본값 */
  public get id(): number { return this._id }

  /** 영문 */
  public get name(): string { return this._name }

  /** 한글 */
  public get dropName(): string { return this._dropName }

  /** 드랍 구역 */
  public get dropArea(): string { return this._dropArea }

  /** 아이템 종류1 */
  public get dropItemIds(): number[] { return this._dropItemIds }

  /** 아이템 최소 수량 값 */
  public get dropItemMin(): number[] { return this._dropItemMin }

  /** 아이템 최대 수량 값 */
  public get dropItemMax(): number[] { return this._dropItemMax }

  /** 확률 */
  public get itemDropRate(): number[] { return this._itemDropRate }

  public _id: number = 0
  public _name: string = ''
  public _dropName: string = ''
  public _dropArea: string = ''
  public _dropItemIds: number[] = []
  public _dropItemMin: number[] = []
  public _dropItemMax: number[] = []
  public _itemDropRate: number[] = []

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._name = dataRow.name
    this._dropName = dataRow.dropName
    this._dropArea = dataRow.dropArea
    this._dropItemIds = dataRow.dropItemIds
    this._dropItemMin = dataRow.dropItemMin
    this._dropItemMax = dataRow.dropItemMax
    this._itemDropRate = dataRow.itemDropRate.map(v => Math.fround(v))
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._name = dataRow[offset++]
    this._dropName = dataRow[offset++]
    this._dropArea = dataRow[offset++]
    this._dropItemIds = dataRow[offset++]
    this._dropItemMin = dataRow[offset++]
    this._dropItemMax = dataRow[offset++]
    this._itemDropRate = dataRow[offset++].map(v => Math.fround(v))
  }
}

// Generated from samples/rescue/MasterData.xlsx : DropGroupTable : A1
export class DropGroupTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): DropGroupRecord[] { return this._records }
  private _records: DropGroupRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, DropGroupRecord> { return this._recordsById }
  private _recordsById: Map<number, DropGroupRecord> = new Map<number, DropGroupRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): DropGroupRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): DropGroupRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "DropGroup" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: DropGroupRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new DropGroupRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new DropGroupRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .scb file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.ScbReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)
    let cursor: sheetman.ScbColumnCursor

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: DropGroupRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new DropGroupRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'DropGroup.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'DropGroup.Id')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = cursor.nextI32()
          }
          break
        case 2:
          sheetman.checkColumn(column, 'DropGroup.Name', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'DropGroup.Name')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._name = cursor.nextString()
          }
          break
        case 3:
          sheetman.checkColumn(column, 'DropGroup.DropName', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'DropGroup.DropName')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._dropName = cursor.nextString()
          }
          break
        case 4:
          sheetman.checkColumn(column, 'DropGroup.DropArea', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'DropGroup.DropArea')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._dropArea = cursor.nextString()
          }
          break
        case 5:
          sheetman.checkColumn(column, 'DropGroup.DropItemIds', sheetman.KIND_VAR_ARRAY, 0, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            const elementCount = reader.readCounter32()
            record._dropItemIds = []
            for (let j = 0; j < elementCount; ++j)
              record._dropItemIds.push(reader.readI32As(column.element))
          }
          break
        case 6:
          sheetman.checkColumn(column, 'DropGroup.DropItemMin', sheetman.KIND_VAR_ARRAY, 0, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            const elementCount = reader.readCounter32()
            record._dropItemMin = []
            for (let j = 0; j < elementCount; ++j)
              record._dropItemMin.push(reader.readI32As(column.element))
          }
          break
        case 7:
          sheetman.checkColumn(column, 'DropGroup.DropItemMax', sheetman.KIND_VAR_ARRAY, 0, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            const elementCount = reader.readCounter32()
            record._dropItemMax = []
            for (let j = 0; j < elementCount; ++j)
              record._dropItemMax.push(reader.readI32As(column.element))
          }
          break
        case 8:
          sheetman.checkColumn(column, 'DropGroup.ItemDropRate', sheetman.KIND_VAR_ARRAY, 0, [sheetman.ELEMENT_F32])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            const elementCount = reader.readCounter32()
            record._itemDropRate = []
            for (let j = 0; j < elementCount; ++j)
              record._itemDropRate.push(reader.readFloat())
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: DropGroupRecord[]): void {
    const recordsById = new Map<number, DropGroupRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
