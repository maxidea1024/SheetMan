// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/scb-reader'

// Automatically import to handle external type references.
import { GachaType } from '../enums/gacha-type'
import { CurrencyType } from '../enums/currency-type'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  name: string
  gachaType: GachaType
  priority: number
  conditionID: number
  rateId: number
  triggerCount: number
  triggerRateId: number
  endCount: number
  endRateId: number
  endCharacterId: number
  enableReset: boolean
  isSharedCounter: boolean
  wishListConditionID: number
  wishListMaxValue: number
  isSingle: boolean
  isTen: boolean
  currencyType1: CurrencyType
  singleCost1: number
  currencyType2: CurrencyType
  singleCost2: number
  iconPath: string
  enableSkip: boolean
}

// Generated from samples/rescue/GachaData.xlsx : GachaInfoTable : A1
export class GachaInfoRecord {
  /** Default constructor */
  constructor() {
  }

  /** HeroGacha_Table */
  public get id(): number { return this._id }

  /** 영문명 */
  public get name(): string { return this._name }

  /** 카테고리 */
  public get gachaType(): GachaType { return this._gachaType }

  /** 출력 우선순위 */
  public get priority(): number { return this._priority }

  /** 해금 조건 ID */
  public get conditionID(): number { return this._conditionID }

  /** 기본 확률 ID */
  public get rateId(): number { return this._rateId }

  /** 중간 변경 확률 가챠 횟수 */
  public get triggerCount(): number { return this._triggerCount }

  /** 중간 변경 확률ID */
  public get triggerRateId(): number { return this._triggerRateId }

  /** 확정 누적 횟수 */
  public get endCount(): number { return this._endCount }

  /** 확정 확률ID */
  public get endRateId(): number { return this._endRateId }

  /** 확정 픽업 캐릭터 ID */
  public get endCharacterId(): number { return this._endCharacterId }

  /** 영입 횟수 초기화 */
  public get enableReset(): boolean { return this._enableReset }

  /** 영입 횟수 공유 */
  public get isSharedCounter(): boolean { return this._isSharedCounter }

  /** 위시리스트 해금 조건 */
  public get wishListConditionID(): number { return this._wishListConditionID }

  /** 위시리스트 슬롯 최대값 */
  public get wishListMaxValue(): number { return this._wishListMaxValue }

  /** 1회 영입 */
  public get isSingle(): boolean { return this._isSingle }

  /** 10회 영입 */
  public get isTen(): boolean { return this._isTen }

  /** 재화 타입1 */
  public get currencyType1(): CurrencyType { return this._currencyType1 }

  /** 티켓 소모값 */
  public get singleCost1(): number { return this._singleCost1 }

  /** 재화 타입2 */
  public get currencyType2(): CurrencyType { return this._currencyType2 }

  /** 다이아 소모값 */
  public get singleCost2(): number { return this._singleCost2 }

  /** 탭 버튼 아이콘 */
  public get iconPath(): string { return this._iconPath }

  /** 건너뛰기 가능 */
  public get enableSkip(): boolean { return this._enableSkip }

  public _id: number = 0
  public _name: string = ''
  public _gachaType: GachaType = 0 as GachaType
  public _priority: number = 0
  public _conditionID: number = 0
  public _rateId: number = 0
  public _triggerCount: number = 0
  public _triggerRateId: number = 0
  public _endCount: number = 0
  public _endRateId: number = 0
  public _endCharacterId: number = 0
  public _enableReset: boolean = false
  public _isSharedCounter: boolean = false
  public _wishListConditionID: number = 0
  public _wishListMaxValue: number = 0
  public _isSingle: boolean = false
  public _isTen: boolean = false
  public _currencyType1: CurrencyType = 0 as CurrencyType
  public _singleCost1: number = 0
  public _currencyType2: CurrencyType = 0 as CurrencyType
  public _singleCost2: number = 0
  public _iconPath: string = ''
  public _enableSkip: boolean = false

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._name = dataRow.name
    this._gachaType = dataRow.gachaType
    this._priority = dataRow.priority
    this._conditionID = dataRow.conditionID
    this._rateId = dataRow.rateId
    this._triggerCount = dataRow.triggerCount
    this._triggerRateId = dataRow.triggerRateId
    this._endCount = dataRow.endCount
    this._endRateId = dataRow.endRateId
    this._endCharacterId = dataRow.endCharacterId
    this._enableReset = dataRow.enableReset
    this._isSharedCounter = dataRow.isSharedCounter
    this._wishListConditionID = dataRow.wishListConditionID
    this._wishListMaxValue = dataRow.wishListMaxValue
    this._isSingle = dataRow.isSingle
    this._isTen = dataRow.isTen
    this._currencyType1 = dataRow.currencyType1
    this._singleCost1 = dataRow.singleCost1
    this._currencyType2 = dataRow.currencyType2
    this._singleCost2 = dataRow.singleCost2
    this._iconPath = dataRow.iconPath
    this._enableSkip = dataRow.enableSkip
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._name = dataRow[offset++]
    this._gachaType = dataRow[offset++]
    this._priority = dataRow[offset++]
    this._conditionID = dataRow[offset++]
    this._rateId = dataRow[offset++]
    this._triggerCount = dataRow[offset++]
    this._triggerRateId = dataRow[offset++]
    this._endCount = dataRow[offset++]
    this._endRateId = dataRow[offset++]
    this._endCharacterId = dataRow[offset++]
    this._enableReset = dataRow[offset++]
    this._isSharedCounter = dataRow[offset++]
    this._wishListConditionID = dataRow[offset++]
    this._wishListMaxValue = dataRow[offset++]
    this._isSingle = dataRow[offset++]
    this._isTen = dataRow[offset++]
    this._currencyType1 = dataRow[offset++]
    this._singleCost1 = dataRow[offset++]
    this._currencyType2 = dataRow[offset++]
    this._singleCost2 = dataRow[offset++]
    this._iconPath = dataRow[offset++]
    this._enableSkip = dataRow[offset++]
  }
}

// Generated from samples/rescue/GachaData.xlsx : GachaInfoTable : A1
export class GachaInfoTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): GachaInfoRecord[] { return this._records }
  private _records: GachaInfoRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, GachaInfoRecord> { return this._recordsById }
  private _recordsById: Map<number, GachaInfoRecord> = new Map<number, GachaInfoRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): GachaInfoRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): GachaInfoRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "GachaInfo" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: GachaInfoRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new GachaInfoRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new GachaInfoRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .scb file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.ScbReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)
    let cursor: sheetman.ScbColumnCursor

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: GachaInfoRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new GachaInfoRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'GachaInfo.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'GachaInfo.Id')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = cursor.nextI32()
          }
          break
        case 2:
          sheetman.checkColumn(column, 'GachaInfo.Name', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'GachaInfo.Name')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._name = cursor.nextString()
          }
          break
        case 3:
          sheetman.checkColumn(column, 'GachaInfo.GachaType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'GachaInfo.GachaType')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._gachaType = cursor.nextI32() as GachaType
          }
          break
        case 4:
          sheetman.checkColumn(column, 'GachaInfo.Priority', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'GachaInfo.Priority')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._priority = cursor.nextI32()
          }
          break
        case 5:
          sheetman.checkColumn(column, 'GachaInfo.ConditionID', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'GachaInfo.ConditionID')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._conditionID = cursor.nextI32()
          }
          break
        case 6:
          sheetman.checkColumn(column, 'GachaInfo.RateId', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'GachaInfo.RateId')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._rateId = cursor.nextI32()
          }
          break
        case 7:
          sheetman.checkColumn(column, 'GachaInfo.TriggerCount', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'GachaInfo.TriggerCount')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._triggerCount = cursor.nextI32()
          }
          break
        case 8:
          sheetman.checkColumn(column, 'GachaInfo.TriggerRateId', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'GachaInfo.TriggerRateId')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._triggerRateId = cursor.nextI32()
          }
          break
        case 9:
          sheetman.checkColumn(column, 'GachaInfo.EndCount', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'GachaInfo.EndCount')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._endCount = cursor.nextI32()
          }
          break
        case 10:
          sheetman.checkColumn(column, 'GachaInfo.EndRateId', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'GachaInfo.EndRateId')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._endRateId = cursor.nextI32()
          }
          break
        case 11:
          sheetman.checkColumn(column, 'GachaInfo.EndCharacterId', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'GachaInfo.EndCharacterId')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._endCharacterId = cursor.nextI32()
          }
          break
        case 12:
          sheetman.checkColumn(column, 'GachaInfo.EnableReset', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_BOOL])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'GachaInfo.EnableReset')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._enableReset = cursor.nextBool()
          }
          break
        case 13:
          sheetman.checkColumn(column, 'GachaInfo.IsSharedCounter', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_BOOL])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'GachaInfo.IsSharedCounter')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._isSharedCounter = cursor.nextBool()
          }
          break
        case 14:
          sheetman.checkColumn(column, 'GachaInfo.WishListConditionID', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'GachaInfo.WishListConditionID')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._wishListConditionID = cursor.nextI32()
          }
          break
        case 15:
          sheetman.checkColumn(column, 'GachaInfo.WishListMaxValue', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'GachaInfo.WishListMaxValue')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._wishListMaxValue = cursor.nextI32()
          }
          break
        case 16:
          sheetman.checkColumn(column, 'GachaInfo.IsSingle', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_BOOL])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'GachaInfo.IsSingle')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._isSingle = cursor.nextBool()
          }
          break
        case 17:
          sheetman.checkColumn(column, 'GachaInfo.IsTen', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_BOOL])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'GachaInfo.IsTen')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._isTen = cursor.nextBool()
          }
          break
        case 18:
          sheetman.checkColumn(column, 'GachaInfo.CurrencyType1', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'GachaInfo.CurrencyType1')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._currencyType1 = cursor.nextI32() as CurrencyType
          }
          break
        case 19:
          sheetman.checkColumn(column, 'GachaInfo.SingleCost1', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'GachaInfo.SingleCost1')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._singleCost1 = cursor.nextI32()
          }
          break
        case 20:
          sheetman.checkColumn(column, 'GachaInfo.CurrencyType2', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'GachaInfo.CurrencyType2')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._currencyType2 = cursor.nextI32() as CurrencyType
          }
          break
        case 21:
          sheetman.checkColumn(column, 'GachaInfo.SingleCost2', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'GachaInfo.SingleCost2')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._singleCost2 = cursor.nextI32()
          }
          break
        case 22:
          sheetman.checkColumn(column, 'GachaInfo.IconPath', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'GachaInfo.IconPath')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._iconPath = cursor.nextString()
          }
          break
        case 23:
          sheetman.checkColumn(column, 'GachaInfo.EnableSkip', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_BOOL])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'GachaInfo.EnableSkip')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._enableSkip = cursor.nextBool()
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: GachaInfoRecord[]): void {
    const recordsById = new Map<number, GachaInfoRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
