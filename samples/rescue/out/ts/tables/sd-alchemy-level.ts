// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/scb-reader'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  name: string
  nameKR: string
  levelUpCost: number
  levelUpTotal: number
  time: string
  useDiaCost: number
  useItemCost: number
  critDMGValue: number
  critDMGTotal: number
  critRateValue: number
  critRateTotal: number
  blockRateValue: number
  blockRateTotal: number
  hITValue: number
  hITTotal: number
}

// Generated from samples/rescue/SD_Contens.xlsx : SDAlchemyLevelTable : A1
export class SDAlchemyLevelRecord {
  /** Default constructor */
  constructor() {
  }

  /** SDAlchemyLevel_Table */
  public get id(): number { return this._id }

  /** 영문명 */
  public get name(): string { return this._name }

  /** 한글명 */
  public get nameKR(): string { return this._nameKR }

  /** 필요 재화값 */
  public get levelUpCost(): number { return this._levelUpCost }

  /** 누적 재화값 */
  public get levelUpTotal(): number { return this._levelUpTotal }

  /** 소요 시간 */
  public get time(): bigint { return this._time }

  /** 필요 다이아 개수 */
  public get useDiaCost(): number { return this._useDiaCost }

  /** 필요 가속권 개수 */
  public get useItemCost(): number { return this._useItemCost }

  /** 크리티컬 데미지 증가값 */
  public get critDMGValue(): number { return this._critDMGValue }

  /** 크리티컬 데미지 누적값 */
  public get critDMGTotal(): number { return this._critDMGTotal }

  /** 크리티컬 확률 증가값 */
  public get critRateValue(): number { return this._critRateValue }

  /** 크리티컬 확률 누적값 */
  public get critRateTotal(): number { return this._critRateTotal }

  /** 블록 확률 증가값 */
  public get blockRateValue(): number { return this._blockRateValue }

  /** 블록 확률 누적값 */
  public get blockRateTotal(): number { return this._blockRateTotal }

  /** 명중률 증가값 */
  public get hITValue(): number { return this._hITValue }

  /** 명중률 누적값 */
  public get hITTotal(): number { return this._hITTotal }

  public _id: number = 0
  public _name: string = ''
  public _nameKR: string = ''
  public _levelUpCost: number = 0
  public _levelUpTotal: number = 0
  public _time: bigint = 0n
  public _useDiaCost: number = 0
  public _useItemCost: number = 0
  public _critDMGValue: number = 0
  public _critDMGTotal: number = 0
  public _critRateValue: number = 0
  public _critRateTotal: number = 0
  public _blockRateValue: number = 0
  public _blockRateTotal: number = 0
  public _hITValue: number = 0
  public _hITTotal: number = 0

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._name = dataRow.name
    this._nameKR = dataRow.nameKR
    this._levelUpCost = dataRow.levelUpCost
    this._levelUpTotal = dataRow.levelUpTotal
    this._time = BigInt(dataRow.time)
    this._useDiaCost = dataRow.useDiaCost
    this._useItemCost = dataRow.useItemCost
    this._critDMGValue = Math.fround(dataRow.critDMGValue)
    this._critDMGTotal = Math.fround(dataRow.critDMGTotal)
    this._critRateValue = Math.fround(dataRow.critRateValue)
    this._critRateTotal = Math.fround(dataRow.critRateTotal)
    this._blockRateValue = Math.fround(dataRow.blockRateValue)
    this._blockRateTotal = Math.fround(dataRow.blockRateTotal)
    this._hITValue = Math.fround(dataRow.hITValue)
    this._hITTotal = Math.fround(dataRow.hITTotal)
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._name = dataRow[offset++]
    this._nameKR = dataRow[offset++]
    this._levelUpCost = dataRow[offset++]
    this._levelUpTotal = dataRow[offset++]
    this._time = BigInt(dataRow[offset++])
    this._useDiaCost = dataRow[offset++]
    this._useItemCost = dataRow[offset++]
    this._critDMGValue = Math.fround(dataRow[offset++])
    this._critDMGTotal = Math.fround(dataRow[offset++])
    this._critRateValue = Math.fround(dataRow[offset++])
    this._critRateTotal = Math.fround(dataRow[offset++])
    this._blockRateValue = Math.fround(dataRow[offset++])
    this._blockRateTotal = Math.fround(dataRow[offset++])
    this._hITValue = Math.fround(dataRow[offset++])
    this._hITTotal = Math.fround(dataRow[offset++])
  }
}

// Generated from samples/rescue/SD_Contens.xlsx : SDAlchemyLevelTable : A1
export class SDAlchemyLevelTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): SDAlchemyLevelRecord[] { return this._records }
  private _records: SDAlchemyLevelRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, SDAlchemyLevelRecord> { return this._recordsById }
  private _recordsById: Map<number, SDAlchemyLevelRecord> = new Map<number, SDAlchemyLevelRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): SDAlchemyLevelRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): SDAlchemyLevelRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "SDAlchemyLevel" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: SDAlchemyLevelRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new SDAlchemyLevelRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new SDAlchemyLevelRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .scb file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.ScbReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)
    let cursor: sheetman.ScbColumnCursor

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: SDAlchemyLevelRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new SDAlchemyLevelRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'SDAlchemyLevel.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDAlchemyLevel.Id')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = cursor.nextI32()
          }
          break
        case 2:
          sheetman.checkColumn(column, 'SDAlchemyLevel.Name', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDAlchemyLevel.Name')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._name = cursor.nextString()
          }
          break
        case 3:
          sheetman.checkColumn(column, 'SDAlchemyLevel.NameKR', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDAlchemyLevel.NameKR')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._nameKR = cursor.nextString()
          }
          break
        case 4:
          sheetman.checkColumn(column, 'SDAlchemyLevel.LevelUpCost', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDAlchemyLevel.LevelUpCost')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._levelUpCost = cursor.nextI32()
          }
          break
        case 5:
          sheetman.checkColumn(column, 'SDAlchemyLevel.LevelUpTotal', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDAlchemyLevel.LevelUpTotal')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._levelUpTotal = cursor.nextI32()
          }
          break
        case 6:
          sheetman.checkColumn(column, 'SDAlchemyLevel.Time', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I64, sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDAlchemyLevel.Time')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._time = cursor.nextI64()
          }
          break
        case 7:
          sheetman.checkColumn(column, 'SDAlchemyLevel.UseDiaCost', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDAlchemyLevel.UseDiaCost')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._useDiaCost = cursor.nextI32()
          }
          break
        case 8:
          sheetman.checkColumn(column, 'SDAlchemyLevel.UseItemCost', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDAlchemyLevel.UseItemCost')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._useItemCost = cursor.nextI32()
          }
          break
        case 9:
          sheetman.checkColumn(column, 'SDAlchemyLevel.CritDMGValue', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDAlchemyLevel.CritDMGValue')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._critDMGValue = cursor.nextF32()
          }
          break
        case 10:
          sheetman.checkColumn(column, 'SDAlchemyLevel.CritDMGTotal', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDAlchemyLevel.CritDMGTotal')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._critDMGTotal = cursor.nextF32()
          }
          break
        case 11:
          sheetman.checkColumn(column, 'SDAlchemyLevel.CritRateValue', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDAlchemyLevel.CritRateValue')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._critRateValue = cursor.nextF32()
          }
          break
        case 12:
          sheetman.checkColumn(column, 'SDAlchemyLevel.CritRateTotal', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDAlchemyLevel.CritRateTotal')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._critRateTotal = cursor.nextF32()
          }
          break
        case 13:
          sheetman.checkColumn(column, 'SDAlchemyLevel.BlockRateValue', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDAlchemyLevel.BlockRateValue')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._blockRateValue = cursor.nextF32()
          }
          break
        case 14:
          sheetman.checkColumn(column, 'SDAlchemyLevel.BlockRateTotal', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDAlchemyLevel.BlockRateTotal')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._blockRateTotal = cursor.nextF32()
          }
          break
        case 15:
          sheetman.checkColumn(column, 'SDAlchemyLevel.HITValue', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDAlchemyLevel.HITValue')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._hITValue = cursor.nextF32()
          }
          break
        case 16:
          sheetman.checkColumn(column, 'SDAlchemyLevel.HITTotal', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDAlchemyLevel.HITTotal')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._hITTotal = cursor.nextF32()
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: SDAlchemyLevelRecord[]): void {
    const recordsById = new Map<number, SDAlchemyLevelRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
