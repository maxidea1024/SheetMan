// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/scb-reader'

// Automatically import to handle external type references.
import { CurrencyType } from '../enums/currency-type'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  name: string
  rewardName: string
  conditionId: number
  rewardCommon: CurrencyType
  commonValue: number
  reward1Type: CurrencyType
  reward1Value: number
  reward2Type: CurrencyType
  reward2Value: number
  reward3Type: CurrencyType
  reward3Value: number
  reward4Type: CurrencyType
  reward4Value: number
  isPerfect: boolean
  rewardPerfect: CurrencyType
  perfectValue: number
  isMinReward: boolean
  rewardMin: CurrencyType
  minRewardValue: number
}

// Generated from samples/rescue/SD_Contens.xlsx : SDDungeonRewardTable : A1
export class SDDungeonRewardRecord {
  /** Default constructor */
  constructor() {
  }

  /** SDDungeonReward_Table */
  public get id(): number { return this._id }

  /** 영문명 */
  public get name(): string { return this._name }

  /** 한글명 */
  public get rewardName(): string { return this._rewardName }

  /** 해제 조건 */
  public get conditionId(): number { return this._conditionId }

  /** 공용 재료 */
  public get rewardCommon(): CurrencyType { return this._rewardCommon }

  /** 공용 재료 값 */
  public get commonValue(): number { return this._commonValue }

  /** 크리티컬 데미지 재료 */
  public get reward1Type(): CurrencyType { return this._reward1Type }

  /** 크리티컬 데미지 재료값 */
  public get reward1Value(): number { return this._reward1Value }

  /** 크리티컬 확률 재료 */
  public get reward2Type(): CurrencyType { return this._reward2Type }

  /** 크리티컬 확률 재료 값 */
  public get reward2Value(): number { return this._reward2Value }

  /** 블록 확률 재료 */
  public get reward3Type(): CurrencyType { return this._reward3Type }

  /** 블록 확률 재료 값 */
  public get reward3Value(): number { return this._reward3Value }

  /** 명중률 재료 */
  public get reward4Type(): CurrencyType { return this._reward4Type }

  /** 명중률 재료 값 */
  public get reward4Value(): number { return this._reward4Value }

  /** 퍼펙트 조건 */
  public get isPerfect(): boolean { return this._isPerfect }

  /** 퍼펙트 보상 */
  public get rewardPerfect(): CurrencyType { return this._rewardPerfect }

  /** 퍼펙트 보상값 */
  public get perfectValue(): number { return this._perfectValue }

  /** 최소 보상 조건 */
  public get isMinReward(): boolean { return this._isMinReward }

  /** 최소 보상 */
  public get rewardMin(): CurrencyType { return this._rewardMin }

  /** 최소 보상값 */
  public get minRewardValue(): number { return this._minRewardValue }

  public _id: number = 0
  public _name: string = ''
  public _rewardName: string = ''
  public _conditionId: number = 0
  public _rewardCommon: CurrencyType = 0 as CurrencyType
  public _commonValue: number = 0
  public _reward1Type: CurrencyType = 0 as CurrencyType
  public _reward1Value: number = 0
  public _reward2Type: CurrencyType = 0 as CurrencyType
  public _reward2Value: number = 0
  public _reward3Type: CurrencyType = 0 as CurrencyType
  public _reward3Value: number = 0
  public _reward4Type: CurrencyType = 0 as CurrencyType
  public _reward4Value: number = 0
  public _isPerfect: boolean = false
  public _rewardPerfect: CurrencyType = 0 as CurrencyType
  public _perfectValue: number = 0
  public _isMinReward: boolean = false
  public _rewardMin: CurrencyType = 0 as CurrencyType
  public _minRewardValue: number = 0

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._name = dataRow.name
    this._rewardName = dataRow.rewardName
    this._conditionId = dataRow.conditionId
    this._rewardCommon = dataRow.rewardCommon
    this._commonValue = dataRow.commonValue
    this._reward1Type = dataRow.reward1Type
    this._reward1Value = dataRow.reward1Value
    this._reward2Type = dataRow.reward2Type
    this._reward2Value = dataRow.reward2Value
    this._reward3Type = dataRow.reward3Type
    this._reward3Value = dataRow.reward3Value
    this._reward4Type = dataRow.reward4Type
    this._reward4Value = dataRow.reward4Value
    this._isPerfect = dataRow.isPerfect
    this._rewardPerfect = dataRow.rewardPerfect
    this._perfectValue = dataRow.perfectValue
    this._isMinReward = dataRow.isMinReward
    this._rewardMin = dataRow.rewardMin
    this._minRewardValue = dataRow.minRewardValue
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._name = dataRow[offset++]
    this._rewardName = dataRow[offset++]
    this._conditionId = dataRow[offset++]
    this._rewardCommon = dataRow[offset++]
    this._commonValue = dataRow[offset++]
    this._reward1Type = dataRow[offset++]
    this._reward1Value = dataRow[offset++]
    this._reward2Type = dataRow[offset++]
    this._reward2Value = dataRow[offset++]
    this._reward3Type = dataRow[offset++]
    this._reward3Value = dataRow[offset++]
    this._reward4Type = dataRow[offset++]
    this._reward4Value = dataRow[offset++]
    this._isPerfect = dataRow[offset++]
    this._rewardPerfect = dataRow[offset++]
    this._perfectValue = dataRow[offset++]
    this._isMinReward = dataRow[offset++]
    this._rewardMin = dataRow[offset++]
    this._minRewardValue = dataRow[offset++]
  }
}

// Generated from samples/rescue/SD_Contens.xlsx : SDDungeonRewardTable : A1
export class SDDungeonRewardTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): SDDungeonRewardRecord[] { return this._records }
  private _records: SDDungeonRewardRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, SDDungeonRewardRecord> { return this._recordsById }
  private _recordsById: Map<number, SDDungeonRewardRecord> = new Map<number, SDDungeonRewardRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): SDDungeonRewardRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): SDDungeonRewardRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "SDDungeonReward" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: SDDungeonRewardRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new SDDungeonRewardRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new SDDungeonRewardRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .scb file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.ScbReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)
    let cursor: sheetman.ScbColumnCursor

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: SDDungeonRewardRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new SDDungeonRewardRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'SDDungeonReward.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDDungeonReward.Id')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = cursor.nextI32()
          }
          break
        case 2:
          sheetman.checkColumn(column, 'SDDungeonReward.Name', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDDungeonReward.Name')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._name = cursor.nextString()
          }
          break
        case 3:
          sheetman.checkColumn(column, 'SDDungeonReward.RewardName', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDDungeonReward.RewardName')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._rewardName = cursor.nextString()
          }
          break
        case 4:
          sheetman.checkColumn(column, 'SDDungeonReward.ConditionId', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDDungeonReward.ConditionId')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._conditionId = cursor.nextI32()
          }
          break
        case 5:
          sheetman.checkColumn(column, 'SDDungeonReward.RewardCommon', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDDungeonReward.RewardCommon')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._rewardCommon = cursor.nextI32() as CurrencyType
          }
          break
        case 6:
          sheetman.checkColumn(column, 'SDDungeonReward.CommonValue', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDDungeonReward.CommonValue')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._commonValue = cursor.nextI32()
          }
          break
        case 7:
          sheetman.checkColumn(column, 'SDDungeonReward.Reward1Type', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDDungeonReward.Reward1Type')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._reward1Type = cursor.nextI32() as CurrencyType
          }
          break
        case 8:
          sheetman.checkColumn(column, 'SDDungeonReward.Reward1Value', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDDungeonReward.Reward1Value')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._reward1Value = cursor.nextI32()
          }
          break
        case 9:
          sheetman.checkColumn(column, 'SDDungeonReward.Reward2Type', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDDungeonReward.Reward2Type')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._reward2Type = cursor.nextI32() as CurrencyType
          }
          break
        case 10:
          sheetman.checkColumn(column, 'SDDungeonReward.Reward2Value', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDDungeonReward.Reward2Value')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._reward2Value = cursor.nextI32()
          }
          break
        case 11:
          sheetman.checkColumn(column, 'SDDungeonReward.Reward3Type', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDDungeonReward.Reward3Type')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._reward3Type = cursor.nextI32() as CurrencyType
          }
          break
        case 12:
          sheetman.checkColumn(column, 'SDDungeonReward.Reward3Value', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDDungeonReward.Reward3Value')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._reward3Value = cursor.nextI32()
          }
          break
        case 13:
          sheetman.checkColumn(column, 'SDDungeonReward.Reward4Type', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDDungeonReward.Reward4Type')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._reward4Type = cursor.nextI32() as CurrencyType
          }
          break
        case 14:
          sheetman.checkColumn(column, 'SDDungeonReward.Reward4Value', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDDungeonReward.Reward4Value')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._reward4Value = cursor.nextI32()
          }
          break
        case 15:
          sheetman.checkColumn(column, 'SDDungeonReward.IsPerfect', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_BOOL])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDDungeonReward.IsPerfect')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._isPerfect = cursor.nextBool()
          }
          break
        case 16:
          sheetman.checkColumn(column, 'SDDungeonReward.RewardPerfect', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDDungeonReward.RewardPerfect')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._rewardPerfect = cursor.nextI32() as CurrencyType
          }
          break
        case 17:
          sheetman.checkColumn(column, 'SDDungeonReward.PerfectValue', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDDungeonReward.PerfectValue')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._perfectValue = cursor.nextI32()
          }
          break
        case 18:
          sheetman.checkColumn(column, 'SDDungeonReward.IsMinReward', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_BOOL])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDDungeonReward.IsMinReward')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._isMinReward = cursor.nextBool()
          }
          break
        case 19:
          sheetman.checkColumn(column, 'SDDungeonReward.RewardMin', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDDungeonReward.RewardMin')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._rewardMin = cursor.nextI32() as CurrencyType
          }
          break
        case 20:
          sheetman.checkColumn(column, 'SDDungeonReward.MinRewardValue', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'SDDungeonReward.MinRewardValue')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._minRewardValue = cursor.nextI32()
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: SDDungeonRewardRecord[]): void {
    const recordsById = new Map<number, SDDungeonRewardRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
