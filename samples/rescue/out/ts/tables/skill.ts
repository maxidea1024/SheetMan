// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/scb-reader'

// Automatically import to handle external type references.
import { SkillType } from '../enums/skill-type'
import { SkillSubType } from '../enums/skill-sub-type'
import { AttributeType } from '../enums/attribute-type'
import { TargetType } from '../enums/target-type'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  name: string
  skillName: string
  skillType: SkillType
  skillSubType: SkillSubType
  attributeType: AttributeType
  targetType: TargetType
  aniPath: string
  skillUseRange: number
  coolTime: number
  buffID: number[]
  skillIcon: string
  description: string
}

// Generated from samples/rescue/MasterData.xlsx : SkillTable : A1
export class SkillRecord {
  /** Default constructor */
  constructor() {
  }

  /** Skill_Table */
  public get id(): number { return this._id }

  /** 영문 */
  public get name(): string { return this._name }

  /** 한글 */
  public get skillName(): string { return this._skillName }

  /** 메모 */
  public get skillType(): SkillType { return this._skillType }

  /** 메모 참조 */
  public get skillSubType(): SkillSubType { return this._skillSubType }

  /** 속성 설정 */
  public get attributeType(): AttributeType { return this._attributeType }

  /** 메모참조 */
  public get targetType(): TargetType { return this._targetType }

  /** 스킬 경로 */
  public get aniPath(): string { return this._aniPath }

  /** 스킬 사용 거리 */
  public get skillUseRange(): number { return this._skillUseRange }

  /** 스킬 쿨타임 */
  public get coolTime(): number { return this._coolTime }

  /** 버프 ID */
  public get buffID(): number[] { return this._buffID }

  /** 필살기 아이콘 경로 */
  public get skillIcon(): string { return this._skillIcon }

  /** 설명 */
  public get description(): string { return this._description }

  public _id: number = 0
  public _name: string = ''
  public _skillName: string = ''
  public _skillType: SkillType = 0 as SkillType
  public _skillSubType: SkillSubType = 0 as SkillSubType
  public _attributeType: AttributeType = 0 as AttributeType
  public _targetType: TargetType = 0 as TargetType
  public _aniPath: string = ''
  public _skillUseRange: number = 0
  public _coolTime: number = 0
  public _buffID: number[] = []
  public _skillIcon: string = ''
  public _description: string = ''

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._name = dataRow.name
    this._skillName = dataRow.skillName
    this._skillType = dataRow.skillType
    this._skillSubType = dataRow.skillSubType
    this._attributeType = dataRow.attributeType
    this._targetType = dataRow.targetType
    this._aniPath = dataRow.aniPath
    this._skillUseRange = Math.fround(dataRow.skillUseRange)
    this._coolTime = Math.fround(dataRow.coolTime)
    this._buffID = dataRow.buffID
    this._skillIcon = dataRow.skillIcon
    this._description = dataRow.description
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._name = dataRow[offset++]
    this._skillName = dataRow[offset++]
    this._skillType = dataRow[offset++]
    this._skillSubType = dataRow[offset++]
    this._attributeType = dataRow[offset++]
    this._targetType = dataRow[offset++]
    this._aniPath = dataRow[offset++]
    this._skillUseRange = Math.fround(dataRow[offset++])
    this._coolTime = Math.fround(dataRow[offset++])
    this._buffID = dataRow[offset++]
    this._skillIcon = dataRow[offset++]
    this._description = dataRow[offset++]
  }
}

// Generated from samples/rescue/MasterData.xlsx : SkillTable : A1
export class SkillTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): SkillRecord[] { return this._records }
  private _records: SkillRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, SkillRecord> { return this._recordsById }
  private _recordsById: Map<number, SkillRecord> = new Map<number, SkillRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): SkillRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): SkillRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "Skill" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: SkillRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new SkillRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new SkillRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .scb file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.ScbReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)
    let cursor: sheetman.ScbColumnCursor

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: SkillRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new SkillRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'Skill.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Skill.Id')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = cursor.nextI32()
          }
          break
        case 2:
          sheetman.checkColumn(column, 'Skill.Name', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Skill.Name')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._name = cursor.nextString()
          }
          break
        case 3:
          sheetman.checkColumn(column, 'Skill.SkillName', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Skill.SkillName')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._skillName = cursor.nextString()
          }
          break
        case 4:
          sheetman.checkColumn(column, 'Skill.SkillType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Skill.SkillType')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._skillType = cursor.nextI32() as SkillType
          }
          break
        case 5:
          sheetman.checkColumn(column, 'Skill.SkillSubType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Skill.SkillSubType')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._skillSubType = cursor.nextI32() as SkillSubType
          }
          break
        case 6:
          sheetman.checkColumn(column, 'Skill.AttributeType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Skill.AttributeType')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._attributeType = cursor.nextI32() as AttributeType
          }
          break
        case 7:
          sheetman.checkColumn(column, 'Skill.TargetType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Skill.TargetType')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._targetType = cursor.nextI32() as TargetType
          }
          break
        case 8:
          sheetman.checkColumn(column, 'Skill.AniPath', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Skill.AniPath')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._aniPath = cursor.nextString()
          }
          break
        case 9:
          sheetman.checkColumn(column, 'Skill.SkillUseRange', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Skill.SkillUseRange')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._skillUseRange = cursor.nextF32()
          }
          break
        case 10:
          sheetman.checkColumn(column, 'Skill.CoolTime', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Skill.CoolTime')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._coolTime = cursor.nextF32()
          }
          break
        case 11:
          sheetman.checkColumn(column, 'Skill.BuffID', sheetman.KIND_VAR_ARRAY, 0, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            const elementCount = reader.readCounter32()
            record._buffID = []
            for (let j = 0; j < elementCount; ++j)
              record._buffID.push(reader.readI32As(column.element))
          }
          break
        case 12:
          sheetman.checkColumn(column, 'Skill.SkillIcon', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Skill.SkillIcon')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._skillIcon = cursor.nextString()
          }
          break
        case 13:
          sheetman.checkColumn(column, 'Skill.Description', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'Skill.Description')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._description = cursor.nextString()
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: SkillRecord[]): void {
    const recordsById = new Map<number, SkillRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
