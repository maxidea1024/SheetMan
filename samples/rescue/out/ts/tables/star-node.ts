// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

import * as fs from 'fs'
import * as sheetman from '../sheetman/scb-reader'

// Automatically import to handle external type references.
import { StatType } from '../enums/stat-type'

/** A type for handling rows when parsing .json. */
interface IDataRow {
  id: number
  name: string
  infuluenceStep: number
  starNodeName: string
  statType: StatType
  starNodeValue: number
  starNodeRate: number
  unlockCondition: number[]
  description: string
  iconPath: string
}

// Generated from samples/rescue/StageGrowth.xlsx : StarNodeTable : A1
export class StarNodeRecord {
  /** Default constructor */
  constructor() {
  }

  /** 기본값 */
  public get id(): number { return this._id }

  /** 영문 */
  public get name(): string { return this._name }

  /** 영향력 단계 */
  public get infuluenceStep(): number { return this._infuluenceStep }

  /** 한글 */
  public get starNodeName(): string { return this._starNodeName }

  /** 스탯 타입 */
  public get statType(): StatType { return this._statType }

  /** 증가 Value */
  public get starNodeValue(): number { return this._starNodeValue }

  /** 증가 Rate */
  public get starNodeRate(): number { return this._starNodeRate }

  /** 해제 조건 */
  public get unlockCondition(): number[] { return this._unlockCondition }

  /** 설명 */
  public get description(): string { return this._description }

  /** 아이콘 */
  public get iconPath(): string { return this._iconPath }

  public _id: number = 0
  public _name: string = ''
  public _infuluenceStep: number = 0
  public _starNodeName: string = ''
  public _statType: StatType = 0 as StatType
  public _starNodeValue: number = 0
  public _starNodeRate: number = 0
  public _unlockCondition: number[] = []
  public _description: string = ''
  public _iconPath: string = ''

  /** Populate field values. */
  public populateFieldValues(dataRow: IDataRow): void {
    this._id = dataRow.id
    this._name = dataRow.name
    this._infuluenceStep = dataRow.infuluenceStep
    this._starNodeName = dataRow.starNodeName
    this._statType = dataRow.statType
    this._starNodeValue = dataRow.starNodeValue
    this._starNodeRate = Math.fround(dataRow.starNodeRate)
    this._unlockCondition = dataRow.unlockCondition
    this._description = dataRow.description
    this._iconPath = dataRow.iconPath
  }

  /** Populate field values. */
  public populateFieldValuesCompact(dataRow: any[]): void {
    let offset = 0
    this._id = dataRow[offset++]
    this._name = dataRow[offset++]
    this._infuluenceStep = dataRow[offset++]
    this._starNodeName = dataRow[offset++]
    this._statType = dataRow[offset++]
    this._starNodeValue = dataRow[offset++]
    this._starNodeRate = Math.fround(dataRow[offset++])
    this._unlockCondition = dataRow[offset++]
    this._description = dataRow[offset++]
    this._iconPath = dataRow[offset++]
  }
}

// Generated from samples/rescue/StageGrowth.xlsx : StarNodeTable : A1
export class StarNodeTable {
  /** Default constructor. */
  constructor() {
  }

  /** All records. */
  public get records(): StarNodeRecord[] { return this._records }
  private _records: StarNodeRecord[] = []

  // Indexing by 'id'
  public get recordsById(): Map<number, StarNodeRecord> { return this._recordsById }
  private _recordsById: Map<number, StarNodeRecord> = new Map<number, StarNodeRecord>()

  /**
   * The row with this id, or undefined when the table has none.
   *
   * The lookup to reach for when a missing row is an ordinary answer.
   */
  public findById(key: number): StarNodeRecord | undefined {
    return this._recordsById.get(key)
  }

  /**
   * The row with this id, or a thrown Error naming what was missing.
   *
   * For a key that has to be there. The name says it throws, because a caller reading
   * `getById(id).name` at a glance cannot otherwise tell whether the
   * next line is a check or a catch.
   */
  public getByIdOrThrow(key: number): StarNodeRecord {
    const found = this._recordsById.get(key)
    if (!found)
      throw new Error(`There is no record in table "StarNode" that corresponds to field "id" value ${key}`)

    return found
  }

  /** Whether the table holds a row with this id. */
  public containsId(key: number): boolean {
    return this._recordsById.has(key)
  }

  /** Read a table from specified file. */
  public async read(filename: string): Promise<void> {
    const json = await fs.promises.readFile(filename, "utf8")
    this.readFromJson(json)
  }

  /** Read a table from specified file synchronously. */
  public readSync(filename: string): void {
    const json = fs.readFileSync(filename, "utf8")
    this.readFromJson(json)
  }

  private readFromJson(json: string): void {
    const dataRows: any[] = JSON.parse(json)
    const records: StarNodeRecord[] = []

    if (this.isCompactRowFormatted(dataRows)) {
      for (const dataRow of dataRows) {
        const record = new StarNodeRecord()
        record.populateFieldValuesCompact(dataRow)
        records.push(record)
      }
    } else {
      for (const dataRow of dataRows as IDataRow[]) {
        const record = new StarNodeRecord()
        record.populateFieldValues(dataRow)
        records.push(record)
      }
    }

    this.publish(records)
  }

  private isCompactRowFormatted(rows: any[]): boolean {
    return rows.length > 0 && Array.isArray(rows[0])
  }

  /** Read a table from a binary .scb file. */
  public readBinarySync(filename: string): void {
    this.readBinaryFrom(sheetman.readAllBytes(filename))
  }

  /**
   * Read a table from binary data already in memory.
   *
   * Column by column, matched by tag rather than position: a column this build does not
   * know is skipped by its block length, and one whose type changed incompatibly fails
   * naming the field.
   */
  public readBinaryFrom(data: Uint8Array): void {
    const reader = new sheetman.ScbReader(data)
    const { rowCount, columns } = sheetman.readTableHeader(reader)
    let cursor: sheetman.ScbColumnCursor

    // Built here and published at the end, so a file that turns out to be truncated - or
    // a column this build cannot read - leaves the rows already loaded exactly as they are.
    const records: StarNodeRecord[] = []
    for (let i = 0; i < rowCount; ++i)
      records.push(new StarNodeRecord())

    for (const column of columns) {
      const blockEnd = reader.position + column.byteLength

      switch (column.tag) {
        case 1:
          sheetman.checkColumn(column, 'StarNode.Id', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'StarNode.Id')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._id = cursor.nextI32()
          }
          break
        case 2:
          sheetman.checkColumn(column, 'StarNode.Name', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'StarNode.Name')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._name = cursor.nextString()
          }
          break
        case 3:
          sheetman.checkColumn(column, 'StarNode.InfuluenceStep', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'StarNode.InfuluenceStep')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._infuluenceStep = cursor.nextI32()
          }
          break
        case 4:
          sheetman.checkColumn(column, 'StarNode.StarNodeName', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'StarNode.StarNodeName')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._starNodeName = cursor.nextString()
          }
          break
        case 5:
          sheetman.checkColumn(column, 'StarNode.StatType', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'StarNode.StatType')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._statType = cursor.nextI32() as StatType
          }
          break
        case 6:
          sheetman.checkColumn(column, 'StarNode.StarNodeValue', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'StarNode.StarNodeValue')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._starNodeValue = cursor.nextI32()
          }
          break
        case 7:
          sheetman.checkColumn(column, 'StarNode.StarNodeRate', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_F32])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'StarNode.StarNodeRate')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._starNodeRate = cursor.nextF32()
          }
          break
        case 8:
          sheetman.checkColumn(column, 'StarNode.UnlockCondition', sheetman.KIND_VAR_ARRAY, 0, [sheetman.ELEMENT_I32, sheetman.ELEMENT_VARINT])
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            const elementCount = reader.readCounter32()
            record._unlockCondition = []
            for (let j = 0; j < elementCount; ++j)
              record._unlockCondition.push(reader.readI32As(column.element))
          }
          break
        case 9:
          sheetman.checkColumn(column, 'StarNode.Description', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'StarNode.Description')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._description = cursor.nextString()
          }
          break
        case 10:
          sheetman.checkColumn(column, 'StarNode.IconPath', sheetman.KIND_SCALAR, 1, [sheetman.ELEMENT_STRING])
          cursor = new sheetman.ScbColumnCursor(reader, column, rowCount, 'StarNode.IconPath')
          for (let i = 0; i < rowCount; ++i) {
            const record = records[i]
            record._iconPath = cursor.nextString()
          }
          break
        default:
          // A column added after this code was generated.
          reader.skip(column.byteLength)
          break
      }

      sheetman.checkBlockEnd(reader, column, blockEnd)
    }

    this.publish(records)
  }

  /** Index mapping. */
  /**
   * Publishes one whole load: the rows and the lookups built from them, together.
   *
   * Reading a table that is already loaded - a refresh, a patched file - used to mutate what
   * consumers were holding: the rows were appended to or emptied first, so a read that threw
   * partway left the table holding some of the new data and none of the old. Everything above
   * builds its own arrays and gets here only if it finished, and this replaces the references
   * in one step. Whoever took `records` before still has the previous load, whole.
   */
  private publish(records: StarNodeRecord[]): void {
    const recordsById = new Map<number, StarNodeRecord>()

    for (const record of records) {
      recordsById.set(record.id, record)
    }

    this._records = records
    this._recordsById = recordsById
  }
}
