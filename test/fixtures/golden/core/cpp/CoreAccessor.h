// ------------------------------------------------------------------------------
// <auto-generated>
//     THIS CODE WAS GENERATED BY SheetMan.
//
//     CHANGES TO THIS FILE MAY CAUSE INCORRECT BEHAVIOR AND WILL BE LOST IF
//     THE CODE IS REGENERATED.
// </auto-generated>
// ------------------------------------------------------------------------------

#ifndef SHEETMAN_GENERATED_COREACCESSOR_H
#define SHEETMAN_GENERATED_COREACCESSOR_H

#include <cstdint>
#include <string>
#include <unordered_map>
#include <vector>

#include "sheetman/lite_binary_reader.h"

namespace sheetman_fixtures {
namespace core {

// Generated from test/fixtures/xlsx/core\core.xlsx : Enums : B2
/// Value types used by the test tables.
enum class ValueType : std::int32_t
{
    /// no value
    None = 0,
    /// 32 bit integer
    Int32 = 1,
    /// 64 bit integer
    Int64 = 2,
    /// single precision float
    Float = 3,
};

// Generated from test/fixtures/xlsx/core\core.xlsx : Enums : G2
/// Item grade. Deliberately omits a zero entry.
enum class Grade : std::int32_t
{
    /// None (automatically inserted by SheetMan)
    None = 0,
    /// common grade
    Common = 1,
    /// rare grade
    Rare = 2,
    /// epic grade
    Epic = 3,
};

// Generated from test/fixtures/xlsx/core\core.xlsx : Enums : L2
/// Declared in snake_case on purpose.
enum class SkillType : std::int32_t
{
    /// no skill
    None = 0,
    /// throws a fireball
    FireBall = 1,
    /// throws an ice shard
    IceShard = 2,
};

// Generated from test/fixtures/xlsx/core\core.xlsx : Consts : B2
/// Assorted tuning constants.
struct GameConfig
{
    /// level cap
    static inline const std::int32_t MaxLevel = 100;
    /// gold granted to new accounts
    static inline const std::int64_t StartGold = 1000LL;
    /// base drop rate
    static inline const float DropRate = 0.25f;
    /// whether debug hooks are active
    static inline const bool DebugMode = false;
    /// grade assigned when unspecified
    static inline const Grade DefaultGrade = Grade::Rare;
    /// when the season opens
    static inline const sheetman::DateTime SeasonStart = sheetman::DateTime{ 637817220000000000LL };
    /// length of one round
    static inline const sheetman::TimeSpan RoundLength = sheetman::TimeSpan{ 3000000000LL };
    /// identifies this data build
    static inline const sheetman::Uuid BuildId = sheetman::Uuid{ { 0xff, 0x19, 0x96, 0x6f, 0x86, 0x8b, 0x11, 0xd0, 0xb4, 0x2d, 0x00, 0xc0, 0x4f, 0xc9, 0x64, 0xff } };
};

// Forward declarations, so records may reference each other in any order.
struct TestFieldTypesRecord;
struct ItemCategoryRecord;
struct ItemRecord;
struct LocalizationRecord;
struct ArrayTypesRecord;
struct ServerTuningRecord;
struct ClientStringsRecord;

// Generated from test/fixtures/xlsx/core\core.xlsx : Types : B2
/// One column per supported primitive type.
struct TestFieldTypesRecord
{
    /// primary index
    std::int32_t index = 0;
    /// utf8 text
    std::string string_field;
    /// logical flag
    bool bool_field = false;
    /// 32 bit integer
    std::int32_t int_field = 0;
    /// 64 bit integer
    std::int64_t big_int_field = 0;
    /// single precision
    float float_field = 0.0f;
    /// double precision
    double double_field = 0.0;
    /// date and time
    sheetman::DateTime datetime_field;
    /// time interval
    sheetman::TimeSpan timespan_field;
    /// globally unique id
    sheetman::Uuid uuid_field;
    /// enum reference
    ValueType value_type_field = static_cast<ValueType>(0);

    /// Reads one record. Field order must match the exporter's.
    void read(sheetman::LiteBinaryReader& reader)
    {
        reader.read(index);
        reader.read(string_field);
        reader.read(bool_field);
        reader.read(int_field);
        reader.read(big_int_field);
        reader.read(float_field);
        reader.read(double_field);
        reader.read(datetime_field);
        reader.read(timespan_field);
        reader.read(uuid_field);
        reader.read_enum(value_type_field);
    }
};

/// One column per supported primitive type.
class TestFieldTypesTable
{
    public:
    const std::vector<TestFieldTypesRecord>& records() const { return records_; }

    /// Record with the given primary index, or nullptr when there is none.
    const TestFieldTypesRecord* find(std::int32_t index) const
    {
        const auto it = by_index_.find(index);
        return it == by_index_.end() ? nullptr : &records_[it->second];
    }

    /// Loads the table from a .table file written by SheetMan.
    void read(const std::string& filename)
    {
        const std::vector<std::uint8_t> buffer = sheetman::read_all_bytes(filename);
        sheetman::LiteBinaryReader reader(buffer);

        const std::int32_t row_count = sheetman::read_table_header(reader);

        records_.clear();
        records_.resize(static_cast<std::size_t>(row_count));
        for (std::int32_t i = 0; i < row_count; ++i)
        {
            records_[static_cast<std::size_t>(i)].read(reader);
        }

        build_index();
    }

    private:
    friend class Tables;

    void build_index()
    {
        by_index_.clear();
        by_index_.reserve(records_.size());
        for (std::size_t i = 0; i < records_.size(); ++i)
        {
            by_index_.emplace(records_[i].index, i);
        }
    }

    std::vector<TestFieldTypesRecord> records_;
    std::unordered_map<std::int32_t, std::size_t> by_index_;
};

// Generated from test/fixtures/xlsx/core\core.xlsx : Refs : B2
/// Referenced by Item.CategoryId.
struct ItemCategoryRecord
{
    /// primary index
    std::int32_t index = 0;
    /// category name
    std::string name;
    /// human readable description
    std::string description;

    /// Reads one record. Field order must match the exporter's.
    void read(sheetman::LiteBinaryReader& reader)
    {
        reader.read(index);
        reader.read(name);
        reader.read(description);
    }
};

/// Referenced by Item.CategoryId.
class ItemCategoryTable
{
    public:
    const std::vector<ItemCategoryRecord>& records() const { return records_; }

    /// Record with the given primary index, or nullptr when there is none.
    const ItemCategoryRecord* find(std::int32_t index) const
    {
        const auto it = by_index_.find(index);
        return it == by_index_.end() ? nullptr : &records_[it->second];
    }

    /// Loads the table from a .table file written by SheetMan.
    void read(const std::string& filename)
    {
        const std::vector<std::uint8_t> buffer = sheetman::read_all_bytes(filename);
        sheetman::LiteBinaryReader reader(buffer);

        const std::int32_t row_count = sheetman::read_table_header(reader);

        records_.clear();
        records_.resize(static_cast<std::size_t>(row_count));
        for (std::int32_t i = 0; i < row_count; ++i)
        {
            records_[static_cast<std::size_t>(i)].read(reader);
        }

        build_index();
    }

    private:
    friend class Tables;

    void build_index()
    {
        by_index_.clear();
        by_index_.reserve(records_.size());
        for (std::size_t i = 0; i < records_.size(); ++i)
        {
            by_index_.emplace(records_[i].index, i);
        }
    }

    std::vector<ItemCategoryRecord> records_;
    std::unordered_map<std::int32_t, std::size_t> by_index_;
};

// Generated from test/fixtures/xlsx/core\core.xlsx : Refs : I2
/// References ItemCategory by record.
struct ItemRecord
{
    /// primary index
    std::int32_t index = 0;
    /// item name
    std::string name;
    /// owning category
    const ItemCategoryRecord* category_id = nullptr;
    std::int32_t category_id_index = 0;
    /// item grade
    Grade grade_field = static_cast<Grade>(0);
    /// granted skill
    SkillType skill_field = static_cast<SkillType>(0);
    /// shop blurb
    std::string description;
    /// shop price
    std::int32_t price = 0;

    /// Reads one record. Field order must match the exporter's.
    void read(sheetman::LiteBinaryReader& reader)
    {
        reader.read(index);
        reader.read(name);
        reader.read(category_id_index);
        reader.read_enum(grade_field);
        reader.read_enum(skill_field);
        reader.read(description);
        reader.read(price);
    }
};

/// References ItemCategory by record.
class ItemTable
{
    public:
    const std::vector<ItemRecord>& records() const { return records_; }

    /// Record with the given primary index, or nullptr when there is none.
    const ItemRecord* find(std::int32_t index) const
    {
        const auto it = by_index_.find(index);
        return it == by_index_.end() ? nullptr : &records_[it->second];
    }

    /// Loads the table from a .table file written by SheetMan.
    void read(const std::string& filename)
    {
        const std::vector<std::uint8_t> buffer = sheetman::read_all_bytes(filename);
        sheetman::LiteBinaryReader reader(buffer);

        const std::int32_t row_count = sheetman::read_table_header(reader);

        records_.clear();
        records_.resize(static_cast<std::size_t>(row_count));
        for (std::int32_t i = 0; i < row_count; ++i)
        {
            records_[static_cast<std::size_t>(i)].read(reader);
        }

        build_index();
    }

    private:
    friend class Tables;

    void build_index()
    {
        by_index_.clear();
        by_index_.reserve(records_.size());
        for (std::size_t i = 0; i < records_.size(); ++i)
        {
            by_index_.emplace(records_[i].index, i);
        }
    }

    std::vector<ItemRecord> records_;
    std::unordered_map<std::int32_t, std::size_t> by_index_;
};

// Generated from test/fixtures/xlsx/core\core.xlsx : Serial : B2
/// Trailing-number columns collapse into arrays.
struct LocalizationRecord
{
    /// primary index
    std::int32_t index = 0;
    /// lookup key
    std::string key;
    /// english text 1
    std::vector<std::string> text_en_array;
    /// korean text 1
    std::vector<std::string> text_ko_array;

    /// Reads one record. Field order must match the exporter's.
    void read(sheetman::LiteBinaryReader& reader)
    {
        reader.read(index);
        reader.read(key);
        text_en_array.resize(2);
        for (std::size_t i = 0; i < 2; ++i)
        {
            reader.read(text_en_array[i]);
        }
        text_ko_array.resize(2);
        for (std::size_t i = 0; i < 2; ++i)
        {
            reader.read(text_ko_array[i]);
        }
    }
};

/// Trailing-number columns collapse into arrays.
class LocalizationTable
{
    public:
    const std::vector<LocalizationRecord>& records() const { return records_; }

    /// Record with the given primary index, or nullptr when there is none.
    const LocalizationRecord* find(std::int32_t index) const
    {
        const auto it = by_index_.find(index);
        return it == by_index_.end() ? nullptr : &records_[it->second];
    }

    /// Loads the table from a .table file written by SheetMan.
    void read(const std::string& filename)
    {
        const std::vector<std::uint8_t> buffer = sheetman::read_all_bytes(filename);
        sheetman::LiteBinaryReader reader(buffer);

        const std::int32_t row_count = sheetman::read_table_header(reader);

        records_.clear();
        records_.resize(static_cast<std::size_t>(row_count));
        for (std::int32_t i = 0; i < row_count; ++i)
        {
            records_[static_cast<std::size_t>(i)].read(reader);
        }

        build_index();
    }

    private:
    friend class Tables;

    void build_index()
    {
        by_index_.clear();
        by_index_.reserve(records_.size());
        for (std::size_t i = 0; i < records_.size(); ++i)
        {
            by_index_.emplace(records_[i].index, i);
        }
    }

    std::vector<LocalizationRecord> records_;
    std::unordered_map<std::int32_t, std::size_t> by_index_;
};

// Generated from test/fixtures/xlsx/core\core.xlsx : Arrays : B2
/// One cell holding several delimited values, length varying per row.
struct ArrayTypesRecord
{
    /// primary index
    std::int32_t index = 0;
    /// free-form tags
    std::vector<std::string> tags;
    /// cost per level
    std::vector<std::int32_t> costs;
    /// drop weights
    std::vector<float> weights;
    /// allowed grades
    std::vector<Grade> grades;
    /// fixed slot 1
    std::vector<std::int32_t> slot_array;

    /// Reads one record. Field order must match the exporter's.
    void read(sheetman::LiteBinaryReader& reader)
    {
        reader.read(index);
        {
            const std::int32_t count = reader.read_counter32();
            tags.resize(static_cast<std::size_t>(count));
            for (std::int32_t i = 0; i < count; ++i)
            {
                reader.read(tags[static_cast<std::size_t>(i)]);
            }
        }
        {
            const std::int32_t count = reader.read_counter32();
            costs.resize(static_cast<std::size_t>(count));
            for (std::int32_t i = 0; i < count; ++i)
            {
                reader.read(costs[static_cast<std::size_t>(i)]);
            }
        }
        {
            const std::int32_t count = reader.read_counter32();
            weights.resize(static_cast<std::size_t>(count));
            for (std::int32_t i = 0; i < count; ++i)
            {
                reader.read(weights[static_cast<std::size_t>(i)]);
            }
        }
        {
            const std::int32_t count = reader.read_counter32();
            grades.resize(static_cast<std::size_t>(count));
            for (std::int32_t i = 0; i < count; ++i)
            {
                reader.read_enum(grades[static_cast<std::size_t>(i)]);
            }
        }
        slot_array.resize(2);
        for (std::size_t i = 0; i < 2; ++i)
        {
            reader.read(slot_array[i]);
        }
    }
};

/// One cell holding several delimited values, length varying per row.
class ArrayTypesTable
{
    public:
    const std::vector<ArrayTypesRecord>& records() const { return records_; }

    /// Record with the given primary index, or nullptr when there is none.
    const ArrayTypesRecord* find(std::int32_t index) const
    {
        const auto it = by_index_.find(index);
        return it == by_index_.end() ? nullptr : &records_[it->second];
    }

    /// Loads the table from a .table file written by SheetMan.
    void read(const std::string& filename)
    {
        const std::vector<std::uint8_t> buffer = sheetman::read_all_bytes(filename);
        sheetman::LiteBinaryReader reader(buffer);

        const std::int32_t row_count = sheetman::read_table_header(reader);

        records_.clear();
        records_.resize(static_cast<std::size_t>(row_count));
        for (std::int32_t i = 0; i < row_count; ++i)
        {
            records_[static_cast<std::size_t>(i)].read(reader);
        }

        build_index();
    }

    private:
    friend class Tables;

    void build_index()
    {
        by_index_.clear();
        by_index_.reserve(records_.size());
        for (std::size_t i = 0; i < records_.size(); ++i)
        {
            by_index_.emplace(records_[i].index, i);
        }
    }

    std::vector<ArrayTypesRecord> records_;
    std::unordered_map<std::int32_t, std::size_t> by_index_;
};

// Generated from test/fixtures/xlsx/core\core.xlsx : Sides : B2
/// Server-only table. Must not appear in client output.
struct ServerTuningRecord
{
    /// primary index
    std::int32_t index = 0;
    /// tuning key
    std::string key;
    /// tuning amount
    std::int32_t amount = 0;

    /// Reads one record. Field order must match the exporter's.
    void read(sheetman::LiteBinaryReader& reader)
    {
        reader.read(index);
        reader.read(key);
        reader.read(amount);
    }
};

/// Server-only table. Must not appear in client output.
class ServerTuningTable
{
    public:
    const std::vector<ServerTuningRecord>& records() const { return records_; }

    /// Record with the given primary index, or nullptr when there is none.
    const ServerTuningRecord* find(std::int32_t index) const
    {
        const auto it = by_index_.find(index);
        return it == by_index_.end() ? nullptr : &records_[it->second];
    }

    /// Loads the table from a .table file written by SheetMan.
    void read(const std::string& filename)
    {
        const std::vector<std::uint8_t> buffer = sheetman::read_all_bytes(filename);
        sheetman::LiteBinaryReader reader(buffer);

        const std::int32_t row_count = sheetman::read_table_header(reader);

        records_.clear();
        records_.resize(static_cast<std::size_t>(row_count));
        for (std::int32_t i = 0; i < row_count; ++i)
        {
            records_[static_cast<std::size_t>(i)].read(reader);
        }

        build_index();
    }

    private:
    friend class Tables;

    void build_index()
    {
        by_index_.clear();
        by_index_.reserve(records_.size());
        for (std::size_t i = 0; i < records_.size(); ++i)
        {
            by_index_.emplace(records_[i].index, i);
        }
    }

    std::vector<ServerTuningRecord> records_;
    std::unordered_map<std::int32_t, std::size_t> by_index_;
};

// Generated from test/fixtures/xlsx/core\core.xlsx : Sides : G2
/// Client-only table. Must not appear in server output.
struct ClientStringsRecord
{
    /// primary index
    std::int32_t index = 0;
    /// string key
    std::string key;
    /// display text
    std::string text;

    /// Reads one record. Field order must match the exporter's.
    void read(sheetman::LiteBinaryReader& reader)
    {
        reader.read(index);
        reader.read(key);
        reader.read(text);
    }
};

/// Client-only table. Must not appear in server output.
class ClientStringsTable
{
    public:
    const std::vector<ClientStringsRecord>& records() const { return records_; }

    /// Record with the given primary index, or nullptr when there is none.
    const ClientStringsRecord* find(std::int32_t index) const
    {
        const auto it = by_index_.find(index);
        return it == by_index_.end() ? nullptr : &records_[it->second];
    }

    /// Loads the table from a .table file written by SheetMan.
    void read(const std::string& filename)
    {
        const std::vector<std::uint8_t> buffer = sheetman::read_all_bytes(filename);
        sheetman::LiteBinaryReader reader(buffer);

        const std::int32_t row_count = sheetman::read_table_header(reader);

        records_.clear();
        records_.resize(static_cast<std::size_t>(row_count));
        for (std::int32_t i = 0; i < row_count; ++i)
        {
            records_[static_cast<std::size_t>(i)].read(reader);
        }

        build_index();
    }

    private:
    friend class Tables;

    void build_index()
    {
        by_index_.clear();
        by_index_.reserve(records_.size());
        for (std::size_t i = 0; i < records_.size(); ++i)
        {
            by_index_.emplace(records_[i].index, i);
        }
    }

    std::vector<ClientStringsRecord> records_;
    std::unordered_map<std::int32_t, std::size_t> by_index_;
};

/// Every table, loaded together so cross-table references can be resolved.
class Tables
{
    public:
    const TestFieldTypesTable& test_field_types() const { return test_field_types_; }
    const ItemCategoryTable& item_category() const { return item_category_; }
    const ItemTable& item() const { return item_; }
    const LocalizationTable& localization() const { return localization_; }
    const ArrayTypesTable& array_types() const { return array_types_; }
    const ServerTuningTable& server_tuning() const { return server_tuning_; }
    const ClientStringsTable& client_strings() const { return client_strings_; }

    /// Reads every table from `base_path`, then links the references between them.
    void read_all(const std::string& base_path, const std::string& file_extension = ".table")
    {
        test_field_types_.read(base_path + "/TestFieldTypes" + file_extension);
        item_category_.read(base_path + "/ItemCategory" + file_extension);
        item_.read(base_path + "/Item" + file_extension);
        localization_.read(base_path + "/Localization" + file_extension);
        array_types_.read(base_path + "/ArrayTypes" + file_extension);
        server_tuning_.read(base_path + "/ServerTuning" + file_extension);
        client_strings_.read(base_path + "/ClientStrings" + file_extension);

        solve_cross_references();
    }

    private:
    void solve_cross_references()
    {
        for (auto& record : item_.records_)
        {
            {
                const auto* target = item_category_.find(record.category_id_index);
                if (target != nullptr) record.category_id = target;
            }
        }
    }

    TestFieldTypesTable test_field_types_;
    ItemCategoryTable item_category_;
    ItemTable item_;
    LocalizationTable localization_;
    ArrayTypesTable array_types_;
    ServerTuningTable server_tuning_;
    ClientStringsTable client_strings_;
};

}  // namespace core
}  // namespace sheetman_fixtures

#endif  // SHEETMAN_GENERATED_COREACCESSOR_H
