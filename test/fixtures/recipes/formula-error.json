{
  // Regression recipe for the `core.xlsx` fixture.
  //
  // Every path is relative to the repository root, which is where the test
  // harness invokes SheetMan from. Output lands in test/fixtures/output/formula-error
  // (transient, gitignored) and is compared against test/fixtures/golden/core.
  //
  // Both JSON row formats are exported because they take different code paths
  // in JsonExporter and only the compact one is currently correct.

  "Sources": {
    "Xlsx": [
      {
        "Path": "test/fixtures/xlsx/formula-error",
        "FileExtensionPatterns": ".xlsx"
      }
    ],
    "GoogleSheets": []
  },

  "Exports": {
    "Binary": [
      {
        "Path": "test/fixtures/output/formula-error/binary",
        "FileExtension": ".table"
      }
    ],
    "Json": [
      {
        "Path": "test/fixtures/output/formula-error/json-compact",
        "UseCompactRowFormat": true,
        "Indented": true
      },
      {
        "Path": "test/fixtures/output/formula-error/json-named",
        "UseCompactRowFormat": false,
        "Indented": true
      }
    ],
    "MongoDb": [],
    "MySql": []
  },

  "CodeGenerations": {
    "Cpp": [
      {
        "Path": "test/fixtures/output/formula-error/cpp",
        "Namespace": "sheetman_fixtures::core",
        "AccessorName": "XAccessor",
        "BinaryTableFileExtension": ".table"
      }
    ],
    "CSharp": [
      {
        "Path": "test/fixtures/output/formula-error/csharp",
        "Namespace": "SheetMan.Fixtures.X",
        "AccessorName": "XAccessor"
      }
    ],
    "Typescript": [
      {
        "Path": "test/fixtures/output/formula-error/typescript"
      }
    ],
    "Html": [
      {
        "Path": "test/fixtures/output/formula-error/html"
      }
    ]
  }
}
