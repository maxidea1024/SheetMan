{
  // Regression recipe for the `core.xlsx` fixture.
  //
  // Every path is relative to the repository root, which is where the test
  // harness invokes SheetMan from. Output lands in test/fixtures/output/layout-edge
  // (transient, gitignored) and is compared against test/fixtures/golden/core.
  //
  // Both JSON row formats are exported because they take different code paths
  // in JsonExporter and only the compact one is currently correct.

  "Sources": {
    "Xlsx": [
      {
        "Path": "test/fixtures/xlsx/layout-edge",
        "FileExtensionPatterns": ".xlsx"
      }
    ],
    "GoogleSheets": []
  },

  "Exports": {
    "Binary": [
      {
        "Path": "test/fixtures/output/layout-edge/binary",
        "FileExtension": ".table"
      }
    ],
    "Json": [
      {
        "Path": "test/fixtures/output/layout-edge/json-compact",
        "UseCompactRowFormat": true,
        "Indented": true
      },
      {
        "Path": "test/fixtures/output/layout-edge/json-named",
        "UseCompactRowFormat": false,
        "Indented": true
      }
    ],
    "MongoDb": [],
    "MySql": []
  },

  "CodeGenerations": {
    "Cpp": [],
    "CSharp": [
      {
        "Path": "test/fixtures/output/layout-edge/csharp",
        "Namespace": "SheetMan.Fixtures.X",
        "AccessorName": "XAccessor"
      }
    ],
    "Typescript": [
      {
        "Path": "test/fixtures/output/layout-edge/typescript"
      }
    ],
    "Html": [
      {
        "Path": "test/fixtures/output/layout-edge/html"
      }
    ]
  }
}
