<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Builds the harness against a scenario's generated C#. Nothing is referenced beyond
    the generated files themselves, which is the point: the output is self-contained.
  -->

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>disable</Nullable>
    <ImplicitUsings>disable</ImplicitUsings>
    <AssemblyName>conformance-csharp</AssemblyName>
    <RootNamespace>SheetMan.Conformance.Harness</RootNamespace>
    <InvariantGlobalization>true</InvariantGlobalization>

    <!-- Selects the non-Unity branches of the generated code, as a non-Unity consumer would. -->
    <DefineConstants>$(DefineConstants);NO_UNITY</DefineConstants>
  </PropertyGroup>

  <ItemGroup>
    <Compile Include="$(GeneratedDir)\**\*.cs" />
  </ItemGroup>

</Project>
